<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet type="text/xsl" href="../assets/xml/rss.xsl" media="all"?><rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/"><channel><title>绿萝间 (Swift)</title><link>http://muxuezi.github.io/</link><description></description><atom:link href="http://muxuezi.github.io/categories/swift.xml" type="application/rss+xml" rel="self"></atom:link><language>en</language><lastBuildDate>Thu, 23 Jul 2015 01:42:08 GMT</lastBuildDate><generator>http://getnikola.com/</generator><docs>http://blogs.law.harvard.edu/tech/rss</docs><item><title>Swift CHS</title><link>http://muxuezi.github.io/posts/swift-chs.html</link><dc:creator>tj2</dc:creator><description>&lt;div&gt;&lt;!--
.. title: Swift CHS
.. slug: swift-chs
.. date: 2014-08-18 14:20:46 UTC+08:00
.. tags: Swift
.. link:
.. description:
.. type: text
--&gt;

&lt;p&gt;Try highlight.js in &lt;a href="http://numbbbbb.gitbooks.io/-the-swift-programming-language-/"&gt;Swift CHS&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href="http://muxuezi.github.io/posts/swift-chs.html"&gt;Read more…&lt;/a&gt; (140 min remaining to read)&lt;/p&gt;&lt;/div&gt;</description><category>Swift</category><guid>http://muxuezi.github.io/posts/swift-chs.html</guid><pubDate>Wed, 24 Jun 2015 05:44:10 GMT</pubDate></item><item><title>swift-codeblocks-syntax-highlight</title><link>http://muxuezi.github.io/posts/swift-codeblocks-syntax-highlight.html</link><dc:creator>tj2</dc:creator><description>&lt;div class="cell border-box-sizing text_cell rendered"&gt;
&lt;div class="prompt input_prompt"&gt;
&lt;/div&gt;
&lt;div class="inner_cell"&gt;
&lt;div class="text_cell_render border-box-sizing rendered_html"&gt;
&lt;p&gt;Everthing is Ok now, thanks &lt;a href="http://www.samypesse.fr/"&gt;Samy Pessé&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Lastest version at &lt;a href="http://numbbbbb.gitbooks.io/-the-swift-programming-language-/"&gt;The Swift Programming Language 中文版&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;&lt;del&gt;由于&lt;a href="http://numbbbbb.gitbooks.io/-the-swift-programming-language-/"&gt;The Swift Programming Language 中文版&lt;/a&gt;不能swift语言高亮.&lt;/del&gt;&lt;/p&gt;
&lt;p&gt;Enjoy：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;p&gt;&lt;a href="http://swift.coding.io/"&gt;Swift Codeblocks Syntax Highlight&lt;/a&gt; on &lt;a href="https://coding.net/u/tj2/p/swift/git"&gt;Coding&lt;/a&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p&gt;&lt;a href="http://muxuezi.gitbooks.io/swift-codeblocks/"&gt;Swift Codeblocks Syntax Highlight&lt;/a&gt; on &lt;a href="https://github.com/muxuezi/swift-codeblocks/"&gt;Gitbhub&lt;/a&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;a href="http://muxuezi.github.io/posts/swift-codeblocks-syntax-highlight.html"&gt;Read more…&lt;/a&gt; (1 min remaining to read)&lt;/p&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;</description><category>ipython</category><category>Swift</category><guid>http://muxuezi.github.io/posts/swift-codeblocks-syntax-highlight.html</guid><pubDate>Wed, 24 Jun 2015 05:44:10 GMT</pubDate></item><item><title>test-swift-code-blocks</title><link>http://muxuezi.github.io/posts/test-swift-code-blocks.html</link><dc:creator>tj2</dc:creator><description>&lt;div&gt;&lt;div class="cell border-box-sizing text_cell rendered"&gt;
&lt;div class="prompt input_prompt"&gt;
&lt;/div&gt;
&lt;div class="inner_cell"&gt;
&lt;div class="text_cell_render border-box-sizing rendered_html"&gt;
&lt;p&gt;&lt;link rel="stylesheet" href="http://muxuezi.github.io/assets/css/xcode.css"&gt;&lt;/p&gt;
&lt;script src="http://muxuezi.github.io/assets/js/highlight.pack.js"&gt;&lt;/script&gt;
&lt;script&gt;hljs.initHighlightingOnLoad();&lt;/script&gt;

&lt;pre&gt;&lt;code class="language-swift"&gt;&lt;span class="class"&gt;&lt;span class="keyword"&gt;class&lt;/span&gt; &lt;span class="title"&gt;DataImporter&lt;/span&gt; {&lt;/span&gt;
    /*
    DataImporter &lt;span class="keyword"&gt;is&lt;/span&gt; a &lt;span class="class"&gt;&lt;span class="keyword"&gt;class&lt;/span&gt; &lt;span class="title"&gt;to&lt;/span&gt; &lt;span class="title"&gt;import&lt;/span&gt; &lt;span class="title"&gt;data&lt;/span&gt; &lt;span class="title"&gt;from&lt;/span&gt; &lt;span class="title"&gt;an&lt;/span&gt; &lt;span class="title"&gt;external&lt;/span&gt; &lt;span class="title"&gt;file&lt;/span&gt;.&lt;/span&gt;
    The &lt;span class="class"&gt;&lt;span class="keyword"&gt;class&lt;/span&gt; &lt;span class="title"&gt;is&lt;/span&gt; &lt;span class="title"&gt;assumed&lt;/span&gt; &lt;span class="title"&gt;to&lt;/span&gt; &lt;span class="title"&gt;take&lt;/span&gt; &lt;span class="title"&gt;a&lt;/span&gt; &lt;span class="title"&gt;non&lt;/span&gt;-&lt;span class="title"&gt;trivial&lt;/span&gt; &lt;span class="title"&gt;amount&lt;/span&gt; &lt;span class="title"&gt;of&lt;/span&gt; &lt;span class="title"&gt;time&lt;/span&gt; &lt;span class="title"&gt;to&lt;/span&gt; &lt;span class="title"&gt;initialize&lt;/span&gt;.&lt;/span&gt;
    */
    &lt;span class="reserved"&gt;var&lt;/span&gt; fileName = &lt;span class="string"&gt;"data.txt"&lt;/span&gt;
    &lt;span class="regexp"&gt;//&lt;/span&gt; the DataImporter &lt;span class="class"&gt;&lt;span class="keyword"&gt;class&lt;/span&gt; &lt;span class="title"&gt;would&lt;/span&gt; &lt;span class="title"&gt;provide&lt;/span&gt; &lt;span class="title"&gt;data&lt;/span&gt; &lt;span class="title"&gt;importing&lt;/span&gt; &lt;span class="title"&gt;functionality&lt;/span&gt; &lt;span class="title"&gt;here&lt;/span&gt;&lt;/span&gt;
}

&lt;span class="class"&gt;&lt;span class="keyword"&gt;class&lt;/span&gt; &lt;span class="title"&gt;DataManager&lt;/span&gt; {&lt;/span&gt;
    lazy &lt;span class="reserved"&gt;var&lt;/span&gt; importer = DataImporter()
    &lt;span class="reserved"&gt;var&lt;/span&gt; data = [String]()
    &lt;span class="regexp"&gt;//&lt;/span&gt; the DataManager &lt;span class="class"&gt;&lt;span class="keyword"&gt;class&lt;/span&gt; &lt;span class="title"&gt;would&lt;/span&gt; &lt;span class="title"&gt;provide&lt;/span&gt; &lt;span class="title"&gt;data&lt;/span&gt; &lt;span class="title"&gt;management&lt;/span&gt; &lt;span class="title"&gt;functionality&lt;/span&gt; &lt;span class="title"&gt;here&lt;/span&gt;&lt;/span&gt;
}

&lt;span class="reserved"&gt;let&lt;/span&gt; manager = DataManager()
manager.data.append(&lt;span class="string"&gt;"Some data"&lt;/span&gt;)
manager.data.append(&lt;span class="string"&gt;"Some more data"&lt;/span&gt;)
&lt;span class="regexp"&gt;//&lt;/span&gt; the DataImporter instance &lt;span class="keyword"&gt;for&lt;/span&gt; the importer property has &lt;span class="keyword"&gt;not&lt;/span&gt; yet been created
&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;div class="cell border-box-sizing code_cell rendered"&gt;
&lt;div class="input"&gt;
&lt;div class="prompt input_prompt"&gt;In [ ]:&lt;/div&gt;
&lt;div class="inner_cell"&gt;
&lt;div class="input_area"&gt;
&lt;div class="highlight"&gt;&lt;pre&gt; 
&lt;/pre&gt;&lt;/div&gt;

&lt;i class="icon-hand-up icon-large" style="float:right; margin-bottom:8px; margin-right:10px"&gt;
  hide output&lt;/i&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;

&lt;/div&gt;&lt;/div&gt;</description><category>Swift</category><guid>http://muxuezi.github.io/posts/test-swift-code-blocks.html</guid><pubDate>Wed, 24 Jun 2015 05:43:14 GMT</pubDate></item><item><title>The Swift Programming Language EN</title><link>http://muxuezi.github.io/posts/the-swift-programming-language-en.html</link><dc:creator>tj2</dc:creator><description>&lt;div&gt;&lt;ol&gt;
&lt;li&gt;Welcome to Swift &lt;ol&gt;
&lt;li&gt;About Swift&lt;/li&gt;
&lt;li&gt;A Swift Tour&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;Language Guide &lt;ol&gt;
&lt;li&gt;The Basics&lt;/li&gt;
&lt;li&gt;Basic Operators&lt;/li&gt;
&lt;li&gt;Strings and Characters&lt;/li&gt;
&lt;li&gt;Collection Types&lt;/li&gt;
&lt;li&gt;Control Flow&lt;/li&gt;
&lt;li&gt;Functions&lt;/li&gt;
&lt;li&gt;Closures&lt;/li&gt;
&lt;li&gt;Enumerations&lt;/li&gt;
&lt;li&gt;Classes and Structures&lt;/li&gt;
&lt;li&gt;Properties&lt;/li&gt;
&lt;li&gt;Methods&lt;/li&gt;
&lt;li&gt;Subscripts&lt;/li&gt;
&lt;li&gt;Inheritance&lt;/li&gt;
&lt;li&gt;Initialization&lt;/li&gt;
&lt;li&gt;Deinitialization&lt;/li&gt;
&lt;li&gt;Automatic Reference Counting&lt;/li&gt;
&lt;li&gt;Optional Chaining&lt;/li&gt;
&lt;li&gt;Type Casting&lt;/li&gt;
&lt;li&gt;Nested Types&lt;/li&gt;
&lt;li&gt;Extensions&lt;/li&gt;
&lt;li&gt;Protocols&lt;/li&gt;
&lt;li&gt;Generics&lt;/li&gt;
&lt;li&gt;Access Control&lt;/li&gt;
&lt;li&gt;Advanced Operators&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;Language Reference &lt;ol&gt;
&lt;li&gt;About the Language Reference&lt;/li&gt;
&lt;li&gt;Lexical Structure&lt;/li&gt;
&lt;li&gt;Types&lt;/li&gt;
&lt;li&gt;Expressions&lt;/li&gt;
&lt;li&gt;Statements&lt;/li&gt;
&lt;li&gt;Declarations&lt;/li&gt;
&lt;li&gt;Attributes&lt;/li&gt;
&lt;li&gt;Patterns&lt;/li&gt;
&lt;li&gt;Generic Parameters and Arguments&lt;/li&gt;
&lt;li&gt;Summary of the Grammar&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;Revision History &lt;ol&gt;
&lt;li&gt;Document Revision History&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;About Swift&lt;/h2&gt;
&lt;p&gt;Swift is a new programming language for iOS and OS X apps that builds on the best of C and Objective-C, without the constraints of C compatibility. Swift adopts safe programming patterns and adds modern features to make programming easier, more flexible, and more fun. Swift’s clean slate, backed by the mature and much-loved Cocoa and Cocoa Touch frameworks, is an opportunity to reimagine how software development works. &lt;/p&gt;
&lt;p&gt;Swift has been years in the making. Apple laid the foundation for Swift by advancing our existing compiler, debugger, and framework infrastructure. We simplified memory management with Automatic Reference Counting (ARC). Our framework stack, built on the solid base of Foundation and Cocoa, has been modernized and standardized throughout. Objective-C itself has evolved to support blocks, collection literals, and modules, enabling framework adoption of modern language technologies without disruption. Thanks to this groundwork, we can now introduce a new language for the future of Apple software development. &lt;/p&gt;
&lt;p&gt;Swift feels familiar to Objective-C developers. It adopts the readability of Objective-C’s named parameters and the power of Objective-C’s dynamic object model. It provides seamless access to existing Cocoa frameworks and mix-and-match interoperability with Objective-C code. Building from this common ground, Swift introduces many new features and unifies the procedural and object-oriented portions of the language. &lt;/p&gt;
&lt;p&gt;Swift is friendly to new programmers. It is the first industrial-quality systems programming language that is as expressive and enjoyable as a scripting language. It supports playgrounds, an innovative feature that allows programmers to experiment with Swift code and see the results immediately, without the overhead of building and running an app. &lt;/p&gt;
&lt;p&gt;Swift combines the best in modern language thinking with wisdom from the wider Apple engineering culture. The compiler is optimized for performance, and the language is optimized for development, without compromising on either. It’s designed to scale from “hello, world” to an entire operating system. All this makes Swift a sound future investment for developers and for Apple. &lt;/p&gt;
&lt;p&gt;Swift is a fantastic way to write iOS and OS X apps, and will continue to evolve with new features and capabilities. Our goals for Swift are ambitious. We can’t wait to see what you create with it. &lt;/p&gt;
&lt;h2&gt;A Swift Tour&lt;/h2&gt;
&lt;p&gt;Tradition suggests that the first program in a new language should print the words “Hello, world!” on the screen. In Swift, this can be done in a single line: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("Hello, world!")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you have written code in C or Objective-C, this syntax looks familiar to you—in Swift, this line of code is a complete program. You don’t need to import a separate library for functionality like input/output or string handling. Code written at global scope is used as the entry point for the program, so you don’t need a &lt;code&gt;main&lt;/code&gt; function. You also don’t need to write semicolons at the end of every statement. &lt;/p&gt;
&lt;p&gt;This tour gives you enough information to start writing code in Swift by showing you how to accomplish a variety of programming tasks. Don’t worry if you don’t understand something—everything introduced in this tour is explained in detail in the rest of this book. &lt;/p&gt;
&lt;p&gt;Note&lt;/p&gt;
&lt;p&gt;For the best experience, open this chapter as a playground in Xcode. Playgrounds allow you to edit the code listings and see the result immediately. &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;a href="http://muxuezi.github.io/posts/GuidedTour.playground.zip"&gt;Download Playground&lt;/a&gt;&lt;/strong&gt;&lt;/p&gt;
&lt;h3&gt;Simple Values&lt;/h3&gt;
&lt;p&gt;Use &lt;code&gt;let&lt;/code&gt; to make a constant and &lt;code&gt;var&lt;/code&gt; to make a variable. The value of a constant doesn’t need to be known at compile time, but you must assign it a value exactly once. This means you can use constants to name a value that you determine once but use in many places. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var myVariable = 42&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myVariable = 50&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let myConstant = 42&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A constant or variable must have the same type as the value you want to assign to it. However, you don’t always have to write the type explicitly. Providing a value when you create a constant or variable lets the compiler infer its type. In the example above, the compiler infers that &lt;code&gt;myVariable&lt;/code&gt; is an integer because its initial value is an integer. &lt;/p&gt;
&lt;p&gt;If the initial value doesn’t provide enough information (or if there is no initial value), specify the type by writing it after the variable, separated by a colon. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let implicitInteger = 70&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let implicitDouble = 70.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let explicitDouble: Double = 70&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Create a constant with an explicit type of &lt;code&gt;Float&lt;/code&gt; and a value of &lt;code&gt;4&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Values are never implicitly converted to another type. If you need to convert a value to a different type, explicitly make an instance of the desired type. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let label = "The width is "&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let width = 94&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let widthLabel = label \+ String(width)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Try removing the conversion to &lt;code&gt;String&lt;/code&gt; from the last line. What error do you get? &lt;/p&gt;
&lt;p&gt;There’s an even simpler way to include values in strings: Write the value in parentheses, and write a backslash (&lt;code&gt;\&lt;/code&gt;) before the parentheses. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let apples = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let oranges = 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let appleSummary = "I have \\(apples) apples."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let fruitSummary = "I have \\(apples \+ oranges) pieces of fruit."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;\\()&lt;/code&gt; to include a floating-point calculation in a string and to include someone’s name in a greeting. &lt;/p&gt;
&lt;p&gt;Create arrays and dictionaries using brackets (&lt;code&gt;[]&lt;/code&gt;), and access their elements by writing the index or key in brackets. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var shoppingList = ["catfish", "water", "tulips", "blue paint"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shoppingList[1] = "bottle of water"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var occupations = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Malcolm": "Captain",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Kaylee": "Mechanic",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;occupations["Jayne"] = "Public Relations"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To create an empty array or dictionary, use the initializer syntax. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let emptyArray = [String]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let emptyDictionary = [String: Float]()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If type information can be inferred, you can write an empty array as &lt;code&gt;[]&lt;/code&gt; and an empty dictionary as &lt;code&gt;[:]&lt;/code&gt;—for example, when you set a new value for a variable or pass an argument to a function. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;shoppingList = []&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;occupations = [:]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Control Flow&lt;/h3&gt;
&lt;p&gt;Use &lt;code&gt;if&lt;/code&gt; and &lt;code&gt;switch&lt;/code&gt; to make conditionals, and use &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt;, &lt;code&gt;for&lt;/code&gt;, &lt;code&gt;while&lt;/code&gt;, and &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; to make loops. Parentheses around the condition or loop variable are optional. Braces around the body are required. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let individualScores = [75, 43, 103, 87, 12]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var teamScore = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for score in individualScores {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if score &amp;gt; 50 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;teamScore += 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;teamScore += 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(teamScore)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In the code above, &lt;code&gt;teamScore&lt;/code&gt; is written on a line by itself. This is a simple way to see the value of a variable inside a playground. &lt;/p&gt;
&lt;p&gt;In an &lt;code&gt;if&lt;/code&gt; statement, the conditional must be a Boolean expression—this means that code such as &lt;code&gt;if score { ... }&lt;/code&gt; is an error, not an implicit comparison to zero. &lt;/p&gt;
&lt;p&gt;You can use &lt;code&gt;if&lt;/code&gt; and &lt;code&gt;let&lt;/code&gt; together to work with values that might be missing. These values are represented as optionals. An optional value either contains a value or contains &lt;code&gt;nil&lt;/code&gt; to indicate that a value is missing. Write a question mark (&lt;code&gt;?&lt;/code&gt;) after the type of a value to mark the value as optional. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var optionalString: String? = "Hello"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(optionalString == nil)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var optionalName: String? = "John Appleseed"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var greeting = "Hello!"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let name = optionalName {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;greeting = "Hello, \\(name)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Change &lt;code&gt;optionalName&lt;/code&gt; to &lt;code&gt;nil&lt;/code&gt;. What greeting do you get? Add an &lt;code&gt;else&lt;/code&gt; clause that sets a different greeting if &lt;code&gt;optionalName&lt;/code&gt; is &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If the optional value is &lt;code&gt;nil&lt;/code&gt;, the conditional is &lt;code&gt;false&lt;/code&gt; and the code in braces is skipped. Otherwise, the optional value is unwrapped and assigned to the constant after &lt;code&gt;let&lt;/code&gt;, which makes the unwrapped value available inside the block of code. &lt;/p&gt;
&lt;p&gt;Switches support any kind of data and a wide variety of comparison operations—they aren’t limited to integers and tests for equality. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let vegetable = "red pepper"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch vegetable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "celery":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let vegetableComment = "Add some raisins and make ants on a log."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "cucumber", "watercress":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let vegetableComment = "That would make a good tea sandwich."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let x where x.hasSuffix("pepper"):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let vegetableComment = "Is it a spicy \\(x)?"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let vegetableComment = "Everything tastes good in soup."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Try removing the default case. What error do you get? &lt;/p&gt;
&lt;p&gt;Notice how &lt;code&gt;let&lt;/code&gt; can be used in a pattern to assign the value that matched that part of a pattern to a constant. &lt;/p&gt;
&lt;p&gt;After executing the code inside the switch case that matched, the program exits from the switch statement. Execution doesn’t continue to the next case, so there is no need to explicitly break out of the switch at the end of each case’s code. &lt;/p&gt;
&lt;p&gt;You use &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; to iterate over items in a dictionary by providing a pair of names to use for each key-value pair. Dictionaries are an unordered collection, so their keys and values are iterated over in an arbitrary order. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let interestingNumbers = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Prime": [2, 3, 5, 7, 11, 13],&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Fibonacci": [1, 1, 2, 3, 5, 8],&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Square": [1, 4, 9, 16, 25],&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var largest = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for (kind, numbers) in interestingNumbers {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for number in numbers {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if number &amp;gt; largest {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;largest = number&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(largest)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Add another variable to keep track of which kind of number was the largest, as well as what that largest number was. &lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;while&lt;/code&gt; to repeat a block of code until a condition changes. The condition of a loop can be at the end instead, ensuring that the loop is run at least once. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var n = 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;while n &amp;lt; 100 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;n = n * 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(n)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var m = 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;do {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;m = m * 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} while m &amp;lt; 100&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(m)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can keep an index in a loop—either by using &lt;code&gt;..&amp;lt;&lt;/code&gt; to make a range of indexes or by writing an explicit initialization, condition, and increment. These two loops do the same thing: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var firstForLoop = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for i in 0..&amp;lt;4 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;firstForLoop += i&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(firstForLoop)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var secondForLoop = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for var i = 0; i &amp;lt; 4; ++i {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;secondForLoop += i&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(secondForLoop)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Use &lt;code&gt;..&amp;lt;&lt;/code&gt; to make a range that omits its upper value, and use &lt;code&gt;...&lt;/code&gt; to make a range that includes both values. &lt;/p&gt;
&lt;h3&gt;Functions and Closures&lt;/h3&gt;
&lt;p&gt;Use &lt;code&gt;func&lt;/code&gt; to declare a function. Call a function by following its name with a list of arguments in parentheses. Use &lt;code&gt;-&amp;gt;&lt;/code&gt; to separate the parameter names and types from the function’s return type. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func greet(name: String, day: String) -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "Hello \\(name), today is \\(day)."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;greet("Bob", "Tuesday")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Remove the &lt;code&gt;day&lt;/code&gt; parameter. Add a parameter to include today’s lunch special in the greeting. &lt;/p&gt;
&lt;p&gt;Use a tuple to make a compound value—for example, to return multiple values from a function. The elements of a tuple can be referred to either by name or by number. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func calculateStatistics(scores: [Int]) -&amp;gt; (min: Int, max: Int, sum: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var min = scores[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var max = scores[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var sum = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for score in scores {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if score &amp;gt; max {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;max = score&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if score &amp;lt; min {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;min = score&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sum += score&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return (min, max, sum)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let statistics = calculateStatistics([5, 3, 100, 3, 9])&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(statistics.sum)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(statistics.2)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Functions can also take a variable number of arguments, collecting them into an array. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func sumOf(numbers: Int...) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var sum = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for number in numbers {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sum += number&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return sum&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sumOf()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sumOf(42, 597, 12)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Write a function that calculates the average of its arguments. &lt;/p&gt;
&lt;p&gt;Functions can be nested. Nested functions have access to variables that were declared in the outer function. You can use nested functions to organize the code in a function that is long or complex. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func returnFifteen() -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var y = 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func add() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;y += 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;add()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return y&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;returnFifteen()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Functions are a first-class type. This means that a function can return another function as its value. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func makeIncrementer() -&amp;gt; (Int -&amp;gt; Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func addOne(number: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return 1 \+ number&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return addOne&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var increment = makeIncrementer()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;increment(7)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A function can take another function as one of its arguments. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func hasAnyMatches(list: [Int], condition: Int -&amp;gt; Bool) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for item in list {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if condition(item) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func lessThanTen(number: Int) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return number &amp;lt; 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var numbers = [20, 19, 7, 12]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;hasAnyMatches(numbers, lessThanTen)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Functions are actually a special case of closures: blocks of code that can be called later. The code in a closure has access to things like variables and functions that were available in the scope where the closure was created, even if the closure is in a different scope when it is executed—you saw an example of this already with nested functions. You can write a closure without a name by surrounding code with braces (&lt;code&gt;{}&lt;/code&gt;). Use &lt;code&gt;in&lt;/code&gt; to separate the arguments and return type from the body. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;numbers.map({&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;(number: Int) -&amp;gt; Int in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let result = 3 * number&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return result&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;})&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Rewrite the closure to return zero for all odd numbers. &lt;/p&gt;
&lt;p&gt;You have several options for writing closures more concisely. When a closure’s type is already known, such as the callback for a delegate, you can omit the type of its parameters, its return type, or both. Single statement closures implicitly return the value of their only statement. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let mappedNumbers = numbers.map({ number in 3 * number })&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(mappedNumbers)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can refer to parameters by number instead of by name—this approach is especially useful in very short closures. A closure passed as the last argument to a function can appear immediately after the parentheses. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let sortedNumbers = sorted(numbers) { $0 &amp;gt; $1 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(sortedNumbers)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Objects and Classes&lt;/h3&gt;
&lt;p&gt;Use &lt;code&gt;class&lt;/code&gt; followed by the class’s name to create a class. A property declaration in a class is written the same way as a constant or variable declaration, except that it is in the context of a class. Likewise, method and function declarations are written the same way. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Shape {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var numberOfSides = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func simpleDescription() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "A shape with \\(numberOfSides) sides."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Add a constant property with &lt;code&gt;let&lt;/code&gt;, and add another method that takes an argument. &lt;/p&gt;
&lt;p&gt;Create an instance of a class by putting parentheses after the class name. Use dot syntax to access the properties and methods of the instance. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var shape = Shape()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shape.numberOfSides = 7&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var shapeDescription = shape.simpleDescription()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This version of the &lt;code&gt;Shape&lt;/code&gt; class is missing something important: an initializer to set up the class when an instance is created. Use &lt;code&gt;init&lt;/code&gt; to create one. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class NamedShape {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var numberOfSides: Int = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func simpleDescription() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "A shape with \\(numberOfSides) sides."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Notice how &lt;code&gt;self&lt;/code&gt; is used to distinguish the &lt;code&gt;name&lt;/code&gt; property from the &lt;code&gt;name&lt;/code&gt; argument to the initializer. The arguments to the initializer are passed like a function call when you create an instance of the class. Every property needs a value assigned—either in its declaration (as with &lt;code&gt;numberOfSides&lt;/code&gt;) or in the initializer (as with &lt;code&gt;name&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;deinit&lt;/code&gt; to create a deinitializer if you need to perform some cleanup before the object is deallocated. &lt;/p&gt;
&lt;p&gt;Subclasses include their superclass name after their class name, separated by a colon. There is no requirement for classes to subclass any standard root class, so you can include or omit a superclass as needed. &lt;/p&gt;
&lt;p&gt;Methods on a subclass that override the superclass’s implementation are marked with &lt;code&gt;override&lt;/code&gt;—overriding a method by accident, without &lt;code&gt;override&lt;/code&gt;, is detected by the compiler as an error. The compiler also detects methods with &lt;code&gt;override&lt;/code&gt; that don’t actually override any method in the superclass. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Square: NamedShape {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var sideLength: Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(sideLength: Double, name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.sideLength = sideLength&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init(name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfSides = 4&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func area() -&amp;gt; Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return sideLength * sideLength&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override func simpleDescription() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "A square with sides of length \\(sideLength)."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let test = Square(sideLength: 5.2, name: "my test square")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;test.area()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;test.simpleDescription()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Make another subclass of &lt;code&gt;NamedShape&lt;/code&gt; called &lt;code&gt;Circle&lt;/code&gt; that takes a radius and a name as arguments to its initializer. Implement an &lt;code&gt;area&lt;/code&gt; and a &lt;code&gt;simpleDescription&lt;/code&gt; method on the &lt;code&gt;Circle&lt;/code&gt; class. &lt;/p&gt;
&lt;p&gt;In addition to simple properties that are stored, properties can have a getter and a setter. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class EquilateralTriangle: NamedShape {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var sideLength: Double = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(sideLength: Double, name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.sideLength = sideLength&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init(name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfSides = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var perimeter: Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;get {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return 3.0 * sideLength&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;set {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sideLength = newValue / 3.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override func simpleDescription() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "An equilateral triangle with sides of length \\(sideLength)."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var triangle = EquilateralTriangle(sideLength: 3.1, name: "a triangle")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(triangle.perimeter)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;triangle.perimeter = 9.9&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(triangle.sideLength)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the setter for &lt;code&gt;perimeter&lt;/code&gt;, the new value has the implicit name &lt;code&gt;newValue&lt;/code&gt;. You can provide an explicit name in parentheses after &lt;code&gt;set&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Notice that the initializer for the &lt;code&gt;EquilateralTriangle&lt;/code&gt; class has three different steps: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Setting the value of properties that the subclass declares. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Calling the superclass’s initializer. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Changing the value of properties defined by the superclass. Any additional setup work that uses methods, getters, or setters can also be done at this point. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you don’t need to compute the property but still need to provide code that is run before and after setting a new value, use &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt;. For example, the class below ensures that the side length of its triangle is always the same as the side length of its square. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class TriangleAndSquare {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var triangle: EquilateralTriangle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;willSet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square.sideLength = newValue.sideLength&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var square: Square {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;willSet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;triangle.sideLength = newValue.sideLength&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(size: Double, name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square = Square(sideLength: size, name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;triangle = EquilateralTriangle(sideLength: size, name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var triangleAndSquare = TriangleAndSquare(size: 10, name: "another test shape")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(triangleAndSquare.square.sideLength)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(triangleAndSquare.triangle.sideLength)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;triangleAndSquare.square = Square(sideLength: 50, name: "larger square")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(triangleAndSquare.triangle.sideLength)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Methods on classes have one important difference from functions. Parameter names in functions are used only within the function, but parameters names in methods are also used when you call the method (except for the first parameter). By default, a method has the same name for its parameters when you call it and within the method itself. You can specify a second name, which is used inside the method. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Counter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var count: Int = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func incrementBy(amount: Int, numberOfTimes times: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count += amount * times&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var counter = Counter()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.incrementBy(2, numberOfTimes: 7)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When working with optional values, you can write &lt;code&gt;?&lt;/code&gt; before operations like methods, properties, and subscripting. If the value before the &lt;code&gt;?&lt;/code&gt; is &lt;code&gt;nil&lt;/code&gt;, everything after the &lt;code&gt;?&lt;/code&gt; is ignored and the value of the whole expression is &lt;code&gt;nil&lt;/code&gt;. Otherwise, the optional value is unwrapped, and everything after the &lt;code&gt;?&lt;/code&gt; acts on the unwrapped value. In both cases, the value of the whole expression is an optional value. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let optionalSquare: Square? = Square(sideLength: 2.5, name: "optional square")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let sideLength = optionalSquare?.sideLength&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Enumerations and Structures&lt;/h3&gt;
&lt;p&gt;Use &lt;code&gt;enum&lt;/code&gt; to create an enumeration. Like classes and all other named types, enumerations can have methods associated with them. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum Rank: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Ace = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Two, Three, Four, Five, Six, Seven, Eight, Nine, Ten&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Jack, Queen, King&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func simpleDescription() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch self {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Ace:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "ace"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Jack:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "jack"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Queen:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "queen"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .King:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "king"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return String(self.rawValue)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let ace = Rank.Ace&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let aceRawValue = ace.rawValue&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Write a function that compares two &lt;code&gt;Rank&lt;/code&gt; values by comparing their raw values. &lt;/p&gt;
&lt;p&gt;In the example above, the raw-value type of the enumeration is &lt;code&gt;Int&lt;/code&gt;, so you only have to specify the first raw value. The rest of the raw values are assigned in order. You can also use strings or floating-point numbers as the raw type of an enumeration. Use the &lt;code&gt;rawValue&lt;/code&gt; property to access the raw value of an enumeration member. &lt;/p&gt;
&lt;p&gt;Use the &lt;code&gt;init?(rawValue:)&lt;/code&gt; initializer to make an instance of an enumeration from a raw value. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let convertedRank = Rank(rawValue: 3) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let threeDescription = convertedRank.simpleDescription()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The member values of an enumeration are actual values, not just another way of writing their raw values. In fact, in cases where there isn’t a meaningful raw value, you don’t have to provide one. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum Suit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Spades, Hearts, Diamonds, Clubs&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func simpleDescription() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch self {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Spades:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "spades"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Hearts:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "hearts"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Diamonds:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "diamonds"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Clubs:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "clubs"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let hearts = Suit.Hearts&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let heartsDescription = hearts.simpleDescription()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Add a &lt;code&gt;color&lt;/code&gt; method to &lt;code&gt;Suit&lt;/code&gt; that returns “black” for spades and clubs, and returns “red” for hearts and diamonds. &lt;/p&gt;
&lt;p&gt;Notice the two ways that the &lt;code&gt;Hearts&lt;/code&gt; member of the enumeration is referred to above: When assigning a value to the &lt;code&gt;hearts&lt;/code&gt; constant, the enumeration member &lt;code&gt;Suit.Hearts&lt;/code&gt; is referred to by its full name because the constant doesn’t have an explicit type specified. Inside the switch, the enumeration member is referred to by the abbreviated form &lt;code&gt;.Hearts&lt;/code&gt; because the value of &lt;code&gt;self&lt;/code&gt; is already known to be a suit. You can use the abbreviated form anytime the value’s type is already known. &lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;struct&lt;/code&gt; to create a structure. Structures support many of the same behaviors as classes, including methods and initializers. One of the most important differences between structures and classes is that structures are always copied when they are passed around in your code, but classes are passed by reference. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Card {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var rank: Rank&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var suit: Suit&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func simpleDescription() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "The \\(rank.simpleDescription()) of \\(suit.simpleDescription())"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let threeOfSpades = Card(rank: .Three, suit: .Spades)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let threeOfSpadesDescription = threeOfSpades.simpleDescription()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Add a method to &lt;code&gt;Card&lt;/code&gt; that creates a full deck of cards, with one card of each combination of rank and suit. &lt;/p&gt;
&lt;p&gt;An instance of an enumeration member can have values associated with the instance. Instances of the same enumeration member can have different values associated with them. You provide the associated values when you create the instance. Associated values and raw values are different: The raw value of an enumeration member is the same for all of its instances, and you provide the raw value when you define the enumeration. &lt;/p&gt;
&lt;p&gt;For example, consider the case of requesting the sunrise and sunset time from a server. The server either responds with the information or it responds with some error information. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum ServerResponse {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Result(String, String)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Error(String)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let success = ServerResponse.Result("6:00 am", "8:09 pm")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let failure = ServerResponse.Error("Out of cheese.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch success {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let .Result(sunrise, sunset):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let serverResponse = "Sunrise is at \\(sunrise) and sunset is at \\(sunset)."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let .Error(error):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let serverResponse = "Failure... \\(error)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Add a third case to &lt;code&gt;ServerResponse&lt;/code&gt; and to the switch. &lt;/p&gt;
&lt;p&gt;Notice how the sunrise and sunset times are extracted from the &lt;code&gt;ServerResponse&lt;/code&gt; value as part of matching the value against the switch cases. &lt;/p&gt;
&lt;h3&gt;Protocols and Extensions&lt;/h3&gt;
&lt;p&gt;Use &lt;code&gt;protocol&lt;/code&gt; to declare a protocol. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol ExampleProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var simpleDescription: String { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func adjust()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Classes, enumerations, and structs can all adopt protocols. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SimpleClass: ExampleProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var simpleDescription: String = "A very simple class."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var anotherProperty: Int = 69105&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func adjust() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;simpleDescription += " Now 100% adjusted."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var a = SimpleClass()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a.adjust()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let aDescription = a.simpleDescription&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct SimpleStructure: ExampleProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var simpleDescription: String = "A simple structure"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func adjust() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;simpleDescription += " (adjusted)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var b = SimpleStructure()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;b.adjust()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let bDescription = b.simpleDescription&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Write an enumeration that conforms to this protocol. &lt;/p&gt;
&lt;p&gt;Notice the use of the &lt;code&gt;mutating&lt;/code&gt; keyword in the declaration of &lt;code&gt;SimpleStructure&lt;/code&gt; to mark a method that modifies the structure. The declaration of &lt;code&gt;SimpleClass&lt;/code&gt; doesn’t need any of its methods marked as mutating because methods on a class can always modify the class. &lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;extension&lt;/code&gt; to add functionality to an existing type, such as new methods and computed properties. You can use an extension to add protocol conformance to a type that is declared elsewhere, or even to a type that you imported from a library or framework. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Int: ExampleProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var simpleDescription: String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "The number \\(self)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func adjust() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self += 42&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(7.simpleDescription)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Write an extension for the &lt;code&gt;Double&lt;/code&gt; type that adds an &lt;code&gt;absoluteValue&lt;/code&gt; property. &lt;/p&gt;
&lt;p&gt;You can use a protocol name just like any other named type—for example, to create a collection of objects that have different types but that all conform to a single protocol. When you work with values whose type is a protocol type, methods outside the protocol definition are not available. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let protocolValue: ExampleProtocol = a&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(protocolValue.simpleDescription)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// println(protocolValue.anotherProperty) // Uncomment to see the error&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Even though the variable &lt;code&gt;protocolValue&lt;/code&gt; has a runtime type of &lt;code&gt;SimpleClass&lt;/code&gt;, the compiler treats it as the given type of &lt;code&gt;ExampleProtocol&lt;/code&gt;. This means that you can’t accidentally access methods or properties that the class implements in addition to its protocol conformance. &lt;/p&gt;
&lt;h3&gt;Generics&lt;/h3&gt;
&lt;p&gt;Write a name inside angle brackets to make a generic function or type. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func repeat&amp;lt;Item&amp;gt;(item: Item, times: Int) -&amp;gt; [Item] {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var result = [Item]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for i in 0..&amp;lt;times {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;result.append(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return result&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;repeat("knock", 4)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can make generic forms of functions and methods, as well as classes, enumerations, and structures. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;// Reimplement the Swift standard library's optional type&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;enum OptionalValue&amp;lt;T&amp;gt; {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case None&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Some(T)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var possibleInteger: OptionalValue&amp;lt;Int&amp;gt; = .None&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;possibleInteger = .Some(100)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Use &lt;code&gt;where&lt;/code&gt; after the type name to specify a list of requirements—for example, to require the type to implement a protocol, to require two types to be the same, or to require a class to have a particular superclass. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func anyCommonElements &amp;lt;T, U where T: SequenceType, U: SequenceType, T.Generator.Element: Equatable, T.Generator.Element == U.Generator.Element&amp;gt; (lhs: T, rhs: U) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for lhsItem in lhs {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for rhsItem in rhs {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if lhsItem == rhsItem {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;anyCommonElements([1, 2, 3], [3])&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Experiment &lt;/p&gt;
&lt;p&gt;Modify the &lt;code&gt;anyCommonElements&lt;/code&gt; function to make a function that returns an array of the elements that any two sequences have in common. &lt;/p&gt;
&lt;p&gt;In the simple cases, you can omit &lt;code&gt;where&lt;/code&gt; and simply write the protocol or class name after a colon. Writing &lt;code&gt;&amp;lt;T: Equatable&amp;gt;&lt;/code&gt; is the same as writing &lt;code&gt;&amp;lt;T where T: Equatable&amp;gt;&lt;/code&gt;. &lt;/p&gt;
&lt;h2&gt;The Basics&lt;/h2&gt;
&lt;p&gt;Swift is a new programming language for iOS and OS X app development. Nonetheless, many parts of Swift will be familiar from your experience of developing in C and Objective-C. &lt;/p&gt;
&lt;p&gt;Swift provides its own versions of all fundamental C and Objective-C types, including &lt;code&gt;Int&lt;/code&gt; for integers, &lt;code&gt;Double&lt;/code&gt; and &lt;code&gt;Float&lt;/code&gt; for floating-point values, &lt;code&gt;Bool&lt;/code&gt; for Boolean values, and &lt;code&gt;String&lt;/code&gt; for textual data. Swift also provides powerful versions of the two primary collection types, &lt;code&gt;Array&lt;/code&gt; and &lt;code&gt;Dictionary&lt;/code&gt;, as described in Collection Types. &lt;/p&gt;
&lt;p&gt;Like C, Swift uses variables to store and refer to values by an identifying name. Swift also makes extensive use of variables whose values cannot be changed. These are known as constants, and are much more powerful than constants in C. Constants are used throughout Swift to make code safer and clearer in intent when you work with values that do not need to change. &lt;/p&gt;
&lt;p&gt;In addition to familiar types, Swift introduces advanced types not found in Objective-C, such as tuples. Tuples enable you to create and pass around groupings of values. You can use a tuple to return multiple values from a function as a single compound value. &lt;/p&gt;
&lt;p&gt;Swift also introduces optional types, which handle the absence of a value. Optionals say either “there &lt;em&gt;is&lt;/em&gt; a value, and it equals &lt;em&gt;x&lt;/em&gt;” or “there &lt;em&gt;isn’t&lt;/em&gt; a value at all”. Optionals are similar to using &lt;code&gt;nil&lt;/code&gt; with pointers in Objective-C, but they work for any type, not just classes. Optionals are safer and more expressive than &lt;code&gt;nil&lt;/code&gt; pointers in Objective-C and are at the heart of many of Swift’s most powerful features. &lt;/p&gt;
&lt;p&gt;Optionals are an example of the fact that Swift is a &lt;em&gt;type safe&lt;/em&gt; language. Swift helps you to be clear about the types of values your code can work with. If part of your code expects a &lt;code&gt;String&lt;/code&gt;, type safety prevents you from passing it an &lt;code&gt;Int&lt;/code&gt; by mistake. This restriction enables you to catch and fix errors as early as possible in the development process. &lt;/p&gt;
&lt;h3&gt;Constants and Variables&lt;/h3&gt;
&lt;p&gt;Constants and variables associate a name (such as &lt;code&gt;maximumNumberOfLoginAttempts&lt;/code&gt; or &lt;code&gt;welcomeMessage&lt;/code&gt;) with a value of a particular type (such as the number &lt;code&gt;10&lt;/code&gt; or the string &lt;code&gt;"Hello"&lt;/code&gt;). The value of a &lt;em&gt;constant&lt;/em&gt; cannot be changed once it is set, whereas a &lt;em&gt;variable&lt;/em&gt; can be set to a different value in the future. &lt;/p&gt;
&lt;h4&gt;Declaring Constants and Variables&lt;/h4&gt;
&lt;p&gt;Constants and variables must be declared before they are used. You declare constants with the &lt;code&gt;let&lt;/code&gt; keyword and variables with the &lt;code&gt;var&lt;/code&gt; keyword. Here’s an example of how constants and variables can be used to track the number of login attempts a user has made: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let maximumNumberOfLoginAttempts = 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentLoginAttempt = 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This code can be read as: &lt;/p&gt;
&lt;p&gt;“Declare a new constant called &lt;code&gt;maximumNumberOfLoginAttempts&lt;/code&gt;, and give it a value of &lt;code&gt;10&lt;/code&gt;. Then, declare a new variable called &lt;code&gt;currentLoginAttempt&lt;/code&gt;, and give it an initial value of &lt;code&gt;0&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;In this example, the maximum number of allowed login attempts is declared as a constant, because the maximum value never changes. The current login attempt counter is declared as a variable, because this value must be incremented after each failed login attempt. &lt;/p&gt;
&lt;p&gt;You can declare multiple constants or multiple variables on a single line, separated by commas: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0, z = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If a stored value in your code is not going to change, always declare it as a constant with the &lt;code&gt;let&lt;/code&gt; keyword. Use variables only for storing values that need to be able to change. &lt;/p&gt;
&lt;h4&gt;Type Annotations&lt;/h4&gt;
&lt;p&gt;You can provide a &lt;em&gt;type annotation&lt;/em&gt; when you declare a constant or variable, to be clear about the kind of values the constant or variable can store. Write a type annotation by placing a colon after the constant or variable name, followed by a space, followed by the name of the type to use. &lt;/p&gt;
&lt;p&gt;This example provides a type annotation for a variable called &lt;code&gt;welcomeMessage&lt;/code&gt;, to indicate that the variable can store &lt;code&gt;String&lt;/code&gt; values: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var welcomeMessage: String&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The colon in the declaration means &lt;em&gt;“…of type…,”&lt;/em&gt; so the code above can be read as: &lt;/p&gt;
&lt;p&gt;“Declare a variable called &lt;code&gt;welcomeMessage&lt;/code&gt; that is of type &lt;code&gt;String&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;The phrase “of type &lt;code&gt;String&lt;/code&gt;” means “can store any &lt;code&gt;String&lt;/code&gt; value.” Think of it as meaning “the type of thing” (or “the kind of thing”) that can be stored. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;welcomeMessage&lt;/code&gt; variable can now be set to any string value without error: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;welcomeMessage = "Hello"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can define multiple related variables of the same type on a single line, separated by commas, with a single type annotation after the final variable name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var red, green, blue: Double&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;It is rare that you need to write type annotations in practice. If you provide an initial value for a constant or variable at the point that it is defined, Swift can almost always infer the type to be used for that constant or variable, as described in Type Safety and Type Inference. In the &lt;code&gt;welcomeMessage&lt;/code&gt; example above, no initial value is provided, and so the type of the &lt;code&gt;welcomeMessage&lt;/code&gt; variable is specified with a type annotation rather than being inferred from an initial value. &lt;/p&gt;
&lt;h4&gt;Naming Constants and Variables&lt;/h4&gt;
&lt;p&gt;Constant and variable names can contain almost any character, including Unicode characters: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let π = 3.14159&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let 你好 = "你好世界"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let 🐶🐮 = "dogcow"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Constant and variable names cannot contain whitespace characters, mathematical symbols, arrows, private-use (or invalid) Unicode code points, or line- and box-drawing characters. Nor can they begin with a number, although numbers may be included elsewhere within the name. &lt;/p&gt;
&lt;p&gt;Once you’ve declared a constant or variable of a certain type, you can’t redeclare it again with the same name, or change it to store values of a different type. Nor can you change a constant into a variable or a variable into a constant. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you need to give a constant or variable the same name as a reserved Swift keyword, surround the keyword with back ticks (```) when using it as a name. However, avoid using keywords as names unless you have absolutely no choice. &lt;/p&gt;
&lt;p&gt;You can change the value of an existing variable to another value of a compatible type. In this example, the value of &lt;code&gt;friendlyWelcome&lt;/code&gt; is changed from &lt;code&gt;"Hello!"&lt;/code&gt; to &lt;code&gt;"Bonjour!"&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var friendlyWelcome = "Hello!"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;friendlyWelcome = "Bonjour!"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// friendlyWelcome is now "Bonjour!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unlike a variable, the value of a constant cannot be changed once it is set. Attempting to do so is reported as an error when your code is compiled: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let languageName = "Swift"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;languageName = "Swift++"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this is a compile-time error - languageName cannot be changed&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Printing Constants and Variables&lt;/h4&gt;
&lt;p&gt;You can print the current value of a constant or variable with the &lt;code&gt;println&lt;/code&gt; function: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println(friendlyWelcome)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Bonjour!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;println&lt;/code&gt; is a global function that prints a value, followed by a line break, to an appropriate output. In Xcode, for example, &lt;code&gt;println&lt;/code&gt; prints its output in Xcode’s “console” pane. (A second function, &lt;code&gt;print&lt;/code&gt;, performs the same task without appending a line break to the end of the value to be printed.) &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;println&lt;/code&gt; function prints any &lt;code&gt;String&lt;/code&gt; value you pass to it: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("This is a string")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "This is a string"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;println&lt;/code&gt; function can print more complex logging messages, in a similar manner to Cocoa’s &lt;code&gt;NSLog&lt;/code&gt; function. These messages can include the current values of constants and variables. &lt;/p&gt;
&lt;p&gt;Swift uses &lt;em&gt;string interpolation&lt;/em&gt; to include the name of a constant or variable as a placeholder in a longer string, and to prompt Swift to replace it with the current value of that constant or variable. Wrap the name in parentheses and escape it with a backslash before the opening parenthesis: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The current value of friendlyWelcome is \\(friendlyWelcome)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The current value of friendlyWelcome is Bonjour!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;All options you can use with string interpolation are described in String Interpolation. &lt;/p&gt;
&lt;h3&gt;Comments&lt;/h3&gt;
&lt;p&gt;Use comments to include non-executable text in your code, as a note or reminder to yourself. Comments are ignored by the Swift compiler when your code is compiled. &lt;/p&gt;
&lt;p&gt;Comments in Swift are very similar to comments in C. Single-line comments begin with two forward-slashes (&lt;code&gt;//&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;// this is a comment&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Multiline comments start with a forward-slash followed by an asterisk (&lt;code&gt;/*&lt;/code&gt;) and end with an asterisk followed by a forward-slash (&lt;code&gt;*/&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;/* this is also a comment,&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;but written over multiple lines */&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unlike multiline comments in C, multiline comments in Swift can be nested inside other multiline comments. You write nested comments by starting a multiline comment block and then starting a second multiline comment within the first block. The second block is then closed, followed by the first block: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;/* this is the start of the first multiline comment&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/* this is the second, nested multiline comment */&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;this is the end of the first multiline comment */&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Nested multiline comments enable you to comment out large blocks of code quickly and easily, even if the code already contains multiline comments. &lt;/p&gt;
&lt;h3&gt;Semicolons&lt;/h3&gt;
&lt;p&gt;Unlike many other languages, Swift does not require you to write a semicolon (&lt;code&gt;;&lt;/code&gt;) after each statement in your code, although you can do so if you wish. Semicolons &lt;em&gt;are&lt;/em&gt; required, however, if you want to write multiple separate statements on a single line: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let cat = "🐱"; println(cat)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "🐱"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Integers&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Integers&lt;/em&gt; are whole numbers with no fractional component, such as &lt;code&gt;42&lt;/code&gt; and &lt;code&gt;-23&lt;/code&gt;. Integers are either &lt;em&gt;signed&lt;/em&gt; (positive, zero, or negative) or &lt;em&gt;unsigned&lt;/em&gt; (positive or zero). &lt;/p&gt;
&lt;p&gt;Swift provides signed and unsigned integers in 8, 16, 32, and 64 bit forms. These integers follow a naming convention similar to C, in that an 8-bit unsigned integer is of type &lt;code&gt;UInt8&lt;/code&gt;, and a 32-bit signed integer is of type &lt;code&gt;Int32&lt;/code&gt;. Like all types in Swift, these integer types have capitalized names. &lt;/p&gt;
&lt;h4&gt;Integer Bounds&lt;/h4&gt;
&lt;p&gt;You can access the minimum and maximum values of each integer type with its &lt;code&gt;min&lt;/code&gt; and &lt;code&gt;max&lt;/code&gt; properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let minValue = UInt8.min // minValue is equal to 0, and is of type UInt8&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let maxValue = UInt8.max // maxValue is equal to 255, and is of type UInt8&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The values of these properties are of the appropriate-sized number type (such as &lt;code&gt;UInt8&lt;/code&gt; in the example above) and can therefore be used in expressions alongside other values of the same type. &lt;/p&gt;
&lt;h4&gt;Int&lt;/h4&gt;
&lt;p&gt;In most cases, you don’t need to pick a specific size of integer to use in your code. Swift provides an additional integer type, &lt;code&gt;Int&lt;/code&gt;, which has the same size as the current platform’s native word size: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;On a 32-bit platform, &lt;code&gt;Int&lt;/code&gt; is the same size as &lt;code&gt;Int32&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;On a 64-bit platform, &lt;code&gt;Int&lt;/code&gt; is the same size as &lt;code&gt;Int64&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unless you need to work with a specific size of integer, always use &lt;code&gt;Int&lt;/code&gt; for integer values in your code. This aids code consistency and interoperability. Even on 32-bit platforms, &lt;code&gt;Int&lt;/code&gt; can store any value between &lt;code&gt;-2,147,483,648&lt;/code&gt; and &lt;code&gt;2,147,483,647&lt;/code&gt;, and is large enough for many integer ranges. &lt;/p&gt;
&lt;h4&gt;UInt&lt;/h4&gt;
&lt;p&gt;Swift also provides an unsigned integer type, &lt;code&gt;UInt&lt;/code&gt;, which has the same size as the current platform’s native word size: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;On a 32-bit platform, &lt;code&gt;UInt&lt;/code&gt; is the same size as &lt;code&gt;UInt32&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;On a 64-bit platform, &lt;code&gt;UInt&lt;/code&gt; is the same size as &lt;code&gt;UInt64&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;UInt&lt;/code&gt; only when you specifically need an unsigned integer type with the same size as the platform’s native word size. If this is not the case, &lt;code&gt;Int&lt;/code&gt; is preferred, even when the values to be stored are known to be non-negative. A consistent use of &lt;code&gt;Int&lt;/code&gt; for integer values aids code interoperability, avoids the need to convert between different number types, and matches integer type inference, as described in Type Safety and Type Inference. &lt;/p&gt;
&lt;h3&gt;Floating-Point Numbers&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Floating-point numbers&lt;/em&gt; are numbers with a fractional component, such as &lt;code&gt;3.14159&lt;/code&gt;, &lt;code&gt;0.1&lt;/code&gt;, and &lt;code&gt;-273.15&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Floating-point types can represent a much wider range of values than integer types, and can store numbers that are much larger or smaller than can be stored in an &lt;code&gt;Int&lt;/code&gt;. Swift provides two signed floating-point number types: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;Double&lt;/code&gt; represents a 64-bit floating-point number. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;Float&lt;/code&gt; represents a 32-bit floating-point number. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;&lt;code&gt;Double&lt;/code&gt; has a precision of at least 15 decimal digits, whereas the precision of &lt;code&gt;Float&lt;/code&gt; can be as little as 6 decimal digits. The appropriate floating-point type to use depends on the nature and range of values you need to work with in your code. In situations where either type would be appropriate, &lt;code&gt;Double&lt;/code&gt; is preferred. &lt;/p&gt;
&lt;h3&gt;Type Safety and Type Inference&lt;/h3&gt;
&lt;p&gt;Swift is a &lt;em&gt;type safe&lt;/em&gt; language. A type safe language encourages you to be clear about the types of values your code can work with. If part of your code expects a &lt;code&gt;String&lt;/code&gt;, you can’t pass it an &lt;code&gt;Int&lt;/code&gt; by mistake. &lt;/p&gt;
&lt;p&gt;Because Swift is type safe, it performs &lt;em&gt;type checks&lt;/em&gt; when compiling your code and flags any mismatched types as errors. This enables you to catch and fix errors as early as possible in the development process. &lt;/p&gt;
&lt;p&gt;Type-checking helps you avoid errors when you’re working with different types of values. However, this doesn’t mean that you have to specify the type of every constant and variable that you declare. If you don’t specify the type of value you need, Swift uses &lt;em&gt;type inference&lt;/em&gt; to work out the appropriate type. Type inference enables a compiler to deduce the type of a particular expression automatically when it compiles your code, simply by examining the values you provide. &lt;/p&gt;
&lt;p&gt;Because of type inference, Swift requires far fewer type declarations than languages such as C or Objective-C. Constants and variables are still explicitly typed, but much of the work of specifying their type is done for you. &lt;/p&gt;
&lt;p&gt;Type inference is particularly useful when you declare a constant or variable with an initial value. This is often done by assigning a &lt;em&gt;literal value&lt;/em&gt; (or &lt;em&gt;literal&lt;/em&gt;) to the constant or variable at the point that you declare it. (A literal value is a value that appears directly in your source code, such as &lt;code&gt;42&lt;/code&gt; and &lt;code&gt;3.14159&lt;/code&gt; in the examples below.) &lt;/p&gt;
&lt;p&gt;For example, if you assign a literal value of &lt;code&gt;42&lt;/code&gt; to a new constant without saying what type it is, Swift infers that you want the constant to be an &lt;code&gt;Int&lt;/code&gt;, because you have initialized it with a number that looks like an integer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let meaningOfLife = 42&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// meaningOfLife is inferred to be of type Int&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Likewise, if you don’t specify a type for a floating-point literal, Swift infers that you want to create a &lt;code&gt;Double&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let pi = 3.14159&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// pi is inferred to be of type Double&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Swift always chooses &lt;code&gt;Double&lt;/code&gt; (rather than &lt;code&gt;Float&lt;/code&gt;) when inferring the type of floating-point numbers. &lt;/p&gt;
&lt;p&gt;If you combine integer and floating-point literals in an expression, a type of &lt;code&gt;Double&lt;/code&gt; will be inferred from the context: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let anotherPi = 3 \+ 0.14159&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// anotherPi is also inferred to be of type Double&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The literal value of &lt;code&gt;3&lt;/code&gt; has no explicit type in and of itself, and so an appropriate output type of &lt;code&gt;Double&lt;/code&gt; is inferred from the presence of a floating-point literal as part of the addition. &lt;/p&gt;
&lt;h3&gt;Numeric Literals&lt;/h3&gt;
&lt;p&gt;Integer literals can be written as: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;A &lt;em&gt;decimal&lt;/em&gt; number, with no prefix &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A &lt;em&gt;binary&lt;/em&gt; number, with a &lt;code&gt;0b&lt;/code&gt; prefix &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;An &lt;em&gt;octal&lt;/em&gt; number, with a &lt;code&gt;0o&lt;/code&gt; prefix &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A &lt;em&gt;hexadecimal&lt;/em&gt; number, with a &lt;code&gt;0x&lt;/code&gt; prefix &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;All of these integer literals have a decimal value of &lt;code&gt;17&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let decimalInteger = 17&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let binaryInteger = 0b10001 // 17 in binary notation&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let octalInteger = 0o21 // 17 in octal notation&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let hexadecimalInteger = 0x11 // 17 in hexadecimal notation&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Floating-point literals can be decimal (with no prefix), or hexadecimal (with a &lt;code&gt;0x&lt;/code&gt; prefix). They must always have a number (or hexadecimal number) on both sides of the decimal point. They can also have an optional &lt;em&gt;exponent&lt;/em&gt;, indicated by an uppercase or lowercase &lt;code&gt;e&lt;/code&gt; for decimal floats, or an uppercase or lowercase &lt;code&gt;p&lt;/code&gt; for hexadecimal floats. &lt;/p&gt;
&lt;p&gt;For decimal numbers with an exponent of &lt;code&gt;exp&lt;/code&gt;, the base number is multiplied by 10exp: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;1.25e2&lt;/code&gt; means 1.25 × 102, or &lt;code&gt;125.0&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;1.25e-2&lt;/code&gt; means 1.25 × 10-2, or &lt;code&gt;0.0125&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For hexadecimal numbers with an exponent of &lt;code&gt;exp&lt;/code&gt;, the base number is multiplied by 2exp: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;0xFp2&lt;/code&gt; means 15 × 22, or &lt;code&gt;60.0&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;0xFp-2&lt;/code&gt; means 15 × 2-2, or &lt;code&gt;3.75&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;All of these floating-point literals have a decimal value of &lt;code&gt;12.1875&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let decimalDouble = 12.1875&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let exponentDouble = 1.21875e1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let hexadecimalDouble = 0xC.3p0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Numeric literals can contain extra formatting to make them easier to read. Both integers and floats can be padded with extra zeroes and can contain underscores to help with readability. Neither type of formatting affects the underlying value of the literal: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let paddedDouble = 000123.456&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let oneMillion = 1_000_000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let justOverOneMillion = 1_000_000.000_000_1&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Numeric Type Conversion&lt;/h3&gt;
&lt;p&gt;Use the &lt;code&gt;Int&lt;/code&gt; type for all general-purpose integer constants and variables in your code, even if they are known to be non-negative. Using the default integer type in everyday situations means that integer constants and variables are immediately interoperable in your code and will match the inferred type for integer literal values. &lt;/p&gt;
&lt;p&gt;Use other integer types only when they are specifically needed for the task at hand, because of explicitly-sized data from an external source, or for performance, memory usage, or other necessary optimization. Using explicitly-sized types in these situations helps to catch any accidental value overflows and implicitly documents the nature of the data being used. &lt;/p&gt;
&lt;h4&gt;Integer Conversion&lt;/h4&gt;
&lt;p&gt;The range of numbers that can be stored in an integer constant or variable is different for each numeric type. An &lt;code&gt;Int8&lt;/code&gt; constant or variable can store numbers between &lt;code&gt;-128&lt;/code&gt; and &lt;code&gt;127&lt;/code&gt;, whereas a &lt;code&gt;UInt8&lt;/code&gt; constant or variable can store numbers between &lt;code&gt;0&lt;/code&gt; and &lt;code&gt;255&lt;/code&gt;. A number that will not fit into a constant or variable of a sized integer type is reported as an error when your code is compiled: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let cannotBeNegative: UInt8 = -1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// UInt8 cannot store negative numbers, and so this will report an error&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let tooBig: Int8 = Int8.max \+ 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Int8 cannot store a number larger than its maximum value,&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// and so this will also report an error&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because each numeric type can store a different range of values, you must opt in to numeric type conversion on a case-by-case basis. This opt-in approach prevents hidden conversion errors and helps make type conversion intentions explicit in your code. &lt;/p&gt;
&lt;p&gt;To convert one specific number type to another, you initialize a new number of the desired type with the existing value. In the example below, the constant &lt;code&gt;twoThousand&lt;/code&gt; is of type &lt;code&gt;UInt16&lt;/code&gt;, whereas the constant &lt;code&gt;one&lt;/code&gt; is of type &lt;code&gt;UInt8&lt;/code&gt;. They cannot be added together directly, because they are not of the same type. Instead, this example calls &lt;code&gt;UInt16(one)&lt;/code&gt; to create a new &lt;code&gt;UInt16&lt;/code&gt; initialized with the value of &lt;code&gt;one&lt;/code&gt;, and uses this value in place of the original: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let twoThousand: UInt16 = 2_000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let one: UInt8 = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let twoThousandAndOne = twoThousand \+ UInt16(one)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because both sides of the addition are now of type &lt;code&gt;UInt16&lt;/code&gt;, the addition is allowed. The output constant (&lt;code&gt;twoThousandAndOne&lt;/code&gt;) is inferred to be of type &lt;code&gt;UInt16&lt;/code&gt;, because it is the sum of two &lt;code&gt;UInt16&lt;/code&gt; values. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;SomeType(ofInitialValue)&lt;/code&gt; is the default way to call the initializer of a Swift type and pass in an initial value. Behind the scenes, &lt;code&gt;UInt16&lt;/code&gt; has an initializer that accepts a &lt;code&gt;UInt8&lt;/code&gt; value, and so this initializer is used to make a new &lt;code&gt;UInt16&lt;/code&gt; from an existing &lt;code&gt;UInt8&lt;/code&gt;. You can’t pass in &lt;em&gt;any&lt;/em&gt; type here, however—it has to be a type for which &lt;code&gt;UInt16&lt;/code&gt; provides an initializer. Extending existing types to provide initializers that accept new types (including your own type definitions) is covered in Extensions. &lt;/p&gt;
&lt;h4&gt;Integer and Floating-Point Conversion&lt;/h4&gt;
&lt;p&gt;Conversions between integer and floating-point numeric types must be made explicit: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let three = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let pointOneFourOneFiveNine = 0.14159&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let pi = Double(three) + pointOneFourOneFiveNine&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// pi equals 3.14159, and is inferred to be of type Double&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, the value of the constant &lt;code&gt;three&lt;/code&gt; is used to create a new value of type &lt;code&gt;Double&lt;/code&gt;, so that both sides of the addition are of the same type. Without this conversion in place, the addition would not be allowed. &lt;/p&gt;
&lt;p&gt;Floating-point to integer conversion must also be made explicit. An integer type can be initialized with a &lt;code&gt;Double&lt;/code&gt; or &lt;code&gt;Float&lt;/code&gt; value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let integerPi = Int(pi)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// integerPi equals 3, and is inferred to be of type Int&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Floating-point values are always truncated when used to initialize a new integer value in this way. This means that &lt;code&gt;4.75&lt;/code&gt; becomes &lt;code&gt;4&lt;/code&gt;, and &lt;code&gt;-3.9&lt;/code&gt; becomes &lt;code&gt;-3&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The rules for combining numeric constants and variables are different from the rules for numeric literals. The literal value &lt;code&gt;3&lt;/code&gt; can be added directly to the literal value &lt;code&gt;0.14159&lt;/code&gt;, because number literals do not have an explicit type in and of themselves. Their type is inferred only at the point that they are evaluated by the compiler. &lt;/p&gt;
&lt;h3&gt;Type Aliases&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Type aliases&lt;/em&gt; define an alternative name for an existing type. You define type aliases with the &lt;code&gt;typealias&lt;/code&gt; keyword. &lt;/p&gt;
&lt;p&gt;Type aliases are useful when you want to refer to an existing type by a name that is contextually more appropriate, such as when working with data of a specific size from an external source: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;typealias AudioSample = UInt16&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Once you define a type alias, you can use the alias anywhere you might use the original name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var maxAmplitudeFound = AudioSample.min&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// maxAmplitudeFound is now 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, &lt;code&gt;AudioSample&lt;/code&gt; is defined as an alias for &lt;code&gt;UInt16&lt;/code&gt;. Because it is an alias, the call to &lt;code&gt;AudioSample.min&lt;/code&gt; actually calls &lt;code&gt;UInt16.min&lt;/code&gt;, which provides an initial value of &lt;code&gt;0&lt;/code&gt; for the &lt;code&gt;maxAmplitudeFound&lt;/code&gt; variable. &lt;/p&gt;
&lt;h3&gt;Booleans&lt;/h3&gt;
&lt;p&gt;Swift has a basic &lt;em&gt;Boolean&lt;/em&gt; type, called &lt;code&gt;Bool&lt;/code&gt;. Boolean values are referred to as &lt;em&gt;logical&lt;/em&gt;, because they can only ever be true or false. Swift provides two Boolean constant values, &lt;code&gt;true&lt;/code&gt; and &lt;code&gt;false&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let orangesAreOrange = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let turnipsAreDelicious = false&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The types of &lt;code&gt;orangesAreOrange&lt;/code&gt; and &lt;code&gt;turnipsAreDelicious&lt;/code&gt; have been inferred as &lt;code&gt;Bool&lt;/code&gt; from the fact that they were initialized with Boolean literal values. As with &lt;code&gt;Int&lt;/code&gt; and &lt;code&gt;Double&lt;/code&gt; above, you don’t need to declare constants or variables as &lt;code&gt;Bool&lt;/code&gt; if you set them to &lt;code&gt;true&lt;/code&gt; or &lt;code&gt;false&lt;/code&gt; as soon as you create them. Type inference helps make Swift code more concise and readable when it initializes constants or variables with other values whose type is already known. &lt;/p&gt;
&lt;p&gt;Boolean values are particularly useful when you work with conditional statements such as the &lt;code&gt;if&lt;/code&gt; statement: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if turnipsAreDelicious {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Mmm, tasty turnips!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Eww, turnips are horrible.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Eww, turnips are horrible."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Conditional statements such as the &lt;code&gt;if&lt;/code&gt; statement are covered in more detail in Control Flow. &lt;/p&gt;
&lt;p&gt;Swift’s type safety prevents non-Boolean values from being substituted for &lt;code&gt;Bool&lt;/code&gt;. The following example reports a compile-time error: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let i = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if i {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this example will not compile, and will report an error&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;However, the alternative example below is valid: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let i = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if i == 1 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this example will compile successfully&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The result of the &lt;code&gt;i == 1&lt;/code&gt; comparison is of type &lt;code&gt;Bool&lt;/code&gt;, and so this second example passes the type-check. Comparisons like &lt;code&gt;i == 1&lt;/code&gt; are discussed in Basic Operators. &lt;/p&gt;
&lt;p&gt;As with other examples of type safety in Swift, this approach avoids accidental errors and ensures that the intention of a particular section of code is always clear. &lt;/p&gt;
&lt;h3&gt;Tuples&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Tuples&lt;/em&gt; group multiple values into a single compound value. The values within a tuple can be of any type and do not have to be of the same type as each other. &lt;/p&gt;
&lt;p&gt;In this example, &lt;code&gt;(404, "Not Found")&lt;/code&gt; is a tuple that describes an &lt;em&gt;HTTP status code&lt;/em&gt;. An HTTP status code is a special value returned by a web server whenever you request a web page. A status code of &lt;code&gt;404 Not Found&lt;/code&gt; is returned if you request a webpage that doesn’t exist. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let http404Error = (404, "Not Found")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// http404Error is of type (Int, String), and equals (404, "Not Found")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;(404, "Not Found")&lt;/code&gt; tuple groups together an &lt;code&gt;Int&lt;/code&gt; and a &lt;code&gt;String&lt;/code&gt; to give the HTTP status code two separate values: a number and a human-readable description. It can be described as “a tuple of type &lt;code&gt;(Int, String)&lt;/code&gt;”. &lt;/p&gt;
&lt;p&gt;You can create tuples from any permutation of types, and they can contain as many different types as you like. There’s nothing stopping you from having a tuple of type &lt;code&gt;(Int, Int, Int)&lt;/code&gt;, or &lt;code&gt;(String, Bool)&lt;/code&gt;, or indeed any other permutation you require. &lt;/p&gt;
&lt;p&gt;You can &lt;em&gt;decompose&lt;/em&gt; a tuple’s contents into separate constants or variables, which you then access as usual: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let (statusCode, statusMessage) = http404Error&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The status code is \\(statusCode)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The status code is 404"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The status message is \\(statusMessage)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The status message is Not Found"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you only need some of the tuple’s values, ignore parts of the tuple with an underscore (&lt;code&gt;_&lt;/code&gt;) when you decompose the tuple: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let (justTheStatusCode, _) = http404Error&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The status code is \\(justTheStatusCode)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The status code is 404"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Alternatively, access the individual element values in a tuple using index numbers starting at zero: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The status code is \\(http404Error.0)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The status code is 404"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The status message is \\(http404Error.1)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The status message is Not Found"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can name the individual elements in a tuple when the tuple is defined: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let http200Status = (statusCode: 200, description: "OK")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you name the elements in a tuple, you can use the element names to access the values of those elements: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The status code is \\(http200Status.statusCode)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The status code is 200"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The status message is \\(http200Status.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The status message is OK"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Tuples are particularly useful as the return values of functions. A function that tries to retrieve a web page might return the &lt;code&gt;(Int, String)&lt;/code&gt; tuple type to describe the success or failure of the page retrieval. By returning a tuple with two distinct values, each of a different type, the function provides more useful information about its outcome than if it could only return a single value of a single type. For more information, see Functions with Multiple Return Values. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Tuples are useful for temporary groups of related values. They are not suited to the creation of complex data structures. If your data structure is likely to persist beyond a temporary scope, model it as a class or structure, rather than as a tuple. For more information, see Classes and Structures. &lt;/p&gt;
&lt;h3&gt;Optionals&lt;/h3&gt;
&lt;p&gt;You use &lt;em&gt;optionals&lt;/em&gt; in situations where a value may be absent. An optional says: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;There &lt;em&gt;is&lt;/em&gt; a value, and it equals &lt;em&gt;x&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;em&gt;or&lt;/em&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;There &lt;em&gt;isn’t&lt;/em&gt; a value at all &lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The concept of optionals doesn’t exist in C or Objective-C. The nearest thing in Objective-C is the ability to return &lt;code&gt;nil&lt;/code&gt; from a method that would otherwise return an object, with &lt;code&gt;nil&lt;/code&gt; meaning “the absence of a valid object.” However, this only works for objects—it doesn’t work for structures, basic C types, or enumeration values. For these types, Objective-C methods typically return a special value (such as &lt;code&gt;NSNotFound&lt;/code&gt;) to indicate the absence of a value. This approach assumes that the method’s caller knows there is a special value to test against and remembers to check for it. Swift’s optionals let you indicate the absence of a value for &lt;em&gt;any type at all&lt;/em&gt;, without the need for special constants. &lt;/p&gt;
&lt;p&gt;Here’s an example of how optionals can be used to cope with the absence of a value. Swift’s &lt;code&gt;String&lt;/code&gt; type has a method called &lt;code&gt;toInt&lt;/code&gt;, which tries to convert a &lt;code&gt;String&lt;/code&gt; value into an &lt;code&gt;Int&lt;/code&gt; value. However, not every string can be converted into an integer. The string &lt;code&gt;"123"&lt;/code&gt; can be converted into the numeric value &lt;code&gt;123&lt;/code&gt;, but the string &lt;code&gt;"hello, world"&lt;/code&gt; does not have an obvious numeric value to convert to. &lt;/p&gt;
&lt;p&gt;The example below uses the &lt;code&gt;toInt&lt;/code&gt; method to try to convert a &lt;code&gt;String&lt;/code&gt; into an &lt;code&gt;Int&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let possibleNumber = "123"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let convertedNumber = possibleNumber.toInt()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// convertedNumber is inferred to be of type "Int?", or "optional Int"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because the &lt;code&gt;toInt&lt;/code&gt; method might fail, it returns an &lt;em&gt;optional&lt;/em&gt; &lt;code&gt;Int&lt;/code&gt;, rather than an &lt;code&gt;Int&lt;/code&gt;. An optional &lt;code&gt;Int&lt;/code&gt; is written as &lt;code&gt;Int?&lt;/code&gt;, not &lt;code&gt;Int&lt;/code&gt;. The question mark indicates that the value it contains is optional, meaning that it might contain &lt;em&gt;some&lt;/em&gt; &lt;code&gt;Int&lt;/code&gt; value, or it might contain &lt;em&gt;no value at all&lt;/em&gt;. (It can’t contain anything else, such as a &lt;code&gt;Bool&lt;/code&gt; value or a &lt;code&gt;String&lt;/code&gt; value. It’s either an &lt;code&gt;Int&lt;/code&gt;, or it’s nothing at all.) &lt;/p&gt;
&lt;h4&gt;nil&lt;/h4&gt;
&lt;p&gt;You set an optional variable to a valueless state by assigning it the special value &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var serverResponseCode: Int? = 404&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// serverResponseCode contains an actual Int value of 404&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;serverResponseCode = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// serverResponseCode now contains no value&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;&lt;code&gt;nil&lt;/code&gt; cannot be used with nonoptional constants and variables. If a constant or variable in your code needs to work with the absence of a value under certain conditions, always declare it as an optional value of the appropriate type. &lt;/p&gt;
&lt;p&gt;If you define an optional constant or variable without providing a default value, the constant or variable is automatically set to &lt;code&gt;nil&lt;/code&gt; for you: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var surveyAnswer: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// surveyAnswer is automatically set to nil&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift’s &lt;code&gt;nil&lt;/code&gt; is not the same as &lt;code&gt;nil&lt;/code&gt; in Objective-C. In Objective-C, &lt;code&gt;nil&lt;/code&gt; is a pointer to a nonexistent object. In Swift, &lt;code&gt;nil&lt;/code&gt; is not a pointer—it is the absence of a value of a certain type. Optionals of &lt;em&gt;any&lt;/em&gt; type can be set to &lt;code&gt;nil&lt;/code&gt;, not just object types. &lt;/p&gt;
&lt;h4&gt;If Statements and Forced Unwrapping&lt;/h4&gt;
&lt;p&gt;You can use an &lt;code&gt;if&lt;/code&gt; statement to find out whether an optional contains a value by comparing the optional against &lt;code&gt;nil&lt;/code&gt;. You perform this comparison with the “equal to” operator (&lt;code&gt;==&lt;/code&gt;) or the “not equal to” operator (&lt;code&gt;!=&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;If an optional has a value, it is considered to be “not equal to” &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if convertedNumber != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("convertedNumber contains some integer value.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "convertedNumber contains some integer value."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Once you’re sure that the optional &lt;em&gt;does&lt;/em&gt; contain a value, you can access its underlying value by adding an exclamation mark (&lt;code&gt;!&lt;/code&gt;) to the end of the optional’s name. The exclamation mark effectively says, “I know that this optional definitely has a value; please use it.” This is known as &lt;em&gt;forced unwrapping&lt;/em&gt; of the optional’s value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if convertedNumber != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("convertedNumber has an integer value of \\(convertedNumber!).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "convertedNumber has an integer value of 123."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more on the &lt;code&gt;if&lt;/code&gt; statement, see Control Flow. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Trying to use &lt;code&gt;!&lt;/code&gt; to access a non-existent optional value triggers a runtime error. Always make sure that an optional contains a non-&lt;code&gt;nil&lt;/code&gt; value before using &lt;code&gt;!&lt;/code&gt; to force-unwrap its value. &lt;/p&gt;
&lt;h4&gt;Optional Binding&lt;/h4&gt;
&lt;p&gt;You use &lt;em&gt;optional binding&lt;/em&gt; to find out whether an optional contains a value, and if so, to make that value available as a temporary constant or variable. Optional binding can be used with &lt;code&gt;if&lt;/code&gt; and &lt;code&gt;while&lt;/code&gt; statements to check for a value inside an optional, and to extract that value into a constant or variable, as part of a single action. &lt;code&gt;if&lt;/code&gt; and &lt;code&gt;while&lt;/code&gt; statements are described in more detail in Control Flow. &lt;/p&gt;
&lt;p&gt;Write optional bindings for the &lt;code&gt;if&lt;/code&gt; statement as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;if let &lt;em&gt;constantName&lt;/em&gt; = &lt;em&gt;someOptional&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can rewrite the &lt;code&gt;possibleNumber&lt;/code&gt; example from the Optionals section to use optional binding rather than forced unwrapping: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let actualNumber = possibleNumber.toInt() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\'\\(possibleNumber)\' has an integer value of \\(actualNumber)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\'\\(possibleNumber)\' could not be converted to an integer")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "'123' has an integer value of 123"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This code can be read as: &lt;/p&gt;
&lt;p&gt;“If the optional &lt;code&gt;Int&lt;/code&gt; returned by &lt;code&gt;possibleNumber.toInt&lt;/code&gt; contains a value, set a new constant called &lt;code&gt;actualNumber&lt;/code&gt; to the value contained in the optional.” &lt;/p&gt;
&lt;p&gt;If the conversion is successful, the &lt;code&gt;actualNumber&lt;/code&gt; constant becomes available for use within the first branch of the &lt;code&gt;if&lt;/code&gt; statement. It has already been initialized with the value contained &lt;em&gt;within&lt;/em&gt; the optional, and so there is no need to use the &lt;code&gt;!&lt;/code&gt; suffix to access its value. In this example, &lt;code&gt;actualNumber&lt;/code&gt; is simply used to print the result of the conversion. &lt;/p&gt;
&lt;p&gt;You can use both constants and variables with optional binding. If you wanted to manipulate the value of &lt;code&gt;actualNumber&lt;/code&gt; within the first branch of the &lt;code&gt;if&lt;/code&gt; statement, you could write &lt;code&gt;if var actualNumber&lt;/code&gt; instead, and the value contained within the optional would be made available as a variable rather than a constant. &lt;/p&gt;
&lt;h4&gt;Implicitly Unwrapped Optionals&lt;/h4&gt;
&lt;p&gt;As described above, optionals indicate that a constant or variable is allowed to have “no value”. Optionals can be checked with an &lt;code&gt;if&lt;/code&gt; statement to see if a value exists, and can be conditionally unwrapped with optional binding to access the optional’s value if it does exist. &lt;/p&gt;
&lt;p&gt;Sometimes it is clear from a program’s structure that an optional will &lt;em&gt;always&lt;/em&gt; have a value, after that value is first set. In these cases, it is useful to remove the need to check and unwrap the optional’s value every time it is accessed, because it can be safely assumed to have a value all of the time. &lt;/p&gt;
&lt;p&gt;These kinds of optionals are defined as &lt;em&gt;implicitly unwrapped optionals&lt;/em&gt;. You write an implicitly unwrapped optional by placing an exclamation mark (&lt;code&gt;String!&lt;/code&gt;) rather than a question mark (&lt;code&gt;String?&lt;/code&gt;) after the type that you want to make optional. &lt;/p&gt;
&lt;p&gt;Implicitly unwrapped optionals are useful when an optional’s value is confirmed to exist immediately after the optional is first defined and can definitely be assumed to exist at every point thereafter. The primary use of implicitly unwrapped optionals in Swift is during class initialization, as described in Unowned References and Implicitly Unwrapped Optional Properties. &lt;/p&gt;
&lt;p&gt;An implicitly unwrapped optional is a normal optional behind the scenes, but can also be used like a nonoptional value, without the need to unwrap the optional value each time it is accessed. The following example shows the difference in behavior between an optional string and an implicitly unwrapped optional string when accessing their wrapped value as an explicit &lt;code&gt;String&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let possibleString: String? = "An optional string."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let forcedString: String = possibleString! // requires an exclamation mark&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let assumedString: String! = "An implicitly unwrapped optional string."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let implicitString: String = assumedString // no need for an exclamation mark&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can think of an implicitly unwrapped optional as giving permission for the optional to be unwrapped automatically whenever it is used. Rather than placing an exclamation mark after the optional’s name each time you use it, you place an exclamation mark after the optional’s type when you declare it. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you try to access an implicitly unwrapped optional when it does not contain a value, you will trigger a runtime error. The result is exactly the same as if you place an exclamation mark after a normal optional that does not contain a value. &lt;/p&gt;
&lt;p&gt;You can still treat an implicitly unwrapped optional like a normal optional, to check if it contains a value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if assumedString != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(assumedString)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "An implicitly unwrapped optional string."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also use an implicitly unwrapped optional with optional binding, to check and unwrap its value in a single statement: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let definiteString = assumedString {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(definiteString)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "An implicitly unwrapped optional string."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Do not use an implicitly unwrapped optional when there is a possibility of a variable becoming &lt;code&gt;nil&lt;/code&gt; at a later point. Always use a normal optional type if you need to check for a &lt;code&gt;nil&lt;/code&gt; value during the lifetime of a variable. &lt;/p&gt;
&lt;h3&gt;Assertions&lt;/h3&gt;
&lt;p&gt;Optionals enable you to check for values that may or may not exist, and to write code that copes gracefully with the absence of a value. In some cases, however, it is simply not possible for your code to continue execution if a value does not exist, or if a provided value does not satisfy certain conditions. In these situations, you can trigger an &lt;em&gt;assertion&lt;/em&gt; in your code to end code execution and to provide an opportunity to debug the cause of the absent or invalid value. &lt;/p&gt;
&lt;h4&gt;Debugging with Assertions&lt;/h4&gt;
&lt;p&gt;An assertion is a runtime check that a logical condition definitely evaluates to &lt;code&gt;true&lt;/code&gt;. Literally put, an assertion “asserts” that a condition is true. You use an assertion to make sure that an essential condition is satisfied before executing any further code. If the condition evaluates to &lt;code&gt;true&lt;/code&gt;, code execution continues as usual; if the condition evaluates to &lt;code&gt;false&lt;/code&gt;, code execution ends, and your app is terminated. &lt;/p&gt;
&lt;p&gt;If your code triggers an assertion while running in a debug environment, such as when you build and run an app in Xcode, you can see exactly where the invalid state occurred and query the state of your app at the time that the assertion was triggered. An assertion also lets you provide a suitable debug message as to the nature of the assert. &lt;/p&gt;
&lt;p&gt;You write an assertion by calling the global &lt;code&gt;assert&lt;/code&gt; function. You pass the &lt;code&gt;assert&lt;/code&gt; function an expression that evaluates to &lt;code&gt;true&lt;/code&gt; or &lt;code&gt;false&lt;/code&gt; and a message that should be displayed if the result of the condition is &lt;code&gt;false&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let age = -3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;assert(age &amp;gt;= 0, "A person's age cannot be less than zero")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this causes the assertion to trigger, because age is not &amp;gt;= 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this example, code execution will continue only if &lt;code&gt;age &amp;gt;= 0&lt;/code&gt; evaluates to &lt;code&gt;true&lt;/code&gt;, that is, if the value of &lt;code&gt;age&lt;/code&gt; is non-negative. If the value of &lt;code&gt;age&lt;/code&gt; &lt;em&gt;is&lt;/em&gt; negative, as in the code above, then &lt;code&gt;age &amp;gt;= 0&lt;/code&gt; evaluates to &lt;code&gt;false&lt;/code&gt;, and the assertion is triggered, terminating the application. &lt;/p&gt;
&lt;p&gt;The assertion message can be omitted if desired, as in the following example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;assert(age &amp;gt;= 0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;When to Use Assertions&lt;/h4&gt;
&lt;p&gt;Use an assertion whenever a condition has the potential to be false, but must &lt;em&gt;definitely&lt;/em&gt; be true in order for your code to continue execution. Suitable scenarios for an assertion check include: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;An integer subscript index is passed to a custom subscript implementation, but the subscript index value could be too low or too high. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A value is passed to a function, but an invalid value means that the function cannot fulfill its task. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;An optional value is currently &lt;code&gt;nil&lt;/code&gt;, but a non-&lt;code&gt;nil&lt;/code&gt; value is essential for subsequent code to execute successfully. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;See also Subscripts and Functions. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Assertions cause your app to terminate and are not a substitute for designing your code in such a way that invalid conditions are unlikely to arise. Nonetheless, in situations where invalid conditions are possible, an assertion is an effective way to ensure that such conditions are highlighted and noticed during development, before your app is published. &lt;/p&gt;
&lt;h2&gt;Basic Operators&lt;/h2&gt;
&lt;p&gt;An &lt;em&gt;operator&lt;/em&gt; is a special symbol or phrase that you use to check, change, or combine values. For example, the addition operator (&lt;code&gt;+&lt;/code&gt;) adds two numbers together (as in &lt;code&gt;let i = 1 + 2&lt;/code&gt;). More complex examples include the logical AND operator &lt;code&gt;&amp;amp;&amp;amp;&lt;/code&gt; (as in &lt;code&gt;if enteredDoorCode &amp;amp;&amp;amp; passedRetinaScan&lt;/code&gt;) and the increment operator &lt;code&gt;++i&lt;/code&gt;, which is a shortcut to increase the value of &lt;code&gt;i&lt;/code&gt; by &lt;code&gt;1&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Swift supports most standard C operators and improves several capabilities to eliminate common coding errors. The assignment operator (&lt;code&gt;=&lt;/code&gt;) does not return a value, to prevent it from being mistakenly used when the equal to operator (&lt;code&gt;==&lt;/code&gt;) is intended. Arithmetic operators (&lt;code&gt;+&lt;/code&gt;, &lt;code&gt;-&lt;/code&gt;, &lt;code&gt;*&lt;/code&gt;, &lt;code&gt;/&lt;/code&gt;, &lt;code&gt;%&lt;/code&gt; and so forth) detect and disallow value overflow, to avoid unexpected results when working with numbers that become larger or smaller than the allowed value range of the type that stores them. You can opt in to value overflow behavior by using Swift’s overflow operators, as described in Overflow Operators. &lt;/p&gt;
&lt;p&gt;Unlike C, Swift lets you perform remainder (&lt;code&gt;%&lt;/code&gt;) calculations on floating-point numbers. Swift also provides two range operators (&lt;code&gt;a..&amp;lt;b&lt;/code&gt; and &lt;code&gt;a...b&lt;/code&gt;) not found in C, as a shortcut for expressing a range of values. &lt;/p&gt;
&lt;p&gt;This chapter describes the common operators in Swift. Advanced Operators covers Swift’s advanced operators, and describes how to define your own custom operators and implement the standard operators for your own custom types. &lt;/p&gt;
&lt;h3&gt;Terminology&lt;/h3&gt;
&lt;p&gt;Operators are unary, binary, or ternary: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;Unary&lt;/em&gt; operators operate on a single target (such as &lt;code&gt;-a&lt;/code&gt;). Unary &lt;em&gt;prefix&lt;/em&gt; operators appear immediately before their target (such as &lt;code&gt;!b&lt;/code&gt;), and unary &lt;em&gt;postfix&lt;/em&gt; operators appear immediately after their target (such as &lt;code&gt;i++&lt;/code&gt;). &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;Binary&lt;/em&gt; operators operate on two targets (such as &lt;code&gt;2 + 3&lt;/code&gt;) and are &lt;em&gt;infix&lt;/em&gt; because they appear in between their two targets. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;Ternary&lt;/em&gt; operators operate on three targets. Like C, Swift has only one ternary operator, the ternary conditional operator (&lt;code&gt;a ? b : c&lt;/code&gt;). &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The values that operators affect are &lt;em&gt;operands&lt;/em&gt;. In the expression &lt;code&gt;1 + 2&lt;/code&gt;, the &lt;code&gt;+&lt;/code&gt; symbol is a binary operator and its two operands are the values &lt;code&gt;1&lt;/code&gt; and &lt;code&gt;2&lt;/code&gt;. &lt;/p&gt;
&lt;h3&gt;Assignment Operator&lt;/h3&gt;
&lt;p&gt;The &lt;em&gt;assignment operator&lt;/em&gt; (&lt;code&gt;a = b&lt;/code&gt;) initializes or updates the value of &lt;code&gt;a&lt;/code&gt; with the value of &lt;code&gt;b&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let b = 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var a = 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a = b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a is now equal to 10&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the right side of the assignment is a tuple with multiple values, its elements can be decomposed into multiple constants or variables at once: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let (x, y) = (1, 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// x is equal to 1, and y is equal to 2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unlike the assignment operator in C and Objective-C, the assignment operator in Swift does not itself return a value. The following statement is not valid: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if x = y {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this is not valid, because x = y does not return a value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This feature prevents the assignment operator (&lt;code&gt;=&lt;/code&gt;) from being used by accident when the equal to operator (&lt;code&gt;==&lt;/code&gt;) is actually intended. By making &lt;code&gt;if x = y&lt;/code&gt; invalid, Swift helps you to avoid these kinds of errors in your code. &lt;/p&gt;
&lt;h3&gt;Arithmetic Operators&lt;/h3&gt;
&lt;p&gt;Swift supports the four standard &lt;em&gt;arithmetic operators&lt;/em&gt; for all number types: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Addition (&lt;code&gt;+&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Subtraction (&lt;code&gt;-&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Multiplication (&lt;code&gt;*&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Division (&lt;code&gt;/&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;1 \+ 2 // equals 3&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;code&gt;5 \- 3 // equals 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;2 * 3 // equals 6&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;10.0 / 2.5 // equals 4.0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unlike the arithmetic operators in C and Objective-C, the Swift arithmetic operators do not allow values to overflow by default. You can opt in to value overflow behavior by using Swift’s overflow operators (such as &lt;code&gt;a &amp;amp;\+ b&lt;/code&gt;). See Overflow Operators. &lt;/p&gt;
&lt;p&gt;The addition operator is also supported for &lt;code&gt;String&lt;/code&gt; concatenation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;"hello, " \+ "world" // equals "hello, world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Remainder Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;remainder operator&lt;/em&gt; (&lt;code&gt;a % b&lt;/code&gt;) works out how many multiples of &lt;code&gt;b&lt;/code&gt; will fit inside &lt;code&gt;a&lt;/code&gt; and returns the value that is left over (known as the &lt;em&gt;remainder&lt;/em&gt;). &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The remainder operator (&lt;code&gt;%&lt;/code&gt;) is also known as a &lt;em&gt;modulo operator&lt;/em&gt; in other languages. However, its behavior in Swift for negative numbers means that it is, strictly speaking, a remainder rather than a modulo operation. &lt;/p&gt;
&lt;p&gt;Here’s how the remainder operator works. To calculate &lt;code&gt;9 % 4&lt;/code&gt;, you first work out how many &lt;code&gt;4&lt;/code&gt;s will fit inside &lt;code&gt;9&lt;/code&gt;: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/remainderInteger_2x.png" src="http://muxuezi.github.io/posts/Art/remainderInteger_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;You can fit two &lt;code&gt;4&lt;/code&gt;s inside &lt;code&gt;9&lt;/code&gt;, and the remainder is &lt;code&gt;1&lt;/code&gt; (shown in orange). &lt;/p&gt;
&lt;p&gt;In Swift, this would be written as: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;9 % 4 // equals 1&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To determine the answer for &lt;code&gt;a % b&lt;/code&gt;, the &lt;code&gt;%&lt;/code&gt; operator calculates the following equation and returns &lt;code&gt;remainder&lt;/code&gt; as its output: &lt;/p&gt;
&lt;p&gt;&lt;code&gt;a&lt;/code&gt; = (&lt;code&gt;b&lt;/code&gt; × &lt;code&gt;some multiplier&lt;/code&gt;) + &lt;code&gt;remainder&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;where &lt;code&gt;some multiplier&lt;/code&gt; is the largest number of multiples of &lt;code&gt;b&lt;/code&gt; that will fit inside &lt;code&gt;a&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Inserting &lt;code&gt;9&lt;/code&gt; and &lt;code&gt;4&lt;/code&gt; into this equation yields: &lt;/p&gt;
&lt;p&gt;&lt;code&gt;9&lt;/code&gt; = (&lt;code&gt;4&lt;/code&gt; × &lt;code&gt;2&lt;/code&gt;) + &lt;code&gt;1&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;The same method is applied when calculating the remainder for a negative value of &lt;code&gt;a&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;-9 % 4 // equals -1&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Inserting &lt;code&gt;-9&lt;/code&gt; and &lt;code&gt;4&lt;/code&gt; into the equation yields: &lt;/p&gt;
&lt;p&gt;&lt;code&gt;-9&lt;/code&gt; = (&lt;code&gt;4&lt;/code&gt; × &lt;code&gt;-2&lt;/code&gt;) + &lt;code&gt;-1&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;giving a remainder value of &lt;code&gt;-1&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The sign of &lt;code&gt;b&lt;/code&gt; is ignored for negative values of &lt;code&gt;b&lt;/code&gt;. This means that &lt;code&gt;a % b&lt;/code&gt; and &lt;code&gt;a % -b&lt;/code&gt; always give the same answer. &lt;/p&gt;
&lt;h4&gt;Floating-Point Remainder Calculations&lt;/h4&gt;
&lt;p&gt;Unlike the remainder operator in C and Objective-C, Swift’s remainder operator can also operate on floating-point numbers: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;8 % 2.5 // equals 0.5&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this example, &lt;code&gt;8&lt;/code&gt; divided by &lt;code&gt;2.5&lt;/code&gt; equals &lt;code&gt;3&lt;/code&gt;, with a remainder of &lt;code&gt;0.5&lt;/code&gt;, so the remainder operator returns a &lt;code&gt;Double&lt;/code&gt; value of &lt;code&gt;0.5&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/remainderFloat_2x.png" src="http://muxuezi.github.io/posts/Art/remainderFloat_2x.png"&gt;&lt;/p&gt;
&lt;h4&gt;Increment and Decrement Operators&lt;/h4&gt;
&lt;p&gt;Like C, Swift provides an &lt;em&gt;increment operator&lt;/em&gt; (&lt;code&gt;++&lt;/code&gt;) and a &lt;em&gt;decrement operator&lt;/em&gt; (&lt;code&gt;\--&lt;/code&gt;) as a shortcut to increase or decrease the value of a numeric variable by &lt;code&gt;1&lt;/code&gt;. You can use these operators with variables of any integer or floating-point type. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var i = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;++i // i now equals 1&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Each time you call &lt;code&gt;++i&lt;/code&gt;, the value of &lt;code&gt;i&lt;/code&gt; is increased by &lt;code&gt;1&lt;/code&gt;. Essentially, &lt;code&gt;++i&lt;/code&gt; is shorthand for saying &lt;code&gt;i = i + 1&lt;/code&gt;. Likewise, &lt;code&gt;\--i&lt;/code&gt; can be used as shorthand for &lt;code&gt;i = i - 1&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;++&lt;/code&gt; and &lt;code&gt;\--&lt;/code&gt; symbols can be used as prefix operators or as postfix operators. &lt;code&gt;++i&lt;/code&gt; and &lt;code&gt;i++&lt;/code&gt; are both valid ways to increase the value of &lt;code&gt;i&lt;/code&gt; by &lt;code&gt;1&lt;/code&gt;. Similarly, &lt;code&gt;\--i&lt;/code&gt; and &lt;code&gt;i--&lt;/code&gt; are both valid ways to decrease the value of &lt;code&gt;i&lt;/code&gt; by &lt;code&gt;1&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note that these operators modify &lt;code&gt;i&lt;/code&gt; and also return a value. If you only want to increment or decrement the value stored in &lt;code&gt;i&lt;/code&gt;, you can ignore the returned value. However, if you &lt;em&gt;do&lt;/em&gt; use the returned value, it will be different based on whether you used the prefix or postfix version of the operator, according to the following rules: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If the operator is written &lt;em&gt;before&lt;/em&gt; the variable, it increments the variable &lt;em&gt;before&lt;/em&gt; returning its value. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If the operator is written &lt;em&gt;after&lt;/em&gt; the variable, it increments the variable &lt;em&gt;after&lt;/em&gt; returning its value. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var a = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let b = ++a&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a and b are now both equal to 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let c = a++&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a is now equal to 2, but c has been set to the pre-increment value of 1&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the example above, &lt;code&gt;let b = ++a&lt;/code&gt; increments &lt;code&gt;a&lt;/code&gt; &lt;em&gt;before&lt;/em&gt; returning its value. This is why both &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt; are equal to to the new value of &lt;code&gt;1&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;However, &lt;code&gt;let c = a++&lt;/code&gt; increments &lt;code&gt;a&lt;/code&gt; &lt;em&gt;after&lt;/em&gt; returning its value. This means that &lt;code&gt;c&lt;/code&gt; gets the old value of &lt;code&gt;1&lt;/code&gt;, and &lt;code&gt;a&lt;/code&gt; is then updated to equal &lt;code&gt;2&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Unless you need the specific behavior of &lt;code&gt;i++&lt;/code&gt;, it is recommended that you use &lt;code&gt;++i&lt;/code&gt; and &lt;code&gt;\--i&lt;/code&gt; in all cases, because they have the typical expected behavior of modifying &lt;code&gt;i&lt;/code&gt; and returning the result. &lt;/p&gt;
&lt;h4&gt;Unary Minus Operator&lt;/h4&gt;
&lt;p&gt;The sign of a numeric value can be toggled using a prefixed &lt;code&gt;-&lt;/code&gt;, known as the &lt;em&gt;unary minus operator&lt;/em&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let three = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let minusThree = -three // minusThree equals -3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let plusThree = -minusThree // plusThree equals 3, or "minus minus three"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The unary minus operator (&lt;code&gt;-&lt;/code&gt;) is prepended directly before the value it operates on, without any white space. &lt;/p&gt;
&lt;h4&gt;Unary Plus Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;unary plus operator&lt;/em&gt; (&lt;code&gt;+&lt;/code&gt;) simply returns the value it operates on, without any change: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let minusSix = -6&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let alsoMinusSix = +minusSix // alsoMinusSix equals -6&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Although the unary plus operator doesn’t actually do anything, you can use it to provide symmetry in your code for positive numbers when also using the unary minus operator for negative numbers. &lt;/p&gt;
&lt;h3&gt;Compound Assignment Operators&lt;/h3&gt;
&lt;p&gt;Like C, Swift provides &lt;em&gt;compound assignment operators&lt;/em&gt; that combine assignment (&lt;code&gt;=&lt;/code&gt;) with another operation. One example is the &lt;em&gt;addition assignment operator&lt;/em&gt; (&lt;code&gt;+=&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var a = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a += 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a is now equal to 3&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The expression &lt;code&gt;a += 2&lt;/code&gt; is shorthand for &lt;code&gt;a = a + 2&lt;/code&gt;. Effectively, the addition and the assignment are combined into one operator that performs both tasks at the same time. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The compound assignment operators do not return a value. You cannot write &lt;code&gt;let b = a += 2&lt;/code&gt;, for example. This behavior is different from the increment and decrement operators mentioned above. &lt;/p&gt;
&lt;p&gt;A complete list of compound assignment operators can be found in Expressions. &lt;/p&gt;
&lt;h3&gt;Comparison Operators&lt;/h3&gt;
&lt;p&gt;Swift supports all standard C &lt;em&gt;comparison operators&lt;/em&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Equal to (&lt;code&gt;a == b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Not equal to (&lt;code&gt;a != b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Greater than (&lt;code&gt;a &amp;gt; b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Less than (&lt;code&gt;a &amp;lt; b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Greater than or equal to (&lt;code&gt;a &amp;gt;= b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Less than or equal to (&lt;code&gt;a &amp;lt;= b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift also provides two &lt;em&gt;identity operators&lt;/em&gt; (&lt;code&gt;===&lt;/code&gt; and &lt;code&gt;!==&lt;/code&gt;), which you use to test whether two object references both refer to the same object instance. For more information, see Classes and Structures. &lt;/p&gt;
&lt;p&gt;Each of the comparison operators returns a &lt;code&gt;Bool&lt;/code&gt; value to indicate whether or not the statement is true: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;1 == 1 // true, because 1 is equal to 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;2 != 1 // true, because 2 is not equal to 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;2 &amp;gt; 1 // true, because 2 is greater than 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;1 &amp;lt; 2 // true, because 1 is less than 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;1 &amp;gt;= 1 // true, because 1 is greater than or equal to 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;2 &amp;lt;= 1 // false, because 2 is not less than or equal to 1&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Comparison operators are often used in conditional statements, such as the &lt;code&gt;if&lt;/code&gt; statement: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let name = "world"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if name == "world" {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("hello, world")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("I'm sorry \\(name), but I don't recognize you")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "hello, world", because name is indeed equal to "world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more on the &lt;code&gt;if&lt;/code&gt; statement, see Control Flow. &lt;/p&gt;
&lt;h3&gt;Ternary Conditional Operator&lt;/h3&gt;
&lt;p&gt;The &lt;em&gt;ternary conditional operator&lt;/em&gt; is a special operator with three parts, which takes the form &lt;code&gt;question ? answer1 : answer2&lt;/code&gt;. It is a shortcut for evaluating one of two expressions based on whether &lt;code&gt;question&lt;/code&gt; is true or false. If &lt;code&gt;question&lt;/code&gt; is true, it evaluates &lt;code&gt;answer1&lt;/code&gt; and returns its value; otherwise, it evaluates &lt;code&gt;answer2&lt;/code&gt; and returns its value. &lt;/p&gt;
&lt;p&gt;The ternary conditional operator is shorthand for the code below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if question {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;answer1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;answer2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s an example, which calculates the height for a table row. The row height should be 50 points taller than the content height if the row has a header, and 20 points taller if the row doesn’t have a header: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let contentHeight = 40&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let hasHeader = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let rowHeight = contentHeight \+ (hasHeader ? 50 : 20)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// rowHeight is equal to 90&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The preceding example is shorthand for the code below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let contentHeight = 40&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let hasHeader = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var rowHeight = contentHeight&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if hasHeader {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;rowHeight = rowHeight \+ 50&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;rowHeight = rowHeight \+ 20&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// rowHeight is equal to 90&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The first example’s use of the ternary conditional operator means that &lt;code&gt;rowHeight&lt;/code&gt; can be set to the correct value on a single line of code. This is more concise than the second example, and removes the need for &lt;code&gt;rowHeight&lt;/code&gt; to be a variable, because its value does not need to be modified within an &lt;code&gt;if&lt;/code&gt; statement. &lt;/p&gt;
&lt;p&gt;The ternary conditional operator provides an efficient shorthand for deciding which of two expressions to consider. Use the ternary conditional operator with care, however. Its conciseness can lead to hard-to-read code if overused. Avoid combining multiple instances of the ternary conditional operator into one compound statement. &lt;/p&gt;
&lt;h3&gt;Nil Coalescing Operator&lt;/h3&gt;
&lt;p&gt;The &lt;em&gt;nil coalescing operator&lt;/em&gt; (&lt;code&gt;a ?? b&lt;/code&gt;) unwraps an optional &lt;code&gt;a&lt;/code&gt; if it contains a value, or returns a default value &lt;code&gt;b&lt;/code&gt; if &lt;code&gt;a&lt;/code&gt; is &lt;code&gt;nil&lt;/code&gt;. The expression &lt;code&gt;a&lt;/code&gt; is always of an optional type. The expression &lt;code&gt;b&lt;/code&gt; must match the type that is stored inside &lt;code&gt;a&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The nil coalescing operator is shorthand for the code below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;a != nil ? a! : b&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The code above uses the ternary conditional operator and forced unwrapping (&lt;code&gt;a!&lt;/code&gt;) to access the value wrapped inside &lt;code&gt;a&lt;/code&gt; when &lt;code&gt;a&lt;/code&gt; is not &lt;code&gt;nil&lt;/code&gt;, and to return &lt;code&gt;b&lt;/code&gt; otherwise. The nil coalescing operator provides a more elegant way to encapsulate this conditional checking and unwrapping in a concise and readable form. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If the value of &lt;code&gt;a&lt;/code&gt; is non-&lt;code&gt;nil&lt;/code&gt;, the value of &lt;code&gt;b&lt;/code&gt; is not evaluated. This is known as &lt;em&gt;short-circuit evaluation&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;The example below uses the nil coalescing operator to choose between a default color name and an optional user-defined color name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let defaultColorName = "red"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var userDefinedColorName: String? // defaults to nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var colorNameToUse = userDefinedColorName ?? defaultColorName&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// userDefinedColorName is nil, so colorNameToUse is set to the default of "red"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;userDefinedColorName&lt;/code&gt; variable is defined as an optional &lt;code&gt;String&lt;/code&gt;, with a default value of &lt;code&gt;nil&lt;/code&gt;. Because &lt;code&gt;userDefinedColorName&lt;/code&gt; is of an optional type, you can use the nil coalescing operator to consider its value. In the example above, the operator is used to determine an initial value for a &lt;code&gt;String&lt;/code&gt; variable called &lt;code&gt;colorNameToUse&lt;/code&gt;. Because &lt;code&gt;userDefinedColorName&lt;/code&gt; is &lt;code&gt;nil&lt;/code&gt;, the expression &lt;code&gt;userDefinedColorName ?? defaultColorName&lt;/code&gt; returns the value of &lt;code&gt;defaultColorName&lt;/code&gt;, or &lt;code&gt;"red"&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If you assign a non-&lt;code&gt;nil&lt;/code&gt; value to &lt;code&gt;userDefinedColorName&lt;/code&gt; and perform the nil coalescing operator check again, the value wrapped inside &lt;code&gt;userDefinedColorName&lt;/code&gt; is used instead of the default: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;userDefinedColorName = "green"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;colorNameToUse = userDefinedColorName ?? defaultColorName&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// userDefinedColorName is not nil, so colorNameToUse is set to "green"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Range Operators&lt;/h3&gt;
&lt;p&gt;Swift includes two &lt;em&gt;range operators&lt;/em&gt;, which are shortcuts for expressing a range of values. &lt;/p&gt;
&lt;h4&gt;Closed Range Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;closed range operator&lt;/em&gt; (&lt;code&gt;a...b&lt;/code&gt;) defines a range that runs from &lt;code&gt;a&lt;/code&gt; to &lt;code&gt;b&lt;/code&gt;, and includes the values &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt;. The value of &lt;code&gt;a&lt;/code&gt; must not be greater than &lt;code&gt;b&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The closed range operator is useful when iterating over a range in which you want all of the values to be used, such as with a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for index in 1...5 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(index) times 5 is \\(index * 5)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 1 times 5 is 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 2 times 5 is 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 3 times 5 is 15&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 4 times 5 is 20&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 5 times 5 is 25&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more on &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loops, see Control Flow. &lt;/p&gt;
&lt;h4&gt;Half-Open Range Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;half-open range operator&lt;/em&gt; (&lt;code&gt;a..&amp;lt;b&lt;/code&gt;) defines a range that runs from &lt;code&gt;a&lt;/code&gt; to &lt;code&gt;b&lt;/code&gt;, but does not include &lt;code&gt;b&lt;/code&gt;. It is said to be &lt;em&gt;half-open&lt;/em&gt; because it contains its first value, but not its final value. As with the closed range operator, the value of &lt;code&gt;a&lt;/code&gt; must not be greater than &lt;code&gt;b&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Half-open ranges are particularly useful when you work with zero-based lists such as arrays, where it is useful to count up to (but not including) the length of the list: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let names = ["Anna", "Alex", "Brian", "Jack"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let count = names.count&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for i in 0..&amp;lt;count {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Person \\(i \+ 1) is called \\(names[i])")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Person 1 is called Anna&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Person 2 is called Alex&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Person 3 is called Brian&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Person 4 is called Jack&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the array contains four items, but &lt;code&gt;0..&amp;lt;count&lt;/code&gt; only counts as far as &lt;code&gt;3&lt;/code&gt; (the index of the last item in the array), because it is a half-open range. For more on arrays, see Arrays. &lt;/p&gt;
&lt;h3&gt;Logical Operators&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Logical operators&lt;/em&gt; modify or combine the Boolean logic values &lt;code&gt;true&lt;/code&gt; and &lt;code&gt;false&lt;/code&gt;. Swift supports the three standard logical operators found in C-based languages: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Logical NOT (&lt;code&gt;!a&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Logical AND (&lt;code&gt;a &amp;amp;&amp;amp; b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Logical OR (&lt;code&gt;a || b&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Logical NOT Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;logical NOT operator&lt;/em&gt; (&lt;code&gt;!a&lt;/code&gt;) inverts a Boolean value so that &lt;code&gt;true&lt;/code&gt; becomes &lt;code&gt;false&lt;/code&gt;, and &lt;code&gt;false&lt;/code&gt; becomes &lt;code&gt;true&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The logical NOT operator is a prefix operator, and appears immediately before the value it operates on, without any white space. It can be read as “not &lt;code&gt;a&lt;/code&gt;”, as seen in the following example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let allowedEntry = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if !allowedEntry {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("ACCESS DENIED")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "ACCESS DENIED"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The phrase &lt;code&gt;if !allowedEntry&lt;/code&gt; can be read as “if not allowed entry.” The subsequent line is only executed if “not allowed entry” is true; that is, if &lt;code&gt;allowedEntry&lt;/code&gt; is &lt;code&gt;false&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;As in this example, careful choice of Boolean constant and variable names can help to keep code readable and concise, while avoiding double negatives or confusing logic statements. &lt;/p&gt;
&lt;h4&gt;Logical AND Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;logical AND operator&lt;/em&gt; (&lt;code&gt;a &amp;amp;&amp;amp; b&lt;/code&gt;) creates logical expressions where both values must be &lt;code&gt;true&lt;/code&gt; for the overall expression to also be &lt;code&gt;true&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If either value is &lt;code&gt;false&lt;/code&gt;, the overall expression will also be &lt;code&gt;false&lt;/code&gt;. In fact, if the &lt;em&gt;first&lt;/em&gt; value is &lt;code&gt;false&lt;/code&gt;, the second value won’t even be evaluated, because it can’t possibly make the overall expression equate to &lt;code&gt;true&lt;/code&gt;. This is known as &lt;em&gt;short-circuit evaluation&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;This example considers two &lt;code&gt;Bool&lt;/code&gt; values and only allows access if both values are &lt;code&gt;true&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let enteredDoorCode = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let passedRetinaScan = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if enteredDoorCode &amp;amp;&amp;amp; passedRetinaScan {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Welcome!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("ACCESS DENIED")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "ACCESS DENIED"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Logical OR Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;logical OR operator&lt;/em&gt; (&lt;code&gt;a || b&lt;/code&gt;) is an infix operator made from two adjacent pipe characters. You use it to create logical expressions in which only &lt;em&gt;one&lt;/em&gt; of the two values has to be &lt;code&gt;true&lt;/code&gt; for the overall expression to be &lt;code&gt;true&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Like the Logical AND operator above, the Logical OR operator uses short-circuit evaluation to consider its expressions. If the left side of a Logical OR expression is &lt;code&gt;true&lt;/code&gt;, the right side is not evaluated, because it cannot change the outcome of the overall expression. &lt;/p&gt;
&lt;p&gt;In the example below, the first &lt;code&gt;Bool&lt;/code&gt; value (&lt;code&gt;hasDoorKey&lt;/code&gt;) is &lt;code&gt;false&lt;/code&gt;, but the second value (&lt;code&gt;knowsOverridePassword&lt;/code&gt;) is &lt;code&gt;true&lt;/code&gt;. Because one value is &lt;code&gt;true&lt;/code&gt;, the overall expression also evaluates to &lt;code&gt;true&lt;/code&gt;, and access is allowed: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let hasDoorKey = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let knowsOverridePassword = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if hasDoorKey || knowsOverridePassword {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Welcome!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("ACCESS DENIED")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Welcome!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Combining Logical Operators&lt;/h4&gt;
&lt;p&gt;You can combine multiple logical operators to create longer compound expressions: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if enteredDoorCode &amp;amp;&amp;amp; passedRetinaScan || hasDoorKey || knowsOverridePassword {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Welcome!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("ACCESS DENIED")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Welcome!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example uses multiple &lt;code&gt;&amp;amp;&amp;amp;&lt;/code&gt; and &lt;code&gt;||&lt;/code&gt; operators to create a longer compound expression. However, the &lt;code&gt;&amp;amp;&amp;amp;&lt;/code&gt; and &lt;code&gt;||&lt;/code&gt; operators still operate on only two values, so this is actually three smaller expressions chained together. The example can be read as: &lt;/p&gt;
&lt;p&gt;If we’ve entered the correct door code and passed the retina scan, or if we have a valid door key, or if we know the emergency override password, then allow access. &lt;/p&gt;
&lt;p&gt;Based on the values of &lt;code&gt;enteredDoorCode&lt;/code&gt;, &lt;code&gt;passedRetinaScan&lt;/code&gt;, and &lt;code&gt;hasDoorKey&lt;/code&gt;, the first two mini-expressions are &lt;code&gt;false&lt;/code&gt;. However, the emergency override password is known, so the overall compound expression still evaluates to &lt;code&gt;true&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Explicit Parentheses&lt;/h4&gt;
&lt;p&gt;It is sometimes useful to include parentheses when they are not strictly needed, to make the intention of a complex expression easier to read. In the door access example above, it is useful to add parentheses around the first part of the compound expression to make its intent explicit: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if (enteredDoorCode &amp;amp;&amp;amp; passedRetinaScan) || hasDoorKey || knowsOverridePassword {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Welcome!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("ACCESS DENIED")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Welcome!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The parentheses make it clear that the first two values are considered as part of a separate possible state in the overall logic. The output of the compound expression doesn’t change, but the overall intention is clearer to the reader. Readability is always preferred over brevity; use parentheses where they help to make your intentions clear. &lt;/p&gt;
&lt;h2&gt;Strings and Characters&lt;/h2&gt;
&lt;p&gt;A &lt;em&gt;string&lt;/em&gt; is an ordered collection of characters, such as &lt;code&gt;"hello, world"&lt;/code&gt; or &lt;code&gt;"albatross"&lt;/code&gt;. Swift strings are represented by the &lt;code&gt;String&lt;/code&gt; type, which in turn represents a collection of values of &lt;code&gt;Character&lt;/code&gt; type. &lt;/p&gt;
&lt;p&gt;Swift’s &lt;code&gt;String&lt;/code&gt; and &lt;code&gt;Character&lt;/code&gt; types provide a fast, Unicode-compliant way to work with text in your code. The syntax for string creation and manipulation is lightweight and readable, with a string literal syntax that is similar to C. String concatenation is as simple as adding together two strings with the &lt;code&gt;+&lt;/code&gt; operator, and string mutability is managed by choosing between a constant or a variable, just like any other value in Swift. &lt;/p&gt;
&lt;p&gt;Despite this simplicity of syntax, Swift’s &lt;code&gt;String&lt;/code&gt; type is a fast, modern string implementation. Every string is composed of encoding-independent Unicode characters, and provides support for accessing those characters in various Unicode representations. &lt;/p&gt;
&lt;p&gt;You can also use strings to insert constants, variables, literals, and expressions into longer strings, in a process known as string interpolation. This makes it easy to create custom string values for display, storage, and printing. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift’s &lt;code&gt;String&lt;/code&gt; type is bridged seamlessly to Foundation’s &lt;code&gt;NSString&lt;/code&gt; class. If you are working with the Foundation framework in Cocoa or Cocoa Touch, the entire &lt;code&gt;NSString&lt;/code&gt; API is available to call on any &lt;code&gt;String&lt;/code&gt; value you create, in addition to the &lt;code&gt;String&lt;/code&gt; features described in this chapter. You can also use a &lt;code&gt;String&lt;/code&gt; value with any API that requires an &lt;code&gt;NSString&lt;/code&gt; instance. &lt;/p&gt;
&lt;p&gt;For more information about using &lt;code&gt;String&lt;/code&gt; with Foundation and Cocoa, see &lt;em&gt;Using Swift with Cocoa and Objective-C&lt;/em&gt;. &lt;/p&gt;
&lt;h3&gt;String Literals&lt;/h3&gt;
&lt;p&gt;You can include predefined &lt;code&gt;String&lt;/code&gt; values within your code as &lt;em&gt;string literals&lt;/em&gt;. A string literal is a fixed sequence of textual characters surrounded by a pair of double quotes (&lt;code&gt;""&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Use a string literal as an initial value for a constant or variable: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someString = "Some string literal value"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that Swift infers a type of &lt;code&gt;String&lt;/code&gt; for the &lt;code&gt;someString&lt;/code&gt; constant, because it is initialized with a string literal value. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;For information about using special characters in string literals, see Special Unicode Characters in String Literals. &lt;/p&gt;
&lt;h3&gt;Initializing an Empty String&lt;/h3&gt;
&lt;p&gt;To create an empty &lt;code&gt;String&lt;/code&gt; value as the starting point for building a longer string, either assign an empty string literal to a variable, or initialize a new &lt;code&gt;String&lt;/code&gt; instance with initializer syntax: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var emptyString = "" // empty string literal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var anotherEmptyString = String() // initializer syntax&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// these two strings are both empty, and are equivalent to each other&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Find out whether a &lt;code&gt;String&lt;/code&gt; value is empty by checking its Boolean &lt;code&gt;isEmpty&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if emptyString.isEmpty {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Nothing to see here")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Nothing to see here"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;String Mutability&lt;/h3&gt;
&lt;p&gt;You indicate whether a particular &lt;code&gt;String&lt;/code&gt; can be modified (or &lt;em&gt;mutated&lt;/em&gt;) by assigning it to a variable (in which case it can be modified), or to a constant (in which case it cannot be modified): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var variableString = "Horse"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;variableString += " and carriage"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// variableString is now "Horse and carriage"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let constantString = "Highlander"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;constantString += " and another Highlander"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this reports a compile-time error - a constant string cannot be modified&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;This approach is different from string mutation in Objective-C and Cocoa, where you choose between two classes (&lt;code&gt;NSString&lt;/code&gt; and &lt;code&gt;NSMutableString&lt;/code&gt;) to indicate whether a string can be mutated. &lt;/p&gt;
&lt;h3&gt;Strings Are Value Types&lt;/h3&gt;
&lt;p&gt;Swift’s &lt;code&gt;String&lt;/code&gt; type is a &lt;em&gt;value type&lt;/em&gt;. If you create a new &lt;code&gt;String&lt;/code&gt; value, that &lt;code&gt;String&lt;/code&gt; value is &lt;em&gt;copied&lt;/em&gt; when it is passed to a function or method, or when it is assigned to a constant or variable. In each case, a new copy of the existing &lt;code&gt;String&lt;/code&gt; value is created, and the new copy is passed or assigned, not the original version. Value types are described in Structures and Enumerations Are Value Types. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;This behavior differs from that of &lt;code&gt;NSString&lt;/code&gt; in Cocoa. When you create an &lt;code&gt;NSString&lt;/code&gt; instance in Cocoa, and pass it to a function or method or assign it to a variable, you are always passing or assigning a &lt;em&gt;reference&lt;/em&gt; to the same single &lt;code&gt;NSString&lt;/code&gt;. No copying of the string takes place, unless you specifically request it. &lt;/p&gt;
&lt;p&gt;Swift’s copy-by-default &lt;code&gt;String&lt;/code&gt; behavior ensures that when a function or method passes you a &lt;code&gt;String&lt;/code&gt; value, it is clear that you own that exact &lt;code&gt;String&lt;/code&gt; value, regardless of where it came from. You can be confident that the string you are passed will not be modified unless you modify it yourself. &lt;/p&gt;
&lt;p&gt;Behind the scenes, Swift’s compiler optimizes string usage so that actual copying takes place only when absolutely necessary. This means you always get great performance when working with strings as value types. &lt;/p&gt;
&lt;h3&gt;Working with Characters&lt;/h3&gt;
&lt;p&gt;Swift’s &lt;code&gt;String&lt;/code&gt; type represents a collection of &lt;code&gt;Character&lt;/code&gt; values in a specified order. You can access the individual &lt;code&gt;Character&lt;/code&gt; values in a string by iterating over that string with a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for character in "Dog!🐶" {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(character)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// D&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// o&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// g&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// !&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 🐶&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop is described in For Loops. &lt;/p&gt;
&lt;p&gt;Alternatively, create a stand-alone &lt;code&gt;Character&lt;/code&gt; constant or variable from a single-character string literal by providing a &lt;code&gt;Character&lt;/code&gt; type annotation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let yenSign: Character = "¥"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Concatenating Strings and Characters&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;String&lt;/code&gt; values can be added together (or &lt;em&gt;concatenated&lt;/em&gt;) with the addition operator (&lt;code&gt;+&lt;/code&gt;) to create a new &lt;code&gt;String&lt;/code&gt; value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let string1 = "hello"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let string2 = " there"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var welcome = string1 \+ string2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// welcome now equals "hello there"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also append a &lt;code&gt;String&lt;/code&gt; value to an existing &lt;code&gt;String&lt;/code&gt; variable with the addition assignment operator (&lt;code&gt;+=&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var instruction = "look over"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;instruction += string2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// instruction now equals "look over there"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can append a &lt;code&gt;Character&lt;/code&gt; value to a &lt;code&gt;String&lt;/code&gt; variable with the &lt;code&gt;String&lt;/code&gt; type’s &lt;code&gt;append&lt;/code&gt; method: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let exclamationMark: Character = "!"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;welcome.append(exclamationMark)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// welcome now equals "hello there!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You can’t append a &lt;code&gt;String&lt;/code&gt; or &lt;code&gt;Character&lt;/code&gt; to an existing &lt;code&gt;Character&lt;/code&gt; variable, because a &lt;code&gt;Character&lt;/code&gt; value must contain a single character only. &lt;/p&gt;
&lt;h3&gt;String Interpolation&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;String interpolation&lt;/em&gt; is a way to construct a new &lt;code&gt;String&lt;/code&gt; value from a mix of constants, variables, literals, and expressions by including their values inside a string literal. Each item that you insert into the string literal is wrapped in a pair of parentheses, prefixed by a backslash: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let multiplier = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let message = "\\(multiplier) times 2.5 is \\(Double(multiplier) * 2.5)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// message is "3 times 2.5 is 7.5"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the example above, the value of &lt;code&gt;multiplier&lt;/code&gt; is inserted into a string literal as &lt;code&gt;\\(multiplier)&lt;/code&gt;. This placeholder is replaced with the actual value of &lt;code&gt;multiplier&lt;/code&gt; when the string interpolation is evaluated to create an actual string. &lt;/p&gt;
&lt;p&gt;The value of &lt;code&gt;multiplier&lt;/code&gt; is also part of a larger expression later in the string. This expression calculates the value of &lt;code&gt;Double(multiplier) * 2.5&lt;/code&gt; and inserts the result (&lt;code&gt;7.5&lt;/code&gt;) into the string. In this case, the expression is written as &lt;code&gt;\\(Double(multiplier) * 2.5)&lt;/code&gt; when it is included inside the string literal. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The expressions you write inside parentheses within an interpolated string cannot contain an unescaped double quote (&lt;code&gt;"&lt;/code&gt;) or backslash (&lt;code&gt;\&lt;/code&gt;), and cannot contain a carriage return or line feed. &lt;/p&gt;
&lt;h3&gt;Unicode&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Unicode&lt;/em&gt; is an international standard for encoding, representing, and processing text in different writing systems. It enables you to represent almost any character from any language in a standardized form, and to read and write those characters to and from an external source such as a text file or web page. Swift’s &lt;code&gt;String&lt;/code&gt; and &lt;code&gt;Character&lt;/code&gt; types are fully Unicode-compliant, as described in this section. &lt;/p&gt;
&lt;h4&gt;Unicode Scalars&lt;/h4&gt;
&lt;p&gt;Behind the scenes, Swift’s native &lt;code&gt;String&lt;/code&gt; type is built from &lt;em&gt;Unicode scalar&lt;/em&gt; values. A Unicode scalar is a unique 21-bit number for a character or modifier, such as &lt;code&gt;U+0061&lt;/code&gt; for &lt;code&gt;LATIN SMALL LETTER A&lt;/code&gt; (&lt;code&gt;"a"&lt;/code&gt;), or &lt;code&gt;U+1F425&lt;/code&gt; for &lt;code&gt;FRONT-FACING BABY CHICK&lt;/code&gt; (&lt;code&gt;"🐥"&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;A Unicode scalar is any Unicode &lt;em&gt;code point&lt;/em&gt; in the range &lt;code&gt;U+0000&lt;/code&gt; to &lt;code&gt;U+D7FF&lt;/code&gt; inclusive or &lt;code&gt;U+E000&lt;/code&gt; to &lt;code&gt;U+10FFFF&lt;/code&gt; inclusive. Unicode scalars do not include the Unicode &lt;em&gt;surrogate pair&lt;/em&gt; code points, which are the code points in the range &lt;code&gt;U+D800&lt;/code&gt; to &lt;code&gt;U+DFFF&lt;/code&gt; inclusive. &lt;/p&gt;
&lt;p&gt;Note that not all 21-bit Unicode scalars are assigned to a character—some scalars are reserved for future assignment. Scalars that have been assigned to a character typically also have a name, such as &lt;code&gt;LATIN SMALL LETTER A&lt;/code&gt; and &lt;code&gt;FRONT-FACING BABY CHICK&lt;/code&gt; in the examples above. &lt;/p&gt;
&lt;h4&gt;Special Unicode Characters in String Literals&lt;/h4&gt;
&lt;p&gt;String literals can include the following special Unicode characters: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The escaped special characters &lt;code&gt;\0&lt;/code&gt; (null character), &lt;code&gt;\\\&lt;/code&gt; (backslash), &lt;code&gt;\t&lt;/code&gt; (horizontal tab), &lt;code&gt;\n&lt;/code&gt; (line feed), &lt;code&gt;\r&lt;/code&gt; (carriage return), &lt;code&gt;\"&lt;/code&gt; (double quote) and &lt;code&gt;\'&lt;/code&gt; (single quote) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;An arbitrary Unicode scalar, written as &lt;code&gt;\u{&lt;/code&gt;&lt;em&gt;n&lt;/em&gt;&lt;code&gt;}&lt;/code&gt;, where &lt;em&gt;n&lt;/em&gt; is between one and eight hexadecimal digits &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The code below shows four examples of these special characters. The &lt;code&gt;wiseWords&lt;/code&gt; constant contains two escaped double quote characters. The &lt;code&gt;dollarSign&lt;/code&gt;, &lt;code&gt;blackHeart&lt;/code&gt;, and &lt;code&gt;sparklingHeart&lt;/code&gt; constants demonstrate the Unicode scalar format: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let wiseWords = "\"Imagination is more important than knowledge\" - Einstein"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// "Imagination is more important than knowledge" - Einstein&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let dollarSign = "\u{24}" // $, Unicode scalar U+0024&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let blackHeart = "\u{2665}" // ♥, Unicode scalar U+2665&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let sparklingHeart = "\u{1F496}" // 💖, Unicode scalar U+1F496&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Extended Grapheme Clusters&lt;/h4&gt;
&lt;p&gt;Every instance of Swift’s &lt;code&gt;Character&lt;/code&gt; type represents a single &lt;em&gt;extended grapheme cluster&lt;/em&gt;. An extended grapheme cluster is a sequence of one or more Unicode scalars that (when combined) produce a single human-readable character. &lt;/p&gt;
&lt;p&gt;Here’s an example. The letter &lt;code&gt;é&lt;/code&gt; can be represented as the single Unicode scalar &lt;code&gt;é&lt;/code&gt; (&lt;code&gt;LATIN SMALL LETTER E WITH ACUTE&lt;/code&gt;, or &lt;code&gt;U+00E9&lt;/code&gt;). However, the same letter can also be represented as a &lt;em&gt;pair&lt;/em&gt; of scalars—a standard letter &lt;code&gt;e&lt;/code&gt; (&lt;code&gt;LATIN SMALL LETTER E&lt;/code&gt;, or &lt;code&gt;U+0065&lt;/code&gt;), followed by the &lt;code&gt;COMBINING ACUTE ACCENT&lt;/code&gt; scalar (&lt;code&gt;U+0301&lt;/code&gt;). The &lt;code&gt;COMBINING ACUTE ACCENT&lt;/code&gt; scalar is graphically applied to the scalar that precedes it, turning an &lt;code&gt;e&lt;/code&gt; into an &lt;code&gt;é&lt;/code&gt; when it is rendered by a Unicode-aware text-rendering system. &lt;/p&gt;
&lt;p&gt;In both cases, the letter &lt;code&gt;é&lt;/code&gt; is represented as a single Swift &lt;code&gt;Character&lt;/code&gt; value that represents an extended grapheme cluster. In the first case, the cluster contains a single scalar; in the second case, it is a cluster of two scalars: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let eAcute: Character = "\u{E9}" // é&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let combinedEAcute: Character = "\u{65}\u{301}" // e followed by ́&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// eAcute is é, combinedEAcute is é&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Extended grapheme clusters are a flexible way to represent many complex script characters as a single &lt;code&gt;Character&lt;/code&gt; value. For example, Hangul syllables from the Korean alphabet can be represented as either a precomposed or decomposed sequence. Both of these representations qualify as a single &lt;code&gt;Character&lt;/code&gt; value in Swift: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let precomposed: Character = "\u{D55C}" // 한&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let decomposed: Character = "\u{1112}\u{1161}\u{11AB}" // ᄒ, ᅡ, ᆫ&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// precomposed is 한, decomposed is 한&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Extended grapheme clusters enable scalars for enclosing marks (such as &lt;code&gt;COMBINING ENCLOSING CIRCLE&lt;/code&gt;, or &lt;code&gt;U+20DD&lt;/code&gt;) to enclose other Unicode scalars as part of a single &lt;code&gt;Character&lt;/code&gt; value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let enclosedEAcute: Character = "\u{E9}\u{20DD}"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// enclosedEAcute is é⃝&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unicode scalars for regional indicator symbols can be combined in pairs to make a single &lt;code&gt;Character&lt;/code&gt; value, such as this combination of &lt;code&gt;REGIONAL INDICATOR SYMBOL LETTER U&lt;/code&gt; (&lt;code&gt;U+1F1FA&lt;/code&gt;) and &lt;code&gt;REGIONAL INDICATOR SYMBOL LETTER S&lt;/code&gt; (&lt;code&gt;U+1F1F8&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let regionalIndicatorForUS: Character = "\u{1F1FA}\u{1F1F8}"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// regionalIndicatorForUS is 🇺🇸&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Counting Characters&lt;/h3&gt;
&lt;p&gt;To retrieve a count of the &lt;code&gt;Character&lt;/code&gt; values in a string, call the global &lt;code&gt;countElements&lt;/code&gt; function and pass in a string as the function’s sole parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let unusualMenagerie = "Koala 🐨, Snail 🐌, Penguin 🐧, Dromedary 🐪"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("unusualMenagerie has \\(countElements(unusualMenagerie)) characters")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "unusualMenagerie has 40 characters"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that Swift’s use of extended grapheme clusters for &lt;code&gt;Character&lt;/code&gt; values means that string concatenation and modification may not always affect a string’s character count. &lt;/p&gt;
&lt;p&gt;For example, if you initialize a new string with the four-character word &lt;code&gt;cafe&lt;/code&gt;, and then append a &lt;code&gt;COMBINING ACUTE ACCENT&lt;/code&gt; (&lt;code&gt;U+0301&lt;/code&gt;) to the end of the string, the resulting string will still have a character count of &lt;code&gt;4&lt;/code&gt;, with a fourth character of &lt;code&gt;é&lt;/code&gt;, not &lt;code&gt;e&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var word = "cafe"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("the number of characters in \\(word) is \\(countElements(word))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "the number of characters in cafe is 4"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;word += "\u{301}" // COMBINING ACUTE ACCENT, U+0301&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("the number of characters in \\(word) is \\(countElements(word))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "the number of characters in café is 4"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Extended grapheme clusters can be composed of one or more Unicode scalars. This means that different characters, and different representations of the same character, can require different amounts of memory to store. Because of this, characters in Swift do not each take up the same amount of memory within a string’s representation. As a result, the number of characters in a string cannot be calculated without iterating through the string to determine its extended grapheme cluster boundaries. If you are working with particularly long string values, be aware that the &lt;code&gt;countElements&lt;/code&gt; function must iterate over the Unicode scalars in the entire string in order to calculate an accurate character count for that string. &lt;/p&gt;
&lt;p&gt;Note also that the character count returned by &lt;code&gt;countElements&lt;/code&gt; is not always the same as the &lt;code&gt;length&lt;/code&gt; property of an &lt;code&gt;NSString&lt;/code&gt; that contains the same characters. The length of an &lt;code&gt;NSString&lt;/code&gt; is based on the number of 16-bit code units within the string’s UTF-16 representation and not the number of Unicode extended grapheme clusters within the string. To reflect this fact, the &lt;code&gt;length&lt;/code&gt; property from &lt;code&gt;NSString&lt;/code&gt; is called &lt;code&gt;utf16Count&lt;/code&gt; when it is accessed on a Swift &lt;code&gt;String&lt;/code&gt; value. &lt;/p&gt;
&lt;h3&gt;Comparing Strings&lt;/h3&gt;
&lt;p&gt;Swift provides three ways to compare textual values: string and character equality, prefix equality, and suffix equality. &lt;/p&gt;
&lt;h4&gt;String and Character Equality&lt;/h4&gt;
&lt;p&gt;String and character equality is checked with the “equal to” operator (&lt;code&gt;==&lt;/code&gt;) and the “not equal to” operator (&lt;code&gt;!=&lt;/code&gt;), as described in Comparison Operators: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let quotation = "We're a lot alike, you and I."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let sameQuotation = "We're a lot alike, you and I."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if quotation == sameQuotation {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("These two strings are considered equal")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "These two strings are considered equal"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Two &lt;code&gt;String&lt;/code&gt; values (or two &lt;code&gt;Character&lt;/code&gt; values) are considered equal if their extended grapheme clusters are &lt;em&gt;canonically equivalent&lt;/em&gt;. Extended grapheme clusters are canonically equivalent if they have the same linguistic meaning and appearance, even if they are composed from different Unicode scalars behind the scenes. &lt;/p&gt;
&lt;p&gt;For example, &lt;code&gt;LATIN SMALL LETTER E WITH ACUTE&lt;/code&gt; (&lt;code&gt;U+00E9&lt;/code&gt;) is canonically equivalent to &lt;code&gt;LATIN SMALL LETTER E&lt;/code&gt; (&lt;code&gt;U+0065&lt;/code&gt;) followed by &lt;code&gt;COMBINING ACUTE ACCENT&lt;/code&gt; (&lt;code&gt;U+0301&lt;/code&gt;). Both of these extended grapheme clusters are valid ways to represent the character &lt;code&gt;é&lt;/code&gt;, and so they are considered to be canonically equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;// "Voulez-vous un café?" using LATIN SMALL LETTER E WITH ACUTE&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let eAcuteQuestion = "Voulez-vous un caf\u{E9}?"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// "Voulez-vous un café?" using LATIN SMALL LETTER E and COMBINING ACUTE ACCENT&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let combinedEAcuteQuestion = "Voulez-vous un caf\u{65}\u{301}?"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if eAcuteQuestion == combinedEAcuteQuestion {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("These two strings are considered equal")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "These two strings are considered equal"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Conversely, &lt;code&gt;LATIN CAPITAL LETTER A&lt;/code&gt; (&lt;code&gt;U+0041&lt;/code&gt;, or &lt;code&gt;"A"&lt;/code&gt;), as used in English, is &lt;em&gt;not&lt;/em&gt; equivalent to &lt;code&gt;CYRILLIC CAPITAL LETTER A&lt;/code&gt; (&lt;code&gt;U+0410&lt;/code&gt;, or &lt;code&gt;"А"&lt;/code&gt;), as used in Russian. The characters are visually similar, but do not have the same linguistic meaning: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let latinCapitalLetterA: Character = "\u{41}"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let cyrillicCapitalLetterA: Character = "\u{0410}"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if latinCapitalLetterA != cyrillicCapitalLetterA {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("These two characters are not equivalent")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "These two characters are not equivalent"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;String and character comparisons in Swift are not locale-sensitive. &lt;/p&gt;
&lt;h4&gt;Prefix and Suffix Equality&lt;/h4&gt;
&lt;p&gt;To check whether a string has a particular string prefix or suffix, call the string’s &lt;code&gt;hasPrefix&lt;/code&gt; and &lt;code&gt;hasSuffix&lt;/code&gt; methods, both of which take a single argument of type &lt;code&gt;String&lt;/code&gt; and return a Boolean value. &lt;/p&gt;
&lt;p&gt;The examples below consider an array of strings representing the scene locations from the first two acts of Shakespeare’s &lt;em&gt;Romeo and Juliet&lt;/em&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let romeoAndJuliet = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 1 Scene 1: Verona, A public place",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 1 Scene 2: Capulet's mansion",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 1 Scene 3: A room in Capulet's mansion",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 1 Scene 4: A street outside Capulet's mansion",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 1 Scene 5: The Great Hall in Capulet's mansion",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 2 Scene 1: Outside Capulet's mansion",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 2 Scene 2: Capulet's orchard",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 2 Scene 3: Outside Friar Lawrence's cell",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 2 Scene 4: A street in Verona",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 2 Scene 5: Capulet's mansion",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Act 2 Scene 6: Friar Lawrence's cell"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can use the &lt;code&gt;hasPrefix&lt;/code&gt; method with the &lt;code&gt;romeoAndJuliet&lt;/code&gt; array to count the number of scenes in Act 1 of the play: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var act1SceneCount = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for scene in romeoAndJuliet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if scene.hasPrefix("Act 1 ") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;++act1SceneCount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("There are \\(act1SceneCount) scenes in Act 1")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "There are 5 scenes in Act 1"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Similarly, use the &lt;code&gt;hasSuffix&lt;/code&gt; method to count the number of scenes that take place in or around Capulet’s mansion and Friar Lawrence’s cell: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var mansionCount = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var cellCount = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for scene in romeoAndJuliet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if scene.hasSuffix("Capulet's mansion") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;++mansionCount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if scene.hasSuffix("Friar Lawrence's cell") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;++cellCount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(mansionCount) mansion scenes; \\(cellCount) cell scenes")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "6 mansion scenes; 2 cell scenes"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;hasPrefix&lt;/code&gt; and &lt;code&gt;hasSuffix&lt;/code&gt; methods perform a character-by-character canonical equivalence comparison between the extended grapheme clusters in each string, as described in String and Character Equality. &lt;/p&gt;
&lt;h3&gt;Unicode Representations of Strings&lt;/h3&gt;
&lt;p&gt;When a Unicode string is written to a text file or some other storage, the Unicode scalars in that string are encoded in one of several Unicode-defined &lt;em&gt;encoding forms&lt;/em&gt;. Each form encodes the string in small chunks known as &lt;em&gt;code units&lt;/em&gt;. These include the UTF-8 encoding form (which encodes a string as 8-bit code units), the UTF-16 encoding form (which encodes a string as 16-bit code units), and the UTF-32 encoding form (which encodes a string as 32-bit code units). &lt;/p&gt;
&lt;p&gt;Swift provides several different ways to access Unicode representations of strings. You can iterate over the string with a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; statement, to access its individual &lt;code&gt;Character&lt;/code&gt; values as Unicode extended grapheme clusters. This process is described in Working with Characters. &lt;/p&gt;
&lt;p&gt;Alternatively, access a &lt;code&gt;String&lt;/code&gt; value in one of three other Unicode-compliant representations: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;A collection of UTF-8 code units (accessed with the string’s &lt;code&gt;utf8&lt;/code&gt; property) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A collection of UTF-16 code units (accessed with the string’s &lt;code&gt;utf16&lt;/code&gt; property) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A collection of 21-bit Unicode scalar values, equivalent to the string’s UTF-32 encoding form (accessed with the string’s &lt;code&gt;unicodeScalars&lt;/code&gt; property) &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Each example below shows a different representation of the following string, which is made up of the characters &lt;code&gt;D&lt;/code&gt;, &lt;code&gt;o&lt;/code&gt;, &lt;code&gt;g&lt;/code&gt;, &lt;code&gt;‼&lt;/code&gt; (&lt;code&gt;DOUBLE EXCLAMATION MARK&lt;/code&gt;, or Unicode scalar &lt;code&gt;U+203C&lt;/code&gt;), and the 🐶 character (&lt;code&gt;DOG FACE&lt;/code&gt;, or Unicode scalar &lt;code&gt;U+1F436&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let dogString = "Dog‼🐶"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;UTF-8 Representation&lt;/h4&gt;
&lt;p&gt;You can access a UTF-8 representation of a &lt;code&gt;String&lt;/code&gt; by iterating over its &lt;code&gt;utf8&lt;/code&gt; property. This property is of type &lt;code&gt;String.UTF8View&lt;/code&gt;, which is a collection of unsigned 8-bit (&lt;code&gt;UInt8&lt;/code&gt;) values, one for each byte in the string’s UTF-8 representation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for codeUnit in dogString.utf8 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("\\(codeUnit) ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("\n")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 68 111 103 226 128 188 240 159 144 182&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the example above, the first three decimal &lt;code&gt;codeUnit&lt;/code&gt; values (&lt;code&gt;68&lt;/code&gt;, &lt;code&gt;111&lt;/code&gt;, &lt;code&gt;103&lt;/code&gt;) represent the characters &lt;code&gt;D&lt;/code&gt;, &lt;code&gt;o&lt;/code&gt;, and &lt;code&gt;g&lt;/code&gt;, whose UTF-8 representation is the same as their ASCII representation. The next three decimal &lt;code&gt;codeUnit&lt;/code&gt; values (&lt;code&gt;226&lt;/code&gt;, &lt;code&gt;128&lt;/code&gt;, &lt;code&gt;188&lt;/code&gt;) are a three-byte UTF-8 representation of the &lt;code&gt;DOUBLE EXCLAMATION MARK&lt;/code&gt; character. The last four &lt;code&gt;codeUnit&lt;/code&gt; values (&lt;code&gt;240&lt;/code&gt;, &lt;code&gt;159&lt;/code&gt;, &lt;code&gt;144&lt;/code&gt;, &lt;code&gt;182&lt;/code&gt;) are a four-byte UTF-8 representation of the &lt;code&gt;DOG FACE&lt;/code&gt; character. &lt;/p&gt;
&lt;h4&gt;UTF-16 Representation&lt;/h4&gt;
&lt;p&gt;You can access a UTF-16 representation of a &lt;code&gt;String&lt;/code&gt; by iterating over its &lt;code&gt;utf16&lt;/code&gt; property. This property is of type &lt;code&gt;String.UTF16View&lt;/code&gt;, which is a collection of unsigned 16-bit (&lt;code&gt;UInt16&lt;/code&gt;) values, one for each 16-bit code unit in the string’s UTF-16 representation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for codeUnit in dogString.utf16 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("\\(codeUnit) ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("\n")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 68 111 103 8252 55357 56374&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Again, the first three &lt;code&gt;codeUnit&lt;/code&gt; values (&lt;code&gt;68&lt;/code&gt;, &lt;code&gt;111&lt;/code&gt;, &lt;code&gt;103&lt;/code&gt;) represent the characters &lt;code&gt;D&lt;/code&gt;, &lt;code&gt;o&lt;/code&gt;, and &lt;code&gt;g&lt;/code&gt;, whose UTF-16 code units have the same values as in the string’s UTF-8 representation (because these Unicode scalars represent ASCII characters). &lt;/p&gt;
&lt;p&gt;The fourth &lt;code&gt;codeUnit&lt;/code&gt; value (&lt;code&gt;8252&lt;/code&gt;) is a decimal equivalent of the hexadecimal value &lt;code&gt;203C&lt;/code&gt;, which represents the Unicode scalar &lt;code&gt;U+203C&lt;/code&gt; for the &lt;code&gt;DOUBLE EXCLAMATION MARK&lt;/code&gt; character. This character can be represented as a single code unit in UTF-16. &lt;/p&gt;
&lt;p&gt;The fifth and sixth &lt;code&gt;codeUnit&lt;/code&gt; values (&lt;code&gt;55357&lt;/code&gt; and &lt;code&gt;56374&lt;/code&gt;) are a UTF-16 surrogate pair representation of the &lt;code&gt;DOG FACE&lt;/code&gt; character. These values are a high-surrogate value of &lt;code&gt;U+D83D&lt;/code&gt; (decimal value &lt;code&gt;55357&lt;/code&gt;) and a low-surrogate value of &lt;code&gt;U+DC36&lt;/code&gt; (decimal value &lt;code&gt;56374&lt;/code&gt;). &lt;/p&gt;
&lt;h4&gt;Unicode Scalar Representation&lt;/h4&gt;
&lt;p&gt;You can access a Unicode scalar representation of a &lt;code&gt;String&lt;/code&gt; value by iterating over its &lt;code&gt;unicodeScalars&lt;/code&gt; property. This property is of type &lt;code&gt;UnicodeScalarView&lt;/code&gt;, which is a collection of values of type &lt;code&gt;UnicodeScalar&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Each &lt;code&gt;UnicodeScalar&lt;/code&gt; has a &lt;code&gt;value&lt;/code&gt; property that returns the scalar’s 21-bit value, represented within a &lt;code&gt;UInt32&lt;/code&gt; value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for scalar in dogString.unicodeScalars {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("\\(scalar.value) ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("\n")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 68 111 103 8252 128054&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;value&lt;/code&gt; properties for the first three &lt;code&gt;UnicodeScalar&lt;/code&gt; values (&lt;code&gt;68&lt;/code&gt;, &lt;code&gt;111&lt;/code&gt;, &lt;code&gt;103&lt;/code&gt;) once again represent the characters &lt;code&gt;D&lt;/code&gt;, &lt;code&gt;o&lt;/code&gt;, and &lt;code&gt;g&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The fourth &lt;code&gt;codeUnit&lt;/code&gt; value (&lt;code&gt;8252&lt;/code&gt;) is again a decimal equivalent of the hexadecimal value &lt;code&gt;203C&lt;/code&gt;, which represents the Unicode scalar &lt;code&gt;U+203C&lt;/code&gt; for the &lt;code&gt;DOUBLE EXCLAMATION MARK&lt;/code&gt; character. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;value&lt;/code&gt; property of the fifth and final &lt;code&gt;UnicodeScalar&lt;/code&gt;, &lt;code&gt;128054&lt;/code&gt;, is a decimal equivalent of the hexadecimal value &lt;code&gt;1F436&lt;/code&gt;, which represents the Unicode scalar &lt;code&gt;U+1F436&lt;/code&gt; for the &lt;code&gt;DOG FACE&lt;/code&gt; character. &lt;/p&gt;
&lt;p&gt;As an alternative to querying their &lt;code&gt;value&lt;/code&gt; properties, each &lt;code&gt;UnicodeScalar&lt;/code&gt; value can also be used to construct a new &lt;code&gt;String&lt;/code&gt; value, such as with string interpolation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for scalar in dogString.unicodeScalars {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(scalar) ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// D&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// o&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// g&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// ‼&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 🐶&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Collection Types&lt;/h2&gt;
&lt;p&gt;Swift provides two &lt;em&gt;collection types&lt;/em&gt;, known as arrays and dictionaries, for storing collections of values. Arrays store ordered lists of values of the same type. Dictionaries store unordered collections of values of the same type, which can be referenced and looked up through a unique identifier (also known as a key). &lt;/p&gt;
&lt;p&gt;Arrays and dictionaries in Swift are always clear about the types of values and keys that they can store. This means that you cannot insert a value of the wrong type into an array or dictionary by mistake. It also means you can be confident about the types of values you will retrieve from an array or dictionary. Swift’s use of explicitly typed collections ensures that your code is always clear about the types of values it can work with and enables you to catch any type mismatches early in your code’s development. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Behind the scenes, Swift’s array and dictionary types are implemented as &lt;em&gt;generic collections&lt;/em&gt;. For more on generic types and collections, see Generics. &lt;/p&gt;
&lt;h3&gt;Mutability of Collections&lt;/h3&gt;
&lt;p&gt;If you create an array or a dictionary and assign it to a variable, the collection that is created will be &lt;em&gt;mutable&lt;/em&gt;. This means that you can change (or &lt;em&gt;mutate&lt;/em&gt;) the collection after it is created by adding, removing, or changing items in the collection. Conversely, if you assign an array or a dictionary to a constant, that array or dictionary is &lt;em&gt;immutable&lt;/em&gt;, and its size and contents cannot be changed. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;It is good practice to create immutable collections in all cases where the collection does not need to change. Doing so enables the Swift compiler to optimize the performance of the collections you create. &lt;/p&gt;
&lt;h3&gt;Arrays&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;array&lt;/em&gt; stores multiple values of the same type in an ordered list. The same value can appear in an array multiple times at different positions. &lt;/p&gt;
&lt;p&gt;Swift arrays are specific about the kinds of values they can store. They differ from Objective-C’s &lt;code&gt;NSArray&lt;/code&gt; and &lt;code&gt;NSMutableArray&lt;/code&gt; classes, which can store any kind of object and do not provide any information about the nature of the objects they return. In Swift, the type of values that a particular array can store is always made clear, either through an explicit type annotation, or through type inference, and does not have to be a class type. If you create an array of &lt;code&gt;Int&lt;/code&gt; values, for example, you can’t insert any value other than &lt;code&gt;Int&lt;/code&gt; values into that array. Swift arrays are type safe, and are always clear about what they may contain. &lt;/p&gt;
&lt;h4&gt;Array Type Shorthand Syntax&lt;/h4&gt;
&lt;p&gt;The type of a Swift array is written in full as &lt;code&gt;Array&amp;lt;SomeType&amp;gt;&lt;/code&gt;, where &lt;code&gt;SomeType&lt;/code&gt; is the type that the array is allowed to store. You can also write the type of an array in shorthand form as &lt;code&gt;[SomeType]&lt;/code&gt;. Although the two forms are functionally identical, the shorthand form is preferred, and is used throughout this guide when referring to the type of an array. &lt;/p&gt;
&lt;h4&gt;Array Literals&lt;/h4&gt;
&lt;p&gt;You can initialize an array with an &lt;em&gt;array literal&lt;/em&gt;, which is a shorthand way to write one or more values as an array collection. An array literal is written as a list of values, separated by commas, surrounded by a pair of square brackets: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;[&lt;em&gt;value 1&lt;/em&gt;, &lt;em&gt;value 2&lt;/em&gt;, &lt;em&gt;value 3&lt;/em&gt;]&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example below creates an array called &lt;code&gt;shoppingList&lt;/code&gt; to store &lt;code&gt;String&lt;/code&gt; values: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var shoppingList: [String] = ["Eggs", "Milk"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList has been initialized with two initial items&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;shoppingList&lt;/code&gt; variable is declared as “an array of &lt;code&gt;String&lt;/code&gt; values”, written as &lt;code&gt;[String]&lt;/code&gt;. Because this particular array has specified a value type of &lt;code&gt;String&lt;/code&gt;, it is &lt;em&gt;only&lt;/em&gt; allowed to store &lt;code&gt;String&lt;/code&gt; values. Here, the &lt;code&gt;shoppingList&lt;/code&gt; array is initialized with two &lt;code&gt;String&lt;/code&gt; values (&lt;code&gt;"Eggs"&lt;/code&gt; and &lt;code&gt;"Milk"&lt;/code&gt;), written within an array literal. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;shoppingList&lt;/code&gt; array is declared as a variable (with the &lt;code&gt;var&lt;/code&gt; introducer) and not a constant (with the &lt;code&gt;let&lt;/code&gt; introducer) because more items are added to the shopping list in the examples below. &lt;/p&gt;
&lt;p&gt;In this case, the array literal contains two &lt;code&gt;String&lt;/code&gt; values and nothing else. This matches the type of the &lt;code&gt;shoppingList&lt;/code&gt; variable’s declaration (an array that can only contain &lt;code&gt;String&lt;/code&gt; values), and so the assignment of the array literal is permitted as a way to initialize &lt;code&gt;shoppingList&lt;/code&gt; with two initial items. &lt;/p&gt;
&lt;p&gt;Thanks to Swift’s type inference, you don’t have to write the type of the array if you’re initializing it with an array literal containing values of the same type. The initialization of &lt;code&gt;shoppingList&lt;/code&gt; could have been written in a shorter form instead: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var shoppingList = ["Eggs", "Milk"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because all values in the array literal are of the same type, Swift can infer that &lt;code&gt;[String]&lt;/code&gt; is the correct type to use for the &lt;code&gt;shoppingList&lt;/code&gt; variable. &lt;/p&gt;
&lt;h4&gt;Accessing and Modifying an Array&lt;/h4&gt;
&lt;p&gt;You access and modify an array through its methods and properties, or by using subscript syntax. &lt;/p&gt;
&lt;p&gt;To find out the number of items in an array, check its read-only &lt;code&gt;count&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The shopping list contains \\(shoppingList.count) items.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The shopping list contains 2 items."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Use the Boolean &lt;code&gt;isEmpty&lt;/code&gt; property as a shortcut for checking whether the &lt;code&gt;count&lt;/code&gt; property is equal to &lt;code&gt;0&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if shoppingList.isEmpty {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The shopping list is empty.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The shopping list is not empty.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The shopping list is not empty."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can add a new item to the end of an array by calling the array’s &lt;code&gt;append&lt;/code&gt; method: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;shoppingList.append("Flour")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList now contains 3 items, and someone is making pancakes&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Alternatively, append an array of one or more compatible items with the addition assignment operator (&lt;code&gt;+=&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;shoppingList += ["Baking Powder"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList now contains 4 items&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shoppingList += ["Chocolate Spread", "Cheese", "Butter"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList now contains 7 items&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Retrieve a value from the array by using &lt;em&gt;subscript syntax&lt;/em&gt;, passing the index of the value you want to retrieve within square brackets immediately after the name of the array: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var firstItem = shoppingList[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// firstItem is equal to "Eggs"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the first item in the array has an index of &lt;code&gt;0&lt;/code&gt;, not &lt;code&gt;1&lt;/code&gt;. Arrays in Swift are always zero-indexed. &lt;/p&gt;
&lt;p&gt;You can use subscript syntax to change an existing value at a given index: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;shoppingList[0] = "Six eggs"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the first item in the list is now equal to "Six eggs" rather than "Eggs"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also use subscript syntax to change a range of values at once, even if the replacement set of values has a different length than the range you are replacing. The following example replaces &lt;code&gt;"Chocolate Spread"&lt;/code&gt;, &lt;code&gt;"Cheese"&lt;/code&gt;, and &lt;code&gt;"Butter"&lt;/code&gt; with &lt;code&gt;"Bananas"&lt;/code&gt; and &lt;code&gt;"Apples"&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;shoppingList[4...6] = ["Bananas", "Apples"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList now contains 6 items&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You can’t use subscript syntax to append a new item to the end of an array. If you try to use subscript syntax to retrieve or set a value for an index that is outside of an array’s existing bounds, you will trigger a runtime error. However, you can check that an index is valid before using it, by comparing it to the array’s &lt;code&gt;count&lt;/code&gt; property. Except when &lt;code&gt;count&lt;/code&gt; is &lt;code&gt;0&lt;/code&gt; (meaning the array is empty), the largest valid index in an array will always be &lt;code&gt;count - 1&lt;/code&gt;, because arrays are indexed from zero. &lt;/p&gt;
&lt;p&gt;To insert an item into the array at a specified index, call the array’s &lt;code&gt;insert(atIndex:)&lt;/code&gt; method: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;shoppingList.insert("Maple Syrup", atIndex: 0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList now contains 7 items&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// "Maple Syrup" is now the first item in the list&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This call to the &lt;code&gt;insert&lt;/code&gt; method inserts a new item with a value of &lt;code&gt;"Maple Syrup"&lt;/code&gt; at the very beginning of the shopping list, indicated by an index of &lt;code&gt;0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Similarly, you remove an item from the array with the &lt;code&gt;removeAtIndex&lt;/code&gt; method. This method removes the item at the specified index and returns the removed item (although you can ignore the returned value if you do not need it): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let mapleSyrup = shoppingList.removeAtIndex(0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the item that was at index 0 has just been removed&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList now contains 6 items, and no Maple Syrup&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the mapleSyrup constant is now equal to the removed "Maple Syrup" string&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Any gaps in an array are closed when an item is removed, and so the value at index &lt;code&gt;0&lt;/code&gt; is once again equal to &lt;code&gt;"Six eggs"&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;firstItem = shoppingList[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// firstItem is now equal to "Six eggs"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you want to remove the final item from an array, use the &lt;code&gt;removeLast&lt;/code&gt; method rather than the &lt;code&gt;removeAtIndex&lt;/code&gt; method to avoid the need to query the array’s &lt;code&gt;count&lt;/code&gt; property. Like the &lt;code&gt;removeAtIndex&lt;/code&gt; method, &lt;code&gt;removeLast&lt;/code&gt; returns the removed item: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let apples = shoppingList.removeLast()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the last item in the array has just been removed&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// shoppingList now contains 5 items, and no apples&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the apples constant is now equal to the removed "Apples" string&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Iterating Over an Array&lt;/h4&gt;
&lt;p&gt;You can iterate over the entire set of values in an array with the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for item in shoppingList {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Six eggs&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Milk&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Flour&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Baking Powder&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Bananas&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you need the integer index of each item as well as its value, use the global &lt;code&gt;enumerate&lt;/code&gt; function to iterate over the array instead. The &lt;code&gt;enumerate&lt;/code&gt; function returns a tuple for each item in the array composed of the index and the value for that item. You can decompose the tuple into temporary constants or variables as part of the iteration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for (index, value) in enumerate(shoppingList) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Item \\(index \+ 1): \\(value)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Item 1: Six eggs&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Item 2: Milk&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Item 3: Flour&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Item 4: Baking Powder&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Item 5: Bananas&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more about the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop, see For Loops. &lt;/p&gt;
&lt;h4&gt;Creating and Initializing an Array&lt;/h4&gt;
&lt;p&gt;You can create an empty array of a certain type (without setting any initial values) using initializer syntax: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var someInts = [Int]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("someInts is of type [Int] with \\(someInts.count) items.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "someInts is of type [Int] with 0 items."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the type of the &lt;code&gt;someInts&lt;/code&gt; variable is inferred to be &lt;code&gt;[Int]&lt;/code&gt;, because it is set to the output of an &lt;code&gt;[Int]&lt;/code&gt; initializer. &lt;/p&gt;
&lt;p&gt;Alternatively, if the context already provides type information, such as a function argument or an already typed variable or constant, you can create an empty array with an empty array literal, which is written as &lt;code&gt;[]&lt;/code&gt; (an empty pair of square brackets): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;someInts.append(3)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someInts now contains 1 value of type Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someInts = []&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someInts is now an empty array, but is still of type [Int]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Swift’s &lt;code&gt;Array&lt;/code&gt; type also provides an initializer for creating an array of a certain size with all of its values set to a provided default value. You pass this initializer the number of items to be added to the new array (called &lt;code&gt;count&lt;/code&gt;) and a default value of the appropriate type (called &lt;code&gt;repeatedValue&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var threeDoubles = [Double](count: 3, repeatedValue: 0.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// threeDoubles is of type [Double], and equals [0.0, 0.0, 0.0]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can create a new array by adding together two existing arrays of compatible type with the addition operator (&lt;code&gt;+&lt;/code&gt;). The new array’s type is inferred from the type of the two arrays you add together: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var anotherThreeDoubles = [Double](count: 3, repeatedValue: 2.5)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// anotherThreeDoubles is inferred as [Double], and equals [2.5, 2.5, 2.5]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var sixDoubles = threeDoubles \+ anotherThreeDoubles&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// sixDoubles is inferred as [Double], and equals [0.0, 0.0, 0.0, 2.5, 2.5, 2.5]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Dictionaries&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;dictionary&lt;/em&gt; is a container that stores multiple values of the same type. Each value is associated with a unique &lt;em&gt;key&lt;/em&gt;, which acts as an identifier for that value within the dictionary. Unlike items in an array, items in a dictionary do not have a specified order. You use a dictionary when you need to look up values based on their identifier, in much the same way that a real-world dictionary is used to look up the definition for a particular word. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift dictionaries are specific about the types of keys and values they can store. They differ from Objective-C’s &lt;code&gt;NSDictionary&lt;/code&gt; and &lt;code&gt;NSMutableDictionary&lt;/code&gt; classes, which can use any kind of object as their keys and values and do not provide any information about the nature of those objects. In Swift, the type of keys and values that a particular dictionary can store is always made clear, either through an explicit type annotation or through type inference. &lt;/p&gt;
&lt;h4&gt;Dictionary Type Shorthand Syntax&lt;/h4&gt;
&lt;p&gt;The type of a Swift dictionary is written in full as &lt;code&gt;Dictionary&amp;lt;Key, Value&amp;gt;&lt;/code&gt;, where &lt;code&gt;Key&lt;/code&gt; is the type of value that can be used as a dictionary key, and &lt;code&gt;Value&lt;/code&gt; is the type of value that the dictionary stores for those keys. &lt;/p&gt;
&lt;p&gt;You can also write the type of a dictionary in shorthand form as &lt;code&gt;[Key: Value]&lt;/code&gt;. Although the two forms are functionally identical, the shorthand form is preferred, and is used throughout this guide when referring to the type of a dictionary. &lt;/p&gt;
&lt;h4&gt;Dictionary Literals&lt;/h4&gt;
&lt;p&gt;You can initialize a dictionary with a &lt;em&gt;dictionary literal&lt;/em&gt;, which has a similar syntax to the array literal seen earlier. A dictionary literal is a shorthand way to write one or more key-value pairs as a &lt;code&gt;Dictionary&lt;/code&gt; collection. &lt;/p&gt;
&lt;p&gt;A &lt;em&gt;key-value pair&lt;/em&gt; is a combination of a key and a value. In a dictionary literal, the key and value in each key-value pair are separated by a colon. The key-value pairs are written as a list, separated by commas, surrounded by a pair of square brackets: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;[&lt;em&gt;key 1&lt;/em&gt;: &lt;em&gt;value 1&lt;/em&gt;, &lt;em&gt;key 2&lt;/em&gt;: &lt;em&gt;value 2&lt;/em&gt;, &lt;em&gt;key 3&lt;/em&gt;: &lt;em&gt;value 3&lt;/em&gt;]&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example below creates a dictionary to store the names of international airports. In this dictionary, the keys are three-letter International Air Transport Association codes, and the values are airport names: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var airports: [String: String] = ["YYZ": "Toronto Pearson", "DUB": "Dublin"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;airports&lt;/code&gt; dictionary is declared as having a type of &lt;code&gt;[String: String]&lt;/code&gt;, which means “a &lt;code&gt;Dictionary&lt;/code&gt; whose keys are of type &lt;code&gt;String&lt;/code&gt;, and whose values are also of type &lt;code&gt;String&lt;/code&gt;”. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;airports&lt;/code&gt; dictionary is declared as a variable (with the &lt;code&gt;var&lt;/code&gt; introducer), and not a constant (with the &lt;code&gt;let&lt;/code&gt; introducer), because more airports are added to the dictionary in the examples below. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;airports&lt;/code&gt; dictionary is initialized with a dictionary literal containing two key-value pairs. The first pair has a key of &lt;code&gt;"YYZ"&lt;/code&gt; and a value of &lt;code&gt;"Toronto Pearson"&lt;/code&gt;. The second pair has a key of &lt;code&gt;"DUB"&lt;/code&gt; and a value of &lt;code&gt;"Dublin"&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;This dictionary literal contains two &lt;code&gt;String: String&lt;/code&gt; pairs. This key-value type matches the type of the &lt;code&gt;airports&lt;/code&gt; variable declaration (a dictionary with only &lt;code&gt;String&lt;/code&gt; keys, and only &lt;code&gt;String&lt;/code&gt; values), and so the assignment of the dictionary literal is permitted as a way to initialize the &lt;code&gt;airports&lt;/code&gt; dictionary with two initial items. &lt;/p&gt;
&lt;p&gt;As with arrays, you don’t have to write the type of the dictionary if you’re initializing it with a dictionary literal whose keys and values have consistent types. The initialization of &lt;code&gt;airports&lt;/code&gt; could have been be written in a shorter form instead: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var airports = ["YYZ": "Toronto Pearson", "DUB": "Dublin"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because all keys in the literal are of the same type as each other, and likewise all values are of the same type as each other, Swift can infer that &lt;code&gt;[String: String]&lt;/code&gt; is the correct type to use for the &lt;code&gt;airports&lt;/code&gt; dictionary. &lt;/p&gt;
&lt;h4&gt;Accessing and Modifying a Dictionary&lt;/h4&gt;
&lt;p&gt;You access and modify a dictionary through its methods and properties, or by using subscript syntax. As with an array, you find out the number of items in a &lt;code&gt;Dictionary&lt;/code&gt; by checking its read-only &lt;code&gt;count&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The airports dictionary contains \\(airports.count) items.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The airports dictionary contains 2 items."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Use the Boolean &lt;code&gt;isEmpty&lt;/code&gt; property as a shortcut for checking whether the &lt;code&gt;count&lt;/code&gt; property is equal to &lt;code&gt;0&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if airports.isEmpty {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The airports dictionary is empty.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The airports dictionary is not empty.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The airports dictionary is not empty."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can add a new item to a dictionary with subscript syntax. Use a new key of the appropriate type as the subscript index, and assign a new value of the appropriate type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;airports["LHR"] = "London"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the airports dictionary now contains 3 items&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also use subscript syntax to change the value associated with a particular key: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;airports["LHR"] = "London Heathrow"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the value for "LHR" has been changed to "London Heathrow"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;As an alternative to subscripting, use a dictionary’s &lt;code&gt;updateValue(_:forKey:)&lt;/code&gt; method to set or update the value for a particular key. Like the subscript examples above, the &lt;code&gt;updateValue(_:forKey:)&lt;/code&gt; method sets a value for a key if none exists, or updates the value if that key already exists. Unlike a subscript, however, the &lt;code&gt;updateValue(_:forKey:)&lt;/code&gt; method returns the &lt;em&gt;old&lt;/em&gt; value after performing an update. This enables you to check whether or not an update took place. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;updateValue(_:forKey:)&lt;/code&gt; method returns an optional value of the dictionary’s value type. For a dictionary that stores &lt;code&gt;String&lt;/code&gt; values, for example, the method returns a value of type &lt;code&gt;String?&lt;/code&gt;, or “optional &lt;code&gt;String&lt;/code&gt;”. This optional value contains the old value for that key if one existed before the update, or &lt;code&gt;nil&lt;/code&gt; if no value existed: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let oldValue = airports.updateValue("Dublin Airport", forKey: "DUB") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The old value for DUB was \\(oldValue).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The old value for DUB was Dublin."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also use subscript syntax to retrieve a value from the dictionary for a particular key. Because it is possible to request a key for which no value exists, a dictionary’s subscript returns an optional value of the dictionary’s value type. If the dictionary contains a value for the requested key, the subscript returns an optional value containing the existing value for that key. Otherwise, the subscript returns &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let airportName = airports["DUB"] {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The name of the airport is \\(airportName).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("That airport is not in the airports dictionary.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The name of the airport is Dublin Airport."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can use subscript syntax to remove a key-value pair from a dictionary by assigning a value of &lt;code&gt;nil&lt;/code&gt; for that key: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;airports["APL"] = "Apple International"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// "Apple International" is not the real airport for APL, so delete it&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;airports["APL"] = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// APL has now been removed from the dictionary&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Alternatively, remove a key-value pair from a dictionary with the &lt;code&gt;removeValueForKey&lt;/code&gt; method. This method removes the key-value pair if it exists and returns the removed value, or returns &lt;code&gt;nil&lt;/code&gt; if no value existed: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let removedValue = airports.removeValueForKey("DUB") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The removed airport's name is \\(removedValue).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The airports dictionary does not contain a value for DUB.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The removed airport's name is Dublin Airport."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Iterating Over a Dictionary&lt;/h4&gt;
&lt;p&gt;You can iterate over the key-value pairs in a dictionary with a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop. Each item in the dictionary is returned as a &lt;code&gt;(key, value)&lt;/code&gt; tuple, and you can decompose the tuple’s members into temporary constants or variables as part of the iteration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for (airportCode, airportName) in airports {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(airportCode): \\(airportName)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// YYZ: Toronto Pearson&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// LHR: London Heathrow&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more about the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop, see For Loops. &lt;/p&gt;
&lt;p&gt;You can also retrieve an iterable collection of a dictionary’s keys or values by accessing its &lt;code&gt;keys&lt;/code&gt; and &lt;code&gt;values&lt;/code&gt; properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for airportCode in airports.keys {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Airport code: \\(airportCode)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Airport code: YYZ&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Airport code: LHR&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for airportName in airports.values {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Airport name: \\(airportName)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Airport name: Toronto Pearson&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Airport name: London Heathrow&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you need to use a dictionary’s keys or values with an API that takes an &lt;code&gt;Array&lt;/code&gt; instance, initialize a new array with the &lt;code&gt;keys&lt;/code&gt; or &lt;code&gt;values&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let airportCodes = [String](airports.keys)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// airportCodes is ["YYZ", "LHR"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let airportNames = [String](airports.values)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// airportNames is ["Toronto Pearson", "London Heathrow"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift’s &lt;code&gt;Dictionary&lt;/code&gt; type is an unordered collection. The order in which keys, values, and key-value pairs are retrieved when iterating over a dictionary is not specified. &lt;/p&gt;
&lt;h4&gt;Creating an Empty Dictionary&lt;/h4&gt;
&lt;p&gt;As with arrays, you can create an empty &lt;code&gt;Dictionary&lt;/code&gt; of a certain type by using initializer syntax: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var namesOfIntegers = [Int: String]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// namesOfIntegers is an empty [Int: String] dictionary&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example creates an empty dictionary of type &lt;code&gt;[Int: String]&lt;/code&gt; to store human-readable names of integer values. Its keys are of type &lt;code&gt;Int&lt;/code&gt;, and its values are of type &lt;code&gt;String&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If the context already provides type information, you can create an empty dictionary with an empty dictionary literal, which is written as &lt;code&gt;[:]&lt;/code&gt; (a colon inside a pair of square brackets): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;namesOfIntegers[16] = "sixteen"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// namesOfIntegers now contains 1 key-value pair&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;namesOfIntegers = [:]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// namesOfIntegers is once again an empty dictionary of type [Int: String]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Hash Values for Dictionary Key Types&lt;/h4&gt;
&lt;p&gt;A type must be &lt;em&gt;hashable&lt;/em&gt; in order to be used as a dictionary’s key type—that is, the type must provide a way to compute a &lt;em&gt;hash value&lt;/em&gt; for itself. A hash value is an &lt;code&gt;Int&lt;/code&gt; value that is the same for all objects that compare equal, such that if &lt;code&gt;a == b&lt;/code&gt;, it follows that &lt;code&gt;a.hashValue == b.hashValue&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;All of Swift’s basic types (such as &lt;code&gt;String&lt;/code&gt;, &lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;Double&lt;/code&gt;, and &lt;code&gt;Bool&lt;/code&gt;) are hashable by default, and all of these types can be used as the keys of a dictionary. Enumeration member values without associated values (as described in Enumerations) are also hashable by default. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You can use your own custom types as dictionary key types by making them conform to the &lt;code&gt;Hashable&lt;/code&gt; protocol from Swift’s standard library. Types that conform to the &lt;code&gt;Hashable&lt;/code&gt; protocol must provide a gettable &lt;code&gt;Int&lt;/code&gt; property called &lt;code&gt;hashValue&lt;/code&gt;, and must also provide an implementation of the “is equal” operator (&lt;code&gt;==&lt;/code&gt;). The value returned by a type’s &lt;code&gt;hashValue&lt;/code&gt; property is not required to be the same across different executions of the same program, or in different programs. &lt;/p&gt;
&lt;p&gt;For more information about conforming to protocols, see Protocols. &lt;/p&gt;
&lt;h2&gt;Control Flow&lt;/h2&gt;
&lt;p&gt;Swift provides all the familiar control flow statements from C-like languages. These include &lt;code&gt;for&lt;/code&gt; and &lt;code&gt;while&lt;/code&gt; loops to perform a task multiple times; &lt;code&gt;if&lt;/code&gt; and &lt;code&gt;switch&lt;/code&gt; statements to execute different branches of code based on certain conditions; and statements such as &lt;code&gt;break&lt;/code&gt; and &lt;code&gt;continue&lt;/code&gt; to transfer the flow of execution to another point in your code. &lt;/p&gt;
&lt;p&gt;In addition to the traditional &lt;code&gt;for&lt;/code&gt; loop found in C, Swift adds a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop that makes it easy to iterate over arrays, dictionaries, ranges, strings, and other sequences. &lt;/p&gt;
&lt;p&gt;Swift’s &lt;code&gt;switch&lt;/code&gt; statement is also considerably more powerful than its counterpart in C. The cases of a &lt;code&gt;switch&lt;/code&gt; statement do not “fall through” to the next case in Swift, avoiding common C errors caused by missing &lt;code&gt;break&lt;/code&gt; statements. Cases can match many different patterns, including interval matches, tuples, and casts to a specific type. Matched values in a &lt;code&gt;switch&lt;/code&gt; case can be bound to temporary constants or variables for use within the case’s body, and complex matching conditions can be expressed with a &lt;code&gt;where&lt;/code&gt; clause for each case. &lt;/p&gt;
&lt;h3&gt;For Loops&lt;/h3&gt;
&lt;p&gt;Swift provides two kinds of loop that perform a set of statements a certain number of times: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop performs a set of statements for each item in a sequence. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;for&lt;/code&gt; loop performs a set of statements until a specific condition is met, typically by incrementing a counter each time the loop ends. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;For-In&lt;/h4&gt;
&lt;p&gt;You use the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop to iterate over a sequence, such as ranges of numbers, items in an array, or characters in a string. &lt;/p&gt;
&lt;p&gt;This example prints the first few entries in the five-times-table: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for index in 1...5 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(index) times 5 is \\(index * 5)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 1 times 5 is 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 2 times 5 is 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 3 times 5 is 15&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 4 times 5 is 20&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 5 times 5 is 25&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The sequence being iterated is a range of numbers from &lt;code&gt;1&lt;/code&gt; to &lt;code&gt;5&lt;/code&gt;, inclusive, as indicated by the use of the closed range operator (&lt;code&gt;...&lt;/code&gt;). The value of &lt;code&gt;index&lt;/code&gt; is set to the first number in the range (&lt;code&gt;1&lt;/code&gt;), and the statements inside the loop are executed. In this case, the loop contains only one statement, which prints an entry from the five-times-table for the current value of &lt;code&gt;index&lt;/code&gt;. After the statement is executed, the value of &lt;code&gt;index&lt;/code&gt; is updated to contain the second value in the range (&lt;code&gt;2&lt;/code&gt;), and the &lt;code&gt;println&lt;/code&gt; function is called again. This process continues until the end of the range is reached. &lt;/p&gt;
&lt;p&gt;In the example above, &lt;code&gt;index&lt;/code&gt; is a constant whose value is automatically set at the start of each iteration of the loop. As such, it does not have to be declared before it is used. It is implicitly declared simply by its inclusion in the loop declaration, without the need for a &lt;code&gt;let&lt;/code&gt; declaration keyword. &lt;/p&gt;
&lt;p&gt;If you don’t need each value from a sequence, you can ignore the values by using an underscore in place of a variable name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let base = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let power = 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var answer = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for _ in 1...power {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;answer *= base&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(base) to the power of \\(power) is \\(answer)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "3 to the power of 10 is 59049"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example calculates the value of one number to the power of another (in this case, &lt;code&gt;3&lt;/code&gt; to the power of &lt;code&gt;10&lt;/code&gt;). It multiplies a starting value of &lt;code&gt;1&lt;/code&gt; (that is, &lt;code&gt;3&lt;/code&gt; to the power of &lt;code&gt;0&lt;/code&gt;) by &lt;code&gt;3&lt;/code&gt;, ten times, using a closed range that starts with &lt;code&gt;1&lt;/code&gt; and ends with &lt;code&gt;10&lt;/code&gt;. This calculation doesn’t need to know the individual counter values each time through the loop—it simply needs to execute the loop the correct number of times. The underscore character &lt;code&gt;_&lt;/code&gt; (used in place of a loop variable) causes the individual values to be ignored and does not provide access to the current value during each iteration of the loop. &lt;/p&gt;
&lt;p&gt;Use a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop with an array to iterate over its items: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let names = ["Anna", "Alex", "Brian", "Jack"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for name in names {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Hello, \\(name)!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello, Anna!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello, Alex!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello, Brian!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello, Jack!&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also iterate over a dictionary to access its key-value pairs. Each item in the dictionary is returned as a &lt;code&gt;(key, value)&lt;/code&gt; tuple when the dictionary is iterated, and you can decompose the &lt;code&gt;(key, value)&lt;/code&gt; tuple’s members as explicitly named constants for use within in the body of the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop. Here, the dictionary’s keys are decomposed into a constant called &lt;code&gt;animalName&lt;/code&gt;, and the dictionary’s values are decomposed into a constant called &lt;code&gt;legCount&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let numberOfLegs = ["spider": 8, "ant": 6, "cat": 4]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for (animalName, legCount) in numberOfLegs {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(animalName)s have \\(legCount) legs")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// ants have 6 legs&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// cats have 4 legs&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// spiders have 8 legs&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Items in a &lt;code&gt;Dictionary&lt;/code&gt; may not necessarily be iterated in the same order as they were inserted. The contents of a &lt;code&gt;Dictionary&lt;/code&gt; are inherently unordered, and iterating over them does not guarantee the order in which they will be retrieved. For more on arrays and dictionaries, see Collection Types.) &lt;/p&gt;
&lt;p&gt;In addition to arrays and dictionaries, you can also use the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop to iterate over the &lt;code&gt;Character&lt;/code&gt; values in a string: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for character in "Hello" {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(character)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// H&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// e&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// l&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// l&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// o&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;For&lt;/h4&gt;
&lt;p&gt;In addition to &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loops, Swift supports traditional C-style &lt;code&gt;for&lt;/code&gt; loops with a condition and an incrementer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for var index = 0; index &amp;lt; 3; ++index {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("index is \\(index)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// index is 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// index is 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// index is 2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s the general form of this loop format: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;for &lt;em&gt;initialization&lt;/em&gt;; &lt;em&gt;condition&lt;/em&gt;; &lt;em&gt;increment&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Semicolons separate the three parts of the loop’s definition, as in C. However, unlike C, Swift doesn’t need parentheses around the entire “initialization; condition; increment” block. &lt;/p&gt;
&lt;p&gt;The loop is executed as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;When the loop is first entered, the &lt;em&gt;initialization expression&lt;/em&gt; is evaluated once, to set up any constants or variables that are needed for the loop. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;em&gt;condition expression&lt;/em&gt; is evaluated. If it evaluates to &lt;code&gt;false&lt;/code&gt;, the loop ends, and code execution continues after the &lt;code&gt;for&lt;/code&gt; loop’s closing brace (&lt;code&gt;}&lt;/code&gt;). If the expression evaluates to &lt;code&gt;true&lt;/code&gt;, code execution continues by executing the statements inside the braces. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;After all statements are executed, the &lt;em&gt;increment expression&lt;/em&gt; is evaluated. It might increase or decrease the value of a counter, or set one of the initialized variables to a new value based on the outcome of the statements. After the increment expression has been evaluated, execution returns to step 2, and the condition expression is evaluated again. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The loop format and execution process described above is shorthand for (and equivalent to) the outline below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;initialization&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;while &lt;em&gt;condition&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;increment&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Constants and variables declared within the initialization expression (such as &lt;code&gt;var index = 0&lt;/code&gt;) are only valid within the scope of the &lt;code&gt;for&lt;/code&gt; loop itself. To retrieve the final value of &lt;code&gt;index&lt;/code&gt; after the loop ends, you must declare &lt;code&gt;index&lt;/code&gt; before the loop’s scope begins: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var index: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for index = 0; index &amp;lt; 3; ++index {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("index is \\(index)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// index is 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// index is 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// index is 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The loop statements were executed \\(index) times")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The loop statements were executed 3 times"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the final value of &lt;code&gt;index&lt;/code&gt; after this loop is completed is &lt;code&gt;3&lt;/code&gt;, not &lt;code&gt;2&lt;/code&gt;. The last time the increment statement &lt;code&gt;++index&lt;/code&gt; is called, it sets &lt;code&gt;index&lt;/code&gt; to &lt;code&gt;3&lt;/code&gt;, which causes &lt;code&gt;index &amp;lt; 3&lt;/code&gt; to equate to &lt;code&gt;false&lt;/code&gt;, ending the loop. &lt;/p&gt;
&lt;h3&gt;While Loops&lt;/h3&gt;
&lt;p&gt;A &lt;code&gt;while&lt;/code&gt; loop performs a set of statements until a condition becomes &lt;code&gt;false&lt;/code&gt;. These kinds of loops are best used when the number of iterations is not known before the first iteration begins. Swift provides two kinds of &lt;code&gt;while&lt;/code&gt; loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;while&lt;/code&gt; evaluates its condition at the start of each pass through the loop. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; evaluates its condition at the end of each pass through the loop. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;While&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;while&lt;/code&gt; loop starts by evaluating a single condition. If the condition is &lt;code&gt;true&lt;/code&gt;, a set of statements is repeated until the condition becomes &lt;code&gt;false&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Here’s the general form of a &lt;code&gt;while&lt;/code&gt; loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;while &lt;em&gt;condition&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example plays a simple game of &lt;em&gt;Snakes and Ladders&lt;/em&gt; (also known as &lt;em&gt;Chutes and Ladders&lt;/em&gt;): &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/snakesAndLadders_2x.png" src="http://muxuezi.github.io/posts/Art/snakesAndLadders_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The rules of the game are as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The board has 25 squares, and the aim is to land on or beyond square 25. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Each turn, you roll a six-sided dice and move by that number of squares, following the horizontal path indicated by the dotted arrow above. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If your turn ends at the bottom of a ladder, you move up that ladder. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If your turn ends at the head of a snake, you move down that snake. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The game board is represented by an array of &lt;code&gt;Int&lt;/code&gt; values. Its size is based on a constant called &lt;code&gt;finalSquare&lt;/code&gt;, which is used to initialize the array and also to check for a win condition later in the example. The board is initialized with 26 zero &lt;code&gt;Int&lt;/code&gt; values, not 25 (one each at indices &lt;code&gt;0&lt;/code&gt; through &lt;code&gt;25&lt;/code&gt; inclusive): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let finalSquare = 25&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var board = [Int](count: finalSquare \+ 1, repeatedValue: 0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Some squares are then set to have more specific values for the snakes and ladders. Squares with a ladder base have a positive number to move you up the board, whereas squares with a snake head have a negative number to move you back down the board: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;board[03] = +08; board[06] = +11; board[09] = +09; board[10] = +02&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board[14] = -10; board[19] = -11; board[22] = -02; board[24] = -08&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Square 3 contains the bottom of a ladder that moves you up to square 11. To represent this, &lt;code&gt;board[03]&lt;/code&gt; is equal to &lt;code&gt;+08&lt;/code&gt;, which is equivalent to an integer value of &lt;code&gt;8&lt;/code&gt; (the difference between &lt;code&gt;3&lt;/code&gt; and &lt;code&gt;11&lt;/code&gt;). The unary plus operator (&lt;code&gt;+i&lt;/code&gt;) balances with the unary minus operator (&lt;code&gt;-i&lt;/code&gt;), and numbers lower than &lt;code&gt;10&lt;/code&gt; are padded with zeros so that all board definitions align. (Neither stylistic tweak is strictly necessary, but they lead to neater code.) &lt;/p&gt;
&lt;p&gt;The player’s starting square is “square zero”, which is just off the bottom left corner of the board. The first dice roll always moves the player on to the board: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var square = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var diceRoll = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;while square &amp;lt; finalSquare {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// roll the dice&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if ++diceRoll == 7 { diceRoll = 1 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// move by the rolled amount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += diceRoll&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if square &amp;lt; board.count {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// if we're still on the board, move up or down for a snake or a ladder&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += board[square]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Game over!")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example uses a very simple approach to dice rolling. Instead of a random number generator, it starts with a &lt;code&gt;diceRoll&lt;/code&gt; value of &lt;code&gt;0&lt;/code&gt;. Each time through the &lt;code&gt;while&lt;/code&gt; loop, &lt;code&gt;diceRoll&lt;/code&gt; is incremented with the prefix increment operator (&lt;code&gt;++i&lt;/code&gt;), and is then checked to see if it has become too large. The return value of &lt;code&gt;++diceRoll&lt;/code&gt; is equal to the value of &lt;code&gt;diceRoll&lt;/code&gt; &lt;em&gt;after&lt;/em&gt; it is incremented. Whenever this return value equals &lt;code&gt;7&lt;/code&gt;, the dice roll has become too large, and is reset to a value of &lt;code&gt;1&lt;/code&gt;. This gives a sequence of &lt;code&gt;diceRoll&lt;/code&gt; values that is always &lt;code&gt;1&lt;/code&gt;, &lt;code&gt;2&lt;/code&gt;, &lt;code&gt;3&lt;/code&gt;, &lt;code&gt;4&lt;/code&gt;, &lt;code&gt;5&lt;/code&gt;, &lt;code&gt;6&lt;/code&gt;, &lt;code&gt;1&lt;/code&gt;, &lt;code&gt;2&lt;/code&gt; and so on. &lt;/p&gt;
&lt;p&gt;After rolling the dice, the player moves forward by &lt;code&gt;diceRoll&lt;/code&gt; squares. It’s possible that the dice roll may have moved the player beyond square 25, in which case the game is over. To cope with this scenario, the code checks that &lt;code&gt;square&lt;/code&gt; is less than the &lt;code&gt;board&lt;/code&gt; array’s &lt;code&gt;count&lt;/code&gt; property before adding the value stored in &lt;code&gt;board[square]&lt;/code&gt; onto the current &lt;code&gt;square&lt;/code&gt; value to move the player up or down any ladders or snakes. &lt;/p&gt;
&lt;p&gt;Had this check not been performed, &lt;code&gt;board[square]&lt;/code&gt; might try to access a value outside the bounds of the &lt;code&gt;board&lt;/code&gt; array, which would trigger an error. If &lt;code&gt;square&lt;/code&gt; is now equal to &lt;code&gt;26&lt;/code&gt;, the code would try to check the value of &lt;code&gt;board[26]&lt;/code&gt;, which is larger than the size of the array. &lt;/p&gt;
&lt;p&gt;The current &lt;code&gt;while&lt;/code&gt; loop execution then ends, and the loop’s condition is checked to see if the loop should be executed again. If the player has moved on or beyond square number &lt;code&gt;25&lt;/code&gt;, the loop’s condition evaluates to &lt;code&gt;false&lt;/code&gt;, and the game ends. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;while&lt;/code&gt; loop is appropriate in this case because the length of the game is not clear at the start of the &lt;code&gt;while&lt;/code&gt; loop. Instead, the loop is executed until a particular condition is satisfied. &lt;/p&gt;
&lt;h4&gt;Do-While&lt;/h4&gt;
&lt;p&gt;The other variation of the &lt;code&gt;while&lt;/code&gt; loop, known as the &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; loop, performs a single pass through the loop block first, &lt;em&gt;before&lt;/em&gt; considering the loop’s condition. It then continues to repeat the loop until the condition is &lt;code&gt;false&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Here’s the general form of a &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;do {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;} while &lt;em&gt;condition&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s the &lt;em&gt;Snakes and Ladders&lt;/em&gt; example again, written as a &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; loop rather than a &lt;code&gt;while&lt;/code&gt; loop. The values of &lt;code&gt;finalSquare&lt;/code&gt;, &lt;code&gt;board&lt;/code&gt;, &lt;code&gt;square&lt;/code&gt;, and &lt;code&gt;diceRoll&lt;/code&gt; are initialized in exactly the same way as with a &lt;code&gt;while&lt;/code&gt; loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let finalSquare = 25&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var board = [Int](count: finalSquare \+ 1, repeatedValue: 0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board[03] = +08; board[06] = +11; board[09] = +09; board[10] = +02&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board[14] = -10; board[19] = -11; board[22] = -02; board[24] = -08&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var square = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var diceRoll = 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this version of the game, the &lt;em&gt;first&lt;/em&gt; action in the loop is to check for a ladder or a snake. No ladder on the board takes the player straight to square 25, and so it is not possible to win the game by moving up a ladder. Therefore, it is safe to check for a snake or a ladder as the first action in the loop. &lt;/p&gt;
&lt;p&gt;At the start of the game, the player is on “square zero”. &lt;code&gt;board[0]&lt;/code&gt; always equals &lt;code&gt;0&lt;/code&gt;, and has no effect: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;do {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// move up or down for a snake or ladder&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += board[square]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// roll the dice&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if ++diceRoll == 7 { diceRoll = 1 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// move by the rolled amount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += diceRoll&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} while square &amp;lt; finalSquare&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Game over!")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;After the code checks for snakes and ladders, the dice is rolled, and the player is moved forward by &lt;code&gt;diceRoll&lt;/code&gt; squares. The current loop execution then ends. &lt;/p&gt;
&lt;p&gt;The loop’s condition (&lt;code&gt;while square &amp;lt; finalSquare&lt;/code&gt;) is the same as before, but this time it is not evaluated until the &lt;em&gt;end&lt;/em&gt; of the first run through the loop. The structure of the &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; loop is better suited to this game than the &lt;code&gt;while&lt;/code&gt; loop in the previous example. In the &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; loop above, &lt;code&gt;square += board[square]&lt;/code&gt; is always executed &lt;em&gt;immediately after&lt;/em&gt; the loop’s &lt;code&gt;while&lt;/code&gt; condition confirms that &lt;code&gt;square&lt;/code&gt; is still on the board. This behavior removes the need for the array bounds check seen in the earlier version of the game. &lt;/p&gt;
&lt;h3&gt;Conditional Statements&lt;/h3&gt;
&lt;p&gt;It is often useful to execute different pieces of code based on certain conditions. You might want to run an extra piece of code when an error occurs, or to display a message when a value becomes too high or too low. To do this, you make parts of your code &lt;em&gt;conditional&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;Swift provides two ways to add conditional branches to your code, known as the &lt;code&gt;if&lt;/code&gt; statement and the &lt;code&gt;switch&lt;/code&gt; statement. Typically, you use the &lt;code&gt;if&lt;/code&gt; statement to evaluate simple conditions with only a few possible outcomes. The &lt;code&gt;switch&lt;/code&gt; statement is better suited to more complex conditions with multiple possible permutations, and is useful in situations where pattern-matching can help select an appropriate code branch to execute. &lt;/p&gt;
&lt;h4&gt;If&lt;/h4&gt;
&lt;p&gt;In its simplest form, the &lt;code&gt;if&lt;/code&gt; statement has a single &lt;code&gt;if&lt;/code&gt; condition. It executes a set of statements only if that condition is &lt;code&gt;true&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var temperatureInFahrenheit = 30&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if temperatureInFahrenheit &amp;lt;= 32 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's very cold. Consider wearing a scarf.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "It's very cold. Consider wearing a scarf."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The preceding example checks whether the temperature is less than or equal to 32 degrees Fahrenheit (the freezing point of water). If it is, a message is printed. Otherwise, no message is printed, and code execution continues after the &lt;code&gt;if&lt;/code&gt; statement’s closing brace. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;if&lt;/code&gt; statement can provide an alternative set of statements, known as an &lt;em&gt;else clause&lt;/em&gt;, for when the &lt;code&gt;if&lt;/code&gt; condition is &lt;code&gt;false&lt;/code&gt;. These statements are indicated by the &lt;code&gt;else&lt;/code&gt; keyword: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;temperatureInFahrenheit = 40&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if temperatureInFahrenheit &amp;lt;= 32 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's very cold. Consider wearing a scarf.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's not that cold. Wear a t-shirt.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "It's not that cold. Wear a t-shirt."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;One of these two branches is always executed. Because the temperature has increased to &lt;code&gt;40&lt;/code&gt; degrees Fahrenheit, it is no longer cold enough to advise wearing a scarf, and so the &lt;code&gt;else&lt;/code&gt; branch is triggered instead. &lt;/p&gt;
&lt;p&gt;You can chain multiple &lt;code&gt;if&lt;/code&gt; statements together, to consider additional clauses: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;temperatureInFahrenheit = 90&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if temperatureInFahrenheit &amp;lt;= 32 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's very cold. Consider wearing a scarf.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if temperatureInFahrenheit &amp;gt;= 86 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's really warm. Don't forget to wear sunscreen.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's not that cold. Wear a t-shirt.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "It's really warm. Don't forget to wear sunscreen."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, an additional &lt;code&gt;if&lt;/code&gt; statement is added to respond to particularly warm temperatures. The final &lt;code&gt;else&lt;/code&gt; clause remains, and prints a response for any temperatures that are neither too warm nor too cold. &lt;/p&gt;
&lt;p&gt;The final &lt;code&gt;else&lt;/code&gt; clause is optional, however, and can be excluded if the set of conditions does not need to be complete: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;temperatureInFahrenheit = 72&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if temperatureInFahrenheit &amp;lt;= 32 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's very cold. Consider wearing a scarf.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if temperatureInFahrenheit &amp;gt;= 86 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It's really warm. Don't forget to wear sunscreen.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this example, the temperature is neither too cold nor too warm to trigger the &lt;code&gt;if&lt;/code&gt; or &lt;code&gt;else if&lt;/code&gt; conditions, and so no message is printed. &lt;/p&gt;
&lt;h4&gt;Switch&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;switch&lt;/code&gt; statement considers a value and compares it against several possible matching patterns. It then executes an appropriate block of code, based on the first pattern that matches successfully. A &lt;code&gt;switch&lt;/code&gt; statement provides an alternative to the &lt;code&gt;if&lt;/code&gt; statement for responding to multiple potential states. &lt;/p&gt;
&lt;p&gt;In its simplest form, a &lt;code&gt;switch&lt;/code&gt; statement compares a value against one or more values of the same type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;switch &lt;em&gt;some value to consider&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;value 1&lt;/em&gt;:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;respond to value 1&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;value 2&lt;/em&gt;,&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;value 3&lt;/em&gt;:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;respond to value 2 or 3&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;default:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;otherwise, do something else&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Every &lt;code&gt;switch&lt;/code&gt; statement consists of multiple possible &lt;em&gt;cases&lt;/em&gt;, each of which begins with the &lt;code&gt;case&lt;/code&gt; keyword. In addition to comparing against specific values, Swift provides several ways for each case to specify more complex matching patterns. These options are described later in this section. &lt;/p&gt;
&lt;p&gt;The body of each &lt;code&gt;switch&lt;/code&gt; case is a separate branch of code execution, in a similar manner to the branches of an &lt;code&gt;if&lt;/code&gt; statement. The &lt;code&gt;switch&lt;/code&gt; statement determines which branch should be selected. This is known as &lt;em&gt;switching&lt;/em&gt; on the value that is being considered. &lt;/p&gt;
&lt;p&gt;Every &lt;code&gt;switch&lt;/code&gt; statement must be &lt;em&gt;exhaustive&lt;/em&gt;. That is, every possible value of the type being considered must be matched by one of the &lt;code&gt;switch&lt;/code&gt; cases. If it is not appropriate to provide a &lt;code&gt;switch&lt;/code&gt; case for every possible value, you can define a default catch-all case to cover any values that are not addressed explicitly. This catch-all case is indicated by the keyword &lt;code&gt;default&lt;/code&gt;, and must always appear last. &lt;/p&gt;
&lt;p&gt;This example uses a &lt;code&gt;switch&lt;/code&gt; statement to consider a single lowercase character called &lt;code&gt;someCharacter&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someCharacter: Character = "e"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch someCharacter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "a", "e", "i", "o", "u":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(someCharacter) is a vowel")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "b", "c", "d", "f", "g", "h", "j", "k", "l", "m",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"n", "p", "q", "r", "s", "t", "v", "w", "x", "y", "z":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(someCharacter) is a consonant")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(someCharacter) is not a vowel or a consonant")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "e is a vowel"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;switch&lt;/code&gt; statement’s first case matches all five lowercase vowels in the English language. Similarly, its second case matches all lowercase English consonants. &lt;/p&gt;
&lt;p&gt;It is not practical to write all other possible characters as part of a &lt;code&gt;switch&lt;/code&gt; case, and so this &lt;code&gt;switch&lt;/code&gt; statement provides a &lt;code&gt;default&lt;/code&gt; case to match all other characters that are not vowels or consonants. This provision ensures that the &lt;code&gt;switch&lt;/code&gt; statement is exhaustive. &lt;/p&gt;
&lt;h4&gt;No Implicit Fallthrough&lt;/h4&gt;
&lt;p&gt;In contrast with &lt;code&gt;switch&lt;/code&gt; statements in C and Objective-C, &lt;code&gt;switch&lt;/code&gt; statements in Swift do not fall through the bottom of each case and into the next one by default. Instead, the entire &lt;code&gt;switch&lt;/code&gt; statement finishes its execution as soon as the first matching &lt;code&gt;switch&lt;/code&gt; case is completed, without requiring an explicit &lt;code&gt;break&lt;/code&gt; statement. This makes the &lt;code&gt;switch&lt;/code&gt; statement safer and easier to use than in C, and avoids executing more than one &lt;code&gt;switch&lt;/code&gt; case by mistake. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Although &lt;code&gt;break&lt;/code&gt; is not required in Swift, you can still use a &lt;code&gt;break&lt;/code&gt; statement to match and ignore a particular case, or to break out of a matched case before that case has completed its execution. See Break in a Switch Statement for details. &lt;/p&gt;
&lt;p&gt;The body of each case &lt;em&gt;must&lt;/em&gt; contain at least one executable statement. It is not valid to write the following code, because the first case is empty: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let anotherCharacter: Character = "a"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch anotherCharacter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "a":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "A":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The letter A")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Not the letter A")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this will report a compile-time error&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unlike a &lt;code&gt;switch&lt;/code&gt; statement in C, this &lt;code&gt;switch&lt;/code&gt; statement does not match both &lt;code&gt;"a"&lt;/code&gt; and &lt;code&gt;"A"&lt;/code&gt;. Rather, it reports a compile-time error that &lt;code&gt;case "a":&lt;/code&gt; does not contain any executable statements. This approach avoids accidental fallthrough from one case to another, and makes for safer code that is clearer in its intent. &lt;/p&gt;
&lt;p&gt;Multiple matches for a single &lt;code&gt;switch&lt;/code&gt; case can be separated by commas, and can be written over multiple lines if the list is long: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;switch &lt;em&gt;some value to consider&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;value 1&lt;/em&gt;,&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;value 2&lt;/em&gt;:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;To opt in to fallthrough behavior for a particular &lt;code&gt;switch&lt;/code&gt; case, use the &lt;code&gt;fallthrough&lt;/code&gt; keyword, as described in Fallthrough. &lt;/p&gt;
&lt;h4&gt;Interval Matching&lt;/h4&gt;
&lt;p&gt;Values in &lt;code&gt;switch&lt;/code&gt; cases can be checked for their inclusion in an interval. This example uses number intervals to provide a natural-language count for numbers of any size: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let count = 3_000_000_000_000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let countedThings = "stars in the Milky Way"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var naturalCount: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch count {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 0:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;naturalCount = "no"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 1...3:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;naturalCount = "a few"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 4...9:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;naturalCount = "several"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 10...99:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;naturalCount = "tens of"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 100...999:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;naturalCount = "hundreds of"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 1000...999_999:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;naturalCount = "thousands of"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;naturalCount = "millions and millions of"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("There are \\(naturalCount) \\(countedThings).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "There are millions and millions of stars in the Milky Way."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Both the closed range operator (&lt;code&gt;...&lt;/code&gt;) and half-open range operator (&lt;code&gt;..&amp;lt;&lt;/code&gt;) functions are overloaded to return either an &lt;code&gt;IntervalType&lt;/code&gt; or &lt;code&gt;Range&lt;/code&gt;. An interval can determine whether it contains a particular element, such as when matching a &lt;code&gt;switch&lt;/code&gt; statement &lt;code&gt;case&lt;/code&gt;. A range is a collection of consecutive values, which can be iterated on in a &lt;code&gt;for-in&lt;/code&gt; statement. &lt;/p&gt;
&lt;h4&gt;Tuples&lt;/h4&gt;
&lt;p&gt;You can use tuples to test multiple values in the same &lt;code&gt;switch&lt;/code&gt; statement. Each element of the tuple can be tested against a different value or interval of values. Alternatively, use the underscore (&lt;code&gt;_&lt;/code&gt;) identifier to match any possible value. &lt;/p&gt;
&lt;p&gt;The example below takes an (x, y) point, expressed as a simple tuple of type &lt;code&gt;(Int, Int)&lt;/code&gt;, and categorizes it on the graph that follows the example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let somePoint = (1, 1)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch somePoint {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (0, 0):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(0, 0) is at the origin")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (_, 0):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(\\(somePoint.0), 0) is on the x-axis")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (0, _):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(0, \\(somePoint.1)) is on the y-axis")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (-2...2, -2...2):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(\\(somePoint.0), \\(somePoint.1)) is inside the box")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(\\(somePoint.0), \\(somePoint.1)) is outside of the box")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "(1, 1) is inside the box"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img alt="image: ../Art/coordinateGraphSimple_2x.png" src="http://muxuezi.github.io/posts/Art/coordinateGraphSimple_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;switch&lt;/code&gt; statement determines if the point is at the origin (0, 0); on the red x-axis; on the orange y-axis; inside the blue 4-by-4 box centered on the origin; or outside of the box. &lt;/p&gt;
&lt;p&gt;Unlike C, Swift allows multiple &lt;code&gt;switch&lt;/code&gt; cases to consider the same value or values. In fact, the point (0, 0) could match all &lt;em&gt;four&lt;/em&gt; of the cases in this example. However, if multiple matches are possible, the first matching case is always used. The point (0, 0) would match &lt;code&gt;case (0, 0)&lt;/code&gt; first, and so all other matching cases would be ignored. &lt;/p&gt;
&lt;h4&gt;Value Bindings&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;switch&lt;/code&gt; case can bind the value or values it matches to temporary constants or variables, for use in the body of the case. This is known as &lt;em&gt;value binding&lt;/em&gt;, because the values are “bound” to temporary constants or variables within the case’s body. &lt;/p&gt;
&lt;p&gt;The example below takes an (x, y) point, expressed as a tuple of type &lt;code&gt;(Int, Int)&lt;/code&gt; and categorizes it on the graph that follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let anotherPoint = (2, 0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch anotherPoint {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (let x, 0):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("on the x-axis with an x value of \\(x)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (0, let y):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("on the y-axis with a y value of \\(y)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let (x, y):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("somewhere else at (\\(x), \\(y))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "on the x-axis with an x value of 2"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img alt="image: ../Art/coordinateGraphMedium_2x.png" src="http://muxuezi.github.io/posts/Art/coordinateGraphMedium_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;switch&lt;/code&gt; statement determines if the point is on the red x-axis, on the orange y-axis, or elsewhere, on neither axis. &lt;/p&gt;
&lt;p&gt;The three &lt;code&gt;switch&lt;/code&gt; cases declare placeholder constants &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt;, which temporarily take on one or both tuple values from &lt;code&gt;anotherPoint&lt;/code&gt;. The first case, &lt;code&gt;case (let x, 0)&lt;/code&gt;, matches any point with a &lt;code&gt;y&lt;/code&gt; value of &lt;code&gt;0&lt;/code&gt; and assigns the point’s &lt;code&gt;x&lt;/code&gt; value to the temporary constant &lt;code&gt;x&lt;/code&gt;. Similarly, the second case, &lt;code&gt;case (0, let y)&lt;/code&gt;, matches any point with an &lt;code&gt;x&lt;/code&gt; value of &lt;code&gt;0&lt;/code&gt; and assigns the point’s &lt;code&gt;y&lt;/code&gt; value to the temporary constant &lt;code&gt;y&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Once the temporary constants are declared, they can be used within the case’s code block. Here, they are used as shorthand for printing the values with the &lt;code&gt;println&lt;/code&gt; function. &lt;/p&gt;
&lt;p&gt;Note that this &lt;code&gt;switch&lt;/code&gt; statement does not have a &lt;code&gt;default&lt;/code&gt; case. The final case, &lt;code&gt;case let (x, y)&lt;/code&gt;, declares a tuple of two placeholder constants that can match any value. As a result, it matches all possible remaining values, and a &lt;code&gt;default&lt;/code&gt; case is not needed to make the &lt;code&gt;switch&lt;/code&gt; statement exhaustive. &lt;/p&gt;
&lt;p&gt;In the example above, &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; are declared as constants with the &lt;code&gt;let&lt;/code&gt; keyword, because there is no need to modify their values within the body of the case. However, they could have been declared as variables instead, with the &lt;code&gt;var&lt;/code&gt; keyword. If this had been done, a temporary variable would have been created and initialized with the appropriate value. Any changes to that variable would only have an effect within the body of the case. &lt;/p&gt;
&lt;h4&gt;Where&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;switch&lt;/code&gt; case can use a &lt;code&gt;where&lt;/code&gt; clause to check for additional conditions. &lt;/p&gt;
&lt;p&gt;The example below categorizes an (x, y) point on the following graph: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let yetAnotherPoint = (1, -1)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch yetAnotherPoint {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let (x, y) where x == y:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(\\(x), \\(y)) is on the line x == y")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let (x, y) where x == -y:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(\\(x), \\(y)) is on the line x == -y")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let (x, y):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(\\(x), \\(y)) is just some arbitrary point")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "(1, -1) is on the line x == -y"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img alt="image: ../Art/coordinateGraphComplex_2x.png" src="http://muxuezi.github.io/posts/Art/coordinateGraphComplex_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;switch&lt;/code&gt; statement determines if the point is on the green diagonal line where &lt;code&gt;x == y&lt;/code&gt;, on the purple diagonal line where &lt;code&gt;x == -y&lt;/code&gt;, or neither. &lt;/p&gt;
&lt;p&gt;The three &lt;code&gt;switch&lt;/code&gt; cases declare placeholder constants &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt;, which temporarily take on the two tuple values from &lt;code&gt;yetAnotherPoint&lt;/code&gt;. These constants are used as part of a &lt;code&gt;where&lt;/code&gt; clause, to create a dynamic filter. The &lt;code&gt;switch&lt;/code&gt; case matches the current value of &lt;code&gt;point&lt;/code&gt; only if the &lt;code&gt;where&lt;/code&gt; clause’s condition evaluates to &lt;code&gt;true&lt;/code&gt; for that value. &lt;/p&gt;
&lt;p&gt;As in the previous example, the final case matches all possible remaining values, and so a &lt;code&gt;default&lt;/code&gt; case is not needed to make the &lt;code&gt;switch&lt;/code&gt; statement exhaustive. &lt;/p&gt;
&lt;h3&gt;Control Transfer Statements&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Control transfer statements&lt;/em&gt; change the order in which your code is executed, by transferring control from one piece of code to another. Swift has four control transfer statements: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;continue&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;break&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;fallthrough&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;return&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;continue&lt;/code&gt;, &lt;code&gt;break&lt;/code&gt;, and &lt;code&gt;fallthrough&lt;/code&gt; statements are described below. The &lt;code&gt;return&lt;/code&gt; statement is described in Functions. &lt;/p&gt;
&lt;h4&gt;Continue&lt;/h4&gt;
&lt;p&gt;The &lt;code&gt;continue&lt;/code&gt; statement tells a loop to stop what it is doing and start again at the beginning of the next iteration through the loop. It says “I am done with the current loop iteration” without leaving the loop altogether. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In a &lt;code&gt;for&lt;/code&gt; loop with a condition and incrementer, the loop’s incrementer is still evaluated after calling the &lt;code&gt;continue&lt;/code&gt; statement. The loop itself continues to work as usual; only the code within the loop’s body is skipped. &lt;/p&gt;
&lt;p&gt;The following example removes all vowels and spaces from a lowercase string to create a cryptic puzzle phrase: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let puzzleInput = "great minds think alike"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var puzzleOutput = ""&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for character in puzzleInput {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch character {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "a", "e", "i", "o", "u", " ":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;continue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;puzzleOutput.append(character)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(puzzleOutput)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "grtmndsthnklk"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The code above calls the &lt;code&gt;continue&lt;/code&gt; keyword whenever it matches a vowel or a space, causing the current iteration of the loop to end immediately and to jump straight to the start of the next iteration. This behavior enables the switch block to match (and ignore) only the vowel and space characters, rather than requiring the block to match every character that should get printed. &lt;/p&gt;
&lt;h4&gt;Break&lt;/h4&gt;
&lt;p&gt;The &lt;code&gt;break&lt;/code&gt; statement ends execution of an entire control flow statement immediately. The &lt;code&gt;break&lt;/code&gt; statement can be used inside a &lt;code&gt;switch&lt;/code&gt; statement or loop statement when you want to terminate the execution of the &lt;code&gt;switch&lt;/code&gt; or loop statement earlier than would otherwise be the case. &lt;/p&gt;
&lt;h4&gt;Break in a Loop Statement&lt;/h4&gt;
&lt;p&gt;When used inside a loop statement, &lt;code&gt;break&lt;/code&gt; ends the loop’s execution immediately, and transfers control to the first line of code after the loop’s closing brace (&lt;code&gt;}&lt;/code&gt;). No further code from the current iteration of the loop is executed, and no further iterations of the loop are started. &lt;/p&gt;
&lt;h4&gt;Break in a Switch Statement&lt;/h4&gt;
&lt;p&gt;When used inside a &lt;code&gt;switch&lt;/code&gt; statement, &lt;code&gt;break&lt;/code&gt; causes the &lt;code&gt;switch&lt;/code&gt; statement to end its execution immediately, and to transfer control to the first line of code after the &lt;code&gt;switch&lt;/code&gt; statement’s closing brace (&lt;code&gt;}&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;This behavior can be used to match and ignore one or more cases in a &lt;code&gt;switch&lt;/code&gt; statement. Because Swift’s &lt;code&gt;switch&lt;/code&gt; statement is exhaustive and does not allow empty cases, it is sometimes necessary to deliberately match and ignore a case in order to make your intentions explicit. You do this by writing the &lt;code&gt;break&lt;/code&gt; statement as the entire body of the case you want to ignore. When that case is matched by the &lt;code&gt;switch&lt;/code&gt; statement, the &lt;code&gt;break&lt;/code&gt; statement inside the case ends the &lt;code&gt;switch&lt;/code&gt; statement’s execution immediately. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;switch&lt;/code&gt; case that only contains a comment is reported as a compile-time error. Comments are not statements and do not cause a &lt;code&gt;switch&lt;/code&gt; case to be ignored. Always use a &lt;code&gt;break&lt;/code&gt; statement to ignore a &lt;code&gt;switch&lt;/code&gt; case. &lt;/p&gt;
&lt;p&gt;The following example switches on a &lt;code&gt;Character&lt;/code&gt; value and determines whether it represents a number symbol in one of four languages. Multiple values are covered in a single &lt;code&gt;switch&lt;/code&gt; case for brevity: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let numberSymbol: Character = "三" // Simplified Chinese for the number 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var possibleIntegerValue: Int?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch numberSymbol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "1", "١", "一", "๑":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;possibleIntegerValue = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "2", "٢", "二", "๒":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;possibleIntegerValue = 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "3", "٣", "三", "๓":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;possibleIntegerValue = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "4", "٤", "四", "๔":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;possibleIntegerValue = 4&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;break&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let integerValue = possibleIntegerValue {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The integer value of \\(numberSymbol) is \\(integerValue).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("An integer value could not be found for \\(numberSymbol).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The integer value of 三 is 3."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example checks &lt;code&gt;numberSymbol&lt;/code&gt; to determine whether it is a Latin, Arabic, Chinese, or Thai symbol for the numbers &lt;code&gt;1&lt;/code&gt; to &lt;code&gt;4&lt;/code&gt;. If a match is found, one of the &lt;code&gt;switch&lt;/code&gt; statement’s cases sets an optional &lt;code&gt;Int?&lt;/code&gt; variable called &lt;code&gt;possibleIntegerValue&lt;/code&gt; to an appropriate integer value. &lt;/p&gt;
&lt;p&gt;After the switch statement completes its execution, the example uses optional binding to determine whether a value was found. The &lt;code&gt;possibleIntegerValue&lt;/code&gt; variable has an implicit initial value of &lt;code&gt;nil&lt;/code&gt; by virtue of being an optional type, and so the optional binding will succeed only if &lt;code&gt;possibleIntegerValue&lt;/code&gt; was set to an actual value by one of the &lt;code&gt;switch&lt;/code&gt; statement’s first four cases. &lt;/p&gt;
&lt;p&gt;It is not practical to list every possible &lt;code&gt;Character&lt;/code&gt; value in the example above, so a &lt;code&gt;default&lt;/code&gt; case provides a catchall for any characters that are not matched. This &lt;code&gt;default&lt;/code&gt; case does not need to perform any action, and so it is written with a single &lt;code&gt;break&lt;/code&gt; statement as its body. As soon as the &lt;code&gt;default&lt;/code&gt; case is matched, the &lt;code&gt;break&lt;/code&gt; statement ends the &lt;code&gt;switch&lt;/code&gt; statement’s execution, and code execution continues from the &lt;code&gt;if let&lt;/code&gt; statement. &lt;/p&gt;
&lt;h4&gt;Fallthrough&lt;/h4&gt;
&lt;p&gt;Switch statements in Swift do not fall through the bottom of each case and into the next one. Instead, the entire switch statement completes its execution as soon as the first matching case is completed. By contrast, C requires you to insert an explicit &lt;code&gt;break&lt;/code&gt; statement at the end of every &lt;code&gt;switch&lt;/code&gt; case to prevent fallthrough. Avoiding default fallthrough means that Swift &lt;code&gt;switch&lt;/code&gt; statements are much more concise and predictable than their counterparts in C, and thus they avoid executing multiple &lt;code&gt;switch&lt;/code&gt; cases by mistake. &lt;/p&gt;
&lt;p&gt;If you really need C-style fallthrough behavior, you can opt in to this behavior on a case-by-case basis with the &lt;code&gt;fallthrough&lt;/code&gt; keyword. The example below uses &lt;code&gt;fallthrough&lt;/code&gt; to create a textual description of a number: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let integerToDescribe = 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var description = "The number \\(integerToDescribe) is"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch integerToDescribe {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 2, 3, 5, 7, 11, 13, 17, 19:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;description += " a prime number, and also"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;fallthrough&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;description += " an integer."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(description)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The number 5 is a prime number, and also an integer."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example declares a new &lt;code&gt;String&lt;/code&gt; variable called &lt;code&gt;description&lt;/code&gt; and assigns it an initial value. The function then considers the value of &lt;code&gt;integerToDescribe&lt;/code&gt; using a &lt;code&gt;switch&lt;/code&gt; statement. If the value of &lt;code&gt;integerToDescribe&lt;/code&gt; is one of the prime numbers in the list, the function appends text to the end of &lt;code&gt;description&lt;/code&gt;, to note that the number is prime. It then uses the &lt;code&gt;fallthrough&lt;/code&gt; keyword to “fall into” the &lt;code&gt;default&lt;/code&gt; case as well. The &lt;code&gt;default&lt;/code&gt; case adds some extra text to the end of the description, and the &lt;code&gt;switch&lt;/code&gt; statement is complete. &lt;/p&gt;
&lt;p&gt;If the value of &lt;code&gt;integerToDescribe&lt;/code&gt; is &lt;em&gt;not&lt;/em&gt; in the list of known prime numbers, it is not matched by the first &lt;code&gt;switch&lt;/code&gt; case at all. There are no other specific cases, and so &lt;code&gt;integerToDescribe&lt;/code&gt; is matched by the catchall &lt;code&gt;default&lt;/code&gt; case. &lt;/p&gt;
&lt;p&gt;After the &lt;code&gt;switch&lt;/code&gt; statement has finished executing, the number’s description is printed using the &lt;code&gt;println&lt;/code&gt; function. In this example, the number &lt;code&gt;5&lt;/code&gt; is correctly identified as a prime number. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;fallthrough&lt;/code&gt; keyword does not check the case conditions for the &lt;code&gt;switch&lt;/code&gt; case that it causes execution to fall into. The &lt;code&gt;fallthrough&lt;/code&gt; keyword simply causes code execution to move directly to the statements inside the next case (or &lt;code&gt;default&lt;/code&gt; case) block, as in C’s standard &lt;code&gt;switch&lt;/code&gt; statement behavior. &lt;/p&gt;
&lt;h4&gt;Labeled Statements&lt;/h4&gt;
&lt;p&gt;You can nest loops and &lt;code&gt;switch&lt;/code&gt; statements inside other loops and &lt;code&gt;switch&lt;/code&gt; statements in Swift to create complex control flow structures. However, loops and &lt;code&gt;switch&lt;/code&gt; statements can both use the &lt;code&gt;break&lt;/code&gt; statement to end their execution prematurely. Therefore, it is sometimes useful to be explicit about which loop or &lt;code&gt;switch&lt;/code&gt; statement you want a &lt;code&gt;break&lt;/code&gt; statement to terminate. Similarly, if you have multiple nested loops, it can be useful to be explicit about which loop the &lt;code&gt;continue&lt;/code&gt; statement should affect. &lt;/p&gt;
&lt;p&gt;To achieve these aims, you can mark a loop statement or &lt;code&gt;switch&lt;/code&gt; statement with a &lt;em&gt;statement label&lt;/em&gt;, and use this label with the &lt;code&gt;break&lt;/code&gt; statement or &lt;code&gt;continue&lt;/code&gt; statement to end or continue the execution of the labeled statement. &lt;/p&gt;
&lt;p&gt;A labeled statement is indicated by placing a label on the same line as the statement’s introducer keyword, followed by a colon. Here’s an example of this syntax for a &lt;code&gt;while&lt;/code&gt; loop, although the principle is the same for all loops and &lt;code&gt;switch&lt;/code&gt; statements: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;label name&lt;/em&gt;: while &lt;em&gt;condition&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The following example uses the &lt;code&gt;break&lt;/code&gt; and &lt;code&gt;continue&lt;/code&gt; statements with a labeled &lt;code&gt;while&lt;/code&gt; loop for an adapted version of the &lt;em&gt;Snakes and Ladders&lt;/em&gt; game that you saw earlier in this chapter. This time around, the game has an extra rule: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;To win, you must land &lt;em&gt;exactly&lt;/em&gt; on square 25. &lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If a particular dice roll would take you beyond square 25, you must roll again until you roll the exact number needed to land on square 25. &lt;/p&gt;
&lt;p&gt;The game board is the same as before: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/snakesAndLadders_2x.png" src="http://muxuezi.github.io/posts/Art/snakesAndLadders_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The values of &lt;code&gt;finalSquare&lt;/code&gt;, &lt;code&gt;board&lt;/code&gt;, &lt;code&gt;square&lt;/code&gt;, and &lt;code&gt;diceRoll&lt;/code&gt; are initialized in the same way as before: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let finalSquare = 25&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var board = [Int](count: finalSquare \+ 1, repeatedValue: 0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board[03] = +08; board[06] = +11; board[09] = +09; board[10] = +02&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board[14] = -10; board[19] = -11; board[22] = -02; board[24] = -08&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var square = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var diceRoll = 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This version of the game uses a &lt;code&gt;while&lt;/code&gt; loop and a &lt;code&gt;switch&lt;/code&gt; statement to implement the game’s logic. The &lt;code&gt;while&lt;/code&gt; loop has a statement label called &lt;code&gt;gameLoop&lt;/code&gt;, to indicate that it is the main game loop for the Snakes and Ladders game. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;while&lt;/code&gt; loop’s condition is &lt;code&gt;while square != finalSquare&lt;/code&gt;, to reflect that you must land exactly on square 25: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;gameLoop: while square != finalSquare {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if ++diceRoll == 7 { diceRoll = 1 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch square \+ diceRoll {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case finalSquare:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// diceRoll will move us to the final square, so the game is over&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;break gameLoop&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let newSquare where newSquare &amp;gt; finalSquare:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// diceRoll will move us beyond the final square, so roll again&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;continue gameLoop&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this is a valid move, so find out its effect&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += diceRoll&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += board[square]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Game over!")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The dice is rolled at the start of each loop. Rather than moving the player immediately, a &lt;code&gt;switch&lt;/code&gt; statement is used to consider the result of the move, and to work out if the move is allowed: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If the dice roll will move the player onto the final square, the game is over. The &lt;code&gt;break gameLoop&lt;/code&gt; statement transfers control to the first line of code outside of the &lt;code&gt;while&lt;/code&gt; loop, which ends the game. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If the dice roll will move the player &lt;em&gt;beyond&lt;/em&gt; the final square, the move is invalid, and the player needs to roll again. The &lt;code&gt;continue gameLoop&lt;/code&gt; statement ends the current &lt;code&gt;while&lt;/code&gt; loop iteration and begins the next iteration of the loop. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;In all other cases, the dice roll is a valid move. The player moves forward by &lt;code&gt;diceRoll&lt;/code&gt; squares, and the game logic checks for any snakes and ladders. The loop then ends, and control returns to the &lt;code&gt;while&lt;/code&gt; condition to decide whether another turn is required. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If the &lt;code&gt;break&lt;/code&gt; statement above did not use the &lt;code&gt;gameLoop&lt;/code&gt; label, it would break out of the &lt;code&gt;switch&lt;/code&gt; statement, not the &lt;code&gt;while&lt;/code&gt; statement. Using the &lt;code&gt;gameLoop&lt;/code&gt; label makes it clear which control statement should be terminated. &lt;/p&gt;
&lt;p&gt;Note also that it is not strictly necessary to use the &lt;code&gt;gameLoop&lt;/code&gt; label when calling &lt;code&gt;continue gameLoop&lt;/code&gt; to jump to the next iteration of the loop. There is only one loop in the game, and so there is no ambiguity as to which loop the &lt;code&gt;continue&lt;/code&gt; statement will affect. However, there is no harm in using the &lt;code&gt;gameLoop&lt;/code&gt; label with the &lt;code&gt;continue&lt;/code&gt; statement. Doing so is consistent with the label’s use alongside the &lt;code&gt;break&lt;/code&gt; statement, and helps make the game’s logic clearer to read and understand. &lt;/p&gt;
&lt;h2&gt;Functions&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Functions&lt;/em&gt; are self-contained chunks of code that perform a specific task. You give a function a name that identifies what it does, and this name is used to “call” the function to perform its task when needed. &lt;/p&gt;
&lt;p&gt;Swift’s unified function syntax is flexible enough to express anything from a simple C-style function with no parameter names to a complex Objective-C-style method with local and external parameter names for each parameter. Parameters can provide default values to simplify function calls and can be passed as in-out parameters, which modify a passed variable once the function has completed its execution. &lt;/p&gt;
&lt;p&gt;Every function in Swift has a type, consisting of the function’s parameter types and return type. You can use this type like any other type in Swift, which makes it easy to pass functions as parameters to other functions, and to return functions from functions. Functions can also be written within other functions to encapsulate useful functionality within a nested function scope. &lt;/p&gt;
&lt;h3&gt;Defining and Calling Functions&lt;/h3&gt;
&lt;p&gt;When you define a function, you can optionally define one or more named, typed values that the function takes as input (known as &lt;em&gt;parameters&lt;/em&gt;), and/or a type of value that the function will pass back as output when it is done (known as its &lt;em&gt;return type&lt;/em&gt;). &lt;/p&gt;
&lt;p&gt;Every function has a &lt;em&gt;function name&lt;/em&gt;, which describes the task that the function performs. To use a function, you “call” that function with its name and pass it input values (known as &lt;em&gt;arguments&lt;/em&gt;) that match the types of the function’s parameters. A function’s arguments must always be provided in the same order as the function’s parameter list. &lt;/p&gt;
&lt;p&gt;The function in the example below is called &lt;code&gt;sayHello&lt;/code&gt;, because that’s what it does—it takes a person’s name as input and returns a greeting for that person. To accomplish this, you define one input parameter—a &lt;code&gt;String&lt;/code&gt; value called &lt;code&gt;personName&lt;/code&gt;—and a return type of &lt;code&gt;String&lt;/code&gt;, which will contain a greeting for that person: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func sayHello(personName: String) -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let greeting = "Hello, " \+ personName \+ "!"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return greeting&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;All of this information is rolled up into the function’s &lt;em&gt;definition&lt;/em&gt;, which is prefixed with the &lt;code&gt;func&lt;/code&gt; keyword. You indicate the function’s return type with the &lt;em&gt;return arrow&lt;/em&gt; &lt;code&gt;-&amp;gt;&lt;/code&gt; (a hyphen followed by a right angle bracket), which is followed by the name of the type to return. &lt;/p&gt;
&lt;p&gt;The definition describes what the function does, what it expects to receive, and what it returns when it is done. The definition makes it easy for the function to be called unambiguously from elsewhere in your code: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println(sayHello("Anna"))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Hello, Anna!"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(sayHello("Brian"))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Hello, Brian!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You call the &lt;code&gt;sayHello&lt;/code&gt; function by passing it a &lt;code&gt;String&lt;/code&gt; argument value in parentheses, such as &lt;code&gt;sayHello("Anna")&lt;/code&gt;. Because the function returns a &lt;code&gt;String&lt;/code&gt; value, &lt;code&gt;sayHello&lt;/code&gt; can be wrapped in a call to the &lt;code&gt;println&lt;/code&gt; function to print that string and see its return value, as shown above. &lt;/p&gt;
&lt;p&gt;The body of the &lt;code&gt;sayHello&lt;/code&gt; function starts by defining a new &lt;code&gt;String&lt;/code&gt; constant called &lt;code&gt;greeting&lt;/code&gt; and setting it to a simple greeting message for &lt;code&gt;personName&lt;/code&gt;. This greeting is then passed back out of the function using the &lt;code&gt;return&lt;/code&gt; keyword. As soon as &lt;code&gt;return greeting&lt;/code&gt; is called, the function finishes its execution and returns the current value of &lt;code&gt;greeting&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can call the &lt;code&gt;sayHello&lt;/code&gt; function multiple times with different input values. The example above shows what happens if it is called with an input value of &lt;code&gt;"Anna"&lt;/code&gt;, and an input value of &lt;code&gt;"Brian"&lt;/code&gt;. The function returns a tailored greeting in each case. &lt;/p&gt;
&lt;p&gt;To simplify the body of this function, combine the message creation and the return statement into one line: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func sayHelloAgain(personName: String) -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "Hello again, " \+ personName \+ "!"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(sayHelloAgain("Anna"))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Hello again, Anna!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Function Parameters and Return Values&lt;/h3&gt;
&lt;p&gt;Function parameters and return values are extremely flexible in Swift. You can define anything from a simple utility function with a single unnamed parameter to a complex function with expressive parameter names and different parameter options. &lt;/p&gt;
&lt;h4&gt;Multiple Input Parameters&lt;/h4&gt;
&lt;p&gt;Functions can have multiple input parameters, which are written within the function’s parentheses, separated by commas. &lt;/p&gt;
&lt;p&gt;This function takes a start and an end index for a half-open range, and works out how many elements the range contains: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func halfOpenRangeLength(start: Int, end: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return end \- start&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(halfOpenRangeLength(1, 10))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "9"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Functions Without Parameters&lt;/h4&gt;
&lt;p&gt;Functions are not required to define input parameters. Here’s a function with no input parameters, which always returns the same &lt;code&gt;String&lt;/code&gt; message whenever it is called: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func sayHelloWorld() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "hello, world"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(sayHelloWorld())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "hello, world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The function definition still needs parentheses after the function’s name, even though it does not take any parameters. The function name is also followed by an empty pair of parentheses when the function is called. &lt;/p&gt;
&lt;h4&gt;Functions Without Return Values&lt;/h4&gt;
&lt;p&gt;Functions are not required to define a return type. Here’s a version of the &lt;code&gt;sayHello&lt;/code&gt; function, called &lt;code&gt;sayGoodbye&lt;/code&gt;, which prints its own &lt;code&gt;String&lt;/code&gt; value rather than returning it: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func sayGoodbye(personName: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Goodbye, \\(personName)!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sayGoodbye("Dave")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Goodbye, Dave!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because it does not need to return a value, the function’s definition does not include the return arrow (&lt;code&gt;-&amp;gt;&lt;/code&gt;) or a return type. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Strictly speaking, the &lt;code&gt;sayGoodbye&lt;/code&gt; function &lt;em&gt;does&lt;/em&gt; still return a value, even though no return value is defined. Functions without a defined return type return a special value of type &lt;code&gt;Void&lt;/code&gt;. This is simply an empty tuple, in effect a tuple with zero elements, which can be written as &lt;code&gt;()&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The return value of a function can be ignored when it is called: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func printAndCount(stringToPrint: String) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(stringToPrint)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return countElements(stringToPrint)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func printWithoutCounting(stringToPrint: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;printAndCount(stringToPrint)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;printAndCount("hello, world")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "hello, world" and returns a value of 12&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;printWithoutCounting("hello, world")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "hello, world" but does not return a value&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The first function, &lt;code&gt;printAndCount&lt;/code&gt;, prints a string, and then returns its character count as an &lt;code&gt;Int&lt;/code&gt;. The second function, &lt;code&gt;printWithoutCounting&lt;/code&gt;, calls the first function, but ignores its return value. When the second function is called, the message is still printed by the first function, but the returned value is not used. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Return values can be ignored, but a function that says it will return a value must always do so. A function with a defined return type cannot allow control to fall out of the bottom of the function without returning a value, and attempting to do so will result in a compile-time error. &lt;/p&gt;
&lt;h4&gt;Functions with Multiple Return Values&lt;/h4&gt;
&lt;p&gt;You can use a tuple type as the return type for a function to return multiple values as part of one compound return value. &lt;/p&gt;
&lt;p&gt;The example below defines a function called &lt;code&gt;minMax&lt;/code&gt;, which finds the smallest and largest numbers in an array of &lt;code&gt;Int&lt;/code&gt; values: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func minMax(array: [Int]) -&amp;gt; (min: Int, max: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentMin = array[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentMax = array[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for value in array[1..&amp;lt;array.count] {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if value &amp;lt; currentMin {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentMin = value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if value &amp;gt; currentMax {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentMax = value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return (currentMin, currentMax)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;minMax&lt;/code&gt; function returns a tuple containing two &lt;code&gt;Int&lt;/code&gt; values. These values are labeled &lt;code&gt;min&lt;/code&gt; and &lt;code&gt;max&lt;/code&gt; so that they can be accessed by name when querying the function’s return value. &lt;/p&gt;
&lt;p&gt;The body of the &lt;code&gt;minMax&lt;/code&gt; function starts by setting two working variables called &lt;code&gt;currentMin&lt;/code&gt; and &lt;code&gt;currentMax&lt;/code&gt; to the value of the first integer in the array. The function then iterates over the remaining values in the array and checks each value to see if it is smaller or larger than the values of &lt;code&gt;currentMin&lt;/code&gt; and &lt;code&gt;currentMax&lt;/code&gt; respectively. Finally, the overall minimum and maximum values are returned as a tuple of two &lt;code&gt;Int&lt;/code&gt; values. &lt;/p&gt;
&lt;p&gt;Because the tuple’s member values are named as part of the function’s return type, they can be accessed with dot syntax to retrieve the minimum and maximum found values: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let bounds = minMax([8, -6, 2, 109, 3, 71])&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("min is \\(bounds.min) and max is \\(bounds.max)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "min is -6 and max is 109"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the tuple’s members do not need to be named at the point that the tuple is returned from the function, because their names are already specified as part of the function’s return type. &lt;/p&gt;
&lt;h4&gt;Optional Tuple Return Types&lt;/h4&gt;
&lt;p&gt;If the tuple type to be returned from a function has the potential to have “no value” for the entire tuple, you can use an &lt;em&gt;optional&lt;/em&gt; tuple return type to reflect the fact that the entire tuple can be &lt;code&gt;nil&lt;/code&gt;. You write an optional tuple return type by placing a question mark after the tuple type’s closing parenthesis, such as &lt;code&gt;(Int, Int)?&lt;/code&gt; or &lt;code&gt;(String, Int, Bool)?&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;An optional tuple type such as &lt;code&gt;(Int, Int)?&lt;/code&gt; is different from a tuple that contains optional types such as &lt;code&gt;(Int?, Int?)&lt;/code&gt;. With an optional tuple type, the entire tuple is optional, not just each individual value within the tuple. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;minMax&lt;/code&gt; function above returns a tuple containing two &lt;code&gt;Int&lt;/code&gt; values. However, the function does not perform any safety checks on the array it is passed. If the &lt;code&gt;array&lt;/code&gt; argument contains an empty array—that is, an array with a &lt;code&gt;count&lt;/code&gt; of &lt;code&gt;0&lt;/code&gt;—the &lt;code&gt;minMax&lt;/code&gt; function, as defined above, will trigger a runtime error when attempting to access &lt;code&gt;array[0]&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;To handle this “empty array” scenario safely, write the &lt;code&gt;minMax&lt;/code&gt; function with an optional tuple return type and return a value of &lt;code&gt;nil&lt;/code&gt; when the array is empty: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func minMax(array: [Int]) -&amp;gt; (min: Int, max: Int)? {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if array.isEmpty { return nil }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentMin = array[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentMax = array[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for value in array[1..&amp;lt;array.count] {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if value &amp;lt; currentMin {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentMin = value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if value &amp;gt; currentMax {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentMax = value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return (currentMin, currentMax)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can use optional binding to check whether this version of the &lt;code&gt;minMax&lt;/code&gt; function returns an actual tuple value or &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let bounds = minMax([8, -6, 2, 109, 3, 71]) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("min is \\(bounds.min) and max is \\(bounds.max)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "min is -6 and max is 109"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Function Parameter Names&lt;/h3&gt;
&lt;p&gt;All of the above functions define &lt;em&gt;parameter names&lt;/em&gt; for their parameters: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func someFunction(parameterName: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// function body goes here, and can use parameterName&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// to refer to the argument value for that parameter&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;However, these parameter names are only used within the body of the function itself, and cannot be used when calling the function. These kinds of parameter names are known as &lt;em&gt;local parameter names&lt;/em&gt;, because they are only available for use within the function’s body. &lt;/p&gt;
&lt;h4&gt;External Parameter Names&lt;/h4&gt;
&lt;p&gt;Sometimes it’s useful to name each parameter when you &lt;em&gt;call&lt;/em&gt; a function, to indicate the purpose of each argument you pass to the function. &lt;/p&gt;
&lt;p&gt;If you want users of your function to provide parameter names when they call your function, define an &lt;em&gt;external parameter name&lt;/em&gt; for each parameter, in addition to the local parameter name. You write an external parameter name before the local parameter name it supports, separated by a space: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func someFunction(externalParameterName localParameterName: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// function body goes here, and can use localParameterName&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// to refer to the argument value for that parameter&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you provide an external parameter name for a parameter, that external name must &lt;em&gt;always&lt;/em&gt; be used when you call the function. &lt;/p&gt;
&lt;p&gt;As an example, consider the following function, which joins two strings by inserting a third “joiner” string between them: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func join(s1: String, s2: String, joiner: String) -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return s1 \+ joiner \+ s2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When you call this function, the purpose of the three strings that you pass to the function is unclear: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;join("hello", "world", ", ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns "hello, world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To make the purpose of these &lt;code&gt;String&lt;/code&gt; values clearer, define external parameter names for each &lt;code&gt;join&lt;/code&gt; function parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func join(string s1: String, toString s2: String, withJoiner joiner: String)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;-&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return s1 \+ joiner \+ s2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this version of the &lt;code&gt;join&lt;/code&gt; function, the first parameter has an external name of &lt;code&gt;string&lt;/code&gt; and a local name of &lt;code&gt;s1&lt;/code&gt;; the second parameter has an external name of &lt;code&gt;toString&lt;/code&gt; and a local name of &lt;code&gt;s2&lt;/code&gt;; and the third parameter has an external name of &lt;code&gt;withJoiner&lt;/code&gt; and a local name of &lt;code&gt;joiner&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can now use these external parameter names to call the function unambiguously: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;join(string: "hello", toString: "world", withJoiner: ", ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns "hello, world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The use of external parameter names enables this second version of the &lt;code&gt;join&lt;/code&gt; function to be called in an expressive, sentence-like manner by users of the function, while still providing a function body that is readable and clear in intent. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Consider using external parameter names whenever the purpose of a function’s arguments would be unclear to someone reading your code for the first time. You do not need to specify external parameter names if the purpose of each parameter is unambiguous when the function is called. &lt;/p&gt;
&lt;h4&gt;Shorthand External Parameter Names&lt;/h4&gt;
&lt;p&gt;If you want to provide an external parameter name for a function parameter, and the local parameter name is already an appropriate name to use, you do not need to write the same name twice for that parameter. Instead, write the name once, and prefix the name with a hash symbol (&lt;code&gt;#&lt;/code&gt;). This tells Swift to use that name as both the local parameter name and the external parameter name. &lt;/p&gt;
&lt;p&gt;This example defines a function called &lt;code&gt;containsCharacter&lt;/code&gt;, which defines external parameter names for both of its parameters by placing a hash symbol before their local parameter names: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func containsCharacter(#string: Character) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for character in string {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if character == characterToFind {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This function’s choice of parameter names makes for a clear, readable function body, while also enabling the function to be called without ambiguity: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let containsAVee = containsCharacter(string: "aardvark", characterToFind: "v")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// containsAVee equals true, because "aardvark" contains a "v"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Default Parameter Values&lt;/h4&gt;
&lt;p&gt;You can define a &lt;em&gt;default value&lt;/em&gt; for any parameter as part of a function’s definition. If a default value is defined, you can omit that parameter when calling the function. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Place parameters with default values at the end of a function’s parameter list. This ensures that all calls to the function use the same order for their non-default arguments, and makes it clear that the same function is being called in each case. &lt;/p&gt;
&lt;p&gt;Here’s a version of the &lt;code&gt;join&lt;/code&gt; function from earlier, which provides a default value for its &lt;code&gt;joiner&lt;/code&gt; parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func join(string s1: String, toString s2: String,&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;withJoiner joiner: String = " ") -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return s1 \+ joiner \+ s2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If a string value for &lt;code&gt;joiner&lt;/code&gt; is provided when the &lt;code&gt;join&lt;/code&gt; function is called, that string value is used to join the two strings together, as before: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;join(string: "hello", toString: "world", withJoiner: "-")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns "hello-world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;However, if no value of &lt;code&gt;joiner&lt;/code&gt; is provided when the function is called, the default value of a single space (&lt;code&gt;" "&lt;/code&gt;) is used instead: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;join(string: "hello", toString: "world")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns "hello world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;External Names for Parameters with Default Values&lt;/h4&gt;
&lt;p&gt;In most cases, it is useful to provide (and therefore require) an external name for any parameter with a default value. This ensures that the argument for that parameter is clear in purpose if a value is provided when the function is called. &lt;/p&gt;
&lt;p&gt;To make this process easier, Swift provides an automatic external name for any parameter that has a default value. The automatic external name is the same as the local name, as if you had written a hash symbol before the local name in your code. &lt;/p&gt;
&lt;p&gt;Here’s a version of the &lt;code&gt;join&lt;/code&gt; function from earlier, which does not provide external names for any of its parameters, but still provides a default value for its &lt;code&gt;joiner&lt;/code&gt; parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func join(s1: String, s2: String, joiner: String = " ") -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return s1 \+ joiner \+ s2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this case, Swift automatically provides an external parameter name for the &lt;code&gt;joiner&lt;/code&gt; parameter. The external name must therefore be provided when calling the function, making the parameter’s purpose clear and unambiguous: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;join("hello", "world", joiner: "-")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns "hello-world"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You can opt out of this behavior by writing an underscore (&lt;code&gt;_&lt;/code&gt;) instead of an explicit external name when you define the parameter. However, external names for parameters with default values are preferred. &lt;/p&gt;
&lt;h4&gt;Variadic Parameters&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;variadic parameter&lt;/em&gt; accepts zero or more values of a specified type. You use a variadic parameter to specify that the parameter can be passed a varying number of input values when the function is called. Write variadic parameters by inserting three period characters (&lt;code&gt;...&lt;/code&gt;) after the parameter’s type name. &lt;/p&gt;
&lt;p&gt;The values passed to a variadic parameter are made available within the function’s body as an array of the appropriate type. For example, a variadic parameter with a name of &lt;code&gt;numbers&lt;/code&gt; and a type of &lt;code&gt;Double...&lt;/code&gt; is made available within the function’s body as a constant array called &lt;code&gt;numbers&lt;/code&gt; of type &lt;code&gt;[Double]&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The example below calculates the &lt;em&gt;arithmetic mean&lt;/em&gt; (also known as the &lt;em&gt;average&lt;/em&gt;) for a list of numbers of any length: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func arithmeticMean(numbers: Double...) -&amp;gt; Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var total: Double = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for number in numbers {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;total += number&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return total / Double(numbers.count)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;arithmeticMean(1, 2, 3, 4, 5)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns 3.0, which is the arithmetic mean of these five numbers&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;arithmeticMean(3, 8.25, 18.75)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns 10.0, which is the arithmetic mean of these three numbers&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;A function may have at most one variadic parameter, and it must always appear last in the parameter list, to avoid ambiguity when calling the function with multiple parameters. &lt;/p&gt;
&lt;p&gt;If your function has one or more parameters with a default value, and also has a variadic parameter, place the variadic parameter after all the defaulted parameters at the very end of the list. &lt;/p&gt;
&lt;h4&gt;Constant and Variable Parameters&lt;/h4&gt;
&lt;p&gt;Function parameters are constants by default. Trying to change the value of a function parameter from within the body of that function results in a compile-time error. This means that you can’t change the value of a parameter by mistake. &lt;/p&gt;
&lt;p&gt;However, sometimes it is useful for a function to have a &lt;em&gt;variable&lt;/em&gt; copy of a parameter’s value to work with. You can avoid defining a new variable yourself within the function by specifying one or more parameters as &lt;em&gt;variable parameters&lt;/em&gt; instead. Variable parameters are available as variables rather than as constants, and give a new modifiable copy of the parameter’s value for your function to work with. &lt;/p&gt;
&lt;p&gt;Define variable parameters by prefixing the parameter name with the keyword &lt;code&gt;var&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func alignRight(var string: String, count: Int, pad: Character) -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let amountToPad = count \- countElements(string)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if amountToPad &amp;lt; 1 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return string&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let padString = String(pad)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for _ in 1...amountToPad {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;string = padString \+ string&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return string&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let originalString = "hello"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let paddedString = alignRight(originalString, 10, "-")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// paddedString is equal to "-----hello"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// originalString is still equal to "hello"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a new function called &lt;code&gt;alignRight&lt;/code&gt;, which aligns an input string to the right edge of a longer output string. Any space on the left is filled with a specified padding character. In this example, the string &lt;code&gt;"hello"&lt;/code&gt; is converted to the string &lt;code&gt;"-----hello"&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;alignRight&lt;/code&gt; function defines the input parameter &lt;code&gt;string&lt;/code&gt; to be a variable parameter. This means that &lt;code&gt;string&lt;/code&gt; is now available as a local variable, initialized with the passed-in string value, and can be manipulated within the body of the function. &lt;/p&gt;
&lt;p&gt;The function starts by working out how many characters need to be added to the left of &lt;code&gt;string&lt;/code&gt; in order to right-align it within the overall string. This value is stored in a local constant called &lt;code&gt;amountToPad&lt;/code&gt;. If no padding is needed (that is, if &lt;code&gt;amountToPad&lt;/code&gt; is less than &lt;code&gt;1&lt;/code&gt;), the function simply returns the input value of &lt;code&gt;string&lt;/code&gt; without any padding. &lt;/p&gt;
&lt;p&gt;Otherwise, the function creates a new temporary &lt;code&gt;String&lt;/code&gt; constant called &lt;code&gt;padString&lt;/code&gt;, initialized with the &lt;code&gt;pad&lt;/code&gt; character, and adds &lt;code&gt;amountToPad&lt;/code&gt; copies of &lt;code&gt;padString&lt;/code&gt; to the left of the existing string. (A &lt;code&gt;String&lt;/code&gt; value cannot be added on to a &lt;code&gt;Character&lt;/code&gt; value, and so the &lt;code&gt;padString&lt;/code&gt; constant is used to ensure that both sides of the &lt;code&gt;+&lt;/code&gt; operator are &lt;code&gt;String&lt;/code&gt; values.) &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The changes you make to a variable parameter do not persist beyond the end of each call to the function, and are not visible outside the function’s body. The variable parameter only exists for the lifetime of that function call. &lt;/p&gt;
&lt;h4&gt;In-Out Parameters&lt;/h4&gt;
&lt;p&gt;Variable parameters, as described above, can only be changed within the function itself. If you want a function to modify a parameter’s value, and you want those changes to persist after the function call has ended, define that parameter as an &lt;em&gt;in-out parameter&lt;/em&gt; instead. &lt;/p&gt;
&lt;p&gt;You write an in-out parameter by placing the &lt;code&gt;inout&lt;/code&gt; keyword at the start of its parameter definition. An in-out parameter has a value that is passed &lt;em&gt;in&lt;/em&gt; to the function, is modified by the function, and is passed back &lt;em&gt;out&lt;/em&gt; of the function to replace the original value. &lt;/p&gt;
&lt;p&gt;You can only pass a variable as the argument for an in-out parameter. You cannot pass a constant or a literal value as the argument, because constants and literals cannot be modified. You place an ampersand (&lt;code&gt;&amp;amp;&lt;/code&gt;) directly before a variable’s name when you pass it as an argument to an inout parameter, to indicate that it can be modified by the function. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In-out parameters cannot have default values, and variadic parameters cannot be marked as &lt;code&gt;inout&lt;/code&gt;. If you mark a parameter as &lt;code&gt;inout&lt;/code&gt;, it cannot also be marked as &lt;code&gt;var&lt;/code&gt; or &lt;code&gt;let&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Here’s an example of a function called &lt;code&gt;swapTwoInts&lt;/code&gt;, which has two in-out integer parameters called &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func swapTwoInts(inout a: Int, inout b: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let temporaryA = a&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a = b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;b = temporaryA&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;swapTwoInts&lt;/code&gt; function simply swaps the value of &lt;code&gt;b&lt;/code&gt; into &lt;code&gt;a&lt;/code&gt;, and the value of &lt;code&gt;a&lt;/code&gt; into &lt;code&gt;b&lt;/code&gt;. The function performs this swap by storing the value of &lt;code&gt;a&lt;/code&gt; in a temporary constant called &lt;code&gt;temporaryA&lt;/code&gt;, assigning the value of &lt;code&gt;b&lt;/code&gt; to &lt;code&gt;a&lt;/code&gt;, and then assigning &lt;code&gt;temporaryA&lt;/code&gt; to &lt;code&gt;b&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can call the &lt;code&gt;swapTwoInts&lt;/code&gt; function with two variables of type &lt;code&gt;Int&lt;/code&gt; to swap their values. Note that the names of &lt;code&gt;someInt&lt;/code&gt; and &lt;code&gt;anotherInt&lt;/code&gt; are prefixed with an ampersand when they are passed to the &lt;code&gt;swapTwoInts&lt;/code&gt; function: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var someInt = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var anotherInt = 107&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;swapTwoInts(&amp;amp;someInt, &amp;amp;anotherInt)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("someInt is now \\(someInt), and anotherInt is now \\(anotherInt)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "someInt is now 107, and anotherInt is now 3"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example above shows that the original values of &lt;code&gt;someInt&lt;/code&gt; and &lt;code&gt;anotherInt&lt;/code&gt; are modified by the &lt;code&gt;swapTwoInts&lt;/code&gt; function, even though they were originally defined outside of the function. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In-out parameters are not the same as returning a value from a function. The &lt;code&gt;swapTwoInts&lt;/code&gt; example above does not define a return type or return a value, but it still modifies the values of &lt;code&gt;someInt&lt;/code&gt; and &lt;code&gt;anotherInt&lt;/code&gt;. In-out parameters are an alternative way for a function to have an effect outside of the scope of its function body. &lt;/p&gt;
&lt;h3&gt;Function Types&lt;/h3&gt;
&lt;p&gt;Every function has a specific &lt;em&gt;function type&lt;/em&gt;, made up of the parameter types and the return type of the function. &lt;/p&gt;
&lt;p&gt;For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func addTwoInts(a: Int, b: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return a \+ b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func multiplyTwoInts(a: Int, b: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return a * b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines two simple mathematical functions called &lt;code&gt;addTwoInts&lt;/code&gt; and &lt;code&gt;multiplyTwoInts&lt;/code&gt;. These functions each take two &lt;code&gt;Int&lt;/code&gt; values, and return an &lt;code&gt;Int&lt;/code&gt; value, which is the result of performing an appropriate mathematical operation. &lt;/p&gt;
&lt;p&gt;The type of both of these functions is &lt;code&gt;(Int, Int) -&amp;gt; Int&lt;/code&gt;. This can be read as: &lt;/p&gt;
&lt;p&gt;“A function type that has two parameters, both of type &lt;code&gt;Int&lt;/code&gt;, and that returns a value of type &lt;code&gt;Int&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;Here’s another example, for a function with no parameters or return value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func printHelloWorld() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("hello, world")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The type of this function is &lt;code&gt;() -&amp;gt; ()&lt;/code&gt;, or “a function that has no parameters, and returns &lt;code&gt;Void&lt;/code&gt;.” Functions that don’t specify a return value always return &lt;code&gt;Void&lt;/code&gt;, which is equivalent to an empty tuple in Swift, shown as &lt;code&gt;()&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Using Function Types&lt;/h4&gt;
&lt;p&gt;You use function types just like any other types in Swift. For example, you can define a constant or variable to be of a function type and assign an appropriate function to that variable: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var mathFunction: (Int, Int) -&amp;gt; Int = addTwoInts&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This can be read as: &lt;/p&gt;
&lt;p&gt;“Define a variable called &lt;code&gt;mathFunction&lt;/code&gt;, which has a type of ‘a function that takes two &lt;code&gt;Int&lt;/code&gt; values, and returns an &lt;code&gt;Int&lt;/code&gt; value.’ Set this new variable to refer to the function called &lt;code&gt;addTwoInts&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;addTwoInts&lt;/code&gt; function has the same type as the &lt;code&gt;mathFunction&lt;/code&gt; variable, and so this assignment is allowed by Swift’s type-checker. &lt;/p&gt;
&lt;p&gt;You can now call the assigned function with the name &lt;code&gt;mathFunction&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("Result: \\(mathFunction(2, 3))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Result: 5"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A different function with the same matching type can be assigned to the same variable, in the same way as for non-function types: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;mathFunction = multiplyTwoInts&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Result: \\(mathFunction(2, 3))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Result: 6"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;As with any other type, you can leave it to Swift to infer the function type when you assign a function to a constant or variable: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let anotherMathFunction = addTwoInts&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// anotherMathFunction is inferred to be of type (Int, Int) -&amp;gt; Int&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Function Types as Parameter Types&lt;/h4&gt;
&lt;p&gt;You can use a function type such as &lt;code&gt;(Int, Int) -&amp;gt; Int&lt;/code&gt; as a parameter type for another function. This enables you to leave some aspects of a function’s implementation for the function’s caller to provide when the function is called. &lt;/p&gt;
&lt;p&gt;Here’s an example to print the results of the math functions from above: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func printMathResult(mathFunction: (Int, Int) -&amp;gt; Int, a: Int, b: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Result: \\(mathFunction(a, b))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;printMathResult(addTwoInts, 3, 5)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Result: 8"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a function called &lt;code&gt;printMathResult&lt;/code&gt;, which has three parameters. The first parameter is called &lt;code&gt;mathFunction&lt;/code&gt;, and is of type &lt;code&gt;(Int, Int) -&amp;gt; Int&lt;/code&gt;. You can pass any function of that type as the argument for this first parameter. The second and third parameters are called &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt;, and are both of type &lt;code&gt;Int&lt;/code&gt;. These are used as the two input values for the provided math function. &lt;/p&gt;
&lt;p&gt;When &lt;code&gt;printMathResult&lt;/code&gt; is called, it is passed the &lt;code&gt;addTwoInts&lt;/code&gt; function, and the integer values &lt;code&gt;3&lt;/code&gt; and &lt;code&gt;5&lt;/code&gt;. It calls the provided function with the values &lt;code&gt;3&lt;/code&gt; and &lt;code&gt;5&lt;/code&gt;, and prints the result of &lt;code&gt;8&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The role of &lt;code&gt;printMathResult&lt;/code&gt; is to print the result of a call to a math function of an appropriate type. It doesn’t matter what that function’s implementation actually does—it matters only that the function is of the correct type. This enables &lt;code&gt;printMathResult&lt;/code&gt; to hand off some of its functionality to the caller of the function in a type-safe way. &lt;/p&gt;
&lt;h4&gt;Function Types as Return Types&lt;/h4&gt;
&lt;p&gt;You can use a function type as the return type of another function. You do this by writing a complete function type immediately after the return arrow (&lt;code&gt;-&amp;gt;&lt;/code&gt;) of the returning function. &lt;/p&gt;
&lt;p&gt;The next example defines two simple functions called &lt;code&gt;stepForward&lt;/code&gt; and &lt;code&gt;stepBackward&lt;/code&gt;. The &lt;code&gt;stepForward&lt;/code&gt; function returns a value one more than its input value, and the &lt;code&gt;stepBackward&lt;/code&gt; function returns a value one less than its input value. Both functions have a type of &lt;code&gt;(Int) -&amp;gt; Int&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func stepForward(input: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return input \+ 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func stepBackward(input: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return input \- 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s a function called &lt;code&gt;chooseStepFunction&lt;/code&gt;, whose return type is “a function of type &lt;code&gt;(Int) -&amp;gt; Int&lt;/code&gt;”. &lt;code&gt;chooseStepFunction&lt;/code&gt; returns the &lt;code&gt;stepForward&lt;/code&gt; function or the &lt;code&gt;stepBackward&lt;/code&gt; function based on a Boolean parameter called &lt;code&gt;backwards&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func chooseStepFunction(backwards: Bool) -&amp;gt; (Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return backwards ? stepBackward : stepForward&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can now use &lt;code&gt;chooseStepFunction&lt;/code&gt; to obtain a function that will step in one direction or the other: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var currentValue = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let moveNearerToZero = chooseStepFunction(currentValue &amp;gt; 0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// moveNearerToZero now refers to the stepBackward() function&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The preceding example determines whether a positive or negative step is needed to move a variable called &lt;code&gt;currentValue&lt;/code&gt; progressively closer to zero. &lt;code&gt;currentValue&lt;/code&gt; has an initial value of &lt;code&gt;3&lt;/code&gt;, which means that &lt;code&gt;currentValue &amp;gt; 0&lt;/code&gt; returns &lt;code&gt;true&lt;/code&gt;, causing &lt;code&gt;chooseStepFunction&lt;/code&gt; to return the &lt;code&gt;stepBackward&lt;/code&gt; function. A reference to the returned function is stored in a constant called &lt;code&gt;moveNearerToZero&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Now that &lt;code&gt;moveNearerToZero&lt;/code&gt; refers to the correct function, it can be used to count to zero: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("Counting to zero:")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Counting to zero:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;while currentValue != 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(currentValue)... ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentValue = moveNearerToZero(currentValue)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("zero!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 3...&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 2...&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 1...&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// zero!&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Nested Functions&lt;/h3&gt;
&lt;p&gt;All of the functions you have encountered so far in this chapter have been examples of &lt;em&gt;global functions&lt;/em&gt;, which are defined at a global scope. You can also define functions inside the bodies of other functions, known as &lt;em&gt;nested functions&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;Nested functions are hidden from the outside world by default, but can still be called and used by their enclosing function. An enclosing function can also return one of its nested functions to allow the nested function to be used in another scope. &lt;/p&gt;
&lt;p&gt;You can rewrite the &lt;code&gt;chooseStepFunction&lt;/code&gt; example above to use and return nested functions: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func chooseStepFunction(backwards: Bool) -&amp;gt; (Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func stepForward(input: Int) -&amp;gt; Int { return input \+ 1 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func stepBackward(input: Int) -&amp;gt; Int { return input \- 1 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return backwards ? stepBackward : stepForward&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentValue = -4&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let moveNearerToZero = chooseStepFunction(currentValue &amp;gt; 0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// moveNearerToZero now refers to the nested stepForward() function&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;while currentValue != 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(currentValue)... ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentValue = moveNearerToZero(currentValue)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("zero!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// -4...&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// -3...&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// -2...&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// -1...&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// zero!&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Closures&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Closures&lt;/em&gt; are self-contained blocks of functionality that can be passed around and used in your code. Closures in Swift are similar to blocks in C and Objective-C and to lambdas in other programming languages. &lt;/p&gt;
&lt;p&gt;Closures can capture and store references to any constants and variables from the context in which they are defined. This is known as &lt;em&gt;closing&lt;/em&gt; over those constants and variables, hence the name “closures”. Swift handles all of the memory management of capturing for you. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Don’t worry if you are not familiar with the concept of “capturing”. It is explained in detail below in Capturing Values. &lt;/p&gt;
&lt;p&gt;Global and nested functions, as introduced in Functions, are actually special cases of closures. Closures take one of three forms: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Global functions are closures that have a name and do not capture any values. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Nested functions are closures that have a name and can capture values from their enclosing function. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Closure expressions are unnamed closures written in a lightweight syntax that can capture values from their surrounding context. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Swift’s closure expressions have a clean, clear style, with optimizations that encourage brief, clutter-free syntax in common scenarios. These optimizations include: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Inferring parameter and return value types from context &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Implicit returns from single-expression closures &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Shorthand argument names &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Trailing closure syntax &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Closure Expressions&lt;/h3&gt;
&lt;p&gt;Nested functions, as introduced in Nested Functions, are a convenient means of naming and defining self-contained blocks of code as part of a larger function. However, it is sometimes useful to write shorter versions of function-like constructs without a full declaration and name. This is particularly true when you work with functions that take other functions as one or more of their arguments. &lt;/p&gt;
&lt;p&gt;&lt;em&gt;Closure expressions&lt;/em&gt; are a way to write inline closures in a brief, focused syntax. Closure expressions provide several syntax optimizations for writing closures in a shortened form without loss of clarity or intent. The closure expression examples below illustrate these optimizations by refining a single example of the &lt;code&gt;sorted&lt;/code&gt; function over several iterations, each of which expresses the same functionality in a more succinct way. &lt;/p&gt;
&lt;h4&gt;The Sorted Function&lt;/h4&gt;
&lt;p&gt;Swift’s standard library provides a function called &lt;code&gt;sorted&lt;/code&gt;, which sorts an array of values of a known type, based on the output of a sorting closure that you provide. Once it completes the sorting process, the &lt;code&gt;sorted&lt;/code&gt; function returns a new array of the same type and size as the old one, with its elements in the correct sorted order. The original array is not modified by the &lt;code&gt;sorted&lt;/code&gt; function. &lt;/p&gt;
&lt;p&gt;The closure expression examples below use the &lt;code&gt;sorted&lt;/code&gt; function to sort an array of &lt;code&gt;String&lt;/code&gt; values in reverse alphabetical order. Here’s the initial array to be sorted: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let names = ["Chris", "Alex", "Ewa", "Barry", "Daniella"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;sorted&lt;/code&gt; function takes two arguments: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;An array of values of a known type. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A closure that takes two arguments of the same type as the array’s contents, and returns a &lt;code&gt;Bool&lt;/code&gt; value to say whether the first value should appear before or after the second value once the values are sorted. The sorting closure needs to return &lt;code&gt;true&lt;/code&gt; if the first value should appear &lt;em&gt;before&lt;/em&gt; the second value, and &lt;code&gt;false&lt;/code&gt; otherwise. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example is sorting an array of &lt;code&gt;String&lt;/code&gt; values, and so the sorting closure needs to be a function of type &lt;code&gt;(String, String) -&amp;gt; Bool&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;One way to provide the sorting closure is to write a normal function of the correct type, and to pass it in as the &lt;code&gt;sorted&lt;/code&gt; function’s second parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func backwards(s1: String, s2: String) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return s1 &amp;gt; s2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var reversed = sorted(names, backwards)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// reversed is equal to ["Ewa", "Daniella", "Chris", "Barry", "Alex"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the first string (&lt;code&gt;s1&lt;/code&gt;) is greater than the second string (&lt;code&gt;s2&lt;/code&gt;), the &lt;code&gt;backwards&lt;/code&gt; function will return &lt;code&gt;true&lt;/code&gt;, indicating that &lt;code&gt;s1&lt;/code&gt; should appear before &lt;code&gt;s2&lt;/code&gt; in the sorted array. For characters in strings, “greater than” means “appears later in the alphabet than”. This means that the letter &lt;code&gt;"B"&lt;/code&gt; is “greater than” the letter &lt;code&gt;"A"&lt;/code&gt;, and the string &lt;code&gt;"Tom"&lt;/code&gt; is greater than the string &lt;code&gt;"Tim"&lt;/code&gt;. This gives a reverse alphabetical sort, with &lt;code&gt;"Barry"&lt;/code&gt; being placed before &lt;code&gt;"Alex"&lt;/code&gt;, and so on. &lt;/p&gt;
&lt;p&gt;However, this is a rather long-winded way to write what is essentially a single-expression function (&lt;code&gt;a &amp;gt; b&lt;/code&gt;). In this example, it would be preferable to write the sorting closure inline, using closure expression syntax. &lt;/p&gt;
&lt;h4&gt;Closure Expression Syntax&lt;/h4&gt;
&lt;p&gt;Closure expression syntax has the following general form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;{ (&lt;em&gt;parameters&lt;/em&gt;) -&amp;gt; &lt;em&gt;return type&lt;/em&gt; in&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Closure expression syntax can use constant parameters, variable parameters, and &lt;code&gt;inout&lt;/code&gt; parameters. Default values cannot be provided. Variadic parameters can be used if you name the variadic parameter and place it last in the parameter list. Tuples can also be used as parameter types and return types. &lt;/p&gt;
&lt;p&gt;The example below shows a closure expression version of the &lt;code&gt;backwards&lt;/code&gt; function from earlier: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reversed = sorted(names, { (s1: String, s2: String) -&amp;gt; Bool in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return s1 &amp;gt; s2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;})&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the declaration of parameters and return type for this inline closure is identical to the declaration from the &lt;code&gt;backwards&lt;/code&gt; function. In both cases, it is written as &lt;code&gt;(s1: String, s2: String) -&amp;gt; Bool&lt;/code&gt;. However, for the inline closure expression, the parameters and return type are written &lt;em&gt;inside&lt;/em&gt; the curly braces, not outside of them. &lt;/p&gt;
&lt;p&gt;The start of the closure’s body is introduced by the &lt;code&gt;in&lt;/code&gt; keyword. This keyword indicates that the definition of the closure’s parameters and return type has finished, and the body of the closure is about to begin. &lt;/p&gt;
&lt;p&gt;Because the body of the closure is so short, it can even be written on a single line: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reversed = sorted(names, { (s1: String, s2: String) -&amp;gt; Bool in return s1 &amp;gt; s2 } )&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This illustrates that the overall call to the &lt;code&gt;sorted&lt;/code&gt; function has remained the same. A pair of parentheses still wrap the entire set of arguments for the function. However, one of those arguments is now an inline closure. &lt;/p&gt;
&lt;h4&gt;Inferring Type From Context&lt;/h4&gt;
&lt;p&gt;Because the sorting closure is passed as an argument to a function, Swift can infer the types of its parameters and the type of the value it returns from the type of the &lt;code&gt;sorted&lt;/code&gt; function’s second parameter. This parameter is expecting a function of type &lt;code&gt;(String, String) -&amp;gt; Bool&lt;/code&gt;. This means that the &lt;code&gt;(String, String)&lt;/code&gt; and &lt;code&gt;Bool&lt;/code&gt; types do not need to be written as part of the closure expression’s definition. Because all of the types can be inferred, the return arrow (&lt;code&gt;-&amp;gt;&lt;/code&gt;) and the parentheses around the names of the parameters can also be omitted: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reversed = sorted(names, { s1, s2 in return s1 &amp;gt; s2 } )&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;It is always possible to infer the parameter types and return type when passing a closure to a function as an inline closure expression. As a result, you never need to write an inline closure in its fullest form when the closure is used as a function argument. &lt;/p&gt;
&lt;p&gt;Nonetheless, you can still make the types explicit if you wish, and doing so is encouraged if it avoids ambiguity for readers of your code. In the case of the &lt;code&gt;sorted&lt;/code&gt; function, the purpose of the closure is clear from the fact that sorting is taking place, and it is safe for a reader to assume that the closure is likely to be working with &lt;code&gt;String&lt;/code&gt; values, because it is assisting with the sorting of an array of strings. &lt;/p&gt;
&lt;h4&gt;Implicit Returns from Single-Expression Closures&lt;/h4&gt;
&lt;p&gt;Single-expression closures can implicitly return the result of their single expression by omitting the &lt;code&gt;return&lt;/code&gt; keyword from their declaration, as in this version of the previous example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reversed = sorted(names, { s1, s2 in s1 &amp;gt; s2 } )&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, the function type of the &lt;code&gt;sorted&lt;/code&gt; function’s second argument makes it clear that a &lt;code&gt;Bool&lt;/code&gt; value must be returned by the closure. Because the closure’s body contains a single expression (&lt;code&gt;s1 &amp;gt; s2&lt;/code&gt;) that returns a &lt;code&gt;Bool&lt;/code&gt; value, there is no ambiguity, and the &lt;code&gt;return&lt;/code&gt; keyword can be omitted. &lt;/p&gt;
&lt;h4&gt;Shorthand Argument Names&lt;/h4&gt;
&lt;p&gt;Swift automatically provides shorthand argument names to inline closures, which can be used to refer to the values of the closure’s arguments by the names &lt;code&gt;$0&lt;/code&gt;, &lt;code&gt;$1&lt;/code&gt;, &lt;code&gt;$2&lt;/code&gt;, and so on. &lt;/p&gt;
&lt;p&gt;If you use these shorthand argument names within your closure expression, you can omit the closure’s argument list from its definition, and the number and type of the shorthand argument names will be inferred from the expected function type. The &lt;code&gt;in&lt;/code&gt; keyword can also be omitted, because the closure expression is made up entirely of its body: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reversed = sorted(names, { $0 &amp;gt; $1 } )&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, &lt;code&gt;$0&lt;/code&gt; and &lt;code&gt;$1&lt;/code&gt; refer to the closure’s first and second &lt;code&gt;String&lt;/code&gt; arguments. &lt;/p&gt;
&lt;h4&gt;Operator Functions&lt;/h4&gt;
&lt;p&gt;There’s actually an even &lt;em&gt;shorter&lt;/em&gt; way to write the closure expression above. Swift’s &lt;code&gt;String&lt;/code&gt; type defines its string-specific implementation of the greater-than operator (&lt;code&gt;&amp;gt;&lt;/code&gt;) as a function that has two parameters of type &lt;code&gt;String&lt;/code&gt;, and returns a value of type &lt;code&gt;Bool&lt;/code&gt;. This exactly matches the function type needed for the &lt;code&gt;sorted&lt;/code&gt; function’s second parameter. Therefore, you can simply pass in the greater-than operator, and Swift will infer that you want to use its string-specific implementation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reversed = sorted(names, &amp;gt;)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more about operator functions, see Operator Functions. &lt;/p&gt;
&lt;h3&gt;Trailing Closures&lt;/h3&gt;
&lt;p&gt;If you need to pass a closure expression to a function as the function’s final argument and the closure expression is long, it can be useful to write it as a &lt;em&gt;trailing closure&lt;/em&gt; instead. A trailing closure is a closure expression that is written outside of (and &lt;em&gt;after&lt;/em&gt;) the parentheses of the function call it supports: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func someFunctionThatTakesAClosure(closure: () -&amp;gt; ()) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// function body goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// here's how you call this function without using a trailing closure:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someFunctionThatTakesAClosure({&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// closure's body goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;})&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// here's how you call this function with a trailing closure instead:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someFunctionThatTakesAClosure() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// trailing closure's body goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If a closure expression is provided as the function’s only argument and you provide that expression as a trailing closure, you do not need to write a pair of parentheses &lt;code&gt;()&lt;/code&gt; after the function’s name when you call the function. &lt;/p&gt;
&lt;p&gt;The string-sorting closure from the Closure Expression Syntax section above can be written outside of the &lt;code&gt;sorted&lt;/code&gt; function’s parentheses as a trailing closure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reversed = sorted(names) { $0 &amp;gt; $1 }&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Trailing closures are most useful when the closure is sufficiently long that it is not possible to write it inline on a single line. As an example, Swift’s &lt;code&gt;Array&lt;/code&gt; type has a &lt;code&gt;map&lt;/code&gt; method which takes a closure expression as its single argument. The closure is called once for each item in the array, and returns an alternative mapped value (possibly of some other type) for that item. The nature of the mapping and the type of the returned value is left up to the closure to specify. &lt;/p&gt;
&lt;p&gt;After applying the provided closure to each array element, the &lt;code&gt;map&lt;/code&gt; method returns a new array containing all of the new mapped values, in the same order as their corresponding values in the original array. &lt;/p&gt;
&lt;p&gt;Here’s how you can use the &lt;code&gt;map&lt;/code&gt; method with a trailing closure to convert an array of &lt;code&gt;Int&lt;/code&gt; values into an array of &lt;code&gt;String&lt;/code&gt; values. The array &lt;code&gt;[16, 58, 510]&lt;/code&gt; is used to create the new array &lt;code&gt;["OneSix", "FiveEight", "FiveOneZero"]&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let digitNames = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;0: "Zero", 1: "One", 2: "Two", 3: "Three", 4: "Four",&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;5: "Five", 6: "Six", 7: "Seven", 8: "Eight", 9: "Nine"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let numbers = [16, 58, 510]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The code above creates a dictionary of mappings between the integer digits and English-language versions of their names. It also defines an array of integers, ready to be converted into strings. &lt;/p&gt;
&lt;p&gt;You can now use the &lt;code&gt;numbers&lt;/code&gt; array to create an array of &lt;code&gt;String&lt;/code&gt; values, by passing a closure expression to the array’s &lt;code&gt;map&lt;/code&gt; method as a trailing closure. Note that the call to &lt;code&gt;numbers.map&lt;/code&gt; does not need to include any parentheses after &lt;code&gt;map&lt;/code&gt;, because the &lt;code&gt;map&lt;/code&gt; method has only one parameter, and that parameter is provided as a trailing closure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let strings = numbers.map {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;(var number) -&amp;gt; String in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var output = ""&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;while number &amp;gt; 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;output = digitNames[number % 10]! + output&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;number /= 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return output&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// strings is inferred to be of type [String]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// its value is ["OneSix", "FiveEight", "FiveOneZero"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;map&lt;/code&gt; method calls the closure expression once for each item in the array. You do not need to specify the type of the closure’s input parameter, &lt;code&gt;number&lt;/code&gt;, because the type can be inferred from the values in the array to be mapped. &lt;/p&gt;
&lt;p&gt;In this example, the closure’s &lt;code&gt;number&lt;/code&gt; parameter is defined as a &lt;em&gt;variable parameter&lt;/em&gt;, as described in Constant and Variable Parameters, so that the parameter’s value can be modified within the closure body, rather than declaring a new local variable and assigning the passed &lt;code&gt;number&lt;/code&gt; value to it. The closure expression also specifies a return type of &lt;code&gt;String&lt;/code&gt;, to indicate the type that will be stored in the mapped output array. &lt;/p&gt;
&lt;p&gt;The closure expression builds a string called &lt;code&gt;output&lt;/code&gt; each time it is called. It calculates the last digit of &lt;code&gt;number&lt;/code&gt; by using the remainder operator (&lt;code&gt;number % 10&lt;/code&gt;), and uses this digit to look up an appropriate string in the &lt;code&gt;digitNames&lt;/code&gt; dictionary. The closure can be used to create a string representation of any integer number greater than zero. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The call to the &lt;code&gt;digitNames&lt;/code&gt; dictionary’s subscript is followed by an exclamation mark (&lt;code&gt;!&lt;/code&gt;), because dictionary subscripts return an optional value to indicate that the dictionary lookup can fail if the key does not exist. In the example above, it is guaranteed that &lt;code&gt;number % 10&lt;/code&gt; will always be a valid subscript key for the &lt;code&gt;digitNames&lt;/code&gt; dictionary, and so an exclamation mark is used to force-unwrap the &lt;code&gt;String&lt;/code&gt; value stored in the subscript’s optional return value. &lt;/p&gt;
&lt;p&gt;The string retrieved from the &lt;code&gt;digitNames&lt;/code&gt; dictionary is added to the &lt;em&gt;front&lt;/em&gt; of &lt;code&gt;output&lt;/code&gt;, effectively building a string version of the number in reverse. (The expression &lt;code&gt;number % 10&lt;/code&gt; gives a value of &lt;code&gt;6&lt;/code&gt; for &lt;code&gt;16&lt;/code&gt;, &lt;code&gt;8&lt;/code&gt; for &lt;code&gt;58&lt;/code&gt;, and &lt;code&gt;0&lt;/code&gt; for &lt;code&gt;510&lt;/code&gt;.) &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;number&lt;/code&gt; variable is then divided by &lt;code&gt;10&lt;/code&gt;. Because it is an integer, it is rounded down during the division, so &lt;code&gt;16&lt;/code&gt; becomes &lt;code&gt;1&lt;/code&gt;, &lt;code&gt;58&lt;/code&gt; becomes &lt;code&gt;5&lt;/code&gt;, and &lt;code&gt;510&lt;/code&gt; becomes &lt;code&gt;51&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The process is repeated until &lt;code&gt;number /= 10&lt;/code&gt; is equal to &lt;code&gt;0&lt;/code&gt;, at which point the &lt;code&gt;output&lt;/code&gt; string is returned by the closure, and is added to the output array by the &lt;code&gt;map&lt;/code&gt; function. &lt;/p&gt;
&lt;p&gt;The use of trailing closure syntax in the example above neatly encapsulates the closure’s functionality immediately after the function that closure supports, without needing to wrap the entire closure within the &lt;code&gt;map&lt;/code&gt; function’s outer parentheses. &lt;/p&gt;
&lt;h3&gt;Capturing Values&lt;/h3&gt;
&lt;p&gt;A closure can &lt;em&gt;capture&lt;/em&gt; constants and variables from the surrounding context in which it is defined. The closure can then refer to and modify the values of those constants and variables from within its body, even if the original scope that defined the constants and variables no longer exists. &lt;/p&gt;
&lt;p&gt;In Swift, the simplest form of a closure that can capture values is a nested function, written within the body of another function. A nested function can capture any of its outer function’s arguments and can also capture any constants and variables defined within the outer function. &lt;/p&gt;
&lt;p&gt;Here’s an example of a function called &lt;code&gt;makeIncrementer&lt;/code&gt;, which contains a nested function called &lt;code&gt;incrementer&lt;/code&gt;. The nested &lt;code&gt;incrementer&lt;/code&gt; function captures two values, &lt;code&gt;runningTotal&lt;/code&gt; and &lt;code&gt;amount&lt;/code&gt;, from its surrounding context. After capturing these values, &lt;code&gt;incrementer&lt;/code&gt; is returned by &lt;code&gt;makeIncrementer&lt;/code&gt; as a closure that increments &lt;code&gt;runningTotal&lt;/code&gt; by &lt;code&gt;amount&lt;/code&gt; each time it is called. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func makeIncrementer(forIncrement amount: Int) -&amp;gt; () -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var runningTotal = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func incrementer() -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;runningTotal += amount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return runningTotal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return incrementer&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The return type of &lt;code&gt;makeIncrementer&lt;/code&gt; is &lt;code&gt;() -&amp;gt; Int&lt;/code&gt;. This means that it returns a &lt;em&gt;function&lt;/em&gt;, rather than a simple value. The function it returns has no parameters, and returns an &lt;code&gt;Int&lt;/code&gt; value each time it is called. To learn how functions can return other functions, see Function Types as Return Types. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;makeIncrementer&lt;/code&gt; function defines an integer variable called &lt;code&gt;runningTotal&lt;/code&gt;, to store the current running total of the incrementer that will be returned. This variable is initialized with a value of &lt;code&gt;0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;makeIncrementer&lt;/code&gt; function has a single &lt;code&gt;Int&lt;/code&gt; parameter with an external name of &lt;code&gt;forIncrement&lt;/code&gt;, and a local name of &lt;code&gt;amount&lt;/code&gt;. The argument value passed to this parameter specifies how much &lt;code&gt;runningTotal&lt;/code&gt; should be incremented by each time the returned incrementer function is called. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;makeIncrementer&lt;/code&gt; defines a nested function called &lt;code&gt;incrementer&lt;/code&gt;, which performs the actual incrementing. This function simply adds &lt;code&gt;amount&lt;/code&gt; to &lt;code&gt;runningTotal&lt;/code&gt;, and returns the result. &lt;/p&gt;
&lt;p&gt;When considered in isolation, the nested &lt;code&gt;incrementer&lt;/code&gt; function might seem unusual: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func incrementer() -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;runningTotal += amount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return runningTotal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;incrementer&lt;/code&gt; function doesn’t have any parameters, and yet it refers to &lt;code&gt;runningTotal&lt;/code&gt; and &lt;code&gt;amount&lt;/code&gt; from within its function body. It does this by capturing the &lt;em&gt;existing&lt;/em&gt; values of &lt;code&gt;runningTotal&lt;/code&gt; and &lt;code&gt;amount&lt;/code&gt; from its surrounding function and using them within its own function body. &lt;/p&gt;
&lt;p&gt;Because it does not modify &lt;code&gt;amount&lt;/code&gt;, &lt;code&gt;incrementer&lt;/code&gt; actually captures and stores a &lt;em&gt;copy&lt;/em&gt; of the value stored in &lt;code&gt;amount&lt;/code&gt;. This value is stored along with the new &lt;code&gt;incrementer&lt;/code&gt; function. &lt;/p&gt;
&lt;p&gt;However, because it modifies the &lt;code&gt;runningTotal&lt;/code&gt; variable each time it is called, &lt;code&gt;incrementer&lt;/code&gt; captures a &lt;em&gt;reference&lt;/em&gt; to the current &lt;code&gt;runningTotal&lt;/code&gt; variable, and not just a copy of its initial value. Capturing a reference ensures that &lt;code&gt;runningTotal&lt;/code&gt; does not disappear when the call to &lt;code&gt;makeIncrementer&lt;/code&gt; ends, and ensures that &lt;code&gt;runningTotal&lt;/code&gt; will continue to be available the next time that the incrementer function is called. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift determines what should be captured by reference and what should be copied by value. You don’t need to annotate &lt;code&gt;amount&lt;/code&gt; or &lt;code&gt;runningTotal&lt;/code&gt; to say that they can be used within the nested &lt;code&gt;incrementer&lt;/code&gt; function. Swift also handles all memory management involved in disposing of &lt;code&gt;runningTotal&lt;/code&gt; when it is no longer needed by the incrementer function. &lt;/p&gt;
&lt;p&gt;Here’s an example of &lt;code&gt;makeIncrementer&lt;/code&gt; in action: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let incrementByTen = makeIncrementer(forIncrement: 10)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example sets a constant called &lt;code&gt;incrementByTen&lt;/code&gt; to refer to an incrementer function that adds &lt;code&gt;10&lt;/code&gt; to its &lt;code&gt;runningTotal&lt;/code&gt; variable each time it is called. Calling the function multiple times shows this behavior in action: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;incrementByTen()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;incrementByTen()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 20&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;incrementByTen()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 30&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you create a second incrementer, it will have its own stored reference to a new, separate &lt;code&gt;runningTotal&lt;/code&gt; variable: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let incrementBySeven = makeIncrementer(forIncrement: 7)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;incrementBySeven()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 7&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Calling the original incrementer (&lt;code&gt;incrementByTen&lt;/code&gt;) again continues to increment its own &lt;code&gt;runningTotal&lt;/code&gt; variable, and does not affect the variable captured by &lt;code&gt;incrementBySeven&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;incrementByTen()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 40&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you assign a closure to a property of a class instance, and the closure captures that instance by referring to the instance or its members, you will create a strong reference cycle between the closure and the instance. Swift uses &lt;em&gt;capture lists&lt;/em&gt; to break these strong reference cycles. For more information, see Strong Reference Cycles for Closures. &lt;/p&gt;
&lt;h3&gt;Closures Are Reference Types&lt;/h3&gt;
&lt;p&gt;In the example above, &lt;code&gt;incrementBySeven&lt;/code&gt; and &lt;code&gt;incrementByTen&lt;/code&gt; are constants, but the closures these constants refer to are still able to increment the &lt;code&gt;runningTotal&lt;/code&gt; variables that they have captured. This is because functions and closures are &lt;em&gt;reference types&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;Whenever you assign a function or a closure to a constant or a variable, you are actually setting that constant or variable to be a &lt;em&gt;reference&lt;/em&gt; to the function or closure. In the example above, it is the choice of closure that &lt;code&gt;incrementByTen&lt;/code&gt; &lt;em&gt;refers to&lt;/em&gt; that is constant, and not the contents of the closure itself. &lt;/p&gt;
&lt;p&gt;This also means that if you assign a closure to two different constants or variables, both of those constants or variables will refer to the same closure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let alsoIncrementByTen = incrementByTen&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;alsoIncrementByTen()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 50&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Enumerations&lt;/h2&gt;
&lt;p&gt;An &lt;em&gt;enumeration&lt;/em&gt; defines a common type for a group of related values and enables you to work with those values in a type-safe way within your code. &lt;/p&gt;
&lt;p&gt;If you are familiar with C, you will know that C enumerations assign related names to a set of integer values. Enumerations in Swift are much more flexible, and do not have to provide a value for each member of the enumeration. If a value (known as a “raw” value) &lt;em&gt;is&lt;/em&gt; provided for each enumeration member, the value can be a string, a character, or a value of any integer or floating-point type. &lt;/p&gt;
&lt;p&gt;Alternatively, enumeration members can specify associated values of &lt;em&gt;any&lt;/em&gt; type to be stored along with each different member value, much as unions or variants do in other languages. You can define a common set of related members as part of one enumeration, each of which has a different set of values of appropriate types associated with it. &lt;/p&gt;
&lt;p&gt;Enumerations in Swift are first-class types in their own right. They adopt many features traditionally supported only by classes, such as computed properties to provide additional information about the enumeration’s current value, and instance methods to provide functionality related to the values the enumeration represents. Enumerations can also define initializers to provide an initial member value; can be extended to expand their functionality beyond their original implementation; and can conform to protocols to provide standard functionality. &lt;/p&gt;
&lt;p&gt;For more on these capabilities, see Properties, Methods, Initialization, Extensions, and Protocols. &lt;/p&gt;
&lt;h3&gt;Enumeration Syntax&lt;/h3&gt;
&lt;p&gt;You introduce enumerations with the &lt;code&gt;enum&lt;/code&gt; keyword and place their entire definition within a pair of braces: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum SomeEnumeration {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// enumeration definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s an example for the four main points of a compass: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum CompassPoint {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case North&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case South&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case East&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case West&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The values defined in an enumeration (such as &lt;code&gt;North&lt;/code&gt;, &lt;code&gt;South&lt;/code&gt;, &lt;code&gt;East&lt;/code&gt;, and &lt;code&gt;West&lt;/code&gt;) are the &lt;em&gt;member values&lt;/em&gt; (or &lt;em&gt;members&lt;/em&gt;) of that enumeration. The &lt;code&gt;case&lt;/code&gt; keyword indicates that a new line of member values is about to be defined. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Unlike C and Objective-C, Swift enumeration members are not assigned a default integer value when they are created. In the &lt;code&gt;CompassPoint&lt;/code&gt; example above, &lt;code&gt;North&lt;/code&gt;, &lt;code&gt;South&lt;/code&gt;, &lt;code&gt;East&lt;/code&gt; and &lt;code&gt;West&lt;/code&gt; do not implicitly equal &lt;code&gt;0&lt;/code&gt;, &lt;code&gt;1&lt;/code&gt;, &lt;code&gt;2&lt;/code&gt; and &lt;code&gt;3&lt;/code&gt;. Instead, the different enumeration members are fully-fledged values in their own right, with an explicitly-defined type of &lt;code&gt;CompassPoint&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Multiple member values can appear on a single line, separated by commas: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum Planet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Mercury, Venus, Earth, Mars, Jupiter, Saturn, Uranus, Neptune&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Each enumeration definition defines a brand new type. Like other types in Swift, their names (such as &lt;code&gt;CompassPoint&lt;/code&gt; and &lt;code&gt;Planet&lt;/code&gt;) should start with a capital letter. Give enumeration types singular rather than plural names, so that they read as self-evident: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var directionToHead = CompassPoint.West&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The type of &lt;code&gt;directionToHead&lt;/code&gt; is inferred when it is initialized with one of the possible values of &lt;code&gt;CompassPoint&lt;/code&gt;. Once &lt;code&gt;directionToHead&lt;/code&gt; is declared as a &lt;code&gt;CompassPoint&lt;/code&gt;, you can set it to a different &lt;code&gt;CompassPoint&lt;/code&gt; value using a shorter dot syntax: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;directionToHead = .East&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The type of &lt;code&gt;directionToHead&lt;/code&gt; is already known, and so you can drop the type when setting its value. This makes for highly readable code when working with explicitly-typed enumeration values. &lt;/p&gt;
&lt;h3&gt;Matching Enumeration Values with a Switch Statement&lt;/h3&gt;
&lt;p&gt;You can match individual enumeration values with a &lt;code&gt;switch&lt;/code&gt; statement: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;directionToHead = .South&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch directionToHead {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .North:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Lots of planets have a north")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .South:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Watch out for penguins")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .East:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Where the sun rises")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .West:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Where the skies are blue")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Watch out for penguins"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can read this code as: &lt;/p&gt;
&lt;p&gt;“Consider the value of &lt;code&gt;directionToHead&lt;/code&gt;. In the case where it equals &lt;code&gt;.North&lt;/code&gt;, print &lt;code&gt;"Lots of planets have a north"&lt;/code&gt;. In the case where it equals &lt;code&gt;.South&lt;/code&gt;, print &lt;code&gt;"Watch out for penguins"&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;…and so on. &lt;/p&gt;
&lt;p&gt;As described in Control Flow, a &lt;code&gt;switch&lt;/code&gt; statement must be exhaustive when considering an enumeration’s members. If the &lt;code&gt;case&lt;/code&gt; for &lt;code&gt;.West&lt;/code&gt; is omitted, this code does not compile, because it does not consider the complete list of &lt;code&gt;CompassPoint&lt;/code&gt; members. Requiring exhaustiveness ensures that enumeration members are not accidentally omitted. &lt;/p&gt;
&lt;p&gt;When it is not appropriate to provide a &lt;code&gt;case&lt;/code&gt; for every enumeration member, you can provide a &lt;code&gt;default&lt;/code&gt; case to cover any members that are not addressed explicitly: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let somePlanet = Planet.Earth&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch somePlanet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Earth:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Mostly harmless")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Not a safe place for humans")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Mostly harmless"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Associated Values&lt;/h3&gt;
&lt;p&gt;The examples in the previous section show how the members of an enumeration are a defined (and typed) value in their own right. You can set a constant or variable to &lt;code&gt;Planet.Earth&lt;/code&gt;, and check for this value later. However, it is sometimes useful to be able to store &lt;em&gt;associated values&lt;/em&gt; of other types alongside these member values. This enables you to store additional custom information along with the member value, and permits this information to vary each time you use that member in your code. &lt;/p&gt;
&lt;p&gt;You can define Swift enumerations to store associated values of any given type, and the value types can be different for each member of the enumeration if needed. Enumerations similar to these are known as &lt;em&gt;discriminated unions&lt;/em&gt;, &lt;em&gt;tagged unions&lt;/em&gt;, or &lt;em&gt;variants&lt;/em&gt; in other programming languages. &lt;/p&gt;
&lt;p&gt;For example, suppose an inventory tracking system needs to track products by two different types of barcode. Some products are labeled with 1D barcodes in UPC-A format, which uses the numbers &lt;code&gt;0&lt;/code&gt; to &lt;code&gt;9&lt;/code&gt;. Each barcode has a “number system” digit, followed by five “manufacturer code” digits and five “product code” digits. These are followed by a “check” digit to verify that the code has been scanned correctly: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/barcode_UPC_2x.png" src="http://muxuezi.github.io/posts/Art/barcode_UPC_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Other products are labeled with 2D barcodes in QR code format, which can use any ISO 8859-1 character and can encode a string up to 2,953 characters long: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/barcode_QR_2x.png" src="http://muxuezi.github.io/posts/Art/barcode_QR_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;It would be convenient for an inventory tracking system to be able to store UPC-A barcodes as a tuple of four integers, and QR code barcodes as a string of any length. &lt;/p&gt;
&lt;p&gt;In Swift, an enumeration to define product barcodes of either type might look like this: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum Barcode {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case UPCA(Int, Int, Int, Int)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case QRCode(String)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This can be read as: &lt;/p&gt;
&lt;p&gt;“Define an enumeration type called &lt;code&gt;Barcode&lt;/code&gt;, which can take either a value of &lt;code&gt;UPCA&lt;/code&gt; with an associated value of type (&lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;Int&lt;/code&gt;), or a value of &lt;code&gt;QRCode&lt;/code&gt; with an associated value of type &lt;code&gt;String&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;This definition does not provide any actual &lt;code&gt;Int&lt;/code&gt; or &lt;code&gt;String&lt;/code&gt; values—it just defines the &lt;em&gt;type&lt;/em&gt; of associated values that &lt;code&gt;Barcode&lt;/code&gt; constants and variables can store when they are equal to &lt;code&gt;Barcode.UPCA&lt;/code&gt; or &lt;code&gt;Barcode.QRCode&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;New barcodes can then be created using either type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var productBarcode = Barcode.UPCA(8, 85909, 51226, 3)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example creates a new variable called &lt;code&gt;productBarcode&lt;/code&gt; and assigns it a value of &lt;code&gt;Barcode.UPCA&lt;/code&gt; with an associated tuple value of &lt;code&gt;(8, 85909, 51226, 3)&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The same product can be assigned a different type of barcode: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;productBarcode = .QRCode("ABCDEFGHIJKLMNOP")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;At this point, the original &lt;code&gt;Barcode.UPCA&lt;/code&gt; and its integer values are replaced by the new &lt;code&gt;Barcode.QRCode&lt;/code&gt; and its string value. Constants and variables of type &lt;code&gt;Barcode&lt;/code&gt; can store either a &lt;code&gt;.UPCA&lt;/code&gt; or a &lt;code&gt;.QRCode&lt;/code&gt; (together with their associated values), but they can only store one of them at any given time. &lt;/p&gt;
&lt;p&gt;The different barcode types can be checked using a switch statement, as before. This time, however, the associated values can be extracted as part of the switch statement. You extract each associated value as a constant (with the &lt;code&gt;let&lt;/code&gt; prefix) or a variable (with the &lt;code&gt;var&lt;/code&gt; prefix) for use within the &lt;code&gt;switch&lt;/code&gt; case’s body: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;switch productBarcode {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .UPCA(let numberSystem, let manufacturer, let product, let check):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("UPC-A: \\(numberSystem), \\(manufacturer), \\(product), \\(check).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .QRCode(let productCode):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("QR code: \\(productCode).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "QR code: ABCDEFGHIJKLMNOP."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If all of the associated values for an enumeration member are extracted as constants, or if all are extracted as variables, you can place a single &lt;code&gt;var&lt;/code&gt; or &lt;code&gt;let&lt;/code&gt; annotation before the member name, for brevity: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;switch productBarcode {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let .UPCA(numberSystem, manufacturer, product, check):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("UPC-A: \\(numberSystem), \\(manufacturer), \\(product), \\(check).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let .QRCode(productCode):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("QR code: \\(productCode).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "QR code: ABCDEFGHIJKLMNOP."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Raw Values&lt;/h3&gt;
&lt;p&gt;The barcode example in Associated Values shows how members of an enumeration can declare that they store associated values of different types. As an alternative to associated values, enumeration members can come prepopulated with default values (called &lt;em&gt;raw values&lt;/em&gt;), which are all of the same type. &lt;/p&gt;
&lt;p&gt;Here’s an example that stores raw ASCII values alongside named enumeration members: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum ASCIIControlCharacter: Character {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Tab = "\t"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case LineFeed = "\n"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case CarriageReturn = "\r"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, the raw values for an enumeration called &lt;code&gt;ASCIIControlCharacter&lt;/code&gt; are defined to be of type &lt;code&gt;Character&lt;/code&gt;, and are set to some of the more common ASCII control characters. &lt;code&gt;Character&lt;/code&gt; values are described in Strings and Characters. &lt;/p&gt;
&lt;p&gt;Note that raw values are &lt;em&gt;not&lt;/em&gt; the same as associated values. Raw values are set to prepopulated values when you first define the enumeration in your code, like the three ASCII codes above. The raw value for a particular enumeration member is always the same. Associated values are set when you create a new constant or variable based on one of the enumeration’s members, and can be different each time you do so. &lt;/p&gt;
&lt;p&gt;Raw values can be strings, characters, or any of the integer or floating-point number types. Each raw value must be unique within its enumeration declaration. When integers are used for raw values, they auto-increment if no value is specified for some of the enumeration members. &lt;/p&gt;
&lt;p&gt;The enumeration below is a refinement of the earlier &lt;code&gt;Planet&lt;/code&gt; enumeration, with raw integer values to represent each planet’s order from the sun: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum Planet: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Mercury = 1, Venus, Earth, Mars, Jupiter, Saturn, Uranus, Neptune&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Auto-incrementation means that &lt;code&gt;Planet.Venus&lt;/code&gt; has a raw value of &lt;code&gt;2&lt;/code&gt;, and so on. &lt;/p&gt;
&lt;p&gt;Access the raw value of an enumeration member with its &lt;code&gt;rawValue&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let earthsOrder = Planet.Earth.rawValue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// earthsOrder is 3&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Initializing from a Raw Value&lt;/h4&gt;
&lt;p&gt;If you define an enumeration with a raw-value type, the enumeration automatically receives an initializer that takes a value of the raw value’s type (as a parameter called &lt;code&gt;rawValue&lt;/code&gt;) and returns either an enumeration member or &lt;code&gt;nil&lt;/code&gt;. You can use this initializer to try to create a new instance of the enumeration. &lt;/p&gt;
&lt;p&gt;This example identifies Uranus from its raw value of &lt;code&gt;7&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let possiblePlanet = Planet(rawValue: 7)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// possiblePlanet is of type Planet? and equals Planet.Uranus&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Not all possible &lt;code&gt;Int&lt;/code&gt; values will find a matching planet, however. Because of this, the raw value initializer always returns an &lt;em&gt;optional&lt;/em&gt; enumeration member. In the example above, &lt;code&gt;possiblePlanet&lt;/code&gt; is of type &lt;code&gt;Planet?&lt;/code&gt;, or “optional &lt;code&gt;Planet&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The raw value initializer is a failable initializer, because not every raw value will return an enumeration member. For more information, see Failable Initializers. &lt;/p&gt;
&lt;p&gt;If you try to find a planet with a position of &lt;code&gt;9&lt;/code&gt;, the optional &lt;code&gt;Planet&lt;/code&gt; value returned by the raw value initializer will be &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let positionToFind = 9&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let somePlanet = Planet(rawValue: positionToFind) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch somePlanet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Earth:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Mostly harmless")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Not a safe place for humans")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("There isn't a planet at position \\(positionToFind)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "There isn't a planet at position 9"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example uses optional binding to try to access a planet with a raw value of &lt;code&gt;9&lt;/code&gt;. The statement &lt;code&gt;if let somePlanet = Planet(rawValue: 9)&lt;/code&gt; creates an optional &lt;code&gt;Planet&lt;/code&gt;, and sets &lt;code&gt;somePlanet&lt;/code&gt; to the value of that optional &lt;code&gt;Planet&lt;/code&gt; if it can be retrieved. In this case, it is not possible to retrieve a planet with a position of &lt;code&gt;9&lt;/code&gt;, and so the &lt;code&gt;else&lt;/code&gt; branch is executed instead. &lt;/p&gt;
&lt;h2&gt;Classes and Structures&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Classes&lt;/em&gt; and &lt;em&gt;structures&lt;/em&gt; are general-purpose, flexible constructs that become the building blocks of your program’s code. You define properties and methods to add functionality to your classes and structures by using exactly the same syntax as for constants, variables, and functions. &lt;/p&gt;
&lt;p&gt;Unlike other programming languages, Swift does not require you to create separate interface and implementation files for custom classes and structures. In Swift, you define a class or a structure in a single file, and the external interface to that class or structure is automatically made available for other code to use. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;An instance of a &lt;em&gt;class&lt;/em&gt; is traditionally known as an &lt;em&gt;object&lt;/em&gt;. However, Swift classes and structures are much closer in functionality than in other languages, and much of this chapter describes functionality that can apply to instances of &lt;em&gt;either&lt;/em&gt; a class or a structure type. Because of this, the more general term &lt;em&gt;instance&lt;/em&gt; is used. &lt;/p&gt;
&lt;h3&gt;Comparing Classes and Structures&lt;/h3&gt;
&lt;p&gt;Classes and structures in Swift have many things in common. Both can: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Define properties to store values &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Define methods to provide functionality &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Define subscripts to provide access to their values using subscript syntax &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Define initializers to set up their initial state &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Be extended to expand their functionality beyond a default implementation &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Conform to protocols to provide standard functionality of a certain kind &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more information, see Properties, Methods, Subscripts, Initialization, Extensions, and Protocols. &lt;/p&gt;
&lt;p&gt;Classes have additional capabilities that structures do not: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Inheritance enables one class to inherit the characteristics of another. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Type casting enables you to check and interpret the type of a class instance at runtime. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Deinitializers enable an instance of a class to free up any resources it has assigned. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Reference counting allows more than one reference to a class instance. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more information, see Inheritance, Type Casting, Deinitialization, and Automatic Reference Counting. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Structures are always copied when they are passed around in your code, and do not use reference counting. &lt;/p&gt;
&lt;h4&gt;Definition Syntax&lt;/h4&gt;
&lt;p&gt;Classes and structures have a similar definition syntax. You introduce classes with the &lt;code&gt;class&lt;/code&gt; keyword and structures with the &lt;code&gt;struct&lt;/code&gt; keyword. Both place their entire definition within a pair of braces: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// class definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct SomeStructure {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// structure definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Whenever you define a new class or structure, you effectively define a brand new Swift type. Give types &lt;code&gt;UpperCamelCase&lt;/code&gt; names (such as &lt;code&gt;SomeClass&lt;/code&gt; and &lt;code&gt;SomeStructure&lt;/code&gt; here) to match the capitalization of standard Swift types (such as &lt;code&gt;String&lt;/code&gt;, &lt;code&gt;Int&lt;/code&gt;, and &lt;code&gt;Bool&lt;/code&gt;). Conversely, always give properties and methods &lt;code&gt;lowerCamelCase&lt;/code&gt; names (such as &lt;code&gt;frameRate&lt;/code&gt; and &lt;code&gt;incrementCount&lt;/code&gt;) to differentiate them from type names. &lt;/p&gt;
&lt;p&gt;Here’s an example of a structure definition and a class definition: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Resolution {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var width = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var height = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class VideoMode {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var resolution = Resolution()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var interlaced = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var frameRate = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example above defines a new structure called &lt;code&gt;Resolution&lt;/code&gt;, to describe a pixel-based display resolution. This structure has two stored properties called &lt;code&gt;width&lt;/code&gt; and &lt;code&gt;height&lt;/code&gt;. Stored properties are constants or variables that are bundled up and stored as part of the class or structure. These two properties are inferred to be of type &lt;code&gt;Int&lt;/code&gt; by setting them to an initial integer value of &lt;code&gt;0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The example above also defines a new class called &lt;code&gt;VideoMode&lt;/code&gt;, to describe a specific video mode for video display. This class has four variable stored properties. The first, &lt;code&gt;resolution&lt;/code&gt;, is initialized with a new &lt;code&gt;Resolution&lt;/code&gt; structure instance, which infers a property type of &lt;code&gt;Resolution&lt;/code&gt;. For the other three properties, new &lt;code&gt;VideoMode&lt;/code&gt; instances will be initialized with an &lt;code&gt;interlaced&lt;/code&gt; setting of &lt;code&gt;false&lt;/code&gt; (meaning “non-interlaced video”), a playback frame rate of &lt;code&gt;0.0&lt;/code&gt;, and an optional &lt;code&gt;String&lt;/code&gt; value called &lt;code&gt;name&lt;/code&gt;. The &lt;code&gt;name&lt;/code&gt; property is automatically given a default value of &lt;code&gt;nil&lt;/code&gt;, or “no &lt;code&gt;name&lt;/code&gt; value”, because it is of an optional type. &lt;/p&gt;
&lt;h4&gt;Class and Structure Instances&lt;/h4&gt;
&lt;p&gt;The &lt;code&gt;Resolution&lt;/code&gt; structure definition and the &lt;code&gt;VideoMode&lt;/code&gt; class definition only describe what a &lt;code&gt;Resolution&lt;/code&gt; or &lt;code&gt;VideoMode&lt;/code&gt; will look like. They themselves do not describe a specific resolution or video mode. To do that, you need to create an instance of the structure or class. &lt;/p&gt;
&lt;p&gt;The syntax for creating instances is very similar for both structures and classes: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someResolution = Resolution()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let someVideoMode = VideoMode()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Structures and classes both use initializer syntax for new instances. The simplest form of initializer syntax uses the type name of the class or structure followed by empty parentheses, such as &lt;code&gt;Resolution()&lt;/code&gt; or &lt;code&gt;VideoMode()&lt;/code&gt;. This creates a new instance of the class or structure, with any properties initialized to their default values. Class and structure initialization is described in more detail in Initialization. &lt;/p&gt;
&lt;h4&gt;Accessing Properties&lt;/h4&gt;
&lt;p&gt;You can access the properties of an instance using &lt;em&gt;dot syntax&lt;/em&gt;. In dot syntax, you write the property name immediately after the instance name, separated by a period (&lt;code&gt;.&lt;/code&gt;), without any spaces: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The width of someResolution is \\(someResolution.width)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The width of someResolution is 0"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this example, &lt;code&gt;someResolution.width&lt;/code&gt; refers to the &lt;code&gt;width&lt;/code&gt; property of &lt;code&gt;someResolution&lt;/code&gt;, and returns its default initial value of &lt;code&gt;0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can drill down into sub-properties, such as the &lt;code&gt;width&lt;/code&gt; property in the &lt;code&gt;resolution&lt;/code&gt; property of a &lt;code&gt;VideoMode&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The width of someVideoMode is \\(someVideoMode.resolution.width)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The width of someVideoMode is 0"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also use dot syntax to assign a new value to a variable property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;someVideoMode.resolution.width = 1280&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The width of someVideoMode is now \\(someVideoMode.resolution.width)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The width of someVideoMode is now 1280"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Unlike Objective-C, Swift enables you to set sub-properties of a structure property directly. In the last example above, the &lt;code&gt;width&lt;/code&gt; property of the &lt;code&gt;resolution&lt;/code&gt; property of &lt;code&gt;someVideoMode&lt;/code&gt; is set directly, without your needing to set the entire &lt;code&gt;resolution&lt;/code&gt; property to a new value. &lt;/p&gt;
&lt;h4&gt;Memberwise Initializers for Structure Types&lt;/h4&gt;
&lt;p&gt;All structures have an automatically-generated &lt;em&gt;memberwise initializer&lt;/em&gt;, which you can use to initialize the member properties of new structure instances. Initial values for the properties of the new instance can be passed to the memberwise initializer by name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let vga = Resolution(width: 640, height: 480)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unlike structures, class instances do not receive a default memberwise initializer. Initializers are described in more detail in Initialization. &lt;/p&gt;
&lt;h3&gt;Structures and Enumerations Are Value Types&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;value type&lt;/em&gt; is a type whose value is &lt;em&gt;copied&lt;/em&gt; when it is assigned to a variable or constant, or when it is passed to a function. &lt;/p&gt;
&lt;p&gt;You’ve actually been using value types extensively throughout the previous chapters. In fact, all of the basic types in Swift—integers, floating-point numbers, Booleans, strings, arrays and dictionaries—are value types, and are implemented as structures behind the scenes. &lt;/p&gt;
&lt;p&gt;All structures and enumerations are value types in Swift. This means that any structure and enumeration instances you create—and any value types they have as properties—are always copied when they are passed around in your code. &lt;/p&gt;
&lt;p&gt;Consider this example, which uses the &lt;code&gt;Resolution&lt;/code&gt; structure from the previous example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let hd = Resolution(width: 1920, height: 1080)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var cinema = hd&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example declares a constant called &lt;code&gt;hd&lt;/code&gt; and sets it to a &lt;code&gt;Resolution&lt;/code&gt; instance initialized with the width and height of full HD video (&lt;code&gt;1920&lt;/code&gt; pixels wide by &lt;code&gt;1080&lt;/code&gt; pixels high). &lt;/p&gt;
&lt;p&gt;It then declares a variable called &lt;code&gt;cinema&lt;/code&gt; and sets it to the current value of &lt;code&gt;hd&lt;/code&gt;. Because &lt;code&gt;Resolution&lt;/code&gt; is a structure, a &lt;em&gt;copy&lt;/em&gt; of the existing instance is made, and this new copy is assigned to &lt;code&gt;cinema&lt;/code&gt;. Even though &lt;code&gt;hd&lt;/code&gt; and &lt;code&gt;cinema&lt;/code&gt; now have the same width and height, they are two completely different instances behind the scenes. &lt;/p&gt;
&lt;p&gt;Next, the &lt;code&gt;width&lt;/code&gt; property of &lt;code&gt;cinema&lt;/code&gt; is amended to be the width of the slightly-wider 2K standard used for digital cinema projection (&lt;code&gt;2048&lt;/code&gt; pixels wide and &lt;code&gt;1080&lt;/code&gt; pixels high): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;cinema.width = 2048&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Checking the &lt;code&gt;width&lt;/code&gt; property of &lt;code&gt;cinema&lt;/code&gt; shows that it has indeed changed to be &lt;code&gt;2048&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("cinema is now \\(cinema.width) pixels wide")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "cinema is now 2048 pixels wide"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;However, the &lt;code&gt;width&lt;/code&gt; property of the original &lt;code&gt;hd&lt;/code&gt; instance still has the old value of &lt;code&gt;1920&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("hd is still \\(hd.width) pixels wide")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "hd is still 1920 pixels wide"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When &lt;code&gt;cinema&lt;/code&gt; was given the current value of &lt;code&gt;hd&lt;/code&gt;, the &lt;em&gt;values&lt;/em&gt; stored in &lt;code&gt;hd&lt;/code&gt; were copied into the new &lt;code&gt;cinema&lt;/code&gt; instance. The end result is two completely separate instances, which just happened to contain the same numeric values. Because they are separate instances, setting the width of &lt;code&gt;cinema&lt;/code&gt; to &lt;code&gt;2048&lt;/code&gt; doesn’t affect the width stored in &lt;code&gt;hd&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The same behavior applies to enumerations: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum CompassPoint {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case North, South, East, West&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentDirection = CompassPoint.West&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let rememberedDirection = currentDirection&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentDirection = .East&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if rememberedDirection == .West {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The remembered direction is still .West")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The remembered direction is still .West"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When &lt;code&gt;rememberedDirection&lt;/code&gt; is assigned the value of &lt;code&gt;currentDirection&lt;/code&gt;, it is actually set to a copy of that value. Changing the value of &lt;code&gt;currentDirection&lt;/code&gt; thereafter does not affect the copy of the original value that was stored in &lt;code&gt;rememberedDirection&lt;/code&gt;. &lt;/p&gt;
&lt;h3&gt;Classes Are Reference Types&lt;/h3&gt;
&lt;p&gt;Unlike value types, &lt;em&gt;reference types&lt;/em&gt; are &lt;em&gt;not&lt;/em&gt; copied when they are assigned to a variable or constant, or when they are passed to a function. Rather than a copy, a reference to the same existing instance is used instead. &lt;/p&gt;
&lt;p&gt;Here’s an example, using the &lt;code&gt;VideoMode&lt;/code&gt; class defined above: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let tenEighty = VideoMode()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tenEighty.resolution = hd&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tenEighty.interlaced = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tenEighty.name = "1080i"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tenEighty.frameRate = 25.0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example declares a new constant called &lt;code&gt;tenEighty&lt;/code&gt; and sets it to refer to a new instance of the &lt;code&gt;VideoMode&lt;/code&gt; class. The video mode is assigned a copy of the HD resolution of &lt;code&gt;1920&lt;/code&gt; by &lt;code&gt;1080&lt;/code&gt; from before. It is set to be interlaced, and is given a name of &lt;code&gt;"1080i"&lt;/code&gt;. Finally, it is set to a frame rate of &lt;code&gt;25.0&lt;/code&gt; frames per second. &lt;/p&gt;
&lt;p&gt;Next, &lt;code&gt;tenEighty&lt;/code&gt; is assigned to a new constant, called &lt;code&gt;alsoTenEighty&lt;/code&gt;, and the frame rate of &lt;code&gt;alsoTenEighty&lt;/code&gt; is modified: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let alsoTenEighty = tenEighty&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;alsoTenEighty.frameRate = 30.0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because classes are reference types, &lt;code&gt;tenEighty&lt;/code&gt; and &lt;code&gt;alsoTenEighty&lt;/code&gt; actually both refer to the &lt;em&gt;same&lt;/em&gt; &lt;code&gt;VideoMode&lt;/code&gt; instance. Effectively, they are just two different names for the same single instance. &lt;/p&gt;
&lt;p&gt;Checking the &lt;code&gt;frameRate&lt;/code&gt; property of &lt;code&gt;tenEighty&lt;/code&gt; shows that it correctly reports the new frame rate of &lt;code&gt;30.0&lt;/code&gt; from the underlying &lt;code&gt;VideoMode&lt;/code&gt; instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The frameRate property of tenEighty is now \\(tenEighty.frameRate)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The frameRate property of tenEighty is now 30.0"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that &lt;code&gt;tenEighty&lt;/code&gt; and &lt;code&gt;alsoTenEighty&lt;/code&gt; are declared as &lt;em&gt;constants&lt;/em&gt;, rather than variables. However, you can still change &lt;code&gt;tenEighty.frameRate&lt;/code&gt; and &lt;code&gt;alsoTenEighty.frameRate&lt;/code&gt; because the values of the &lt;code&gt;tenEighty&lt;/code&gt; and &lt;code&gt;alsoTenEighty&lt;/code&gt; constants themselves do not actually change. &lt;code&gt;tenEighty&lt;/code&gt; and &lt;code&gt;alsoTenEighty&lt;/code&gt; themselves do not “store” the &lt;code&gt;VideoMode&lt;/code&gt; instance—instead, they both &lt;em&gt;refer&lt;/em&gt; to a &lt;code&gt;VideoMode&lt;/code&gt; instance behind the scenes. It is the &lt;code&gt;frameRate&lt;/code&gt; property of the underlying &lt;code&gt;VideoMode&lt;/code&gt; that is changed, not the values of the constant references to that &lt;code&gt;VideoMode&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Identity Operators&lt;/h4&gt;
&lt;p&gt;Because classes are reference types, it is possible for multiple constants and variables to refer to the same single instance of a class behind the scenes. (The same is not true for structures and enumerations, because they are always copied when they are assigned to a constant or variable, or passed to a function.) &lt;/p&gt;
&lt;p&gt;It can sometimes be useful to find out if two constants or variables refer to exactly the same instance of a class. To enable this, Swift provides two identity operators: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Identical to (&lt;code&gt;===&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Not identical to (&lt;code&gt;!==&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Use these operators to check whether two constants or variables refer to the same single instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if tenEighty === alsoTenEighty {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("tenEighty and alsoTenEighty refer to the same VideoMode instance.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "tenEighty and alsoTenEighty refer to the same VideoMode instance."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that “identical to” (represented by three equals signs, or &lt;code&gt;===&lt;/code&gt;) does not mean the same thing as “equal to” (represented by two equals signs, or &lt;code&gt;==&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;“Identical to” means that two constants or variables of class type refer to exactly the same class instance. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;“Equal to” means that two instances are considered “equal” or “equivalent” in value, for some appropriate meaning of “equal”, as defined by the type’s designer. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When you define your own custom classes and structures, it is your responsibility to decide what qualifies as two instances being “equal”. The process of defining your own implementations of the “equal to” and “not equal to” operators is described in Equivalence Operators. &lt;/p&gt;
&lt;h4&gt;Pointers&lt;/h4&gt;
&lt;p&gt;If you have experience with C, C++, or Objective-C, you may know that these languages use &lt;em&gt;pointers&lt;/em&gt; to refer to addresses in memory. A Swift constant or variable that refers to an instance of some reference type is similar to a pointer in C, but is not a direct pointer to an address in memory, and does not require you to write an asterisk (&lt;code&gt;*&lt;/code&gt;) to indicate that you are creating a reference. Instead, these references are defined like any other constant or variable in Swift. &lt;/p&gt;
&lt;h3&gt;Choosing Between Classes and Structures&lt;/h3&gt;
&lt;p&gt;You can use both classes and structures to define custom data types to use as the building blocks of your program’s code. &lt;/p&gt;
&lt;p&gt;However, structure instances are always passed by &lt;em&gt;value&lt;/em&gt;, and class instances are always passed by &lt;em&gt;reference&lt;/em&gt;. This means that they are suited to different kinds of tasks. As you consider the data constructs and functionality that you need for a project, decide whether each data construct should be defined as a class or as a structure. &lt;/p&gt;
&lt;p&gt;As a general guideline, consider creating a structure when one or more of these conditions apply: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The structure’s primary purpose is to encapsulate a few relatively simple data values. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;It is reasonable to expect that the encapsulated values will be copied rather than referenced when you assign or pass around an instance of that structure. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Any properties stored by the structure are themselves value types, which would also be expected to be copied rather than referenced. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The structure does not need to inherit properties or behavior from another existing type. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Examples of good candidates for structures include: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The size of a geometric shape, perhaps encapsulating a &lt;code&gt;width&lt;/code&gt; property and a &lt;code&gt;height&lt;/code&gt; property, both of type &lt;code&gt;Double&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A way to refer to ranges within a series, perhaps encapsulating a &lt;code&gt;start&lt;/code&gt; property and a &lt;code&gt;length&lt;/code&gt; property, both of type &lt;code&gt;Int&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A point in a 3D coordinate system, perhaps encapsulating &lt;code&gt;x&lt;/code&gt;, &lt;code&gt;y&lt;/code&gt; and &lt;code&gt;z&lt;/code&gt; properties, each of type &lt;code&gt;Double&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In all other cases, define a class, and create instances of that class to be managed and passed by reference. In practice, this means that most custom data constructs should be classes, not structures. &lt;/p&gt;
&lt;h3&gt;Assignment and Copy Behavior for Strings, Arrays, and Dictionaries&lt;/h3&gt;
&lt;p&gt;Swift’s &lt;code&gt;String&lt;/code&gt;, &lt;code&gt;Array&lt;/code&gt;, and &lt;code&gt;Dictionary&lt;/code&gt; types are implemented as structures. This means that strings, arrays, and dictionaries are copied when they are assigned to a new constant or variable, or when they are passed to a function or method. &lt;/p&gt;
&lt;p&gt;This behavior is different from &lt;code&gt;NSString&lt;/code&gt;, &lt;code&gt;NSArray&lt;/code&gt;, and &lt;code&gt;NSDictionary&lt;/code&gt; in Foundation, which are implemented as classes, not structures. &lt;code&gt;NSString&lt;/code&gt;, &lt;code&gt;NSArray&lt;/code&gt;, and &lt;code&gt;NSDictionary&lt;/code&gt; instances are always assigned and passed around as a reference to an existing instance, rather than as a copy. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The description above refers to the “copying” of strings, arrays, and dictionaries. The behavior you see in your code will always be as if a copy took place. However, Swift only performs an &lt;em&gt;actual&lt;/em&gt; copy behind the scenes when it is absolutely necessary to do so. Swift manages all value copying to ensure optimal performance, and you should not avoid assignment to try to preempt this optimization. &lt;/p&gt;
&lt;h2&gt;Properties&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Properties&lt;/em&gt; associate values with a particular class, structure, or enumeration. Stored properties store constant and variable values as part of an instance, whereas computed properties calculate (rather than store) a value. Computed properties are provided by classes, structures, and enumerations. Stored properties are provided only by classes and structures. &lt;/p&gt;
&lt;p&gt;Stored and computed properties are usually associated with instances of a particular type. However, properties can also be associated with the type itself. Such properties are known as type properties. &lt;/p&gt;
&lt;p&gt;In addition, you can define property observers to monitor changes in a property’s value, which you can respond to with custom actions. Property observers can be added to stored properties you define yourself, and also to properties that a subclass inherits from its superclass. &lt;/p&gt;
&lt;h3&gt;Stored Properties&lt;/h3&gt;
&lt;p&gt;In its simplest form, a stored property is a constant or variable that is stored as part of an instance of a particular class or structure. Stored properties can be either &lt;em&gt;variable stored properties&lt;/em&gt; (introduced by the &lt;code&gt;var&lt;/code&gt; keyword) or &lt;em&gt;constant stored properties&lt;/em&gt; (introduced by the &lt;code&gt;let&lt;/code&gt; keyword). &lt;/p&gt;
&lt;p&gt;You can provide a default value for a stored property as part of its definition, as described in Default Property Values. You can also set and modify the initial value for a stored property during initialization. This is true even for constant stored properties, as described in Modifying Constant Properties During Initialization. &lt;/p&gt;
&lt;p&gt;The example below defines a structure called &lt;code&gt;FixedLengthRange&lt;/code&gt;, which describes a range of integers whose range length cannot be changed once it is created: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct FixedLengthRange {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var firstValue: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let length: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var rangeOfThreeItems = FixedLengthRange(firstValue: 0, length: 3)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the range represents integer values 0, 1, and 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;rangeOfThreeItems.firstValue = 6&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the range now represents integer values 6, 7, and 8&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Instances of &lt;code&gt;FixedLengthRange&lt;/code&gt; have a variable stored property called &lt;code&gt;firstValue&lt;/code&gt; and a constant stored property called &lt;code&gt;length&lt;/code&gt;. In the example above, &lt;code&gt;length&lt;/code&gt; is initialized when the new range is created and cannot be changed thereafter, because it is a constant property. &lt;/p&gt;
&lt;h4&gt;Stored Properties of Constant Structure Instances&lt;/h4&gt;
&lt;p&gt;If you create an instance of a structure and assign that instance to a constant, you cannot modify the instance’s properties, even if they were declared as variable properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let rangeOfFourItems = FixedLengthRange(firstValue: 0, length: 4)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this range represents integer values 0, 1, 2, and 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;rangeOfFourItems.firstValue = 6&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this will report an error, even though firstValue is a variable property&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because &lt;code&gt;rangeOfFourItems&lt;/code&gt; is declared as a constant (with the &lt;code&gt;let&lt;/code&gt; keyword), it is not possible to change its &lt;code&gt;firstValue&lt;/code&gt; property, even though &lt;code&gt;firstValue&lt;/code&gt; is a variable property. &lt;/p&gt;
&lt;p&gt;This behavior is due to structures being &lt;em&gt;value types&lt;/em&gt;. When an instance of a value type is marked as a constant, so are all of its properties. &lt;/p&gt;
&lt;p&gt;The same is not true for classes, which are &lt;em&gt;reference types&lt;/em&gt;. If you assign an instance of a reference type to a constant, you can still change that instance’s variable properties. &lt;/p&gt;
&lt;h4&gt;Lazy Stored Properties&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;lazy stored property&lt;/em&gt; is a property whose initial value is not calculated until the first time it is used. You indicate a lazy stored property by writing the &lt;code&gt;lazy&lt;/code&gt; modifier before its declaration. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You must always declare a lazy property as a variable (with the &lt;code&gt;var&lt;/code&gt; keyword), because its initial value might not be retrieved until after instance initialization completes. Constant properties must always have a value &lt;em&gt;before&lt;/em&gt; initialization completes, and therefore cannot be declared as lazy. &lt;/p&gt;
&lt;p&gt;Lazy properties are useful when the initial value for a property is dependent on outside factors whose values are not known until after an instance’s initialization is complete. Lazy properties are also useful when the initial value for a property requires complex or computationally expensive setup that should not be performed unless or until it is needed. &lt;/p&gt;
&lt;p&gt;The example below uses a lazy stored property to avoid unnecessary initialization of a complex class. This example defines two classes called &lt;code&gt;DataImporter&lt;/code&gt; and &lt;code&gt;DataManager&lt;/code&gt;, neither of which is shown in full: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class DataImporter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/*&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;DataImporter is a class to import data from an external file.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;The class is assumed to take a non-trivial amount of time to initialize.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;*/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var fileName = "data.txt"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the DataImporter class would provide data importing functionality here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class DataManager {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;lazy var importer = DataImporter()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var data = [String]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the DataManager class would provide data management functionality here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let manager = DataManager()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;manager.data.append("Some data")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;manager.data.append("Some more data")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the DataImporter instance for the importer property has not yet been created&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;DataManager&lt;/code&gt; class has a stored property called &lt;code&gt;data&lt;/code&gt;, which is initialized with a new, empty array of &lt;code&gt;String&lt;/code&gt; values. Although the rest of its functionality is not shown, the purpose of this &lt;code&gt;DataManager&lt;/code&gt; class is to manage and provide access to this array of &lt;code&gt;String&lt;/code&gt; data. &lt;/p&gt;
&lt;p&gt;Part of the functionality of the &lt;code&gt;DataManager&lt;/code&gt; class is the ability to import data from a file. This functionality is provided by the &lt;code&gt;DataImporter&lt;/code&gt; class, which is assumed to take a non-trivial amount of time to initialize. This might be because a &lt;code&gt;DataImporter&lt;/code&gt; instance needs to open a file and read its contents into memory when the &lt;code&gt;DataImporter&lt;/code&gt; instance is initialized. &lt;/p&gt;
&lt;p&gt;It is possible for a &lt;code&gt;DataManager&lt;/code&gt; instance to manage its data without ever importing data from a file, so there is no need to create a new &lt;code&gt;DataImporter&lt;/code&gt; instance when the &lt;code&gt;DataManager&lt;/code&gt; itself is created. Instead, it makes more sense to create the &lt;code&gt;DataImporter&lt;/code&gt; instance if and when it is first used. &lt;/p&gt;
&lt;p&gt;Because it is marked with the &lt;code&gt;lazy&lt;/code&gt; modifier, the &lt;code&gt;DataImporter&lt;/code&gt; instance for the &lt;code&gt;importer&lt;/code&gt; property is only created when the &lt;code&gt;importer&lt;/code&gt; property is first accessed, such as when its &lt;code&gt;fileName&lt;/code&gt; property is queried: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println(manager.importer.fileName)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the DataImporter instance for the importer property has now been created&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "data.txt"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Stored Properties and Instance Variables&lt;/h4&gt;
&lt;p&gt;If you have experience with Objective-C, you may know that it provides &lt;em&gt;two&lt;/em&gt; ways to store values and references as part of a class instance. In addition to properties, you can use instance variables as a backing store for the values stored in a property. &lt;/p&gt;
&lt;p&gt;Swift unifies these concepts into a single property declaration. A Swift property does not have a corresponding instance variable, and the backing store for a property is not accessed directly. This approach avoids confusion about how the value is accessed in different contexts and simplifies the property’s declaration into a single, definitive statement. All information about the property—including its name, type, and memory management characteristics—is defined in a single location as part of the type’s definition. &lt;/p&gt;
&lt;h3&gt;Computed Properties&lt;/h3&gt;
&lt;p&gt;In addition to stored properties, classes, structures, and enumerations can define &lt;em&gt;computed properties&lt;/em&gt;, which do not actually store a value. Instead, they provide a getter and an optional setter to retrieve and set other properties and values indirectly. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct Size {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var width = 0.0, height = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct Rect {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var origin = Point()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var size = Size()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var center: Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;get {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let centerX = origin.x \+ (size.width / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let centerY = origin.y \+ (size.height / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Point(x: centerX, y: centerY)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;set(newCenter) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;origin.x = newCenter.x \- (size.width / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;origin.y = newCenter.y \- (size.height / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var square = Rect(origin: Point(x: 0.0, y: 0.0),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;size: Size(width: 10.0, height: 10.0))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let initialSquareCenter = square.center&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square.center = Point(x: 15.0, y: 15.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("square.origin is now at (\\(square.origin.x), \\(square.origin.y))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "square.origin is now at (10.0, 10.0)"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines three structures for working with geometric shapes: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;Point&lt;/code&gt; encapsulates an &lt;code&gt;(x, y)&lt;/code&gt; coordinate. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;Size&lt;/code&gt; encapsulates a &lt;code&gt;width&lt;/code&gt; and a &lt;code&gt;height&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;Rect&lt;/code&gt; defines a rectangle by an origin point and a size. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Rect&lt;/code&gt; structure also provides a computed property called &lt;code&gt;center&lt;/code&gt;. The current center position of a &lt;code&gt;Rect&lt;/code&gt; can always be determined from its &lt;code&gt;origin&lt;/code&gt; and &lt;code&gt;size&lt;/code&gt;, and so you don’t need to store the center point as an explicit &lt;code&gt;Point&lt;/code&gt; value. Instead, &lt;code&gt;Rect&lt;/code&gt; defines a custom getter and setter for a computed variable called &lt;code&gt;center&lt;/code&gt;, to enable you to work with the rectangle’s &lt;code&gt;center&lt;/code&gt; as if it were a real stored property. &lt;/p&gt;
&lt;p&gt;The preceding example creates a new &lt;code&gt;Rect&lt;/code&gt; variable called &lt;code&gt;square&lt;/code&gt;. The &lt;code&gt;square&lt;/code&gt; variable is initialized with an origin point of &lt;code&gt;(0, 0)&lt;/code&gt;, and a width and height of &lt;code&gt;10&lt;/code&gt;. This square is represented by the blue square in the diagram below. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;square&lt;/code&gt; variable’s &lt;code&gt;center&lt;/code&gt; property is then accessed through dot syntax (&lt;code&gt;square.center&lt;/code&gt;), which causes the getter for &lt;code&gt;center&lt;/code&gt; to be called, to retrieve the current property value. Rather than returning an existing value, the getter actually calculates and returns a new &lt;code&gt;Point&lt;/code&gt; to represent the center of the square. As can be seen above, the getter correctly returns a center point of &lt;code&gt;(5, 5)&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;center&lt;/code&gt; property is then set to a new value of &lt;code&gt;(15, 15)&lt;/code&gt;, which moves the square up and to the right, to the new position shown by the orange square in the diagram below. Setting the &lt;code&gt;center&lt;/code&gt; property calls the setter for &lt;code&gt;center&lt;/code&gt;, which modifies the &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; values of the stored &lt;code&gt;origin&lt;/code&gt; property, and moves the square to its new position. &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/computedProperties_2x.png" src="http://muxuezi.github.io/posts/Art/computedProperties_2x.png"&gt;&lt;/p&gt;
&lt;h4&gt;Shorthand Setter Declaration&lt;/h4&gt;
&lt;p&gt;If a computed property’s setter does not define a name for the new value to be set, a default name of &lt;code&gt;newValue&lt;/code&gt; is used. Here’s an alternative version of the &lt;code&gt;Rect&lt;/code&gt; structure, which takes advantage of this shorthand notation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct AlternativeRect {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var origin = Point()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var size = Size()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var center: Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;get {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let centerX = origin.x \+ (size.width / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let centerY = origin.y \+ (size.height / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Point(x: centerX, y: centerY)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;set {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;origin.x = newValue.x \- (size.width / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;origin.y = newValue.y \- (size.height / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Read-Only Computed Properties&lt;/h4&gt;
&lt;p&gt;A computed property with a getter but no setter is known as a &lt;em&gt;read-only computed property&lt;/em&gt;. A read-only computed property always returns a value, and can be accessed through dot syntax, but cannot be set to a different value. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You must declare computed properties—including read-only computed properties—as variable properties with the &lt;code&gt;var&lt;/code&gt; keyword, because their value is not fixed. The &lt;code&gt;let&lt;/code&gt; keyword is only used for constant properties, to indicate that their values cannot be changed once they are set as part of instance initialization. &lt;/p&gt;
&lt;p&gt;You can simplify the declaration of a read-only computed property by removing the &lt;code&gt;get&lt;/code&gt; keyword and its braces: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Cuboid {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var width = 0.0, height = 0.0, depth = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var volume: Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return width * height * depth&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let fourByFiveByTwo = Cuboid(width: 4.0, height: 5.0, depth: 2.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("the volume of fourByFiveByTwo is \\(fourByFiveByTwo.volume)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "the volume of fourByFiveByTwo is 40.0"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a new structure called &lt;code&gt;Cuboid&lt;/code&gt;, which represents a 3D rectangular box with &lt;code&gt;width&lt;/code&gt;, &lt;code&gt;height&lt;/code&gt;, and &lt;code&gt;depth&lt;/code&gt; properties. This structure also has a read-only computed property called &lt;code&gt;volume&lt;/code&gt;, which calculates and returns the current volume of the cuboid. It doesn’t make sense for &lt;code&gt;volume&lt;/code&gt; to be settable, because it would be ambiguous as to which values of &lt;code&gt;width&lt;/code&gt;, &lt;code&gt;height&lt;/code&gt;, and &lt;code&gt;depth&lt;/code&gt; should be used for a particular &lt;code&gt;volume&lt;/code&gt; value. Nonetheless, it is useful for a &lt;code&gt;Cuboid&lt;/code&gt; to provide a read-only computed property to enable external users to discover its current calculated volume. &lt;/p&gt;
&lt;h3&gt;Property Observers&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Property observers&lt;/em&gt; observe and respond to changes in a property’s value. Property observers are called every time a property’s value is set, even if the new value is the same as the property’s current value. &lt;/p&gt;
&lt;p&gt;You can add property observers to any stored properties you define, apart from lazy stored properties. You can also add property observers to any inherited property (whether stored or computed) by overriding the property within a subclass. Property overriding is described in Overriding. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You don’t need to define property observers for non-overridden computed properties, because you can observe and respond to changes to their value from directly within the computed property’s setter. &lt;/p&gt;
&lt;p&gt;You have the option to define either or both of these observers on a property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;willSet&lt;/code&gt; is called just before the value is stored. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;didSet&lt;/code&gt; is called immediately after the new value is stored. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you implement a &lt;code&gt;willSet&lt;/code&gt; observer, it is passed the new property value as a constant parameter. You can specify a name for this parameter as part of your &lt;code&gt;willSet&lt;/code&gt; implementation. If you choose not to write the parameter name and parentheses within your implementation, the parameter will still be made available with a default parameter name of &lt;code&gt;newValue&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Similarly, if you implement a &lt;code&gt;didSet&lt;/code&gt; observer, it will be passed a constant parameter containing the old property value. You can name the parameter if you wish, or use the default parameter name of &lt;code&gt;oldValue&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;&lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; observers are not called when a property is set in an initializer before delegation takes place. &lt;/p&gt;
&lt;p&gt;For more information about initializer delegation, see Initializer Delegation for Value Types and Initializer Delegation for Class Types. &lt;/p&gt;
&lt;p&gt;Here’s an example of &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; in action. The example below defines a new class called &lt;code&gt;StepCounter&lt;/code&gt;, which tracks the total number of steps that a person takes while walking. This class might be used with input data from a pedometer or other step counter to keep track of a person’s exercise during their daily routine. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class StepCounter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var totalSteps: Int = 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;willSet(newTotalSteps) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("About to set totalSteps to \\(newTotalSteps)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;didSet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if totalSteps &amp;gt; oldValue {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Added \\(totalSteps \- oldValue) steps")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let stepCounter = StepCounter()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stepCounter.totalSteps = 200&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// About to set totalSteps to 200&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Added 200 steps&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stepCounter.totalSteps = 360&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// About to set totalSteps to 360&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Added 160 steps&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stepCounter.totalSteps = 896&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// About to set totalSteps to 896&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Added 536 steps&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;StepCounter&lt;/code&gt; class declares a &lt;code&gt;totalSteps&lt;/code&gt; property of type &lt;code&gt;Int&lt;/code&gt;. This is a stored property with &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; observers. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; observers for &lt;code&gt;totalSteps&lt;/code&gt; are called whenever the property is assigned a new value. This is true even if the new value is the same as the current value. &lt;/p&gt;
&lt;p&gt;This example’s &lt;code&gt;willSet&lt;/code&gt; observer uses a custom parameter name of &lt;code&gt;newTotalSteps&lt;/code&gt; for the upcoming new value. In this example, it simply prints out the value that is about to be set. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;didSet&lt;/code&gt; observer is called after the value of &lt;code&gt;totalSteps&lt;/code&gt; is updated. It compares the new value of &lt;code&gt;totalSteps&lt;/code&gt; against the old value. If the total number of steps has increased, a message is printed to indicate how many new steps have been taken. The &lt;code&gt;didSet&lt;/code&gt; observer does not provide a custom parameter name for the old value, and the default name of &lt;code&gt;oldValue&lt;/code&gt; is used instead. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you assign a value to a property within its own &lt;code&gt;didSet&lt;/code&gt; observer, the new value that you assign will replace the one that was just set. &lt;/p&gt;
&lt;h3&gt;Global and Local Variables&lt;/h3&gt;
&lt;p&gt;The capabilities described above for computing and observing properties are also available to &lt;em&gt;global variables&lt;/em&gt; and &lt;em&gt;local variables&lt;/em&gt;. Global variables are variables that are defined outside of any function, method, closure, or type context. Local variables are variables that are defined within a function, method, or closure context. &lt;/p&gt;
&lt;p&gt;The global and local variables you have encountered in previous chapters have all been &lt;em&gt;stored variables&lt;/em&gt;. Stored variables, like stored properties, provide storage for a value of a certain type and allow that value to be set and retrieved. &lt;/p&gt;
&lt;p&gt;However, you can also define &lt;em&gt;computed variables&lt;/em&gt; and define observers for stored variables, in either a global or local scope. Computed variables calculate rather than store a value, and are written in the same way as computed properties. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Global constants and variables are always computed lazily, in a similar manner to Lazy Stored Properties. Unlike lazy stored properties, global constants and variables do not need to be marked with the &lt;code&gt;lazy&lt;/code&gt; modifier. &lt;/p&gt;
&lt;p&gt;Local constants and variables are never computed lazily. &lt;/p&gt;
&lt;h3&gt;Type Properties&lt;/h3&gt;
&lt;p&gt;Instance properties are properties that belong to an instance of a particular type. Every time you create a new instance of that type, it has its own set of property values, separate from any other instance. &lt;/p&gt;
&lt;p&gt;You can also define properties that belong to the type itself, not to any one instance of that type. There will only ever be one copy of these properties, no matter how many instances of that type you create. These kinds of properties are called &lt;em&gt;type properties&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;Type properties are useful for defining values that are universal to &lt;em&gt;all&lt;/em&gt; instances of a particular type, such as a constant property that all instances can use (like a static constant in C), or a variable property that stores a value that is global to all instances of that type (like a static variable in C). &lt;/p&gt;
&lt;p&gt;For value types (that is, structures and enumerations), you can define stored and computed type properties. For classes, you can define computed type properties only. &lt;/p&gt;
&lt;p&gt;Stored type properties for value types can be variables or constants. Computed type properties are always declared as variable properties, in the same way as computed instance properties. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Unlike stored instance properties, you must always give stored type properties a default value. This is because the type itself does not have an initializer that can assign a value to a stored type property at initialization time. &lt;/p&gt;
&lt;h4&gt;Type Property Syntax&lt;/h4&gt;
&lt;p&gt;In C and Objective-C, you define static constants and variables associated with a type as &lt;em&gt;global&lt;/em&gt; static variables. In Swift, however, type properties are written as part of the type’s definition, within the type’s outer curly braces, and each type property is explicitly scoped to the type it supports. &lt;/p&gt;
&lt;p&gt;You define type properties for value types with the &lt;code&gt;static&lt;/code&gt; keyword, and type properties for class types with the &lt;code&gt;class&lt;/code&gt; keyword. The example below shows the syntax for stored and computed type properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct SomeStructure {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static var storedTypeProperty = "Some value."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static var computedTypeProperty: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// return an Int value here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;enum SomeEnumeration {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static var storedTypeProperty = "Some value."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static var computedTypeProperty: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// return an Int value here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class var computedTypeProperty: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// return an Int value here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The computed type property examples above are for read-only computed type properties, but you can also define read-write computed type properties with the same syntax as for computed instance properties. &lt;/p&gt;
&lt;h4&gt;Querying and Setting Type Properties&lt;/h4&gt;
&lt;p&gt;Type properties are queried and set with dot syntax, just like instance properties. However, type properties are queried and set on the &lt;em&gt;type&lt;/em&gt;, not on an instance of that type. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println(SomeClass.computedTypeProperty)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "42"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(SomeStructure.storedTypeProperty)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Some value."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SomeStructure.storedTypeProperty = "Another value."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(SomeStructure.storedTypeProperty)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Another value."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The examples that follow use two stored type properties as part of a structure that models an audio level meter for a number of audio channels. Each channel has an integer audio level between &lt;code&gt;0&lt;/code&gt; and &lt;code&gt;10&lt;/code&gt; inclusive. &lt;/p&gt;
&lt;p&gt;The figure below illustrates how two of these audio channels can be combined to model a stereo audio level meter. When a channel’s audio level is &lt;code&gt;0&lt;/code&gt;, none of the lights for that channel are lit. When the audio level is &lt;code&gt;10&lt;/code&gt;, all of the lights for that channel are lit. In this figure, the left channel has a current level of &lt;code&gt;9&lt;/code&gt;, and the right channel has a current level of &lt;code&gt;7&lt;/code&gt;: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/staticPropertiesVUMeter_2x.png" src="http://muxuezi.github.io/posts/Art/staticPropertiesVUMeter_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The audio channels described above are represented by instances of the &lt;code&gt;AudioChannel&lt;/code&gt; structure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct AudioChannel {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static let thresholdLevel = 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static var maxInputLevelForAllChannels = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentLevel: Int = 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;didSet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if currentLevel &amp;gt; AudioChannel.thresholdLevel {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// cap the new audio level to the threshold level&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentLevel = AudioChannel.thresholdLevel&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if currentLevel &amp;gt; AudioChannel.maxInputLevelForAllChannels {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// store this as the new overall maximum input level&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;AudioChannel.maxInputLevelForAllChannels = currentLevel&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;AudioChannel&lt;/code&gt; structure defines two stored type properties to support its functionality. The first, &lt;code&gt;thresholdLevel&lt;/code&gt;, defines the maximum threshold value an audio level can take. This is a constant value of &lt;code&gt;10&lt;/code&gt; for all &lt;code&gt;AudioChannel&lt;/code&gt; instances. If an audio signal comes in with a higher value than &lt;code&gt;10&lt;/code&gt;, it will be capped to this threshold value (as described below). &lt;/p&gt;
&lt;p&gt;The second type property is a variable stored property called &lt;code&gt;maxInputLevelForAllChannels&lt;/code&gt;. This keeps track of the maximum input value that has been received by &lt;em&gt;any&lt;/em&gt; &lt;code&gt;AudioChannel&lt;/code&gt; instance. It starts with an initial value of &lt;code&gt;0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;AudioChannel&lt;/code&gt; structure also defines a stored instance property called &lt;code&gt;currentLevel&lt;/code&gt;, which represents the channel’s current audio level on a scale of &lt;code&gt;0&lt;/code&gt; to &lt;code&gt;10&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;currentLevel&lt;/code&gt; property has a &lt;code&gt;didSet&lt;/code&gt; property observer to check the value of &lt;code&gt;currentLevel&lt;/code&gt; whenever it is set. This observer performs two checks: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If the new value of &lt;code&gt;currentLevel&lt;/code&gt; is greater than the allowed &lt;code&gt;thresholdLevel&lt;/code&gt;, the property observer caps &lt;code&gt;currentLevel&lt;/code&gt; to &lt;code&gt;thresholdLevel&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If the new value of &lt;code&gt;currentLevel&lt;/code&gt; (after any capping) is higher than any value previously received by &lt;em&gt;any&lt;/em&gt; &lt;code&gt;AudioChannel&lt;/code&gt; instance, the property observer stores the new &lt;code&gt;currentLevel&lt;/code&gt; value in the &lt;code&gt;maxInputLevelForAllChannels&lt;/code&gt; static property. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In the first of these two checks, the &lt;code&gt;didSet&lt;/code&gt; observer sets &lt;code&gt;currentLevel&lt;/code&gt; to a different value. This does not, however, cause the observer to be called again. &lt;/p&gt;
&lt;p&gt;You can use the &lt;code&gt;AudioChannel&lt;/code&gt; structure to create two new audio channels called &lt;code&gt;leftChannel&lt;/code&gt; and &lt;code&gt;rightChannel&lt;/code&gt;, to represent the audio levels of a stereo sound system: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var leftChannel = AudioChannel()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var rightChannel = AudioChannel()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you set the &lt;code&gt;currentLevel&lt;/code&gt; of the &lt;em&gt;left&lt;/em&gt; channel to &lt;code&gt;7&lt;/code&gt;, you can see that the &lt;code&gt;maxInputLevelForAllChannels&lt;/code&gt; type property is updated to equal &lt;code&gt;7&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;leftChannel.currentLevel = 7&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(leftChannel.currentLevel)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "7"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(AudioChannel.maxInputLevelForAllChannels)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "7"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you try to set the &lt;code&gt;currentLevel&lt;/code&gt; of the &lt;em&gt;right&lt;/em&gt; channel to &lt;code&gt;11&lt;/code&gt;, you can see that the right channel’s &lt;code&gt;currentLevel&lt;/code&gt; property is capped to the maximum value of &lt;code&gt;10&lt;/code&gt;, and the &lt;code&gt;maxInputLevelForAllChannels&lt;/code&gt; type property is updated to equal &lt;code&gt;10&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;rightChannel.currentLevel = 11&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(rightChannel.currentLevel)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "10"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(AudioChannel.maxInputLevelForAllChannels)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "10"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Methods&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Methods&lt;/em&gt; are functions that are associated with a particular type. Classes, structures, and enumerations can all define instance methods, which encapsulate specific tasks and functionality for working with an instance of a given type. Classes, structures, and enumerations can also define type methods, which are associated with the type itself. Type methods are similar to class methods in Objective-C. &lt;/p&gt;
&lt;p&gt;The fact that structures and enumerations can define methods in Swift is a major difference from C and Objective-C. In Objective-C, classes are the only types that can define methods. In Swift, you can choose whether to define a class, structure, or enumeration, and still have the flexibility to define methods on the type you create. &lt;/p&gt;
&lt;h3&gt;Instance Methods&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Instance methods&lt;/em&gt; are functions that belong to instances of a particular class, structure, or enumeration. They support the functionality of those instances, either by providing ways to access and modify instance properties, or by providing functionality related to the instance’s purpose. Instance methods have exactly the same syntax as functions, as described in Functions. &lt;/p&gt;
&lt;p&gt;You write an instance method within the opening and closing braces of the type it belongs to. An instance method has implicit access to all other instance methods and properties of that type. An instance method can be called only on a specific instance of the type it belongs to. It cannot be called in isolation without an existing instance. &lt;/p&gt;
&lt;p&gt;Here’s an example that defines a simple &lt;code&gt;Counter&lt;/code&gt; class, which can be used to count the number of times an action occurs: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Counter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var count = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func increment() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count++&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func incrementBy(amount: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count += amount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func reset() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Counter&lt;/code&gt; class defines three instance methods: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;increment&lt;/code&gt; increments the counter by &lt;code&gt;1&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;incrementBy(amount: Int)&lt;/code&gt; increments the counter by a specified integer amount. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;reset&lt;/code&gt; resets the counter to zero. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Counter&lt;/code&gt; class also declares a variable property, &lt;code&gt;count&lt;/code&gt;, to keep track of the current counter value. &lt;/p&gt;
&lt;p&gt;You call instance methods with the same dot syntax as properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let counter = Counter()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the initial counter value is 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.increment()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the counter's value is now 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.incrementBy(5)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the counter's value is now 6&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.reset()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the counter's value is now 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Local and External Parameter Names for Methods&lt;/h4&gt;
&lt;p&gt;Function parameters can have both a local name (for use within the function’s body) and an external name (for use when calling the function), as described in External Parameter Names. The same is true for method parameters, because methods are just functions that are associated with a type. However, the default behavior of local names and external names is different for functions and methods. &lt;/p&gt;
&lt;p&gt;Methods in Swift are very similar to their counterparts in Objective-C. As in Objective-C, the name of a method in Swift typically refers to the method’s first parameter using a preposition such as &lt;code&gt;with&lt;/code&gt;, &lt;code&gt;for&lt;/code&gt;, or &lt;code&gt;by&lt;/code&gt;, as seen in the &lt;code&gt;incrementBy&lt;/code&gt; method from the preceding &lt;code&gt;Counter&lt;/code&gt; class example. The use of a preposition enables the method to be read as a sentence when it is called. Swift makes this established method naming convention easy to write by using a different default approach for method parameters than it uses for function parameters. &lt;/p&gt;
&lt;p&gt;Specifically, Swift gives the &lt;em&gt;first&lt;/em&gt; parameter name in a method a local parameter name by default, and gives the second and subsequent parameter names both local &lt;em&gt;and&lt;/em&gt; external parameter names by default. This convention matches the typical naming and calling convention you will be familiar with from writing Objective-C methods, and makes for expressive method calls without the need to qualify your parameter names. &lt;/p&gt;
&lt;p&gt;Consider this alternative version of the &lt;code&gt;Counter&lt;/code&gt; class, which defines a more complex form of the &lt;code&gt;incrementBy&lt;/code&gt; method: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Counter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var count: Int = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func incrementBy(amount: Int, numberOfTimes: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count += amount * numberOfTimes&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This &lt;code&gt;incrementBy&lt;/code&gt; method has two parameters—&lt;code&gt;amount&lt;/code&gt; and &lt;code&gt;numberOfTimes&lt;/code&gt;. By default, Swift treats &lt;code&gt;amount&lt;/code&gt; as a local name only, but treats &lt;code&gt;numberOfTimes&lt;/code&gt; as both a local &lt;em&gt;and&lt;/em&gt; an external name. You call the method as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let counter = Counter()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.incrementBy(5, numberOfTimes: 3)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// counter value is now 15&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You don’t need to define an external parameter name for the first argument value, because its purpose is clear from the function name &lt;code&gt;incrementBy&lt;/code&gt;. The second argument, however, is qualified by an external parameter name to make its purpose clear when the method is called. &lt;/p&gt;
&lt;p&gt;This default behavior effectively treats the method as if you had written a hash symbol (&lt;code&gt;#&lt;/code&gt;) before the &lt;code&gt;numberOfTimes&lt;/code&gt; parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func incrementBy(amount: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count += amount * numberOfTimes&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The default behavior described above means that method definitions in Swift are written with the same grammatical style as Objective-C, and are called in a natural, expressive way. &lt;/p&gt;
&lt;h4&gt;Modifying External Parameter Name Behavior for Methods&lt;/h4&gt;
&lt;p&gt;Sometimes it’s useful to provide an external parameter name for a method’s first parameter, even though this is not the default behavior. You can either add an explicit external name yourself, or you can prefix the first parameter’s name with a hash symbol to use the local name as an external name too. &lt;/p&gt;
&lt;p&gt;Conversely, if you do not want to provide an external name for the second or subsequent parameter of a method, override the default behavior by using an underscore character (&lt;code&gt;_&lt;/code&gt;) as an explicit external parameter name for that parameter. &lt;/p&gt;
&lt;h4&gt;The self Property&lt;/h4&gt;
&lt;p&gt;Every instance of a type has an implicit property called &lt;code&gt;self&lt;/code&gt;, which is exactly equivalent to the instance itself. You use the &lt;code&gt;self&lt;/code&gt; property to refer to the current instance within its own instance methods. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;increment&lt;/code&gt; method in the example above could have been written like this: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func increment() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.count++&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In practice, you don’t need to write &lt;code&gt;self&lt;/code&gt; in your code very often. If you don’t explicitly write &lt;code&gt;self&lt;/code&gt;, Swift assumes that you are referring to a property or method of the current instance whenever you use a known property or method name within a method. This assumption is demonstrated by the use of &lt;code&gt;count&lt;/code&gt; (rather than &lt;code&gt;self.count&lt;/code&gt;) inside the three instance methods for &lt;code&gt;Counter&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The main exception to this rule occurs when a parameter name for an instance method has the same name as a property of that instance. In this situation, the parameter name takes precedence, and it becomes necessary to refer to the property in a more qualified way. You use the &lt;code&gt;self&lt;/code&gt; property to distinguish between the parameter name and the property name. &lt;/p&gt;
&lt;p&gt;Here, &lt;code&gt;self&lt;/code&gt; disambiguates between a method parameter called &lt;code&gt;x&lt;/code&gt; and an instance property that is also called &lt;code&gt;x&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func isToTheRightOfX(x: Double) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return self.x &amp;gt; x&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let somePoint = Point(x: 4.0, y: 5.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if somePoint.isToTheRightOfX(1.0) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("This point is to the right of the line where x == 1.0")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "This point is to the right of the line where x == 1.0"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Without the &lt;code&gt;self&lt;/code&gt; prefix, Swift would assume that both uses of &lt;code&gt;x&lt;/code&gt; referred to the method parameter called &lt;code&gt;x&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Modifying Value Types from Within Instance Methods&lt;/h4&gt;
&lt;p&gt;Structures and enumerations are &lt;em&gt;value types&lt;/em&gt;. By default, the properties of a value type cannot be modified from within its instance methods. &lt;/p&gt;
&lt;p&gt;However, if you need to modify the properties of your structure or enumeration within a particular method, you can opt in to &lt;em&gt;mutating&lt;/em&gt; behavior for that method. The method can then mutate (that is, change) its properties from within the method, and any changes that it makes are written back to the original structure when the method ends. The method can also assign a completely new instance to its implicit &lt;code&gt;self&lt;/code&gt; property, and this new instance will replace the existing one when the method ends. &lt;/p&gt;
&lt;p&gt;You can opt in to this behavior by placing the &lt;code&gt;mutating&lt;/code&gt; keyword before the &lt;code&gt;func&lt;/code&gt; keyword for that method: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func moveByX(deltaX: Double, y deltaY: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;x += deltaX&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;y += deltaY&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var somePoint = Point(x: 1.0, y: 1.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;somePoint.moveByX(2.0, y: 3.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The point is now at (\\(somePoint.x), \\(somePoint.y))")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The point is now at (3.0, 4.0)"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Point&lt;/code&gt; structure above defines a mutating &lt;code&gt;moveByX&lt;/code&gt; method, which moves a &lt;code&gt;Point&lt;/code&gt; instance by a certain amount. Instead of returning a new point, this method actually modifies the point on which it is called. The &lt;code&gt;mutating&lt;/code&gt; keyword is added to its definition to enable it to modify its properties. &lt;/p&gt;
&lt;p&gt;Note that you cannot call a mutating method on a constant of structure type, because its properties cannot be changed, even if they are variable properties, as described in Stored Properties of Constant Structure Instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let fixedPoint = Point(x: 3.0, y: 3.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;fixedPoint.moveByX(2.0, y: 3.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this will report an error&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Assigning to self Within a Mutating Method&lt;/h4&gt;
&lt;p&gt;Mutating methods can assign an entirely new instance to the implicit &lt;code&gt;self&lt;/code&gt; property. The &lt;code&gt;Point&lt;/code&gt; example shown above could have been written in the following way instead: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func moveByX(deltaX: Double, y deltaY: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = Point(x: x \+ deltaX, y: y \+ deltaY)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This version of the mutating &lt;code&gt;moveByX&lt;/code&gt; method creates a brand new structure whose &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; values are set to the target location. The end result of calling this alternative version of the method will be exactly the same as for calling the earlier version. &lt;/p&gt;
&lt;p&gt;Mutating methods for enumerations can set the implicit &lt;code&gt;self&lt;/code&gt; parameter to be a different member from the same enumeration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum TriStateSwitch {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Off, Low, High&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func next() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch self {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Off:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = Low&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Low:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = High&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case High:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = Off&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var ovenLight = TriStateSwitch.Low&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ovenLight.next()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// ovenLight is now equal to .High&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ovenLight.next()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// ovenLight is now equal to .Off&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines an enumeration for a three-state switch. The switch cycles between three different power states (&lt;code&gt;Off&lt;/code&gt;, &lt;code&gt;Low&lt;/code&gt; and &lt;code&gt;High&lt;/code&gt;) every time its &lt;code&gt;next&lt;/code&gt; method is called. &lt;/p&gt;
&lt;h3&gt;Type Methods&lt;/h3&gt;
&lt;p&gt;Instance methods, as described above, are methods that are called on an instance of a particular type. You can also define methods that are called on the type itself. These kinds of methods are called &lt;em&gt;type methods&lt;/em&gt;. You indicate type methods for classes by writing the keyword &lt;code&gt;class&lt;/code&gt; before the method’s &lt;code&gt;func&lt;/code&gt; keyword, and type methods for structures and enumerations by writing the keyword &lt;code&gt;static&lt;/code&gt; before the method’s &lt;code&gt;func&lt;/code&gt; keyword. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In Objective-C, you can define type-level methods only for Objective-C classes. In Swift, you can define type-level methods for all classes, structures, and enumerations. Each type method is explicitly scoped to the type it supports. &lt;/p&gt;
&lt;p&gt;Type methods are called with dot syntax, like instance methods. However, you call type methods on the type, not on an instance of that type. Here’s how you call a type method on a class called &lt;code&gt;SomeClass&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class func someTypeMethod() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// type method implementation goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SomeClass.someTypeMethod()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Within the body of a type method, the implicit &lt;code&gt;self&lt;/code&gt; property refers to the type itself, rather than an instance of that type. For structures and enumerations, this means that you can use &lt;code&gt;self&lt;/code&gt; to disambiguate between static properties and static method parameters, just as you do for instance properties and instance method parameters. &lt;/p&gt;
&lt;p&gt;More generally, any unqualified method and property names that you use within the body of a type method will refer to other type-level methods and properties. A type method can call another type method with the other method’s name, without needing to prefix it with the type name. Similarly, type methods on structures and enumerations can access static properties by using the static property’s name without a type name prefix. &lt;/p&gt;
&lt;p&gt;The example below defines a structure called &lt;code&gt;LevelTracker&lt;/code&gt;, which tracks a player’s progress through the different levels or stages of a game. It is a single-player game, but can store information for multiple players on a single device. &lt;/p&gt;
&lt;p&gt;All of the game’s levels (apart from level one) are locked when the game is first played. Every time a player finishes a level, that level is unlocked for all players on the device. The &lt;code&gt;LevelTracker&lt;/code&gt; structure uses static properties and methods to keep track of which levels of the game have been unlocked. It also tracks the current level for an individual player. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct LevelTracker {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static var highestUnlockedLevel = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static func unlockLevel(level: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if level &amp;gt; highestUnlockedLevel { highestUnlockedLevel = level }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static func levelIsUnlocked(level: Int) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return level &amp;lt;= highestUnlockedLevel&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentLevel = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func advanceToLevel(level: Int) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if LevelTracker.levelIsUnlocked(level) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;currentLevel = level&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;LevelTracker&lt;/code&gt; structure keeps track of the highest level that any player has unlocked. This value is stored in a static property called &lt;code&gt;highestUnlockedLevel&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;LevelTracker&lt;/code&gt; also defines two type functions to work with the &lt;code&gt;highestUnlockedLevel&lt;/code&gt; property. The first is a type function called &lt;code&gt;unlockLevel&lt;/code&gt;, which updates the value of &lt;code&gt;highestUnlockedLevel&lt;/code&gt; whenever a new level is unlocked. The second is a convenience type function called &lt;code&gt;levelIsUnlocked&lt;/code&gt;, which returns &lt;code&gt;true&lt;/code&gt; if a particular level number is already unlocked. (Note that these type methods can access the &lt;code&gt;highestUnlockedLevel&lt;/code&gt; static property without your needing to write it as &lt;code&gt;LevelTracker.highestUnlockedLevel&lt;/code&gt;.) &lt;/p&gt;
&lt;p&gt;In addition to its static property and type methods, &lt;code&gt;LevelTracker&lt;/code&gt; tracks an individual player’s progress through the game. It uses an instance property called &lt;code&gt;currentLevel&lt;/code&gt; to track the level that a player is currently playing. &lt;/p&gt;
&lt;p&gt;To help manage the &lt;code&gt;currentLevel&lt;/code&gt; property, &lt;code&gt;LevelTracker&lt;/code&gt; defines an instance method called &lt;code&gt;advanceToLevel&lt;/code&gt;. Before updating &lt;code&gt;currentLevel&lt;/code&gt;, this method checks whether the requested new level is already unlocked. The &lt;code&gt;advanceToLevel&lt;/code&gt; method returns a Boolean value to indicate whether or not it was actually able to set &lt;code&gt;currentLevel&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;LevelTracker&lt;/code&gt; structure is used with the &lt;code&gt;Player&lt;/code&gt; class, shown below, to track and update the progress of an individual player: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Player {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var tracker = LevelTracker()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let playerName: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func completedLevel(level: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;LevelTracker.unlockLevel(level \+ 1)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tracker.advanceToLevel(level \+ 1)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;playerName = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Player&lt;/code&gt; class creates a new instance of &lt;code&gt;LevelTracker&lt;/code&gt; to track that player’s progress. It also provides a method called &lt;code&gt;completedLevel&lt;/code&gt;, which is called whenever a player completes a particular level. This method unlocks the next level for all players and updates the player’s progress to move them to the next level. (The Boolean return value of &lt;code&gt;advanceToLevel&lt;/code&gt; is ignored, because the level is known to have been unlocked by the call to &lt;code&gt;LevelTracker.unlockLevel&lt;/code&gt; on the previous line.) &lt;/p&gt;
&lt;p&gt;You can create an instance of the &lt;code&gt;Player&lt;/code&gt; class for a new player, and see what happens when the player completes level one: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var player = Player(name: "Argyrios")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;player.completedLevel(1)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("highest unlocked level is now \\(LevelTracker.highestUnlockedLevel)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "highest unlocked level is now 2"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you create a second player, whom you try to move to a level that is not yet unlocked by any player in the game, the attempt to set the player’s current level fails: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;player = Player(name: "Beto")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if player.tracker.advanceToLevel(6) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("player is now on level 6")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("level 6 has not yet been unlocked")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "level 6 has not yet been unlocked"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Subscripts&lt;/h2&gt;
&lt;p&gt;Classes, structures, and enumerations can define &lt;em&gt;subscripts&lt;/em&gt;, which are shortcuts for accessing the member elements of a collection, list, or sequence. You use subscripts to set and retrieve values by index without needing separate methods for setting and retrieval. For example, you access elements in an &lt;code&gt;Array&lt;/code&gt; instance as &lt;code&gt;someArray[index]&lt;/code&gt; and elements in a &lt;code&gt;Dictionary&lt;/code&gt; instance as &lt;code&gt;someDictionary[key]&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can define multiple subscripts for a single type, and the appropriate subscript overload to use is selected based on the type of index value you pass to the subscript. Subscripts are not limited to a single dimension, and you can define subscripts with multiple input parameters to suit your custom type’s needs. &lt;/p&gt;
&lt;h3&gt;Subscript Syntax&lt;/h3&gt;
&lt;p&gt;Subscripts enable you to query instances of a type by writing one or more values in square brackets after the instance name. Their syntax is similar to both instance method syntax and computed property syntax. You write subscript definitions with the &lt;code&gt;subscript&lt;/code&gt; keyword, and specify one or more input parameters and a return type, in the same way as instance methods. Unlike instance methods, subscripts can be read-write or read-only. This behavior is communicated by a getter and setter in the same way as for computed properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;subscript(index: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;get {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// return an appropriate subscript value here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;set(newValue) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// perform a suitable setting action here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The type of &lt;code&gt;newValue&lt;/code&gt; is the same as the return value of the subscript. As with computed properties, you can choose not to specify the setter’s &lt;code&gt;(newValue)&lt;/code&gt; parameter. A default parameter called &lt;code&gt;newValue&lt;/code&gt; is provided to your setter if you do not provide one yourself. &lt;/p&gt;
&lt;p&gt;As with read-only computed properties, you can drop the &lt;code&gt;get&lt;/code&gt; keyword for read-only subscripts: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;subscript(index: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// return an appropriate subscript value here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s an example of a read-only subscript implementation, which defines a &lt;code&gt;TimesTable&lt;/code&gt; structure to represent an &lt;em&gt;n&lt;/em&gt;-times-table of integers: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct TimesTable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let multiplier: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;subscript(index: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return multiplier * index&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let threeTimesTable = TimesTable(multiplier: 3)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("six times three is \\(threeTimesTable[6])")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "six times three is 18"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this example, a new instance of &lt;code&gt;TimesTable&lt;/code&gt; is created to represent the three-times-table. This is indicated by passing a value of &lt;code&gt;3&lt;/code&gt; to the structure’s &lt;code&gt;initializer&lt;/code&gt; as the value to use for the instance’s &lt;code&gt;multiplier&lt;/code&gt; parameter. &lt;/p&gt;
&lt;p&gt;You can query the &lt;code&gt;threeTimesTable&lt;/code&gt; instance by calling its subscript, as shown in the call to &lt;code&gt;threeTimesTable[6]&lt;/code&gt;. This requests the sixth entry in the three-times-table, which returns a value of &lt;code&gt;18&lt;/code&gt;, or &lt;code&gt;3&lt;/code&gt; times &lt;code&gt;6&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;An &lt;em&gt;n&lt;/em&gt;-times-table is based on a fixed mathematical rule. It is not appropriate to set &lt;code&gt;threeTimesTable[someIndex]&lt;/code&gt; to a new value, and so the subscript for &lt;code&gt;TimesTable&lt;/code&gt; is defined as a read-only subscript. &lt;/p&gt;
&lt;h3&gt;Subscript Usage&lt;/h3&gt;
&lt;p&gt;The exact meaning of “subscript” depends on the context in which it is used. Subscripts are typically used as a shortcut for accessing the member elements in a collection, list, or sequence. You are free to implement subscripts in the most appropriate way for your particular class or structure’s functionality. &lt;/p&gt;
&lt;p&gt;For example, Swift’s &lt;code&gt;Dictionary&lt;/code&gt; type implements a subscript to set and retrieve the values stored in a &lt;code&gt;Dictionary&lt;/code&gt; instance. You can set a value in a dictionary by providing a key of the dictionary’s key type within subscript braces, and assigning a value of the dictionary’s value type to the subscript: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var numberOfLegs = ["spider": 8, "ant": 6, "cat": 4]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfLegs["bird"] = 2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example above defines a variable called &lt;code&gt;numberOfLegs&lt;/code&gt; and initializes it with a dictionary literal containing three key-value pairs. The type of the &lt;code&gt;numberOfLegs&lt;/code&gt; dictionary is inferred to be &lt;code&gt;[String: Int]&lt;/code&gt;. After creating the dictionary, this example uses subscript assignment to add a &lt;code&gt;String&lt;/code&gt; key of &lt;code&gt;"bird"&lt;/code&gt; and an &lt;code&gt;Int&lt;/code&gt; value of &lt;code&gt;2&lt;/code&gt; to the dictionary. &lt;/p&gt;
&lt;p&gt;For more information about &lt;code&gt;Dictionary&lt;/code&gt; subscripting, see Accessing and Modifying a Dictionary. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift’s &lt;code&gt;Dictionary&lt;/code&gt; type implements its key-value subscripting as a subscript that takes and receives an &lt;em&gt;optional&lt;/em&gt; type. For the &lt;code&gt;numberOfLegs&lt;/code&gt; dictionary above, the key-value subscript takes and returns a value of type &lt;code&gt;Int?&lt;/code&gt;, or “optional int”. The &lt;code&gt;Dictionary&lt;/code&gt; type uses an optional subscript type to model the fact that not every key will have a value, and to give a way to delete a value for a key by assigning a &lt;code&gt;nil&lt;/code&gt; value for that key. &lt;/p&gt;
&lt;h3&gt;Subscript Options&lt;/h3&gt;
&lt;p&gt;Subscripts can take any number of input parameters, and these input parameters can be of any type. Subscripts can also return any type. Subscripts can use variable parameters and variadic parameters, but cannot use in-out parameters or provide default parameter values. &lt;/p&gt;
&lt;p&gt;A class or structure can provide as many subscript implementations as it needs, and the appropriate subscript to be used will be inferred based on the types of the value or values that are contained within the subscript braces at the point that the subscript is used. This definition of multiple subscripts is known as &lt;em&gt;subscript overloading&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;While it is most common for a subscript to take a single parameter, you can also define a subscript with multiple parameters if it is appropriate for your type. The following example defines a &lt;code&gt;Matrix&lt;/code&gt; structure, which represents a two-dimensional matrix of &lt;code&gt;Double&lt;/code&gt; values. The &lt;code&gt;Matrix&lt;/code&gt; structure’s subscript takes two integer parameters: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Matrix {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let rows: Int, columns: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var grid: [Double]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(rows: Int, columns: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.rows = rows&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.columns = columns&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;grid = Array(count: rows * columns, repeatedValue: 0.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func indexIsValidForRow(row: Int, column: Int) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return row &amp;gt;= 0 &amp;amp;&amp;amp; row &amp;lt; rows &amp;amp;&amp;amp; column &amp;gt;= 0 &amp;amp;&amp;amp; column &amp;lt; columns&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;subscript(row: Int, column: Int) -&amp;gt; Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;get {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;assert(indexIsValidForRow(row, column: column), "Index out of range")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return grid[(row * columns) + column]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;set {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;assert(indexIsValidForRow(row, column: column), "Index out of range")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;grid[(row * columns) + column] = newValue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;Matrix&lt;/code&gt; provides an initializer that takes two parameters called &lt;code&gt;rows&lt;/code&gt; and &lt;code&gt;columns&lt;/code&gt;, and creates an array that is large enough to store &lt;code&gt;rows * columns&lt;/code&gt; values of type &lt;code&gt;Double&lt;/code&gt;. Each position in the matrix is given an initial value of &lt;code&gt;0.0&lt;/code&gt;. To achieve this, the array’s size, and an initial cell value of &lt;code&gt;0.0&lt;/code&gt;, are passed to an array initializer that creates and initializes a new array of the correct size. This initializer is described in more detail in Creating and Initializing an Array. &lt;/p&gt;
&lt;p&gt;You can construct a new &lt;code&gt;Matrix&lt;/code&gt; instance by passing an appropriate row and column count to its initializer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var matrix = Matrix(rows: 2, columns: 2)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The preceding example creates a new &lt;code&gt;Matrix&lt;/code&gt; instance with two rows and two columns. The &lt;code&gt;grid&lt;/code&gt; array for this &lt;code&gt;Matrix&lt;/code&gt; instance is effectively a flattened version of the matrix, as read from top left to bottom right: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/subscriptMatrix01_2x.png" src="http://muxuezi.github.io/posts/Art/subscriptMatrix01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Values in the matrix can be set by passing row and column values into the subscript, separated by a comma: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;matrix[0, 1] = 1.5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;matrix[1, 0] = 3.2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;These two statements call the subscript’s setter to set a value of &lt;code&gt;1.5&lt;/code&gt; in the top right position of the matrix (where &lt;code&gt;row&lt;/code&gt; is &lt;code&gt;0&lt;/code&gt; and &lt;code&gt;column&lt;/code&gt; is &lt;code&gt;1&lt;/code&gt;), and &lt;code&gt;3.2&lt;/code&gt; in the bottom left position (where &lt;code&gt;row&lt;/code&gt; is &lt;code&gt;1&lt;/code&gt; and &lt;code&gt;column&lt;/code&gt; is &lt;code&gt;0&lt;/code&gt;): &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/subscriptMatrix02_2x.png" src="http://muxuezi.github.io/posts/Art/subscriptMatrix02_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Matrix&lt;/code&gt; subscript’s getter and setter both contain an assertion to check that the subscript’s &lt;code&gt;row&lt;/code&gt; and &lt;code&gt;column&lt;/code&gt; values are valid. To assist with these assertions, &lt;code&gt;Matrix&lt;/code&gt; includes a convenience method called &lt;code&gt;indexIsValidForRow(_:column:)&lt;/code&gt;, which checks whether the requested &lt;code&gt;row&lt;/code&gt; and &lt;code&gt;column&lt;/code&gt; are inside the bounds of the matrix: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func indexIsValidForRow(row: Int, column: Int) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return row &amp;gt;= 0 &amp;amp;&amp;amp; row &amp;lt; rows &amp;amp;&amp;amp; column &amp;gt;= 0 &amp;amp;&amp;amp; column &amp;lt; columns&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;An assertion is triggered if you try to access a subscript that is outside of the matrix bounds: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someValue = matrix[2, 2]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this triggers an assert, because [2, 2] is outside of the matrix bounds&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Inheritance&lt;/h2&gt;
&lt;p&gt;A class can &lt;em&gt;inherit&lt;/em&gt; methods, properties, and other characteristics from another class. When one class inherits from another, the inheriting class is known as a &lt;em&gt;subclass&lt;/em&gt;, and the class it inherits from is known as its &lt;em&gt;superclass&lt;/em&gt;. Inheritance is a fundamental behavior that differentiates classes from other types in Swift. &lt;/p&gt;
&lt;p&gt;Classes in Swift can call and access methods, properties, and subscripts belonging to their superclass and can provide their own overriding versions of those methods, properties, and subscripts to refine or modify their behavior. Swift helps to ensure your overrides are correct by checking that the override definition has a matching superclass definition. &lt;/p&gt;
&lt;p&gt;Classes can also add property observers to inherited properties in order to be notified when the value of a property changes. Property observers can be added to any property, regardless of whether it was originally defined as a stored or computed property. &lt;/p&gt;
&lt;h3&gt;Defining a Base Class&lt;/h3&gt;
&lt;p&gt;Any class that does not inherit from another class is known as a &lt;em&gt;base class&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift classes do not inherit from a universal base class. Classes you define without specifying a superclass automatically become base classes for you to build upon. &lt;/p&gt;
&lt;p&gt;The example below defines a base class called &lt;code&gt;Vehicle&lt;/code&gt;. This base class defines a stored property called &lt;code&gt;currentSpeed&lt;/code&gt;, with a default value of &lt;code&gt;0.0&lt;/code&gt; (inferring a property type of &lt;code&gt;Double&lt;/code&gt;). The &lt;code&gt;currentSpeed&lt;/code&gt; property’s value is used by a read-only computed &lt;code&gt;String&lt;/code&gt; property called &lt;code&gt;description&lt;/code&gt; to create a description of the vehicle. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Vehicle&lt;/code&gt; base class also defines a method called &lt;code&gt;makeNoise&lt;/code&gt;. This method does not actually do anything for a base &lt;code&gt;Vehicle&lt;/code&gt; instance, but will be customized by subclasses of &lt;code&gt;Vehicle&lt;/code&gt; later on: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Vehicle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentSpeed = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var description: String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "traveling at \\(currentSpeed) miles per hour"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func makeNoise() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// do nothing - an arbitrary vehicle doesn't necessarily make a noise&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You create a new instance of &lt;code&gt;Vehicle&lt;/code&gt; with &lt;em&gt;initializer syntax&lt;/em&gt;, which is written as a &lt;code&gt;TypeName&lt;/code&gt; followed by empty parentheses: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someVehicle = Vehicle()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Having created a new &lt;code&gt;Vehicle&lt;/code&gt; instance, you can access its &lt;code&gt;description&lt;/code&gt; property to print a human-readable description of the vehicle’s current speed: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("Vehicle: \\(someVehicle.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Vehicle: traveling at 0.0 miles per hour&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Vehicle&lt;/code&gt; class defines common characteristics for an arbitrary vehicle, but is not much use in itself. To make it more useful, you need to refine it to describe more specific kinds of vehicle. &lt;/p&gt;
&lt;h3&gt;Subclassing&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Subclassing&lt;/em&gt; is the act of basing a new class on an existing class. The subclass inherits characteristics from the existing class, which you can then refine. You can also add new characteristics to the subclass. &lt;/p&gt;
&lt;p&gt;To indicate that a subclass has a superclass, write the subclass name before the superclass name, separated by a colon: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeSubclass: SomeSuperclass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// subclass definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The following example defines a subclass called &lt;code&gt;Bicycle&lt;/code&gt;, with a superclass of &lt;code&gt;Vehicle&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Bicycle: Vehicle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var hasBasket = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The new &lt;code&gt;Bicycle&lt;/code&gt; class automatically gains all of the characteristics of &lt;code&gt;Vehicle&lt;/code&gt;, such as its &lt;code&gt;currentSpeed&lt;/code&gt; and &lt;code&gt;description&lt;/code&gt; properties and its &lt;code&gt;makeNoise&lt;/code&gt; method. &lt;/p&gt;
&lt;p&gt;In addition to the characteristics it inherits, the &lt;code&gt;Bicycle&lt;/code&gt; class defines a new stored property, &lt;code&gt;hasBasket&lt;/code&gt;, with a default value of &lt;code&gt;false&lt;/code&gt; (inferring a type of &lt;code&gt;Bool&lt;/code&gt; for the property). &lt;/p&gt;
&lt;p&gt;By default, any new &lt;code&gt;Bicycle&lt;/code&gt; instance you create will not have a basket. You can set the &lt;code&gt;hasBasket&lt;/code&gt; property to &lt;code&gt;true&lt;/code&gt; for a particular &lt;code&gt;Bicycle&lt;/code&gt; instance after that instance is created: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let bicycle = Bicycle()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;bicycle.hasBasket = true&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also modify the inherited &lt;code&gt;currentSpeed&lt;/code&gt; property of a &lt;code&gt;Bicycle&lt;/code&gt; instance, and query the instance’s inherited &lt;code&gt;description&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;bicycle.currentSpeed = 15.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Bicycle: \\(bicycle.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Bicycle: traveling at 15.0 miles per hour&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Subclasses can themselves be subclassed. The next example creates a subclass of &lt;code&gt;Bicycle&lt;/code&gt; for a two-seater bicycle known as a “tandem”: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Tandem: Bicycle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var currentNumberOfPassengers = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;Tandem&lt;/code&gt; inherits all of the properties and methods from &lt;code&gt;Bicycle&lt;/code&gt;, which in turn inherits all of the properties and methods from &lt;code&gt;Vehicle&lt;/code&gt;. The &lt;code&gt;Tandem&lt;/code&gt; subclass also adds a new stored property called &lt;code&gt;currentNumberOfPassengers&lt;/code&gt;, with a default value of &lt;code&gt;0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If you create an instance of &lt;code&gt;Tandem&lt;/code&gt;, you can work with any of its new and inherited properties, and query the read-only &lt;code&gt;description&lt;/code&gt; property it inherits from &lt;code&gt;Vehicle&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let tandem = Tandem()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tandem.hasBasket = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tandem.currentNumberOfPassengers = 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tandem.currentSpeed = 22.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Tandem: \\(tandem.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Tandem: traveling at 22.0 miles per hour&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Overriding&lt;/h3&gt;
&lt;p&gt;A subclass can provide its own custom implementation of an instance method, class method, instance property, class property, or subscript that it would otherwise inherit from a superclass. This is known as &lt;em&gt;overriding&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;To override a characteristic that would otherwise be inherited, you prefix your overriding definition with the &lt;code&gt;override&lt;/code&gt; keyword. Doing so clarifies that you intend to provide an override and have not provided a matching definition by mistake. Overriding by accident can cause unexpected behavior, and any overrides without the &lt;code&gt;override&lt;/code&gt; keyword are diagnosed as an error when your code is compiled. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;override&lt;/code&gt; keyword also prompts the Swift compiler to check that your overriding class’s superclass (or one of its parents) has a declaration that matches the one you provided for the override. This check ensures that your overriding definition is correct. &lt;/p&gt;
&lt;h4&gt;Accessing Superclass Methods, Properties, and Subscripts&lt;/h4&gt;
&lt;p&gt;When you provide a method, property, or subscript override for a subclass, it is sometimes useful to use the existing superclass implementation as part of your override. For example, you can refine the behavior of that existing implementation, or store a modified value in an existing inherited variable. &lt;/p&gt;
&lt;p&gt;Where this is appropriate, you access the superclass version of a method, property, or subscript by using the &lt;code&gt;super&lt;/code&gt; prefix: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;An overridden method named &lt;code&gt;someMethod&lt;/code&gt; can call the superclass version of &lt;code&gt;someMethod&lt;/code&gt; by calling &lt;code&gt;super.someMethod()&lt;/code&gt; within the overriding method implementation. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;An overridden property called &lt;code&gt;someProperty&lt;/code&gt; can access the superclass version of &lt;code&gt;someProperty&lt;/code&gt; as &lt;code&gt;super.someProperty&lt;/code&gt; within the overriding getter or setter implementation. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;An overridden subscript for &lt;code&gt;someIndex&lt;/code&gt; can access the superclass version of the same subscript as &lt;code&gt;super[someIndex]&lt;/code&gt; from within the overriding subscript implementation. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Overriding Methods&lt;/h4&gt;
&lt;p&gt;You can override an inherited instance or class method to provide a tailored or alternative implementation of the method within your subclass. &lt;/p&gt;
&lt;p&gt;The following example defines a new subclass of &lt;code&gt;Vehicle&lt;/code&gt; called &lt;code&gt;Train&lt;/code&gt;, which overrides the &lt;code&gt;makeNoise&lt;/code&gt; method that &lt;code&gt;Train&lt;/code&gt; inherits from &lt;code&gt;Vehicle&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Train: Vehicle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override func makeNoise() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Choo Choo")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you create a new instance of &lt;code&gt;Train&lt;/code&gt; and call its &lt;code&gt;makeNoise&lt;/code&gt; method, you can see that the &lt;code&gt;Train&lt;/code&gt; subclass version of the method is called: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let train = Train()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;train.makeNoise()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Choo Choo"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Overriding Properties&lt;/h4&gt;
&lt;p&gt;You can override an inherited instance or class property to provide your own custom getter and setter for that property, or to add property observers to enable the overriding property to observe when the underlying property value changes. &lt;/p&gt;
&lt;h4&gt;Overriding Property Getters and Setters&lt;/h4&gt;
&lt;p&gt;You can provide a custom getter (and setter, if appropriate) to override &lt;em&gt;any&lt;/em&gt; inherited property, regardless of whether the inherited property is implemented as a stored or computed property at source. The stored or computed nature of an inherited property is not known by a subclass—it only knows that the inherited property has a certain name and type. You must always state both the name and the type of the property you are overriding, to enable the compiler to check that your override matches a superclass property with the same name and type. &lt;/p&gt;
&lt;p&gt;You can present an inherited read-only property as a read-write property by providing both a getter and a setter in your subclass property override. You cannot, however, present an inherited read-write property as a read-only property. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you provide a setter as part of a property override, you must also provide a getter for that override. If you don’t want to modify the inherited property’s value within the overriding getter, you can simply pass through the inherited value by returning &lt;code&gt;super.someProperty&lt;/code&gt; from the getter, where &lt;code&gt;someProperty&lt;/code&gt; is the name of the property you are overriding. &lt;/p&gt;
&lt;p&gt;The following example defines a new class called &lt;code&gt;Car&lt;/code&gt;, which is a subclass of &lt;code&gt;Vehicle&lt;/code&gt;. The &lt;code&gt;Car&lt;/code&gt; class introduces a new stored property called &lt;code&gt;gear&lt;/code&gt;, with a default integer value of &lt;code&gt;1&lt;/code&gt;. The &lt;code&gt;Car&lt;/code&gt; class also overrides the &lt;code&gt;description&lt;/code&gt; property it inherits from &lt;code&gt;Vehicle&lt;/code&gt;, to provide a custom description that includes the current gear: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Car: Vehicle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var gear = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override var description: String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return super.description \+ " in gear \\(gear)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The override of the &lt;code&gt;description&lt;/code&gt; property starts by calling &lt;code&gt;super.description&lt;/code&gt;, which returns the &lt;code&gt;Vehicle&lt;/code&gt; class’s &lt;code&gt;description&lt;/code&gt; property. The &lt;code&gt;Car&lt;/code&gt; class’s version of &lt;code&gt;description&lt;/code&gt; then adds some extra text onto the end of this description to provide information about the current gear. &lt;/p&gt;
&lt;p&gt;If you create an instance of the &lt;code&gt;Car&lt;/code&gt; class and set its &lt;code&gt;gear&lt;/code&gt; and &lt;code&gt;currentSpeed&lt;/code&gt; properties, you can see that its &lt;code&gt;description&lt;/code&gt; property returns the tailored description defined within the &lt;code&gt;Car&lt;/code&gt; class: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let car = Car()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;car.currentSpeed = 25.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;car.gear = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Car: \\(car.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Car: traveling at 25.0 miles per hour in gear 3&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Overriding Property Observers&lt;/h4&gt;
&lt;p&gt;You can use property overriding to add property observers to an inherited property. This enables you to be notified when the value of an inherited property changes, regardless of how that property was originally implemented. For more information on property observers, see Property Observers. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You cannot add property observers to inherited constant stored properties or inherited read-only computed properties. The value of these properties cannot be set, and so it is not appropriate to provide a &lt;code&gt;willSet&lt;/code&gt; or &lt;code&gt;didSet&lt;/code&gt; implementation as part of an override. &lt;/p&gt;
&lt;p&gt;Note also that you cannot provide both an overriding setter and an overriding property observer for the same property. If you want to observe changes to a property’s value, and you are already providing a custom setter for that property, you can simply observe any value changes from within the custom setter. &lt;/p&gt;
&lt;p&gt;The following example defines a new class called &lt;code&gt;AutomaticCar&lt;/code&gt;, which is a subclass of &lt;code&gt;Car&lt;/code&gt;. The &lt;code&gt;AutomaticCar&lt;/code&gt; class represents a car with an automatic gearbox, which automatically selects an appropriate gear to use based on the current speed: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class AutomaticCar: Car {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override var currentSpeed: Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;didSet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;gear = Int(currentSpeed / 10.0) + 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Whenever you set the &lt;code&gt;currentSpeed&lt;/code&gt; property of an &lt;code&gt;AutomaticCar&lt;/code&gt; instance, the property’s &lt;code&gt;didSet&lt;/code&gt; observer sets the instance’s &lt;code&gt;gear&lt;/code&gt; property to an appropriate choice of gear for the new speed. Specifically, the property observer chooses a gear that is the new &lt;code&gt;currentSpeed&lt;/code&gt; value divided by &lt;code&gt;10&lt;/code&gt;, rounded down to the nearest integer, plus &lt;code&gt;1&lt;/code&gt;. A speed of &lt;code&gt;35.0&lt;/code&gt; produces a gear of &lt;code&gt;4&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let automatic = AutomaticCar()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;automatic.currentSpeed = 35.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("AutomaticCar: \\(automatic.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// AutomaticCar: traveling at 35.0 miles per hour in gear 4&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Preventing Overrides&lt;/h3&gt;
&lt;p&gt;You can prevent a method, property, or subscript from being overridden by marking it as &lt;em&gt;final&lt;/em&gt;. Do this by writing the &lt;code&gt;final&lt;/code&gt; modifier before the method, property, or subscript’s introducer keyword (such as &lt;code&gt;final var&lt;/code&gt;, &lt;code&gt;final func&lt;/code&gt;, &lt;code&gt;final class func&lt;/code&gt;, and &lt;code&gt;final subscript&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Any attempt to override a final method, property, or subscript in a subclass is reported as a compile-time error. Methods, properties, or subscripts that you add to a class in an extension can also be marked as final within the extension’s definition. &lt;/p&gt;
&lt;p&gt;You can mark an entire class as final by writing the &lt;code&gt;final&lt;/code&gt; modifier before the &lt;code&gt;class&lt;/code&gt; keyword in its class definition (&lt;code&gt;final class&lt;/code&gt;). Any attempt to subclass a final class is reported as a compile-time error. &lt;/p&gt;
&lt;h2&gt;Initialization&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Initialization&lt;/em&gt; is the process of preparing an instance of a class, structure, or enumeration for use. This process involves setting an initial value for each stored property on that instance and performing any other setup or initialization that is required before the new instance is ready for use. &lt;/p&gt;
&lt;p&gt;You implement this initialization process by defining &lt;em&gt;initializers&lt;/em&gt;, which are like special methods that can be called to create a new instance of a particular type. Unlike Objective-C initializers, Swift initializers do not return a value. Their primary role is to ensure that new instances of a type are correctly initialized before they are used for the first time. &lt;/p&gt;
&lt;p&gt;Instances of class types can also implement a &lt;em&gt;deinitializer&lt;/em&gt;, which performs any custom cleanup just before an instance of that class is deallocated. For more information about deinitializers, see Deinitialization. &lt;/p&gt;
&lt;h3&gt;Setting Initial Values for Stored Properties&lt;/h3&gt;
&lt;p&gt;Classes and structures &lt;em&gt;must&lt;/em&gt; set all of their stored properties to an appropriate initial value by the time an instance of that class or structure is created. Stored properties cannot be left in an indeterminate state. &lt;/p&gt;
&lt;p&gt;You can set an initial value for a stored property within an initializer, or by assigning a default property value as part of the property’s definition. These actions are described in the following sections. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;When you assign a default value to a stored property, or set its initial value within an initializer, the value of that property is set directly, without calling any property observers. &lt;/p&gt;
&lt;h4&gt;Initializers&lt;/h4&gt;
&lt;p&gt;&lt;em&gt;Initializers&lt;/em&gt; are called to create a new instance of a particular type. In its simplest form, an initializer is like an instance method with no parameters, written using the &lt;code&gt;init&lt;/code&gt; keyword: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// perform some initialization here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example below defines a new structure called &lt;code&gt;Fahrenheit&lt;/code&gt; to store temperatures expressed in the Fahrenheit scale. The &lt;code&gt;Fahrenheit&lt;/code&gt; structure has one stored property, &lt;code&gt;temperature&lt;/code&gt;, which is of type &lt;code&gt;Double&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Fahrenheit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var temperature: Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;temperature = 32.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var f = Fahrenheit()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The default temperature is \\(f.temperature)° Fahrenheit")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The default temperature is 32.0° Fahrenheit"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The structure defines a single initializer, &lt;code&gt;init&lt;/code&gt;, with no parameters, which initializes the stored temperature with a value of &lt;code&gt;32.0&lt;/code&gt; (the freezing point of water when expressed in the Fahrenheit scale). &lt;/p&gt;
&lt;h4&gt;Default Property Values&lt;/h4&gt;
&lt;p&gt;You can set the initial value of a stored property from within an initializer, as shown above. Alternatively, specify a &lt;em&gt;default property value&lt;/em&gt; as part of the property’s declaration. You specify a default property value by assigning an initial value to the property when it is defined. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If a property always takes the same initial value, provide a default value rather than setting a value within an initializer. The end result is the same, but the default value ties the property’s initialization more closely to its declaration. It makes for shorter, clearer initializers and enables you to infer the type of the property from its default value. The default value also makes it easier for you to take advantage of default initializers and initializer inheritance, as described later in this chapter. &lt;/p&gt;
&lt;p&gt;You can write the &lt;code&gt;Fahrenheit&lt;/code&gt; structure from above in a simpler form by providing a default value for its &lt;code&gt;temperature&lt;/code&gt; property at the point that the property is declared: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Fahrenheit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var temperature = 32.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Customizing Initialization&lt;/h3&gt;
&lt;p&gt;You can customize the initialization process with input parameters and optional property types, or by modifying constant properties during initialization, as described in the following sections. &lt;/p&gt;
&lt;h4&gt;Initialization Parameters&lt;/h4&gt;
&lt;p&gt;You can provide &lt;em&gt;initialization parameters&lt;/em&gt; as part of an initializer’s definition, to define the types and names of values that customize the initialization process. Initialization parameters have the same capabilities and syntax as function and method parameters. &lt;/p&gt;
&lt;p&gt;The following example defines a structure called &lt;code&gt;Celsius&lt;/code&gt;, which stores temperatures expressed in the Celsius scale. The &lt;code&gt;Celsius&lt;/code&gt; structure implements two custom initializers called &lt;code&gt;init(fromFahrenheit:)&lt;/code&gt; and &lt;code&gt;init(fromKelvin:)&lt;/code&gt;, which initialize a new instance of the structure with a value from a different temperature scale: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Celsius {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var temperatureInCelsius: Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(fromFahrenheit fahrenheit: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;temperatureInCelsius = (fahrenheit \- 32.0) / 1.8&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(fromKelvin kelvin: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;temperatureInCelsius = kelvin \- 273.15&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let boilingPointOfWater = Celsius(fromFahrenheit: 212.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// boilingPointOfWater.temperatureInCelsius is 100.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let freezingPointOfWater = Celsius(fromKelvin: 273.15)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// freezingPointOfWater.temperatureInCelsius is 0.0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The first initializer has a single initialization parameter with an external name of &lt;code&gt;fromFahrenheit&lt;/code&gt; and a local name of &lt;code&gt;fahrenheit&lt;/code&gt;. The second initializer has a single initialization parameter with an external name of &lt;code&gt;fromKelvin&lt;/code&gt; and a local name of &lt;code&gt;kelvin&lt;/code&gt;. Both initializers convert their single argument into a value in the Celsius scale and store this value in a property called &lt;code&gt;temperatureInCelsius&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Local and External Parameter Names&lt;/h4&gt;
&lt;p&gt;As with function and method parameters, initialization parameters can have both a local name for use within the initializer’s body and an external name for use when calling the initializer. &lt;/p&gt;
&lt;p&gt;However, initializers do not have an identifying function name before their parentheses in the way that functions and methods do. Therefore, the names and types of an initializer’s parameters play a particularly important role in identifying which initializer should be called. Because of this, Swift provides an automatic external name for &lt;em&gt;every&lt;/em&gt; parameter in an initializer if you don’t provide an external name yourself. This automatic external name is the same as the local name, as if you had written a hash symbol before every initialization parameter. &lt;/p&gt;
&lt;p&gt;The following example defines a structure called &lt;code&gt;Color&lt;/code&gt;, with three constant properties called &lt;code&gt;red&lt;/code&gt;, &lt;code&gt;green&lt;/code&gt;, and &lt;code&gt;blue&lt;/code&gt;. These properties store a value between &lt;code&gt;0.0&lt;/code&gt; and &lt;code&gt;1.0&lt;/code&gt; to indicate the amount of red, green, and blue in the color. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;Color&lt;/code&gt; provides an initializer with three appropriately named parameters of type &lt;code&gt;Double&lt;/code&gt; for its red, green, and blue components. &lt;code&gt;Color&lt;/code&gt; also provides a second initializer with a single &lt;code&gt;white&lt;/code&gt; parameter, which is used to provide the same value for all three color components. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Color {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let red, green, blue: Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(red: Double, green: Double, blue: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.red = red&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.green = green&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.blue = blue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(white: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;red = white&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;green = white&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;blue = white&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Both initializers can be used to create a new &lt;code&gt;Color&lt;/code&gt; instance, by providing named values for each initializer parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let magenta = Color(red: 1.0, green: 0.0, blue: 1.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let halfGray = Color(white: 0.5)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that it is not possible to call these initializers without using external parameter names. External names must always be used in an initializer if they are defined, and omitting them is a compile-time error: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let veryGreen = Color(0.0, 1.0, 0.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this reports a compile-time error - external names are required&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Initializer Parameters Without External Names&lt;/h4&gt;
&lt;p&gt;If you do not want to use an external name for an initializer parameter, write an underscore (&lt;code&gt;_&lt;/code&gt;) instead of an explicit external name for that parameter to override the default behavior. &lt;/p&gt;
&lt;p&gt;Here’s an expanded version of the &lt;code&gt;Celsius&lt;/code&gt; example from earlier, with an additional initializer to create a new &lt;code&gt;Celsius&lt;/code&gt; instance from a &lt;code&gt;Double&lt;/code&gt; value that is already in the Celsius scale: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Celsius {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var temperatureInCelsius: Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(fromFahrenheit fahrenheit: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;temperatureInCelsius = (fahrenheit \- 32.0) / 1.8&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(fromKelvin kelvin: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;temperatureInCelsius = kelvin \- 273.15&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(_ celsius: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;temperatureInCelsius = celsius&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let bodyTemperature = Celsius(37.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// bodyTemperature.temperatureInCelsius is 37.0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The initializer call &lt;code&gt;Celsius(37.0)&lt;/code&gt; is clear in its intent without the need for an external parameter name. It is therefore appropriate to write this initializer as &lt;code&gt;init(_ celsius: Double)&lt;/code&gt; so that it can be called by providing an unnamed &lt;code&gt;Double&lt;/code&gt; value. &lt;/p&gt;
&lt;h4&gt;Optional Property Types&lt;/h4&gt;
&lt;p&gt;If your custom type has a stored property that is logically allowed to have “no value”—perhaps because its value cannot be set during initialization, or because it is allowed to have “no value” at some later point—declare the property with an &lt;em&gt;optional&lt;/em&gt; type. Properties of optional type are automatically initialized with a value of &lt;code&gt;nil&lt;/code&gt;, indicating that the property is deliberately intended to have “no value yet” during initialization. &lt;/p&gt;
&lt;p&gt;The following example defines a class called &lt;code&gt;SurveyQuestion&lt;/code&gt;, with an optional &lt;code&gt;String&lt;/code&gt; property called &lt;code&gt;response&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SurveyQuestion {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var text: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var response: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(text: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.text = text&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func ask() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(text)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let cheeseQuestion = SurveyQuestion(text: "Do you like cheese?")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;cheeseQuestion.ask()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Do you like cheese?"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;cheeseQuestion.response = "Yes, I do like cheese."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The response to a survey question cannot be known until it is asked, and so the &lt;code&gt;response&lt;/code&gt; property is declared with a type of &lt;code&gt;String?&lt;/code&gt;, or “optional &lt;code&gt;String&lt;/code&gt;”. It is automatically assigned a default value of &lt;code&gt;nil&lt;/code&gt;, meaning “no string yet”, when a new instance of &lt;code&gt;SurveyQuestion&lt;/code&gt; is initialized. &lt;/p&gt;
&lt;h4&gt;Modifying Constant Properties During Initialization&lt;/h4&gt;
&lt;p&gt;You can modify the value of a constant property at any point during initialization, as long as it is set to a definite value by the time initialization finishes. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;For class instances, a constant property can only be modified during initialization by the class that introduces it. It cannot be modified by a subclass. &lt;/p&gt;
&lt;p&gt;You can revise the &lt;code&gt;SurveyQuestion&lt;/code&gt; example from above to use a constant property rather than a variable property for the &lt;code&gt;text&lt;/code&gt; property of the question, to indicate that the question does not change once an instance of &lt;code&gt;SurveyQuestion&lt;/code&gt; is created. Even though the &lt;code&gt;text&lt;/code&gt; property is now a constant, it can still be set within the class’s initializer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SurveyQuestion {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let text: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var response: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(text: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.text = text&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func ask() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(text)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let beetsQuestion = SurveyQuestion(text: "How about beets?")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;beetsQuestion.ask()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "How about beets?"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;beetsQuestion.response = "I also like beets. (But not with cheese.)"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Default Initializers&lt;/h3&gt;
&lt;p&gt;Swift provides a &lt;em&gt;default initializer&lt;/em&gt; for any structure or base class that provides default values for all of its properties and does not provide at least one initializer itself. The default initializer simply creates a new instance with all of its properties set to their default values. &lt;/p&gt;
&lt;p&gt;This example defines a class called &lt;code&gt;ShoppingListItem&lt;/code&gt;, which encapsulates the name, quantity, and purchase state of an item in a shopping list: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class ShoppingListItem {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var quantity = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var purchased = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var item = ShoppingListItem()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because all properties of the &lt;code&gt;ShoppingListItem&lt;/code&gt; class have default values, and because it is a base class with no superclass, &lt;code&gt;ShoppingListItem&lt;/code&gt; automatically gains a default initializer implementation that creates a new instance with all of its properties set to their default values. (The &lt;code&gt;name&lt;/code&gt; property is an optional &lt;code&gt;String&lt;/code&gt; property, and so it automatically receives a default value of &lt;code&gt;nil&lt;/code&gt;, even though this value is not written in the code.) The example above uses the default initializer for the &lt;code&gt;ShoppingListItem&lt;/code&gt; class to create a new instance of the class with initializer syntax, written as &lt;code&gt;ShoppingListItem()&lt;/code&gt;, and assigns this new instance to a variable called &lt;code&gt;item&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Memberwise Initializers for Structure Types&lt;/h4&gt;
&lt;p&gt;Structure types automatically receive a &lt;em&gt;memberwise initializer&lt;/em&gt; if they do not define any of their own custom initializers. Unlike a default initializer, the structure receives a memberwise initializer even if it has stored properties that do not have default values. &lt;/p&gt;
&lt;p&gt;The memberwise initializer is a shorthand way to initialize the member properties of new structure instances. Initial values for the properties of the new instance can be passed to the memberwise initializer by name. &lt;/p&gt;
&lt;p&gt;The example below defines a structure called &lt;code&gt;Size&lt;/code&gt; with two properties called &lt;code&gt;width&lt;/code&gt; and &lt;code&gt;height&lt;/code&gt;. Both properties are inferred to be of type &lt;code&gt;Double&lt;/code&gt; by assigning a default value of &lt;code&gt;0.0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Size&lt;/code&gt; structure automatically receives an &lt;code&gt;init(width:height:)&lt;/code&gt; memberwise initializer, which you can use to initialize a new &lt;code&gt;Size&lt;/code&gt; instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Size {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var width = 0.0, height = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let twoByTwo = Size(width: 2.0, height: 2.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Initializer Delegation for Value Types&lt;/h3&gt;
&lt;p&gt;Initializers can call other initializers to perform part of an instance’s initialization. This process, known as &lt;em&gt;initializer delegation&lt;/em&gt;, avoids duplicating code across multiple initializers. &lt;/p&gt;
&lt;p&gt;The rules for how initializer delegation works, and for what forms of delegation are allowed, are different for value types and class types. Value types (structures and enumerations) do not support inheritance, and so their initializer delegation process is relatively simple, because they can only delegate to another initializer that they provide themselves. Classes, however, can inherit from other classes, as described in Inheritance. This means that classes have additional responsibilities for ensuring that all stored properties they inherit are assigned a suitable value during initialization. These responsibilities are described in Class Inheritance and Initialization below. &lt;/p&gt;
&lt;p&gt;For value types, you use &lt;code&gt;self.init&lt;/code&gt; to refer to other initializers from the same value type when writing your own custom initializers. You can only call &lt;code&gt;self.init&lt;/code&gt; from within an initializer. &lt;/p&gt;
&lt;p&gt;Note that if you define a custom initializer for a value type, you will no longer have access to the default initializer (or the memberwise initializer, if it is a structure) for that type. This constraint prevents a situation in which additional essential setup provided in a more complex initializer is circumvented by someone accidentally using one of the automatic initializers instead. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you want your custom value type to be initializable with the default initializer and memberwise initializer, and also with your own custom initializers, write your custom initializers in an extension rather than as part of the value type’s original implementation. For more information, see Extensions. &lt;/p&gt;
&lt;p&gt;The following example defines a custom &lt;code&gt;Rect&lt;/code&gt; structure to represent a geometric rectangle. The example requires two supporting structures called &lt;code&gt;Size&lt;/code&gt; and &lt;code&gt;Point&lt;/code&gt;, both of which provide default values of &lt;code&gt;0.0&lt;/code&gt; for all of their properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Size {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var width = 0.0, height = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can initialize the &lt;code&gt;Rect&lt;/code&gt; structure below in one of three ways—by using its default zero-initialized &lt;code&gt;origin&lt;/code&gt; and &lt;code&gt;size&lt;/code&gt; property values, by providing a specific origin point and size, or by providing a specific center point and size. These initialization options are represented by three custom initializers that are part of the &lt;code&gt;Rect&lt;/code&gt; structure’s definition: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Rect {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var origin = Point()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var size = Size()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init() {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(origin: Point, size: Size) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.origin = origin&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.size = size&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(center: Point, size: Size) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let originX = center.x \- (size.width / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let originY = center.y \- (size.height / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.init(origin: Point(x: originX, y: originY), size: size)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The first &lt;code&gt;Rect&lt;/code&gt; initializer, &lt;code&gt;init()&lt;/code&gt;, is functionally the same as the default initializer that the structure would have received if it did not have its own custom initializers. This initializer has an empty body, represented by an empty pair of curly braces &lt;code&gt;{}&lt;/code&gt;, and does not perform any initialization. Calling this initializer returns a &lt;code&gt;Rect&lt;/code&gt; instance whose &lt;code&gt;origin&lt;/code&gt; and &lt;code&gt;size&lt;/code&gt; properties are both initialized with the default values of &lt;code&gt;Point(x: 0.0, y: 0.0)&lt;/code&gt; and &lt;code&gt;Size(width: 0.0, height: 0.0)&lt;/code&gt; from their property definitions: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let basicRect = Rect()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// basicRect's origin is (0.0, 0.0) and its size is (0.0, 0.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The second &lt;code&gt;Rect&lt;/code&gt; initializer, &lt;code&gt;init(origin:size:)&lt;/code&gt;, is functionally the same as the memberwise initializer that the structure would have received if it did not have its own custom initializers. This initializer simply assigns the &lt;code&gt;origin&lt;/code&gt; and &lt;code&gt;size&lt;/code&gt; argument values to the appropriate stored properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let originRect = Rect(origin: Point(x: 2.0, y: 2.0),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;size: Size(width: 5.0, height: 5.0))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// originRect's origin is (2.0, 2.0) and its size is (5.0, 5.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The third &lt;code&gt;Rect&lt;/code&gt; initializer, &lt;code&gt;init(center:size:)&lt;/code&gt;, is slightly more complex. It starts by calculating an appropriate origin point based on a &lt;code&gt;center&lt;/code&gt; point and a &lt;code&gt;size&lt;/code&gt; value. It then calls (or &lt;em&gt;delegates&lt;/em&gt;) to the &lt;code&gt;init(origin:size:)&lt;/code&gt; initializer, which stores the new origin and size values in the appropriate properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let centerRect = Rect(center: Point(x: 4.0, y: 4.0),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;size: Size(width: 3.0, height: 3.0))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// centerRect's origin is (2.5, 2.5) and its size is (3.0, 3.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;init(center:size:)&lt;/code&gt; initializer could have assigned the new values of &lt;code&gt;origin&lt;/code&gt; and &lt;code&gt;size&lt;/code&gt; to the appropriate properties itself. However, it is more convenient (and clearer in intent) for the &lt;code&gt;init(center:size:)&lt;/code&gt; initializer to take advantage of an existing initializer that already provides exactly that functionality. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;For an alternative way to write this example without defining the &lt;code&gt;init()&lt;/code&gt; and &lt;code&gt;init(origin:size:)&lt;/code&gt; initializers yourself, see Extensions. &lt;/p&gt;
&lt;h3&gt;Class Inheritance and Initialization&lt;/h3&gt;
&lt;p&gt;All of a class’s stored properties—including any properties the class inherits from its superclass—&lt;em&gt;must&lt;/em&gt; be assigned an initial value during initialization. &lt;/p&gt;
&lt;p&gt;Swift defines two kinds of initializers for class types to help ensure all stored properties receive an initial value. These are known as designated initializers and convenience initializers. &lt;/p&gt;
&lt;h4&gt;Designated Initializers and Convenience Initializers&lt;/h4&gt;
&lt;p&gt;&lt;em&gt;Designated initializers&lt;/em&gt; are the primary initializers for a class. A designated initializer fully initializes all properties introduced by that class and calls an appropriate superclass initializer to continue the initialization process up the superclass chain. &lt;/p&gt;
&lt;p&gt;Classes tend to have very few designated initializers, and it is quite common for a class to have only one. Designated initializers are “funnel” points through which initialization takes place, and through which the initialization process continues up the superclass chain. &lt;/p&gt;
&lt;p&gt;Every class must have at least one designated initializer. In some cases, this requirement is satisfied by inheriting one or more designated initializers from a superclass, as described in Automatic Initializer Inheritance below. &lt;/p&gt;
&lt;p&gt;&lt;em&gt;Convenience initializers&lt;/em&gt; are secondary, supporting initializers for a class. You can define a convenience initializer to call a designated initializer from the same class as the convenience initializer with some of the designated initializer’s parameters set to default values. You can also define a convenience initializer to create an instance of that class for a specific use case or input value type. &lt;/p&gt;
&lt;p&gt;You do not have to provide convenience initializers if your class does not require them. Create convenience initializers whenever a shortcut to a common initialization pattern will save time or make initialization of the class clearer in intent. &lt;/p&gt;
&lt;h4&gt;Syntax for Designated and Convenience Initializers&lt;/h4&gt;
&lt;p&gt;Designated initializers for classes are written in the same way as simple initializers for value types: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;init(&lt;em&gt;parameters&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Convenience initializers are written in the same style, but with the &lt;code&gt;convenience&lt;/code&gt; modifier placed before the &lt;code&gt;init&lt;/code&gt; keyword, separated by a space: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;convenience init(&lt;em&gt;parameters&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Initializer Delegation for Class Types&lt;/h4&gt;
&lt;p&gt;To simplify the relationships between designated and convenience initializers, Swift applies the following three rules for delegation calls between initializers: &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Rule 1&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;A designated initializer must call a designated initializer from its immediate superclass. &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Rule 2&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;A convenience initializer must call another initializer from the &lt;em&gt;same&lt;/em&gt; class. &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Rule 3&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;A convenience initializer must ultimately call a designated initializer. &lt;/p&gt;
&lt;p&gt;A simple way to remember this is: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Designated initializers must always delegate &lt;em&gt;up&lt;/em&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Convenience initializers must always delegate &lt;em&gt;across&lt;/em&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;These rules are illustrated in the figure below: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/initializerDelegation01_2x.png" src="http://muxuezi.github.io/posts/Art/initializerDelegation01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Here, the superclass has a single designated initializer and two convenience initializers. One convenience initializer calls another convenience initializer, which in turn calls the single designated initializer. This satisfies rules 2 and 3 from above. The superclass does not itself have a further superclass, and so rule 1 does not apply. &lt;/p&gt;
&lt;p&gt;The subclass in this figure has two designated initializers and one convenience initializer. The convenience initializer must call one of the two designated initializers, because it can only call another initializer from the same class. This satisfies rules 2 and 3 from above. Both designated initializers must call the single designated initializer from the superclass, to satisfy rule 1 from above. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;These rules don’t affect how users of your classes &lt;em&gt;create&lt;/em&gt; instances of each class. Any initializer in the diagram above can be used to create a fully-initialized instance of the class they belong to. The rules only affect how you write the class’s implementation. &lt;/p&gt;
&lt;p&gt;The figure below shows a more complex class hierarchy for four classes. It illustrates how the designated initializers in this hierarchy act as “funnel” points for class initialization, simplifying the interrelationships among classes in the chain: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/initializerDelegation02_2x.png" src="http://muxuezi.github.io/posts/Art/initializerDelegation02_2x.png"&gt;&lt;/p&gt;
&lt;h4&gt;Two-Phase Initialization&lt;/h4&gt;
&lt;p&gt;Class initialization in Swift is a two-phase process. In the first phase, each stored property is assigned an initial value by the class that introduced it. Once the initial state for every stored property has been determined, the second phase begins, and each class is given the opportunity to customize its stored properties further before the new instance is considered ready for use. &lt;/p&gt;
&lt;p&gt;The use of a two-phase initialization process makes initialization safe, while still giving complete flexibility to each class in a class hierarchy. Two-phase initialization prevents property values from being accessed before they are initialized, and prevents property values from being set to a different value by another initializer unexpectedly. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift’s two-phase initialization process is similar to initialization in Objective-C. The main difference is that during phase 1, Objective-C assigns zero or null values (such as &lt;code&gt;0&lt;/code&gt; or &lt;code&gt;nil&lt;/code&gt;) to every property. Swift’s initialization flow is more flexible in that it lets you set custom initial values, and can cope with types for which &lt;code&gt;0&lt;/code&gt; or &lt;code&gt;nil&lt;/code&gt; is not a valid default value. &lt;/p&gt;
&lt;p&gt;Swift’s compiler performs four helpful safety-checks to make sure that two-phase initialization is completed without error: &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Safety check 1&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;A designated initializer must ensure that all of the properties introduced by its class are initialized before it delegates up to a superclass initializer. &lt;/p&gt;
&lt;p&gt;As mentioned above, the memory for an object is only considered fully initialized once the initial state of all of its stored properties is known. In order for this rule to be satisfied, a designated initializer must make sure that all its own properties are initialized before it hands off up the chain. &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Safety check 2&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;A designated initializer must delegate up to a superclass initializer before assigning a value to an inherited property. If it doesn’t, the new value the designated initializer assigns will be overwritten by the superclass as part of its own initialization. &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Safety check 3&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;A convenience initializer must delegate to another initializer before assigning a value to &lt;em&gt;any&lt;/em&gt; property (including properties defined by the same class). If it doesn’t, the new value the convenience initializer assigns will be overwritten by its own class’s designated initializer. &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Safety check 4&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;An initializer cannot call any instance methods, read the values of any instance properties, or refer to &lt;code&gt;self&lt;/code&gt; as a value until after the first phase of initialization is complete. &lt;/p&gt;
&lt;p&gt;The class instance is not fully valid until the first phase ends. Properties can only be accessed, and methods can only be called, once the class instance is known to be valid at the end of the first phase. &lt;/p&gt;
&lt;p&gt;Here’s how two-phase initialization plays out, based on the four safety checks above: &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Phase 1&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;A designated or convenience initializer is called on a class. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Memory for a new instance of that class is allocated. The memory is not yet initialized. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A designated initializer for that class confirms that all stored properties introduced by that class have a value. The memory for these stored properties is now initialized. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The designated initializer hands off to a superclass initializer to perform the same task for its own stored properties. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;This continues up the class inheritance chain until the top of the chain is reached. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Once the top of the chain is reached, and the final class in the chain has ensured that all of its stored properties have a value, the instance’s memory is considered to be fully initialized, and phase 1 is complete. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Phase 2&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Working back down from the top of the chain, each designated initializer in the chain has the option to customize the instance further. Initializers are now able to access &lt;code&gt;self&lt;/code&gt; and can modify its properties, call its instance methods, and so on. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Finally, any convenience initializers in the chain have the option to customize the instance and to work with &lt;code&gt;self&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how phase 1 looks for an initialization call for a hypothetical subclass and superclass: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/twoPhaseInitialization01_2x.png" src="http://muxuezi.github.io/posts/Art/twoPhaseInitialization01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;In this example, initialization begins with a call to a convenience initializer on the subclass. This convenience initializer cannot yet modify any properties. It delegates across to a designated initializer from the same class. &lt;/p&gt;
&lt;p&gt;The designated initializer makes sure that all of the subclass’s properties have a value, as per safety check 1. It then calls a designated initializer on its superclass to continue the initialization up the chain. &lt;/p&gt;
&lt;p&gt;The superclass’s designated initializer makes sure that all of the superclass properties have a value. There are no further superclasses to initialize, and so no further delegation is needed. &lt;/p&gt;
&lt;p&gt;As soon as all properties of the superclass have an initial value, its memory is considered fully initialized, and Phase 1 is complete. &lt;/p&gt;
&lt;p&gt;Here’s how phase 2 looks for the same initialization call: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/twoPhaseInitialization02_2x.png" src="http://muxuezi.github.io/posts/Art/twoPhaseInitialization02_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The superclass’s designated initializer now has an opportunity to customize the instance further (although it does not have to). &lt;/p&gt;
&lt;p&gt;Once the superclass’s designated initializer is finished, the subclass’s designated initializer can perform additional customization (although again, it does not have to). &lt;/p&gt;
&lt;p&gt;Finally, once the subclass’s designated initializer is finished, the convenience initializer that was originally called can perform additional customization. &lt;/p&gt;
&lt;h4&gt;Initializer Inheritance and Overriding&lt;/h4&gt;
&lt;p&gt;Unlike subclasses in Objective-C, Swift subclasses do not inherit their superclass initializers by default. Swift’s approach prevents a situation in which a simple initializer from a superclass is inherited by a more specialized subclass and is used to create a new instance of the subclass that is not fully or correctly initialized. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Superclass initializers &lt;em&gt;are&lt;/em&gt; inherited in certain circumstances, but only when it is safe and appropriate to do so. For more information, see Automatic Initializer Inheritance below. &lt;/p&gt;
&lt;p&gt;If you want a custom subclass to present one or more of the same initializers as its superclass, you can provide a custom implementation of those initializers within the subclass. &lt;/p&gt;
&lt;p&gt;When you write a subclass initializer that matches a superclass &lt;em&gt;designated&lt;/em&gt; initializer, you are effectively providing an override of that designated initializer. Therefore, you must write the &lt;code&gt;override&lt;/code&gt; modifier before the subclass’s initializer definition. This is true even if you are overriding an automatically provided default initializer, as described in Default Initializers. &lt;/p&gt;
&lt;p&gt;As with an overridden property, method or subscript, the presence of the &lt;code&gt;override&lt;/code&gt; modifier prompts Swift to check that the superclass has a matching designated initializer to be overridden, and validates that the parameters for your overriding initializer have been specified as intended. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You always write the &lt;code&gt;override&lt;/code&gt; modifier when overriding a superclass designated initializer, even if your subclass’s implementation of the initializer is a convenience initializer. &lt;/p&gt;
&lt;p&gt;Conversely, if you write a subclass initializer that matches a superclass &lt;em&gt;convenience&lt;/em&gt; initializer, that superclass convenience initializer can never be called directly by your subclass, as per the rules described above in Initializer Delegation for Class Types. Therefore, your subclass is not (strictly speaking) providing an override of the superclass initializer. As a result, you do not write the &lt;code&gt;override&lt;/code&gt; modifier when providing a matching implementation of a superclass convenience initializer. &lt;/p&gt;
&lt;p&gt;The example below defines a base class called &lt;code&gt;Vehicle&lt;/code&gt;. This base class declares a stored property called &lt;code&gt;numberOfWheels&lt;/code&gt;, with a default &lt;code&gt;Int&lt;/code&gt; value of &lt;code&gt;0&lt;/code&gt;. The &lt;code&gt;numberOfWheels&lt;/code&gt; property is used by a computed property called &lt;code&gt;description&lt;/code&gt; to create a &lt;code&gt;String&lt;/code&gt; description of the vehicle’s characteristics: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Vehicle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var numberOfWheels = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var description: String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "\\(numberOfWheels) wheel(s)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Vehicle&lt;/code&gt; class provides a default value for its only stored property, and does not provide any custom initializers itself. As a result, it automatically receives a default initializer, as described in Default Initializers. The default initializer (when available) is always a designated initializer for a class, and can be used to create a new &lt;code&gt;Vehicle&lt;/code&gt; instance with a &lt;code&gt;numberOfWheels&lt;/code&gt; of &lt;code&gt;0&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let vehicle = Vehicle()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Vehicle: \\(vehicle.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Vehicle: 0 wheel(s)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The next example defines a subclass of &lt;code&gt;Vehicle&lt;/code&gt; called &lt;code&gt;Bicycle&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Bicycle: Vehicle {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfWheels = 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Bicycle&lt;/code&gt; subclass defines a custom designated initializer, &lt;code&gt;init()&lt;/code&gt;. This designated initializer matches a designated initializer from the superclass of &lt;code&gt;Bicycle&lt;/code&gt;, and so the &lt;code&gt;Bicycle&lt;/code&gt; version of this initializer is marked with the &lt;code&gt;override&lt;/code&gt; modifier. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;init()&lt;/code&gt; initializer for &lt;code&gt;Bicycle&lt;/code&gt; starts by calling &lt;code&gt;super.init()&lt;/code&gt;, which calls the default initializer for the &lt;code&gt;Bicycle&lt;/code&gt; class’s superclass, &lt;code&gt;Vehicle&lt;/code&gt;. This ensures that the &lt;code&gt;numberOfWheels&lt;/code&gt; inherited property is initialized by &lt;code&gt;Vehicle&lt;/code&gt; before &lt;code&gt;Bicycle&lt;/code&gt; has the opportunity to modify the property. After calling &lt;code&gt;super.init()&lt;/code&gt;, the original value of &lt;code&gt;numberOfWheels&lt;/code&gt; is replaced with a new value of &lt;code&gt;2&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If you create an instance of &lt;code&gt;Bicycle&lt;/code&gt;, you can call its inherited &lt;code&gt;description&lt;/code&gt; computed property to see how its &lt;code&gt;numberOfWheels&lt;/code&gt; property has been updated: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let bicycle = Bicycle()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Bicycle: \\(bicycle.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Bicycle: 2 wheel(s)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Subclasses can modify inherited variable properties during initialization, but can not modify inherited constant properties. &lt;/p&gt;
&lt;h4&gt;Automatic Initializer Inheritance&lt;/h4&gt;
&lt;p&gt;As mentioned above, subclasses do not inherit their superclass initializers by default. However, superclass initializers &lt;em&gt;are&lt;/em&gt; automatically inherited if certain conditions are met. In practice, this means that you do not need to write initializer overrides in many common scenarios, and can inherit your superclass initializers with minimal effort whenever it is safe to do so. &lt;/p&gt;
&lt;p&gt;Assuming that you provide default values for any new properties you introduce in a subclass, the following two rules apply: &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Rule 1&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;If your subclass doesn’t define any designated initializers, it automatically inherits all of its superclass designated initializers. &lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Rule 2&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;If your subclass provides an implementation of &lt;em&gt;all&lt;/em&gt; of its superclass designated initializers—either by inheriting them as per rule 1, or by providing a custom implementation as part of its definition—then it automatically inherits all of the superclass convenience initializers. &lt;/p&gt;
&lt;p&gt;These rules apply even if your subclass adds further convenience initializers. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;A subclass can implement a superclass designated initializer as a subclass convenience initializer as part of satisfying rule 2. &lt;/p&gt;
&lt;h4&gt;Designated and Convenience Initializers in Action&lt;/h4&gt;
&lt;p&gt;The following example shows designated initializers, convenience initializers, and automatic initializer inheritance in action. This example defines a hierarchy of three classes called &lt;code&gt;Food&lt;/code&gt;, &lt;code&gt;RecipeIngredient&lt;/code&gt;, and &lt;code&gt;ShoppingListItem&lt;/code&gt;, and demonstrates how their initializers interact. &lt;/p&gt;
&lt;p&gt;The base class in the hierarchy is called &lt;code&gt;Food&lt;/code&gt;, which is a simple class to encapsulate the name of a foodstuff. The &lt;code&gt;Food&lt;/code&gt; class introduces a single &lt;code&gt;String&lt;/code&gt; property called &lt;code&gt;name&lt;/code&gt; and provides two initializers for creating &lt;code&gt;Food&lt;/code&gt; instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Food {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;convenience init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.init(name: "[Unnamed]")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The figure below shows the initializer chain for the &lt;code&gt;Food&lt;/code&gt; class: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/initializersExample01_2x.png" src="http://muxuezi.github.io/posts/Art/initializersExample01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Classes do not have a default memberwise initializer, and so the &lt;code&gt;Food&lt;/code&gt; class provides a designated initializer that takes a single argument called &lt;code&gt;name&lt;/code&gt;. This initializer can be used to create a new &lt;code&gt;Food&lt;/code&gt; instance with a specific name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let namedMeat = Food(name: "Bacon")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// namedMeat's name is "Bacon"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;init(name: String)&lt;/code&gt; initializer from the &lt;code&gt;Food&lt;/code&gt; class is provided as a &lt;em&gt;designated&lt;/em&gt; initializer, because it ensures that all stored properties of a new &lt;code&gt;Food&lt;/code&gt; instance are fully initialized. The &lt;code&gt;Food&lt;/code&gt; class does not have a superclass, and so the &lt;code&gt;init(name: String)&lt;/code&gt; initializer does not need to call &lt;code&gt;super.init()&lt;/code&gt; to complete its initialization. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Food&lt;/code&gt; class also provides a &lt;em&gt;convenience&lt;/em&gt; initializer, &lt;code&gt;init()&lt;/code&gt;, with no arguments. The &lt;code&gt;init()&lt;/code&gt; initializer provides a default placeholder name for a new food by delegating across to the &lt;code&gt;Food&lt;/code&gt; class’s &lt;code&gt;init(name: String)&lt;/code&gt; with a &lt;code&gt;name&lt;/code&gt; value of &lt;code&gt;[Unnamed]&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let mysteryMeat = Food()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// mysteryMeat's name is "[Unnamed]"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The second class in the hierarchy is a subclass of &lt;code&gt;Food&lt;/code&gt; called &lt;code&gt;RecipeIngredient&lt;/code&gt;. The &lt;code&gt;RecipeIngredient&lt;/code&gt; class models an ingredient in a cooking recipe. It introduces an &lt;code&gt;Int&lt;/code&gt; property called &lt;code&gt;quantity&lt;/code&gt; (in addition to the &lt;code&gt;name&lt;/code&gt; property it inherits from &lt;code&gt;Food&lt;/code&gt;) and defines two initializers for creating &lt;code&gt;RecipeIngredient&lt;/code&gt; instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class RecipeIngredient: Food {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var quantity: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, quantity: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.quantity = quantity&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init(name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override convenience init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.init(name: name, quantity: 1)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The figure below shows the initializer chain for the &lt;code&gt;RecipeIngredient&lt;/code&gt; class: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/initializersExample02_2x.png" src="http://muxuezi.github.io/posts/Art/initializersExample02_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;RecipeIngredient&lt;/code&gt; class has a single designated initializer, &lt;code&gt;init(name: String, quantity: Int)&lt;/code&gt;, which can be used to populate all of the properties of a new &lt;code&gt;RecipeIngredient&lt;/code&gt; instance. This initializer starts by assigning the passed &lt;code&gt;quantity&lt;/code&gt; argument to the &lt;code&gt;quantity&lt;/code&gt; property, which is the only new property introduced by &lt;code&gt;RecipeIngredient&lt;/code&gt;. After doing so, the initializer delegates up to the &lt;code&gt;init(name: String)&lt;/code&gt; initializer of the &lt;code&gt;Food&lt;/code&gt; class. This process satisfies safety check 1 from Two-Phase Initialization above. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;RecipeIngredient&lt;/code&gt; also defines a convenience initializer, &lt;code&gt;init(name: String)&lt;/code&gt;, which is used to create a &lt;code&gt;RecipeIngredient&lt;/code&gt; instance by name alone. This convenience initializer assumes a quantity of &lt;code&gt;1&lt;/code&gt; for any &lt;code&gt;RecipeIngredient&lt;/code&gt; instance that is created without an explicit quantity. The definition of this convenience initializer makes &lt;code&gt;RecipeIngredient&lt;/code&gt; instances quicker and more convenient to create, and avoids code duplication when creating several single-quantity &lt;code&gt;RecipeIngredient&lt;/code&gt; instances. This convenience initializer simply delegates across to the class’s designated initializer, passing in a &lt;code&gt;quantity&lt;/code&gt; value of &lt;code&gt;1&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;init(name: String)&lt;/code&gt; convenience initializer provided by &lt;code&gt;RecipeIngredient&lt;/code&gt; takes the same parameters as the &lt;code&gt;init(name: String)&lt;/code&gt; &lt;em&gt;designated&lt;/em&gt; initializer from &lt;code&gt;Food&lt;/code&gt;. Because this convenience initializer overrides a designated initializer from its superclass, it must be marked with the &lt;code&gt;override&lt;/code&gt; modifier (as described in Initializer Inheritance and Overriding). &lt;/p&gt;
&lt;p&gt;Even though &lt;code&gt;RecipeIngredient&lt;/code&gt; provides the &lt;code&gt;init(name: String)&lt;/code&gt; initializer as a convenience initializer, &lt;code&gt;RecipeIngredient&lt;/code&gt; has nonetheless provided an implementation of all of its superclass’s designated initializers. Therefore, &lt;code&gt;RecipeIngredient&lt;/code&gt; automatically inherits all of its superclass’s convenience initializers too. &lt;/p&gt;
&lt;p&gt;In this example, the superclass for &lt;code&gt;RecipeIngredient&lt;/code&gt; is &lt;code&gt;Food&lt;/code&gt;, which has a single convenience initializer called &lt;code&gt;init()&lt;/code&gt;. This initializer is therefore inherited by &lt;code&gt;RecipeIngredient&lt;/code&gt;. The inherited version of &lt;code&gt;init()&lt;/code&gt; functions in exactly the same way as the &lt;code&gt;Food&lt;/code&gt; version, except that it delegates to the &lt;code&gt;RecipeIngredient&lt;/code&gt; version of &lt;code&gt;init(name: String)&lt;/code&gt; rather than the &lt;code&gt;Food&lt;/code&gt; version. &lt;/p&gt;
&lt;p&gt;All three of these initializers can be used to create new &lt;code&gt;RecipeIngredient&lt;/code&gt; instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let oneMysteryItem = RecipeIngredient()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let oneBacon = RecipeIngredient(name: "Bacon")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let sixEggs = RecipeIngredient(name: "Eggs", quantity: 6)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The third and final class in the hierarchy is a subclass of &lt;code&gt;RecipeIngredient&lt;/code&gt; called &lt;code&gt;ShoppingListItem&lt;/code&gt;. The &lt;code&gt;ShoppingListItem&lt;/code&gt; class models a recipe ingredient as it appears in a shopping list. &lt;/p&gt;
&lt;p&gt;Every item in the shopping list starts out as “unpurchased”. To represent this fact, &lt;code&gt;ShoppingListItem&lt;/code&gt; introduces a Boolean property called &lt;code&gt;purchased&lt;/code&gt;, with a default value of &lt;code&gt;false&lt;/code&gt;. &lt;code&gt;ShoppingListItem&lt;/code&gt; also adds a computed &lt;code&gt;description&lt;/code&gt; property, which provides a textual description of a &lt;code&gt;ShoppingListItem&lt;/code&gt; instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class ShoppingListItem: RecipeIngredient {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var purchased = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var description: String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var output = "\\(quantity) x \\(name)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;output += purchased ? " ✔" : " ✘"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return output&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;&lt;code&gt;ShoppingListItem&lt;/code&gt; does not define an initializer to provide an initial value for &lt;code&gt;purchased&lt;/code&gt;, because items in a shopping list (as modeled here) always start out unpurchased. &lt;/p&gt;
&lt;p&gt;Because it provides a default value for all of the properties it introduces and does not define any initializers itself, &lt;code&gt;ShoppingListItem&lt;/code&gt; automatically inherits &lt;em&gt;all&lt;/em&gt; of the designated and convenience initializers from its superclass. &lt;/p&gt;
&lt;p&gt;The figure below shows the overall initializer chain for all three classes: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/initializersExample03_2x.png" src="http://muxuezi.github.io/posts/Art/initializersExample03_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;You can use all three of the inherited initializers to create a new &lt;code&gt;ShoppingListItem&lt;/code&gt; instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var breakfastList = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ShoppingListItem(),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ShoppingListItem(name: "Bacon"),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ShoppingListItem(name: "Eggs", quantity: 6),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;breakfastList[0].name = "Orange juice"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;breakfastList[0].purchased = true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for item in breakfastList {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(item.description)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 1 x Orange juice ✔&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 1 x Bacon ✘&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 6 x Eggs ✘&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, a new array called &lt;code&gt;breakfastList&lt;/code&gt; is created from an array literal containing three new &lt;code&gt;ShoppingListItem&lt;/code&gt; instances. The type of the array is inferred to be &lt;code&gt;[ShoppingListItem]&lt;/code&gt;. After the array is created, the name of the &lt;code&gt;ShoppingListItem&lt;/code&gt; at the start of the array is changed from &lt;code&gt;"[Unnamed]"&lt;/code&gt; to &lt;code&gt;"Orange juice"&lt;/code&gt; and it is marked as having been purchased. Printing the description of each item in the array shows that their default states have been set as expected. &lt;/p&gt;
&lt;h3&gt;Failable Initializers&lt;/h3&gt;
&lt;p&gt;It is sometimes useful to define a class, structure, or enumeration for which initialization can fail. This failure might be triggered by invalid initialization parameter values, the absence of a required external resource, or some other condition that prevents initialization from succeeding. &lt;/p&gt;
&lt;p&gt;To cope with initialization conditions that can fail, define one or more &lt;em&gt;failable initializers&lt;/em&gt; as part of a class, structure, or enumeration definition. You write a failable initializer by placing a question mark after the &lt;code&gt;init&lt;/code&gt; keyword (&lt;code&gt;init?&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You cannot define a failable and a nonfailable initializer with the same parameter types and names. &lt;/p&gt;
&lt;p&gt;A failable initializer creates an &lt;em&gt;optional&lt;/em&gt; value of the type it initializes. You write &lt;code&gt;return nil&lt;/code&gt; within a failable initializer to indicate a point at which initialization failure can be triggered. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Strictly speaking, initializers do not return a value. Rather, their role is to ensure that &lt;code&gt;self&lt;/code&gt; is fully and correctly initialized by the time that initialization ends. Although you write &lt;code&gt;return nil&lt;/code&gt; to trigger an initialization failure, you do not use the &lt;code&gt;return&lt;/code&gt; keyword to indicate initialization success. &lt;/p&gt;
&lt;p&gt;The example below defines a structure called &lt;code&gt;Animal&lt;/code&gt;, with a constant &lt;code&gt;String&lt;/code&gt; property called &lt;code&gt;species&lt;/code&gt;. The &lt;code&gt;Animal&lt;/code&gt; structure also defines a failable initializer with a single parameter called &lt;code&gt;species&lt;/code&gt;. This initializer checks if the &lt;code&gt;species&lt;/code&gt; value passed to the initializer is an empty string. If an empty string is found, an initialization failure is triggered. Otherwise, the &lt;code&gt;species&lt;/code&gt; property’s value is set, and initialization succeeds: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Animal {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let species: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init?(species: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if species.isEmpty { return nil }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.species = species&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can use this failable initializer to try to initialize a new &lt;code&gt;Animal&lt;/code&gt; instance and to check if initialization succeeded: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someCreature = Animal(species: "Giraffe")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someCreature is of type Animal?, not Animal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let giraffe = someCreature {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("An animal was initialized with a species of \\(giraffe.species)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "An animal was initialized with a species of Giraffe"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you pass an empty string value to the failable initializer’s &lt;code&gt;species&lt;/code&gt; parameter, the initializer triggers an initialization failure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let anonymousCreature = Animal(species: "")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// anonymousCreature is of type Animal?, not Animal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if anonymousCreature == nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The anonymous creature could not be initialized")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The anonymous creature could not be initialized"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Checking for an empty string value (such as &lt;code&gt;""&lt;/code&gt; rather than &lt;code&gt;"Giraffe"&lt;/code&gt;) is not the same as checking for &lt;code&gt;nil&lt;/code&gt; to indicate the absence of an &lt;em&gt;optional&lt;/em&gt; &lt;code&gt;String&lt;/code&gt; value. In the example above, an empty string (&lt;code&gt;""&lt;/code&gt;) is a valid, non-optional &lt;code&gt;String&lt;/code&gt;. However, it is not appropriate for an animal to have an empty string as the value of its &lt;code&gt;species&lt;/code&gt; property. To model this restriction, the failable initializer triggers an initialization failure if an empty string is found. &lt;/p&gt;
&lt;h4&gt;Failable Initializers for Enumerations&lt;/h4&gt;
&lt;p&gt;You can use a failable initializer to select an appropriate enumeration member based on one or more parameters. The initializer can then fail if the provided parameters do not match an appropriate enumeration member. &lt;/p&gt;
&lt;p&gt;The example below defines an enumeration called &lt;code&gt;TemperatureUnit&lt;/code&gt;, with three possible states (&lt;code&gt;Kelvin&lt;/code&gt;, &lt;code&gt;Celsius&lt;/code&gt;, and &lt;code&gt;Fahrenheit&lt;/code&gt;). A failable initializer is used to find an appropriate enumeration member for a &lt;code&gt;Character&lt;/code&gt; value representing a temperature symbol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum TemperatureUnit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Kelvin, Celsius, Fahrenheit&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init?(symbol: Character) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch symbol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "K":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = .Kelvin&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "C":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = .Celsius&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case "F":&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = .Fahrenheit&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can use this failable initializer to choose an appropriate enumeration member for the three possible states and to cause initialization to fail if the parameter does not match one of these states: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let fahrenheitUnit = TemperatureUnit(symbol: "F")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if fahrenheitUnit != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("This is a defined temperature unit, so initialization succeeded.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "This is a defined temperature unit, so initialization succeeded."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let unknownUnit = TemperatureUnit(symbol: "X")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if unknownUnit == nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("This is not a defined temperature unit, so initialization failed.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "This is not a defined temperature unit, so initialization failed."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Failable Initializers for Enumerations with Raw Values&lt;/h4&gt;
&lt;p&gt;Enumerations with raw values automatically receive a failable initializer, &lt;code&gt;init?(rawValue:)&lt;/code&gt;, that takes a parameter called &lt;code&gt;rawValue&lt;/code&gt; of the appropriate raw-value type and selects a matching enumeration member if one is found, or triggers an initialization failure if no matching value exists. &lt;/p&gt;
&lt;p&gt;You can rewrite the &lt;code&gt;TemperatureUnit&lt;/code&gt; example from above to use raw values of type &lt;code&gt;Character&lt;/code&gt; and to take advantage of the &lt;code&gt;init?(rawValue:)&lt;/code&gt; initializer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum TemperatureUnit: Character {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Kelvin = "K", Celsius = "C", Fahrenheit = "F"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let fahrenheitUnit = TemperatureUnit(rawValue: "F")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if fahrenheitUnit != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("This is a defined temperature unit, so initialization succeeded.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "This is a defined temperature unit, so initialization succeeded."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let unknownUnit = TemperatureUnit(rawValue: "X")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if unknownUnit == nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("This is not a defined temperature unit, so initialization failed.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "This is not a defined temperature unit, so initialization failed."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Failable Initializers for Classes&lt;/h4&gt;
&lt;p&gt;A failable initializer for a value type (that is, a structure or enumeration) can trigger an initialization failure at any point within its initializer implementation. In the &lt;code&gt;Animal&lt;/code&gt; structure example above, the initializer triggers an initialization failure at the very start of its implementation, before the &lt;code&gt;species&lt;/code&gt; property has been set. &lt;/p&gt;
&lt;p&gt;For classes, however, a failable initializer can trigger an initialization failure only &lt;em&gt;after&lt;/em&gt; all stored properties introduced by that class have been set to an initial value and any initializer delegation has taken place. &lt;/p&gt;
&lt;p&gt;The example below shows how you can use an implicitly unwrapped optional property to satisfy this requirement within a failable class initializer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Product {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init?(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if name.isEmpty { return nil }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Product&lt;/code&gt; class defined above is very similar to the &lt;code&gt;Animal&lt;/code&gt; structure seen earlier. The &lt;code&gt;Product&lt;/code&gt; class has a constant &lt;code&gt;name&lt;/code&gt; property that must not be allowed to take an empty string value. To enforce this requirement, the &lt;code&gt;Product&lt;/code&gt; class uses a failable initializer to ensure that the property’s value is non-empty before allowing initialization to succeed. &lt;/p&gt;
&lt;p&gt;However, &lt;code&gt;Product&lt;/code&gt; is a class, not a structure. This means that unlike &lt;code&gt;Animal&lt;/code&gt;, any failable initializer for the &lt;code&gt;Product&lt;/code&gt; class must provide an initial value for the &lt;code&gt;name&lt;/code&gt; property &lt;em&gt;before&lt;/em&gt; triggering an initialization failure. &lt;/p&gt;
&lt;p&gt;In the example above, the &lt;code&gt;name&lt;/code&gt; property of the &lt;code&gt;Product&lt;/code&gt; class is defined as having an implicitly unwrapped optional string type (&lt;code&gt;String!&lt;/code&gt;). Because it is of an optional type, this means that the &lt;code&gt;name&lt;/code&gt; property has a default value of &lt;code&gt;nil&lt;/code&gt; before it is assigned a specific value during initialization. This default value of &lt;code&gt;nil&lt;/code&gt; in turn means that all of the properties introduced by the &lt;code&gt;Product&lt;/code&gt; class have a valid initial value. As a result, the failable initializer for &lt;code&gt;Product&lt;/code&gt; can trigger an initialization failure at the start of the initializer if it is passed an empty string, &lt;em&gt;before&lt;/em&gt; assigning a specific value to the &lt;code&gt;name&lt;/code&gt; property within the initializer. &lt;/p&gt;
&lt;p&gt;Because the &lt;code&gt;name&lt;/code&gt; property is a constant, you can be confident that it will always contain a non-&lt;code&gt;nil&lt;/code&gt; value if initialization succeeds. Even though it is defined with an implicitly unwrapped optional type, you can always access its implicitly unwrapped value with confidence, without needing to check for a value of &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let bowTie = Product(name: "bow tie") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// no need to check if bowTie.name == nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The product's name is \\(bowTie.name)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The product's name is bow tie"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Propagation of Initialization Failure&lt;/h4&gt;
&lt;p&gt;A failable initializer of a class, structure, or enumeration can delegate across to another failable initializer from the same class, structure, or enumeration. Similarly, a subclass failable initializer can delegate up to a superclass failable initializer. &lt;/p&gt;
&lt;p&gt;In either case, if you delegate to another initializer that causes initialization to fail, the entire initialization process fails immediately, and no further initialization code is executed. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;A failable initializer can also delegate to a nonfailable initializer. Use this approach if you need to add a potential failure state to an existing initialization process that does not otherwise fail. &lt;/p&gt;
&lt;p&gt;The example below defines a subclass of &lt;code&gt;Product&lt;/code&gt; called &lt;code&gt;CartItem&lt;/code&gt;. The &lt;code&gt;CartItem&lt;/code&gt; class models an item in an online shopping cart. &lt;code&gt;CartItem&lt;/code&gt; introduces a stored constant property called &lt;code&gt;quantity&lt;/code&gt; and ensures that this property always has a value of at least &lt;code&gt;1&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class CartItem: Product {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let quantity: Int!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init?(name: String, quantity: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init(name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if quantity &amp;lt; 1 { return nil }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.quantity = quantity&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;quantity&lt;/code&gt; property has an implicitly unwrapped integer type (&lt;code&gt;Int!&lt;/code&gt;). As with the &lt;code&gt;name&lt;/code&gt; property of the &lt;code&gt;Product&lt;/code&gt; class, this means that the &lt;code&gt;quantity&lt;/code&gt; property has a default value of &lt;code&gt;nil&lt;/code&gt; before it is assigned a specific value during initialization. &lt;/p&gt;
&lt;p&gt;The failable initializer for &lt;code&gt;CartItem&lt;/code&gt; starts by delegating up to the &lt;code&gt;init(name:)&lt;/code&gt; initializer from its superclass, &lt;code&gt;Product&lt;/code&gt;. This satisfies the requirement that a failable initializer must always perform initializer delegation before triggering an initialization failure. &lt;/p&gt;
&lt;p&gt;If the superclass initialization fails because of an empty &lt;code&gt;name&lt;/code&gt; value, the entire initialization process fails immediately and no further initialization code is executed. If the superclass initialization succeeds, the &lt;code&gt;CartItem&lt;/code&gt; initializer validates that it has received a &lt;code&gt;quantity&lt;/code&gt; value of &lt;code&gt;1&lt;/code&gt; or more. &lt;/p&gt;
&lt;p&gt;If you create a &lt;code&gt;CartItem&lt;/code&gt; instance with a non-empty name and a quantity of &lt;code&gt;1&lt;/code&gt; or more, initialization succeeds: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let twoSocks = CartItem(name: "sock", quantity: 2) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Item: \\(twoSocks.name), quantity: \\(twoSocks.quantity)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Item: sock, quantity: 2"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you try to create a &lt;code&gt;CartItem&lt;/code&gt; instance with a &lt;code&gt;quantity&lt;/code&gt; value of &lt;code&gt;0&lt;/code&gt;, the &lt;code&gt;CartItem&lt;/code&gt; initializer causes initialization to fail: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let zeroShirts = CartItem(name: "shirt", quantity: 0) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Item: \\(zeroShirts.name), quantity: \\(zeroShirts.quantity)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to initialize zero shirts")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Unable to initialize zero shirts"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Similarly, if you try to create a &lt;code&gt;CartItem&lt;/code&gt; instance with an empty &lt;code&gt;name&lt;/code&gt; value, the superclass &lt;code&gt;Product&lt;/code&gt; initializer causes initialization to fail: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let oneUnnamed = CartItem(name: "", quantity: 1) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Item: \\(oneUnnamed.name), quantity: \\(oneUnnamed.quantity)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to initialize one unnamed product")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Unable to initialize one unnamed product"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Overriding a Failable Initializer&lt;/h4&gt;
&lt;p&gt;You can override a superclass failable initializer in a subclass, just like any other initializer. Alternatively, you can override a superclass failable initializer with a subclass &lt;em&gt;non&lt;/em&gt;-failable initializer. This enables you to define a subclass for which initialization cannot fail, even though initialization of the superclass is allowed to fail. &lt;/p&gt;
&lt;p&gt;Note that if you override a failable superclass initializer with a nonfailable subclass initializer, the subclass initializer cannot delegate up to the superclass initializer. A nonfailable initializer can never delegate to a failable initializer. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You can override a failable initializer with a nonfailable initializer but not the other way around. &lt;/p&gt;
&lt;p&gt;The example below defines a class called &lt;code&gt;Document&lt;/code&gt;. This class models a document that can be initialized with a &lt;code&gt;name&lt;/code&gt; property that is either a non-empty string value or &lt;code&gt;nil&lt;/code&gt;, but cannot be an empty string: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Document {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this initializer creates a document with a nil name value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init() {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this initializer creates a document with a non-empty name value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init?(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if name.isEmpty { return nil }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The next example defines a subclass of &lt;code&gt;Document&lt;/code&gt; called &lt;code&gt;AutomaticallyNamedDocument&lt;/code&gt;. The &lt;code&gt;AutomaticallyNamedDocument&lt;/code&gt; subclass overrides both of the designated initializers introduced by &lt;code&gt;Document&lt;/code&gt;. These overrides ensure that an &lt;code&gt;AutomaticallyNamedDocument&lt;/code&gt; instance has an initial &lt;code&gt;name&lt;/code&gt; value of &lt;code&gt;"[Untitled]"&lt;/code&gt; if the instance is initialized without a name, or if an empty string is passed to the &lt;code&gt;init(name:)&lt;/code&gt; initializer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class AutomaticallyNamedDocument: Document {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = "[Untitled]"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if name.isEmpty {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = "[Untitled]"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;AutomaticallyNamedDocument&lt;/code&gt; overrides its superclass’s failable &lt;code&gt;init?(name:)&lt;/code&gt; initializer with a nonfailable &lt;code&gt;init(name:)&lt;/code&gt; initializer. Because &lt;code&gt;AutomaticallyNamedDocument&lt;/code&gt; copes with the empty string case in a different way than its superclass, its initializer does not need to fail, and so it provides a nonfailable version of the initializer instead. &lt;/p&gt;
&lt;h4&gt;The init! Failable Initializer&lt;/h4&gt;
&lt;p&gt;You typically define a failable initializer that creates an optional instance of the appropriate type by placing a question mark after the &lt;code&gt;init&lt;/code&gt; keyword (&lt;code&gt;init?&lt;/code&gt;). Alternatively, you can define a failable initializer that creates an implicitly unwrapped optional instance of the appropriate type. Do this by placing an exclamation mark after the &lt;code&gt;init&lt;/code&gt; keyword (&lt;code&gt;init!&lt;/code&gt;) instead of a question mark. &lt;/p&gt;
&lt;p&gt;You can delegate from &lt;code&gt;init?&lt;/code&gt; to &lt;code&gt;init!&lt;/code&gt; and vice versa, and you can override &lt;code&gt;init?&lt;/code&gt; with &lt;code&gt;init!&lt;/code&gt; and vice versa. You can also delegate from &lt;code&gt;init&lt;/code&gt; to &lt;code&gt;init!&lt;/code&gt;, although doing so will trigger an assertion if the &lt;code&gt;init!&lt;/code&gt; initializer causes initialization to fail. &lt;/p&gt;
&lt;h3&gt;Required Initializers&lt;/h3&gt;
&lt;p&gt;Write the &lt;code&gt;required&lt;/code&gt; modifier before the definition of a class initializer to indicate that every subclass of the class must implement that initializer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;required init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// initializer implementation goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You must also write the &lt;code&gt;required&lt;/code&gt; modifier before every subclass implementation of a required initializer, to indicate that the initializer requirement applies to further subclasses in the chain. You do not write the &lt;code&gt;override&lt;/code&gt; modifier when overriding a required designated initializer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeSubclass: SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;required init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// subclass implementation of the required initializer goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You do not have to provide an explicit implementation of a required initializer if you can satisfy the requirement with an inherited initializer. &lt;/p&gt;
&lt;h3&gt;Setting a Default Property Value with a Closure or Function&lt;/h3&gt;
&lt;p&gt;If a stored property’s default value requires some customization or setup, you can use a closure or global function to provide a customized default value for that property. Whenever a new instance of the type that the property belongs to is initialized, the closure or function is called, and its return value is assigned as the property’s default value. &lt;/p&gt;
&lt;p&gt;These kinds of closures or functions typically create a temporary value of the same type as the property, tailor that value to represent the desired initial state, and then return that temporary value to be used as the property’s default value. &lt;/p&gt;
&lt;p&gt;Here’s a skeleton outline of how a closure can be used to provide a default property value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let someProperty: SomeType = {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// create a default value for someProperty inside this closure&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someValue must be of the same type as SomeType&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return someValue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the closure’s end curly brace is followed by an empty pair of parentheses. This tells Swift to execute the closure immediately. If you omit these parentheses, you are trying to assign the closure itself to the property, and not the return value of the closure. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you use a closure to initialize a property, remember that the rest of the instance has not yet been initialized at the point that the closure is executed. This means that you cannot access any other property values from within your closure, even if those properties have default values. You also cannot use the implicit &lt;code&gt;self&lt;/code&gt; property, or call any of the instance’s methods. &lt;/p&gt;
&lt;p&gt;The example below defines a structure called &lt;code&gt;Checkerboard&lt;/code&gt;, which models a board for the game of &lt;em&gt;Checkers&lt;/em&gt; (also known as &lt;em&gt;Draughts&lt;/em&gt;): &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/checkersBoard_2x.png" src="http://muxuezi.github.io/posts/Art/checkersBoard_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The game of &lt;em&gt;Checkers&lt;/em&gt; is played on a ten-by-ten board, with alternating black and white squares. To represent this game board, the &lt;code&gt;Checkerboard&lt;/code&gt; structure has a single property called &lt;code&gt;boardColors&lt;/code&gt;, which is an array of 100 &lt;code&gt;Bool&lt;/code&gt; values. A value of &lt;code&gt;true&lt;/code&gt; in the array represents a black square and a value of &lt;code&gt;false&lt;/code&gt; represents a white square. The first item in the array represents the top left square on the board and the last item in the array represents the bottom right square on the board. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;boardColors&lt;/code&gt; array is initialized with a closure to set up its color values: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Checkerboard {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let boardColors: [Bool] = {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var temporaryBoard = [Bool]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var isBlack = false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for i in 1...10 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for j in 1...10 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;temporaryBoard.append(isBlack)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;isBlack = !isBlack&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;isBlack = !isBlack&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return temporaryBoard&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func squareIsBlackAtRow(row: Int, column: Int) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return boardColors[(row * 10) + column]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Whenever a new &lt;code&gt;Checkerboard&lt;/code&gt; instance is created, the closure is executed, and the default value of &lt;code&gt;boardColors&lt;/code&gt; is calculated and returned. The closure in the example above calculates and sets the appropriate color for each square on the board in a temporary array called &lt;code&gt;temporaryBoard&lt;/code&gt;, and returns this temporary array as the closure’s return value once its setup is complete. The returned array value is stored in &lt;code&gt;boardColors&lt;/code&gt; and can be queried with the &lt;code&gt;squareIsBlackAtRow&lt;/code&gt; utility function: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let board = Checkerboard()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(board.squareIsBlackAtRow(0, column: 1))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "true"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(board.squareIsBlackAtRow(9, column: 9))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "false"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Deinitialization&lt;/h2&gt;
&lt;p&gt;A &lt;em&gt;deinitializer&lt;/em&gt; is called immediately before a class instance is deallocated. You write deinitializers with the &lt;code&gt;deinit&lt;/code&gt; keyword, similar to how initializers are written with the &lt;code&gt;init&lt;/code&gt; keyword. Deinitializers are only available on class types. &lt;/p&gt;
&lt;h3&gt;How Deinitialization Works&lt;/h3&gt;
&lt;p&gt;Swift automatically deallocates your instances when they are no longer needed, to free up resources. Swift handles the memory management of instances through &lt;em&gt;automatic reference counting&lt;/em&gt; (&lt;em&gt;ARC&lt;/em&gt;), as described in Automatic Reference Counting. Typically you don’t need to perform manual clean-up when your instances are deallocated. However, when you are working with your own resources, you might need to perform some additional clean-up yourself. For example, if you create a custom class to open a file and write some data to it, you might need to close the file before the class instance is deallocated. &lt;/p&gt;
&lt;p&gt;Class definitions can have at most one deinitializer per class. The deinitializer does not take any parameters and is written without parentheses: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;deinit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// perform the deinitialization&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Deinitializers are called automatically, just before instance deallocation takes place. You are not allowed to call a deinitializer yourself. Superclass deinitializers are inherited by their subclasses, and the superclass deinitializer is called automatically at the end of a subclass deinitializer implementation. Superclass deinitializers are always called, even if a subclass does not provide its own deinitializer. &lt;/p&gt;
&lt;p&gt;Because an instance is not deallocated until after its deinitializer is called, a deinitializer can access all properties of the instance it is called on and can modify its behavior based on those properties (such as looking up the name of a file that needs to be closed). &lt;/p&gt;
&lt;h3&gt;Deinitializers in Action&lt;/h3&gt;
&lt;p&gt;Here’s an example of a deinitializer in action. This example defines two new types, &lt;code&gt;Bank&lt;/code&gt; and &lt;code&gt;Player&lt;/code&gt;, for a simple game. The &lt;code&gt;Bank&lt;/code&gt; structure manages a made-up currency, which can never have more than 10,000 coins in circulation. There can only ever be one &lt;code&gt;Bank&lt;/code&gt; in the game, and so the &lt;code&gt;Bank&lt;/code&gt; is implemented as a structure with static properties and methods to store and manage its current state: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Bank {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static var coinsInBank = 10_000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static func vendCoins(var numberOfCoinsToVend: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfCoinsToVend = min(numberOfCoinsToVend, coinsInBank)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;coinsInBank -= numberOfCoinsToVend&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return numberOfCoinsToVend&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;static func receiveCoins(coins: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;coinsInBank += coins&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;Bank&lt;/code&gt; keeps track of the current number of coins it holds with its &lt;code&gt;coinsInBank&lt;/code&gt; property. It also offers two methods—&lt;code&gt;vendCoins&lt;/code&gt; and &lt;code&gt;receiveCoins&lt;/code&gt;—to handle the distribution and collection of coins. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;vendCoins&lt;/code&gt; checks that there are enough coins in the bank before distributing them. If there are not enough coins, &lt;code&gt;Bank&lt;/code&gt; returns a smaller number than the number that was requested (and returns zero if no coins are left in the bank). &lt;code&gt;vendCoins&lt;/code&gt; declares &lt;code&gt;numberOfCoinsToVend&lt;/code&gt; as a variable parameter, so that the number can be modified within the method’s body without the need to declare a new variable. It returns an integer value to indicate the actual number of coins that were provided. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;receiveCoins&lt;/code&gt; method simply adds the received number of coins back into the bank’s coin store. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Player&lt;/code&gt; class describes a player in the game. Each player has a certain number of coins stored in their purse at any time. This is represented by the player’s &lt;code&gt;coinsInPurse&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Player {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var coinsInPurse: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(coins: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;coinsInPurse = Bank.vendCoins(coins)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func winCoins(coins: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;coinsInPurse += Bank.vendCoins(coins)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Bank.receiveCoins(coinsInPurse)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Each &lt;code&gt;Player&lt;/code&gt; instance is initialized with a starting allowance of a specified number of coins from the bank during initialization, although a &lt;code&gt;Player&lt;/code&gt; instance may receive fewer than that number if not enough coins are available. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Player&lt;/code&gt; class defines a &lt;code&gt;winCoins&lt;/code&gt; method, which retrieves a certain number of coins from the bank and adds them to the player’s purse. The &lt;code&gt;Player&lt;/code&gt; class also implements a deinitializer, which is called just before a &lt;code&gt;Player&lt;/code&gt; instance is deallocated. Here, the deinitializer simply returns all of the player’s coins to the bank: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var playerOne: Player? = Player(coins: 100)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("A new player has joined the game with \\(playerOne!.coinsInPurse) coins")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "A new player has joined the game with 100 coins"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("There are now \\(Bank.coinsInBank) coins left in the bank")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "There are now 9900 coins left in the bank"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A new &lt;code&gt;Player&lt;/code&gt; instance is created, with a request for 100 coins if they are available. This &lt;code&gt;Player&lt;/code&gt; instance is stored in an optional &lt;code&gt;Player&lt;/code&gt; variable called &lt;code&gt;playerOne&lt;/code&gt;. An optional variable is used here, because players can leave the game at any point. The optional lets you track whether there is currently a player in the game. &lt;/p&gt;
&lt;p&gt;Because &lt;code&gt;playerOne&lt;/code&gt; is an optional, it is qualified with an exclamation mark (&lt;code&gt;!&lt;/code&gt;) when its &lt;code&gt;coinsInPurse&lt;/code&gt; property is accessed to print its default number of coins, and whenever its &lt;code&gt;winCoins&lt;/code&gt; method is called: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;playerOne!.winCoins(2_000)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("PlayerOne won 2000 coins &amp;amp; now has \\(playerOne!.coinsInPurse) coins")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "PlayerOne won 2000 coins &amp;amp; now has 2100 coins"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The bank now only has \\(Bank.coinsInBank) coins left")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The bank now only has 7900 coins left"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here, the player has won 2,000 coins. The player’s purse now contains 2,100 coins, and the bank has only 7,900 coins left. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;playerOne = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("PlayerOne has left the game")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "PlayerOne has left the game"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The bank now has \\(Bank.coinsInBank) coins")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The bank now has 10000 coins"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The player has now left the game. This is indicated by setting the optional &lt;code&gt;playerOne&lt;/code&gt; variable to &lt;code&gt;nil&lt;/code&gt;, meaning “no &lt;code&gt;Player&lt;/code&gt; instance.” At the point that this happens, the &lt;code&gt;playerOne&lt;/code&gt; variable’s reference to the &lt;code&gt;Player&lt;/code&gt; instance is broken. No other properties or variables are still referring to the &lt;code&gt;Player&lt;/code&gt; instance, and so it is deallocated in order to free up its memory. Just before this happens, its deinitializer is called automatically, and its coins are returned to the bank. &lt;/p&gt;
&lt;h2&gt;Automatic Reference Counting&lt;/h2&gt;
&lt;p&gt;Swift uses &lt;em&gt;Automatic Reference Counting&lt;/em&gt; (ARC) to track and manage your app’s memory usage. In most cases, this means that memory management “just works” in Swift, and you do not need to think about memory management yourself. ARC automatically frees up the memory used by class instances when those instances are no longer needed. &lt;/p&gt;
&lt;p&gt;However, in a few cases ARC requires more information about the relationships between parts of your code in order to manage memory for you. This chapter describes those situations and shows how you enable ARC to manage all of your app’s memory. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Reference counting only applies to instances of classes. Structures and enumerations are value types, not reference types, and are not stored and passed by reference. &lt;/p&gt;
&lt;h3&gt;How ARC Works&lt;/h3&gt;
&lt;p&gt;Every time you create a new instance of a class, ARC allocates a chunk of memory to store information about that instance. This memory holds information about the type of the instance, together with the values of any stored properties associated with that instance. &lt;/p&gt;
&lt;p&gt;Additionally, when an instance is no longer needed, ARC frees up the memory used by that instance so that the memory can be used for other purposes instead. This ensures that class instances do not take up space in memory when they are no longer needed. &lt;/p&gt;
&lt;p&gt;However, if ARC were to deallocate an instance that was still in use, it would no longer be possible to access that instance’s properties, or call that instance’s methods. Indeed, if you tried to access the instance, your app would most likely crash. &lt;/p&gt;
&lt;p&gt;To make sure that instances don’t disappear while they are still needed, ARC tracks how many properties, constants, and variables are currently referring to each class instance. ARC will not deallocate an instance as long as at least one active reference to that instance still exists. &lt;/p&gt;
&lt;p&gt;To make this possible, whenever you assign a class instance to a property, constant, or variable, that property, constant, or variable makes a &lt;em&gt;strong reference&lt;/em&gt; to the instance. The reference is called a “strong“ reference because it keeps a firm hold on that instance, and does not allow it to be deallocated for as long as that strong reference remains. &lt;/p&gt;
&lt;h3&gt;ARC in Action&lt;/h3&gt;
&lt;p&gt;Here’s an example of how Automatic Reference Counting works. This example starts with a simple class called &lt;code&gt;Person&lt;/code&gt;, which defines a stored constant property called &lt;code&gt;name&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Person {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(name) is being initialized")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(name) is being deinitialized")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Person&lt;/code&gt; class has an initializer that sets the instance’s &lt;code&gt;name&lt;/code&gt; property and prints a message to indicate that initialization is underway. The &lt;code&gt;Person&lt;/code&gt; class also has a deinitializer that prints a message when an instance of the class is deallocated. &lt;/p&gt;
&lt;p&gt;The next code snippet defines three variables of type &lt;code&gt;Person?&lt;/code&gt;, which are used to set up multiple references to a new &lt;code&gt;Person&lt;/code&gt; instance in subsequent code snippets. Because these variables are of an optional type (&lt;code&gt;Person?&lt;/code&gt;, not &lt;code&gt;Person&lt;/code&gt;), they are automatically initialized with a value of &lt;code&gt;nil&lt;/code&gt;, and do not currently reference a &lt;code&gt;Person&lt;/code&gt; instance. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var reference1: Person?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var reference2: Person?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var reference3: Person?&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can now create a new &lt;code&gt;Person&lt;/code&gt; instance and assign it to one of these three variables: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reference1 = Person(name: "John Appleseed")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John Appleseed is being initialized"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the message &lt;code&gt;"John Appleseed is being initialized"&lt;/code&gt; is printed at the point that you call the &lt;code&gt;Person&lt;/code&gt; class’s initializer. This confirms that initialization has taken place. &lt;/p&gt;
&lt;p&gt;Because the new &lt;code&gt;Person&lt;/code&gt; instance has been assigned to the &lt;code&gt;reference1&lt;/code&gt; variable, there is now a strong reference from &lt;code&gt;reference1&lt;/code&gt; to the new &lt;code&gt;Person&lt;/code&gt; instance. Because there is at least one strong reference, ARC makes sure that this &lt;code&gt;Person&lt;/code&gt; is kept in memory and is not deallocated. &lt;/p&gt;
&lt;p&gt;If you assign the same &lt;code&gt;Person&lt;/code&gt; instance to two more variables, two more strong references to that instance are established: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reference2 = reference1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;reference3 = reference1&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;There are now &lt;em&gt;three&lt;/em&gt; strong references to this single &lt;code&gt;Person&lt;/code&gt; instance. &lt;/p&gt;
&lt;p&gt;If you break two of these strong references (including the original reference) by assigning &lt;code&gt;nil&lt;/code&gt; to two of the variables, a single strong reference remains, and the &lt;code&gt;Person&lt;/code&gt; instance is not deallocated: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reference1 = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;reference2 = nil&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;ARC does not deallocate the &lt;code&gt;Person&lt;/code&gt; instance until the third and final strong reference is broken, at which point it is clear that you are no longer using the &lt;code&gt;Person&lt;/code&gt; instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;reference3 = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John Appleseed is being deinitialized"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Strong Reference Cycles Between Class Instances&lt;/h3&gt;
&lt;p&gt;In the examples above, ARC is able to track the number of references to the new &lt;code&gt;Person&lt;/code&gt; instance you create and to deallocate that &lt;code&gt;Person&lt;/code&gt; instance when it is no longer needed. &lt;/p&gt;
&lt;p&gt;However, it is possible to write code in which an instance of a class &lt;em&gt;never&lt;/em&gt; gets to a point where it has zero strong references. This can happen if two class instances hold a strong reference to each other, such that each instance keeps the other alive. This is known as a &lt;em&gt;strong reference cycle&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;You resolve strong reference cycles by defining some of the relationships between classes as weak or unowned references instead of as strong references. This process is described in Resolving Strong Reference Cycles Between Class Instances. However, before you learn how to resolve a strong reference cycle, it is useful to understand how such a cycle is caused. &lt;/p&gt;
&lt;p&gt;Here’s an example of how a strong reference cycle can be created by accident. This example defines two classes called &lt;code&gt;Person&lt;/code&gt; and &lt;code&gt;Apartment&lt;/code&gt;, which model a block of apartments and its residents: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Person {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) { self.name = name }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var apartment: Apartment?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit { println("\\(name) is being deinitialized") }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class Apartment {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let number: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(number: Int) { self.number = number }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var tenant: Person?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit { println("Apartment #\\(number) is being deinitialized") }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Every &lt;code&gt;Person&lt;/code&gt; instance has a &lt;code&gt;name&lt;/code&gt; property of type &lt;code&gt;String&lt;/code&gt; and an optional &lt;code&gt;apartment&lt;/code&gt; property that is initially &lt;code&gt;nil&lt;/code&gt;. The &lt;code&gt;apartment&lt;/code&gt; property is optional, because a person may not always have an apartment. &lt;/p&gt;
&lt;p&gt;Similarly, every &lt;code&gt;Apartment&lt;/code&gt; instance has a &lt;code&gt;number&lt;/code&gt; property of type &lt;code&gt;Int&lt;/code&gt; and has an optional &lt;code&gt;tenant&lt;/code&gt; property that is initially &lt;code&gt;nil&lt;/code&gt;. The tenant property is optional because an apartment may not always have a tenant. &lt;/p&gt;
&lt;p&gt;Both of these classes also define a deinitializer, which prints the fact that an instance of that class is being deinitialized. This enables you to see whether instances of &lt;code&gt;Person&lt;/code&gt; and &lt;code&gt;Apartment&lt;/code&gt; are being deallocated as expected. &lt;/p&gt;
&lt;p&gt;This next code snippet defines two variables of optional type called &lt;code&gt;john&lt;/code&gt; and &lt;code&gt;number73&lt;/code&gt;, which will be set to a specific &lt;code&gt;Apartment&lt;/code&gt; and &lt;code&gt;Person&lt;/code&gt; instance below. Both of these variables have an initial value of &lt;code&gt;nil&lt;/code&gt;, by virtue of being optional: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var john: Person?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var number73: Apartment?&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can now create a specific &lt;code&gt;Person&lt;/code&gt; instance and &lt;code&gt;Apartment&lt;/code&gt; instance and assign these new instances to the &lt;code&gt;john&lt;/code&gt; and &lt;code&gt;number73&lt;/code&gt; variables: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john = Person(name: "John Appleseed")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;number73 = Apartment(number: 73)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how the strong references look after creating and assigning these two instances. The &lt;code&gt;john&lt;/code&gt; variable now has a strong reference to the new &lt;code&gt;Person&lt;/code&gt; instance, and the &lt;code&gt;number73&lt;/code&gt; variable has a strong reference to the new &lt;code&gt;Apartment&lt;/code&gt; instance: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/referenceCycle01_2x.png" src="http://muxuezi.github.io/posts/Art/referenceCycle01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;You can now link the two instances together so that the person has an apartment, and the apartment has a tenant. Note that an exclamation mark (&lt;code&gt;!&lt;/code&gt;) is used to unwrap and access the instances stored inside the &lt;code&gt;john&lt;/code&gt; and &lt;code&gt;number73&lt;/code&gt; optional variables, so that the properties of those instances can be set: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john!.apartment = number73&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;number73!.tenant = john&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how the strong references look after you link the two instances together: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/referenceCycle02_2x.png" src="http://muxuezi.github.io/posts/Art/referenceCycle02_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Unfortunately, linking these two instances creates a strong reference cycle between them. The &lt;code&gt;Person&lt;/code&gt; instance now has a strong reference to the &lt;code&gt;Apartment&lt;/code&gt; instance, and the &lt;code&gt;Apartment&lt;/code&gt; instance has a strong reference to the &lt;code&gt;Person&lt;/code&gt; instance. Therefore, when you break the strong references held by the &lt;code&gt;john&lt;/code&gt; and &lt;code&gt;number73&lt;/code&gt; variables, the reference counts do not drop to zero, and the instances are not deallocated by ARC: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;number73 = nil&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that neither deinitializer was called when you set these two variables to &lt;code&gt;nil&lt;/code&gt;. The strong reference cycle prevents the &lt;code&gt;Person&lt;/code&gt; and &lt;code&gt;Apartment&lt;/code&gt; instances from ever being deallocated, causing a memory leak in your app. &lt;/p&gt;
&lt;p&gt;Here’s how the strong references look after you set the &lt;code&gt;john&lt;/code&gt; and &lt;code&gt;number73&lt;/code&gt; variables to &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/referenceCycle03_2x.png" src="http://muxuezi.github.io/posts/Art/referenceCycle03_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The strong references between the &lt;code&gt;Person&lt;/code&gt; instance and the &lt;code&gt;Apartment&lt;/code&gt; instance remain and cannot be broken. &lt;/p&gt;
&lt;h3&gt;Resolving Strong Reference Cycles Between Class Instances&lt;/h3&gt;
&lt;p&gt;Swift provides two ways to resolve strong reference cycles when you work with properties of class type: weak references and unowned references. &lt;/p&gt;
&lt;p&gt;Weak and unowned references enable one instance in a reference cycle to refer to the other instance &lt;em&gt;without&lt;/em&gt; keeping a strong hold on it. The instances can then refer to each other without creating a strong reference cycle. &lt;/p&gt;
&lt;p&gt;Use a weak reference whenever it is valid for that reference to become &lt;code&gt;nil&lt;/code&gt; at some point during its lifetime. Conversely, use an unowned reference when you know that the reference will never be &lt;code&gt;nil&lt;/code&gt; once it has been set during initialization. &lt;/p&gt;
&lt;h4&gt;Weak References&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;weak reference&lt;/em&gt; is a reference that does not keep a strong hold on the instance it refers to, and so does not stop ARC from disposing of the referenced instance. This behavior prevents the reference from becoming part of a strong reference cycle. You indicate a weak reference by placing the &lt;code&gt;weak&lt;/code&gt; keyword before a property or variable declaration. &lt;/p&gt;
&lt;p&gt;Use a weak reference to avoid reference cycles whenever it is possible for that reference to have “no value” at some point in its life. If the reference will &lt;em&gt;always&lt;/em&gt; have a value, use an unowned reference instead, as described in Unowned References. In the &lt;code&gt;Apartment&lt;/code&gt; example above, it is appropriate for an apartment to be able to have “no tenant” at some point in its lifetime, and so a weak reference is an appropriate way to break the reference cycle in this case. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Weak references must be declared as variables, to indicate that their value can change at runtime. A weak reference cannot be declared as a constant. &lt;/p&gt;
&lt;p&gt;Because weak references are allowed to have “no value”, you must declare every weak reference as having an optional type. Optional types are the preferred way to represent the possibility for “no value” in Swift. &lt;/p&gt;
&lt;p&gt;Because a weak reference does not keep a strong hold on the instance it refers to, it is possible for that instance to be deallocated while the weak reference is still referring to it. Therefore, ARC automatically sets a weak reference to &lt;code&gt;nil&lt;/code&gt; when the instance that it refers to is deallocated. You can check for the existence of a value in the weak reference, just like any other optional value, and you will never end up with a reference to an invalid instance that no longer exists. &lt;/p&gt;
&lt;p&gt;The example below is identical to the &lt;code&gt;Person&lt;/code&gt; and &lt;code&gt;Apartment&lt;/code&gt; example from above, with one important difference. This time around, the &lt;code&gt;Apartment&lt;/code&gt; type’s &lt;code&gt;tenant&lt;/code&gt; property is declared as a weak reference: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Person {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) { self.name = name }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var apartment: Apartment?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit { println("\\(name) is being deinitialized") }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class Apartment {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let number: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(number: Int) { self.number = number }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;weak var tenant: Person?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit { println("Apartment #\\(number) is being deinitialized") }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The strong references from the two variables (&lt;code&gt;john&lt;/code&gt; and &lt;code&gt;number73&lt;/code&gt;) and the links between the two instances are created as before: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var john: Person?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var number73: Apartment?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;john = Person(name: "John Appleseed")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;number73 = Apartment(number: 73)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;john!.apartment = number73&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;number73!.tenant = john&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how the references look now that you’ve linked the two instances together: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/weakReference01_2x.png" src="http://muxuezi.github.io/posts/Art/weakReference01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Person&lt;/code&gt; instance still has a strong reference to the &lt;code&gt;Apartment&lt;/code&gt; instance, but the &lt;code&gt;Apartment&lt;/code&gt; instance now has a &lt;em&gt;weak&lt;/em&gt; reference to the &lt;code&gt;Person&lt;/code&gt; instance. This means that when you break the strong reference held by the &lt;code&gt;john&lt;/code&gt; variables, there are no more strong references to the &lt;code&gt;Person&lt;/code&gt; instance: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/weakReference02_2x.png" src="http://muxuezi.github.io/posts/Art/weakReference02_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Because there are no more strong references to the &lt;code&gt;Person&lt;/code&gt; instance, it is deallocated: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John Appleseed is being deinitialized"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The only remaining strong reference to the &lt;code&gt;Apartment&lt;/code&gt; instance is from the &lt;code&gt;number73&lt;/code&gt; variable. If you break &lt;em&gt;that&lt;/em&gt; strong reference, there are no more strong references to the &lt;code&gt;Apartment&lt;/code&gt; instance: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/weakReference03_2x.png" src="http://muxuezi.github.io/posts/Art/weakReference03_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Because there are no more strong references to the &lt;code&gt;Apartment&lt;/code&gt; instance, it too is deallocated: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;number73 = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Apartment #73 is being deinitialized"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The final two code snippets above show that the deinitializers for the &lt;code&gt;Person&lt;/code&gt; instance and &lt;code&gt;Apartment&lt;/code&gt; instance print their “deinitialized” messages after the &lt;code&gt;john&lt;/code&gt; and &lt;code&gt;number73&lt;/code&gt; variables are set to &lt;code&gt;nil&lt;/code&gt;. This proves that the reference cycle has been broken. &lt;/p&gt;
&lt;h4&gt;Unowned References&lt;/h4&gt;
&lt;p&gt;Like weak references, an &lt;em&gt;unowned reference&lt;/em&gt; does not keep a strong hold on the instance it refers to. Unlike a weak reference, however, an unowned reference is assumed to &lt;em&gt;always&lt;/em&gt; have a value. Because of this, an unowned reference is always defined as a non-optional type. You indicate an unowned reference by placing the &lt;code&gt;unowned&lt;/code&gt; keyword before a property or variable declaration. &lt;/p&gt;
&lt;p&gt;Because an unowned reference is non-optional, you don’t need to unwrap the unowned reference each time it is used. An unowned reference can always be accessed directly. However, ARC cannot set the reference to &lt;code&gt;nil&lt;/code&gt; when the instance it refers to is deallocated, because variables of a non-optional type cannot be set to &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you try to access an unowned reference after the instance that it references is deallocated, you will trigger a runtime error. Use unowned references only when you are sure that the reference will &lt;em&gt;always&lt;/em&gt; refer to an instance. &lt;/p&gt;
&lt;p&gt;Note also that Swift guarantees your app will crash if you try to access an unowned reference after the instance it references is deallocated. You will never encounter unexpected behavior in this situation. Your app will always crash reliably, although you should, of course, prevent it from doing so. &lt;/p&gt;
&lt;p&gt;The following example defines two classes, &lt;code&gt;Customer&lt;/code&gt; and &lt;code&gt;CreditCard&lt;/code&gt;, which model a bank customer and a possible credit card for that customer. These two classes each store an instance of the other class as a property. This relationship has the potential to create a strong reference cycle. &lt;/p&gt;
&lt;p&gt;The relationship between &lt;code&gt;Customer&lt;/code&gt; and &lt;code&gt;CreditCard&lt;/code&gt; is slightly different from the relationship between &lt;code&gt;Apartment&lt;/code&gt; and &lt;code&gt;Person&lt;/code&gt; seen in the weak reference example above. In this data model, a customer may or may not have a credit card, but a credit card will &lt;em&gt;always&lt;/em&gt; be associated with a customer. To represent this, the &lt;code&gt;Customer&lt;/code&gt; class has an optional &lt;code&gt;card&lt;/code&gt; property, but the &lt;code&gt;CreditCard&lt;/code&gt; class has a non-optional &lt;code&gt;customer&lt;/code&gt; property. &lt;/p&gt;
&lt;p&gt;Furthermore, a new &lt;code&gt;CreditCard&lt;/code&gt; instance can &lt;em&gt;only&lt;/em&gt; be created by passing a &lt;code&gt;number&lt;/code&gt; value and a &lt;code&gt;customer&lt;/code&gt; instance to a custom &lt;code&gt;CreditCard&lt;/code&gt; initializer. This ensures that a &lt;code&gt;CreditCard&lt;/code&gt; instance always has a &lt;code&gt;customer&lt;/code&gt; instance associated with it when the &lt;code&gt;CreditCard&lt;/code&gt; instance is created. &lt;/p&gt;
&lt;p&gt;Because a credit card will always have a customer, you define its &lt;code&gt;customer&lt;/code&gt; property as an unowned reference, to avoid a strong reference cycle: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Customer {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var card: CreditCard?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit { println("\\(name) is being deinitialized") }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class CreditCard {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let number: UInt64&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;unowned let customer: Customer&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(number: UInt64, customer: Customer) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.number = number&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.customer = customer&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit { println("Card #\\(number) is being deinitialized") }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;number&lt;/code&gt; property of the &lt;code&gt;CreditCard&lt;/code&gt; class is defined with a type of &lt;code&gt;UInt64&lt;/code&gt; rather than &lt;code&gt;Int&lt;/code&gt;, to ensure that the &lt;code&gt;number&lt;/code&gt; property’s capacity is large enough to store a 16-digit card number on both 32-bit and 64-bit systems. &lt;/p&gt;
&lt;p&gt;This next code snippet defines an optional &lt;code&gt;Customer&lt;/code&gt; variable called &lt;code&gt;john&lt;/code&gt;, which will be used to store a reference to a specific customer. This variable has an initial value of nil, by virtue of being optional: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var john: Customer?&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can now create a &lt;code&gt;Customer&lt;/code&gt; instance, and use it to initialize and assign a new &lt;code&gt;CreditCard&lt;/code&gt; instance as that customer’s &lt;code&gt;card&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john = Customer(name: "John Appleseed")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;john!.card = CreditCard(number: 1234_5678_9012_3456, customer: john!)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how the references look, now that you’ve linked the two instances: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/unownedReference01_2x.png" src="http://muxuezi.github.io/posts/Art/unownedReference01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Customer&lt;/code&gt; instance now has a strong reference to the &lt;code&gt;CreditCard&lt;/code&gt; instance, and the &lt;code&gt;CreditCard&lt;/code&gt; instance has an unowned reference to the &lt;code&gt;Customer&lt;/code&gt; instance. &lt;/p&gt;
&lt;p&gt;Because of the unowned &lt;code&gt;customer&lt;/code&gt; reference, when you break the strong reference held by the &lt;code&gt;john&lt;/code&gt; variable, there are no more strong references to the &lt;code&gt;Customer&lt;/code&gt; instance: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/unownedReference02_2x.png" src="http://muxuezi.github.io/posts/Art/unownedReference02_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Because there are no more strong references to the &lt;code&gt;Customer&lt;/code&gt; instance, it is deallocated. After this happens, there are no more strong references to the &lt;code&gt;CreditCard&lt;/code&gt; instance, and it too is deallocated: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John Appleseed is being deinitialized"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Card #1234567890123456 is being deinitialized"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The final code snippet above shows that the deinitializers for the &lt;code&gt;Customer&lt;/code&gt; instance and &lt;code&gt;CreditCard&lt;/code&gt; instance both print their “deinitialized” messages after the &lt;code&gt;john&lt;/code&gt; variable is set to &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Unowned References and Implicitly Unwrapped Optional Properties&lt;/h4&gt;
&lt;p&gt;The examples for weak and unowned references above cover two of the more common scenarios in which it is necessary to break a strong reference cycle. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Person&lt;/code&gt; and &lt;code&gt;Apartment&lt;/code&gt; example shows a situation where two properties, both of which are allowed to be &lt;code&gt;nil&lt;/code&gt;, have the potential to cause a strong reference cycle. This scenario is best resolved with a weak reference. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Customer&lt;/code&gt; and &lt;code&gt;CreditCard&lt;/code&gt; example shows a situation where one property that is allowed to be &lt;code&gt;nil&lt;/code&gt; and another property that cannot be &lt;code&gt;nil&lt;/code&gt; have the potential to cause a strong reference cycle. This scenario is best resolved with an unowned reference. &lt;/p&gt;
&lt;p&gt;However, there is a third scenario, in which &lt;em&gt;both&lt;/em&gt; properties should always have a value, and neither property should ever be &lt;code&gt;nil&lt;/code&gt; once initialization is complete. In this scenario, it is useful to combine an unowned property on one class with an implicitly unwrapped optional property on the other class. &lt;/p&gt;
&lt;p&gt;This enables both properties to be accessed directly (without optional unwrapping) once initialization is complete, while still avoiding a reference cycle. This section shows you how to set up such a relationship. &lt;/p&gt;
&lt;p&gt;The example below defines two classes, &lt;code&gt;Country&lt;/code&gt; and &lt;code&gt;City&lt;/code&gt;, each of which stores an instance of the other class as a property. In this data model, every country must always have a capital city, and every city must always belong to a country. To represent this, the &lt;code&gt;Country&lt;/code&gt; class has a &lt;code&gt;capitalCity&lt;/code&gt; property, and the &lt;code&gt;City&lt;/code&gt; class has a &lt;code&gt;country&lt;/code&gt; property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Country {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let capitalCity: City!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, capitalName: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.capitalCity = City(name: capitalName, country: self)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class City {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;unowned let country: Country&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, country: Country) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.country = country&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To set up the interdependency between the two classes, the initializer for &lt;code&gt;City&lt;/code&gt; takes a &lt;code&gt;Country&lt;/code&gt; instance, and stores this instance in its &lt;code&gt;country&lt;/code&gt; property. &lt;/p&gt;
&lt;p&gt;The initializer for &lt;code&gt;City&lt;/code&gt; is called from within the initializer for &lt;code&gt;Country&lt;/code&gt;. However, the initializer for &lt;code&gt;Country&lt;/code&gt; cannot pass &lt;code&gt;self&lt;/code&gt; to the &lt;code&gt;City&lt;/code&gt; initializer until a new &lt;code&gt;Country&lt;/code&gt; instance is fully initialized, as described in Two-Phase Initialization. &lt;/p&gt;
&lt;p&gt;To cope with this requirement, you declare the &lt;code&gt;capitalCity&lt;/code&gt; property of &lt;code&gt;Country&lt;/code&gt; as an implicitly unwrapped optional property, indicated by the exclamation mark at the end of its type annotation (&lt;code&gt;City!&lt;/code&gt;). This means that the &lt;code&gt;capitalCity&lt;/code&gt; property has a default value of &lt;code&gt;nil&lt;/code&gt;, like any other optional, but can be accessed without the need to unwrap its value as described in Implicitly Unwrapped Optionals. &lt;/p&gt;
&lt;p&gt;Because &lt;code&gt;capitalCity&lt;/code&gt; has a default &lt;code&gt;nil&lt;/code&gt; value, a new &lt;code&gt;Country&lt;/code&gt; instance is considered fully initialized as soon as the &lt;code&gt;Country&lt;/code&gt; instance sets its &lt;code&gt;name&lt;/code&gt; property within its initializer. This means that the &lt;code&gt;Country&lt;/code&gt; initializer can start to reference and pass around the implicit &lt;code&gt;self&lt;/code&gt; property as soon as the &lt;code&gt;name&lt;/code&gt; property is set. The &lt;code&gt;Country&lt;/code&gt; initializer can therefore pass &lt;code&gt;self&lt;/code&gt; as one of the parameters for the &lt;code&gt;City&lt;/code&gt; initializer when the &lt;code&gt;Country&lt;/code&gt; initializer is setting its own &lt;code&gt;capitalCity&lt;/code&gt; property. &lt;/p&gt;
&lt;p&gt;All of this means that you can create the &lt;code&gt;Country&lt;/code&gt; and &lt;code&gt;City&lt;/code&gt; instances in a single statement, without creating a strong reference cycle, and the &lt;code&gt;capitalCity&lt;/code&gt; property can be accessed directly, without needing to use an exclamation mark to unwrap its optional value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var country = Country(name: "Canada", capitalName: "Ottawa")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(country.name)'s capital city is called \\(country.capitalCity.name)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Canada's capital city is called Ottawa"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the example above, the use of an implicitly unwrapped optional means that all of the two-phase class initializer requirements are satisfied. The &lt;code&gt;capitalCity&lt;/code&gt; property can be used and accessed like a non-optional value once initialization is complete, while still avoiding a strong reference cycle. &lt;/p&gt;
&lt;h3&gt;Strong Reference Cycles for Closures&lt;/h3&gt;
&lt;p&gt;You saw above how a strong reference cycle can be created when two class instance properties hold a strong reference to each other. You also saw how to use weak and unowned references to break these strong reference cycles. &lt;/p&gt;
&lt;p&gt;A strong reference cycle can also occur if you assign a closure to a property of a class instance, and the body of that closure captures the instance. This capture might occur because the closure’s body accesses a property of the instance, such as &lt;code&gt;self.someProperty&lt;/code&gt;, or because the closure calls a method on the instance, such as &lt;code&gt;self.someMethod()&lt;/code&gt;. In either case, these accesses cause the closure to “capture” &lt;code&gt;self&lt;/code&gt;, creating a strong reference cycle. &lt;/p&gt;
&lt;p&gt;This strong reference cycle occurs because closures, like classes, are &lt;em&gt;reference types&lt;/em&gt;. When you assign a closure to a property, you are assigning a &lt;em&gt;reference&lt;/em&gt; to that closure. In essence, it’s the same problem as above—two strong references are keeping each other alive. However, rather than two class instances, this time it’s a class instance and a closure that are keeping each other alive. &lt;/p&gt;
&lt;p&gt;Swift provides an elegant solution to this problem, known as a &lt;em&gt;closure capture list&lt;/em&gt;. However, before you learn how to break a strong reference cycle with a closure capture list, it is useful to understand how such a cycle can be caused. &lt;/p&gt;
&lt;p&gt;The example below shows how you can create a strong reference cycle when using a closure that references &lt;code&gt;self&lt;/code&gt;. This example defines a class called &lt;code&gt;HTMLElement&lt;/code&gt;, which provides a simple model for an individual element within an HTML document: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class HTMLElement {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let text: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;lazy var asHTML: () -&amp;gt; String = {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let text = self.text {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "&amp;lt;\\(self.name)&amp;gt;\\(text)&amp;lt;/\\(self.name)&amp;gt;"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "&amp;lt;\\(self.name) /&amp;gt;"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, text: String? = nil) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.text = text&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(name) is being deinitialized")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;HTMLElement&lt;/code&gt; class defines a &lt;code&gt;name&lt;/code&gt; property, which indicates the name of the element, such as &lt;code&gt;"p"&lt;/code&gt; for a paragraph element, or &lt;code&gt;"br"&lt;/code&gt; for a line break element. &lt;code&gt;HTMLElement&lt;/code&gt; also defines an optional &lt;code&gt;text&lt;/code&gt; property, which you can set to a string that represents the text to be rendered within that HTML element. &lt;/p&gt;
&lt;p&gt;In addition to these two simple properties, the &lt;code&gt;HTMLElement&lt;/code&gt; class defines a lazy property called &lt;code&gt;asHTML&lt;/code&gt;. This property references a closure that combines &lt;code&gt;name&lt;/code&gt; and &lt;code&gt;text&lt;/code&gt; into an HTML string fragment. The &lt;code&gt;asHTML&lt;/code&gt; property is of type &lt;code&gt;() -&amp;gt; String&lt;/code&gt;, or “a function that takes no parameters, and returns a &lt;code&gt;String&lt;/code&gt; value”. &lt;/p&gt;
&lt;p&gt;By default, the &lt;code&gt;asHTML&lt;/code&gt; property is assigned a closure that returns a string representation of an HTML tag. This tag contains the optional &lt;code&gt;text&lt;/code&gt; value if it exists, or no text content if &lt;code&gt;text&lt;/code&gt; does not exist. For a paragraph element, the closure would return &lt;code&gt;"&amp;lt;p&amp;gt;some text&amp;lt;/p&amp;gt;"&lt;/code&gt; or &lt;code&gt;"&amp;lt;p /&amp;gt;"&lt;/code&gt;, depending on whether the &lt;code&gt;text&lt;/code&gt; property equals &lt;code&gt;"some text"&lt;/code&gt; or &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;asHTML&lt;/code&gt; property is named and used somewhat like an instance method. However, because &lt;code&gt;asHTML&lt;/code&gt; is a closure property rather than an instance method, you can replace the default value of the &lt;code&gt;asHTML&lt;/code&gt; property with a custom closure, if you want to change the HTML rendering for a particular HTML element. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;asHTML&lt;/code&gt; property is declared as a lazy property, because it is only needed if and when the element actually needs to be rendered as a string value for some HTML output target. The fact that &lt;code&gt;asHTML&lt;/code&gt; is a lazy property means that you can refer to &lt;code&gt;self&lt;/code&gt; within the default closure, because the lazy property will not be accessed until after initialization has been completed and &lt;code&gt;self&lt;/code&gt; is known to exist. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;HTMLElement&lt;/code&gt; class provides a single initializer, which takes a &lt;code&gt;name&lt;/code&gt; argument and (if desired) a &lt;code&gt;text&lt;/code&gt; argument to initialize a new element. The class also defines a deinitializer, which prints a message to show when an &lt;code&gt;HTMLElement&lt;/code&gt; instance is deallocated. &lt;/p&gt;
&lt;p&gt;Here’s how you use the &lt;code&gt;HTMLElement&lt;/code&gt; class to create and print a new instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var paragraph: HTMLElement? = HTMLElement(name: "p", text: "hello, world")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(paragraph!.asHTML())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "&amp;lt;p&amp;gt;hello, world&amp;lt;/p&amp;gt;"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;paragraph&lt;/code&gt; variable above is defined as an &lt;em&gt;optional&lt;/em&gt; &lt;code&gt;HTMLElement&lt;/code&gt;, so that it can be set to &lt;code&gt;nil&lt;/code&gt; below to demonstrate the presence of a strong reference cycle. &lt;/p&gt;
&lt;p&gt;Unfortunately, the &lt;code&gt;HTMLElement&lt;/code&gt; class, as written above, creates a strong reference cycle between an &lt;code&gt;HTMLElement&lt;/code&gt; instance and the closure used for its default &lt;code&gt;asHTML&lt;/code&gt; value. Here’s how the cycle looks: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/closureReferenceCycle01_2x.png" src="http://muxuezi.github.io/posts/Art/closureReferenceCycle01_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The instance’s &lt;code&gt;asHTML&lt;/code&gt; property holds a strong reference to its closure. However, because the closure refers to &lt;code&gt;self&lt;/code&gt; within its body (as a way to reference &lt;code&gt;self.name&lt;/code&gt; and &lt;code&gt;self.text&lt;/code&gt;), the closure &lt;em&gt;captures&lt;/em&gt; self, which means that it holds a strong reference back to the &lt;code&gt;HTMLElement&lt;/code&gt; instance. A strong reference cycle is created between the two. (For more information about capturing values in a closure, see Capturing Values.) &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Even though the closure refers to &lt;code&gt;self&lt;/code&gt; multiple times, it only captures one strong reference to the &lt;code&gt;HTMLElement&lt;/code&gt; instance. &lt;/p&gt;
&lt;p&gt;If you set the &lt;code&gt;paragraph&lt;/code&gt; variable to &lt;code&gt;nil&lt;/code&gt; and break its strong reference to the &lt;code&gt;HTMLElement&lt;/code&gt; instance, neither the &lt;code&gt;HTMLElement&lt;/code&gt; instance nor its closure are deallocated, because of the strong reference cycle: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;paragraph = nil&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the message in the &lt;code&gt;HTMLElement&lt;/code&gt; deinitializer is not printed, which shows that the &lt;code&gt;HTMLElement&lt;/code&gt; instance is not deallocated. &lt;/p&gt;
&lt;h3&gt;Resolving Strong Reference Cycles for Closures&lt;/h3&gt;
&lt;p&gt;You resolve a strong reference cycle between a closure and a class instance by defining a &lt;em&gt;capture list&lt;/em&gt; as part of the closure’s definition. A capture list defines the rules to use when capturing one or more reference types within the closure’s body. As with strong reference cycles between two class instances, you declare each captured reference to be a weak or unowned reference rather than a strong reference. The appropriate choice of weak or unowned depends on the relationships between the different parts of your code. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift requires you to write &lt;code&gt;self.someProperty&lt;/code&gt; or &lt;code&gt;self.someMethod&lt;/code&gt; (rather than just &lt;code&gt;someProperty&lt;/code&gt; or &lt;code&gt;someMethod&lt;/code&gt;) whenever you refer to a member of &lt;code&gt;self&lt;/code&gt; within a closure. This helps you remember that it’s possible to capture &lt;code&gt;self&lt;/code&gt; by accident. &lt;/p&gt;
&lt;h4&gt;Defining a Capture List&lt;/h4&gt;
&lt;p&gt;Each item in a capture list is a pairing of the &lt;code&gt;weak&lt;/code&gt; or &lt;code&gt;unowned&lt;/code&gt; keyword with a reference to a class instance (such as &lt;code&gt;self&lt;/code&gt; or &lt;code&gt;someInstance&lt;/code&gt;). These pairings are written within a pair of square braces, separated by commas. &lt;/p&gt;
&lt;p&gt;Place the capture list before a closure’s parameter list and return type if they are provided: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;lazy var someClosure: (Int, String) -&amp;gt; String = {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;[unowned self] (index: Int, stringToProcess: String) -&amp;gt; String in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// closure body goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If a closure does not specify a parameter list or return type because they can be inferred from context, place the capture list at the very start of the closure, followed by the &lt;code&gt;in&lt;/code&gt; keyword: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;lazy var someClosure: () -&amp;gt; String = {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;[unowned self] in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// closure body goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Weak and Unowned References&lt;/h4&gt;
&lt;p&gt;Define a capture in a closure as an unowned reference when the closure and the instance it captures will always refer to each other, and will always be deallocated at the same time. &lt;/p&gt;
&lt;p&gt;Conversely, define a capture as a weak reference when the captured reference may become &lt;code&gt;nil&lt;/code&gt; at some point in the future. Weak references are always of an optional type, and automatically become &lt;code&gt;nil&lt;/code&gt; when the instance they reference is deallocated. This enables you to check for their existence within the closure’s body. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If the captured reference will never become &lt;code&gt;nil&lt;/code&gt;, it should always be captured as an unowned reference, rather than a weak reference. &lt;/p&gt;
&lt;p&gt;An unowned reference is the appropriate capture method to use to resolve the strong reference cycle in the &lt;code&gt;HTMLElement&lt;/code&gt; example from earlier. Here’s how you write the &lt;code&gt;HTMLElement&lt;/code&gt; class to avoid the cycle: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class HTMLElement {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let text: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;lazy var asHTML: () -&amp;gt; String = {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;[unowned self] in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let text = self.text {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "&amp;lt;\\(self.name)&amp;gt;\\(text)&amp;lt;/\\(self.name)&amp;gt;"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "&amp;lt;\\(self.name) /&amp;gt;"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, text: String? = nil) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.text = text&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;deinit {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("\\(name) is being deinitialized")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This implementation of &lt;code&gt;HTMLElement&lt;/code&gt; is identical to the previous implementation, apart from the addition of a capture list within the &lt;code&gt;asHTML&lt;/code&gt; closure. In this case, the capture list is &lt;code&gt;[unowned self]&lt;/code&gt;, which means “capture self as an unowned reference rather than a strong reference”. &lt;/p&gt;
&lt;p&gt;You can create and print an &lt;code&gt;HTMLElement&lt;/code&gt; instance as before: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var paragraph: HTMLElement? = HTMLElement(name: "p", text: "hello, world")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(paragraph!.asHTML())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "&amp;lt;p&amp;gt;hello, world&amp;lt;/p&amp;gt;"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how the references look with the capture list in place: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/closureReferenceCycle02_2x.png" src="http://muxuezi.github.io/posts/Art/closureReferenceCycle02_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;This time, the capture of &lt;code&gt;self&lt;/code&gt; by the closure is an unowned reference, and does not keep a strong hold on the &lt;code&gt;HTMLElement&lt;/code&gt; instance it has captured. If you set the strong reference from the &lt;code&gt;paragraph&lt;/code&gt; variable to &lt;code&gt;nil&lt;/code&gt;, the &lt;code&gt;HTMLElement&lt;/code&gt; instance is deallocated, as can be seen from the printing of its deinitializer message in the example below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;paragraph = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "p is being deinitialized"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Optional Chaining&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Optional chaining&lt;/em&gt; is a process for querying and calling properties, methods, and subscripts on an optional that might currently be &lt;code&gt;nil&lt;/code&gt;. If the optional contains a value, the property, method, or subscript call succeeds; if the optional is &lt;code&gt;nil&lt;/code&gt;, the property, method, or subscript call returns &lt;code&gt;nil&lt;/code&gt;. Multiple queries can be chained together, and the entire chain fails gracefully if any link in the chain is &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Optional chaining in Swift is similar to messaging &lt;code&gt;nil&lt;/code&gt; in Objective-C, but in a way that works for any type, and that can be checked for success or failure. &lt;/p&gt;
&lt;h3&gt;Optional Chaining as an Alternative to Forced Unwrapping&lt;/h3&gt;
&lt;p&gt;You specify optional chaining by placing a question mark (&lt;code&gt;?&lt;/code&gt;) after the optional value on which you wish to call a property, method or subscript if the optional is non-&lt;code&gt;nil&lt;/code&gt;. This is very similar to placing an exclamation mark (&lt;code&gt;!&lt;/code&gt;) after an optional value to force the unwrapping of its value. The main difference is that optional chaining fails gracefully when the optional is &lt;code&gt;nil&lt;/code&gt;, whereas forced unwrapping triggers a runtime error when the optional is &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;To reflect the fact that optional chaining can be called on a &lt;code&gt;nil&lt;/code&gt; value, the result of an optional chaining call is always an optional value, even if the property, method, or subscript you are querying returns a non-optional value. You can use this optional return value to check whether the optional chaining call was successful (the returned optional contains a value), or did not succeed due to a &lt;code&gt;nil&lt;/code&gt; value in the chain (the returned optional value is &lt;code&gt;nil&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Specifically, the result of an optional chaining call is of the same type as the expected return value, but wrapped in an optional. A property that normally returns an &lt;code&gt;Int&lt;/code&gt; will return an &lt;code&gt;Int?&lt;/code&gt; when accessed through optional chaining. &lt;/p&gt;
&lt;p&gt;The next several code snippets demonstrate how optional chaining differs from forced unwrapping and enables you to check for success. &lt;/p&gt;
&lt;p&gt;First, two classes called &lt;code&gt;Person&lt;/code&gt; and &lt;code&gt;Residence&lt;/code&gt; are defined: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Person {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var residence: Residence?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class Residence {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var numberOfRooms = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;Residence&lt;/code&gt; instances have a single &lt;code&gt;Int&lt;/code&gt; property called &lt;code&gt;numberOfRooms&lt;/code&gt;, with a default value of &lt;code&gt;1&lt;/code&gt;. &lt;code&gt;Person&lt;/code&gt; instances have an optional &lt;code&gt;residence&lt;/code&gt; property of type &lt;code&gt;Residence?&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If you create a new &lt;code&gt;Person&lt;/code&gt; instance, its &lt;code&gt;residence&lt;/code&gt; property is default initialized to &lt;code&gt;nil&lt;/code&gt;, by virtue of being optional. In the code below, &lt;code&gt;john&lt;/code&gt; has a &lt;code&gt;residence&lt;/code&gt; property value of &lt;code&gt;nil&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let john = Person()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you try to access the &lt;code&gt;numberOfRooms&lt;/code&gt; property of this person’s &lt;code&gt;residence&lt;/code&gt;, by placing an exclamation mark after &lt;code&gt;residence&lt;/code&gt; to force the unwrapping of its value, you trigger a runtime error, because there is no &lt;code&gt;residence&lt;/code&gt; value to unwrap: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let roomCount = john.residence!.numberOfRooms&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this triggers a runtime error&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The code above succeeds when &lt;code&gt;john.residence&lt;/code&gt; has a non-&lt;code&gt;nil&lt;/code&gt; value and will set &lt;code&gt;roomCount&lt;/code&gt; to an &lt;code&gt;Int&lt;/code&gt; value containing the appropriate number of rooms. However, this code always triggers a runtime error when &lt;code&gt;residence&lt;/code&gt; is &lt;code&gt;nil&lt;/code&gt;, as illustrated above. &lt;/p&gt;
&lt;p&gt;Optional chaining provides an alternative way to access the value of &lt;code&gt;numberOfRooms&lt;/code&gt;. To use optional chaining, use a question mark in place of the exclamation mark: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let roomCount = john.residence?.numberOfRooms {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's residence has \\(roomCount) room(s).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to retrieve the number of rooms.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Unable to retrieve the number of rooms."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This tells Swift to “chain” on the optional &lt;code&gt;residence&lt;/code&gt; property and to retrieve the value of &lt;code&gt;numberOfRooms&lt;/code&gt; if &lt;code&gt;residence&lt;/code&gt; exists. &lt;/p&gt;
&lt;p&gt;Because the attempt to access &lt;code&gt;numberOfRooms&lt;/code&gt; has the potential to fail, the optional chaining attempt returns a value of type &lt;code&gt;Int?&lt;/code&gt;, or “optional &lt;code&gt;Int&lt;/code&gt;”. When &lt;code&gt;residence&lt;/code&gt; is &lt;code&gt;nil&lt;/code&gt;, as in the example above, this optional &lt;code&gt;Int&lt;/code&gt; will also be &lt;code&gt;nil&lt;/code&gt;, to reflect the fact that it was not possible to access &lt;code&gt;numberOfRooms&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note that this is true even though &lt;code&gt;numberOfRooms&lt;/code&gt; is a non-optional &lt;code&gt;Int&lt;/code&gt;. The fact that it is queried through an optional chain means that the call to &lt;code&gt;numberOfRooms&lt;/code&gt; will always return an &lt;code&gt;Int?&lt;/code&gt; instead of an &lt;code&gt;Int&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can assign a &lt;code&gt;Residence&lt;/code&gt; instance to &lt;code&gt;john.residence&lt;/code&gt;, so that it no longer has a &lt;code&gt;nil&lt;/code&gt; value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john.residence = Residence()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;john.residence&lt;/code&gt; now contains an actual &lt;code&gt;Residence&lt;/code&gt; instance, rather than &lt;code&gt;nil&lt;/code&gt;. If you try to access &lt;code&gt;numberOfRooms&lt;/code&gt; with the same optional chaining as before, it will now return an &lt;code&gt;Int?&lt;/code&gt; that contains the default &lt;code&gt;numberOfRooms&lt;/code&gt; value of &lt;code&gt;1&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let roomCount = john.residence?.numberOfRooms {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's residence has \\(roomCount) room(s).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to retrieve the number of rooms.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John's residence has 1 room(s)."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Defining Model Classes for Optional Chaining&lt;/h3&gt;
&lt;p&gt;You can use optional chaining with calls to properties, methods, and subscripts that are more than one level deep. This enables you to drill down into subproperties within complex models of interrelated types, and to check whether it is possible to access properties, methods, and subscripts on those subproperties. &lt;/p&gt;
&lt;p&gt;The code snippets below define four model classes for use in several subsequent examples, including examples of multilevel optional chaining. These classes expand upon the &lt;code&gt;Person&lt;/code&gt; and &lt;code&gt;Residence&lt;/code&gt; model from above by adding a &lt;code&gt;Room&lt;/code&gt; and &lt;code&gt;Address&lt;/code&gt; class, with associated properties, methods, and subscripts. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Person&lt;/code&gt; class is defined in the same way as before: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Person {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var residence: Residence?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Residence&lt;/code&gt; class is more complex than before. This time, the &lt;code&gt;Residence&lt;/code&gt; class defines a variable property called &lt;code&gt;rooms&lt;/code&gt;, which is initialized with an empty array of type &lt;code&gt;[Room]&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Residence {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var rooms = [Room]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var numberOfRooms: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return rooms.count&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;subscript(i: Int) -&amp;gt; Room {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;get {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return rooms[i]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;set {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;rooms[i] = newValue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func printNumberOfRooms() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The number of rooms is \\(numberOfRooms)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var address: Address?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because this version of &lt;code&gt;Residence&lt;/code&gt; stores an array of &lt;code&gt;Room&lt;/code&gt; instances, its &lt;code&gt;numberOfRooms&lt;/code&gt; property is implemented as a computed property, not a stored property. The computed &lt;code&gt;numberOfRooms&lt;/code&gt; property simply returns the value of the &lt;code&gt;count&lt;/code&gt; property from the &lt;code&gt;rooms&lt;/code&gt; array. &lt;/p&gt;
&lt;p&gt;As a shortcut to accessing its &lt;code&gt;rooms&lt;/code&gt; array, this version of &lt;code&gt;Residence&lt;/code&gt; provides a read-write subscript that provides access to the room at the requested index in the &lt;code&gt;rooms&lt;/code&gt; array. &lt;/p&gt;
&lt;p&gt;This version of &lt;code&gt;Residence&lt;/code&gt; also provides a method called &lt;code&gt;printNumberOfRooms&lt;/code&gt;, which simply prints the number of rooms in the residence. &lt;/p&gt;
&lt;p&gt;Finally, &lt;code&gt;Residence&lt;/code&gt; defines an optional property called &lt;code&gt;address&lt;/code&gt;, with a type of &lt;code&gt;Address?&lt;/code&gt;. The &lt;code&gt;Address&lt;/code&gt; class type for this property is defined below. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Room&lt;/code&gt; class used for the &lt;code&gt;rooms&lt;/code&gt; array is a simple class with one property called &lt;code&gt;name&lt;/code&gt;, and an initializer to set that property to a suitable room name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Room {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) { self.name = name }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The final class in this model is called &lt;code&gt;Address&lt;/code&gt;. This class has three optional properties of type &lt;code&gt;String?&lt;/code&gt;. The first two properties, &lt;code&gt;buildingName&lt;/code&gt; and &lt;code&gt;buildingNumber&lt;/code&gt;, are alternative ways to identify a particular building as part of an address. The third property, &lt;code&gt;street&lt;/code&gt;, is used to name the street for that address: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Address {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var buildingName: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var buildingNumber: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var street: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func buildingIdentifier() -&amp;gt; String? {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if buildingName != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return buildingName&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if buildingNumber != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return buildingNumber&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Address&lt;/code&gt; class also provides a method called &lt;code&gt;buildingIdentifier&lt;/code&gt;, which has a return type of &lt;code&gt;String?&lt;/code&gt;. This method checks the &lt;code&gt;buildingName&lt;/code&gt; and &lt;code&gt;buildingNumber&lt;/code&gt; properties and returns &lt;code&gt;buildingName&lt;/code&gt; if it has a value, or &lt;code&gt;buildingNumber&lt;/code&gt; if it has a value, or &lt;code&gt;nil&lt;/code&gt; if neither property has a value. &lt;/p&gt;
&lt;h3&gt;Accessing Properties Through Optional Chaining&lt;/h3&gt;
&lt;p&gt;As demonstrated in Optional Chaining as an Alternative to Forced Unwrapping, you can use optional chaining to access a property on an optional value, and to check if that property access is successful. &lt;/p&gt;
&lt;p&gt;Use the classes defined above to create a new &lt;code&gt;Person&lt;/code&gt; instance, and try to access its &lt;code&gt;numberOfRooms&lt;/code&gt; property as before: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let john = Person()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let roomCount = john.residence?.numberOfRooms {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's residence has \\(roomCount) room(s).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to retrieve the number of rooms.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Unable to retrieve the number of rooms."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because &lt;code&gt;john.residence&lt;/code&gt; is &lt;code&gt;nil&lt;/code&gt;, this optional chaining call fails in the same way as before. &lt;/p&gt;
&lt;p&gt;You can also attempt to set a property’s value through optional chaining: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someAddress = Address()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someAddress.buildingNumber = "29"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someAddress.street = "Acacia Road"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;john.residence?.address = someAddress&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this example, the attempt to set the &lt;code&gt;address&lt;/code&gt; property of &lt;code&gt;john.residence&lt;/code&gt; will fail, because &lt;code&gt;john.residence&lt;/code&gt; is currently &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;h3&gt;Calling Methods Through Optional Chaining&lt;/h3&gt;
&lt;p&gt;You can use optional chaining to call a method on an optional value, and to check whether that method call is successful. You can do this even if that method does not define a return value. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;printNumberOfRooms&lt;/code&gt; method on the &lt;code&gt;Residence&lt;/code&gt; class prints the current value of &lt;code&gt;numberOfRooms&lt;/code&gt;. Here’s how the method looks: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func printNumberOfRooms() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The number of rooms is \\(numberOfRooms)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This method does not specify a return type. However, functions and methods with no return type have an implicit return type of &lt;code&gt;Void&lt;/code&gt;, as described in Functions Without Return Values. This means that they return a value of &lt;code&gt;()&lt;/code&gt;, or an empty tuple. &lt;/p&gt;
&lt;p&gt;If you call this method on an optional value with optional chaining, the method’s return type will be &lt;code&gt;Void?&lt;/code&gt;, not &lt;code&gt;Void&lt;/code&gt;, because return values are always of an optional type when called through optional chaining. This enables you to use an &lt;code&gt;if&lt;/code&gt; statement to check whether it was possible to call the &lt;code&gt;printNumberOfRooms&lt;/code&gt; method, even though the method does not itself define a return value. Compare the return value from the &lt;code&gt;printNumberOfRooms&lt;/code&gt; call against &lt;code&gt;nil&lt;/code&gt; to see if the method call was successful: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if john.residence?.printNumberOfRooms() != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It was possible to print the number of rooms.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It was not possible to print the number of rooms.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "It was not possible to print the number of rooms."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The same is true if you attempt to set a property through optional chaining. The example above in Accessing Properties Through Optional Chaining attempts to set an &lt;code&gt;address&lt;/code&gt; value for &lt;code&gt;john.residence&lt;/code&gt;, even though the &lt;code&gt;residence&lt;/code&gt; property is &lt;code&gt;nil&lt;/code&gt;. Any attempt to set a property through optional chaining returns a value of type &lt;code&gt;Void?&lt;/code&gt;, which enables you to compare against &lt;code&gt;nil&lt;/code&gt; to see if the property was set successfully: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if (john.residence?.address = someAddress) != nil {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It was possible to set the address.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("It was not possible to set the address.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "It was not possible to set the address."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Accessing Subscripts Through Optional Chaining&lt;/h3&gt;
&lt;p&gt;You can use optional chaining to try to retrieve and set a value from a subscript on an optional value, and to check whether that subscript call is successful. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;When you access a subscript on an optional value through optional chaining, you place the question mark &lt;em&gt;before&lt;/em&gt; the subscript’s braces, not after. The optional chaining question mark always follows immediately after the part of the expression that is optional. &lt;/p&gt;
&lt;p&gt;The example below tries to retrieve the name of the first room in the &lt;code&gt;rooms&lt;/code&gt; array of the &lt;code&gt;john.residence&lt;/code&gt; property using the subscript defined on the &lt;code&gt;Residence&lt;/code&gt; class. Because &lt;code&gt;john.residence&lt;/code&gt; is currently &lt;code&gt;nil&lt;/code&gt;, the subscript call fails: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let firstRoomName = john.residence?[0].name {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The first room name is \\(firstRoomName).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to retrieve the first room name.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Unable to retrieve the first room name."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The optional chaining question mark in this subscript call is placed immediately after &lt;code&gt;john.residence&lt;/code&gt;, before the subscript brackets, because &lt;code&gt;john.residence&lt;/code&gt; is the optional value on which optional chaining is being attempted. &lt;/p&gt;
&lt;p&gt;Similarly, you can try to set a new value through a subscript with optional chaining: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;john.residence?[0] = Room(name: "Bathroom")&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This subscript setting attempt also fails, because &lt;code&gt;residence&lt;/code&gt; is currently &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If you create and assign an actual &lt;code&gt;Residence&lt;/code&gt; instance to &lt;code&gt;john.residence&lt;/code&gt;, with one or more &lt;code&gt;Room&lt;/code&gt; instances in its &lt;code&gt;rooms&lt;/code&gt; array, you can use the &lt;code&gt;Residence&lt;/code&gt; subscript to access the actual items in the &lt;code&gt;rooms&lt;/code&gt; array through optional chaining: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let johnsHouse = Residence()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;johnsHouse.rooms.append(Room(name: "Living Room"))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;johnsHouse.rooms.append(Room(name: "Kitchen"))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;john.residence = johnsHouse&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let firstRoomName = john.residence?[0].name {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The first room name is \\(firstRoomName).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to retrieve the first room name.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The first room name is Living Room."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Accessing Subscripts of Optional Type&lt;/h4&gt;
&lt;p&gt;If a subscript returns a value of optional type—such as the key subscript of Swift’s &lt;code&gt;Dictionary&lt;/code&gt; type—place a question mark &lt;em&gt;after&lt;/em&gt; the subscript’s closing bracket to chain on its optional return value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var testScores = ["Dave": [86, 82, 84], "Bev": [79, 94, 81]]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;testScores["Dave"]?[0] = 91&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;testScores["Bev"]?[0]++&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;testScores["Brian"]?[0] = 72&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the "Dave" array is now [91, 82, 84] and the "Bev" array is now [80, 94, 81]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example above defines a dictionary called &lt;code&gt;testScores&lt;/code&gt;, which contains two key-value pairs that map a &lt;code&gt;String&lt;/code&gt; key to an array of &lt;code&gt;Int&lt;/code&gt; values. The example uses optional chaining to set the first item in the &lt;code&gt;"Dave"&lt;/code&gt; array to &lt;code&gt;91&lt;/code&gt;; to increment the first item in the &lt;code&gt;"Bev"&lt;/code&gt; array by &lt;code&gt;1&lt;/code&gt;; and to try to set the first item in an array for a key of &lt;code&gt;"Brian"&lt;/code&gt;. The first two calls succeed, because the &lt;code&gt;testScores&lt;/code&gt; dictionary contains keys for &lt;code&gt;"Dave"&lt;/code&gt; and &lt;code&gt;"Bev"&lt;/code&gt;. The third call fails, because the &lt;code&gt;testScores&lt;/code&gt; dictionary does not contain a key for &lt;code&gt;"Brian"&lt;/code&gt;. &lt;/p&gt;
&lt;h3&gt;Linking Multiple Levels of Chaining&lt;/h3&gt;
&lt;p&gt;You can link together multiple levels of optional chaining to drill down to properties, methods, and subscripts deeper within a model. However, multiple levels of optional chaining do not add more levels of optionality to the returned value. &lt;/p&gt;
&lt;p&gt;To put it another way: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If the type you are trying to retrieve is not optional, it will become optional because of the optional chaining. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If the type you are trying to retrieve is &lt;em&gt;already&lt;/em&gt; optional, it will not become &lt;em&gt;more&lt;/em&gt; optional because of the chaining. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Therefore: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If you try to retrieve an &lt;code&gt;Int&lt;/code&gt; value through optional chaining, an &lt;code&gt;Int?&lt;/code&gt; is always returned, no matter how many levels of chaining are used. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Similarly, if you try to retrieve an &lt;code&gt;Int?&lt;/code&gt; value through optional chaining, an &lt;code&gt;Int?&lt;/code&gt; is always returned, no matter how many levels of chaining are used. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example below tries to access the &lt;code&gt;street&lt;/code&gt; property of the &lt;code&gt;address&lt;/code&gt; property of the &lt;code&gt;residence&lt;/code&gt; property of &lt;code&gt;john&lt;/code&gt;. There are &lt;em&gt;two&lt;/em&gt; levels of optional chaining in use here, to chain through the &lt;code&gt;residence&lt;/code&gt; and &lt;code&gt;address&lt;/code&gt; properties, both of which are of optional type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let johnsStreet = john.residence?.address?.street {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's street name is \\(johnsStreet).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to retrieve the address.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Unable to retrieve the address."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The value of &lt;code&gt;john.residence&lt;/code&gt; currently contains a valid &lt;code&gt;Residence&lt;/code&gt; instance. However, the value of &lt;code&gt;john.residence.address&lt;/code&gt; is currently &lt;code&gt;nil&lt;/code&gt;. Because of this, the call to &lt;code&gt;john.residence?.address?.street&lt;/code&gt; fails. &lt;/p&gt;
&lt;p&gt;Note that in the example above, you are trying to retrieve the value of the &lt;code&gt;street&lt;/code&gt; property. The type of this property is &lt;code&gt;String?&lt;/code&gt;. The return value of &lt;code&gt;john.residence?.address?.street&lt;/code&gt; is therefore also &lt;code&gt;String?&lt;/code&gt;, even though two levels of optional chaining are applied in addition to the underlying optional type of the property. &lt;/p&gt;
&lt;p&gt;If you set an actual &lt;code&gt;Address&lt;/code&gt; instance as the value for &lt;code&gt;john.residence.address&lt;/code&gt;, and set an actual value for the address’s &lt;code&gt;street&lt;/code&gt; property, you can access the value of the &lt;code&gt;street&lt;/code&gt; property through multilevel optional chaining: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let johnsAddress = Address()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;johnsAddress.buildingName = "The Larches"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;johnsAddress.street = "Laurel Street"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;john.residence!.address = johnsAddress&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let johnsStreet = john.residence?.address?.street {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's street name is \\(johnsStreet).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Unable to retrieve the address.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John's street name is Laurel Street."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note the use of an exclamation mark during the assignment of an address instance to &lt;code&gt;john.residence.address&lt;/code&gt;. The &lt;code&gt;john.residence&lt;/code&gt; property has an optional type, and so you need to unwrap its actual value with an exclamation mark before accessing the residence’s &lt;code&gt;address&lt;/code&gt; property. &lt;/p&gt;
&lt;h3&gt;Chaining on Methods with Optional Return Values&lt;/h3&gt;
&lt;p&gt;The previous example shows how to retrieve the value of a property of optional type through optional chaining. You can also use optional chaining to call a method that returns a value of optional type, and to chain on that method’s return value if needed. &lt;/p&gt;
&lt;p&gt;The example below calls the &lt;code&gt;Address&lt;/code&gt; class’s &lt;code&gt;buildingIdentifier&lt;/code&gt; method through optional chaining. This method returns a value of type &lt;code&gt;String?&lt;/code&gt;. As described above, the ultimate return type of this method call after optional chaining is also &lt;code&gt;String?&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let buildingIdentifier = john.residence?.address?.buildingIdentifier() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's building identifier is \\(buildingIdentifier).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John's building identifier is The Larches."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you want to perform further optional chaining on this method’s return value, place the optional chaining question mark &lt;em&gt;after&lt;/em&gt; the method’s parentheses: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let beginsWithThe =&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;john.residence?.address?.buildingIdentifier()?.hasPrefix("The") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if beginsWithThe {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's building identifier begins with \"The\".")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("John's building identifier does not begin with \"The\".")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "John's building identifier begins with "The"."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In the example above, you place the optional chaining question mark &lt;em&gt;after&lt;/em&gt; the parentheses, because the optional value you are chaining on is the &lt;code&gt;buildingIdentifier&lt;/code&gt; method’s return value, and not the &lt;code&gt;buildingIdentifier&lt;/code&gt; method itself. &lt;/p&gt;
&lt;h2&gt;Type Casting&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Type casting&lt;/em&gt; is a way to check the type of an instance, and/or to treat that instance as if it is a different superclass or subclass from somewhere else in its own class hierarchy. &lt;/p&gt;
&lt;p&gt;Type casting in Swift is implemented with the &lt;code&gt;is&lt;/code&gt; and &lt;code&gt;as&lt;/code&gt; operators. These two operators provide a simple and expressive way to check the type of a value or cast a value to a different type. &lt;/p&gt;
&lt;p&gt;You can also use type casting to check whether a type conforms to a protocol, as described in Checking for Protocol Conformance. &lt;/p&gt;
&lt;h3&gt;Defining a Class Hierarchy for Type Casting&lt;/h3&gt;
&lt;p&gt;You can use type casting with a hierarchy of classes and subclasses to check the type of a particular class instance and to cast that instance to another class within the same hierarchy. The three code snippets below define a hierarchy of classes and an array containing instances of those classes, for use in an example of type casting. &lt;/p&gt;
&lt;p&gt;The first snippet defines a new base class called &lt;code&gt;MediaItem&lt;/code&gt;. This class provides basic functionality for any kind of item that appears in a digital media library. Specifically, it declares a &lt;code&gt;name&lt;/code&gt; property of type &lt;code&gt;String&lt;/code&gt;, and an &lt;code&gt;init name&lt;/code&gt; initializer. (It is assumed that all media items, including all movies and songs, will have a name.) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class MediaItem {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The next snippet defines two subclasses of &lt;code&gt;MediaItem&lt;/code&gt;. The first subclass, &lt;code&gt;Movie&lt;/code&gt;, encapsulates additional information about a movie or film. It adds a &lt;code&gt;director&lt;/code&gt; property on top of the base &lt;code&gt;MediaItem&lt;/code&gt; class, with a corresponding initializer. The second subclass, &lt;code&gt;Song&lt;/code&gt;, adds an &lt;code&gt;artist&lt;/code&gt; property and initializer on top of the base class: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Movie: MediaItem {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var director: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, director: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.director = director&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init(name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class Song: MediaItem {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var artist: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, artist: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.artist = artist&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init(name: name)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The final snippet creates a constant array called &lt;code&gt;library&lt;/code&gt;, which contains two &lt;code&gt;Movie&lt;/code&gt; instances and three &lt;code&gt;Song&lt;/code&gt; instances. The type of the &lt;code&gt;library&lt;/code&gt; array is inferred by initializing it with the contents of an array literal. Swift’s type checker is able to deduce that &lt;code&gt;Movie&lt;/code&gt; and &lt;code&gt;Song&lt;/code&gt; have a common superclass of &lt;code&gt;MediaItem&lt;/code&gt;, and so it infers a type of &lt;code&gt;[MediaItem]&lt;/code&gt; for the &lt;code&gt;library&lt;/code&gt; array: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let library = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Movie(name: "Casablanca", director: "Michael Curtiz"),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Song(name: "Blue Suede Shoes", artist: "Elvis Presley"),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Movie(name: "Citizen Kane", director: "Orson Welles"),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Song(name: "The One And Only", artist: "Chesney Hawkes"),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Song(name: "Never Gonna Give You Up", artist: "Rick Astley")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the type of "library" is inferred to be [MediaItem]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The items stored in &lt;code&gt;library&lt;/code&gt; are still &lt;code&gt;Movie&lt;/code&gt; and &lt;code&gt;Song&lt;/code&gt; instances behind the scenes. However, if you iterate over the contents of this array, the items you receive back are typed as &lt;code&gt;MediaItem&lt;/code&gt;, and not as &lt;code&gt;Movie&lt;/code&gt; or &lt;code&gt;Song&lt;/code&gt;. In order to work with them as their native type, you need to &lt;em&gt;check&lt;/em&gt; their type, or &lt;em&gt;downcast&lt;/em&gt; them to a different type, as described below. &lt;/p&gt;
&lt;h3&gt;Checking Type&lt;/h3&gt;
&lt;p&gt;Use the &lt;em&gt;type check operator&lt;/em&gt; (&lt;code&gt;is&lt;/code&gt;) to check whether an instance is of a certain subclass type. The type check operator returns &lt;code&gt;true&lt;/code&gt; if the instance is of that subclass type and &lt;code&gt;false&lt;/code&gt; if it is not. &lt;/p&gt;
&lt;p&gt;The example below defines two variables, &lt;code&gt;movieCount&lt;/code&gt; and &lt;code&gt;songCount&lt;/code&gt;, which count the number of &lt;code&gt;Movie&lt;/code&gt; and &lt;code&gt;Song&lt;/code&gt; instances in the &lt;code&gt;library&lt;/code&gt; array: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var movieCount = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var songCount = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for item in library {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if item is Movie {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;++movieCount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if item is Song {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;++songCount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Media library contains \\(movieCount) movies and \\(songCount) songs")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Media library contains 2 movies and 3 songs"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example iterates through all items in the &lt;code&gt;library&lt;/code&gt; array. On each pass, the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop sets the &lt;code&gt;item&lt;/code&gt; constant to the next &lt;code&gt;MediaItem&lt;/code&gt; in the array. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;item is Movie&lt;/code&gt; returns &lt;code&gt;true&lt;/code&gt; if the current &lt;code&gt;MediaItem&lt;/code&gt; is a &lt;code&gt;Movie&lt;/code&gt; instance and &lt;code&gt;false&lt;/code&gt; if it is not. Similarly, &lt;code&gt;item is Song&lt;/code&gt; checks whether the item is a &lt;code&gt;Song&lt;/code&gt; instance. At the end of the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop, the values of &lt;code&gt;movieCount&lt;/code&gt; and &lt;code&gt;songCount&lt;/code&gt; contain a count of how many &lt;code&gt;MediaItem&lt;/code&gt; instances were found of each type. &lt;/p&gt;
&lt;h3&gt;Downcasting&lt;/h3&gt;
&lt;p&gt;A constant or variable of a certain class type may actually refer to an instance of a subclass behind the scenes. Where you believe this is the case, you can try to &lt;em&gt;downcast&lt;/em&gt; to the subclass type with the &lt;em&gt;type cast operator&lt;/em&gt; (&lt;code&gt;as&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Because downcasting can fail, the type cast operator comes in two different forms. The optional form, &lt;code&gt;as?&lt;/code&gt;, returns an optional value of the type you are trying to downcast to. The forced form, &lt;code&gt;as&lt;/code&gt;, attempts the downcast and force-unwraps the result as a single compound action. &lt;/p&gt;
&lt;p&gt;Use the optional form of the type cast operator (&lt;code&gt;as?&lt;/code&gt;) when you are not sure if the downcast will succeed. This form of the operator will always return an optional value, and the value will be &lt;code&gt;nil&lt;/code&gt; if the downcast was not possible. This enables you to check for a successful downcast. &lt;/p&gt;
&lt;p&gt;Use the forced form of the type cast operator (&lt;code&gt;as&lt;/code&gt;) only when you are sure that the downcast will always succeed. This form of the operator will trigger a runtime error if you try to downcast to an incorrect class type. &lt;/p&gt;
&lt;p&gt;The example below iterates over each &lt;code&gt;MediaItem&lt;/code&gt; in &lt;code&gt;library&lt;/code&gt;, and prints an appropriate description for each item. To do this, it needs to access each item as a true &lt;code&gt;Movie&lt;/code&gt; or &lt;code&gt;Song&lt;/code&gt;, and not just as a &lt;code&gt;MediaItem&lt;/code&gt;. This is necessary in order for it to be able to access the &lt;code&gt;director&lt;/code&gt; or &lt;code&gt;artist&lt;/code&gt; property of a &lt;code&gt;Movie&lt;/code&gt; or &lt;code&gt;Song&lt;/code&gt; for use in the description. &lt;/p&gt;
&lt;p&gt;In this example, each item in the array might be a &lt;code&gt;Movie&lt;/code&gt;, or it might be a &lt;code&gt;Song&lt;/code&gt;. You don’t know in advance which actual class to use for each item, and so it is appropriate to use the optional form of the type cast operator (&lt;code&gt;as?&lt;/code&gt;) to check the downcast each time through the loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for item in library {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let movie = item as? Movie {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Movie: '\\(movie.name)', dir. \\(movie.director)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if let song = item as? Song {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Song: '\\(song.name)', by \\(song.artist)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: 'Casablanca', dir. Michael Curtiz&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Song: 'Blue Suede Shoes', by Elvis Presley&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: 'Citizen Kane', dir. Orson Welles&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Song: 'The One And Only', by Chesney Hawkes&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Song: 'Never Gonna Give You Up', by Rick Astley&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example starts by trying to downcast the current &lt;code&gt;item&lt;/code&gt; as a &lt;code&gt;Movie&lt;/code&gt;. Because &lt;code&gt;item&lt;/code&gt; is a &lt;code&gt;MediaItem&lt;/code&gt; instance, it’s possible that it &lt;em&gt;might&lt;/em&gt; be a &lt;code&gt;Movie&lt;/code&gt;; equally, it’s also possible that it might be a &lt;code&gt;Song&lt;/code&gt;, or even just a base &lt;code&gt;MediaItem&lt;/code&gt;. Because of this uncertainty, the &lt;code&gt;as?&lt;/code&gt; form of the type cast operator returns an &lt;em&gt;optional&lt;/em&gt; value when attempting to downcast to a subclass type. The result of &lt;code&gt;item as? Movie&lt;/code&gt; is of type &lt;code&gt;Movie?&lt;/code&gt;, or “optional &lt;code&gt;Movie&lt;/code&gt;”. &lt;/p&gt;
&lt;p&gt;Downcasting to &lt;code&gt;Movie&lt;/code&gt; fails when applied to the &lt;code&gt;Song&lt;/code&gt; instances in the library array. To cope with this, the example above uses optional binding to check whether the optional &lt;code&gt;Movie&lt;/code&gt; actually contains a value (that is, to find out whether the downcast succeeded.) This optional binding is written “&lt;code&gt;if let movie = item as? Movie&lt;/code&gt;”, which can be read as: &lt;/p&gt;
&lt;p&gt;“Try to access &lt;code&gt;item&lt;/code&gt; as a &lt;code&gt;Movie&lt;/code&gt;. If this is successful, set a new temporary constant called &lt;code&gt;movie&lt;/code&gt; to the value stored in the returned optional &lt;code&gt;Movie&lt;/code&gt;.” &lt;/p&gt;
&lt;p&gt;If the downcasting succeeds, the properties of &lt;code&gt;movie&lt;/code&gt; are then used to print a description for that &lt;code&gt;Movie&lt;/code&gt; instance, including the name of its &lt;code&gt;director&lt;/code&gt;. A similar principle is used to check for &lt;code&gt;Song&lt;/code&gt; instances, and to print an appropriate description (including &lt;code&gt;artist&lt;/code&gt; name) whenever a &lt;code&gt;Song&lt;/code&gt; is found in the library. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Casting does not actually modify the instance or change its values. The underlying instance remains the same; it is simply treated and accessed as an instance of the type to which it has been cast. &lt;/p&gt;
&lt;h3&gt;Type Casting for Any and AnyObject&lt;/h3&gt;
&lt;p&gt;Swift provides two special type aliases for working with non-specific types: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;AnyObject&lt;/code&gt; can represent an instance of any class type. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;Any&lt;/code&gt; can represent an instance of any type at all, including function types. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Use &lt;code&gt;Any&lt;/code&gt; and &lt;code&gt;AnyObject&lt;/code&gt; only when you explicitly need the behavior and capabilities they provide. It is always better to be specific about the types you expect to work with in your code. &lt;/p&gt;
&lt;h4&gt;AnyObject&lt;/h4&gt;
&lt;p&gt;When working with Cocoa APIs, it is common to receive an array with a type of &lt;code&gt;[AnyObject]&lt;/code&gt;, or “an array of values of any object type”. This is because Objective-C does not have explicitly typed arrays. However, you can often be confident about the type of objects contained in such an array just from the information you know about the API that provided the array. &lt;/p&gt;
&lt;p&gt;In these situations, you can use the forced version of the type cast operator (&lt;code&gt;as&lt;/code&gt;) to downcast each item in the array to a more specific class type than &lt;code&gt;AnyObject&lt;/code&gt;, without the need for optional unwrapping. &lt;/p&gt;
&lt;p&gt;The example below defines an array of type &lt;code&gt;[AnyObject]&lt;/code&gt; and populates this array with three instances of the &lt;code&gt;Movie&lt;/code&gt; class: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someObjects: [AnyObject] = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Movie(name: "2001: A Space Odyssey", director: "Stanley Kubrick"),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Movie(name: "Moon", director: "Duncan Jones"),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Movie(name: "Alien", director: "Ridley Scott")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because this array is known to contain only &lt;code&gt;Movie&lt;/code&gt; instances, you can downcast and unwrap directly to a non-optional &lt;code&gt;Movie&lt;/code&gt; with the forced version of the type cast operator (&lt;code&gt;as&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for object in someObjects {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let movie = object as Movie&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Movie: '\\(movie.name)', dir. \\(movie.director)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: '2001: A Space Odyssey', dir. Stanley Kubrick&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: 'Moon', dir. Duncan Jones&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: 'Alien', dir. Ridley Scott&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For an even shorter form of this loop, downcast the &lt;code&gt;someObjects&lt;/code&gt; array to a type of &lt;code&gt;[Movie]&lt;/code&gt; instead of downcasting each item: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for movie in someObjects as [Movie] {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Movie: '\\(movie.name)', dir. \\(movie.director)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: '2001: A Space Odyssey', dir. Stanley Kubrick&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: 'Moon', dir. Duncan Jones&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Movie: 'Alien', dir. Ridley Scott&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Any&lt;/h4&gt;
&lt;p&gt;Here’s an example of using &lt;code&gt;Any&lt;/code&gt; to work with a mix of different types, including function types and non-class types. The example creates an array called &lt;code&gt;things&lt;/code&gt;, which can store values of type &lt;code&gt;Any&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var things = [Any]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append(0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append(0.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append(42)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append(3.14159)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append("hello")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append((3.0, 5.0))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append(Movie(name: "Ghostbusters", director: "Ivan Reitman"))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;things.append({ (name: String) -&amp;gt; String in "Hello, \\(name)" })&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;things&lt;/code&gt; array contains two &lt;code&gt;Int&lt;/code&gt; values, two &lt;code&gt;Double&lt;/code&gt; values, a &lt;code&gt;String&lt;/code&gt; value, a tuple of type &lt;code&gt;(Double, Double)&lt;/code&gt;, the movie “Ghostbusters”, and a closure expression that takes a &lt;code&gt;String&lt;/code&gt; value and returns another &lt;code&gt;String&lt;/code&gt; value. &lt;/p&gt;
&lt;p&gt;You can use the &lt;code&gt;is&lt;/code&gt; and &lt;code&gt;as&lt;/code&gt; operators in a &lt;code&gt;switch&lt;/code&gt; statement’s cases to discover the specific type of a constant or variable that is known only to be of type &lt;code&gt;Any&lt;/code&gt; or &lt;code&gt;AnyObject&lt;/code&gt;. The example below iterates over the items in the &lt;code&gt;things&lt;/code&gt; array and queries the type of each item with a &lt;code&gt;switch&lt;/code&gt; statement. Several of the &lt;code&gt;switch&lt;/code&gt; statement’s cases bind their matched value to a constant of the specified type to enable its value to be printed: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for thing in things {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch thing {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 0 as Int:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("zero as an Int")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 0 as Double:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("zero as a Double")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let someInt as Int:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("an integer value of \\(someInt)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let someDouble as Double where someDouble &amp;gt; 0:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("a positive double value of \\(someDouble)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case is Double:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("some other double value that I don't want to print")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let someString as String:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("a string value of \"\\(someString)\"")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let (x, y) as (Double, Double):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("an (x, y) point at \\(x), \\(y)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let movie as Movie:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("a movie called '\\(movie.name)', dir. \\(movie.director)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let stringConverter as String -&amp;gt; String:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(stringConverter("Michael"))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("something else")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// zero as an Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// zero as a Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// an integer value of 42&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a positive double value of 3.14159&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a string value of "hello"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// an (x, y) point at 3.0, 5.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a movie called 'Ghostbusters', dir. Ivan Reitman&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello, Michael&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The cases of a &lt;code&gt;switch&lt;/code&gt; statement use the forced version of the type cast operator (&lt;code&gt;as&lt;/code&gt;, not &lt;code&gt;as?&lt;/code&gt;) to check and cast to a specific type. This check is always safe within the context of a &lt;code&gt;switch&lt;/code&gt; case statement. &lt;/p&gt;
&lt;h2&gt;Nested Types&lt;/h2&gt;
&lt;p&gt;Enumerations are often created to support a specific class or structure’s functionality. Similarly, it can be convenient to define utility classes and structures purely for use within the context of a more complex type. To accomplish this, Swift enables you to define &lt;em&gt;nested types&lt;/em&gt;, whereby you nest supporting enumerations, classes, and structures within the definition of the type they support. &lt;/p&gt;
&lt;p&gt;To nest a type within another type, write its definition within the outer braces of the type it supports. Types can be nested to as many levels as are required. &lt;/p&gt;
&lt;h3&gt;Nested Types in Action&lt;/h3&gt;
&lt;p&gt;The example below defines a structure called &lt;code&gt;BlackjackCard&lt;/code&gt;, which models a playing card as used in the game of Blackjack. The &lt;code&gt;BlackJack&lt;/code&gt; structure contains two nested enumeration types called &lt;code&gt;Suit&lt;/code&gt; and &lt;code&gt;Rank&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;In Blackjack, the Ace cards have a value of either one or eleven. This feature is represented by a structure called &lt;code&gt;Values&lt;/code&gt;, which is nested within the &lt;code&gt;Rank&lt;/code&gt; enumeration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct BlackjackCard {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// nested Suit enumeration&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;enum Suit: Character {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Spades = "♠", Hearts = "♡", Diamonds = "♢", Clubs = "♣"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// nested Rank enumeration&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;enum Rank: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Two = 2, Three, Four, Five, Six, Seven, Eight, Nine, Ten&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Jack, Queen, King, Ace&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct Values {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let first: Int, second: Int?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var values: Values {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch self {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Ace:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Values(first: 1, second: 11)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Jack, .Queen, .King:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Values(first: 10, second: nil)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Values(first: self.rawValue, second: nil)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// BlackjackCard properties and methods&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let rank: Rank, suit: Suit&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var description: String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var output = "suit is \\(suit.rawValue),"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;output += " value is \\(rank.values.first)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let second = rank.values.second {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;output += " or \\(second)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return output&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Suit&lt;/code&gt; enumeration describes the four common playing card suits, together with a raw &lt;code&gt;Character&lt;/code&gt; value to represent their symbol. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Rank&lt;/code&gt; enumeration describes the thirteen possible playing card ranks, together with a raw &lt;code&gt;Int&lt;/code&gt; value to represent their face value. (This raw &lt;code&gt;Int&lt;/code&gt; value is not used for the Jack, Queen, King, and Ace cards.) &lt;/p&gt;
&lt;p&gt;As mentioned above, the &lt;code&gt;Rank&lt;/code&gt; enumeration defines a further nested structure of its own, called &lt;code&gt;Values&lt;/code&gt;. This structure encapsulates the fact that most cards have one value, but the Ace card has two values. The &lt;code&gt;Values&lt;/code&gt; structure defines two properties to represent this: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;first&lt;/code&gt;, of type &lt;code&gt;Int&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;second&lt;/code&gt;, of type &lt;code&gt;Int?&lt;/code&gt;, or “optional &lt;code&gt;Int&lt;/code&gt;” &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;Rank&lt;/code&gt; also defines a computed property, &lt;code&gt;values&lt;/code&gt;, which returns an instance of the &lt;code&gt;Values&lt;/code&gt; structure. This computed property considers the rank of the card and initializes a new &lt;code&gt;Values&lt;/code&gt; instance with appropriate values based on its rank. It uses special values for &lt;code&gt;Jack&lt;/code&gt;, &lt;code&gt;Queen&lt;/code&gt;, &lt;code&gt;King&lt;/code&gt;, and &lt;code&gt;Ace&lt;/code&gt;. For the numeric cards, it uses the rank’s raw &lt;code&gt;Int&lt;/code&gt; value. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;BlackjackCard&lt;/code&gt; structure itself has two properties—&lt;code&gt;rank&lt;/code&gt; and &lt;code&gt;suit&lt;/code&gt;. It also defines a computed property called &lt;code&gt;description&lt;/code&gt;, which uses the values stored in &lt;code&gt;rank&lt;/code&gt; and &lt;code&gt;suit&lt;/code&gt; to build a description of the name and value of the card. The &lt;code&gt;description&lt;/code&gt; property uses optional binding to check whether there is a second value to display, and if so, inserts additional description detail for that second value. &lt;/p&gt;
&lt;p&gt;Because &lt;code&gt;BlackjackCard&lt;/code&gt; is a structure with no custom initializers, it has an implicit memberwise initializer, as described in Memberwise Initializers for Structure Types. You can use this initializer to initialize a new constant called &lt;code&gt;theAceOfSpades&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let theAceOfSpades = BlackjackCard(rank: .Ace, suit: .Spades)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("theAceOfSpades: \\(theAceOfSpades.description)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "theAceOfSpades: suit is ♠, value is 1 or 11"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Even though &lt;code&gt;Rank&lt;/code&gt; and &lt;code&gt;Suit&lt;/code&gt; are nested within &lt;code&gt;BlackjackCard&lt;/code&gt;, their type can be inferred from context, and so the initialization of this instance is able to refer to the enumeration members by their member names (&lt;code&gt;.Ace&lt;/code&gt; and &lt;code&gt;.Spades&lt;/code&gt;) alone. In the example above, the &lt;code&gt;description&lt;/code&gt; property correctly reports that the Ace of Spades has a value of &lt;code&gt;1&lt;/code&gt; or &lt;code&gt;11&lt;/code&gt;. &lt;/p&gt;
&lt;h3&gt;Referring to Nested Types&lt;/h3&gt;
&lt;p&gt;To use a nested type outside of its definition context, prefix its name with the name of the type it is nested within: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let heartsSymbol = BlackjackCard.Suit.Hearts.rawValue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// heartsSymbol is "♡"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For the example above, this enables the names of &lt;code&gt;Suit&lt;/code&gt;, &lt;code&gt;Rank&lt;/code&gt;, and &lt;code&gt;Values&lt;/code&gt; to be kept deliberately short, because their names are naturally qualified by the context in which they are defined. &lt;/p&gt;
&lt;h2&gt;Extensions&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Extensions&lt;/em&gt; add new functionality to an existing class, structure, or enumeration type. This includes the ability to extend types for which you do not have access to the original source code (known as &lt;em&gt;retroactive modeling&lt;/em&gt;). Extensions are similar to categories in Objective-C. (Unlike Objective-C categories, Swift extensions do not have names.) &lt;/p&gt;
&lt;p&gt;Extensions in Swift can: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Add computed properties and computed static properties &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Define instance methods and type methods &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Provide new initializers &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Define subscripts &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Define and use new nested types &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Make an existing type conform to a protocol &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Extensions can add new functionality to a type, but they cannot override existing functionality. &lt;/p&gt;
&lt;h3&gt;Extension Syntax&lt;/h3&gt;
&lt;p&gt;Declare extensions with the &lt;code&gt;extension&lt;/code&gt; keyword: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension SomeType {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// new functionality to add to SomeType goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;An extension can extend an existing type to make it adopt one or more protocols. Where this is the case, the protocol names are written in exactly the same way as for a class or structure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension SomeType: SomeProtocol, AnotherProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// implementation of protocol requirements goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Adding protocol conformance in this way is described in Adding Protocol Conformance with an Extension. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you define an extension to add new functionality to an existing type, the new functionality will be available on all existing instances of that type, even if they were created before the extension was defined. &lt;/p&gt;
&lt;h3&gt;Computed Properties&lt;/h3&gt;
&lt;p&gt;Extensions can add computed instance properties and computed type properties to existing types. This example adds five computed instance properties to Swift’s built-in &lt;code&gt;Double&lt;/code&gt; type, to provide basic support for working with distance units: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var km: Double { return self * 1_000.0 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var m: Double { return self }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var cm: Double { return self / 100.0 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var mm: Double { return self / 1_000.0 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var ft: Double { return self / 3.28084 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let oneInch = 25.4.mm&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("One inch is \\(oneInch) meters")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "One inch is 0.0254 meters"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let threeFeet = 3.ft&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Three feet is \\(threeFeet) meters")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Three feet is 0.914399970739201 meters"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;These computed properties express that a &lt;code&gt;Double&lt;/code&gt; value should be considered as a certain unit of length. Although they are implemented as computed properties, the names of these properties can be appended to a floating-point literal value with dot syntax, as a way to use that literal value to perform distance conversions. &lt;/p&gt;
&lt;p&gt;In this example, a &lt;code&gt;Double&lt;/code&gt; value of &lt;code&gt;1.0&lt;/code&gt; is considered to represent “one meter”. This is why the &lt;code&gt;m&lt;/code&gt; computed property returns &lt;code&gt;self&lt;/code&gt;—the expression &lt;code&gt;1.m&lt;/code&gt; is considered to calculate a &lt;code&gt;Double&lt;/code&gt; value of &lt;code&gt;1.0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Other units require some conversion to be expressed as a value measured in meters. One kilometer is the same as 1,000 meters, so the &lt;code&gt;km&lt;/code&gt; computed property multiplies the value by &lt;code&gt;1_000.00&lt;/code&gt; to convert into a number expressed in meters. Similarly, there are 3.28024 feet in a meter, and so the &lt;code&gt;ft&lt;/code&gt; computed property divides the underlying &lt;code&gt;Double&lt;/code&gt; value by &lt;code&gt;3.28024&lt;/code&gt;, to convert it from feet to meters. &lt;/p&gt;
&lt;p&gt;These properties are read-only computed properties, and so they are expressed without the &lt;code&gt;get&lt;/code&gt; keyword, for brevity. Their return value is of type &lt;code&gt;Double&lt;/code&gt;, and can be used within mathematical calculations wherever a &lt;code&gt;Double&lt;/code&gt; is accepted: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let aMarathon = 42.km \+ 195.m&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("A marathon is \\(aMarathon) meters long")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "A marathon is 42195.0 meters long"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Extensions can add new computed properties, but they cannot add stored properties, or add property observers to existing properties. &lt;/p&gt;
&lt;h3&gt;Initializers&lt;/h3&gt;
&lt;p&gt;Extensions can add new initializers to existing types. This enables you to extend other types to accept your own custom types as initializer parameters, or to provide additional initialization options that were not included as part of the type’s original implementation. &lt;/p&gt;
&lt;p&gt;Extensions can add new convenience initializers to a class, but they cannot add new designated initializers or deinitializers to a class. Designated initializers and deinitializers must always be provided by the original class implementation. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you use an extension to add an initializer to a value type that provides default values for all of its stored properties and does not define any custom initializers, you can call the default initializer and memberwise initializer for that value type from within your extension’s initializer. &lt;/p&gt;
&lt;p&gt;This would not be the case if you had written the initializer as part of the value type’s original implementation, as described in Initializer Delegation for Value Types. &lt;/p&gt;
&lt;p&gt;The example below defines a custom &lt;code&gt;Rect&lt;/code&gt; structure to represent a geometric rectangle. The example also defines two supporting structures called &lt;code&gt;Size&lt;/code&gt; and &lt;code&gt;Point&lt;/code&gt;, both of which provide default values of &lt;code&gt;0.0&lt;/code&gt; for all of their properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Size {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var width = 0.0, height = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct Rect {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var origin = Point()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var size = Size()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because the &lt;code&gt;Rect&lt;/code&gt; structure provides default values for all of its properties, it receives a default initializer and a memberwise initializer automatically, as described in Default Initializers. These initializers can be used to create new &lt;code&gt;Rect&lt;/code&gt; instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let defaultRect = Rect()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let memberwiseRect = Rect(origin: Point(x: 2.0, y: 2.0),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;size: Size(width: 5.0, height: 5.0))&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can extend the &lt;code&gt;Rect&lt;/code&gt; structure to provide an additional initializer that takes a specific center point and size: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Rect {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(center: Point, size: Size) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let originX = center.x \- (size.width / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let originY = center.y \- (size.height / 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.init(origin: Point(x: originX, y: originY), size: size)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This new initializer starts by calculating an appropriate origin point based on the provided &lt;code&gt;center&lt;/code&gt; point and &lt;code&gt;size&lt;/code&gt; value. The initializer then calls the structure’s automatic memberwise initializer &lt;code&gt;init(origin:size:)&lt;/code&gt;, which stores the new origin and size values in the appropriate properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let centerRect = Rect(center: Point(x: 4.0, y: 4.0),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;size: Size(width: 3.0, height: 3.0))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// centerRect's origin is (2.5, 2.5) and its size is (3.0, 3.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you provide a new initializer with an extension, you are still responsible for making sure that each instance is fully initialized once the initializer completes. &lt;/p&gt;
&lt;h3&gt;Methods&lt;/h3&gt;
&lt;p&gt;Extensions can add new instance methods and type methods to existing types. The following example adds a new instance method called &lt;code&gt;repetitions&lt;/code&gt; to the &lt;code&gt;Int&lt;/code&gt; type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func repetitions(task: () -&amp;gt; ()) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for _ in 0..&amp;lt;self {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;task()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;repetitions&lt;/code&gt; method takes a single argument of type &lt;code&gt;() -&amp;gt; ()&lt;/code&gt;, which indicates a function that has no parameters and does not return a value. &lt;/p&gt;
&lt;p&gt;After defining this extension, you can call the &lt;code&gt;repetitions&lt;/code&gt; method on any integer number to perform a task that many number of times: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;3.repetitions({&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Hello!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;})&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Hello!&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Use trailing closure syntax to make the call more succinct: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;3.repetitions {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Goodbye!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Goodbye!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Goodbye!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Goodbye!&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Mutating Instance Methods&lt;/h4&gt;
&lt;p&gt;Instance methods added with an extension can also modify (or &lt;em&gt;mutate&lt;/em&gt;) the instance itself. Structure and enumeration methods that modify &lt;code&gt;self&lt;/code&gt; or its properties must mark the instance method as &lt;code&gt;mutating&lt;/code&gt;, just like mutating methods from an original implementation. &lt;/p&gt;
&lt;p&gt;The example below adds a new mutating method called &lt;code&gt;square&lt;/code&gt; to Swift’s &lt;code&gt;Int&lt;/code&gt; type, which squares the original value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func square() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = self * self&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someInt = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someInt.square()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someInt is now 9&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Subscripts&lt;/h3&gt;
&lt;p&gt;Extensions can add new subscripts to an existing type. This example adds an integer subscript to Swift’s built-in &lt;code&gt;Int&lt;/code&gt; type. This subscript &lt;code&gt;[n]&lt;/code&gt; returns the decimal digit &lt;code&gt;n&lt;/code&gt; places in from the right of the number: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;123456789[0]&lt;/code&gt; returns &lt;code&gt;9&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;123456789[1]&lt;/code&gt; returns &lt;code&gt;8&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;…and so on: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;subscript(var digitIndex: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var decimalBase = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;while digitIndex &amp;gt; 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;decimalBase *= 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;\--digitIndex&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return (self / decimalBase) % 10&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;746381295[0]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;746381295[1]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns 9&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;746381295[2]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;746381295[8]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns 7&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the &lt;code&gt;Int&lt;/code&gt; value does not have enough digits for the requested index, the subscript implementation returns &lt;code&gt;0&lt;/code&gt;, as if the number had been padded with zeroes to the left: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;746381295[9]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns 0, as if you had requested:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;0746381295[9]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Nested Types&lt;/h3&gt;
&lt;p&gt;Extensions can add new nested types to existing classes, structures and enumerations: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;enum Kind {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Negative, Zero, Positive&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var kind: Kind {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch self {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case 0:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return .Zero&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let x where x &amp;gt; 0:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return .Positive&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return .Negative&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example adds a new nested enumeration to &lt;code&gt;Int&lt;/code&gt;. This enumeration, called &lt;code&gt;Kind&lt;/code&gt;, expresses the kind of number that a particular integer represents. Specifically, it expresses whether the number is negative, zero, or positive. &lt;/p&gt;
&lt;p&gt;This example also adds a new computed instance property to &lt;code&gt;Int&lt;/code&gt;, called &lt;code&gt;kind&lt;/code&gt;, which returns the appropriate &lt;code&gt;Kind&lt;/code&gt; enumeration member for that integer. &lt;/p&gt;
&lt;p&gt;The nested enumeration can now be used with any &lt;code&gt;Int&lt;/code&gt; value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func printIntegerKinds(numbers: [Int]) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for number in numbers {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch number.kind {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Negative:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("- ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Zero:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("0 ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case .Positive:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("+ ")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;print("\n")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;printIntegerKinds([3, 19, -27, 0, -6, 0, 7])&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "+ + - 0 - 0 +"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This function, &lt;code&gt;printIntegerKinds&lt;/code&gt;, takes an input array of &lt;code&gt;Int&lt;/code&gt; values and iterates over those values in turn. For each integer in the array, the function considers the &lt;code&gt;kind&lt;/code&gt; computed property for that integer, and prints an appropriate description. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;&lt;code&gt;number.kind&lt;/code&gt; is already known to be of type &lt;code&gt;Int.Kind&lt;/code&gt;. Because of this, all of the &lt;code&gt;Int.Kind&lt;/code&gt; member values can be written in shorthand form inside the &lt;code&gt;switch&lt;/code&gt; statement, such as &lt;code&gt;.Negative&lt;/code&gt; rather than &lt;code&gt;Int.Kind.Negative&lt;/code&gt;. &lt;/p&gt;
&lt;h2&gt;Protocols&lt;/h2&gt;
&lt;p&gt;A &lt;em&gt;protocol&lt;/em&gt; defines a blueprint of methods, properties, and other requirements that suit a particular task or piece of functionality. The protocol doesn’t actually provide an implementation for any of these requirements—it only describes what an implementation will look like. The protocol can then be &lt;em&gt;adopted&lt;/em&gt; by a class, structure, or enumeration to provide an actual implementation of those requirements. Any type that satisfies the requirements of a protocol is said to &lt;em&gt;conform&lt;/em&gt; to that protocol. &lt;/p&gt;
&lt;p&gt;Protocols can require that conforming types have specific instance properties, instance methods, type methods, operators, and subscripts. &lt;/p&gt;
&lt;h3&gt;Protocol Syntax&lt;/h3&gt;
&lt;p&gt;You define protocols in a very similar way to classes, structures, and enumerations: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol SomeProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// protocol definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Custom types state that they adopt a particular protocol by placing the protocol’s name after the type’s name, separated by a colon, as part of their definition. Multiple protocols can be listed, and are separated by commas: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct SomeStructure: FirstProtocol, AnotherProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// structure definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If a class has a superclass, list the superclass name before any protocols it adopts, followed by a comma: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass: SomeSuperclass, FirstProtocol, AnotherProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// class definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Property Requirements&lt;/h3&gt;
&lt;p&gt;A protocol can require any conforming type to provide an instance property or type property with a particular name and type. The protocol doesn’t specify whether the property should be a stored property or a computed property—it only specifies the required property name and type. The protocol also specifies whether each property must be gettable or gettable &lt;em&gt;and&lt;/em&gt; settable. &lt;/p&gt;
&lt;p&gt;If a protocol requires a property to be gettable and settable, that property requirement cannot be fulfilled by a constant stored property or a read-only computed property. If the protocol only requires a property to be gettable, the requirement can be satisfied by any kind of property, and it is valid for the property to be also settable if this is useful for your own code. &lt;/p&gt;
&lt;p&gt;Property requirements are always declared as variable properties, prefixed with the &lt;code&gt;var&lt;/code&gt; keyword. Gettable and settable properties are indicated by writing &lt;code&gt;{ get set }&lt;/code&gt; after their type declaration, and gettable properties are indicated by writing &lt;code&gt;{ get }&lt;/code&gt;. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol SomeProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var mustBeSettable: Int { get set }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var doesNotNeedToBeSettable: Int { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Always prefix type property requirements with the &lt;code&gt;class&lt;/code&gt; keyword when you define them in a protocol. This rule pertains even though type property requirements are prefixed with the &lt;code&gt;static&lt;/code&gt; keyword when implemented by a structure or enumeration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol AnotherProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class var someTypeProperty: Int { get set }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s an example of a protocol with a single instance property requirement: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol FullyNamed {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var fullName: String { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;FullyNamed&lt;/code&gt; protocol requires a conforming type to provide a fully-qualified name. The protocol doesn’t specify anything else about the nature of the conforming type—it only specifies that the type must be able to provide a full name for itself. The protocol states that any &lt;code&gt;FullyNamed&lt;/code&gt; type must have a gettable instance property called &lt;code&gt;fullName&lt;/code&gt;, which is of type &lt;code&gt;String&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Here’s an example of a simple structure that adopts and conforms to the &lt;code&gt;FullyNamed&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Person: FullyNamed {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var fullName: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let john = Person(fullName: "John Appleseed")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// john.fullName is "John Appleseed"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a structure called &lt;code&gt;Person&lt;/code&gt;, which represents a specific named person. It states that it adopts the &lt;code&gt;FullyNamed&lt;/code&gt; protocol as part of the first line of its definition. &lt;/p&gt;
&lt;p&gt;Each instance of &lt;code&gt;Person&lt;/code&gt; has a single stored property called &lt;code&gt;fullName&lt;/code&gt;, which is of type &lt;code&gt;String&lt;/code&gt;. This matches the single requirement of the &lt;code&gt;FullyNamed&lt;/code&gt; protocol, and means that &lt;code&gt;Person&lt;/code&gt; has correctly conformed to the protocol. (Swift reports an error at compile-time if a protocol requirement is not fulfilled.) &lt;/p&gt;
&lt;p&gt;Here’s a more complex class, which also adopts and conforms to the &lt;code&gt;FullyNamed&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Starship: FullyNamed {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var prefix: String?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(name: String, prefix: String? = nil) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.name = name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.prefix = prefix&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var fullName: String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return (prefix != nil ? prefix! + " " : "") + name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var ncc1701 = Starship(name: "Enterprise", prefix: "USS")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// ncc1701.fullName is "USS Enterprise"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This class implements the &lt;code&gt;fullName&lt;/code&gt; property requirement as a computed read-only property for a starship. Each &lt;code&gt;Starship&lt;/code&gt; class instance stores a mandatory &lt;code&gt;name&lt;/code&gt; and an optional &lt;code&gt;prefix&lt;/code&gt;. The &lt;code&gt;fullName&lt;/code&gt; property uses the &lt;code&gt;prefix&lt;/code&gt; value if it exists, and prepends it to the beginning of &lt;code&gt;name&lt;/code&gt; to create a full name for the starship. &lt;/p&gt;
&lt;h3&gt;Method Requirements&lt;/h3&gt;
&lt;p&gt;Protocols can require specific instance methods and type methods to be implemented by conforming types. These methods are written as part of the protocol’s definition in exactly the same way as for normal instance and type methods, but without curly braces or a method body. Variadic parameters are allowed, subject to the same rules as for normal methods. Default values, however, cannot be specified for method parameters within a protocol’s definition. &lt;/p&gt;
&lt;p&gt;As with type property requirements, you always prefix type method requirements with the &lt;code&gt;class&lt;/code&gt; keyword when they are defined in a protocol. This is true even though type method requirements are prefixed with the &lt;code&gt;static&lt;/code&gt; keyword when implemented by a structure or enumeration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol SomeProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class func someTypeMethod()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The following example defines a protocol with a single instance method requirement: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol RandomNumberGenerator {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func random() -&amp;gt; Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This protocol, &lt;code&gt;RandomNumberGenerator&lt;/code&gt;, requires any conforming type to have an instance method called &lt;code&gt;random&lt;/code&gt;, which returns a &lt;code&gt;Double&lt;/code&gt; value whenever it is called. Although it is not specified as part of the protocol, it is assumed that this value will be a number from &lt;code&gt;0.0&lt;/code&gt; up to (but not including) &lt;code&gt;1.0&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;RandomNumberGenerator&lt;/code&gt; protocol does not make any assumptions about how each random number will be generated—it simply requires the generator to provide a standard way to generate a new random number. &lt;/p&gt;
&lt;p&gt;Here’s an implementation of a class that adopts and conforms to the &lt;code&gt;RandomNumberGenerator&lt;/code&gt; protocol. This class implements a pseudorandom number generator algorithm known as a &lt;em&gt;linear congruential generator&lt;/em&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class LinearCongruentialGenerator: RandomNumberGenerator {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var lastRandom = 42.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let m = 139968.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let a = 3877.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let c = 29573.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func random() -&amp;gt; Double {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;lastRandom = ((lastRandom * a \+ c) % m)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return lastRandom / m&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let generator = LinearCongruentialGenerator()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Here's a random number: \\(generator.random())")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Here's a random number: 0.37464991998171"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("And another one: \\(generator.random())")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "And another one: 0.729023776863283"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Mutating Method Requirements&lt;/h3&gt;
&lt;p&gt;It is sometimes necessary for a method to modify (or &lt;em&gt;mutate&lt;/em&gt;) the instance it belongs to. For instance methods on value types (that is, structures and enumerations) you place the &lt;code&gt;mutating&lt;/code&gt; keyword before a method’s &lt;code&gt;func&lt;/code&gt; keyword to indicate that the method is allowed to modify the instance it belongs to and/or any properties of that instance. This process is described in Modifying Value Types from Within Instance Methods. &lt;/p&gt;
&lt;p&gt;If you define a protocol instance method requirement that is intended to mutate instances of any type that adopts the protocol, mark the method with the &lt;code&gt;mutating&lt;/code&gt; keyword as part of the protocol’s definition. This enables structures and enumerations to adopt the protocol and satisfy that method requirement. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you mark a protocol instance method requirement as &lt;code&gt;mutating&lt;/code&gt;, you do not need to write the &lt;code&gt;mutating&lt;/code&gt; keyword when writing an implementation of that method for a class. The &lt;code&gt;mutating&lt;/code&gt; keyword is only used by structures and enumerations. &lt;/p&gt;
&lt;p&gt;The example below defines a protocol called &lt;code&gt;Togglable&lt;/code&gt;, which defines a single instance method requirement called &lt;code&gt;toggle&lt;/code&gt;. As its name suggests, the &lt;code&gt;toggle&lt;/code&gt; method is intended to toggle or invert the state of any conforming type, typically by modifying a property of that type. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;toggle&lt;/code&gt; method is marked with the &lt;code&gt;mutating&lt;/code&gt; keyword as part of the &lt;code&gt;Togglable&lt;/code&gt; protocol definition, to indicate that the method is expected to mutate the state of a conforming instance when it is called: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol Togglable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func toggle()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you implement the &lt;code&gt;Togglable&lt;/code&gt; protocol for a structure or enumeration, that structure or enumeration can conform to the protocol by providing an implementation of the &lt;code&gt;toggle&lt;/code&gt; method that is also marked as &lt;code&gt;mutating&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The example below defines an enumeration called &lt;code&gt;OnOffSwitch&lt;/code&gt;. This enumeration toggles between two states, indicated by the enumeration cases &lt;code&gt;On&lt;/code&gt; and &lt;code&gt;Off&lt;/code&gt;. The enumeration’s &lt;code&gt;toggle&lt;/code&gt; implementation is marked as &lt;code&gt;mutating&lt;/code&gt;, to match the &lt;code&gt;Togglable&lt;/code&gt; protocol’s requirements: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum OnOffSwitch: Togglable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Off, On&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func toggle() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch self {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case Off:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = On&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case On:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = Off&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var lightSwitch = OnOffSwitch.Off&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;lightSwitch.toggle()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// lightSwitch is now equal to .On&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Initializer Requirements&lt;/h3&gt;
&lt;p&gt;Protocols can require specific initializers to be implemented by conforming types. You write these initializers as part of the protocol’s definition in exactly the same way as for normal initializers, but without curly braces or an initializer body: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol SomeProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(someParameter: Int)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Class Implementations of Protocol Initializer Requirements&lt;/h4&gt;
&lt;p&gt;You can implement a protocol initializer requirement on a conforming class as either a designated initializer or a convenience initializer. In both cases, you must mark the initializer implementation with the &lt;code&gt;required&lt;/code&gt; modifier: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass: SomeProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;required init(someParameter: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// initializer implementation goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The use of the &lt;code&gt;required&lt;/code&gt; modifier ensures that you provide an explicit or inherited implementation of the initializer requirement on all subclasses of the conforming class, such that they also conform to the protocol. &lt;/p&gt;
&lt;p&gt;For more information on required initializers, see Required Initializers. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You do not need to mark protocol initializer implementations with the &lt;code&gt;required&lt;/code&gt; modifier on classes that are marked with the &lt;code&gt;final&lt;/code&gt; modifier, because final classes cannot be subclassed. For more on the &lt;code&gt;final&lt;/code&gt; modifier, see Preventing Overrides. &lt;/p&gt;
&lt;p&gt;If a subclass overrides a designated initializer from a superclass, and also implements a matching initializer requirement from a protocol, mark the initializer implementation with both the &lt;code&gt;required&lt;/code&gt; and &lt;code&gt;override&lt;/code&gt; modifiers: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol SomeProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class SomeSuperClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// initializer implementation goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class SomeSubClass: SomeSuperClass, SomeProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// "required" from SomeProtocol conformance; "override" from SomeSuperClass&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;required override init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// initializer implementation goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Failable Initializer Requirements&lt;/h4&gt;
&lt;p&gt;Protocols can define failable initializer requirements for conforming types, as defined in Failable Initializers. &lt;/p&gt;
&lt;p&gt;A failable initializer requirement can be satisfied by a failable or nonfailable initializer on a conforming type. A nonfailable initializer requirement can be satisfied by a nonfailable initializer or an implicitly unwrapped failable initializer. &lt;/p&gt;
&lt;h3&gt;Protocols as Types&lt;/h3&gt;
&lt;p&gt;Protocols do not actually implement any functionality themselves. Nonetheless, any protocol you create will become a fully-fledged type for use in your code. &lt;/p&gt;
&lt;p&gt;Because it is a type, you can use a protocol in many places where other types are allowed, including: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;As a parameter type or return type in a function, method, or initializer &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;As the type of a constant, variable, or property &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;As the type of items in an array, dictionary, or other container &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Because protocols are types, begin their names with a capital letter (such as &lt;code&gt;FullyNamed&lt;/code&gt; and &lt;code&gt;RandomNumberGenerator&lt;/code&gt;) to match the names of other types in Swift (such as &lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;String&lt;/code&gt;, and &lt;code&gt;Double&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Here’s an example of a protocol used as a type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Dice {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let sides: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let generator: RandomNumberGenerator&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(sides: Int, generator: RandomNumberGenerator) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.sides = sides&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.generator = generator&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func roll() -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Int(generator.random() * Double(sides)) + 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a new class called &lt;code&gt;Dice&lt;/code&gt;, which represents an &lt;em&gt;n&lt;/em&gt;-sided dice for use in a board game. &lt;code&gt;Dice&lt;/code&gt; instances have an integer property called &lt;code&gt;sides&lt;/code&gt;, which represents how many sides they have, and a property called &lt;code&gt;generator&lt;/code&gt;, which provides a random number generator from which to create dice roll values. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;generator&lt;/code&gt; property is of type &lt;code&gt;RandomNumberGenerator&lt;/code&gt;. Therefore, you can set it to an instance of &lt;em&gt;any&lt;/em&gt; type that adopts the &lt;code&gt;RandomNumberGenerator&lt;/code&gt; protocol. Nothing else is required of the instance you assign to this property, except that the instance must adopt the &lt;code&gt;RandomNumberGenerator&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;Dice&lt;/code&gt; also has an initializer, to set up its initial state. This initializer has a parameter called &lt;code&gt;generator&lt;/code&gt;, which is also of type &lt;code&gt;RandomNumberGenerator&lt;/code&gt;. You can pass a value of any conforming type in to this parameter when initializing a new &lt;code&gt;Dice&lt;/code&gt; instance. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;Dice&lt;/code&gt; provides one instance method, &lt;code&gt;roll&lt;/code&gt;, which returns an integer value between 1 and the number of sides on the dice. This method calls the generator’s &lt;code&gt;random&lt;/code&gt; method to create a new random number between &lt;code&gt;0.0&lt;/code&gt; and &lt;code&gt;1.0&lt;/code&gt;, and uses this random number to create a dice roll value within the correct range. Because &lt;code&gt;generator&lt;/code&gt; is known to adopt &lt;code&gt;RandomNumberGenerator&lt;/code&gt;, it is guaranteed to have a &lt;code&gt;random&lt;/code&gt; method to call. &lt;/p&gt;
&lt;p&gt;Here’s how the &lt;code&gt;Dice&lt;/code&gt; class can be used to create a six-sided dice with a &lt;code&gt;LinearCongruentialGenerator&lt;/code&gt; instance as its random number generator: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var d6 = Dice(sides: 6, generator: LinearCongruentialGenerator())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for _ in 1...5 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Random dice roll is \\(d6.roll())")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Random dice roll is 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Random dice roll is 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Random dice roll is 4&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Random dice roll is 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Random dice roll is 4&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Delegation&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Delegation&lt;/em&gt; is a design pattern that enables a class or structure to hand off (or &lt;em&gt;delegate&lt;/em&gt;) some of its responsibilities to an instance of another type. This design pattern is implemented by defining a protocol that encapsulates the delegated responsibilities, such that a conforming type (known as a delegate) is guaranteed to provide the functionality that has been delegated. Delegation can be used to respond to a particular action, or to retrieve data from an external source without needing to know the underlying type of that source. &lt;/p&gt;
&lt;p&gt;The example below defines two protocols for use with dice-based board games: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol DiceGame {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var dice: Dice { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func play()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;protocol DiceGameDelegate {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func gameDidStart(game: DiceGame)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func game(game: DiceGame, didStartNewTurnWithDiceRoll diceRoll: Int)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func gameDidEnd(game: DiceGame)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;DiceGame&lt;/code&gt; protocol is a protocol that can be adopted by any game that involves dice. The &lt;code&gt;DiceGameDelegate&lt;/code&gt; protocol can be adopted by any type to track the progress of a &lt;code&gt;DiceGame&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Here’s a version of the &lt;em&gt;Snakes and Ladders&lt;/em&gt; game originally introduced in Control Flow. This version is adapted to use a &lt;code&gt;Dice&lt;/code&gt; instance for its dice-rolls; to adopt the &lt;code&gt;DiceGame&lt;/code&gt; protocol; and to notify a &lt;code&gt;DiceGameDelegate&lt;/code&gt; about its progress: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SnakesAndLadders: DiceGame {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let finalSquare = 25&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let dice = Dice(sides: 6, generator: LinearCongruentialGenerator())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var square = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var board: [Int]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board = [Int](count: finalSquare \+ 1, repeatedValue: 0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board[03] = +08; board[06] = +11; board[09] = +09; board[10] = +02&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;board[14] = -10; board[19] = -11; board[22] = -02; board[24] = -08&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var delegate: DiceGameDelegate?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func play() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;delegate?.gameDidStart(self)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;gameLoop: while square != finalSquare {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let diceRoll = dice.roll()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;delegate?.game(self, didStartNewTurnWithDiceRoll: diceRoll)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch square \+ diceRoll {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case finalSquare:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;break gameLoop&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let newSquare where newSquare &amp;gt; finalSquare:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;continue gameLoop&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += diceRoll&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;square += board[square]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;delegate?.gameDidEnd(self)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For a description of the &lt;em&gt;Snakes and Ladders&lt;/em&gt; gameplay, see the Break section of the Control Flow chapter. &lt;/p&gt;
&lt;p&gt;This version of the game is wrapped up as a class called &lt;code&gt;SnakesAndLadders&lt;/code&gt;, which adopts the &lt;code&gt;DiceGame&lt;/code&gt; protocol. It provides a gettable &lt;code&gt;dice&lt;/code&gt; property and a &lt;code&gt;play&lt;/code&gt; method in order to conform to the protocol. (The &lt;code&gt;dice&lt;/code&gt; property is declared as a constant property because it does not need to change after initialization, and the protocol only requires that it is gettable.) &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;Snakes and Ladders&lt;/em&gt; game board setup takes place within the class’s &lt;code&gt;init()&lt;/code&gt; initializer. All game logic is moved into the protocol’s &lt;code&gt;play&lt;/code&gt; method, which uses the protocol’s required &lt;code&gt;dice&lt;/code&gt; property to provide its dice roll values. &lt;/p&gt;
&lt;p&gt;Note that the &lt;code&gt;delegate&lt;/code&gt; property is defined as an &lt;em&gt;optional&lt;/em&gt; &lt;code&gt;DiceGameDelegate&lt;/code&gt;, because a delegate isn’t required in order to play the game. Because it is of an optional type, the &lt;code&gt;delegate&lt;/code&gt; property is automatically set to an initial value of &lt;code&gt;nil&lt;/code&gt;. Thereafter, the game instantiator has the option to set the property to a suitable delegate. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;DiceGameDelegate&lt;/code&gt; provides three methods for tracking the progress of a game. These three methods have been incorporated into the game logic within the &lt;code&gt;play&lt;/code&gt; method above, and are called when a new game starts, a new turn begins, or the game ends. &lt;/p&gt;
&lt;p&gt;Because the &lt;code&gt;delegate&lt;/code&gt; property is an &lt;em&gt;optional&lt;/em&gt; &lt;code&gt;DiceGameDelegate&lt;/code&gt;, the &lt;code&gt;play&lt;/code&gt; method uses optional chaining each time it calls a method on the delegate. If the &lt;code&gt;delegate&lt;/code&gt; property is nil, these delegate calls fail gracefully and without error. If the &lt;code&gt;delegate&lt;/code&gt; property is non-nil, the delegate methods are called, and are passed the &lt;code&gt;SnakesAndLadders&lt;/code&gt; instance as a parameter. &lt;/p&gt;
&lt;p&gt;This next example shows a class called &lt;code&gt;DiceGameTracker&lt;/code&gt;, which adopts the &lt;code&gt;DiceGameDelegate&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class DiceGameTracker: DiceGameDelegate {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var numberOfTurns = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func gameDidStart(game: DiceGame) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfTurns = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if game is SnakesAndLadders {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Started a new game of Snakes and Ladders")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The game is using a \\(game.dice.sides)-sided dice")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func game(game: DiceGame, didStartNewTurnWithDiceRoll diceRoll: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;++numberOfTurns&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Rolled a \\(diceRoll)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func gameDidEnd(game: DiceGame) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The game lasted for \\(numberOfTurns) turns")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;DiceGameTracker&lt;/code&gt; implements all three methods required by &lt;code&gt;DiceGameDelegate&lt;/code&gt;. It uses these methods to keep track of the number of turns a game has taken. It resets a &lt;code&gt;numberOfTurns&lt;/code&gt; property to zero when the game starts, increments it each time a new turn begins, and prints out the total number of turns once the game has ended. &lt;/p&gt;
&lt;p&gt;The implementation of &lt;code&gt;gameDidStart&lt;/code&gt; shown above uses the &lt;code&gt;game&lt;/code&gt; parameter to print some introductory information about the game that is about to be played. The &lt;code&gt;game&lt;/code&gt; parameter has a type of &lt;code&gt;DiceGame&lt;/code&gt;, not &lt;code&gt;SnakesAndLadders&lt;/code&gt;, and so &lt;code&gt;gameDidStart&lt;/code&gt; can access and use only methods and properties that are implemented as part of the &lt;code&gt;DiceGame&lt;/code&gt; protocol. However, the method is still able to use type casting to query the type of the underlying instance. In this example, it checks whether &lt;code&gt;game&lt;/code&gt; is actually an instance of &lt;code&gt;SnakesAndLadders&lt;/code&gt; behind the scenes, and prints an appropriate message if so. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;gameDidStart&lt;/code&gt; also accesses the &lt;code&gt;dice&lt;/code&gt; property of the passed &lt;code&gt;game&lt;/code&gt; parameter. Because &lt;code&gt;game&lt;/code&gt; is known to conform to the &lt;code&gt;DiceGame&lt;/code&gt; protocol, it is guaranteed to have a &lt;code&gt;dice&lt;/code&gt; property, and so the &lt;code&gt;gameDidStart&lt;/code&gt; method is able to access and print the dice’s &lt;code&gt;sides&lt;/code&gt; property, regardless of what kind of game is being played. &lt;/p&gt;
&lt;p&gt;Here’s how &lt;code&gt;DiceGameTracker&lt;/code&gt; looks in action: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let tracker = DiceGameTracker()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let game = SnakesAndLadders()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;game.delegate = tracker&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;game.play()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Started a new game of Snakes and Ladders&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// The game is using a 6-sided dice&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Rolled a 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Rolled a 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Rolled a 4&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Rolled a 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// The game lasted for 4 turns&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Adding Protocol Conformance with an Extension&lt;/h3&gt;
&lt;p&gt;You can extend an existing type to adopt and conform to a new protocol, even if you do not have access to the source code for the existing type. Extensions can add new properties, methods, and subscripts to an existing type, and are therefore able to add any requirements that a protocol may demand. For more about extensions, see Extensions. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Existing instances of a type automatically adopt and conform to a protocol when that conformance is added to the instance’s type in an extension. &lt;/p&gt;
&lt;p&gt;For example, this protocol, called &lt;code&gt;TextRepresentable&lt;/code&gt;, can be implemented by any type that has a way to be represented as text. This might be a description of itself, or a text version of its current state: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol TextRepresentable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func asText() -&amp;gt; String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Dice&lt;/code&gt; class from earlier can be extended to adopt and conform to &lt;code&gt;TextRepresentable&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Dice: TextRepresentable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func asText() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "A \\(sides)-sided dice"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This extension adopts the new protocol in exactly the same way as if &lt;code&gt;Dice&lt;/code&gt; had provided it in its original implementation. The protocol name is provided after the type name, separated by a colon, and an implementation of all requirements of the protocol is provided within the extension’s curly braces. &lt;/p&gt;
&lt;p&gt;Any &lt;code&gt;Dice&lt;/code&gt; instance can now be treated as &lt;code&gt;TextRepresentable&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let d12 = Dice(sides: 12, generator: LinearCongruentialGenerator())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(d12.asText())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "A 12-sided dice"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Similarly, the &lt;code&gt;SnakesAndLadders&lt;/code&gt; game class can be extended to adopt and conform to the &lt;code&gt;TextRepresentable&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension SnakesAndLadders: TextRepresentable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func asText() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "A game of Snakes and Ladders with \\(finalSquare) squares"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(game.asText())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "A game of Snakes and Ladders with 25 squares"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Declaring Protocol Adoption with an Extension&lt;/h4&gt;
&lt;p&gt;If a type already conforms to all of the requirements of a protocol, but has not yet stated that it adopts that protocol, you can make it adopt the protocol with an empty extension: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Hamster {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func asText() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return "A hamster named \\(name)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;extension Hamster: TextRepresentable {}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Instances of &lt;code&gt;Hamster&lt;/code&gt; can now be used wherever &lt;code&gt;TextRepresentable&lt;/code&gt; is the required type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let simonTheHamster = Hamster(name: "Simon")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let somethingTextRepresentable: TextRepresentable = simonTheHamster&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(somethingTextRepresentable.asText())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "A hamster named Simon"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Types do not automatically adopt a protocol just by satisfying its requirements. They must always explicitly declare their adoption of the protocol. &lt;/p&gt;
&lt;h3&gt;Collections of Protocol Types&lt;/h3&gt;
&lt;p&gt;A protocol can be used as the type to be stored in a collection such as an array or a dictionary, as mentioned in Protocols as Types. This example creates an array of &lt;code&gt;TextRepresentable&lt;/code&gt; things: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let things: [TextRepresentable] = [game, d12, simonTheHamster]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;It is now possible to iterate over the items in the array, and print each item’s textual representation: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for thing in things {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(thing.asText())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// A game of Snakes and Ladders with 25 squares&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// A 12-sided dice&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// A hamster named Simon&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the &lt;code&gt;thing&lt;/code&gt; constant is of type &lt;code&gt;TextRepresentable&lt;/code&gt;. It is not of type &lt;code&gt;Dice&lt;/code&gt;, or &lt;code&gt;DiceGame&lt;/code&gt;, or &lt;code&gt;Hamster&lt;/code&gt;, even if the actual instance behind the scenes is of one of those types. Nonetheless, because it is of type &lt;code&gt;TextRepresentable&lt;/code&gt;, and anything that is &lt;code&gt;TextRepresentable&lt;/code&gt; is known to have an &lt;code&gt;asText&lt;/code&gt; method, it is safe to call &lt;code&gt;thing.asText&lt;/code&gt; each time through the loop. &lt;/p&gt;
&lt;h3&gt;Protocol Inheritance&lt;/h3&gt;
&lt;p&gt;A protocol can &lt;em&gt;inherit&lt;/em&gt; one or more other protocols and can add further requirements on top of the requirements it inherits. The syntax for protocol inheritance is similar to the syntax for class inheritance, but with the option to list multiple inherited protocols, separated by commas: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol InheritingProtocol: SomeProtocol, AnotherProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// protocol definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s an example of a protocol that inherits the &lt;code&gt;TextRepresentable&lt;/code&gt; protocol from above: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol PrettyTextRepresentable: TextRepresentable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func asPrettyText() -&amp;gt; String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a new protocol, &lt;code&gt;PrettyTextRepresentable&lt;/code&gt;, which inherits from &lt;code&gt;TextRepresentable&lt;/code&gt;. Anything that adopts &lt;code&gt;PrettyTextRepresentable&lt;/code&gt; must satisfy all of the requirements enforced by &lt;code&gt;TextRepresentable&lt;/code&gt;, &lt;em&gt;plus&lt;/em&gt; the additional requirements enforced by &lt;code&gt;PrettyTextRepresentable&lt;/code&gt;. In this example, &lt;code&gt;PrettyTextRepresentable&lt;/code&gt; adds a single requirement to provide an instance method called &lt;code&gt;asPrettyText&lt;/code&gt; that returns a &lt;code&gt;String&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;SnakesAndLadders&lt;/code&gt; class can be extended to adopt and conform to &lt;code&gt;PrettyTextRepresentable&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension SnakesAndLadders: PrettyTextRepresentable {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func asPrettyText() -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var output = asText() + ":\n"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for index in 1...finalSquare {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch board[index] {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let ladder where ladder &amp;gt; 0:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;output += "▲ "&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let snake where snake &amp;lt; 0:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;output += "▼ "&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;output += "○ "&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return output&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This extension states that it adopts the &lt;code&gt;PrettyTextRepresentable&lt;/code&gt; protocol and provides an implementation of the &lt;code&gt;asPrettyText&lt;/code&gt; method for the &lt;code&gt;SnakesAndLadders&lt;/code&gt; type. Anything that is &lt;code&gt;PrettyTextRepresentable&lt;/code&gt; must also be &lt;code&gt;TextRepresentable&lt;/code&gt;, and so the &lt;code&gt;asPrettyText&lt;/code&gt; implementation starts by calling the &lt;code&gt;asText&lt;/code&gt; method from the &lt;code&gt;TextRepresentable&lt;/code&gt; protocol to begin an output string. It appends a colon and a line break, and uses this as the start of its pretty text representation. It then iterates through the array of board squares, and appends a geometric shape to represent the contents of each square: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If the square’s value is greater than &lt;code&gt;0&lt;/code&gt;, it is the base of a ladder, and is represented by &lt;code&gt;▲&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If the square’s value is less than &lt;code&gt;0&lt;/code&gt;, it is the head of a snake, and is represented by &lt;code&gt;▼&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Otherwise, the square’s value is &lt;code&gt;0&lt;/code&gt;, and it is a “free” square, represented by &lt;code&gt;○&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The method implementation can now be used to print a pretty text description of any &lt;code&gt;SnakesAndLadders&lt;/code&gt; instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println(game.asPrettyText())&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// A game of Snakes and Ladders with 25 squares:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// ○ ○ ▲ ○ ○ ▲ ○ ○ ▲ ▲ ○ ○ ○ ▼ ○ ○ ○ ○ ▼ ○ ○ ▼ ○ ▼ ○&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Class-Only Protocols&lt;/h3&gt;
&lt;p&gt;You can limit protocol adoption to class types (and not structures or enumerations) by adding the &lt;code&gt;class&lt;/code&gt; keyword to a protocol’s inheritance list. The &lt;code&gt;class&lt;/code&gt; keyword must always appear first in a protocol’s inheritance list, before any inherited protocols: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol SomeClassOnlyProtocol: class, SomeInheritedProtocol {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// class-only protocol definition goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the example above, &lt;code&gt;SomeClassOnlyProtocol&lt;/code&gt; can only be adopted by class types. It is a compile-time error to write a structure or enumeration definition that tries to adopt &lt;code&gt;SomeClassOnlyProtocol&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Use a class-only protocol when the behavior defined by that protocol’s requirements assumes or requires that a conforming type has reference semantics rather than value semantics. For more on reference and value semantics, see Structures and Enumerations Are Value Types and Classes Are Reference Types. &lt;/p&gt;
&lt;h3&gt;Protocol Composition&lt;/h3&gt;
&lt;p&gt;It can be useful to require a type to conform to multiple protocols at once. You can combine multiple protocols into a single requirement with a &lt;em&gt;protocol composition&lt;/em&gt;. Protocol compositions have the form &lt;code&gt;protocol&amp;lt;SomeProtocol, AnotherProtocol&amp;gt;&lt;/code&gt;. You can list as many protocols within the pair of angle brackets (&lt;code&gt;&amp;lt;&amp;gt;&lt;/code&gt;) as you need, separated by commas. &lt;/p&gt;
&lt;p&gt;Here’s an example that combines two protocols called &lt;code&gt;Named&lt;/code&gt; and &lt;code&gt;Aged&lt;/code&gt; into a single protocol composition requirement on a function parameter: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol Named {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;protocol Aged {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var age: Int { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;struct Person: Named, Aged {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var name: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var age: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func wishHappyBirthday(celebrator: protocol&amp;lt;Named, Aged&amp;gt;) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Happy birthday \\(celebrator.name) \- you're \\(celebrator.age)!")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let birthdayPerson = Person(name: "Malcolm", age: 21)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;wishHappyBirthday(birthdayPerson)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "Happy birthday Malcolm - you're 21!"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a protocol called &lt;code&gt;Named&lt;/code&gt;, with a single requirement for a gettable &lt;code&gt;String&lt;/code&gt; property called &lt;code&gt;name&lt;/code&gt;. It also defines a protocol called &lt;code&gt;Aged&lt;/code&gt;, with a single requirement for a gettable &lt;code&gt;Int&lt;/code&gt; property called &lt;code&gt;age&lt;/code&gt;. Both of these protocols are adopted by a structure called &lt;code&gt;Person&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The example also defines a function called &lt;code&gt;wishHappyBirthday&lt;/code&gt;, which takes a single parameter called &lt;code&gt;celebrator&lt;/code&gt;. The type of this parameter is &lt;code&gt;protocol&amp;lt;Named, Aged&amp;gt;&lt;/code&gt;, which means “any type that conforms to both the &lt;code&gt;Named&lt;/code&gt; and &lt;code&gt;Aged&lt;/code&gt; protocols.” It doesn’t matter what specific type is passed to the function, as long as it conforms to both of the required protocols. &lt;/p&gt;
&lt;p&gt;The example then creates a new &lt;code&gt;Person&lt;/code&gt; instance called &lt;code&gt;birthdayPerson&lt;/code&gt; and passes this new instance to the &lt;code&gt;wishHappyBirthday&lt;/code&gt; function. Because &lt;code&gt;Person&lt;/code&gt; conforms to both protocols, this is a valid call, and the &lt;code&gt;wishHappyBirthday&lt;/code&gt; function is able to print its birthday greeting. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Protocol compositions do not define a new, permanent protocol type. Rather, they define a temporary local protocol that has the combined requirements of all protocols in the composition. &lt;/p&gt;
&lt;h3&gt;Checking for Protocol Conformance&lt;/h3&gt;
&lt;p&gt;You can use the &lt;code&gt;is&lt;/code&gt; and &lt;code&gt;as&lt;/code&gt; operators described in Type Casting to check for protocol conformance, and to cast to a specific protocol. Checking for and casting to a protocol follows exactly the same syntax as checking for and casting to a type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;is&lt;/code&gt; operator returns &lt;code&gt;true&lt;/code&gt; if an instance conforms to a protocol and returns &lt;code&gt;false&lt;/code&gt; if it does not. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;as?&lt;/code&gt; version of the downcast operator returns an optional value of the protocol’s type, and this value is &lt;code&gt;nil&lt;/code&gt; if the instance does not conform to that protocol. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;as&lt;/code&gt; version of the downcast operator forces the downcast to the protocol type and triggers a runtime error if the downcast does not succeed. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example defines a protocol called &lt;code&gt;HasArea&lt;/code&gt;, with a single property requirement of a gettable &lt;code&gt;Double&lt;/code&gt; property called &lt;code&gt;area&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol HasArea {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var area: Double { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here are two classes, &lt;code&gt;Circle&lt;/code&gt; and &lt;code&gt;Country&lt;/code&gt;, both of which conform to the &lt;code&gt;HasArea&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Circle: HasArea {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let pi = 3.1415927&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var radius: Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var area: Double { return pi * radius * radius }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(radius: Double) { self.radius = radius }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class Country: HasArea {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var area: Double&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(area: Double) { self.area = area }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Circle&lt;/code&gt; class implements the &lt;code&gt;area&lt;/code&gt; property requirement as a computed property, based on a stored &lt;code&gt;radius&lt;/code&gt; property. The &lt;code&gt;Country&lt;/code&gt; class implements the &lt;code&gt;area&lt;/code&gt; requirement directly as a stored property. Both classes correctly conform to the &lt;code&gt;HasArea&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;Here’s a class called &lt;code&gt;Animal&lt;/code&gt;, which does not conform to the &lt;code&gt;HasArea&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Animal {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var legs: Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(legs: Int) { self.legs = legs }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Circle&lt;/code&gt;, &lt;code&gt;Country&lt;/code&gt; and &lt;code&gt;Animal&lt;/code&gt; classes do not have a shared base class. Nonetheless, they are all classes, and so instances of all three types can be used to initialize an array that stores values of type &lt;code&gt;AnyObject&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let objects: [AnyObject] = [&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Circle(radius: 2.0),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Country(area: 243_610),&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Animal(legs: 4)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;objects&lt;/code&gt; array is initialized with an array literal containing a &lt;code&gt;Circle&lt;/code&gt; instance with a radius of 2 units; a &lt;code&gt;Country&lt;/code&gt; instance initialized with the surface area of the United Kingdom in square kilometers; and an &lt;code&gt;Animal&lt;/code&gt; instance with four legs. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;objects&lt;/code&gt; array can now be iterated, and each object in the array can be checked to see if it conforms to the &lt;code&gt;HasArea&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for object in objects {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let objectWithArea = object as? HasArea {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Area is \\(objectWithArea.area)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Something that doesn't have an area")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Area is 12.5663708&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Area is 243610.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Something that doesn't have an area&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Whenever an object in the array conforms to the &lt;code&gt;HasArea&lt;/code&gt; protocol, the optional value returned by the &lt;code&gt;as?&lt;/code&gt; operator is unwrapped with optional binding into a constant called &lt;code&gt;objectWithArea&lt;/code&gt;. The &lt;code&gt;objectWithArea&lt;/code&gt; constant is known to be of type &lt;code&gt;HasArea&lt;/code&gt;, and so its &lt;code&gt;area&lt;/code&gt; property can be accessed and printed in a type-safe way. &lt;/p&gt;
&lt;p&gt;Note that the underlying objects are not changed by the casting process. They continue to be a &lt;code&gt;Circle&lt;/code&gt;, a &lt;code&gt;Country&lt;/code&gt; and an &lt;code&gt;Animal&lt;/code&gt;. However, at the point that they are stored in the &lt;code&gt;objectWithArea&lt;/code&gt; constant, they are only known to be of type &lt;code&gt;HasArea&lt;/code&gt;, and so only their &lt;code&gt;area&lt;/code&gt; property can be accessed. &lt;/p&gt;
&lt;h3&gt;Optional Protocol Requirements&lt;/h3&gt;
&lt;p&gt;You can define &lt;em&gt;optional requirements&lt;/em&gt; for protocols, These requirements do not have to be implemented by types that conform to the protocol. Optional requirements are prefixed by the &lt;code&gt;optional&lt;/code&gt; modifier as part of the protocol’s definition. &lt;/p&gt;
&lt;p&gt;An optional protocol requirement can be called with optional chaining, to account for the possibility that the requirement was not implemented by a type that conforms to the protocol. For information on optional chaining, see Optional Chaining. &lt;/p&gt;
&lt;p&gt;You check for an implementation of an optional requirement by writing a question mark after the name of the requirement when it is called, such as &lt;code&gt;someOptionalMethod?(someArgument)&lt;/code&gt;. Optional property requirements, and optional method requirements that return a value, will always return an optional value of the appropriate type when they are accessed or called, to reflect the fact that the optional requirement may not have been implemented. &lt;/p&gt;
&lt;p&gt;The following example defines an integer-counting class called &lt;code&gt;Counter&lt;/code&gt;, which uses an external data source to provide its increment amount. This data source is defined by the &lt;code&gt;CounterDataSource&lt;/code&gt; protocol, which has two optional requirements: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol CounterDataSource {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;optional func incrementForCount(count: Int) -&amp;gt; Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;optional var fixedIncrement: Int { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;CounterDataSource&lt;/code&gt; protocol defines an optional method requirement called &lt;code&gt;incrementForCount&lt;/code&gt; and an optional property requirement called &lt;code&gt;fixedIncrement&lt;/code&gt;. These requirements define two different ways for data sources to provide an appropriate increment amount for a &lt;code&gt;Counter&lt;/code&gt; instance. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Strictly speaking, you can write a custom class that conforms to &lt;code&gt;CounterDataSource&lt;/code&gt; without implementing &lt;em&gt;either&lt;/em&gt; protocol requirement. They are both optional, after all. Although technically allowed, this wouldn’t make for a very good data source. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;Counter&lt;/code&gt; class, defined below, has an optional &lt;code&gt;dataSource&lt;/code&gt; property of type &lt;code&gt;CounterDataSource?&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class Counter {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var count = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var dataSource: CounterDataSource?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func increment() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let amount = dataSource?.incrementForCount?(count) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count += amount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if let amount = dataSource?.fixedIncrement? {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;count += amount&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Counter&lt;/code&gt; class stores its current value in a variable property called &lt;code&gt;count&lt;/code&gt;. The &lt;code&gt;Counter&lt;/code&gt; class also defines a method called &lt;code&gt;increment&lt;/code&gt;, which increments the &lt;code&gt;count&lt;/code&gt; property every time the method is called. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;increment&lt;/code&gt; method first tries to retrieve an increment amount by looking for an implementation of the &lt;code&gt;incrementForCount&lt;/code&gt; method on its data source. The &lt;code&gt;increment&lt;/code&gt; method uses optional chaining to try to call &lt;code&gt;incrementForCount&lt;/code&gt;, and passes the current &lt;code&gt;count&lt;/code&gt; value as the method’s single argument. &lt;/p&gt;
&lt;p&gt;Note &lt;em&gt;two&lt;/em&gt; levels of optional chaining at play here. Firstly, it is possible that &lt;code&gt;dataSource&lt;/code&gt; may be &lt;code&gt;nil&lt;/code&gt;, and so &lt;code&gt;dataSource&lt;/code&gt; has a question mark after its name to indicate that &lt;code&gt;incrementForCount&lt;/code&gt; should only be called if &lt;code&gt;dataSource&lt;/code&gt; is non-nil. Secondly, even if &lt;code&gt;dataSource&lt;/code&gt; &lt;em&gt;does&lt;/em&gt; exist, there is no guarantee that it implements &lt;code&gt;incrementForCount&lt;/code&gt;, because it is an optional requirement. This is why &lt;code&gt;incrementForCount&lt;/code&gt; is also written with a question mark after its name. &lt;/p&gt;
&lt;p&gt;Because the call to &lt;code&gt;incrementForCount&lt;/code&gt; can fail for either of these two reasons, the call returns an &lt;em&gt;optional&lt;/em&gt; &lt;code&gt;Int&lt;/code&gt; value. This is true even though &lt;code&gt;incrementForCount&lt;/code&gt; is defined as returning a non-optional &lt;code&gt;Int&lt;/code&gt; value in the definition of &lt;code&gt;CounterDataSource&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;After calling &lt;code&gt;incrementForCount&lt;/code&gt;, the optional &lt;code&gt;Int&lt;/code&gt; that it returns is unwrapped into a constant called &lt;code&gt;amount&lt;/code&gt;, using optional binding. If the optional &lt;code&gt;Int&lt;/code&gt; does contain a value—that is, if the delegate and method both exist, and the method returned a value—the unwrapped &lt;code&gt;amount&lt;/code&gt; is added onto the stored &lt;code&gt;count&lt;/code&gt; property, and incrementation is complete. &lt;/p&gt;
&lt;p&gt;If it is &lt;em&gt;not&lt;/em&gt; possible to retrieve a value from the &lt;code&gt;incrementForCount&lt;/code&gt; method—either because &lt;code&gt;dataSource&lt;/code&gt; is nil, or because the data source does not implement &lt;code&gt;incrementForCount&lt;/code&gt;—then the &lt;code&gt;increment&lt;/code&gt; method tries to retrieve a value from the data source’s &lt;code&gt;fixedIncrement&lt;/code&gt; property instead. The &lt;code&gt;fixedIncrement&lt;/code&gt; property is also an optional requirement, and so its name is also written using optional chaining with a question mark on the end, to indicate that the attempt to access the property’s value can fail. As before, the returned value is an optional &lt;code&gt;Int&lt;/code&gt; value, even though &lt;code&gt;fixedIncrement&lt;/code&gt; is defined as a non-optional &lt;code&gt;Int&lt;/code&gt; property as part of the &lt;code&gt;CounterDataSource&lt;/code&gt; protocol definition. &lt;/p&gt;
&lt;p&gt;Here’s a simple &lt;code&gt;CounterDataSource&lt;/code&gt; implementation where the data source returns a constant value of &lt;code&gt;3&lt;/code&gt; every time it is queried. It does this by implementing the optional &lt;code&gt;fixedIncrement&lt;/code&gt; property requirement: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class ThreeSource: CounterDataSource {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let fixedIncrement = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can use an instance of &lt;code&gt;ThreeSource&lt;/code&gt; as the data source for a new &lt;code&gt;Counter&lt;/code&gt; instance: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var counter = Counter()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.dataSource = ThreeSource()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for _ in 1...4 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.increment()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(counter.count)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 6&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 9&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 12&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The code above creates a new &lt;code&gt;Counter&lt;/code&gt; instance; sets its data source to be a new &lt;code&gt;ThreeSource&lt;/code&gt; instance; and calls the counter’s &lt;code&gt;increment&lt;/code&gt; method four times. As expected, the counter’s &lt;code&gt;count&lt;/code&gt; property increases by three each time &lt;code&gt;increment&lt;/code&gt; is called. &lt;/p&gt;
&lt;p&gt;Here’s a more complex data source called &lt;code&gt;TowardsZeroSource&lt;/code&gt;, which makes a &lt;code&gt;Counter&lt;/code&gt; instance count up or down towards zero from its current &lt;code&gt;count&lt;/code&gt; value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class TowardsZeroSource: CounterDataSource {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func incrementForCount(count: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if count == 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else if count &amp;lt; 0 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return -1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;TowardsZeroSource&lt;/code&gt; class implements the optional &lt;code&gt;incrementForCount&lt;/code&gt; method from the &lt;code&gt;CounterDataSource&lt;/code&gt; protocol and uses the &lt;code&gt;count&lt;/code&gt; argument value to work out which direction to count in. If &lt;code&gt;count&lt;/code&gt; is already zero, the method returns &lt;code&gt;0&lt;/code&gt; to indicate that no further counting should take place. &lt;/p&gt;
&lt;p&gt;You can use an instance of &lt;code&gt;TowardsZeroSource&lt;/code&gt; with the existing &lt;code&gt;Counter&lt;/code&gt; instance to count from &lt;code&gt;-4&lt;/code&gt; to zero. Once the counter reaches zero, no more counting takes place: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;counter.count = -4&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.dataSource = TowardsZeroSource()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for _ in 1...5 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;counter.increment()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(counter.count)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// -3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// -2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// -1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Generics&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Generic code&lt;/em&gt; enables you to write flexible, reusable functions and types that can work with any type, subject to requirements that you define. You can write code that avoids duplication and expresses its intent in a clear, abstracted manner. &lt;/p&gt;
&lt;p&gt;Generics are one of the most powerful features of Swift, and much of the Swift standard library is built with generic code. In fact, you’ve been using generics throughout the &lt;em&gt;Language Guide&lt;/em&gt;, even if you didn’t realize it. For example, Swift’s &lt;code&gt;Array&lt;/code&gt; and &lt;code&gt;Dictionary&lt;/code&gt; types are both generic collections. You can create an array that holds &lt;code&gt;Int&lt;/code&gt; values, or an array that holds &lt;code&gt;String&lt;/code&gt; values, or indeed an array for any other type that can be created in Swift. Similarly, you can create a dictionary to store values of any specified type, and there are no limitations on what that type can be. &lt;/p&gt;
&lt;h3&gt;The Problem That Generics Solve&lt;/h3&gt;
&lt;p&gt;Here’s a standard, non-generic function called &lt;code&gt;swapTwoInts&lt;/code&gt;, which swaps two &lt;code&gt;Int&lt;/code&gt; values: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func swapTwoInts(inout a: Int, inout b: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let temporaryA = a&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a = b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;b = temporaryA&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This function makes use of in-out parameters to swap the values of &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt;, as described in In-Out Parameters. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;swapTwoInts&lt;/code&gt; function swaps the original value of &lt;code&gt;b&lt;/code&gt; into &lt;code&gt;a&lt;/code&gt;, and the original value of &lt;code&gt;a&lt;/code&gt; into &lt;code&gt;b&lt;/code&gt;. You can call this function to swap the values in two &lt;code&gt;Int&lt;/code&gt; variables: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var someInt = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var anotherInt = 107&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;swapTwoInts(&amp;amp;someInt, &amp;amp;anotherInt)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("someInt is now \\(someInt), and anotherInt is now \\(anotherInt)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "someInt is now 107, and anotherInt is now 3"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;swapTwoInts&lt;/code&gt; function is useful, but it can only be used with &lt;code&gt;Int&lt;/code&gt; values. If you want to swap two &lt;code&gt;String&lt;/code&gt; values, or two &lt;code&gt;Double&lt;/code&gt; values, you have to write more functions, such as the &lt;code&gt;swapTwoStrings&lt;/code&gt; and &lt;code&gt;swapTwoDoubles&lt;/code&gt; functions shown below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func swapTwoStrings(inout a: String, inout b: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let temporaryA = a&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a = b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;b = temporaryA&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func swapTwoDoubles(inout a: Double, inout b: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let temporaryA = a&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a = b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;b = temporaryA&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You may have noticed that the bodies of the &lt;code&gt;swapTwoInts&lt;/code&gt;, &lt;code&gt;swapTwoStrings&lt;/code&gt;, and &lt;code&gt;swapTwoDoubles&lt;/code&gt; functions are identical. The only difference is the type of the values that they accept (&lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;String&lt;/code&gt;, and &lt;code&gt;Double&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;It would be much more useful, and considerably more flexible, to write a single function that could swap two values of &lt;em&gt;any&lt;/em&gt; type. Generic code enables you to write such a function. (A generic version of these functions is defined below.) &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In all three functions, it is important that the types of &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt; are defined to be the same as each other. If &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt; were not of the same type, it would not be possible to swap their values. Swift is a type-safe language, and does not allow (for example) a variable of type &lt;code&gt;String&lt;/code&gt; and a variable of type &lt;code&gt;Double&lt;/code&gt; to swap values with each other. Attempting to do so would be reported as a compile-time error. &lt;/p&gt;
&lt;h3&gt;Generic Functions&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Generic functions&lt;/em&gt; can work with any type. Here’s a generic version of the &lt;code&gt;swapTwoInts&lt;/code&gt; function from above, called &lt;code&gt;swapTwoValues&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func swapTwoValues&amp;lt;T&amp;gt;(inout a: T, inout b: T) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let temporaryA = a&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a = b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;b = temporaryA&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The body of the &lt;code&gt;swapTwoValues&lt;/code&gt; function is identical to the body of the &lt;code&gt;swapTwoInts&lt;/code&gt; function. However, the first line of &lt;code&gt;swapTwoValues&lt;/code&gt; is slightly different from &lt;code&gt;swapTwoInts&lt;/code&gt;. Here’s how the first lines compare: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func swapTwoInts(inout a: Int, inout b: Int)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func swapTwoValues&amp;lt;T&amp;gt;(inout a: T, inout b: T)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The generic version of the function uses a &lt;em&gt;placeholder&lt;/em&gt; type name (called &lt;code&gt;T&lt;/code&gt;, in this case) instead of an &lt;em&gt;actual&lt;/em&gt; type name (such as &lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;String&lt;/code&gt;, or &lt;code&gt;Double&lt;/code&gt;). The placeholder type name doesn’t say anything about what &lt;code&gt;T&lt;/code&gt; must be, but it &lt;em&gt;does&lt;/em&gt; say that both &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt; must be of the same type &lt;code&gt;T&lt;/code&gt;, whatever &lt;code&gt;T&lt;/code&gt; represents. The actual type to use in place of &lt;code&gt;T&lt;/code&gt; will be determined each time the &lt;code&gt;swapTwoValues&lt;/code&gt; function is called. &lt;/p&gt;
&lt;p&gt;The other difference is that the generic function’s name (&lt;code&gt;swapTwoValues&lt;/code&gt;) is followed by the placeholder type name (&lt;code&gt;T&lt;/code&gt;) inside angle brackets (&lt;code&gt;&amp;lt;T&amp;gt;&lt;/code&gt;). The brackets tell Swift that &lt;code&gt;T&lt;/code&gt; is a placeholder type name within the &lt;code&gt;swapTwoValues&lt;/code&gt; function definition. Because &lt;code&gt;T&lt;/code&gt; is a placeholder, Swift does not look for an actual type called &lt;code&gt;T&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;swapTwoValues&lt;/code&gt; function can now be called in the same way as &lt;code&gt;swapTwoInts&lt;/code&gt;, except that it can be passed two values of &lt;em&gt;any&lt;/em&gt; type, as long as both of those values are of the same type as each other. Each time &lt;code&gt;swapTwoValues&lt;/code&gt; is called, the type to use for &lt;code&gt;T&lt;/code&gt; is inferred from the types of values passed to the function. &lt;/p&gt;
&lt;p&gt;In the two examples below, &lt;code&gt;T&lt;/code&gt; is inferred to be &lt;code&gt;Int&lt;/code&gt; and &lt;code&gt;String&lt;/code&gt; respectively: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var someInt = 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var anotherInt = 107&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;swapTwoValues(&amp;amp;someInt, &amp;amp;anotherInt)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someInt is now 107, and anotherInt is now 3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someString = "hello"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var anotherString = "world"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;swapTwoValues(&amp;amp;someString, &amp;amp;anotherString)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someString is now "world", and anotherString is now "hello"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;swapTwoValues&lt;/code&gt; function defined above is inspired by a generic function called &lt;code&gt;swap&lt;/code&gt;, which is part of the Swift standard library, and is automatically made available for you to use in your apps. If you need the behavior of the &lt;code&gt;swapTwoValues&lt;/code&gt; function in your own code, you can use Swift’s existing &lt;code&gt;swap&lt;/code&gt; function rather than providing your own implementation. &lt;/p&gt;
&lt;h3&gt;Type Parameters&lt;/h3&gt;
&lt;p&gt;In the &lt;code&gt;swapTwoValues&lt;/code&gt; example above, the placeholder type &lt;code&gt;T&lt;/code&gt; is an example of a &lt;em&gt;type parameter&lt;/em&gt;. Type parameters specify and name a placeholder type, and are written immediately after the function’s name, between a pair of matching angle brackets (such as &lt;code&gt;&amp;lt;T&amp;gt;&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Once you specify a type parameter, you can use it to define the type of a function’s parameters (such as the &lt;code&gt;a&lt;/code&gt; and &lt;code&gt;b&lt;/code&gt; parameters of the &lt;code&gt;swapTwoValues&lt;/code&gt; function), or as the function’s return type, or as a type annotation within the body of the function. In each case, the placeholder type represented by the type parameter is replaced with an &lt;em&gt;actual&lt;/em&gt; type whenever the function is called. (In the &lt;code&gt;swapTwoValues&lt;/code&gt; example above, &lt;code&gt;T&lt;/code&gt; was replaced with &lt;code&gt;Int&lt;/code&gt; the first time the function was called, and was replaced with &lt;code&gt;String&lt;/code&gt; the second time it was called.) &lt;/p&gt;
&lt;p&gt;You can provide more than one type parameter by writing multiple type parameter names within the angle brackets, separated by commas. &lt;/p&gt;
&lt;h3&gt;Naming Type Parameters&lt;/h3&gt;
&lt;p&gt;In simple cases where a generic function or generic type refers to a single placeholder type (such as the &lt;code&gt;swapTwoValues&lt;/code&gt; generic function above, or a generic collection that stores a single type, such as &lt;code&gt;Array&lt;/code&gt;), it is traditional to use the single-character name &lt;code&gt;T&lt;/code&gt; for the type parameter. However, you can use any valid identifier as the type parameter name. &lt;/p&gt;
&lt;p&gt;If you are defining more complex generic functions, or generic types with multiple parameters, it is useful to provide more descriptive type parameter names. For example, Swift’s &lt;code&gt;Dictionary&lt;/code&gt; type has two type parameters—one for its keys and one for its values. If you were writing &lt;code&gt;Dictionary&lt;/code&gt; yourself, you might name these two type parameters &lt;code&gt;Key&lt;/code&gt; and &lt;code&gt;Value&lt;/code&gt; to remind you of their purpose as you use them within your generic code. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Always give type parameters &lt;code&gt;UpperCamelCase&lt;/code&gt; names (such as &lt;code&gt;T&lt;/code&gt; and &lt;code&gt;Key&lt;/code&gt;) to indicate that they are a placeholder for a &lt;em&gt;type&lt;/em&gt;, not a value. &lt;/p&gt;
&lt;h3&gt;Generic Types&lt;/h3&gt;
&lt;p&gt;In addition to generic functions, Swift enables you to define your own &lt;em&gt;generic types&lt;/em&gt;. These are custom classes, structures, and enumerations that can work with &lt;em&gt;any&lt;/em&gt; type, in a similar way to &lt;code&gt;Array&lt;/code&gt; and &lt;code&gt;Dictionary&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;This section shows you how to write a generic collection type called &lt;code&gt;Stack&lt;/code&gt;. A stack is an ordered set of values, similar to an array, but with a more restricted set of operations than Swift’s &lt;code&gt;Array&lt;/code&gt; type. An array allows new items to be inserted and removed at any location in the array. A stack, however, allows new items to be appended only to the end of the collection (known as &lt;em&gt;pushing&lt;/em&gt; a new value on to the stack). Similarly, a stack allows items to be removed only from the end of the collection (known as &lt;em&gt;popping&lt;/em&gt; a value off the stack). &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The concept of a stack is used by the &lt;code&gt;UINavigationController&lt;/code&gt; class to model the view controllers in its navigation hierarchy. You call the &lt;code&gt;UINavigationController&lt;/code&gt; class &lt;code&gt;pushViewController:animated:&lt;/code&gt; method to add (or push) a view controller on to the navigation stack, and its &lt;code&gt;popViewControllerAnimated:&lt;/code&gt; method to remove (or pop) a view controller from the navigation stack. A stack is a useful collection model whenever you need a strict “last in, first out” approach to managing a collection. &lt;/p&gt;
&lt;p&gt;The illustration below shows the push / pop behavior for a stack: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/stackPushPop_2x.png" src="http://muxuezi.github.io/posts/Art/stackPushPop_2x.png"&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;There are currently three values on the stack. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A fourth value is “pushed” on to the top of the stack. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The stack now holds four values, with the most recent one at the top. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The top item in the stack is removed, or “popped”. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;After popping a value, the stack once again holds three values. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how to write a non-generic version of a stack, in this case for a stack of &lt;code&gt;Int&lt;/code&gt; values: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct IntStack {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var items = [Int]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func push(item: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;items.append(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func pop() -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items.removeLast()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This structure uses an &lt;code&gt;Array&lt;/code&gt; property called &lt;code&gt;items&lt;/code&gt; to store the values in the stack. &lt;code&gt;Stack&lt;/code&gt; provides two methods, &lt;code&gt;push&lt;/code&gt; and &lt;code&gt;pop&lt;/code&gt;, to push and pop values on and off the stack. These methods are marked as &lt;code&gt;mutating&lt;/code&gt;, because they need to modify (or &lt;em&gt;mutate&lt;/em&gt;) the structure’s &lt;code&gt;items&lt;/code&gt; array. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;IntStack&lt;/code&gt; type shown above can only be used with &lt;code&gt;Int&lt;/code&gt; values, however. It would be much more useful to define a &lt;em&gt;generic&lt;/em&gt; &lt;code&gt;Stack&lt;/code&gt; class, that can manage a stack of &lt;em&gt;any&lt;/em&gt; type of value. &lt;/p&gt;
&lt;p&gt;Here’s a generic version of the same code: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Stack&amp;lt;T&amp;gt; {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var items = [T]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func push(item: T) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;items.append(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func pop() -&amp;gt; T {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items.removeLast()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note how the generic version of &lt;code&gt;Stack&lt;/code&gt; is essentially the same as the non-generic version, but with a placeholder type parameter called &lt;code&gt;T&lt;/code&gt; instead of an actual type of &lt;code&gt;Int&lt;/code&gt;. This type parameter is written within a pair of angle brackets (&lt;code&gt;&amp;lt;T&amp;gt;&lt;/code&gt;) immediately after the structure’s name. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;T&lt;/code&gt; defines a placeholder name for “some type &lt;code&gt;T&lt;/code&gt;” to be provided later on. This future type can be referred to as “&lt;code&gt;T&lt;/code&gt;” anywhere within the structure’s definition. In this case, &lt;code&gt;T&lt;/code&gt; is used as a placeholder in three places: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;To create a property called &lt;code&gt;items&lt;/code&gt;, which is initialized with an empty array of values of type &lt;code&gt;T&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;To specify that the &lt;code&gt;push&lt;/code&gt; method has a single parameter called &lt;code&gt;item&lt;/code&gt;, which must be of type &lt;code&gt;T&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;To specify that the value returned by the &lt;code&gt;pop&lt;/code&gt; method will be a value of type &lt;code&gt;T&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because it is a generic type, &lt;code&gt;Stack&lt;/code&gt; can be used to create a stack of &lt;em&gt;any&lt;/em&gt; valid type in Swift, in a similar manner to &lt;code&gt;Array&lt;/code&gt; and &lt;code&gt;Dictionary&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You create a new &lt;code&gt;Stack&lt;/code&gt; instance by writing the type to be stored in the stack within angle brackets. For example, to create a new stack of strings, you write &lt;code&gt;Stack&amp;lt;String&amp;gt;()&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var stackOfStrings = Stack&amp;lt;String&amp;gt;()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stackOfStrings.push("uno")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stackOfStrings.push("dos")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stackOfStrings.push("tres")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stackOfStrings.push("cuatro")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// the stack now contains 4 strings&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how &lt;code&gt;stackOfStrings&lt;/code&gt; looks after pushing these four values on to the stack: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/stackPushedFourStrings_2x.png" src="http://muxuezi.github.io/posts/Art/stackPushedFourStrings_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Popping a value from the stack returns and removes the top value, &lt;code&gt;"cuatro"&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let fromTheTop = stackOfStrings.pop()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// fromTheTop is equal to "cuatro", and the stack now contains 3 strings&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here’s how the stack looks after popping its top value: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/stackPoppedOneString_2x.png" src="http://muxuezi.github.io/posts/Art/stackPoppedOneString_2x.png"&gt;&lt;/p&gt;
&lt;h3&gt;Extending a Generic Type&lt;/h3&gt;
&lt;p&gt;When you extend a generic type, you do not provide a type parameter list as part of the extension’s definition. Instead, the type parameter list from the &lt;em&gt;original&lt;/em&gt; type definition is available within the body of the extension, and the original type parameter names are used to refer to the type parameters from the original definition. &lt;/p&gt;
&lt;p&gt;The following example extends the generic &lt;code&gt;Stack&lt;/code&gt; type to add a read-only computed property called &lt;code&gt;topItem&lt;/code&gt;, which returns the top item on the stack without popping it from the stack: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Stack {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var topItem: T? {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items.isEmpty ? nil : items[items.count \- 1]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;topItem&lt;/code&gt; property returns an optional value of type &lt;code&gt;T&lt;/code&gt;. If the stack is empty, &lt;code&gt;topItem&lt;/code&gt; returns &lt;code&gt;nil&lt;/code&gt;; if the stack is not empty, &lt;code&gt;topItem&lt;/code&gt; returns the final item in the &lt;code&gt;items&lt;/code&gt; array. &lt;/p&gt;
&lt;p&gt;Note that this extension does not define a type parameter list. Instead, the &lt;code&gt;Stack&lt;/code&gt; type’s existing type parameter name, &lt;code&gt;T&lt;/code&gt;, is used within the extension to indicate the optional type of the &lt;code&gt;topItem&lt;/code&gt; computed property. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;topItem&lt;/code&gt; computed property can now be used with any &lt;code&gt;Stack&lt;/code&gt; instance to access and query its top item without removing it: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let topItem = stackOfStrings.topItem {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The top item on the stack is \\(topItem).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The top item on the stack is tres."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Type Constraints&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;swapTwoValues&lt;/code&gt; function and the &lt;code&gt;Stack&lt;/code&gt; type can work with any type. However, it is sometimes useful to enforce certain &lt;em&gt;type constraints&lt;/em&gt; on the types that can be used with generic functions and generic types. Type constraints specify that a type parameter must inherit from a specific class, or conform to a particular protocol or protocol composition. &lt;/p&gt;
&lt;p&gt;For example, Swift’s &lt;code&gt;Dictionary&lt;/code&gt; type places a limitation on the types that can be used as keys for a dictionary. As described in Dictionaries, the type of a dictionary’s keys must be &lt;em&gt;hashable&lt;/em&gt;. That is, it must provide a way to make itself uniquely representable. &lt;code&gt;Dictionary&lt;/code&gt; needs its keys to be hashable so that it can check whether it already contains a value for a particular key. Without this requirement, &lt;code&gt;Dictionary&lt;/code&gt; could not tell whether it should insert or replace a value for a particular key, nor would it be able to find a value for a given key that is already in the dictionary. &lt;/p&gt;
&lt;p&gt;This requirement is enforced by a type constraint on the key type for &lt;code&gt;Dictionary&lt;/code&gt;, which specifies that the key type must conform to the &lt;code&gt;Hashable&lt;/code&gt; protocol, a special protocol defined in the Swift standard library. All of Swift’s basic types (such as &lt;code&gt;String&lt;/code&gt;, &lt;code&gt;Int&lt;/code&gt;, &lt;code&gt;Double&lt;/code&gt;, and &lt;code&gt;Bool&lt;/code&gt;) are hashable by default. &lt;/p&gt;
&lt;p&gt;You can define your own type constraints when creating custom generic types, and these constraints provide much of the power of generic programming. Abstract concepts like &lt;code&gt;Hashable&lt;/code&gt; characterize types in terms of their conceptual characteristics, rather than their explicit type. &lt;/p&gt;
&lt;h4&gt;Type Constraint Syntax&lt;/h4&gt;
&lt;p&gt;You write type constraints by placing a single class or protocol constraint after a type parameter’s name, separated by a colon, as part of the type parameter list. The basic syntax for type constraints on a generic function is shown below (although the syntax is the same for generic types): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func someFunction&amp;lt;T: SomeClass, U: SomeProtocol&amp;gt;(someT: T, someU: U) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// function body goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The hypothetical function above has two type parameters. The first type parameter, &lt;code&gt;T&lt;/code&gt;, has a type constraint that requires &lt;code&gt;T&lt;/code&gt; to be a subclass of &lt;code&gt;SomeClass&lt;/code&gt;. The second type parameter, &lt;code&gt;U&lt;/code&gt;, has a type constraint that requires &lt;code&gt;U&lt;/code&gt; to conform to the protocol &lt;code&gt;SomeProtocol&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Type Constraints in Action&lt;/h4&gt;
&lt;p&gt;Here’s a non-generic function called &lt;code&gt;findStringIndex&lt;/code&gt;, which is given a &lt;code&gt;String&lt;/code&gt; value to find and an array of &lt;code&gt;String&lt;/code&gt; values within which to find it. The &lt;code&gt;findStringIndex&lt;/code&gt; function returns an optional &lt;code&gt;Int&lt;/code&gt; value, which will be the index of the first matching string in the array if it is found, or &lt;code&gt;nil&lt;/code&gt; if the string cannot be found: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func findStringIndex(array: [String], valueToFind: String) -&amp;gt; Int? {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for (index, value) in enumerate(array) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if value == valueToFind {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return index&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;findStringIndex&lt;/code&gt; function can be used to find a string value in an array of strings: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let strings = ["cat", "dog", "llama", "parakeet", "terrapin"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let foundIndex = findStringIndex(strings, "llama") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The index of llama is \\(foundIndex)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The index of llama is 2"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The principle of finding the index of a value in an array isn’t useful only for strings, however. You can write the same functionality as a generic function called &lt;code&gt;findIndex&lt;/code&gt;, by replacing any mention of strings with values of some type &lt;code&gt;T&lt;/code&gt; instead. &lt;/p&gt;
&lt;p&gt;Here’s how you might expect a generic version of &lt;code&gt;findStringIndex&lt;/code&gt;, called &lt;code&gt;findIndex&lt;/code&gt;, to be written. Note that the return type of this function is still &lt;code&gt;Int?&lt;/code&gt;, because the function returns an optional index number, not an optional value from the array. Be warned, though—this function does not compile, for reasons explained after the example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func findIndex&amp;lt;T&amp;gt;(array: [T], valueToFind: T) -&amp;gt; Int? {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for (index, value) in enumerate(array) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if value == valueToFind {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return index&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This function does not compile as written above. The problem lies with the equality check, “&lt;code&gt;if value == valueToFind&lt;/code&gt;”. Not every type in Swift can be compared with the equal to operator (&lt;code&gt;==&lt;/code&gt;). If you create your own class or structure to represent a complex data model, for example, then the meaning of “equal to” for that class or structure is not something that Swift can guess for you. Because of this, it is not possible to guarantee that this code will work for &lt;em&gt;every&lt;/em&gt; possible type &lt;code&gt;T&lt;/code&gt;, and an appropriate error is reported when you try to compile the code. &lt;/p&gt;
&lt;p&gt;All is not lost, however. The Swift standard library defines a protocol called &lt;code&gt;Equatable&lt;/code&gt;, which requires any conforming type to implement the equal to operator (&lt;code&gt;==&lt;/code&gt;) and the not equal to operator (&lt;code&gt;!=&lt;/code&gt;) to compare any two values of that type. All of Swift’s standard types automatically support the &lt;code&gt;Equatable&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;Any type that is &lt;code&gt;Equatable&lt;/code&gt; can be used safely with the &lt;code&gt;findIndex&lt;/code&gt; function, because it is guaranteed to support the equal to operator. To express this fact, you write a type constraint of &lt;code&gt;Equatable&lt;/code&gt; as part of the type parameter’s definition when you define the function: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func findIndex&amp;lt;T: Equatable&amp;gt;(array: [T], valueToFind: T) -&amp;gt; Int? {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for (index, value) in enumerate(array) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if value == valueToFind {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return index&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The single type parameter for &lt;code&gt;findIndex&lt;/code&gt; is written as &lt;code&gt;T: Equatable&lt;/code&gt;, which means “any type &lt;code&gt;T&lt;/code&gt; that conforms to the &lt;code&gt;Equatable&lt;/code&gt; protocol.” &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;findIndex&lt;/code&gt; function now compiles successfully and can be used with any type that is &lt;code&gt;Equatable&lt;/code&gt;, such as &lt;code&gt;Double&lt;/code&gt; or &lt;code&gt;String&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let doubleIndex = findIndex([3.14159, 0.1, 0.25], 9.3)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// doubleIndex is an optional Int with no value, because 9.3 is not in the array&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let stringIndex = findIndex(["Mike", "Malcolm", "Andrea"], "Andrea")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// stringIndex is an optional Int containing a value of 2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Associated Types&lt;/h3&gt;
&lt;p&gt;When defining a protocol, it is sometimes useful to declare one or more &lt;em&gt;associated types&lt;/em&gt; as part of the protocol’s definition. An associated type gives a placeholder name (or &lt;em&gt;alias&lt;/em&gt;) to a type that is used as part of the protocol. The actual type to use for that associated type is not specified until the protocol is adopted. Associated types are specified with the &lt;code&gt;typealias&lt;/code&gt; keyword. &lt;/p&gt;
&lt;h4&gt;Associated Types in Action&lt;/h4&gt;
&lt;p&gt;Here’s an example of a protocol called &lt;code&gt;Container&lt;/code&gt;, which declares an associated type called &lt;code&gt;ItemType&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol Container {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;typealias ItemType&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func append(item: ItemType)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var count: Int { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;subscript(i: Int) -&amp;gt; ItemType { get }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;Container&lt;/code&gt; protocol defines three required capabilities that any container must provide: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;It must be possible to add a new item to the container with an &lt;code&gt;append&lt;/code&gt; method. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;It must be possible to access a count of the items in the container through a &lt;code&gt;count&lt;/code&gt; property that returns an &lt;code&gt;Int&lt;/code&gt; value. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;It must be possible to retrieve each item in the container with a subscript that takes an &lt;code&gt;Int&lt;/code&gt; index value. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This protocol doesn’t specify how the items in the container should be stored or what type they are allowed to be. The protocol only specifies the three bits of functionality that any type must provide in order to be considered a &lt;code&gt;Container&lt;/code&gt;. A conforming type can provide additional functionality, as long as it satisfies these three requirements. &lt;/p&gt;
&lt;p&gt;Any type that conforms to the &lt;code&gt;Container&lt;/code&gt; protocol must be able to specify the type of values it stores. Specifically, it must ensure that only items of the right type are added to the container, and it must be clear about the type of the items returned by its subscript. &lt;/p&gt;
&lt;p&gt;To define these requirements, the &lt;code&gt;Container&lt;/code&gt; protocol needs a way to refer to the type of the elements that a container will hold, without knowing what that type is for a specific container. The &lt;code&gt;Container&lt;/code&gt; protocol needs to specify that any value passed to the &lt;code&gt;append&lt;/code&gt; method must have the same type as the container’s element type, and that the value returned by the container’s subscript will be of the same type as the container’s element type. &lt;/p&gt;
&lt;p&gt;To achieve this, the &lt;code&gt;Container&lt;/code&gt; protocol declares an associated type called &lt;code&gt;ItemType&lt;/code&gt;, written as &lt;code&gt;typealias ItemType&lt;/code&gt;. The protocol does not define what &lt;code&gt;ItemType&lt;/code&gt; is an alias &lt;em&gt;for&lt;/em&gt;—that information is left for any conforming type to provide. Nonetheless, the &lt;code&gt;ItemType&lt;/code&gt; alias provides a way to refer to the type of the items in a &lt;code&gt;Container&lt;/code&gt;, and to define a type for use with the &lt;code&gt;append&lt;/code&gt; method and subscript, to ensure that the expected behavior of any &lt;code&gt;Container&lt;/code&gt; is enforced. &lt;/p&gt;
&lt;p&gt;Here’s a version of the non-generic &lt;code&gt;IntStack&lt;/code&gt; type from earlier, adapted to conform to the &lt;code&gt;Container&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct IntStack: Container {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// original IntStack implementation&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var items = [Int]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func push(item: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;items.append(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func pop() -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items.removeLast()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// conformance to the Container protocol&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;typealias ItemType = Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func append(item: Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.push(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var count: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items.count&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;subscript(i: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items[i]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;IntStack&lt;/code&gt; type implements all three of the &lt;code&gt;Container&lt;/code&gt; protocol’s requirements, and in each case wraps part of the &lt;code&gt;IntStack&lt;/code&gt; type’s existing functionality to satisfy these requirements. &lt;/p&gt;
&lt;p&gt;Moreover, &lt;code&gt;IntStack&lt;/code&gt; specifies that for this implementation of &lt;code&gt;Container&lt;/code&gt;, the appropriate &lt;code&gt;ItemType&lt;/code&gt; to use is a type of &lt;code&gt;Int&lt;/code&gt;. The definition of &lt;code&gt;typealias ItemType = Int&lt;/code&gt; turns the abstract type of &lt;code&gt;ItemType&lt;/code&gt; into a concrete type of &lt;code&gt;Int&lt;/code&gt; for this implementation of the &lt;code&gt;Container&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;Thanks to Swift’s type inference, you don’t actually need to declare a concrete &lt;code&gt;ItemType&lt;/code&gt; of &lt;code&gt;Int&lt;/code&gt; as part of the definition of &lt;code&gt;IntStack&lt;/code&gt;. Because &lt;code&gt;IntStack&lt;/code&gt; conforms to all of the requirements of the &lt;code&gt;Container&lt;/code&gt; protocol, Swift can infer the appropriate &lt;code&gt;ItemType&lt;/code&gt; to use, simply by looking at the type of the &lt;code&gt;append&lt;/code&gt; method’s &lt;code&gt;item&lt;/code&gt; parameter and the return type of the subscript. Indeed, if you delete the &lt;code&gt;typealias ItemType = Int&lt;/code&gt; line from the code above, everything still works, because it is clear what type should be used for &lt;code&gt;ItemType&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can also make the generic &lt;code&gt;Stack&lt;/code&gt; type conform to the &lt;code&gt;Container&lt;/code&gt; protocol: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Stack&amp;lt;T&amp;gt;: Container {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// original Stack&amp;lt;T&amp;gt; implementation&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var items = [T]()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func push(item: T) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;items.append(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func pop() -&amp;gt; T {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items.removeLast()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// conformance to the Container protocol&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func append(item: T) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.push(item)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var count: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items.count&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;subscript(i: Int) -&amp;gt; T {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return items[i]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This time, the placeholder type parameter &lt;code&gt;T&lt;/code&gt; is used as the type of the &lt;code&gt;append&lt;/code&gt; method’s &lt;code&gt;item&lt;/code&gt; parameter and the return type of the subscript. Swift can therefore infer that &lt;code&gt;T&lt;/code&gt; is the appropriate type to use as the &lt;code&gt;ItemType&lt;/code&gt; for this particular container. &lt;/p&gt;
&lt;h4&gt;Extending an Existing Type to Specify an Associated Type&lt;/h4&gt;
&lt;p&gt;You can extend an existing type to add conformance to a protocol, as described in Adding Protocol Conformance with an Extension. This includes a protocol with an associated type. &lt;/p&gt;
&lt;p&gt;Swift’s &lt;code&gt;Array&lt;/code&gt; type already provides an &lt;code&gt;append&lt;/code&gt; method, a &lt;code&gt;count&lt;/code&gt; property, and a subscript with an &lt;code&gt;Int&lt;/code&gt; index to retrieve its elements. These three capabilities match the requirements of the &lt;code&gt;Container&lt;/code&gt; protocol. This means that you can extend &lt;code&gt;Array&lt;/code&gt; to conform to the &lt;code&gt;Container&lt;/code&gt; protocol simply by declaring that &lt;code&gt;Array&lt;/code&gt; adopts the protocol. You do this with an empty extension, as described in Declaring Protocol Adoption with an Extension: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;extension Array: Container {}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Array’s existing &lt;code&gt;append&lt;/code&gt; method and subscript enable Swift to infer the appropriate type to use for &lt;code&gt;ItemType&lt;/code&gt;, just as for the generic &lt;code&gt;Stack&lt;/code&gt; type above. After defining this extension, you can use any &lt;code&gt;Array&lt;/code&gt; as a &lt;code&gt;Container&lt;/code&gt;. &lt;/p&gt;
&lt;h3&gt;Where Clauses&lt;/h3&gt;
&lt;p&gt;Type constraints, as described in Type Constraints, enable you to define requirements on the type parameters associated with a generic function or type. &lt;/p&gt;
&lt;p&gt;It can also be useful to define requirements for associated types. You do this by defining &lt;em&gt;where clauses&lt;/em&gt; as part of a type parameter list. A where clause enables you to require that an associated type conforms to a certain protocol, and/or that certain type parameters and associated types be the same. You write a where clause by placing the &lt;code&gt;where&lt;/code&gt; keyword immediately after the list of type parameters, followed by one or more constraints for associated types, and/or one or more equality relationships between types and associated types. &lt;/p&gt;
&lt;p&gt;The example below defines a generic function called &lt;code&gt;allItemsMatch&lt;/code&gt;, which checks to see if two &lt;code&gt;Container&lt;/code&gt; instances contain the same items in the same order. The function returns a Boolean value of &lt;code&gt;true&lt;/code&gt; if all items match and a value of &lt;code&gt;false&lt;/code&gt; if they do not. &lt;/p&gt;
&lt;p&gt;The two containers to be checked do not have to be the same type of container (although they can be), but they do have to hold the same type of items. This requirement is expressed through a combination of type constraints and where clauses: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func allItemsMatch&amp;lt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;C1: Container, C2: Container&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;where C1.ItemType == C2.ItemType, C1.ItemType: Equatable&amp;gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;(someContainer: C1, anotherContainer: C2) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// check that both containers contain the same number of items&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if someContainer.count != anotherContainer.count {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// check each pair of items to see if they are equivalent&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for i in 0..&amp;lt;someContainer.count {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if someContainer[i] != anotherContainer[i] {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return false&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// all items match, so return true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return true&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This function takes two arguments called &lt;code&gt;someContainer&lt;/code&gt; and &lt;code&gt;anotherContainer&lt;/code&gt;. The &lt;code&gt;someContainer&lt;/code&gt; argument is of type &lt;code&gt;C1&lt;/code&gt;, and the &lt;code&gt;anotherContainer&lt;/code&gt; argument is of type &lt;code&gt;C2&lt;/code&gt;. Both &lt;code&gt;C1&lt;/code&gt; and &lt;code&gt;C2&lt;/code&gt; are placeholder type parameters for two container types to be determined when the function is called. &lt;/p&gt;
&lt;p&gt;The function’s type parameter list places the following requirements on the two type parameters: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;C1&lt;/code&gt; must conform to the &lt;code&gt;Container&lt;/code&gt; protocol (written as &lt;code&gt;C1: Container&lt;/code&gt;). &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;C2&lt;/code&gt; must also conform to the &lt;code&gt;Container&lt;/code&gt; protocol (written as &lt;code&gt;C2: Container&lt;/code&gt;). &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;ItemType&lt;/code&gt; for &lt;code&gt;C1&lt;/code&gt; must be the same as the &lt;code&gt;ItemType&lt;/code&gt; for &lt;code&gt;C2&lt;/code&gt; (written as &lt;code&gt;C1.ItemType == C2.ItemType&lt;/code&gt;). &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;ItemType&lt;/code&gt; for &lt;code&gt;C1&lt;/code&gt; must conform to the &lt;code&gt;Equatable&lt;/code&gt; protocol (written as &lt;code&gt;C1.ItemType: Equatable&lt;/code&gt;). &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The third and fourth requirements are defined as part of a where clause, and are written after the &lt;code&gt;where&lt;/code&gt; keyword as part of the function’s type parameter list. &lt;/p&gt;
&lt;p&gt;These requirements mean: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;someContainer&lt;/code&gt; is a container of type &lt;code&gt;C1&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;anotherContainer&lt;/code&gt; is a container of type &lt;code&gt;C2&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;someContainer&lt;/code&gt; and &lt;code&gt;anotherContainer&lt;/code&gt; contain the same type of items. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The items in &lt;code&gt;someContainer&lt;/code&gt; can be checked with the not equal operator (&lt;code&gt;!=&lt;/code&gt;) to see if they are different from each other. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The third and fourth requirements combine to mean that the items in &lt;code&gt;anotherContainer&lt;/code&gt; can &lt;em&gt;also&lt;/em&gt; be checked with the &lt;code&gt;!=&lt;/code&gt; operator, because they are exactly the same type as the items in &lt;code&gt;someContainer&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;These requirements enable the &lt;code&gt;allItemsMatch&lt;/code&gt; function to compare the two containers, even if they are of a different container type. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;allItemsMatch&lt;/code&gt; function starts by checking that both containers contain the same number of items. If they contain a different number of items, there is no way that they can match, and the function returns &lt;code&gt;false&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;After making this check, the function iterates over all of the items in &lt;code&gt;someContainer&lt;/code&gt; with a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; loop and the half-open range operator (&lt;code&gt;..&amp;lt;&lt;/code&gt;). For each item, the function checks whether the item from &lt;code&gt;someContainer&lt;/code&gt; is not equal to the corresponding item in &lt;code&gt;anotherContainer&lt;/code&gt;. If the two items are not equal, then the two containers do not match, and the function returns &lt;code&gt;false&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If the loop finishes without finding a mismatch, the two containers match, and the function returns &lt;code&gt;true&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Here’s how the &lt;code&gt;allItemsMatch&lt;/code&gt; function looks in action: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var stackOfStrings = Stack&amp;lt;String&amp;gt;()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stackOfStrings.push("uno")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stackOfStrings.push("dos")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stackOfStrings.push("tres")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var arrayOfStrings = ["uno", "dos", "tres"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if allItemsMatch(stackOfStrings, arrayOfStrings) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("All items match.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("Not all items match.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "All items match."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example above creates a &lt;code&gt;Stack&lt;/code&gt; instance to store &lt;code&gt;String&lt;/code&gt; values, and pushes three strings onto the stack. The example also creates an &lt;code&gt;Array&lt;/code&gt; instance initialized with an array literal containing the same three strings as the stack. Even though the stack and the array are of a different type, they both conform to the &lt;code&gt;Container&lt;/code&gt; protocol, and both contain the same type of values. You can therefore call the &lt;code&gt;allItemsMatch&lt;/code&gt; function with these two containers as its arguments. In the example above, the &lt;code&gt;allItemsMatch&lt;/code&gt; function correctly reports that all of the items in the two containers match. &lt;/p&gt;
&lt;h2&gt;Access Control&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Access control&lt;/em&gt; restricts access to parts of your code from code in other source files and modules. This feature enables you to hide the implementation details of your code, and to specify a preferred interface through which that code can be accessed and used. &lt;/p&gt;
&lt;p&gt;You can assign specific access levels to individual types (classes, structures, and enumerations), as well as to properties, methods, initializers, and subscripts belonging to those types. Protocols can be restricted to a certain context, as can global constants, variables, and functions. &lt;/p&gt;
&lt;p&gt;In addition to offering various levels of access control, Swift reduces the need to specify explicit access control levels by providing default access levels for typical scenarios. Indeed, if you are writing a single-target app, you may not need to specify explicit access control levels at all. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The various aspects of your code that can have access control applied to them (properties, types, functions, and so on) are referred to as “entities” in the sections below, for brevity. &lt;/p&gt;
&lt;h3&gt;Modules and Source Files&lt;/h3&gt;
&lt;p&gt;Swift’s access control model is based on the concept of modules and source files. &lt;/p&gt;
&lt;p&gt;A &lt;em&gt;module&lt;/em&gt; is a single unit of code distribution—a framework or application that is built and shipped as a single unit and that can be imported by another module with Swift’s &lt;code&gt;import&lt;/code&gt; keyword. &lt;/p&gt;
&lt;p&gt;Each build target (such as an app bundle or framework) in Xcode is treated as a separate module in Swift. If you group together aspects of your app’s code as a stand-alone framework—perhaps to encapsulate and reuse that code across multiple applications—then everything you define within that framework will be part of a separate module when it is imported and used within an app, or when it is used within another framework. &lt;/p&gt;
&lt;p&gt;A &lt;em&gt;source file&lt;/em&gt; is a single Swift source code file within a module (in effect, a single file within an app or framework). Although it is common to define individual types in separate source files, a single source file can contain definitions for multiple types, functions, and so on. &lt;/p&gt;
&lt;h3&gt;Access Levels&lt;/h3&gt;
&lt;p&gt;Swift provides three different &lt;em&gt;access levels&lt;/em&gt; for entities within your code. These access levels are relative to the source file in which an entity is defined, and also relative to the module that source file belongs to. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;Public access&lt;/em&gt; enables entities to be used within any source file from their defining module, and also in a source file from another module that imports the defining module. You typically use public access when specifying the public interface to a framework. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;Internal access&lt;/em&gt; enables entities to be used within any source file from their defining module, but not in any source file outside of that module. You typically use internal access when defining an app’s or a framework’s internal structure. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;Private access&lt;/em&gt; restricts the use of an entity to its own defining source file. Use private access to hide the implementation details of a specific piece of functionality. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Public access is the highest (least restrictive) access level and private access is the lowest (or most restrictive) access level. &lt;/p&gt;
&lt;h4&gt;Guiding Principle of Access Levels&lt;/h4&gt;
&lt;p&gt;Access levels in Swift follow an overall guiding principle: &lt;em&gt;No entity can be defined in terms of another entity that has a lower (more restrictive) access level.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;A public variable cannot be defined as having an internal or private type, because the type might not be available everywhere that the public variable is used. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A function cannot have a higher access level than its parameter types and return type, because the function could be used in situations where its constituent types are not available to the surrounding code. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The specific implications of this guiding principle for different aspects of the language are covered in detail below. &lt;/p&gt;
&lt;h4&gt;Default Access Levels&lt;/h4&gt;
&lt;p&gt;All entities in your code (with a few specific exceptions, as described later in this chapter) have a default access level of internal if you do not specify an explicit access level yourself. As a result, in many cases you do not need to specify an explicit access level in your code. &lt;/p&gt;
&lt;h4&gt;Access Levels for Single-Target Apps&lt;/h4&gt;
&lt;p&gt;When you write a simple single-target app, the code in your app is typically self-contained within the app and does not need to be made available outside of the app’s module. The default access level of internal already matches this requirement. Therefore, you do not need to specify a custom access level. You may, however, want to mark some parts of your code as private in order to hide their implementation details from other code within the app’s module. &lt;/p&gt;
&lt;h4&gt;Access Levels for Frameworks&lt;/h4&gt;
&lt;p&gt;When you develop a framework, mark the public-facing interface to that framework as public so that it can be viewed and accessed by other modules, such as an app that imports the framework. This public-facing interface is the application programming interface (or API) for the framework. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Any internal implementation details of your framework can still use the default access level of internal, or can be marked as private if you want to hide them from other parts of the framework’s internal code. You need to mark an entity as public only if you want it to become part of your framework’s API. &lt;/p&gt;
&lt;h3&gt;Access Control Syntax&lt;/h3&gt;
&lt;p&gt;Define the access level for an entity by placing one of the &lt;code&gt;public&lt;/code&gt;, &lt;code&gt;internal&lt;/code&gt;, or &lt;code&gt;private&lt;/code&gt; modifiers before the entity’s introducer: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;public class SomePublicClass {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal class SomeInternalClass {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private class SomePrivateClass {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;public var somePublicVariable = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal let someInternalConstant = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private func somePrivateFunction() {}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unless otherwise specified, the default access level is internal, as described in Default Access Levels. This means that &lt;code&gt;SomeInternalClass&lt;/code&gt; and &lt;code&gt;someInternalConstant&lt;/code&gt; can be written without an explicit access level modifier, and will still have an access level of internal: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeInternalClass {} // implicitly internal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someInternalConstant = 0 // implicitly internal&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Custom Types&lt;/h3&gt;
&lt;p&gt;If you want to specify an explicit access level for a custom type, do so at the point that you define the type. The new type can then be used wherever its access level permits. For example, if you define a private class, that class can only be used as the type of a property, or as a function parameter or return type, in the source file in which the private class is defined. &lt;/p&gt;
&lt;p&gt;The access control level of a type also affects the default access level of that type’s &lt;em&gt;members&lt;/em&gt; (its properties, methods, initializers, and subscripts). If you define a type’s access level as private, the default access level of its members will also be private. If you define a type’s access level as internal or public (or use the default access level of internal without specifying an access level explicitly), the default access level of the type’s members will be internal. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;As mentioned above, a public type defaults to having internal members, not public members. If you want a type member to be public, you must explicitly mark it as such. This requirement ensures that the public-facing API for a type is something you opt in to publishing, and avoids presenting the internal workings of a type as public API by mistake. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;public class SomePublicClass { // explicitly public class&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;public var somePublicProperty = 0 // explicitly public class member&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someInternalProperty = 0 // implicitly internal class member&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private func somePrivateMethod() {} // explicitly private class member&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class SomeInternalClass { // implicitly internal class&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someInternalProperty = 0 // implicitly internal class member&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private func somePrivateMethod() {} // explicitly private class member&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private class SomePrivateClass { // explicitly private class&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var somePrivateProperty = 0 // implicitly private class member&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func somePrivateMethod() {} // implicitly private class member&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Tuple Types&lt;/h4&gt;
&lt;p&gt;The access level for a tuple type is the most restrictive access level of all types used in that tuple. For example, if you compose a tuple from two different types, one with internal access and one with private access, the access level for that compound tuple type will be private. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Tuple types do not have a standalone definition in the way that classes, structures, enumerations, and functions do. A tuple type’s access level is deduced automatically when the tuple type is used, and cannot be specified explicitly. &lt;/p&gt;
&lt;h4&gt;Function Types&lt;/h4&gt;
&lt;p&gt;The access level for a function type is calculated as the most restrictive access level of the function’s parameter types and return type. You must specify the access level explicitly as part of the function’s definition if the function’s calculated access level does not match the contextual default. &lt;/p&gt;
&lt;p&gt;The example below defines a global function called &lt;code&gt;someFunction&lt;/code&gt;, without providing a specific access level modifier for the function itself. You might expect this function to have the default access level of “internal”, but this is not the case. In fact, &lt;code&gt;someFunction&lt;/code&gt; will not compile as written below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func someFunction() -&amp;gt; (SomeInternalClass, SomePrivateClass) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// function implementation goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The function’s return type is a tuple type composed from two of the custom classes defined above in Custom Types. One of these classes was defined as “internal”, and the other was defined as “private”. Therefore, the overall access level of the compound tuple type is “private” (the minimum access level of the tuple’s constituent types). &lt;/p&gt;
&lt;p&gt;Because the function’s return type is private, you must mark the function’s overall access level with the &lt;code&gt;private&lt;/code&gt; modifier for the function declaration to be valid: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;private func someFunction() -&amp;gt; (SomeInternalClass, SomePrivateClass) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// function implementation goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;It is not valid to mark the definition of &lt;code&gt;someFunction&lt;/code&gt; with the &lt;code&gt;public&lt;/code&gt; or &lt;code&gt;internal&lt;/code&gt; modifiers, or to use the default setting of internal, because public or internal users of the function might not have appropriate access to the private class used in the function’s return type. &lt;/p&gt;
&lt;h4&gt;Enumeration Types&lt;/h4&gt;
&lt;p&gt;The individual cases of an enumeration automatically receive the same access level as the enumeration they belong to. You cannot specify a different access level for individual enumeration cases. &lt;/p&gt;
&lt;p&gt;In the example below, the &lt;code&gt;CompassPoint&lt;/code&gt; enumeration has an explicit access level of “public”. The enumeration cases &lt;code&gt;North&lt;/code&gt;, &lt;code&gt;South&lt;/code&gt;, &lt;code&gt;East&lt;/code&gt;, and &lt;code&gt;West&lt;/code&gt; therefore also have an access level of “public”: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;public enum CompassPoint {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case North&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case South&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case East&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case West&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Raw Values and Associated Values&lt;/h4&gt;
&lt;p&gt;The types used for any raw values or associated values in an enumeration definition must have an access level at least as high as the enumeration’s access level. You cannot use a &lt;code&gt;private&lt;/code&gt; type as the raw-value type of an enumeration with an &lt;code&gt;internal&lt;/code&gt; access level, for example. &lt;/p&gt;
&lt;h4&gt;Nested Types&lt;/h4&gt;
&lt;p&gt;Nested types defined within a private type have an automatic access level of private. Nested types defined within a public type or an internal type have an automatic access level of internal. If you want a nested type within a public type to be publicly available, you must explicitly declare the nested type as public. &lt;/p&gt;
&lt;h3&gt;Subclassing&lt;/h3&gt;
&lt;p&gt;You can subclass any class that can be accessed in the current access context. A subclass cannot have a higher access level than its superclass—for example, you cannot write a public subclass of an internal superclass. &lt;/p&gt;
&lt;p&gt;In addition, you can override any class member (method, property, initializer, or subscript) that is visible in a certain access context. &lt;/p&gt;
&lt;p&gt;An override can make an inherited class member more accessible than its superclass version. In the example below, class &lt;code&gt;A&lt;/code&gt; is a public class with a private method called &lt;code&gt;someMethod&lt;/code&gt;. Class &lt;code&gt;B&lt;/code&gt; is a subclass of &lt;code&gt;A&lt;/code&gt;, with a reduced access level of “internal”. Nonetheless, class &lt;code&gt;B&lt;/code&gt; provides an override of &lt;code&gt;someMethod&lt;/code&gt; with an access level of “internal”, which is &lt;em&gt;higher&lt;/em&gt; than the original implementation of &lt;code&gt;someMethod&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;public class A {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private func someMethod() {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal class B: A {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override internal func someMethod() {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;It is even valid for a subclass member to call a superclass member that has lower access permissions than the subclass member, as long as the call to the superclass’s member takes place within an allowed access level context (that is, within the same source file as the superclass for a private member call, or within the same module as the superclass for an internal member call): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;public class A {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private func someMethod() {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;internal class B: A {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override internal func someMethod() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.someMethod()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because superclass &lt;code&gt;A&lt;/code&gt; and subclass &lt;code&gt;B&lt;/code&gt; are defined in the same source file, it is valid for the &lt;code&gt;B&lt;/code&gt; implementation of &lt;code&gt;someMethod&lt;/code&gt; to call &lt;code&gt;super.someMethod()&lt;/code&gt;. &lt;/p&gt;
&lt;h3&gt;Constants, Variables, Properties, and Subscripts&lt;/h3&gt;
&lt;p&gt;A constant, variable, or property cannot be more public than its type. It is not valid to write a public property with a private type, for example. Similarly, a subscript cannot be more public than either its index type or return type. &lt;/p&gt;
&lt;p&gt;If a constant, variable, property, or subscript makes use of a private type, the constant, variable, property, or subscript must also be marked as &lt;code&gt;private&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;private var privateInstance = SomePrivateClass()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Getters and Setters&lt;/h4&gt;
&lt;p&gt;Getters and setters for constants, variables, properties, and subscripts automatically receive the same access level as the constant, variable, property, or subscript they belong to. &lt;/p&gt;
&lt;p&gt;You can give a setter a &lt;em&gt;lower&lt;/em&gt; access level than its corresponding getter, to restrict the read-write scope of that variable, property, or subscript. You assign a lower access level by writing &lt;code&gt;private(set)&lt;/code&gt; or &lt;code&gt;internal(set)&lt;/code&gt; before the &lt;code&gt;var&lt;/code&gt; or &lt;code&gt;subscript&lt;/code&gt; introducer. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;This rule applies to stored properties as well as computed properties. Even though you do not write an explicit getter and setter for a stored property, Swift still synthesizes an implicit getter and setter for you to provide access to the stored property’s backing storage. Use &lt;code&gt;private(set)&lt;/code&gt; and &lt;code&gt;internal(set)&lt;/code&gt; to change the access level of this synthesized setter in exactly the same way as for an explicit setter in a computed property. &lt;/p&gt;
&lt;p&gt;The example below defines a structure called &lt;code&gt;TrackedString&lt;/code&gt;, which keeps track of the number of times a string property is modified: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct TrackedString {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;private(set) var numberOfEdits = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var value: String = "" {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;didSet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfEdits++&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;TrackedString&lt;/code&gt; structure defines a stored string property called &lt;code&gt;value&lt;/code&gt;, with an initial value of &lt;code&gt;""&lt;/code&gt; (an empty string). The structure also defines a stored integer property called &lt;code&gt;numberOfEdits&lt;/code&gt;, which is used to track the number of times that &lt;code&gt;value&lt;/code&gt; is modified. This modification tracking is implemented with a &lt;code&gt;didSet&lt;/code&gt; property observer on the &lt;code&gt;value&lt;/code&gt; property, which increments &lt;code&gt;numberOfEdits&lt;/code&gt; every time the &lt;code&gt;value&lt;/code&gt; property is set to a new value. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;TrackedString&lt;/code&gt; structure and the &lt;code&gt;value&lt;/code&gt; property do not provide an explicit access level modifier, and so they both receive the default access level of internal. However, the access level for the &lt;code&gt;numberOfEdits&lt;/code&gt; property is marked with a &lt;code&gt;private(set)&lt;/code&gt; modifier to indicate that the property should be settable only from within the same source file as the &lt;code&gt;TrackedString&lt;/code&gt; structure’s definition. The property’s getter still has the default access level of internal, but its setter is now private to the source file in which &lt;code&gt;TrackedString&lt;/code&gt; is defined. This enables &lt;code&gt;TrackedString&lt;/code&gt; to modify the &lt;code&gt;numberOfEdits&lt;/code&gt; property internally, but to present the property as a read-only property when it is used by other source files within the same module. &lt;/p&gt;
&lt;p&gt;If you create a &lt;code&gt;TrackedString&lt;/code&gt; instance and modify its string value a few times, you can see the &lt;code&gt;numberOfEdits&lt;/code&gt; property value update to match the number of modifications: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var stringToEdit = TrackedString()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stringToEdit.value = "This string will be tracked."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stringToEdit.value += " This edit will increment numberOfEdits."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;stringToEdit.value += " So will this one."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The number of edits is \\(stringToEdit.numberOfEdits)")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The number of edits is 3"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Although you can query the current value of the &lt;code&gt;numberOfEdits&lt;/code&gt; property from within another source file, you cannot &lt;em&gt;modify&lt;/em&gt; the property from another source file. This restriction protects the implementation details of the &lt;code&gt;TrackedString&lt;/code&gt; edit-tracking functionality, while still providing convenient access to an aspect of that functionality. &lt;/p&gt;
&lt;p&gt;Note that you can assign an explicit access level for both a getter and a setter if required. The example below shows a version of the &lt;code&gt;TrackedString&lt;/code&gt; structure in which the structure is defined with an explicit access level of public. The structure’s members (including the &lt;code&gt;numberOfEdits&lt;/code&gt; property) therefore have an internal access level by default. You can make the structure’s &lt;code&gt;numberOfEdits&lt;/code&gt; property getter public, and its property setter private, by combining the &lt;code&gt;public&lt;/code&gt; and &lt;code&gt;private(set)&lt;/code&gt; access level modifiers: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;public struct TrackedString {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;public private(set) var numberOfEdits = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;public var value: String = "" {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;didSet {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;numberOfEdits++&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;public init() {}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Initializers&lt;/h3&gt;
&lt;p&gt;Custom initializers can be assigned an access level less than or equal to the type that they initialize. The only exception is for required initializers (as defined in Required Initializers). A required initializer must have the same access level as the class it belongs to. &lt;/p&gt;
&lt;p&gt;As with function and method parameters, the types of an initializer’s parameters cannot be more private than the initializer’s own access level. &lt;/p&gt;
&lt;h4&gt;Default Initializers&lt;/h4&gt;
&lt;p&gt;As described in Default Initializers, Swift automatically provides a &lt;em&gt;default initializer&lt;/em&gt; without any arguments for any structure or base class that provides default values for all of its properties and does not provide at least one initializer itself. &lt;/p&gt;
&lt;p&gt;A default initializer has the same access level as the type it initializes, unless that type is defined as &lt;code&gt;public&lt;/code&gt;. For a type that is defined as &lt;code&gt;public&lt;/code&gt;, the default initializer is considered internal. If you want a public type to be initializable with a no-argument initializer when used in another module, you must explicitly provide a public no-argument initializer yourself as part of the type’s definition. &lt;/p&gt;
&lt;h4&gt;Default Memberwise Initializers for Structure Types&lt;/h4&gt;
&lt;p&gt;The default memberwise initializer for a structure type is considered private if any of the structure’s stored properties are private. Otherwise, the initializer has an access level of internal. &lt;/p&gt;
&lt;p&gt;As with the default initializer above, if you want a public structure type to be initializable with a memberwise initializer when used in another module, you must provide a public memberwise initializer yourself as part of the type’s definition. &lt;/p&gt;
&lt;h3&gt;Protocols&lt;/h3&gt;
&lt;p&gt;If you want to assign an explicit access level to a protocol type, do so at the point that you define the protocol. This enables you to create protocols that can only be adopted within a certain access context. &lt;/p&gt;
&lt;p&gt;The access level of each requirement within a protocol definition is automatically set to the same access level as the protocol. You cannot set a protocol requirement to a different access level than the protocol it supports. This ensures that all of the protocol’s requirements will be visible on any type that adopts the protocol. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you define a public protocol, the protocol’s requirements require a public access level for those requirements when they are implemented. This behavior is different than other types, where a public type definition implies an access level of internal for the type’s members. &lt;/p&gt;
&lt;h4&gt;Protocol Inheritance&lt;/h4&gt;
&lt;p&gt;If you define a new protocol that inherits from an existing protocol, the new protocol can have at most the same access level as the protocol it inherits from. You cannot write a public protocol that inherits from an internal protocol, for example. &lt;/p&gt;
&lt;h4&gt;Protocol Conformance&lt;/h4&gt;
&lt;p&gt;A type can conform to a protocol with a lower access level than the type itself. For example, you can define a public type that can be used in other modules, but whose conformance to an internal protocol can only be used within the internal protocol’s defining module. &lt;/p&gt;
&lt;p&gt;The context in which a type conforms to a particular protocol is the minimum of the type’s access level and the protocol’s access level. If a type is public, but a protocol it conforms to is internal, the type’s conformance to that protocol is also internal. &lt;/p&gt;
&lt;p&gt;When you write or extend a type to conform to a protocol, you must ensure that the type’s implementation of each protocol requirement has at least the same access level as the type’s conformance to that protocol. For example, if a public type conforms to an internal protocol, the type’s implementation of each protocol requirement must be at least “internal”. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;In Swift, as in Objective-C, protocol conformance is global—it is not possible for a type to conform to a protocol in two different ways within the same program. &lt;/p&gt;
&lt;h3&gt;Extensions&lt;/h3&gt;
&lt;p&gt;You can extend a class, structure, or enumeration in any access context in which the class, structure, or enumeration is available. Any type members added in an extension have the same default access level as type members declared in the original type being extended. For example, if you extend a public type, any new type members you add will have a default access level of internal. &lt;/p&gt;
&lt;p&gt;Alternatively, you can mark an extension with an explicit access level modifier (for example, &lt;code&gt;private extension&lt;/code&gt;) to set a new default access level for all members defined within the extension. This new default can still be overridden within the extension for individual type members. &lt;/p&gt;
&lt;h4&gt;Adding Protocol Conformance with an Extension&lt;/h4&gt;
&lt;p&gt;You cannot provide an explicit access level modifier for an extension if you are using that extension to add protocol conformance. Instead, the protocol’s own access level is used to provide the default access level for each protocol requirement implementation within the extension. &lt;/p&gt;
&lt;h3&gt;Generics&lt;/h3&gt;
&lt;p&gt;The access level for a generic type or generic function is the minimum of the access level of the generic type or function itself and the access level of any type constraints on its type parameters. &lt;/p&gt;
&lt;h3&gt;Type Aliases&lt;/h3&gt;
&lt;p&gt;Any type aliases you define are treated as distinct types for the purposes of access control. A type alias can have an access level less than or equal to the access level of the type it aliases. For example, a private type alias can alias a private, internal, or public type, but a public type alias cannot alias an internal or private type. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;This rule also applies to type aliases for associated types used to satisfy protocol conformances. &lt;/p&gt;
&lt;h2&gt;Advanced Operators&lt;/h2&gt;
&lt;p&gt;In addition to the operators described in Basic Operators, Swift provides several advanced operators that perform more complex value manipulation. These include all of the bitwise and bit shifting operators you will be familiar with from C and Objective-C. &lt;/p&gt;
&lt;p&gt;Unlike arithmetic operators in C, arithmetic operators in Swift do not overflow by default. Overflow behavior is trapped and reported as an error. To opt in to overflow behavior, use Swift’s second set of arithmetic operators that overflow by default, such as the overflow addition operator (&lt;code&gt;&amp;amp;+&lt;/code&gt;). All of these overflow operators begin with an ampersand (&lt;code&gt;&amp;amp;&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;When you define your own structures, classes, and enumerations, it can be useful to provide your own implementations of the standard Swift operators for these custom types. Swift makes it easy to provide tailored implementations of these operators and to determine exactly what their behavior should be for each type you create. &lt;/p&gt;
&lt;p&gt;You’re not limited to the predefined operators. Swift gives you the freedom to define your own custom infix, prefix, postfix, and assignment operators, with custom precedence and associativity values. These operators can be used and adopted in your code like any of the predefined operators, and you can even extend existing types to support the custom operators you define. &lt;/p&gt;
&lt;h3&gt;Bitwise Operators&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Bitwise operators&lt;/em&gt; enable you to manipulate the individual raw data bits within a data structure. They are often used in low-level programming, such as graphics programming and device driver creation. Bitwise operators can also be useful when you work with raw data from external sources, such as encoding and decoding data for communication over a custom protocol. &lt;/p&gt;
&lt;p&gt;Swift supports all of the bitwise operators found in C, as described below. &lt;/p&gt;
&lt;h4&gt;Bitwise NOT Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;bitwise NOT operator&lt;/em&gt; (&lt;code&gt;~&lt;/code&gt;) inverts all bits in a number: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitwiseNOT_2x.png" src="http://muxuezi.github.io/posts/Art/bitwiseNOT_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The bitwise NOT operator is a prefix operator, and appears immediately before the value it operates on, without any white space: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let initialBits: UInt8 = 0b00001111&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let invertedBits = ~initialBits // equals 11110000&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;UInt8&lt;/code&gt; integers have eight bits and can store any value between &lt;code&gt;0&lt;/code&gt; and &lt;code&gt;255&lt;/code&gt;. This example initializes a &lt;code&gt;UInt8&lt;/code&gt; integer with the binary value &lt;code&gt;00001111&lt;/code&gt;, which has its first four bits set to &lt;code&gt;0&lt;/code&gt;, and its second four bits set to &lt;code&gt;1&lt;/code&gt;. This is equivalent to a decimal value of &lt;code&gt;15&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The bitwise NOT operator is then used to create a new constant called &lt;code&gt;invertedBits&lt;/code&gt;, which is equal to &lt;code&gt;initialBits&lt;/code&gt;, but with all of the bits inverted. Zeroes become ones, and ones become zeroes. The value of &lt;code&gt;invertedBits&lt;/code&gt; is &lt;code&gt;11110000&lt;/code&gt;, which is equal to an unsigned decimal value of &lt;code&gt;240&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Bitwise AND Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;bitwise AND operator&lt;/em&gt; (&lt;code&gt;&amp;amp;&lt;/code&gt;) combines the bits of two numbers. It returns a new number whose bits are set to &lt;code&gt;1&lt;/code&gt; only if the bits were equal to &lt;code&gt;1&lt;/code&gt; in &lt;em&gt;both&lt;/em&gt; input numbers: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitwiseAND_2x.png" src="http://muxuezi.github.io/posts/Art/bitwiseAND_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;In the example below, the values of &lt;code&gt;firstSixBits&lt;/code&gt; and &lt;code&gt;lastSixBits&lt;/code&gt; both have four middle bits equal to &lt;code&gt;1&lt;/code&gt;. The bitwise AND operator combines them to make the number &lt;code&gt;00111100&lt;/code&gt;, which is equal to an unsigned decimal value of &lt;code&gt;60&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let firstSixBits: UInt8 = 0b11111100&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let lastSixBits: UInt8 = 0b00111111&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let middleFourBits = firstSixBits &amp;amp; lastSixBits // equals 00111100&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Bitwise OR Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;bitwise OR operator&lt;/em&gt; (&lt;code&gt;|&lt;/code&gt;) compares the bits of two numbers. The operator returns a new number whose bits are set to &lt;code&gt;1&lt;/code&gt; if the bits are equal to &lt;code&gt;1&lt;/code&gt; in &lt;em&gt;either&lt;/em&gt; input number: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitwiseOR_2x.png" src="http://muxuezi.github.io/posts/Art/bitwiseOR_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;In the example below, the values of &lt;code&gt;someBits&lt;/code&gt; and &lt;code&gt;moreBits&lt;/code&gt; have different bits set to &lt;code&gt;1&lt;/code&gt;. The bitwise OR operator combines them to make the number &lt;code&gt;11111110&lt;/code&gt;, which equals an unsigned decimal of &lt;code&gt;254&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someBits: UInt8 = 0b10110010&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let moreBits: UInt8 = 0b01011110&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let combinedbits = someBits | moreBits // equals 11111110&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Bitwise XOR Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;bitwise XOR operator&lt;/em&gt;, or “exclusive OR operator” (&lt;code&gt;^&lt;/code&gt;), compares the bits of two numbers. The operator returns a new number whose bits are set to &lt;code&gt;1&lt;/code&gt; where the input bits are different and are set to &lt;code&gt;0&lt;/code&gt; where the input bits are the same: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitwiseXOR_2x.png" src="http://muxuezi.github.io/posts/Art/bitwiseXOR_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;In the example below, the values of &lt;code&gt;firstBits&lt;/code&gt; and &lt;code&gt;otherBits&lt;/code&gt; each have a bit set to &lt;code&gt;1&lt;/code&gt; in a location that the other does not. The bitwise XOR operator sets both of these bits to &lt;code&gt;1&lt;/code&gt; in its output value. All of the other bits in &lt;code&gt;firstBits&lt;/code&gt; and &lt;code&gt;otherBits&lt;/code&gt; match and are set to &lt;code&gt;0&lt;/code&gt; in the output value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let firstBits: UInt8 = 0b00010100&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let otherBits: UInt8 = 0b00000101&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let outputBits = firstBits ^ otherBits // equals 00010001&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Bitwise Left and Right Shift Operators&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;bitwise left shift operator&lt;/em&gt; (&lt;code&gt;&amp;lt;&amp;lt;&lt;/code&gt;) and &lt;em&gt;bitwise right shift operator&lt;/em&gt; (&lt;code&gt;&amp;gt;&amp;gt;&lt;/code&gt;) move all bits in a number to the left or the right by a certain number of places, according to the rules defined below. &lt;/p&gt;
&lt;p&gt;Bitwise left and right shifts have the effect of multiplying or dividing an integer number by a factor of two. Shifting an integer’s bits to the left by one position doubles its value, whereas shifting it to the right by one position halves its value. &lt;/p&gt;
&lt;h4&gt;Shifting Behavior for Unsigned Integers&lt;/h4&gt;
&lt;p&gt;The bit-shifting behavior for unsigned integers is as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Existing bits are moved to the left or right by the requested number of places. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Any bits that are moved beyond the bounds of the integer’s storage are discarded. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Zeroes are inserted in the spaces left behind after the original bits are moved to the left or right. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This approach is known as a &lt;em&gt;logical shift&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;The illustration below shows the results of &lt;code&gt;11111111 &amp;lt;&amp;lt; 1&lt;/code&gt; (which is &lt;code&gt;11111111&lt;/code&gt; shifted to the left by &lt;code&gt;1&lt;/code&gt; place), and &lt;code&gt;11111111 &amp;gt;&amp;gt; 1&lt;/code&gt; (which is &lt;code&gt;11111111&lt;/code&gt; shifted to the right by &lt;code&gt;1&lt;/code&gt; place). Blue numbers are shifted, gray numbers are discarded, and orange zeroes are inserted: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitshiftUnsigned_2x.png" src="http://muxuezi.github.io/posts/Art/bitshiftUnsigned_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Here’s how bit shifting looks in Swift code: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let shiftBits: UInt8 = 4 // 00000100 in binary&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shiftBits &amp;lt;&amp;lt; 1 // 00001000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shiftBits &amp;lt;&amp;lt; 2 // 00010000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shiftBits &amp;lt;&amp;lt; 5 // 10000000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shiftBits &amp;lt;&amp;lt; 6 // 00000000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;shiftBits &amp;gt;&amp;gt; 2 // 00000001&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can use bit shifting to encode and decode values within other data types: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let pink: UInt32 = 0xCC6699&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let redComponent = (pink &amp;amp; 0xFF0000) &amp;gt;&amp;gt; 16 // redComponent is 0xCC, or 204&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let greenComponent = (pink &amp;amp; 0x00FF00) &amp;gt;&amp;gt; 8 // greenComponent is 0x66, or 102&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let blueComponent = pink &amp;amp; 0x0000FF // blueComponent is 0x99, or 153&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example uses a &lt;code&gt;UInt32&lt;/code&gt; constant called &lt;code&gt;pink&lt;/code&gt; to store a Cascading Style Sheets color value for the color pink. The CSS color value &lt;code&gt;#CC6699&lt;/code&gt; is written as &lt;code&gt;0xCC6699&lt;/code&gt; in Swift’s hexadecimal number representation. This color is then decomposed into its red (&lt;code&gt;CC&lt;/code&gt;), green (&lt;code&gt;66&lt;/code&gt;), and blue (&lt;code&gt;99&lt;/code&gt;) components by the bitwise AND operator (&lt;code&gt;&amp;amp;&lt;/code&gt;) and the bitwise right shift operator (&lt;code&gt;&amp;gt;&amp;gt;&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;The red component is obtained by performing a bitwise AND between the numbers &lt;code&gt;0xCC6699&lt;/code&gt; and &lt;code&gt;0xFF0000&lt;/code&gt;. The zeroes in &lt;code&gt;0xFF0000&lt;/code&gt; effectively “mask” the second and third bytes of &lt;code&gt;0xCC6699&lt;/code&gt;, causing the &lt;code&gt;6699&lt;/code&gt; to be ignored and leaving &lt;code&gt;0xCC0000&lt;/code&gt; as the result. &lt;/p&gt;
&lt;p&gt;This number is then shifted 16 places to the right (&lt;code&gt;&amp;gt;&amp;gt; 16&lt;/code&gt;). Each pair of characters in a hexadecimal number uses 8 bits, so a move 16 places to the right will convert &lt;code&gt;0xCC0000&lt;/code&gt; into &lt;code&gt;0x0000CC&lt;/code&gt;. This is the same as &lt;code&gt;0xCC&lt;/code&gt;, which has a decimal value of &lt;code&gt;204&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Similarly, the green component is obtained by performing a bitwise AND between the numbers &lt;code&gt;0xCC6699&lt;/code&gt; and &lt;code&gt;0x00FF00&lt;/code&gt;, which gives an output value of &lt;code&gt;0x006600&lt;/code&gt;. This output value is then shifted eight places to the right, giving a value of &lt;code&gt;0x66&lt;/code&gt;, which has a decimal value of &lt;code&gt;102&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Finally, the blue component is obtained by performing a bitwise AND between the numbers &lt;code&gt;0xCC6699&lt;/code&gt; and &lt;code&gt;0x0000FF&lt;/code&gt;, which gives an output value of &lt;code&gt;0x000099&lt;/code&gt;. There’s no need to shift this to the right, as &lt;code&gt;0x000099&lt;/code&gt; already equals &lt;code&gt;0x99&lt;/code&gt;, which has a decimal value of &lt;code&gt;153&lt;/code&gt;. &lt;/p&gt;
&lt;h4&gt;Shifting Behavior for Signed Integers&lt;/h4&gt;
&lt;p&gt;The shifting behavior is more complex for signed integers than for unsigned integers, because of the way signed integers are represented in binary. (The examples below are based on 8-bit signed integers for simplicity, but the same principles apply for signed integers of any size.) &lt;/p&gt;
&lt;p&gt;Signed integers use their first bit (known as the &lt;em&gt;sign bit&lt;/em&gt;) to indicate whether the integer is positive or negative. A sign bit of &lt;code&gt;0&lt;/code&gt; means positive, and a sign bit of &lt;code&gt;1&lt;/code&gt; means negative. &lt;/p&gt;
&lt;p&gt;The remaining bits (known as the &lt;em&gt;value bits&lt;/em&gt;) store the actual value. Positive numbers are stored in exactly the same way as for unsigned integers, counting upwards from &lt;code&gt;0&lt;/code&gt;. Here’s how the bits inside an &lt;code&gt;Int8&lt;/code&gt; look for the number &lt;code&gt;4&lt;/code&gt;: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitshiftSignedFour_2x.png" src="http://muxuezi.github.io/posts/Art/bitshiftSignedFour_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The sign bit is &lt;code&gt;0&lt;/code&gt; (meaning “positive”), and the seven value bits are just the number &lt;code&gt;4&lt;/code&gt;, written in binary notation. &lt;/p&gt;
&lt;p&gt;Negative numbers, however, are stored differently. They are stored by subtracting their absolute value from &lt;code&gt;2&lt;/code&gt; to the power of &lt;code&gt;n&lt;/code&gt;, where &lt;code&gt;n&lt;/code&gt; is the number of value bits. An eight-bit number has seven value bits, so this means &lt;code&gt;2&lt;/code&gt; to the power of &lt;code&gt;7&lt;/code&gt;, or &lt;code&gt;128&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Here’s how the bits inside an &lt;code&gt;Int8&lt;/code&gt; look for the number &lt;code&gt;-4&lt;/code&gt;: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitshiftSignedMinusFour_2x.png" src="http://muxuezi.github.io/posts/Art/bitshiftSignedMinusFour_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;This time, the sign bit is &lt;code&gt;1&lt;/code&gt; (meaning “negative”), and the seven value bits have a binary value of &lt;code&gt;124&lt;/code&gt; (which is &lt;code&gt;128 - 4&lt;/code&gt;): &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitshiftSignedMinusFourValue_2x.png" src="http://muxuezi.github.io/posts/Art/bitshiftSignedMinusFourValue_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;The encoding for negative numbers is known as a &lt;em&gt;two’s complement&lt;/em&gt; representation. It may seem an unusual way to represent negative numbers, but it has several advantages. &lt;/p&gt;
&lt;p&gt;First, you can add &lt;code&gt;-1&lt;/code&gt; to &lt;code&gt;-4&lt;/code&gt;, simply by performing a standard binary addition of all eight bits (including the sign bit), and discarding anything that doesn’t fit in the eight bits once you’re done: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitshiftSignedAddition_2x.png" src="http://muxuezi.github.io/posts/Art/bitshiftSignedAddition_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Second, the two’s complement representation also lets you shift the bits of negative numbers to the left and right like positive numbers, and still end up doubling them for every shift you make to the left, or halving them for every shift you make to the right. To achieve this, an extra rule is used when signed integers are shifted to the right: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;When you shift signed integers to the right, apply the same rules as for unsigned integers, but fill any empty bits on the left with the &lt;em&gt;sign bit&lt;/em&gt;, rather than with a zero. &lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img alt="image: ../Art/bitshiftSigned_2x.png" src="http://muxuezi.github.io/posts/Art/bitshiftSigned_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;This action ensures that signed integers have the same sign after they are shifted to the right, and is known as an &lt;em&gt;arithmetic shift&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;Because of the special way that positive and negative numbers are stored, shifting either of them to the right moves them closer to zero. Keeping the sign bit the same during this shift means that negative integers remain negative as their value moves closer to zero. &lt;/p&gt;
&lt;h3&gt;Overflow Operators&lt;/h3&gt;
&lt;p&gt;If you try to insert a number into an integer constant or variable that cannot hold that value, by default Swift reports an error rather than allowing an invalid value to be created. This behavior gives extra safety when you work with numbers that are too large or too small. &lt;/p&gt;
&lt;p&gt;For example, the &lt;code&gt;Int16&lt;/code&gt; integer type can hold any signed integer number between &lt;code&gt;-32768&lt;/code&gt; and &lt;code&gt;32767&lt;/code&gt;. Trying to set an &lt;code&gt;Int16&lt;/code&gt; constant or variable to a number outside of this range causes an error: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var potentialOverflow = Int16.max&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// potentialOverflow equals 32767, which is the largest value an Int16 can hold&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;potentialOverflow += 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this causes an error&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Providing error handling when values get too large or too small gives you much more flexibility when coding for boundary value conditions. &lt;/p&gt;
&lt;p&gt;However, when you specifically want an overflow condition to truncate the number of available bits, you can opt in to this behavior rather than triggering an error. Swift provides five arithmetic &lt;em&gt;overflow operators&lt;/em&gt; that opt in to the overflow behavior for integer calculations. These operators all begin with an ampersand (&lt;code&gt;&amp;amp;&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Overflow addition (&lt;code&gt;&amp;amp;+&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Overflow subtraction (&lt;code&gt;&amp;amp;-&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Overflow multiplication (&lt;code&gt;&amp;amp;*&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Overflow division (&lt;code&gt;&amp;amp;/&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Overflow remainder (&lt;code&gt;&amp;amp;%&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Value Overflow&lt;/h4&gt;
&lt;p&gt;Here’s an example of what happens when an unsigned value is allowed to overflow, using the overflow addition operator (&lt;code&gt;&amp;amp;+&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var willOverflow = UInt8.max&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// willOverflow equals 255, which is the largest value a UInt8 can hold&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;willOverflow = willOverflow &amp;amp;\+ 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// willOverflow is now equal to 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The variable &lt;code&gt;willOverflow&lt;/code&gt; is initialized with the largest value a &lt;code&gt;UInt8&lt;/code&gt; can hold (&lt;code&gt;255&lt;/code&gt;, or &lt;code&gt;11111111&lt;/code&gt; in binary). It is then incremented by &lt;code&gt;1&lt;/code&gt; using the overflow addition operator (&lt;code&gt;&amp;amp;+&lt;/code&gt;). This pushes its binary representation just over the size that a &lt;code&gt;UInt8&lt;/code&gt; can hold, causing it to overflow beyond its bounds, as shown in the diagram below. The value that remains within the bounds of the &lt;code&gt;UInt8&lt;/code&gt; after the overflow addition is &lt;code&gt;00000000&lt;/code&gt;, or zero: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/overflowAddition_2x.png" src="http://muxuezi.github.io/posts/Art/overflowAddition_2x.png"&gt;&lt;/p&gt;
&lt;h4&gt;Value Underflow&lt;/h4&gt;
&lt;p&gt;Numbers can also become too small to fit in their type’s maximum bounds. Here’s an example. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;smallest&lt;/em&gt; value that a UInt8 can hold is &lt;code&gt;0&lt;/code&gt; (which is &lt;code&gt;00000000&lt;/code&gt; in eight-bit binary form). If you subtract &lt;code&gt;1&lt;/code&gt; from &lt;code&gt;00000000&lt;/code&gt; using the overflow subtraction operator, the number will overflow back round to &lt;code&gt;11111111&lt;/code&gt;, or &lt;code&gt;255&lt;/code&gt; in decimal: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/overflowUnsignedSubtraction_2x.png" src="http://muxuezi.github.io/posts/Art/overflowUnsignedSubtraction_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Here’s how that looks in Swift code: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var willUnderflow = UInt8.min&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// willUnderflow equals 0, which is the smallest value a UInt8 can hold&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;willUnderflow = willUnderflow &amp;amp;\- 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// willUnderflow is now equal to 255&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A similar underflow occurs for signed integers. All subtraction for signed integers is performed as straight binary subtraction, with the sign bit included as part of the numbers being subtracted, as described in Bitwise Left and Right Shift Operators. The smallest number that an &lt;code&gt;Int8&lt;/code&gt; can hold is &lt;code&gt;-128&lt;/code&gt;, which is &lt;code&gt;10000000&lt;/code&gt; in binary. Subtracting &lt;code&gt;1&lt;/code&gt; from this binary number with the overflow operator gives a binary value of &lt;code&gt;01111111&lt;/code&gt;, which toggles the sign bit and gives positive &lt;code&gt;127&lt;/code&gt;, the largest positive value that an &lt;code&gt;Int8&lt;/code&gt; can hold: &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/overflowSignedSubtraction_2x.png" src="http://muxuezi.github.io/posts/Art/overflowSignedSubtraction_2x.png"&gt;&lt;/p&gt;
&lt;p&gt;Here’s the same thing in Swift code: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var signedUnderflow = Int8.min&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// signedUnderflow equals -128, which is the smallest value an Int8 can hold&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;signedUnderflow = signedUnderflow &amp;amp;\- 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// signedUnderflow is now equal to 127&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The end result of the overflow and underflow behavior described above is that for both signed and unsigned integers, overflow always wraps around from the largest valid integer value back to the smallest, and underflow always wraps around from the smallest value to the largest. &lt;/p&gt;
&lt;h4&gt;Division by Zero&lt;/h4&gt;
&lt;p&gt;Dividing a number by zero (&lt;code&gt;i / 0&lt;/code&gt;), or trying to calculate remainder by zero (&lt;code&gt;i % 0&lt;/code&gt;), causes an error: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let x = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let y = x / 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;However, the overflow versions of these operators (&lt;code&gt;&amp;amp;/&lt;/code&gt; and &lt;code&gt;&amp;amp;%&lt;/code&gt;) return a value of zero if you divide by zero: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let x = 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let y = x &amp;amp;/ 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// y is equal to 0&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Precedence and Associativity&lt;/h3&gt;
&lt;p&gt;Operator &lt;em&gt;precedence&lt;/em&gt; gives some operators higher priority than others; these operators are applied first. &lt;/p&gt;
&lt;p&gt;Operator &lt;em&gt;associativity&lt;/em&gt; defines how operators of the same precedence are grouped together (or &lt;em&gt;associated&lt;/em&gt;)—either grouped from the left, or grouped from the right. Think of it as meaning “they associate with the expression to their left,” or “they associate with the expression to their right.” &lt;/p&gt;
&lt;p&gt;It is important to consider each operator’s precedence and associativity when working out the order in which a compound expression will be calculated. Here’s an example. Why does the following expression equal &lt;code&gt;4&lt;/code&gt;? &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;2 \+ 3 * 4 % 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// this equals 4&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Taken strictly from left to right, you might expect this to read as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;2 plus 3 equals 5; &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;5 times 4 equals 20; &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;20 remainder 5 equals 0 &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;However, the actual answer is &lt;code&gt;4&lt;/code&gt;, not &lt;code&gt;0&lt;/code&gt;. Higher-precedence operators are evaluated before lower-precedence ones. In Swift, as in C, the multiplication operator (&lt;code&gt;*&lt;/code&gt;) and the remainder operator (&lt;code&gt;%&lt;/code&gt;) have a higher precedence than the addition operator (&lt;code&gt;+&lt;/code&gt;). As a result, they are both evaluated before the addition is considered. &lt;/p&gt;
&lt;p&gt;However, multiplication and remainder have the &lt;em&gt;same&lt;/em&gt; precedence as each other. To work out the exact evaluation order to use, you also need to consider their associativity. Multiplication and remainder both associate with the expression to their left. Think of this as adding implicit parentheses around these parts of the expression, starting from their left: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;2 \+ ((3 * 4) % 5)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;(3 * 4)&lt;/code&gt; is &lt;code&gt;12&lt;/code&gt;, so this is equivalent to: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;2 \+ (12 % 5)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;(12 % 5)&lt;/code&gt; is &lt;code&gt;2&lt;/code&gt;, so this is equivalent to: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;2 \+ 2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This calculation yields the final answer of &lt;code&gt;4&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;For a complete list of Swift operator precedences and associativity rules, see Expressions. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;Swift’s operator precedences and associativity rules are simpler and more predictable than those found in C and Objective-C. However, this means that they are not the same as in C-based languages. Be careful to ensure that operator interactions still behave in the way you intend when porting existing code to Swift. &lt;/p&gt;
&lt;h3&gt;Operator Functions&lt;/h3&gt;
&lt;p&gt;Classes and structures can provide their own implementations of existing operators. This is known as &lt;em&gt;overloading&lt;/em&gt; the existing operators. &lt;/p&gt;
&lt;p&gt;The example below shows how to implement the arithmetic addition operator (&lt;code&gt;+&lt;/code&gt;) for a custom structure. The arithmetic addition operator is a &lt;em&gt;binary operator&lt;/em&gt; because it operates on two targets and is said to be &lt;em&gt;infix&lt;/em&gt; because it appears in between those two targets. &lt;/p&gt;
&lt;p&gt;The example defines a &lt;code&gt;Vector2D&lt;/code&gt; structure for a two-dimensional position vector &lt;code&gt;(x, y)&lt;/code&gt;, followed by a definition of an &lt;em&gt;operator function&lt;/em&gt; to add together instances of the &lt;code&gt;Vector2D&lt;/code&gt; structure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Vector2D {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func \+ (left: Vector2D, right: Vector2D) -&amp;gt; Vector2D {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Vector2D(x: left.x \+ right.x, y: left.y \+ right.y)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The operator function is defined as a global function with a function name that matches the operator to be overloaded (&lt;code&gt;+&lt;/code&gt;). Because the arithmetic addition operator is a binary operator, this operator function takes two input parameters of type &lt;code&gt;Vector2D&lt;/code&gt; and returns a single output value, also of type &lt;code&gt;Vector2D&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;In this implementation, the input parameters are named &lt;code&gt;left&lt;/code&gt; and &lt;code&gt;right&lt;/code&gt; to represent the &lt;code&gt;Vector2D&lt;/code&gt; instances that will be on the left side and right side of the &lt;code&gt;+&lt;/code&gt; operator. The function returns a new &lt;code&gt;Vector2D&lt;/code&gt; instance, whose &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; properties are initialized with the sum of the &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; properties from the two &lt;code&gt;Vector2D&lt;/code&gt; instances that are added together. &lt;/p&gt;
&lt;p&gt;The function is defined globally, rather than as a method on the &lt;code&gt;Vector2D&lt;/code&gt; structure, so that it can be used as an infix operator between existing &lt;code&gt;Vector2D&lt;/code&gt; instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let vector = Vector2D(x: 3.0, y: 1.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let anotherVector = Vector2D(x: 2.0, y: 4.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let combinedVector = vector \+ anotherVector&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// combinedVector is a Vector2D instance with values of (5.0, 5.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This example adds together the vectors &lt;code&gt;(3.0, 1.0)&lt;/code&gt; and &lt;code&gt;(2.0, 4.0)&lt;/code&gt; to make the vector &lt;code&gt;(5.0, 5.0)&lt;/code&gt;, as illustrated below. &lt;/p&gt;
&lt;p&gt;&lt;img alt="image: ../Art/vectorAddition_2x.png" src="http://muxuezi.github.io/posts/Art/vectorAddition_2x.png"&gt;&lt;/p&gt;
&lt;h4&gt;Prefix and Postfix Operators&lt;/h4&gt;
&lt;p&gt;The example shown above demonstrates a custom implementation of a binary infix operator. Classes and structures can also provide implementations of the standard &lt;em&gt;unary operators&lt;/em&gt;. Unary operators operate on a single target. They are &lt;em&gt;prefix&lt;/em&gt; if they precede their target (such as &lt;code&gt;-a&lt;/code&gt;) and &lt;em&gt;postfix&lt;/em&gt; operators if they follow their target (such as &lt;code&gt;i++&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;You implement a prefix or postfix unary operator by writing the &lt;code&gt;prefix&lt;/code&gt; or &lt;code&gt;postfix&lt;/code&gt; modifier before the &lt;code&gt;func&lt;/code&gt; keyword when declaring the operator function: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;prefix func \- (vector: Vector2D) -&amp;gt; Vector2D {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Vector2D(x: -vector.x, y: -vector.y)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example above implements the unary minus operator (&lt;code&gt;-a&lt;/code&gt;) for &lt;code&gt;Vector2D&lt;/code&gt; instances. The unary minus operator is a prefix operator, and so this function has to be qualified with the &lt;code&gt;prefix&lt;/code&gt; modifier. &lt;/p&gt;
&lt;p&gt;For simple numeric values, the unary minus operator converts positive numbers into their negative equivalent and vice versa. The corresponding implementation for &lt;code&gt;Vector2D&lt;/code&gt; instances performs this operation on both the &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; properties: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let positive = Vector2D(x: 3.0, y: 4.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let negative = -positive&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// negative is a Vector2D instance with values of (-3.0, -4.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let alsoPositive = -negative&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// alsoPositive is a Vector2D instance with values of (3.0, 4.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Compound Assignment Operators&lt;/h4&gt;
&lt;p&gt;&lt;em&gt;Compound assignment operators&lt;/em&gt; combine assignment (&lt;code&gt;=&lt;/code&gt;) with another operation. For example, the addition assignment operator (&lt;code&gt;+=&lt;/code&gt;) combines addition and assignment into a single operation. You mark a compound assignment operator’s left input parameter as &lt;code&gt;inout&lt;/code&gt;, because the parameter’s value will be modified directly from within the operator function. &lt;/p&gt;
&lt;p&gt;The example below implements an addition assignment operator function for &lt;code&gt;Vector2D&lt;/code&gt; instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func += (inout left: Vector2D, right: Vector2D) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;left = left \+ right&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because an addition operator was defined earlier, you don’t need to reimplement the addition process here. Instead, the addition assignment operator function takes advantage of the existing addition operator function, and uses it to set the left value to be the left value plus the right value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var original = Vector2D(x: 1.0, y: 2.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let vectorToAdd = Vector2D(x: 3.0, y: 4.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;original += vectorToAdd&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// original now has values of (4.0, 6.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can combine assignment with either the &lt;code&gt;prefix&lt;/code&gt; or &lt;code&gt;postfix&lt;/code&gt; modifier, as in this implementation of the prefix increment operator (&lt;code&gt;++a&lt;/code&gt;) for &lt;code&gt;Vector2D&lt;/code&gt; instances: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;prefix func ++ (inout vector: Vector2D) -&amp;gt; Vector2D {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;vector += Vector2D(x: 1.0, y: 1.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return vector&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The prefix increment operator function above takes advantage of the addition assignment operator defined earlier. It adds a &lt;code&gt;Vector2D&lt;/code&gt; with &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; values of &lt;code&gt;1.0&lt;/code&gt; to the &lt;code&gt;Vector2D&lt;/code&gt; on which it is called, and returns the result: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var toIncrement = Vector2D(x: 3.0, y: 4.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let afterIncrement = ++toIncrement&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// toIncrement now has values of (4.0, 5.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// afterIncrement also has values of (4.0, 5.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;It is not possible to overload the default assignment operator (&lt;code&gt;=&lt;/code&gt;). Only the compound assignment operators can be overloaded. Similarly, the ternary conditional operator (&lt;code&gt;a ? b : c&lt;/code&gt;) cannot be overloaded. &lt;/p&gt;
&lt;h4&gt;Equivalence Operators&lt;/h4&gt;
&lt;p&gt;Custom classes and structures do not receive a default implementation of the &lt;em&gt;equivalence operators&lt;/em&gt;, known as the “equal to” operator (&lt;code&gt;==&lt;/code&gt;) and “not equal to” operator (&lt;code&gt;!=&lt;/code&gt;). It is not possible for Swift to guess what would qualify as “equal” for your own custom types, because the meaning of “equal” depends on the roles that those types play in your code. &lt;/p&gt;
&lt;p&gt;To use the equivalence operators to check for equivalence of your own custom type, provide an implementation of the operators in the same way as for other infix operators: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func == (left: Vector2D, right: Vector2D) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return (left.x == right.x) &amp;amp;&amp;amp; (left.y == right.y)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func != (left: Vector2D, right: Vector2D) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return !(left == right)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The above example implements an “equal to” operator (&lt;code&gt;==&lt;/code&gt;) to check if two &lt;code&gt;Vector2D&lt;/code&gt; instances have equivalent values. In the context of &lt;code&gt;Vector2D&lt;/code&gt;, it makes sense to consider “equal” as meaning “both instances have the same &lt;code&gt;x&lt;/code&gt; values and &lt;code&gt;y&lt;/code&gt; values”, and so this is the logic used by the operator implementation. The example also implements the “not equal to” operator (&lt;code&gt;!=&lt;/code&gt;), which simply returns the inverse of the result of the “equal to” operator. &lt;/p&gt;
&lt;p&gt;You can now use these operators to check whether two &lt;code&gt;Vector2D&lt;/code&gt; instances are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let twoThree = Vector2D(x: 2.0, y: 3.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let anotherTwoThree = Vector2D(x: 2.0, y: 3.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if twoThree == anotherTwoThree {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("These two vectors are equivalent.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "These two vectors are equivalent."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Custom Operators&lt;/h3&gt;
&lt;p&gt;You can declare and implement your own &lt;em&gt;custom operators&lt;/em&gt; in addition to the standard operators provided by Swift. For a list of characters that can be used to define custom operators, see Operators. &lt;/p&gt;
&lt;p&gt;New operators are declared at a global level using the &lt;code&gt;operator&lt;/code&gt; keyword, and are marked with the &lt;code&gt;prefix&lt;/code&gt;, &lt;code&gt;infix&lt;/code&gt; or &lt;code&gt;postfix&lt;/code&gt; modifiers: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;prefix operator +++ {}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The example above defines a new prefix operator called &lt;code&gt;+++&lt;/code&gt;. This operator does not have an existing meaning in Swift, and so it is given its own custom meaning below in the specific context of working with &lt;code&gt;Vector2D&lt;/code&gt; instances. For the purposes of this example, &lt;code&gt;+++&lt;/code&gt; is treated as a new “prefix doubling incrementer” operator. It doubles the &lt;code&gt;x&lt;/code&gt; and &lt;code&gt;y&lt;/code&gt; values of a &lt;code&gt;Vector2D&lt;/code&gt; instance, by adding the vector to itself with the addition assignment operator defined earlier: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;prefix func +++ (inout vector: Vector2D) -&amp;gt; Vector2D {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;vector += vector&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return vector&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This implementation of &lt;code&gt;+++&lt;/code&gt; is very similar to the implementation of &lt;code&gt;++&lt;/code&gt; for &lt;code&gt;Vector2D&lt;/code&gt;, except that this operator function adds the vector to itself, rather than adding &lt;code&gt;Vector2D(1.0, 1.0)&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var toBeDoubled = Vector2D(x: 1.0, y: 4.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let afterDoubling = +++toBeDoubled&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// toBeDoubled now has values of (2.0, 8.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// afterDoubling also has values of (2.0, 8.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Precedence and Associativity for Custom Infix Operators&lt;/h4&gt;
&lt;p&gt;Custom &lt;code&gt;infix&lt;/code&gt; operators can also specify a &lt;em&gt;precedence&lt;/em&gt; and an &lt;em&gt;associativity&lt;/em&gt;. See Precedence and Associativity for an explanation of how these two characteristics affect an infix operator’s interaction with other infix operators. &lt;/p&gt;
&lt;p&gt;The possible values for &lt;code&gt;associativity&lt;/code&gt; are &lt;code&gt;left&lt;/code&gt;, &lt;code&gt;right&lt;/code&gt;, and &lt;code&gt;none&lt;/code&gt;. Left-associative operators associate to the left if written next to other left-associative operators of the same precedence. Similarly, right-associative operators associate to the right if written next to other right-associative operators of the same precedence. Non-associative operators cannot be written next to other operators with the same precedence. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;associativity&lt;/code&gt; value defaults to &lt;code&gt;none&lt;/code&gt; if it is not specified. The &lt;code&gt;precedence&lt;/code&gt; value defaults to &lt;code&gt;100&lt;/code&gt; if it is not specified. &lt;/p&gt;
&lt;p&gt;The following example defines a new custom &lt;code&gt;infix&lt;/code&gt; operator called &lt;code&gt;+-&lt;/code&gt;, with &lt;code&gt;left&lt;/code&gt; associativity and a precedence of &lt;code&gt;140&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;infix operator +- { associativity left precedence 140 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func +- (left: Vector2D, right: Vector2D) -&amp;gt; Vector2D {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return Vector2D(x: left.x \+ right.x, y: left.y \- right.y)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let firstVector = Vector2D(x: 1.0, y: 2.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let secondVector = Vector2D(x: 3.0, y: 4.0)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let plusMinusVector = firstVector +- secondVector&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// plusMinusVector is a Vector2D instance with values of (4.0, -2.0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This operator adds together the &lt;code&gt;x&lt;/code&gt; values of two vectors, and subtracts the &lt;code&gt;y&lt;/code&gt; value of the second vector from the first. Because it is in essence an “additive” operator, it has been given the same associativity and precedence values (&lt;code&gt;left&lt;/code&gt; and &lt;code&gt;140&lt;/code&gt;) as default additive infix operators such as &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt;. For a complete list of the default Swift operator precedence and associativity settings, see Expressions. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You do not specify a precedence when defining a prefix or postfix operator. However, if you apply both a prefix and a postfix operator to the same operand, the postfix operator is applied first. &lt;/p&gt;
&lt;h2&gt;About the Language Reference&lt;/h2&gt;
&lt;p&gt;This part of the book describes the formal grammar of the Swift programming language. The grammar described here is intended to help you understand the language in more detail, rather than to allow you to directly implement a parser or compiler. &lt;/p&gt;
&lt;p&gt;The Swift language is relatively small, because many common types, functions, and operators that appear virtually everywhere in Swift code are actually defined in the Swift standard library. Although these types, functions, and operators are not part of the Swift language itself, they are used extensively in the discussions and code examples in this part of the book. &lt;/p&gt;
&lt;h4&gt;How to Read the Grammar&lt;/h4&gt;
&lt;p&gt;The notation used to describe the formal grammar of the Swift programming language follows a few conventions: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;An arrow (→) is used to mark grammar productions and can be read as “can consist of.” &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Syntactic categories are indicated by &lt;em&gt;italic&lt;/em&gt; text and appear on both sides of a grammar production rule. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Literal words and punctuation are indicated by boldface &lt;code&gt;constant width&lt;/code&gt; text and appear only on the right-hand side of a grammar production rule. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Alternative grammar productions are separated by vertical bars (|). When alternative productions are too long to read easily, they are broken into multiple grammar production rules on new lines. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;In a few cases, regular font text is used to describe the right-hand side of a grammar production rule. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Optional syntactic categories and literals are marked by a trailing subscript, &lt;em&gt;opt&lt;/em&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;As an example, the grammar of a getter-setter block is defined as follows: &lt;/p&gt;
&lt;p&gt;Grammar of a getter-setter block &lt;/p&gt;
&lt;p&gt;getter-setter-block  →  &lt;code&gt;{­&lt;/code&gt;getter-clause­setter-clause­opt­&lt;code&gt;}­&lt;/code&gt; &lt;code&gt;{­&lt;/code&gt;setter-clause­getter-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;This definition indicates that a getter-setter block can consist of a getter clause followed by an optional setter clause, enclosed in braces, &lt;em&gt;or&lt;/em&gt; a setter clause followed by a getter clause, enclosed in braces. The grammar production above is equivalent to the following two productions, where the alternatives are spelled out explicitly: &lt;/p&gt;
&lt;p&gt;Grammar of a getter-setter block &lt;/p&gt;
&lt;p&gt;getter-setter-block  → &lt;code&gt;{­&lt;/code&gt;getter-clause­setter-clause­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-setter-block  → &lt;code&gt;{­&lt;/code&gt;setter-clause­getter-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Lexical Structure&lt;/h2&gt;
&lt;p&gt;The &lt;em&gt;lexical structure&lt;/em&gt; of Swift describes what sequence of characters form valid tokens of the language. These valid tokens form the lowest-level building blocks of the language and are used to describe the rest of the language in subsequent chapters. A token consists of an identifier, keyword, punctuation, literal, or operator. &lt;/p&gt;
&lt;p&gt;In most cases, tokens are generated from the characters of a Swift source file by considering the longest possible substring from the input text, within the constraints of the grammar that are specified below. This behavior is referred to as &lt;em&gt;longest match&lt;/em&gt; or &lt;em&gt;maximal munch&lt;/em&gt;. &lt;/p&gt;
&lt;h3&gt;Whitespace and Comments&lt;/h3&gt;
&lt;p&gt;Whitespace has two uses: to separate tokens in the source file and to help determine whether an operator is a prefix or postfix (see Operators), but is otherwise ignored. The following characters are considered whitespace: space (U+0020), line feed (U+000A), carriage return (U+000D), horizontal tab (U+0009), vertical tab (U+000B), form feed (U+000C) and null (U+0000). &lt;/p&gt;
&lt;p&gt;Comments are treated as whitespace by the compiler. Single line comments begin with &lt;code&gt;//&lt;/code&gt; and continue until a carriage return (U+000D) or line feed (U+000A). Multiline comments begin with &lt;code&gt;/*&lt;/code&gt; and end with &lt;code&gt;*/&lt;/code&gt;. Nesting multiline comments is allowed, but the comment markers must be balanced. &lt;/p&gt;
&lt;h3&gt;Identifiers&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Identifiers&lt;/em&gt; begin with an uppercase or lowercase letter A through Z, an underscore (&lt;code&gt;_&lt;/code&gt;), a noncombining alphanumeric Unicode character in the Basic Multilingual Plane, or a character outside the Basic Multilingual Plane that isn’t in a Private Use Area. After the first character, digits and combining Unicode characters are also allowed. &lt;/p&gt;
&lt;p&gt;To use a reserved word as an identifier, put a backtick (&lt;code&gt;`) before and after it. For example, `class` is not a valid identifier, but&lt;/code&gt;class&lt;code&gt;is valid. The backticks are not considered part of the identifier;&lt;/code&gt;x&lt;code&gt;` and&lt;/code&gt;x` have the same meaning. &lt;/p&gt;
&lt;p&gt;Inside a closure with no explicit parameter names, the parameters are implicitly named &lt;code&gt;$0&lt;/code&gt;, &lt;code&gt;$1&lt;/code&gt;, &lt;code&gt;$2&lt;/code&gt;, and so on. These names are valid identifiers within the scope of the closure. &lt;/p&gt;
&lt;p&gt;Grammar of an identifier &lt;/p&gt;
&lt;p&gt;identifier  → identifier-characters­opt­&lt;/p&gt;
&lt;p&gt;identifier  → &lt;code&gt;­`identifier-characters­opt­&lt;/code&gt;­`&lt;/p&gt;
&lt;p&gt;identifier  → implicit-parameter-name­&lt;/p&gt;
&lt;p&gt;identifier-list  →  identifier­ identifier-list­&lt;/p&gt;
&lt;p&gt;identifier-head  → Upper- or lowercase letter A through Z&lt;/p&gt;
&lt;p&gt;identifier-head  → &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;identifier-head  → U+00A8, U+00AA, U+00AD, U+00AF, U+00B2–U+00B5, or U+00B7–U+00BA&lt;/p&gt;
&lt;p&gt;identifier-head  → U+00BC–U+00BE, U+00C0–U+00D6, U+00D8–U+00F6, or U+00F8–U+00FF&lt;/p&gt;
&lt;p&gt;identifier-head  → U+0100–U+02FF, U+0370–U+167F, U+1681–U+180D, or U+180F–U+1DBF&lt;/p&gt;
&lt;p&gt;identifier-head  → U+1E00–U+1FFF&lt;/p&gt;
&lt;p&gt;identifier-head  → U+200B–U+200D, U+202A–U+202E, U+203F–U+2040, U+2054, or U+2060–U+206F&lt;/p&gt;
&lt;p&gt;identifier-head  → U+2070–U+20CF, U+2100–U+218F, U+2460–U+24FF, or U+2776–U+2793&lt;/p&gt;
&lt;p&gt;identifier-head  → U+2C00–U+2DFF or U+2E80–U+2FFF&lt;/p&gt;
&lt;p&gt;identifier-head  → U+3004–U+3007, U+3021–U+302F, U+3031–U+303F, or U+3040–U+D7FF&lt;/p&gt;
&lt;p&gt;identifier-head  → U+F900–U+FD3D, U+FD40–U+FDCF, U+FDF0–U+FE1F, or U+FE30–U+FE44&lt;/p&gt;
&lt;p&gt;identifier-head  → U+FE47–U+FFFD&lt;/p&gt;
&lt;p&gt;identifier-head  → U+10000–U+1FFFD, U+20000–U+2FFFD, U+30000–U+3FFFD, or U+40000–U+4FFFD&lt;/p&gt;
&lt;p&gt;identifier-head  → U+50000–U+5FFFD, U+60000–U+6FFFD, U+70000–U+7FFFD, or U+80000–U+8FFFD&lt;/p&gt;
&lt;p&gt;identifier-head  → U+90000–U+9FFFD, U+A0000–U+AFFFD, U+B0000–U+BFFFD, or U+C0000–U+CFFFD&lt;/p&gt;
&lt;p&gt;identifier-head  → U+D0000–U+DFFFD or U+E0000–U+EFFFD&lt;/p&gt;
&lt;p&gt;identifier-character  → Digit 0 through 9&lt;/p&gt;
&lt;p&gt;identifier-character  → U+0300–U+036F, U+1DC0–U+1DFF, U+20D0–U+20FF, or U+FE20–U+FE2F&lt;/p&gt;
&lt;p&gt;identifier-character  → identifier-head­&lt;/p&gt;
&lt;p&gt;identifier-characters  → identifier-characters­opt­&lt;/p&gt;
&lt;p&gt;implicit-parameter-name  → &lt;code&gt;$­&lt;/code&gt;decimal-digits­&lt;/p&gt;
&lt;h3&gt;Keywords and Punctuation&lt;/h3&gt;
&lt;p&gt;The following keywords are reserved and can’t be used as identifiers, unless they’re escaped with backticks, as described above in Identifiers. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Keywords used in declarations: &lt;code&gt;class&lt;/code&gt;, &lt;code&gt;deinit&lt;/code&gt;, &lt;code&gt;enum&lt;/code&gt;, &lt;code&gt;extension&lt;/code&gt;, &lt;code&gt;func&lt;/code&gt;, &lt;code&gt;import&lt;/code&gt;, &lt;code&gt;init&lt;/code&gt;, &lt;code&gt;internal&lt;/code&gt;, &lt;code&gt;let&lt;/code&gt;, &lt;code&gt;operator&lt;/code&gt;, &lt;code&gt;private&lt;/code&gt;, &lt;code&gt;protocol&lt;/code&gt;, &lt;code&gt;public&lt;/code&gt;, &lt;code&gt;static&lt;/code&gt;, &lt;code&gt;struct&lt;/code&gt;, &lt;code&gt;subscript&lt;/code&gt;, &lt;code&gt;typealias&lt;/code&gt;, and &lt;code&gt;var&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Keywords used in statements: &lt;code&gt;break&lt;/code&gt;, &lt;code&gt;case&lt;/code&gt;, &lt;code&gt;continue&lt;/code&gt;, &lt;code&gt;default&lt;/code&gt;, &lt;code&gt;do&lt;/code&gt;, &lt;code&gt;else&lt;/code&gt;, &lt;code&gt;fallthrough&lt;/code&gt;, &lt;code&gt;for&lt;/code&gt;, &lt;code&gt;if&lt;/code&gt;, &lt;code&gt;in&lt;/code&gt;, &lt;code&gt;return&lt;/code&gt;, &lt;code&gt;switch&lt;/code&gt;, &lt;code&gt;where&lt;/code&gt;, and &lt;code&gt;while&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Keywords used in expressions and types: &lt;code&gt;as&lt;/code&gt;, &lt;code&gt;dynamicType&lt;/code&gt;, &lt;code&gt;false&lt;/code&gt;, &lt;code&gt;is&lt;/code&gt;, &lt;code&gt;nil&lt;/code&gt;, &lt;code&gt;self&lt;/code&gt;, &lt;code&gt;Self&lt;/code&gt;, &lt;code&gt;super&lt;/code&gt;, &lt;code&gt;true&lt;/code&gt;, &lt;code&gt;__COLUMN__&lt;/code&gt;, &lt;code&gt;__FILE__&lt;/code&gt;, &lt;code&gt;__FUNCTION__&lt;/code&gt;, and &lt;code&gt;__LINE__&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Keywords reserved in particular contexts: &lt;code&gt;associativity&lt;/code&gt;, &lt;code&gt;convenience&lt;/code&gt;, &lt;code&gt;dynamic&lt;/code&gt;, &lt;code&gt;didSet&lt;/code&gt;, &lt;code&gt;final&lt;/code&gt;, &lt;code&gt;get&lt;/code&gt;, &lt;code&gt;infix&lt;/code&gt;, &lt;code&gt;inout&lt;/code&gt;, &lt;code&gt;lazy&lt;/code&gt;, &lt;code&gt;left&lt;/code&gt;, &lt;code&gt;mutating&lt;/code&gt;, &lt;code&gt;none&lt;/code&gt;, &lt;code&gt;nonmutating&lt;/code&gt;, &lt;code&gt;optional&lt;/code&gt;, &lt;code&gt;override&lt;/code&gt;, &lt;code&gt;postfix&lt;/code&gt;, &lt;code&gt;precedence&lt;/code&gt;, &lt;code&gt;prefix&lt;/code&gt;, &lt;code&gt;Protocol&lt;/code&gt;, &lt;code&gt;required&lt;/code&gt;, &lt;code&gt;right&lt;/code&gt;, &lt;code&gt;set&lt;/code&gt;, &lt;code&gt;Type&lt;/code&gt;, &lt;code&gt;unowned&lt;/code&gt;, &lt;code&gt;weak&lt;/code&gt;, and &lt;code&gt;willSet&lt;/code&gt;. Outside the context in which they appear in the grammar, they can be used as identifiers. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The following tokens are reserved as punctuation and can’t be used as custom operators: &lt;code&gt;(&lt;/code&gt;, &lt;code&gt;)&lt;/code&gt;, &lt;code&gt;{&lt;/code&gt;, &lt;code&gt;}&lt;/code&gt;, &lt;code&gt;[&lt;/code&gt;, &lt;code&gt;]&lt;/code&gt;, &lt;code&gt;.&lt;/code&gt;, &lt;code&gt;,&lt;/code&gt;, &lt;code&gt;:&lt;/code&gt;, &lt;code&gt;;&lt;/code&gt;, &lt;code&gt;=&lt;/code&gt;, &lt;code&gt;@&lt;/code&gt;, &lt;code&gt;#&lt;/code&gt;, &lt;code&gt;&amp;amp;&lt;/code&gt; (as a prefix operator), &lt;code&gt;-&amp;gt;&lt;/code&gt;, &lt;code&gt;``,&lt;/code&gt;?&lt;code&gt;, and&lt;/code&gt;!` (as a postfix operator). &lt;/p&gt;
&lt;h3&gt;Literals&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;literal&lt;/em&gt; is the source code representation of a value of a type, such as a number or string. &lt;/p&gt;
&lt;p&gt;The following are examples of literals: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;42 // Integer literal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;3.14159 // Floating-point literal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"Hello, world!" // String literal&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;true // Boolean literal&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A literal doesn’t have a type on its own. Instead, a literal is parsed as having infinite precision and Swift’s type inference attempts to infer a type for the literal. For example, in the declaration &lt;code&gt;let x: Int8 = 42&lt;/code&gt;, Swift uses the explicit type annotation (&lt;code&gt;: Int8&lt;/code&gt;) to infer that the type of the integer literal &lt;code&gt;42&lt;/code&gt; is &lt;code&gt;Int8&lt;/code&gt;. If there isn’t suitable type information available, Swift infers that the literal’s type is one of the default literal types defined in the Swift standard library. The default types are &lt;code&gt;Int&lt;/code&gt; for integer literals, &lt;code&gt;Double&lt;/code&gt; for floating-point literals, &lt;code&gt;String&lt;/code&gt; for string literals, and &lt;code&gt;Bool&lt;/code&gt; for Boolean literals. For example, in the declaration &lt;code&gt;let str = "Hello, world"&lt;/code&gt;, the default inferred type of the string literal &lt;code&gt;"Hello, world"&lt;/code&gt; is &lt;code&gt;String&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;When specifying the type annotation for a literal value, the annotation’s type must be a type that can be instantiated from that literal value. That is, the type must conform to one of the following Swift standard library protocols: &lt;code&gt;IntegerLiteralConvertible&lt;/code&gt; for integer literals, &lt;code&gt;FloatingPointLiteralConvertible&lt;/code&gt; for floating-point literals, &lt;code&gt;StringLiteralConvertible&lt;/code&gt; for string literals, and &lt;code&gt;BooleanLiteralConvertible&lt;/code&gt; for Boolean literals. For example, &lt;code&gt;Int8&lt;/code&gt; conforms to the &lt;code&gt;IntegerLiteralConvertible&lt;/code&gt; protocol, and therefore it can be used in the type annotation for the integer literal &lt;code&gt;42&lt;/code&gt; in the declaration &lt;code&gt;let x: Int8 = 42&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Grammar of a literal &lt;/p&gt;
&lt;p&gt;literal  →  numeric-literal­ string-literal­ boolean-literal­ nil-literal­&lt;/p&gt;
&lt;p&gt;numeric-literal  →  &lt;code&gt;-­&lt;/code&gt;opt­integer-literal­ &lt;code&gt;-­&lt;/code&gt;opt­floating-point-literal­&lt;/p&gt;
&lt;p&gt;boolean-literal  →  &lt;code&gt;true­&lt;/code&gt; &lt;code&gt;false­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;nil-literal  → &lt;code&gt;nil­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Integer Literals&lt;/h4&gt;
&lt;p&gt;&lt;em&gt;Integer literals&lt;/em&gt; represent integer values of unspecified precision. By default, integer literals are expressed in decimal; you can specify an alternate base using a prefix. Binary literals begin with &lt;code&gt;0b&lt;/code&gt;, octal literals begin with &lt;code&gt;0o&lt;/code&gt;, and hexadecimal literals begin with &lt;code&gt;0x&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Decimal literals contain the digits &lt;code&gt;0&lt;/code&gt; through &lt;code&gt;9&lt;/code&gt;. Binary literals contain &lt;code&gt;0&lt;/code&gt; and &lt;code&gt;1&lt;/code&gt;, octal literals contain &lt;code&gt;0&lt;/code&gt; through &lt;code&gt;7&lt;/code&gt;, and hexadecimal literals contain &lt;code&gt;0&lt;/code&gt; through &lt;code&gt;9&lt;/code&gt; as well as &lt;code&gt;A&lt;/code&gt; through &lt;code&gt;F&lt;/code&gt; in upper- or lowercase. &lt;/p&gt;
&lt;p&gt;Negative integers literals are expressed by prepending a minus sign (&lt;code&gt;-&lt;/code&gt;) to an integer literal, as in &lt;code&gt;-42&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Underscores (&lt;code&gt;_&lt;/code&gt;) are allowed between digits for readability, but they are ignored and therefore don’t affect the value of the literal. Integer literals can begin with leading zeros (&lt;code&gt;0&lt;/code&gt;), but they are likewise ignored and don’t affect the base or value of the literal. &lt;/p&gt;
&lt;p&gt;Unless otherwise specified, the default inferred type of an integer literal is the Swift standard library type &lt;code&gt;Int&lt;/code&gt;. The Swift standard library also defines types for various sizes of signed and unsigned integers, as described in Integers. &lt;/p&gt;
&lt;p&gt;Grammar of an integer literal &lt;/p&gt;
&lt;p&gt;integer-literal  → binary-literal­&lt;/p&gt;
&lt;p&gt;integer-literal  → octal-literal­&lt;/p&gt;
&lt;p&gt;integer-literal  → decimal-literal­&lt;/p&gt;
&lt;p&gt;integer-literal  → hexadecimal-literal­&lt;/p&gt;
&lt;p&gt;binary-literal  → &lt;code&gt;0b­&lt;/code&gt;binary-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;binary-digit  → Digit 0 or 1&lt;/p&gt;
&lt;p&gt;binary-literal-character  →  binary-digit­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;binary-literal-characters  → binary-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;octal-literal  → &lt;code&gt;0o­&lt;/code&gt;octal-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;octal-digit  → Digit 0 through 7&lt;/p&gt;
&lt;p&gt;octal-literal-character  →  octal-digit­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;octal-literal-characters  → octal-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;decimal-literal  → decimal-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;decimal-digit  → Digit 0 through 9&lt;/p&gt;
&lt;p&gt;decimal-digits  → decimal-digits­opt­&lt;/p&gt;
&lt;p&gt;decimal-literal-character  →  decimal-digit­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;decimal-literal-characters  → decimal-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;hexadecimal-literal  → &lt;code&gt;0x­&lt;/code&gt;hexadecimal-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;hexadecimal-digit  → Digit 0 through 9, a through f, or A through F&lt;/p&gt;
&lt;p&gt;hexadecimal-literal-character  →  hexadecimal-digit­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;hexadecimal-literal-characters  → hexadecimal-literal-characters­opt­&lt;/p&gt;
&lt;h4&gt;Floating-Point Literals&lt;/h4&gt;
&lt;p&gt;&lt;em&gt;Floating-point literals&lt;/em&gt; represent floating-point values of unspecified precision. &lt;/p&gt;
&lt;p&gt;By default, floating-point literals are expressed in decimal (with no prefix), but they can also be expressed in hexadecimal (with a &lt;code&gt;0x&lt;/code&gt; prefix). &lt;/p&gt;
&lt;p&gt;Decimal floating-point literals consist of a sequence of decimal digits followed by either a decimal fraction, a decimal exponent, or both. The decimal fraction consists of a decimal point (&lt;code&gt;.&lt;/code&gt;) followed by a sequence of decimal digits. The exponent consists of an upper- or lowercase &lt;code&gt;e&lt;/code&gt; prefix followed by a sequence of decimal digits that indicates what power of 10 the value preceding the &lt;code&gt;e&lt;/code&gt; is multiplied by. For example, &lt;code&gt;1.25e2&lt;/code&gt; represents 1.25 × 102, which evaluates to &lt;code&gt;125.0&lt;/code&gt;. Similarly, &lt;code&gt;1.25e-2&lt;/code&gt; represents 1.25 × 10-2, which evaluates to &lt;code&gt;0.0125&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Hexadecimal floating-point literals consist of a &lt;code&gt;0x&lt;/code&gt; prefix, followed by an optional hexadecimal fraction, followed by a hexadecimal exponent. The hexadecimal fraction consists of a decimal point followed by a sequence of hexadecimal digits. The exponent consists of an upper- or lowercase &lt;code&gt;p&lt;/code&gt; prefix followed by a sequence of decimal digits that indicates what power of 2 the value preceding the &lt;code&gt;p&lt;/code&gt; is multiplied by. For example, &lt;code&gt;0xFp2&lt;/code&gt; represents 15 × 22, which evaluates to &lt;code&gt;60&lt;/code&gt;. Similarly, &lt;code&gt;0xFp-2&lt;/code&gt; represents 15 × 2-2, which evaluates to &lt;code&gt;3.75&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Negative floating-point literals are expressed by prepending a minus sign (&lt;code&gt;-&lt;/code&gt;) to a floating-point literal, as in &lt;code&gt;-42.5&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Underscores (&lt;code&gt;_&lt;/code&gt;) are allowed between digits for readability, but are ignored and therefore don’t affect the value of the literal. Floating-point literals can begin with leading zeros (&lt;code&gt;0&lt;/code&gt;), but are likewise ignored and don’t affect the base or value of the literal. &lt;/p&gt;
&lt;p&gt;Unless otherwise specified, the default inferred type of a floating-point literal is the Swift standard library type &lt;code&gt;Double&lt;/code&gt;, which represents a 64-bit floating-point number. The Swift standard library also defines a &lt;code&gt;Float&lt;/code&gt; type, which represents a 32-bit floating-point number. &lt;/p&gt;
&lt;p&gt;Grammar of a floating-point literal &lt;/p&gt;
&lt;p&gt;floating-point-literal  → decimal-fraction­opt­decimal-exponent­opt­&lt;/p&gt;
&lt;p&gt;floating-point-literal  → hexadecimal-fraction­opt­hexadecimal-exponent­&lt;/p&gt;
&lt;p&gt;decimal-fraction  → &lt;code&gt;.­&lt;/code&gt;decimal-literal­&lt;/p&gt;
&lt;p&gt;decimal-exponent  → sign­opt­decimal-literal­&lt;/p&gt;
&lt;p&gt;hexadecimal-fraction  → &lt;code&gt;.­&lt;/code&gt;hexadecimal-literal-characters­opt­&lt;/p&gt;
&lt;p&gt;hexadecimal-exponent  → sign­opt­decimal-literal­&lt;/p&gt;
&lt;p&gt;floating-point-e  →  &lt;code&gt;e­&lt;/code&gt; &lt;code&gt;E­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;floating-point-p  →  &lt;code&gt;p­&lt;/code&gt; &lt;code&gt;P­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;sign  →  &lt;code&gt;+­&lt;/code&gt; &lt;code&gt;-­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;String Literals&lt;/h4&gt;
&lt;p&gt;A string literal is a sequence of characters surrounded by double quotes, with the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;"&lt;em&gt;characters&lt;/em&gt;"&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;String literals cannot contain an unescaped double quote (&lt;code&gt;"&lt;/code&gt;), an unescaped backslash (&lt;code&gt;\&lt;/code&gt;), a carriage return, or a line feed. &lt;/p&gt;
&lt;p&gt;Special characters can be included in string literals using the following escape sequences: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Null Character (&lt;code&gt;\0&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Backslash (&lt;code&gt;\\\&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Horizontal Tab (&lt;code&gt;\t&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Line Feed (&lt;code&gt;\n&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Carriage Return (&lt;code&gt;\r&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Double Quote (&lt;code&gt;\"&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Single Quote (&lt;code&gt;\'&lt;/code&gt;) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Unicode scalar (&lt;code&gt;\u{&lt;/code&gt;&lt;em&gt;n&lt;/em&gt;&lt;code&gt;}&lt;/code&gt;), where &lt;em&gt;n&lt;/em&gt; is between one and eight hexadecimal digits &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The value of an expression can be inserted into a string literal by placing the expression in parentheses after a backslash (&lt;code&gt;\&lt;/code&gt;). The interpolated expression must not contain an unescaped double quote (&lt;code&gt;"&lt;/code&gt;), an unescaped backslash (&lt;code&gt;\&lt;/code&gt;), a carriage return, or a line feed. The expression must evaluate to a value of a type that the &lt;code&gt;String&lt;/code&gt; class has an initializer for. &lt;/p&gt;
&lt;p&gt;For example, all the following string literals have the same value: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;"1 2 3"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"1 2 \\(3)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;"1 2 \\(1 \+ 2)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let x = 3; "1 2 \\(x)"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The default inferred type of a string literal is &lt;code&gt;String&lt;/code&gt;. The default inferred type of the characters that make up a string is &lt;code&gt;Character&lt;/code&gt;. For more information about the &lt;code&gt;String&lt;/code&gt; and &lt;code&gt;Character&lt;/code&gt; types, see Strings and Characters. &lt;/p&gt;
&lt;p&gt;Grammar of a string literal &lt;/p&gt;
&lt;p&gt;string-literal  → &lt;code&gt;"­&lt;/code&gt;quoted-text­opt­&lt;code&gt;"­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;quoted-text  → quoted-text­opt­&lt;/p&gt;
&lt;p&gt;quoted-text-item  → escaped-character­&lt;/p&gt;
&lt;p&gt;quoted-text-item  → &lt;code&gt;\\(­&lt;/code&gt;expression­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;quoted-text-item  → Any Unicode extended grapheme cluster except &lt;code&gt;"­&lt;/code&gt;, &lt;code&gt;\­&lt;/code&gt;, U+000A, or U+000D&lt;/p&gt;
&lt;p&gt;escaped-character  →  &lt;code&gt;\0­&lt;/code&gt; &lt;code&gt;\\\­&lt;/code&gt; &lt;code&gt;\t­&lt;/code&gt; &lt;code&gt;\n­&lt;/code&gt; &lt;code&gt;\r­&lt;/code&gt; &lt;code&gt;\"­&lt;/code&gt; &lt;code&gt;\'­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;escaped-character  → &lt;code&gt;\u­``{­&lt;/code&gt;unicode-scalar-digits­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;unicode-scalar-digits  → Between one and eight hexadecimal digits&lt;/p&gt;
&lt;h3&gt;Operators&lt;/h3&gt;
&lt;p&gt;The Swift standard library defines a number of operators for your use, many of which are discussed in Basic Operators and Advanced Operators. The present section describes which characters can be used to define custom operators. &lt;/p&gt;
&lt;p&gt;Custom operators can begin with one of the ASCII characters &lt;code&gt;/&lt;/code&gt;, &lt;code&gt;=&lt;/code&gt;, &lt;code&gt;-&lt;/code&gt;, &lt;code&gt;+&lt;/code&gt;, &lt;code&gt;!&lt;/code&gt;, &lt;code&gt;*&lt;/code&gt;, &lt;code&gt;%&lt;/code&gt;, &lt;code&gt;&amp;lt;&lt;/code&gt;, &lt;code&gt;&amp;gt;&lt;/code&gt;, &lt;code&gt;&amp;amp;&lt;/code&gt;, &lt;code&gt;|&lt;/code&gt;, &lt;code&gt;^&lt;/code&gt;, &lt;code&gt;?&lt;/code&gt;, or &lt;code&gt;~&lt;/code&gt;, or one of the Unicode characters defined in the grammar below. After the first character, combining Unicode characters are also allowed. You can also define custom operators as a sequence of two or more dots (for example, &lt;code&gt;....&lt;/code&gt;). Although you can define custom operators that contain a question mark character (&lt;code&gt;?&lt;/code&gt;), they can’t consist of a single question mark character only. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;The tokens &lt;code&gt;=&lt;/code&gt;, &lt;code&gt;-&amp;gt;&lt;/code&gt;, &lt;code&gt;//&lt;/code&gt;, &lt;code&gt;/*&lt;/code&gt;, &lt;code&gt;*/&lt;/code&gt;, &lt;code&gt;.&lt;/code&gt;, the prefix operators &lt;code&gt;&amp;lt;&lt;/code&gt;, &lt;code&gt;&amp;amp;&lt;/code&gt;, and &lt;code&gt;?&lt;/code&gt;, the infix operator &lt;code&gt;?&lt;/code&gt;, and the postfix operators &lt;code&gt;&amp;gt;&lt;/code&gt;, &lt;code&gt;!&lt;/code&gt;, and &lt;code&gt;?&lt;/code&gt; are reserved. These tokens can’t be overloaded, nor can they be used as custom operators. &lt;/p&gt;
&lt;p&gt;The whitespace around an operator is used to determine whether an operator is used as a prefix operator, a postfix operator, or a binary operator. This behavior is summarized in the following rules: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If an operator has whitespace around both sides or around neither side, it is treated as a binary operator. As an example, the &lt;code&gt;+&lt;/code&gt; operator in &lt;code&gt;a+b&lt;/code&gt; and &lt;code&gt;a + b&lt;/code&gt; is treated as a binary operator. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If an operator has whitespace on the left side only, it is treated as a prefix unary operator. As an example, the &lt;code&gt;++&lt;/code&gt; operator in &lt;code&gt;a ++b&lt;/code&gt; is treated as a prefix unary operator. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If an operator has whitespace on the right side only, it is treated as a postfix unary operator. As an example, the &lt;code&gt;++&lt;/code&gt; operator in &lt;code&gt;a++ b&lt;/code&gt; is treated as a postfix unary operator. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If an operator has no whitespace on the left but is followed immediately by a dot (&lt;code&gt;.&lt;/code&gt;), it is treated as a postfix unary operator. As an example, the &lt;code&gt;++&lt;/code&gt; operator in &lt;code&gt;a++.b&lt;/code&gt; is treated as a postfix unary operator (&lt;code&gt;a++ .b&lt;/code&gt; rather than &lt;code&gt;a ++ .b&lt;/code&gt;). &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For the purposes of these rules, the characters &lt;code&gt;(&lt;/code&gt;, &lt;code&gt;[&lt;/code&gt;, and &lt;code&gt;{&lt;/code&gt; before an operator, the characters &lt;code&gt;)&lt;/code&gt;, &lt;code&gt;]&lt;/code&gt;, and &lt;code&gt;}&lt;/code&gt; after an operator, and the characters &lt;code&gt;,&lt;/code&gt;, &lt;code&gt;;&lt;/code&gt;, and &lt;code&gt;:&lt;/code&gt; are also considered whitespace. &lt;/p&gt;
&lt;p&gt;There is one caveat to the rules above. If the &lt;code&gt;!&lt;/code&gt; or &lt;code&gt;?&lt;/code&gt; predefined operator has no whitespace on the left, it is treated as a postfix operator, regardless of whether it has whitespace on the right. To use the &lt;code&gt;?&lt;/code&gt; as the optional-chaining operator, it must not have whitespace on the left. To use it in the ternary conditional (&lt;code&gt;?&lt;/code&gt; &lt;code&gt;:&lt;/code&gt;) operator, it must have whitespace around both sides. &lt;/p&gt;
&lt;p&gt;In certain constructs, operators with a leading &lt;code&gt;&amp;lt;&lt;/code&gt; or &lt;code&gt;&amp;gt;&lt;/code&gt; may be split into two or more tokens. The remainder is treated the same way and may be split again. As a result, there is no need to use whitespace to disambiguate between the closing &lt;code&gt;&amp;gt;&lt;/code&gt; characters in constructs like &lt;code&gt;Dictionary&amp;lt;String, Array&amp;lt;Int&amp;gt;&amp;gt;&lt;/code&gt;. In this example, the closing &lt;code&gt;&amp;gt;&lt;/code&gt; characters are not treated as a single token that may then be misinterpreted as a bit shift &lt;code&gt;&amp;gt;&amp;gt;&lt;/code&gt; operator. &lt;/p&gt;
&lt;p&gt;To learn how to define new, custom operators, see Custom Operators and Operator Declaration. To learn how to overload existing operators, see Operator Functions. &lt;/p&gt;
&lt;p&gt;Grammar of operators &lt;/p&gt;
&lt;p&gt;operator  → operator-characters­opt­&lt;/p&gt;
&lt;p&gt;operator  → dot-operator-characters­opt­&lt;/p&gt;
&lt;p&gt;operator-head  →  &lt;code&gt;/­&lt;/code&gt; &lt;code&gt;=­&lt;/code&gt; &lt;code&gt;-­&lt;/code&gt; &lt;code&gt;+­&lt;/code&gt; &lt;code&gt;!­&lt;/code&gt; &lt;code&gt;*­&lt;/code&gt; &lt;code&gt;%­&lt;/code&gt; &lt;code&gt;&amp;lt;­&lt;/code&gt; &lt;code&gt;&amp;gt;­&lt;/code&gt; &lt;code&gt;&amp;amp;­&lt;/code&gt; &lt;code&gt;|­&lt;/code&gt; &lt;code&gt;^­&lt;/code&gt; &lt;code&gt;~­&lt;/code&gt; &lt;code&gt;?­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;operator-head  → U+00A1–U+00A7&lt;/p&gt;
&lt;p&gt;operator-head  → U+00A9 or U+00AB&lt;/p&gt;
&lt;p&gt;operator-head  → U+00AC or U+00AE&lt;/p&gt;
&lt;p&gt;operator-head  → U+00B0–U+00B1, U+00B6, U+00BB, U+00BF, U+00D7, or U+00F7&lt;/p&gt;
&lt;p&gt;operator-head  → U+2016–U+2017 or U+2020–U+2027&lt;/p&gt;
&lt;p&gt;operator-head  → U+2030–U+203E&lt;/p&gt;
&lt;p&gt;operator-head  → U+2041–U+2053&lt;/p&gt;
&lt;p&gt;operator-head  → U+2055–U+205E&lt;/p&gt;
&lt;p&gt;operator-head  → U+2190–U+23FF&lt;/p&gt;
&lt;p&gt;operator-head  → U+2500–U+2775&lt;/p&gt;
&lt;p&gt;operator-head  → U+2794–U+2BFF&lt;/p&gt;
&lt;p&gt;operator-head  → U+2E00–U+2E7F&lt;/p&gt;
&lt;p&gt;operator-head  → U+3001–U+3003&lt;/p&gt;
&lt;p&gt;operator-head  → U+3008–U+3030&lt;/p&gt;
&lt;p&gt;operator-character  → operator-head­&lt;/p&gt;
&lt;p&gt;operator-character  → U+0300–U+036F&lt;/p&gt;
&lt;p&gt;operator-character  → U+1DC0–U+1DFF&lt;/p&gt;
&lt;p&gt;operator-character  → U+20D0–U+20FF&lt;/p&gt;
&lt;p&gt;operator-character  → U+FE00–U+FE0F&lt;/p&gt;
&lt;p&gt;operator-character  → U+FE20–U+FE2F&lt;/p&gt;
&lt;p&gt;operator-character  → U+E0100–U+E01EF&lt;/p&gt;
&lt;p&gt;operator-characters  → operator-characters­opt­&lt;/p&gt;
&lt;p&gt;dot-operator-head  → &lt;code&gt;..­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;dot-operator-character  →  &lt;code&gt;.­&lt;/code&gt; operator-character­&lt;/p&gt;
&lt;p&gt;dot-operator-characters  → dot-operator-characters­opt­&lt;/p&gt;
&lt;p&gt;binary-operator  → operator­&lt;/p&gt;
&lt;p&gt;prefix-operator  → operator­&lt;/p&gt;
&lt;p&gt;postfix-operator  → operator­&lt;/p&gt;
&lt;h2&gt;Types&lt;/h2&gt;
&lt;p&gt;In Swift, there are two kinds of types: named types and compound types. A &lt;em&gt;named type&lt;/em&gt; is a type that can be given a particular name when it is defined. Named types include classes, structures, enumerations, and protocols. For example, instances of a user-defined class named &lt;code&gt;MyClass&lt;/code&gt; have the type &lt;code&gt;MyClass&lt;/code&gt;. In addition to user-defined named types, the Swift standard library defines many commonly used named types, including those that represent arrays, dictionaries, and optional values. &lt;/p&gt;
&lt;p&gt;Data types that are normally considered basic or primitive in other languages—such as types that represent numbers, characters, and strings—are actually named types, defined and implemented in the Swift standard library using structures. Because they are named types, you can extend their behavior to suit the needs of your program, using an extension declaration, discussed in Extensions and Extension Declaration. &lt;/p&gt;
&lt;p&gt;A &lt;em&gt;compound type&lt;/em&gt; is a type without a name, defined in the Swift language itself. There are two compound types: function types and tuple types. A compound type may contain named types and other compound types. For instance, the tuple type &lt;code&gt;(Int, (Int, Int))&lt;/code&gt; contains two elements: The first is the named type &lt;code&gt;Int&lt;/code&gt;, and the second is another compound type &lt;code&gt;(Int, Int)&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;This chapter discusses the types defined in the Swift language itself and describes the type inference behavior of Swift. &lt;/p&gt;
&lt;p&gt;Grammar of a type &lt;/p&gt;
&lt;p&gt;type  →  array-type­ dictionary-type­ function-type­ type-identifier­ tuple-type­ optional-type­ implicitly-unwrapped-optional-type­ protocol-composition-type­ metatype-type­&lt;/p&gt;
&lt;h3&gt;Type Annotation&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;type annotation&lt;/em&gt; explicitly specifies the type of a variable or expression. Type annotations begin with a colon (&lt;code&gt;:&lt;/code&gt;) and end with a type, as the following examples show: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someTuple: (Double, Double) = (3.14159, 2.71828)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func someFunction(a: Int) { /* ... */ }&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the first example, the expression &lt;code&gt;someTuple&lt;/code&gt; is specified to have the tuple type &lt;code&gt;(Double, Double)&lt;/code&gt;. In the second example, the parameter &lt;code&gt;a&lt;/code&gt; to the function &lt;code&gt;someFunction&lt;/code&gt; is specified to have the type &lt;code&gt;Int&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Type annotations can contain an optional list of type attributes before the type. &lt;/p&gt;
&lt;p&gt;Grammar of a type annotation &lt;/p&gt;
&lt;p&gt;type-annotation  → &lt;code&gt;:­&lt;/code&gt;attributes­opt­type­&lt;/p&gt;
&lt;h3&gt;Type Identifier&lt;/h3&gt;
&lt;p&gt;A type identifier refers to either a named type or a type alias of a named or compound type. &lt;/p&gt;
&lt;p&gt;Most of the time, a type identifier directly refers to a named type with the same name as the identifier. For example, &lt;code&gt;Int&lt;/code&gt; is a type identifier that directly refers to the named type &lt;code&gt;Int&lt;/code&gt;, and the type identifier &lt;code&gt;Dictionary&amp;lt;String, Int&amp;gt;&lt;/code&gt; directly refers to the named type &lt;code&gt;Dictionary&amp;lt;String, Int&amp;gt;&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;There are two cases in which a type identifier does not refer to a type with the same name. In the first case, a type identifier refers to a type alias of a named or compound type. For instance, in the example below, the use of &lt;code&gt;Point&lt;/code&gt; in the type annotation refers to the tuple type &lt;code&gt;(Int, Int)&lt;/code&gt;. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;typealias Point = (Int, Int)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let origin: Point = (0, 0)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the second case, a type identifier uses dot (&lt;code&gt;.&lt;/code&gt;) syntax to refer to named types declared in other modules or nested within other types. For example, the type identifier in the following code references the named type &lt;code&gt;MyType&lt;/code&gt; that is declared in the &lt;code&gt;ExampleModule&lt;/code&gt; module. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var someValue: ExampleModule.MyType&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a type identifier &lt;/p&gt;
&lt;p&gt;type-identifier  →  generic-argument-clause­opt­ generic-argument-clause­opt­&lt;code&gt;.­&lt;/code&gt;type-identifier­&lt;/p&gt;
&lt;p&gt;type-name  → identifier­&lt;/p&gt;
&lt;h3&gt;Tuple Type&lt;/h3&gt;
&lt;p&gt;A tuple type is a comma-separated list of zero or more types, enclosed in parentheses. &lt;/p&gt;
&lt;p&gt;You can use a tuple type as the return type of a function to enable the function to return a single tuple containing multiple values. You can also name the elements of a tuple type and use those names to refer to the values of the individual elements. An element name consists of an identifier followed immediately by a colon (:). For an example that demonstrates both of these features, see Functions with Multiple Return Values. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;Void&lt;/code&gt; is a typealias for the empty tuple type, &lt;code&gt;()&lt;/code&gt;. If there is only one element inside the parentheses, the type is simply the type of that element. For example, the type of &lt;code&gt;(Int)&lt;/code&gt; is &lt;code&gt;Int&lt;/code&gt;, not &lt;code&gt;(Int)&lt;/code&gt;. As a result, you can name a tuple element only when the tuple type has two or more elements. &lt;/p&gt;
&lt;p&gt;Grammar of a tuple type &lt;/p&gt;
&lt;p&gt;tuple-type  → &lt;code&gt;(­&lt;/code&gt;tuple-type-body­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;tuple-type-body  → tuple-type-element-list­&lt;code&gt;...­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;tuple-type-element-list  →  tuple-type-element­ tuple-type-element-list­&lt;/p&gt;
&lt;p&gt;tuple-type-element  →  attributes­opt­&lt;code&gt;inout­&lt;/code&gt;opt­type­ &lt;code&gt;inout­&lt;/code&gt;opt­type-annotation­&lt;/p&gt;
&lt;p&gt;element-name  → identifier­&lt;/p&gt;
&lt;h3&gt;Function Type&lt;/h3&gt;
&lt;p&gt;A function type represents the type of a function, method, or closure and consists of a parameter and return type separated by an arrow (&lt;code&gt;-&amp;gt;&lt;/code&gt;): &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;parameter type&lt;/em&gt; -&amp;gt; &lt;em&gt;return type&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because the &lt;em&gt;parameter type&lt;/em&gt; and the &lt;em&gt;return type&lt;/em&gt; can be a tuple type, function types support functions and methods that take multiple parameters and return multiple values. &lt;/p&gt;
&lt;p&gt;You can apply the &lt;code&gt;autoclosure&lt;/code&gt; attribute to a function type that has a parameter type of &lt;code&gt;()&lt;/code&gt; and that returns the type of an expression (see Type Attributes). An autoclosure function captures an implicit closure over the specified expression, instead of the expression itself. The following example uses the &lt;code&gt;autoclosure&lt;/code&gt; attribute in defining a very simple assert function: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func simpleAssert(condition: @autoclosure () -&amp;gt; Bool, message: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if !condition() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(message)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let testNumber = 5&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;simpleAssert(testNumber % 2 == 0, "testNumber isn't an even number.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "testNumber isn't an even number."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A function type can have a variadic parameter as the last parameter in its &lt;em&gt;parameter type&lt;/em&gt;. Syntactically, a variadic parameter consists of a base type name followed immediately by three dots (&lt;code&gt;...&lt;/code&gt;), as in &lt;code&gt;Int...&lt;/code&gt;. A variadic parameter is treated as an array that contains elements of the base type name. For instance, the variadic parameter &lt;code&gt;Int...&lt;/code&gt; is treated as &lt;code&gt;[Int]&lt;/code&gt;. For an example that uses a variadic parameter, see Variadic Parameters. &lt;/p&gt;
&lt;p&gt;To specify an in-out parameter, prefix the parameter type with the &lt;code&gt;inout&lt;/code&gt; keyword. You can’t mark a variadic parameter or a return type with the &lt;code&gt;inout&lt;/code&gt; keyword. In-out parameters are discussed in In-Out Parameters. &lt;/p&gt;
&lt;p&gt;The function types of a curried function are grouped from right to left. For instance, the function type &lt;code&gt;Int -&amp;gt; Int -&amp;gt; Int&lt;/code&gt; is understood as &lt;code&gt;Int -&amp;gt; (Int -&amp;gt; Int)&lt;/code&gt;—that is, a function that takes an &lt;code&gt;Int&lt;/code&gt; and returns another function that takes and return an &lt;code&gt;Int&lt;/code&gt;. Curried function are described in Curried Functions. &lt;/p&gt;
&lt;p&gt;Grammar of a function type &lt;/p&gt;
&lt;p&gt;function-type  → type­&lt;/p&gt;
&lt;h3&gt;Array Type&lt;/h3&gt;
&lt;p&gt;The Swift language provides the following syntactic sugar for the Swift standard library &lt;code&gt;Array&amp;lt;T&amp;gt;&lt;/code&gt; type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;[&lt;em&gt;type&lt;/em&gt;]&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In other words, the following two declarations are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someArray: Array&amp;lt;String&amp;gt; = ["Alex", "Brian", "Dave"]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let someArray: [String] = ["Alex", "Brian", "Dave"]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In both cases, the constant &lt;code&gt;someArray&lt;/code&gt; is declared as an array of strings. The elements of an array can be accessed through subscripting by specifying a valid index value in square brackets: &lt;code&gt;someArray[0]&lt;/code&gt; refers to the element at index 0, &lt;code&gt;"Alex"&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can create multidimensional arrays by nesting pairs of square brackets, where the name of the base type of the elements is contained in the innermost pair of square brackets. For example, you can create a three-dimensional array of integers using three sets of square brackets: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var array3D: [[[Int]]] = [[[1, 2], [3, 4]], [[5, 6], [7, 8]]]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When accessing the elements in a multidimensional array, the left-most subscript index refers to the element at that index in the outermost array. The next subscript index to the right refers to the element at that index in the array that’s nested one level in. And so on. This means that in the example above, &lt;code&gt;array3D[0]&lt;/code&gt; refers to &lt;code&gt;[[1, 2], [3, 4]]&lt;/code&gt;, &lt;code&gt;array3D[0][1]&lt;/code&gt; refers to &lt;code&gt;[3, 4]&lt;/code&gt;, and &lt;code&gt;array3D[0][1][1]&lt;/code&gt; refers to the value 4. &lt;/p&gt;
&lt;p&gt;For a detailed discussion of the Swift standard library &lt;code&gt;Array&lt;/code&gt; type, see Arrays. &lt;/p&gt;
&lt;p&gt;Grammar of an array type &lt;/p&gt;
&lt;p&gt;array-type  → &lt;code&gt;[­&lt;/code&gt;type­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Dictionary Type&lt;/h3&gt;
&lt;p&gt;The Swift language provides the following syntactic sugar for the Swift standard library &lt;code&gt;Dictionary&amp;lt;Key, Value&amp;gt;&lt;/code&gt; type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;[&lt;em&gt;key type&lt;/em&gt;: &lt;em&gt;value type&lt;/em&gt;]&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In other words, the following two declarations are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someDictionary: [String: Int] = ["Alex": 31, "Paul": 39]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let someDictionary: Dictionary&amp;lt;String, Int&amp;gt; = ["Alex": 31, "Paul": 39]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In both cases, the constant &lt;code&gt;someDictionary&lt;/code&gt; is declared as a dictionary with strings as keys and integers as values. &lt;/p&gt;
&lt;p&gt;The values of a dictionary can be accessed through subscripting by specifying the corresponding key in square brackets: &lt;code&gt;someDictionary["Alex"]&lt;/code&gt; refers to the value associated with the key &lt;code&gt;"Alex"&lt;/code&gt;. The subscript returns an optional value of the dictionary’s value type. If the specified key isn’t contained in the dictionary, the subscript returns &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The key type of a dictionary must conform to the Swift standard library &lt;code&gt;Hashable&lt;/code&gt; protocol, as described in Hash Values for Dictionary Key Types. &lt;/p&gt;
&lt;p&gt;For a detailed discussion of the Swift standard library &lt;code&gt;Dictionary&lt;/code&gt; type, see Dictionaries. &lt;/p&gt;
&lt;p&gt;Grammar of a dictionary type &lt;/p&gt;
&lt;p&gt;dictionary-type  → &lt;code&gt;[­&lt;/code&gt;type­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Optional Type&lt;/h3&gt;
&lt;p&gt;The Swift language defines the postfix &lt;code&gt;?&lt;/code&gt; as syntactic sugar for the named type &lt;code&gt;Optional&amp;lt;T&amp;gt;&lt;/code&gt;, which is defined in the Swift standard library. In other words, the following two declarations are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var optionalInteger: Int?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var optionalInteger: Optional&amp;lt;Int&amp;gt;&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In both cases, the variable &lt;code&gt;optionalInteger&lt;/code&gt; is declared to have the type of an optional integer. Note that no whitespace may appear between the type and the &lt;code&gt;?&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The type &lt;code&gt;Optional&amp;lt;T&amp;gt;&lt;/code&gt; is an enumeration with two cases, &lt;code&gt;None&lt;/code&gt; and &lt;code&gt;Some(T)&lt;/code&gt;, which are used to represent values that may or may not be present. Any type can be explicitly declared to be (or implicitly converted to) an optional type. If you don’t provide an initial value when you declare an optional variable or property, its value automatically defaults to &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;If an instance of an optional type contains a value, you can access that value using the postfix operator &lt;code&gt;!&lt;/code&gt;, as shown below: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;optionalInteger = 42&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;optionalInteger! // 42&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Using the &lt;code&gt;!&lt;/code&gt; operator to unwrap an optional that has a value of &lt;code&gt;nil&lt;/code&gt; results in a runtime error. &lt;/p&gt;
&lt;p&gt;You can also use optional chaining and optional binding to conditionally perform an operation on an optional expression. If the value is &lt;code&gt;nil&lt;/code&gt;, no operation is performed and therefore no runtime error is produced. &lt;/p&gt;
&lt;p&gt;For more information and to see examples that show how to use optional types, see Optionals. &lt;/p&gt;
&lt;p&gt;Grammar of an optional type &lt;/p&gt;
&lt;p&gt;optional-type  → type­&lt;code&gt;?­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Implicitly Unwrapped Optional Type&lt;/h3&gt;
&lt;p&gt;The Swift language defines the postfix &lt;code&gt;!&lt;/code&gt; as syntactic sugar for the named type &lt;code&gt;ImplicitlyUnwrappedOptional&amp;lt;T&amp;gt;&lt;/code&gt;, which is defined in the Swift standard library. In other words, the following two declarations are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var implicitlyUnwrappedString: String!&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var implicitlyUnwrappedString: String!&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In both cases, the variable &lt;code&gt;implicitlyUnwrappedString&lt;/code&gt; is declared to have the type of an implicitly unwrapped optional string. Note that no whitespace may appear between the type and the &lt;code&gt;!&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can use implicitly unwrapped optionals in all the same places in your code that you can use optionals. For instance, you can assign values of implicitly unwrapped optionals to variables, constants, and properties of optionals, and vice versa. &lt;/p&gt;
&lt;p&gt;As with optionals, if you don’t provide an initial value when you declare an implicitly unwrapped optional variable or property, its value automatically defaults to &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Because the value of an implicitly unwrapped optional is automatically unwrapped when you use it, there’s no need to use the &lt;code&gt;!&lt;/code&gt; operator to unwrap it. That said, if you try to use an implicitly unwrapped optional that has a value of &lt;code&gt;nil&lt;/code&gt;, you’ll get a runtime error. &lt;/p&gt;
&lt;p&gt;Use optional chaining to conditionally perform an operation on an implicitly unwrapped optional expression. If the value is &lt;code&gt;nil&lt;/code&gt;, no operation is performed and therefore no runtime error is produced. &lt;/p&gt;
&lt;p&gt;For more information about implicitly unwrapped optional types, see Implicitly Unwrapped Optionals. &lt;/p&gt;
&lt;p&gt;Grammar of an implicitly unwrapped optional type &lt;/p&gt;
&lt;p&gt;implicitly-unwrapped-optional-type  → type­&lt;code&gt;!­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Protocol Composition Type&lt;/h3&gt;
&lt;p&gt;A protocol composition type describes a type that conforms to each protocol in a list of specified protocols. Protocol composition types may be used in type annotations and in generic parameters. &lt;/p&gt;
&lt;p&gt;Protocol composition types have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;protocol&amp;lt;&lt;em&gt;Protocol 1&lt;/em&gt;, &lt;em&gt;Protocol 2&lt;/em&gt;&amp;gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A protocol composition type allows you to specify a value whose type conforms to the requirements of multiple protocols without having to explicitly define a new, named protocol that inherits from each protocol you want the type to conform to. For example, specifying a protocol composition type &lt;code&gt;protocol&amp;lt;ProtocolA, ProtocolB, ProtocolC&amp;gt;&lt;/code&gt; is effectively the same as defining a new protocol &lt;code&gt;ProtocolD&lt;/code&gt; that inherits from &lt;code&gt;ProtocolA&lt;/code&gt;, &lt;code&gt;ProtocolB&lt;/code&gt;, and &lt;code&gt;ProtocolC&lt;/code&gt;, but without having to introduce a new name. &lt;/p&gt;
&lt;p&gt;Each item in a protocol composition list must be either the name of protocol or a type alias of a protocol composition type. If the list is empty, it specifies the empty protocol composition type, which every type conforms to. &lt;/p&gt;
&lt;p&gt;Grammar of a protocol composition type &lt;/p&gt;
&lt;p&gt;protocol-composition-type  → &lt;code&gt;protocol­``&amp;lt;­&lt;/code&gt;protocol-identifier-list­opt­&lt;code&gt;&amp;gt;­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;protocol-identifier-list  →  protocol-identifier­ protocol-identifier-list­&lt;/p&gt;
&lt;p&gt;protocol-identifier  → type-identifier­&lt;/p&gt;
&lt;h3&gt;Metatype Type&lt;/h3&gt;
&lt;p&gt;A metatype type refers to the type of any type, including class types, structure types, enumeration types, and protocol types. &lt;/p&gt;
&lt;p&gt;The metatype of a class, structure, or enumeration type is the name of that type followed by &lt;code&gt;.Type&lt;/code&gt;. The metatype of a protocol type—not the concrete type that conforms to the protocol at runtime—is the name of that protocol followed by &lt;code&gt;.Protocol&lt;/code&gt;. For example, the metatype of the class type &lt;code&gt;SomeClass&lt;/code&gt; is &lt;code&gt;SomeClass.Type&lt;/code&gt; and the metatype of the protocol &lt;code&gt;SomeProtocol&lt;/code&gt; is &lt;code&gt;SomeProtocol.Protocol&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can use the postfix &lt;code&gt;self&lt;/code&gt; expression to access a type as a value. For example, &lt;code&gt;SomeClass.self&lt;/code&gt; returns &lt;code&gt;SomeClass&lt;/code&gt; itself, not an instance of &lt;code&gt;SomeClass&lt;/code&gt;. And &lt;code&gt;SomeProtocol.self&lt;/code&gt; returns &lt;code&gt;SomeProtocol&lt;/code&gt; itself, not an instance of a type that conforms to &lt;code&gt;SomeProtocol&lt;/code&gt; at runtime. You can use a &lt;code&gt;dynamicType&lt;/code&gt; expression with an instance of a type to access that instance’s runtime type as a value, as the following example shows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeBaseClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class func printClassName() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("SomeBaseClass")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class SomeSubClass: SomeBaseClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override class func printClassName() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("SomeSubClass")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let someInstance: SomeBaseClass = SomeSubClass()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someInstance is of type SomeBaseClass at compile time, but&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someInstance is of type SomeSubClass at runtime&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someInstance.dynamicType.printClassName()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "SomeSubClass"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a metatype type &lt;/p&gt;
&lt;p&gt;metatype-type  →  type­&lt;code&gt;.­``Type­&lt;/code&gt; type­&lt;code&gt;.­``Protocol­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Type Inheritance Clause&lt;/h3&gt;
&lt;p&gt;A type inheritance clause is used to specify which class a named type inherits from and which protocols a named type conforms to. A type inheritance clause is also used to specify a &lt;code&gt;class&lt;/code&gt; requirement on a protocol. A type inheritance clause begins with a colon (&lt;code&gt;:&lt;/code&gt;), followed by either a &lt;code&gt;class&lt;/code&gt; requirement, a list of type identifiers, or both. &lt;/p&gt;
&lt;p&gt;Class types can inherit from a single superclass and conform to any number of protocols. When defining a class, the name of the superclass must appear first in the list of type identifiers, followed by any number of protocols the class must conform to. If the class does not inherit from another class, the list can begin with a protocol instead. For an extended discussion and several examples of class inheritance, see Inheritance. &lt;/p&gt;
&lt;p&gt;Other named types can only inherit from or conform to a list of protocols. Protocol types can inherit from any number of other protocols. When a protocol type inherits from other protocols, the set of requirements from those other protocols are aggregated together, and any type that inherits from the current protocol must conform to all of those requirements. As discussed in Protocol Declaration, you can include the &lt;code&gt;class&lt;/code&gt; keyword as the first item in the type inheritance clause to mark a protocol declaration with a &lt;code&gt;class&lt;/code&gt; requirement. &lt;/p&gt;
&lt;p&gt;A type inheritance clause in an enumeration definition can be either a list of protocols, or in the case of an enumeration that assigns raw values to its cases, a single, named type that specifies the type of those raw values. For an example of an enumeration definition that uses a type inheritance clause to specify the type of its raw values, see Raw Values. &lt;/p&gt;
&lt;p&gt;Grammar of a type inheritance clause &lt;/p&gt;
&lt;p&gt;type-inheritance-clause  → &lt;code&gt;:­&lt;/code&gt;type-inheritance-list­&lt;/p&gt;
&lt;p&gt;type-inheritance-clause  → &lt;code&gt;:­&lt;/code&gt;class-requirement­&lt;/p&gt;
&lt;p&gt;type-inheritance-clause  → &lt;code&gt;:­&lt;/code&gt;type-inheritance-list­&lt;/p&gt;
&lt;p&gt;type-inheritance-list  →  type-identifier­ type-inheritance-list­&lt;/p&gt;
&lt;p&gt;class-requirement  → &lt;code&gt;class­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Type Inference&lt;/h3&gt;
&lt;p&gt;Swift uses type inference extensively, allowing you to omit the type or part of the type of many variables and expressions in your code. For example, instead of writing &lt;code&gt;var x: Int = 0&lt;/code&gt;, you can write &lt;code&gt;var x = 0&lt;/code&gt;, omitting the type completely—the compiler correctly infers that &lt;code&gt;x&lt;/code&gt; names a value of type &lt;code&gt;Int&lt;/code&gt;. Similarly, you can omit part of a type when the full type can be inferred from context. For instance, if you write &lt;code&gt;let dict: Dictionary = ["A": 1]&lt;/code&gt;, the compiler infers that &lt;code&gt;dict&lt;/code&gt; has the type &lt;code&gt;Dictionary&amp;lt;String, Int&amp;gt;&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;In both of the examples above, the type information is passed up from the leaves of the expression tree to its root. That is, the type of &lt;code&gt;x&lt;/code&gt; in &lt;code&gt;var x: Int = 0&lt;/code&gt; is inferred by first checking the type of &lt;code&gt;0&lt;/code&gt; and then passing this type information up to the root (the variable &lt;code&gt;x&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;In Swift, type information can also flow in the opposite direction—from the root down to the leaves. In the following example, for instance, the explicit type annotation (&lt;code&gt;: Float&lt;/code&gt;) on the constant &lt;code&gt;eFloat&lt;/code&gt; causes the numeric literal &lt;code&gt;2.71828&lt;/code&gt; to have an inferred type of &lt;code&gt;Float&lt;/code&gt; instead of &lt;code&gt;Double&lt;/code&gt;. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let e = 2.71828 // The type of e is inferred to be Double.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let eFloat: Float = 2.71828 // The type of eFloat is Float.&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Type inference in Swift operates at the level of a single expression or statement. This means that all of the information needed to infer an omitted type or part of a type in an expression must be accessible from type-checking the expression or one of its subexpressions. &lt;/p&gt;
&lt;h2&gt;Expressions&lt;/h2&gt;
&lt;p&gt;In Swift, there are four kinds of expressions: prefix expressions, binary expressions, primary expressions, and postfix expressions. Evaluating an expression returns a value, causes a side effect, or both. &lt;/p&gt;
&lt;p&gt;Prefix and binary expressions let you apply operators to smaller expressions. Primary expressions are conceptually the simplest kind of expression, and they provide a way to access values. Postfix expressions, like prefix and binary expressions, let you build up more complex expressions using postfixes such as function calls and member access. Each kind of expression is described in detail in the sections below. &lt;/p&gt;
&lt;p&gt;Grammar of an expression &lt;/p&gt;
&lt;p&gt;expression  → binary-expressions­opt­&lt;/p&gt;
&lt;p&gt;expression-list  →  expression­ expression-list­&lt;/p&gt;
&lt;h3&gt;Prefix Expressions&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Prefix expressions&lt;/em&gt; combine an optional prefix operator with an expression. Prefix operators take one argument, the expression that follows them. &lt;/p&gt;
&lt;p&gt;The Swift standard library provides the following prefix operators: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;++&lt;/code&gt; Increment &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;\--&lt;/code&gt; Decrement &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;!&lt;/code&gt; Logical NOT &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;~&lt;/code&gt; Bitwise NOT &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;+&lt;/code&gt; Unary plus &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;-&lt;/code&gt; Unary minus &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For information about the behavior of these operators, see Basic Operators and Advanced Operators. &lt;/p&gt;
&lt;p&gt;In addition to the standard library operators listed above, you use &lt;code&gt;&amp;amp;&lt;/code&gt; immediately before the name of a variable that’s being passed as an in-out argument to a function call expression. For more information and to see an example, see In-Out Parameters. &lt;/p&gt;
&lt;p&gt;Grammar of a prefix expression &lt;/p&gt;
&lt;p&gt;prefix-expression  → prefix-operator­opt­postfix-expression­&lt;/p&gt;
&lt;p&gt;prefix-expression  → in-out-expression­&lt;/p&gt;
&lt;p&gt;in-out-expression  → &lt;code&gt;&amp;amp;­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;h3&gt;Binary Expressions&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Binary expressions&lt;/em&gt; combine an infix binary operator with the expression that it takes as its left-hand and right-hand arguments. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;left-hand argument&lt;/em&gt; &lt;em&gt;operator&lt;/em&gt; &lt;em&gt;right-hand argument&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The Swift standard library provides the following binary operators: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Exponentiative (No associativity, precedence level 160) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;lt;&amp;lt;&lt;/code&gt; Bitwise left shift &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;gt;&amp;gt;&lt;/code&gt; Bitwise right shift &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Multiplicative (Left associative, precedence level 150) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;*&lt;/code&gt; Multiply &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;/&lt;/code&gt; Divide &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;%&lt;/code&gt; Remainder &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;*&lt;/code&gt; Multiply, ignoring overflow &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;/&lt;/code&gt; Divide, ignoring overflow &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;%&lt;/code&gt; Remainder, ignoring overflow &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;&lt;/code&gt; Bitwise AND &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Additive (Left associative, precedence level 140) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;+&lt;/code&gt; Add &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;-&lt;/code&gt; Subtract &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;+&lt;/code&gt; Add with overflow &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;-&lt;/code&gt; Subtract with overflow &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;|&lt;/code&gt; Bitwise OR &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;^&lt;/code&gt; Bitwise XOR &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Range (No associativity, precedence level 135) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;..&amp;lt;&lt;/code&gt; Half-open range &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;...&lt;/code&gt; Closed range &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Cast (No associativity, precedence level 132) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;is&lt;/code&gt; Type check &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;as&lt;/code&gt; Type cast &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Comparative (No associativity, precedence level 130) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;lt;&lt;/code&gt; Less than &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;lt;=&lt;/code&gt; Less than or equal &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;gt;&lt;/code&gt; Greater than &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;gt;=&lt;/code&gt; Greater than or equal &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;==&lt;/code&gt; Equal &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;!=&lt;/code&gt; Not equal &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;===&lt;/code&gt; Identical &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;!==&lt;/code&gt; Not identical &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;~=&lt;/code&gt; Pattern match &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Conjunctive (Left associative, precedence level 120) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;&amp;amp;&amp;amp;&lt;/code&gt; Logical AND &lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Disjunctive (Left associative, precedence level 110) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;||&lt;/code&gt; Logical OR &lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Nil Coalescing (Right associative, precedence level 110) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;??&lt;/code&gt; Nil coalescing &lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Ternary Conditional (Right associative, precedence level 100) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;?``:&lt;/code&gt; Ternary conditional &lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Assignment (Right associative, precedence level 90) &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;=&lt;/code&gt; Assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;*=&lt;/code&gt; Multiply and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;/=&lt;/code&gt; Divide and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;%=&lt;/code&gt; Remainder and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;+=&lt;/code&gt; Add and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;-=&lt;/code&gt; Subtract and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;lt;&amp;lt;=&lt;/code&gt; Left bit shift and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;gt;&amp;gt;=&lt;/code&gt; Right bit shift and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;=&lt;/code&gt; Bitwise AND and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;^=&lt;/code&gt; Bitwise XOR and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;|=&lt;/code&gt; Bitwise OR and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;&amp;amp;&amp;amp;=&lt;/code&gt; Logical AND and assign &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;||=&lt;/code&gt; Logical OR and assign &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For information about the behavior of these operators, see Basic Operators and Advanced Operators. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;At parse time, an expression made up of binary operators is represented as a flat list. This list is transformed into a tree by applying operator precedence. For example, the expression &lt;code&gt;2 + 3 * 5&lt;/code&gt; is initially understood as a flat list of five items, &lt;code&gt;2&lt;/code&gt;, &lt;code&gt;+&lt;/code&gt;, &lt;code&gt;3&lt;/code&gt;, &lt;code&gt;*&lt;/code&gt;, and &lt;code&gt;5&lt;/code&gt;. This process transforms it into the tree (2 + (3 * 5)). &lt;/p&gt;
&lt;p&gt;Grammar of a binary expression &lt;/p&gt;
&lt;p&gt;binary-expression  → binary-operator­prefix-expression­&lt;/p&gt;
&lt;p&gt;binary-expression  → prefix-expression­&lt;/p&gt;
&lt;p&gt;binary-expression  → prefix-expression­&lt;/p&gt;
&lt;p&gt;binary-expression  → type-casting-operator­&lt;/p&gt;
&lt;p&gt;binary-expressions  → binary-expressions­opt­&lt;/p&gt;
&lt;h4&gt;Assignment Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;assignment operator&lt;/em&gt; sets a new value for a given expression. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;expression&lt;/em&gt; = &lt;em&gt;value&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The value of the &lt;em&gt;expression&lt;/em&gt; is set to the value obtained by evaluating the &lt;em&gt;value&lt;/em&gt;. If the &lt;em&gt;expression&lt;/em&gt; is a tuple, the &lt;em&gt;value&lt;/em&gt; must be a tuple with the same number of elements. (Nested tuples are allowed.) Assignment is performed from each part of the &lt;em&gt;value&lt;/em&gt; to the corresponding part of the &lt;em&gt;expression&lt;/em&gt;. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;(a, _, (b, c)) = ("test", 9.45, (12, 3))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// a is "test", b is 12, c is 3, and 9.45 is ignored&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The assignment operator does not return any value. &lt;/p&gt;
&lt;p&gt;Grammar of an assignment operator &lt;/p&gt;
&lt;p&gt;assignment-operator  → &lt;code&gt;=­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Ternary Conditional Operator&lt;/h4&gt;
&lt;p&gt;The &lt;em&gt;ternary conditional operator&lt;/em&gt; evaluates to one of two given values based on the value of a condition. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;condition&lt;/em&gt; ? &lt;em&gt;expression used if true&lt;/em&gt; : &lt;em&gt;expression used if false&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the &lt;em&gt;condition&lt;/em&gt; evaluates to &lt;code&gt;true&lt;/code&gt;, the conditional operator evaluates the first expression and returns its value. Otherwise, it evaluates the second expression and returns its value. The unused expression is not evaluated. &lt;/p&gt;
&lt;p&gt;For an example that uses the ternary conditional operator, see Ternary Conditional Operator. &lt;/p&gt;
&lt;p&gt;Grammar of a conditional operator &lt;/p&gt;
&lt;p&gt;conditional-operator  → &lt;code&gt;?­&lt;/code&gt;expression­&lt;code&gt;:­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Type-Casting Operators&lt;/h4&gt;
&lt;p&gt;There are three type-casting operators, the &lt;code&gt;is&lt;/code&gt; operator, the &lt;code&gt;as?&lt;/code&gt; operator, and the &lt;code&gt;as&lt;/code&gt; operator. They have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;expression&lt;/em&gt; is &lt;em&gt;type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;expression&lt;/em&gt; as? &lt;em&gt;type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;expression&lt;/em&gt; as &lt;em&gt;type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;is&lt;/code&gt; operator checks at runtime whether the &lt;em&gt;expression&lt;/em&gt; can be downcast to the specified &lt;em&gt;type&lt;/em&gt;. It returns &lt;code&gt;true&lt;/code&gt; if the &lt;em&gt;expression&lt;/em&gt; can be downcast to the specified &lt;em&gt;type&lt;/em&gt;; otherwise, it returns &lt;code&gt;false&lt;/code&gt;. If casting to the specified &lt;em&gt;type&lt;/em&gt; is guaranteed to succeed or fail, a compile-time error is raised. For example, &lt;code&gt;10 is Int&lt;/code&gt; and &lt;code&gt;10 is String&lt;/code&gt; both raise compile-time errors. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;as?&lt;/code&gt; operator performs a conditional cast of the &lt;em&gt;expression&lt;/em&gt; to the specified &lt;em&gt;type&lt;/em&gt;. The &lt;code&gt;as?&lt;/code&gt; operator returns an optional of the specified &lt;em&gt;type&lt;/em&gt;. At runtime, if the cast succeeds, the value of &lt;em&gt;expression&lt;/em&gt; is wrapped in an optional and returned; otherwise, the value returned is &lt;code&gt;nil&lt;/code&gt;. If casting to the specified &lt;em&gt;type&lt;/em&gt; is guaranteed to fail, a compile-time error is raised. For example, casting to a type that’s neither a subclass or superclass of the type of the &lt;em&gt;expression&lt;/em&gt; is an error. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;as&lt;/code&gt; operator performs a forced cast of the &lt;em&gt;expression&lt;/em&gt; to the specified &lt;em&gt;type&lt;/em&gt;. The &lt;code&gt;as&lt;/code&gt; operator returns a value of the specified &lt;em&gt;type&lt;/em&gt;, not an optional type. If the cast fails, a runtime error is raised. The behavior of &lt;code&gt;x as T&lt;/code&gt; is the same as the behavior of &lt;code&gt;(x as? T)!&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;For more information about type casting and to see examples that use the type-casting operators, see Type Casting. &lt;/p&gt;
&lt;p&gt;Grammar of a type-casting operator &lt;/p&gt;
&lt;p&gt;type-casting-operator  → &lt;code&gt;is­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;type-casting-operator  → &lt;code&gt;as­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;type-casting-operator  → &lt;code&gt;as­``?­&lt;/code&gt;type­&lt;/p&gt;
&lt;h3&gt;Primary Expressions&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Primary expressions&lt;/em&gt; are the most basic kind of expression. They can be used as expressions on their own, and they can be combined with other tokens to make prefix expressions, binary expressions, and postfix expressions. &lt;/p&gt;
&lt;p&gt;Grammar of a primary expression &lt;/p&gt;
&lt;p&gt;primary-expression  → identifier­generic-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;primary-expression  → literal-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → self-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → superclass-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → closure-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → parenthesized-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → implicit-member-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → wildcard-expression­&lt;/p&gt;
&lt;h4&gt;Literal Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;literal expression&lt;/em&gt; consists of either an ordinary literal (such as a string or a number), an array or dictionary literal, or one of the following special literals: &lt;/p&gt;
&lt;p&gt;Literal &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;Type &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;Value   &lt;/p&gt;
&lt;p&gt;---|---|---  &lt;/p&gt;
&lt;p&gt;&lt;code&gt;__FILE__&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;&lt;code&gt;String&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;The name of the file in which it appears.   &lt;/p&gt;
&lt;p&gt;&lt;code&gt;__LINE__&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;&lt;code&gt;Int&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;The line number on which it appears.   &lt;/p&gt;
&lt;p&gt;&lt;code&gt;__COLUMN__&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;&lt;code&gt;Int&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;The column number in which it begins.   &lt;/p&gt;
&lt;p&gt;&lt;code&gt;__FUNCTION__&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;&lt;code&gt;String&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;The name of the declaration in which it appears.   &lt;/p&gt;
&lt;p&gt;Inside a function, the value of &lt;code&gt;__FUNCTION__&lt;/code&gt; is the name of that function, inside a method it is the name of that method, inside a property getter or setter it is the name of that property, inside special members like &lt;code&gt;init&lt;/code&gt; or &lt;code&gt;subscript&lt;/code&gt; it is the name of that keyword, and at the top level of a file it is the name of the current module. &lt;/p&gt;
&lt;p&gt;When used as the default value of a function or method, the special literal’s value is determined when the default value expression is evaluated at the call site. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func logFunctionName(string: String = __FUNCTION__) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println(string)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func myFunction() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;logFunctionName() // Prints "myFunction()".&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myFunction()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;namedArgs(1, withJay: 2)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;An &lt;em&gt;array literal&lt;/em&gt; is an ordered collection of values. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;[&lt;em&gt;value 1&lt;/em&gt;, &lt;em&gt;value 2&lt;/em&gt;, &lt;em&gt;...&lt;/em&gt;]&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The last expression in the array can be followed by an optional comma. The value of an array literal has type &lt;code&gt;[T]&lt;/code&gt;, where &lt;code&gt;T&lt;/code&gt; is the type of the expressions inside it. If there are expressions of multiple types, &lt;code&gt;T&lt;/code&gt; is their closest common supertype. Empty array literals are written using an empty pair of square brackets and can be used to create an empty array of a specified type. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var emptyArray: [Double] = []&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A &lt;em&gt;dictionary literal&lt;/em&gt; is an unordered collection of key-value pairs. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;[&lt;em&gt;key 1&lt;/em&gt;: &lt;em&gt;value 1&lt;/em&gt;, &lt;em&gt;key 2&lt;/em&gt;: &lt;em&gt;value 2&lt;/em&gt;, &lt;em&gt;...&lt;/em&gt;]&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The last expression in the dictionary can be followed by an optional comma. The value of a dictionary literal has type &lt;code&gt;[Key: Value]&lt;/code&gt;, where &lt;code&gt;Key&lt;/code&gt; is the type of its key expressions and &lt;code&gt;Value&lt;/code&gt; is the type of its value expressions. If there are expressions of multiple types, &lt;code&gt;Key&lt;/code&gt; and &lt;code&gt;Value&lt;/code&gt; are the closest common supertype for their respective values. An empty dictionary literal is written as a colon inside a pair of brackets (&lt;code&gt;[:]&lt;/code&gt;) to distinguish it from an empty array literal. You can use an empty dictionary literal to create an empty dictionary literal of specified key and value types. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var emptyDictionary: [String: Double] = [:]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a literal expression &lt;/p&gt;
&lt;p&gt;literal-expression  → literal­&lt;/p&gt;
&lt;p&gt;literal-expression  →  array-literal­ dictionary-literal­&lt;/p&gt;
&lt;p&gt;literal-expression  →  &lt;code&gt;__FILE__­&lt;/code&gt; &lt;code&gt;__LINE__­&lt;/code&gt; &lt;code&gt;__COLUMN__­&lt;/code&gt; &lt;code&gt;__FUNCTION__­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;array-literal  → &lt;code&gt;[­&lt;/code&gt;array-literal-items­opt­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;array-literal-items  →  array-literal-item­&lt;code&gt;,­&lt;/code&gt;opt­ array-literal-items­&lt;/p&gt;
&lt;p&gt;array-literal-item  → expression­&lt;/p&gt;
&lt;p&gt;dictionary-literal  →  &lt;code&gt;[­&lt;/code&gt;dictionary-literal-items­&lt;code&gt;]­&lt;/code&gt; &lt;code&gt;[­``:­``]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;dictionary-literal-items  →  dictionary-literal-item­&lt;code&gt;,­&lt;/code&gt;opt­ dictionary-literal-items­&lt;/p&gt;
&lt;p&gt;dictionary-literal-item  → expression­&lt;/p&gt;
&lt;h4&gt;Self Expression&lt;/h4&gt;
&lt;p&gt;The &lt;code&gt;self&lt;/code&gt; expression is an explicit reference to the current type or instance of the type in which it occurs. It has the following forms: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;self&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;self.&lt;em&gt;member name&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;self[&lt;em&gt;subscript index&lt;/em&gt;]&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;self(&lt;em&gt;initializer arguments&lt;/em&gt;)&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;self.init(&lt;em&gt;initializer arguments&lt;/em&gt;)&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In an initializer, subscript, or instance method, &lt;code&gt;self&lt;/code&gt; refers to the current instance of the type in which it occurs. In a static or class method, &lt;code&gt;self&lt;/code&gt; refers to the current type in which it occurs. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;self&lt;/code&gt; expression is used to specify scope when accessing members, providing disambiguation when there is another variable of the same name in scope, such as a function parameter. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var greeting: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init(greeting: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self.greeting = greeting&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In a mutating method of a value type, you can assign a new instance of that value type to &lt;code&gt;self&lt;/code&gt;. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var x = 0.0, y = 0.0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mutating func moveByX(deltaX: Double, y deltaY: Double) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;self = Point(x: x \+ deltaX, y: y \+ deltaY)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a self expression &lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­``.­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­``[­&lt;/code&gt;expression­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­``.­``init­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Superclass Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;superclass expression&lt;/em&gt; lets a class interact with its superclass. It has one of the following forms: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;super.&lt;em&gt;member name&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;super[&lt;em&gt;subscript index&lt;/em&gt;]&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;super.init(&lt;em&gt;initializer arguments&lt;/em&gt;)&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The first form is used to access a member of the superclass. The second form is used to access the superclass’s subscript implementation. The third form is used to access an initializer of the superclass. &lt;/p&gt;
&lt;p&gt;Subclasses can use a superclass expression in their implementation of members, subscripting, and initializers to make use of the implementation in their superclass. &lt;/p&gt;
&lt;p&gt;Grammar of a superclass expression &lt;/p&gt;
&lt;p&gt;superclass-expression  →  superclass-method-expression­ superclass-subscript-expression­ superclass-initializer-expression­&lt;/p&gt;
&lt;p&gt;superclass-method-expression  → &lt;code&gt;super­``.­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;p&gt;superclass-subscript-expression  → &lt;code&gt;super­``[­&lt;/code&gt;expression­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;superclass-initializer-expression  → &lt;code&gt;super­``.­``init­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Closure Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;closure expression&lt;/em&gt; creates a closure, also known as a &lt;em&gt;lambda&lt;/em&gt; or an &lt;em&gt;anonymous function&lt;/em&gt; in other programming languages. Like function declarations, closures contain statements which they execute, and they capture values from their enclosing scope. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;{ (&lt;em&gt;parameters&lt;/em&gt;) -&amp;gt; &lt;em&gt;return type&lt;/em&gt; in&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;parameters&lt;/em&gt; have the same form as the parameters in a function declaration, as described in Function Declaration. &lt;/p&gt;
&lt;p&gt;There are several special forms that allow closures to be written more concisely: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;A closure can omit the types of its parameters, its return type, or both. If you omit the parameter names and both types, omit the &lt;code&gt;in&lt;/code&gt; keyword before the statements. If the omitted types can’t be inferred, a compile-time error is raised. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A closure may omit names for its parameters. Its parameters are then implicitly named &lt;code&gt;$&lt;/code&gt; followed by their position: &lt;code&gt;$0&lt;/code&gt;, &lt;code&gt;$1&lt;/code&gt;, &lt;code&gt;$2&lt;/code&gt;, and so on. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A closure that consists of only a single expression is understood to return the value of that expression. The contents of this expression are also considered when performing type inference on the surrounding expression. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The following closure expressions are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;myFunction {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;(x: Int, y: Int) -&amp;gt; Int in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return x \+ y&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myFunction {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;(x, y) in&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return x \+ y&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myFunction { return $0 \+ $1 }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myFunction { $0 \+ $1 }&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For information about passing a closure as an argument to a function, see Function Call Expression. &lt;/p&gt;
&lt;p&gt;A closure expression can explicitly specify the values that it captures from the surrounding scope using a &lt;em&gt;capture list&lt;/em&gt;. A capture list is written as a comma separated list surrounded by square brackets, before the list of parameters. If you use a capture list, you must also use the &lt;code&gt;in&lt;/code&gt; keyword, even if you omit the parameter names, parameter types, and return type. &lt;/p&gt;
&lt;p&gt;Each entry in the capture list can be marked as &lt;code&gt;weak&lt;/code&gt; or &lt;code&gt;unowned&lt;/code&gt; to capture a weak or unowned reference to the value. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;myFunction { print(self.title) } // strong capture&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myFunction { [weak self] in print(self!.title) } // weak capture&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myFunction { [unowned self] in print(self.title) } // unowned capture&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can also bind an arbitrary expression to a named value in the capture list. The expression is evaluated when the closure is formed, and captured with the specified strength. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;// Weak capture of "self.parent" as "parent"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myFunction { [weak parent = self.parent] in print(parent!.title) }&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For more information and examples of closure expressions, see Closure Expressions. &lt;/p&gt;
&lt;p&gt;Grammar of a closure expression &lt;/p&gt;
&lt;p&gt;closure-expression  → &lt;code&gt;{­&lt;/code&gt;statements­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → parameter-clause­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → identifier-list­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → parameter-clause­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → identifier-list­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → capture-list­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;capture-list  → &lt;code&gt;[­&lt;/code&gt;expression­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;capture-specifier  →  &lt;code&gt;weak­&lt;/code&gt; &lt;code&gt;unowned­&lt;/code&gt; &lt;code&gt;unowned(safe)­&lt;/code&gt; &lt;code&gt;unowned(unsafe)­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Implicit Member Expression&lt;/h4&gt;
&lt;p&gt;An &lt;em&gt;implicit member expression&lt;/em&gt; is an abbreviated way to access a member of a type, such as an enumeration case or a class method, in a context where type inference can determine the implied type. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;.&lt;em&gt;member name&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var x = MyEnumeration.SomeValue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;x = .AnotherValue&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a implicit member expression &lt;/p&gt;
&lt;p&gt;implicit-member-expression  → &lt;code&gt;.­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;h4&gt;Parenthesized Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;parenthesized expression&lt;/em&gt; consists of a comma-separated list of expressions surrounded by parentheses. Each expression can have an optional identifier before it, separated by a colon (&lt;code&gt;:&lt;/code&gt;). It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;(&lt;em&gt;identifier 1&lt;/em&gt;: &lt;em&gt;expression 1&lt;/em&gt;, &lt;em&gt;identifier 2&lt;/em&gt;: &lt;em&gt;expression 2&lt;/em&gt;, &lt;em&gt;...&lt;/em&gt;)&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Use parenthesized expressions to create tuples and to pass arguments to a function call. If there is only one value inside the parenthesized expression, the type of the parenthesized expression is the type of that value. For example, the type of the parenthesized expression &lt;code&gt;(1)&lt;/code&gt; is &lt;code&gt;Int&lt;/code&gt;, not &lt;code&gt;(Int)&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Grammar of a parenthesized expression &lt;/p&gt;
&lt;p&gt;parenthesized-expression  → &lt;code&gt;(­&lt;/code&gt;expression-element-list­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;expression-element-list  →  expression-element­ expression-element-list­&lt;/p&gt;
&lt;p&gt;expression-element  →  expression­ identifier­&lt;code&gt;:­&lt;/code&gt;expression­&lt;/p&gt;
&lt;h4&gt;Wildcard Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;wildcard expression&lt;/em&gt; is used to explicitly ignore a value during an assignment. For example, in the following assignment 10 is assigned to &lt;code&gt;x&lt;/code&gt; and 20 is ignored: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;(x, _) = (10, 20)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// x is 10, and 20 is ignored&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a wildcard expression &lt;/p&gt;
&lt;p&gt;wildcard-expression  → &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Postfix Expressions&lt;/h3&gt;
&lt;p&gt;&lt;em&gt;Postfix expressions&lt;/em&gt; are formed by applying a postfix operator or other postfix syntax to an expression. Syntactically, every primary expression is also a postfix expression. &lt;/p&gt;
&lt;p&gt;The Swift standard library provides the following postfix operators: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;++&lt;/code&gt; Increment &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;\--&lt;/code&gt; Decrement &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For information about the behavior of these operators, see Basic Operators and Advanced Operators. &lt;/p&gt;
&lt;p&gt;Grammar of a postfix expression &lt;/p&gt;
&lt;p&gt;postfix-expression  → primary-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → postfix-operator­&lt;/p&gt;
&lt;p&gt;postfix-expression  → function-call-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → initializer-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → explicit-member-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → postfix-self-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → dynamic-type-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → subscript-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → forced-value-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → optional-chaining-expression­&lt;/p&gt;
&lt;h4&gt;Function Call Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;function call expression&lt;/em&gt; consists of a function name followed by a comma-separated list of the function’s arguments in parentheses. Function call expressions have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;function name&lt;/em&gt;(&lt;em&gt;argument value 1&lt;/em&gt;, &lt;em&gt;argument value 2&lt;/em&gt;)&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;function name&lt;/em&gt; can be any expression whose value is of a function type. &lt;/p&gt;
&lt;p&gt;If the function definition includes names for its parameters, the function call must include names before its argument values separated by a colon (&lt;code&gt;:&lt;/code&gt;). This kind of function call expression has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;function name&lt;/em&gt;(&lt;em&gt;argument name 1&lt;/em&gt;: &lt;em&gt;argument value 1&lt;/em&gt;, &lt;em&gt;argument name 2&lt;/em&gt;: &lt;em&gt;argument value 2&lt;/em&gt;)&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A function call expression can include a trailing closure in the form of a closure expression immediately after the closing parenthesis. The trailing closure is understood as an argument to the function, added after the last parenthesized argument. The following function calls are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;// someFunction takes an integer and a closure as its arguments&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someFunction(x, {$0 == 13})&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someFunction(x) {$0 == 13}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the trailing closure is the function’s only argument, the parentheses can be omitted. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;// someFunction takes a closure as its only argument&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myData.someMethod() {$0 == 13}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;myData.someMethod {$0 == 13}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a function call expression &lt;/p&gt;
&lt;p&gt;function-call-expression  → parenthesized-expression­&lt;/p&gt;
&lt;p&gt;function-call-expression  → parenthesized-expression­opt­trailing-closure­&lt;/p&gt;
&lt;p&gt;trailing-closure  → closure-expression­&lt;/p&gt;
&lt;h4&gt;Initializer Expression&lt;/h4&gt;
&lt;p&gt;An &lt;em&gt;initializer expression&lt;/em&gt; provides access to a type’s initializer. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;expression&lt;/em&gt;.init(&lt;em&gt;initializer arguments&lt;/em&gt;)&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You use the initializer expression in a function call expression to initialize a new instance of a type. Unlike functions, an initializer can’t be used as a value. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var x = SomeClass.someClassFunction // ok&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var y = SomeClass.init // error&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You also use an initializer expression to delegate to the initializer of a superclass. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeSubClass: SomeSuperClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override init() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// subclass initialization goes here&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;super.init()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of an initializer expression &lt;/p&gt;
&lt;p&gt;initializer-expression  → postfix-expression­&lt;code&gt;.­``init­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Explicit Member Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;explicit member expression&lt;/em&gt; allows access to the members of a named type, a tuple, or a module. It consists of a period (&lt;code&gt;.&lt;/code&gt;) between the item and the identifier of its member. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;expression&lt;/em&gt;.&lt;em&gt;member name&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The members of a named type are named as part of the type’s declaration or extension. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someProperty = 42&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let c = SomeClass()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let y = c.someProperty // Member access&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The members of a tuple are implicitly named using integers in the order they appear, starting from zero. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var t = (10, 20, 30)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;t.0 = t.1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Now t is (20, 20, 30)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The members of a module access the top-level declarations of that module. &lt;/p&gt;
&lt;p&gt;Grammar of an explicit member expression &lt;/p&gt;
&lt;p&gt;explicit-member-expression  → decimal-digits­&lt;/p&gt;
&lt;p&gt;explicit-member-expression  → identifier­generic-argument-clause­opt­&lt;/p&gt;
&lt;h4&gt;Postfix Self Expression&lt;/h4&gt;
&lt;p&gt;A postfix &lt;code&gt;self&lt;/code&gt; expression consists of an expression or the name of a type, immediately followed by &lt;code&gt;.self&lt;/code&gt;. It has the following forms: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;expression&lt;/em&gt;.self&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;type&lt;/em&gt;.self&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The first form evaluates to the value of the &lt;em&gt;expression&lt;/em&gt;. For example, &lt;code&gt;x.self&lt;/code&gt; evaluates to &lt;code&gt;x&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The second form evaluates to the value of the &lt;em&gt;type&lt;/em&gt;. Use this form to access a type as a value. For example, because &lt;code&gt;SomeClass.self&lt;/code&gt; evaluates to the &lt;code&gt;SomeClass&lt;/code&gt; type itself, you can pass it to a function or method that accepts a type-level argument. &lt;/p&gt;
&lt;p&gt;Grammar of a self expression &lt;/p&gt;
&lt;p&gt;postfix-self-expression  → postfix-expression­&lt;code&gt;.­``self­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Dynamic Type Expression&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;dynamicType&lt;/code&gt; expression consists of an expression, immediately followed by &lt;code&gt;.dynamicType&lt;/code&gt;. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;expression&lt;/em&gt;.dynamicType&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;expression&lt;/em&gt; can’t be the name of a type. The entire &lt;code&gt;dynamicType&lt;/code&gt; expression evaluates to the value of the runtime type of the &lt;em&gt;expression&lt;/em&gt;, as the following example shows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;class SomeBaseClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class func printClassName() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("SomeBaseClass")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class SomeSubClass: SomeBaseClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;override class func printClassName() {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("SomeSubClass")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let someInstance: SomeBaseClass = SomeSubClass()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someInstance has a static type of SomeBaseClass at compile time, and&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// it has a dynamc type of SomeSubClass at runtime&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someInstance.dynamicType.printClassName()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "SomeSubClass"&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a dynamic type expression &lt;/p&gt;
&lt;p&gt;dynamic-type-expression  → postfix-expression­&lt;code&gt;.­``dynamicType­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Subscript Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;subscript expression&lt;/em&gt; provides subscript access using the getter and setter of the corresponding subscript declaration. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;expression&lt;/em&gt;[&lt;em&gt;index expressions&lt;/em&gt;]&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;To evaluate the value of a subscript expression, the subscript getter for the &lt;em&gt;expression&lt;/em&gt;’s type is called with the &lt;em&gt;index expressions&lt;/em&gt; passed as the subscript parameters. To set its value, the subscript setter is called in the same way. &lt;/p&gt;
&lt;p&gt;For information about subscript declarations, see Protocol Subscript Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a subscript expression &lt;/p&gt;
&lt;p&gt;subscript-expression  → expression-list­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Forced-Value Expression&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;forced-value expression&lt;/em&gt; unwraps an optional value that you are certain is not &lt;code&gt;nil&lt;/code&gt;. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;expression&lt;/em&gt;!&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the value of the &lt;em&gt;expression&lt;/em&gt; is not &lt;code&gt;nil&lt;/code&gt;, the optional value is unwrapped and returned with the corresponding nonoptional type. Otherwise, a runtime error is raised. &lt;/p&gt;
&lt;p&gt;The unwrapped value of a forced-value expression can be modified, either by mutating the value itself, or by assigning to one of the value’s members. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var x: Int? = 0&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;x!++&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// x is now 1&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someDictionary = ["a": [1, 2, 3], "b": [10, 20]]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someDictionary["a"]![0] = 100&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someDictionary is now [b: [10, 20], a: [100, 2, 3]]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a forced-value expression &lt;/p&gt;
&lt;p&gt;forced-value-expression  → postfix-expression­&lt;code&gt;!­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Optional-Chaining Expression&lt;/h4&gt;
&lt;p&gt;An &lt;em&gt;optional-chaining expression&lt;/em&gt; provides a simplified syntax for using optional values in postfix expressions. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;expression&lt;/em&gt;?&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;On its own, the postfix &lt;code&gt;?&lt;/code&gt; operator simply returns the value of its argument as an optional. &lt;/p&gt;
&lt;p&gt;Postfix expressions that contain an optional-chaining expression are evaluated in a special way. If the optional-chaining expression is &lt;code&gt;nil&lt;/code&gt;, all of the other operations in the postfix expression are ignored and the entire postfix expression evaluates to &lt;code&gt;nil&lt;/code&gt;. If the optional-chaining expression is not &lt;code&gt;nil&lt;/code&gt;, the value of the optional-chaining expression is unwrapped and used to evaluate the rest of the postfix expression. In either case, the value of the postfix expression is still of an optional type. &lt;/p&gt;
&lt;p&gt;If a postfix expression that contains an optional-chaining expression is nested inside other postfix expressions, only the outermost expression returns an optional type. In the example below, when &lt;code&gt;c&lt;/code&gt; is not &lt;code&gt;nil&lt;/code&gt;, its value is unwrapped and used to evaluate &lt;code&gt;.property&lt;/code&gt;, the value of which is used to evaluate &lt;code&gt;.performAction()&lt;/code&gt;. The entire expression &lt;code&gt;c?.property.performAction()&lt;/code&gt; has a value of an optional type. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var c: SomeClass?&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var result: Bool? = c?.property.performAction()&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The following example shows the behavior of the example above without using optional chaining. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;var result: Bool? = nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if let unwrappedC = c {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;result = unwrappedC.property.performAction()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The unwrapped value of an optional-chaining expression can be modified, either by mutating the value itself, or by assigning to one of the value’s members. If the value of the optional-chaining expression is &lt;code&gt;nil&lt;/code&gt;, the expression on the right hand side of the assignment operator is not evaluated. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func someFunctionWithSideEffects() -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return 42 // No actual side effects.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var someDictionary = ["a": [1, 2, 3], "b": [10, 20]]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someDictionary["not here"]?[0] = someFunctionWithSideEffects()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someFunctionWithSideEffects is not evaluated&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someDictionary is still [b: [10, 20], a: [1, 2, 3]]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;someDictionary["a"]?[0] = someFunctionWithSideEffects()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someFunctionWithSideEffects is evaluated and returns 42&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// someDictionary is now [b: [10, 20], a: [42, 2, 3]]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of an optional-chaining expression &lt;/p&gt;
&lt;p&gt;optional-chaining-expression  → postfix-expression­&lt;code&gt;?­&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Statements&lt;/h2&gt;
&lt;p&gt;In Swift, there are two kinds of statements: simple statements and control flow statements. Simple statements are the most common and consist of either an expression or a declaration. Control flow statements are used to control the flow of execution in a program. There are three types of control flow statements in Swift: loop statements, branch statements, and control transfer statements. &lt;/p&gt;
&lt;p&gt;Loop statements allow a block of code to be executed repeatedly, branch statements allow a certain block of code to be executed only when certain conditions are met, and control transfer statements provide a way to alter the order in which code is executed. Each type of control flow statement is described in detail below. &lt;/p&gt;
&lt;p&gt;A semicolon (&lt;code&gt;;&lt;/code&gt;) can optionally appear after any statement and is used to separate multiple statements if they appear on the same line. &lt;/p&gt;
&lt;p&gt;Grammar of a statement &lt;/p&gt;
&lt;p&gt;statement  → expression­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → declaration­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → loop-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → branch-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → labeled-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → control-transfer-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statements  → statements­opt­&lt;/p&gt;
&lt;h3&gt;Loop Statements&lt;/h3&gt;
&lt;p&gt;Loop statements allow a block of code to be executed repeatedly, depending on the conditions specified in the loop. Swift has four loop statements: a &lt;code&gt;for&lt;/code&gt; statement, a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; statement, a &lt;code&gt;while&lt;/code&gt; statement, and a &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; statement. &lt;/p&gt;
&lt;p&gt;Control flow in a loop statement can be changed by a &lt;code&gt;break&lt;/code&gt; statement and a &lt;code&gt;continue&lt;/code&gt; statement and is discussed in Continue Statement below. &lt;/p&gt;
&lt;p&gt;Grammar of a loop statement &lt;/p&gt;
&lt;p&gt;loop-statement  → for-statement­&lt;/p&gt;
&lt;p&gt;loop-statement  → for-in-statement­&lt;/p&gt;
&lt;p&gt;loop-statement  → while-statement­&lt;/p&gt;
&lt;p&gt;loop-statement  → do-while-statement­&lt;/p&gt;
&lt;h4&gt;For Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;for&lt;/code&gt; statement allows a block of code to be executed repeatedly while incrementing a counter, as long as a condition remains true. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;for&lt;/code&gt; statement has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;for &lt;em&gt;initialization&lt;/em&gt;; &lt;em&gt;condition&lt;/em&gt;; &lt;em&gt;increment&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The semicolons between the &lt;em&gt;initialization&lt;/em&gt;, &lt;em&gt;condition&lt;/em&gt;, and &lt;em&gt;increment&lt;/em&gt; are required. The braces around the &lt;em&gt;statements&lt;/em&gt; in the body of the loop are also required. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;for&lt;/code&gt; statement is executed as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;em&gt;initialization&lt;/em&gt; is evaluated only once. It is typically used to declare and initialize any variables that are needed for the remainder of the loop. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;em&gt;condition&lt;/em&gt; expression is evaluated. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If &lt;code&gt;true&lt;/code&gt;, the program executes the &lt;em&gt;statements&lt;/em&gt;, and execution continues to step 3. If &lt;code&gt;false&lt;/code&gt;, the program does not execute the &lt;em&gt;statements&lt;/em&gt; or the &lt;em&gt;increment&lt;/em&gt; expression, and the program is finished executing the &lt;code&gt;for&lt;/code&gt; statement. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The &lt;em&gt;increment&lt;/em&gt; expression is evaluated, and execution returns to step 2. &lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Variables defined within the &lt;em&gt;initialization&lt;/em&gt; are valid only within the scope of the &lt;code&gt;for&lt;/code&gt; statement itself. &lt;/p&gt;
&lt;p&gt;The value of the &lt;em&gt;condition&lt;/em&gt; expression must have a type that conforms to the &lt;code&gt;BooleanType&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;Grammar of a for statement &lt;/p&gt;
&lt;p&gt;for-statement  → &lt;code&gt;for­&lt;/code&gt;expression­opt­&lt;code&gt;;­&lt;/code&gt;expression­opt­code-block­&lt;/p&gt;
&lt;p&gt;for-statement  → &lt;code&gt;for­``(­&lt;/code&gt;expression­opt­&lt;code&gt;;­&lt;/code&gt;expression­opt­&lt;code&gt;)­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;for-init  →  variable-declaration­ expression-list­&lt;/p&gt;
&lt;h4&gt;For-In Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; statement allows a block of code to be executed once for each item in a collection (or any type) that conforms to the &lt;code&gt;SequenceType&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; statement has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;for &lt;em&gt;item&lt;/em&gt; in &lt;em&gt;collection&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;generate&lt;/code&gt; method is called on the &lt;em&gt;collection&lt;/em&gt; expression to obtain a value of a generator type—that is, a type that conforms to the &lt;code&gt;GeneratorType&lt;/code&gt; protocol. The program begins executing a loop by calling the &lt;code&gt;next&lt;/code&gt; method on the stream. If the value returned is not &lt;code&gt;None&lt;/code&gt;, it is assigned to the &lt;em&gt;item&lt;/em&gt; pattern, the program executes the &lt;em&gt;statements&lt;/em&gt;, and then continues execution at the beginning of the loop. Otherwise, the program does not perform assignment or execute the &lt;em&gt;statements&lt;/em&gt;, and it is finished executing the &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; statement. &lt;/p&gt;
&lt;p&gt;Grammar of a for-in statement &lt;/p&gt;
&lt;p&gt;for-in-statement  → &lt;code&gt;for­&lt;/code&gt;pattern­&lt;code&gt;in­&lt;/code&gt;expression­code-block­&lt;/p&gt;
&lt;h4&gt;While Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;while&lt;/code&gt; statement allows a block of code to be executed repeatedly, as long as a condition remains true. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;while&lt;/code&gt; statement has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;while &lt;em&gt;condition&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A &lt;code&gt;while&lt;/code&gt; statement is executed as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The &lt;em&gt;condition&lt;/em&gt; is evaluated. &lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If &lt;code&gt;true&lt;/code&gt;, execution continues to step 2. If &lt;code&gt;false&lt;/code&gt;, the program is finished executing the &lt;code&gt;while&lt;/code&gt; statement. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The program executes the &lt;em&gt;statements&lt;/em&gt;, and execution returns to step 1. &lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because the value of the &lt;em&gt;condition&lt;/em&gt; is evaluated before the &lt;em&gt;statements&lt;/em&gt; are executed, the &lt;em&gt;statements&lt;/em&gt; in a &lt;code&gt;while&lt;/code&gt; statement can be executed zero or more times. &lt;/p&gt;
&lt;p&gt;The value of the &lt;em&gt;condition&lt;/em&gt; must have a type that conforms to the &lt;code&gt;BooleanType&lt;/code&gt; protocol. The condition can also be an optional binding declaration, as discussed in Optional Binding. &lt;/p&gt;
&lt;p&gt;Grammar of a while statement &lt;/p&gt;
&lt;p&gt;while-statement  → &lt;code&gt;while­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;while-condition  →  expression­ declaration­&lt;/p&gt;
&lt;h4&gt;Do-While Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; statement allows a block of code to be executed one or more times, as long as a condition remains true. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; statement has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;do {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;} while &lt;em&gt;condition&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; statement is executed as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The program executes the &lt;em&gt;statements&lt;/em&gt;, and execution continues to step 2. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;em&gt;condition&lt;/em&gt; is evaluated. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If &lt;code&gt;true&lt;/code&gt;, execution returns to step 1. If &lt;code&gt;false&lt;/code&gt;, the program is finished executing the &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; statement. &lt;/p&gt;
&lt;p&gt;Because the value of the &lt;em&gt;condition&lt;/em&gt; is evaluated after the &lt;em&gt;statements&lt;/em&gt; are executed, the &lt;em&gt;statements&lt;/em&gt; in a &lt;code&gt;do&lt;/code&gt;-&lt;code&gt;while&lt;/code&gt; statement are executed at least once. &lt;/p&gt;
&lt;p&gt;The value of the &lt;em&gt;condition&lt;/em&gt; must have a type that conforms to the &lt;code&gt;BooleanType&lt;/code&gt; protocol. The condition can also be an optional binding declaration, as discussed in Optional Binding. &lt;/p&gt;
&lt;p&gt;Grammar of a do-while statement &lt;/p&gt;
&lt;p&gt;do-while-statement  → &lt;code&gt;do­&lt;/code&gt;code-block­&lt;code&gt;while­&lt;/code&gt;while-condition­&lt;/p&gt;
&lt;h3&gt;Branch Statements&lt;/h3&gt;
&lt;p&gt;Branch statements allow the program to execute certain parts of code depending on the value of one or more conditions. The values of the conditions specified in a branch statement control how the program branches and, therefore, what block of code is executed. Swift has two branch statements: an &lt;code&gt;if&lt;/code&gt; statement and a &lt;code&gt;switch&lt;/code&gt; statement. &lt;/p&gt;
&lt;p&gt;Control flow in a &lt;code&gt;switch&lt;/code&gt; statement can be changed by a &lt;code&gt;break&lt;/code&gt; statement and is discussed in Break Statement below. &lt;/p&gt;
&lt;p&gt;Grammar of a branch statement &lt;/p&gt;
&lt;p&gt;branch-statement  → if-statement­&lt;/p&gt;
&lt;p&gt;branch-statement  → switch-statement­&lt;/p&gt;
&lt;h4&gt;If Statement&lt;/h4&gt;
&lt;p&gt;An &lt;code&gt;if&lt;/code&gt; statement is used for executing code based on the evaluation of one or more conditions. &lt;/p&gt;
&lt;p&gt;There are two basic forms of an &lt;code&gt;if&lt;/code&gt; statement. In each form, the opening and closing braces are required. &lt;/p&gt;
&lt;p&gt;The first form allows code to be executed only when a condition is true and has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;if &lt;em&gt;condition&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The second form of an &lt;code&gt;if&lt;/code&gt; statement provides an additional &lt;em&gt;else clause&lt;/em&gt; (introduced by the &lt;code&gt;else&lt;/code&gt; keyword) and is used for executing one part of code when the condition is true and another part of code when the same condition is false. When a single else clause is present, an &lt;code&gt;if&lt;/code&gt; statement has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;if &lt;em&gt;condition&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements to execute if condition is true&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;} else {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements to execute if condition is false&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The else clause of an &lt;code&gt;if&lt;/code&gt; statement can contain another &lt;code&gt;if&lt;/code&gt; statement to test more than one condition. An &lt;code&gt;if&lt;/code&gt; statement chained together in this way has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;if &lt;em&gt;condition 1&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements to execute if condition 1 is true&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;} else if &lt;em&gt;condition 2&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements to execute if condition 2 is true&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;} else {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements to execute if both conditions are false&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The value of any condition in an &lt;code&gt;if&lt;/code&gt; statement must have a type that conforms to the &lt;code&gt;BooleanType&lt;/code&gt; protocol. The condition can also be an optional binding declaration, as discussed in Optional Binding. &lt;/p&gt;
&lt;p&gt;Grammar of an if statement &lt;/p&gt;
&lt;p&gt;if-statement  → &lt;code&gt;if­&lt;/code&gt;code-block­else-clause­opt­&lt;/p&gt;
&lt;p&gt;if-condition  →  expression­ value-binding-pattern­&lt;/p&gt;
&lt;p&gt;else-clause  →  &lt;code&gt;else­&lt;/code&gt;code-block­ &lt;code&gt;else­&lt;/code&gt;if-statement­&lt;/p&gt;
&lt;h4&gt;Switch Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;switch&lt;/code&gt; statement allows certain blocks of code to be executed depending on the value of a control expression. &lt;/p&gt;
&lt;p&gt;A switch statement has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;switch &lt;em&gt;control expression&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;pattern 1&lt;/em&gt;:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;pattern 2&lt;/em&gt; where &lt;em&gt;condition&lt;/em&gt;:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;pattern 3&lt;/em&gt; where &lt;em&gt;condition&lt;/em&gt;,&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;pattern 4&lt;/em&gt; where &lt;em&gt;condition&lt;/em&gt;:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;default:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;control expression&lt;/em&gt; of the &lt;code&gt;switch&lt;/code&gt; statement is evaluated and then compared with the patterns specified in each case. If a match is found, the program executes the &lt;em&gt;statements&lt;/em&gt; listed within the scope of that case. The scope of each case can’t be empty. As a result, you must include at least one statement following the colon (&lt;code&gt;:&lt;/code&gt;) of each case label. Use a single &lt;code&gt;break&lt;/code&gt; statement if you don’t intend to execute any code in the body of a matched case. &lt;/p&gt;
&lt;p&gt;The values of expressions your code can branch on are very flexible. For instance, in addition to the values of scalar types, such as integers and characters, your code can branch on the values of any type, including floating-point numbers, strings, tuples, instances of custom classes, and optionals. The value of the &lt;em&gt;control expression&lt;/em&gt; can even be matched to the value of a case in an enumeration and checked for inclusion in a specified range of values. For examples of how to use these various types of values in &lt;code&gt;switch&lt;/code&gt; statements, see Switch in the Control Flow chapter. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;switch&lt;/code&gt; case can optionally contain a guard expression after each pattern. A &lt;em&gt;guard expression&lt;/em&gt; is introduced by the keyword &lt;code&gt;where&lt;/code&gt; followed by an expression, and is used to provide an additional condition before a pattern in a case is considered matched to the &lt;em&gt;control expression&lt;/em&gt;. If a guard expression is present, the &lt;em&gt;statements&lt;/em&gt; within the relevant case are executed only if the value of the &lt;em&gt;control expression&lt;/em&gt; matches one of the patterns of the case and the guard expression evaluates to &lt;code&gt;true&lt;/code&gt;. For instance, a &lt;em&gt;control expression&lt;/em&gt; matches the case in the example below only if it is a tuple that contains two elements of the same value, such as &lt;code&gt;(1, 1)&lt;/code&gt;. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;case let (x, y) where x == y:&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;As the above example shows, patterns in a case can also bind constants using the keyword &lt;code&gt;let&lt;/code&gt; (they can also bind variables using the keyword &lt;code&gt;var&lt;/code&gt;). These constants (or variables) can then be referenced in a corresponding guard expression and throughout the rest of the code within the scope of the case. That said, if the case contains multiple patterns that match the control expression, none of those patterns can contain constant or variable bindings. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;switch&lt;/code&gt; statement can also include a default case, introduced by the keyword &lt;code&gt;default&lt;/code&gt;. The code within a default case is executed only if no other cases match the control expression. A &lt;code&gt;switch&lt;/code&gt; statement can include only one default case, which must appear at the end of the &lt;code&gt;switch&lt;/code&gt; statement. &lt;/p&gt;
&lt;p&gt;Although the actual execution order of pattern-matching operations, and in particular the evaluation order of patterns in cases, is unspecified, pattern matching in a &lt;code&gt;switch&lt;/code&gt; statement behaves as if the evaluation is performed in source order—that is, the order in which they appear in source code. As a result, if multiple cases contain patterns that evaluate to the same value, and thus can match the value of the control expression, the program executes only the code within the first matching case in source order. &lt;/p&gt;
&lt;h4&gt;Switch Statements Must Be Exhaustive&lt;/h4&gt;
&lt;p&gt;In Swift, every possible value of the control expression’s type must match the value of at least one pattern of a case. When this simply isn’t feasible (for instance, when the control expression’s type is &lt;code&gt;Int&lt;/code&gt;), you can include a default case to satisfy the requirement. &lt;/p&gt;
&lt;h4&gt;Execution Does Not Fall Through Cases Implicitly&lt;/h4&gt;
&lt;p&gt;After the code within a matched case has finished executing, the program exits from the &lt;code&gt;switch&lt;/code&gt; statement. Program execution does not continue or “fall through” to the next case or default case. That said, if you want execution to continue from one case to the next, explicitly include a &lt;code&gt;fallthrough&lt;/code&gt; statement, which simply consists of the keyword &lt;code&gt;fallthrough&lt;/code&gt;, in the case from which you want execution to continue. For more information about the &lt;code&gt;fallthrough&lt;/code&gt; statement, see Fallthrough Statement below. &lt;/p&gt;
&lt;p&gt;Grammar of a switch statement &lt;/p&gt;
&lt;p&gt;switch-statement  → &lt;code&gt;switch­&lt;/code&gt;expression­&lt;code&gt;{­&lt;/code&gt;switch-cases­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;switch-cases  → switch-cases­opt­&lt;/p&gt;
&lt;p&gt;switch-case  →  statements­ statements­&lt;/p&gt;
&lt;p&gt;switch-case  →  case-label­&lt;code&gt;;­&lt;/code&gt; default-label­&lt;code&gt;;­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;case-label  → &lt;code&gt;case­&lt;/code&gt;case-item-list­&lt;code&gt;:­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;case-item-list  →  pattern­guard-clause­opt­ pattern­case-item-list­&lt;/p&gt;
&lt;p&gt;default-label  → &lt;code&gt;default­``:­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;guard-clause  → &lt;code&gt;where­&lt;/code&gt;guard-expression­&lt;/p&gt;
&lt;p&gt;guard-expression  → expression­&lt;/p&gt;
&lt;h3&gt;Labeled Statement&lt;/h3&gt;
&lt;p&gt;You can prefix a loop statement or a &lt;code&gt;switch&lt;/code&gt; statement with a &lt;em&gt;statement label&lt;/em&gt;, which consists of the name of the label followed immediately by a colon (:). Use statement labels with &lt;code&gt;break&lt;/code&gt; and &lt;code&gt;continue&lt;/code&gt; statements to be explicit about how you want to change control flow in a loop statement or a &lt;code&gt;switch&lt;/code&gt; statement, as discussed in Continue Statement below. &lt;/p&gt;
&lt;p&gt;The scope of a labeled statement is the entire statement following the statement label. You can nest labeled statements, but the name of each statement label must be unique. &lt;/p&gt;
&lt;p&gt;For more information and to see examples of how to use statement labels, see Labeled Statements in the Control Flow chapter. &lt;/p&gt;
&lt;p&gt;Grammar of a labeled statement &lt;/p&gt;
&lt;p&gt;labeled-statement  →  loop-statement­ switch-statement­&lt;/p&gt;
&lt;p&gt;statement-label  → label-name­&lt;code&gt;:­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;label-name  → identifier­&lt;/p&gt;
&lt;h3&gt;Control Transfer Statements&lt;/h3&gt;
&lt;p&gt;Control transfer statements can change the order in which code in your program is executed by unconditionally transferring program control from one piece of code to another. Swift has four control transfer statements: a &lt;code&gt;break&lt;/code&gt; statement, a &lt;code&gt;continue&lt;/code&gt; statement, a &lt;code&gt;fallthrough&lt;/code&gt; statement, and a &lt;code&gt;return&lt;/code&gt; statement. &lt;/p&gt;
&lt;p&gt;Grammar of a control transfer statement &lt;/p&gt;
&lt;p&gt;control-transfer-statement  → break-statement­&lt;/p&gt;
&lt;p&gt;control-transfer-statement  → continue-statement­&lt;/p&gt;
&lt;p&gt;control-transfer-statement  → fallthrough-statement­&lt;/p&gt;
&lt;p&gt;control-transfer-statement  → return-statement­&lt;/p&gt;
&lt;h4&gt;Break Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;break&lt;/code&gt; statement ends program execution of a loop or a &lt;code&gt;switch&lt;/code&gt; statement. A &lt;code&gt;break&lt;/code&gt; statement can consist of only the keyword &lt;code&gt;break&lt;/code&gt;, or it can consist of the keyword &lt;code&gt;break&lt;/code&gt; followed by the name of a statement label, as shown below. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;break&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;break &lt;em&gt;label name&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When a &lt;code&gt;break&lt;/code&gt; statement is followed by the name of a statement label, it ends program execution of the loop or &lt;code&gt;switch&lt;/code&gt; statement named by that label. &lt;/p&gt;
&lt;p&gt;When a &lt;code&gt;break&lt;/code&gt; statement is not followed by the name of a statement label, it ends program execution of the &lt;code&gt;switch&lt;/code&gt; statement or the innermost enclosing loop statement in which it occurs. &lt;/p&gt;
&lt;p&gt;In both cases, program control is then transferred to the first line of code following the enclosing loop or &lt;code&gt;switch&lt;/code&gt; statement, if any. &lt;/p&gt;
&lt;p&gt;For examples of how to use a &lt;code&gt;break&lt;/code&gt; statement, see Break and Labeled Statements in the Control Flow chapter. &lt;/p&gt;
&lt;p&gt;Grammar of a break statement &lt;/p&gt;
&lt;p&gt;break-statement  → &lt;code&gt;break­&lt;/code&gt;label-name­opt­&lt;/p&gt;
&lt;h4&gt;Continue Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;continue&lt;/code&gt; statement ends program execution of the current iteration of a loop statement but does not stop execution of the loop statement. A &lt;code&gt;continue&lt;/code&gt; statement can consist of only the keyword &lt;code&gt;continue&lt;/code&gt;, or it can consist of the keyword &lt;code&gt;continue&lt;/code&gt; followed by the name of a statement label, as shown below. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;continue&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;continue &lt;em&gt;label name&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When a &lt;code&gt;continue&lt;/code&gt; statement is followed by the name of a statement label, it ends program execution of the current iteration of the loop statement named by that label. &lt;/p&gt;
&lt;p&gt;When a &lt;code&gt;continue&lt;/code&gt; statement is not followed by the name of a statement label, it ends program execution of the current iteration of the innermost enclosing loop statement in which it occurs. &lt;/p&gt;
&lt;p&gt;In both cases, program control is then transferred to the condition of the enclosing loop statement. &lt;/p&gt;
&lt;p&gt;In a &lt;code&gt;for&lt;/code&gt; statement, the increment expression is still evaluated after the &lt;code&gt;continue&lt;/code&gt; statement is executed, because the increment expression is evaluated after the execution of the loop’s body. &lt;/p&gt;
&lt;p&gt;For examples of how to use a &lt;code&gt;continue&lt;/code&gt; statement, see Continue and Labeled Statements in the Control Flow chapter. &lt;/p&gt;
&lt;p&gt;Grammar of a continue statement &lt;/p&gt;
&lt;p&gt;continue-statement  → &lt;code&gt;continue­&lt;/code&gt;label-name­opt­&lt;/p&gt;
&lt;h4&gt;Fallthrough Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;fallthrough&lt;/code&gt; statement consists of the &lt;code&gt;fallthrough&lt;/code&gt; keyword and occurs only in a case block of a &lt;code&gt;switch&lt;/code&gt; statement. A &lt;code&gt;fallthrough&lt;/code&gt; statement causes program execution to continue from one case in a &lt;code&gt;switch&lt;/code&gt; statement to the next case. Program execution continues to the next case even if the patterns of the case label do not match the value of the &lt;code&gt;switch&lt;/code&gt; statement’s control expression. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;fallthrough&lt;/code&gt; statement can appear anywhere inside a &lt;code&gt;switch&lt;/code&gt; statement, not just as the last statement of a case block, but it can’t be used in the final case block. It also cannot transfer control into a case block whose pattern contains value binding patterns. &lt;/p&gt;
&lt;p&gt;For an example of how to use a &lt;code&gt;fallthrough&lt;/code&gt; statement in a &lt;code&gt;switch&lt;/code&gt; statement, see Control Transfer Statements in the Control Flow chapter. &lt;/p&gt;
&lt;p&gt;Grammar of a fallthrough statement &lt;/p&gt;
&lt;p&gt;fallthrough-statement  → &lt;code&gt;fallthrough­&lt;/code&gt;&lt;/p&gt;
&lt;h4&gt;Return Statement&lt;/h4&gt;
&lt;p&gt;A &lt;code&gt;return&lt;/code&gt; statement occurs in the body of a function or method definition and causes program execution to return to the calling function or method. Program execution continues at the point immediately following the function or method call. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;return&lt;/code&gt; statement can consist of only the keyword &lt;code&gt;return&lt;/code&gt;, or it can consist of the keyword &lt;code&gt;return&lt;/code&gt; followed by an expression, as shown below. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;return&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;return &lt;em&gt;expression&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When a &lt;code&gt;return&lt;/code&gt; statement is followed by an expression, the value of the expression is returned to the calling function or method. If the value of the expression does not match the value of the return type declared in the function or method declaration, the expression’s value is converted to the return type before it is returned to the calling function or method. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;As described in Failable Initializers, a special form of the &lt;code&gt;return&lt;/code&gt; statement (&lt;code&gt;return nil&lt;/code&gt;) can be used in a failable initializer to indicate initialization failure. &lt;/p&gt;
&lt;p&gt;When a &lt;code&gt;return&lt;/code&gt; statement is not followed by an expression, it can be used only to return from a function or method that does not return a value (that is, when the return type of the function or method is &lt;code&gt;Void&lt;/code&gt; or &lt;code&gt;()&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;Grammar of a return statement &lt;/p&gt;
&lt;p&gt;return-statement  → &lt;code&gt;return­&lt;/code&gt;expression­opt­&lt;/p&gt;
&lt;h2&gt;Declarations&lt;/h2&gt;
&lt;p&gt;A &lt;em&gt;declaration&lt;/em&gt; introduces a new name or construct into your program. For example, you use declarations to introduce functions and methods, variables and constants, and to define new, named enumeration, structure, class, and protocol types. You can also use a declaration to extend the behavior of an existing named type and to import symbols into your program that are declared elsewhere. &lt;/p&gt;
&lt;p&gt;In Swift, most declarations are also definitions in the sense that they are implemented or initialized at the same time they are declared. That said, because protocols don’t implement their members, most protocol members are declarations only. For convenience and because the distinction isn’t that important in Swift, the term &lt;em&gt;declaration&lt;/em&gt; covers both declarations and definitions. &lt;/p&gt;
&lt;p&gt;Grammar of a declaration &lt;/p&gt;
&lt;p&gt;declaration  → import-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → constant-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → variable-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → typealias-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → function-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → enum-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → struct-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → class-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → protocol-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → initializer-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → deinitializer-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → extension-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → subscript-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → operator-declaration­&lt;/p&gt;
&lt;p&gt;declarations  → declarations­opt­&lt;/p&gt;
&lt;h3&gt;Top-Level Code&lt;/h3&gt;
&lt;p&gt;The top-level code in a Swift source file consists of zero or more statements, declarations, and expressions. By default, variables, constants, and other named declarations that are declared at the top-level of a source file are accessible to code in every source file that is part of the same module. You can override this default behavior by marking the declaration with an access level modifier, as described in Access Control Levels. &lt;/p&gt;
&lt;p&gt;Grammar of a top-level declaration &lt;/p&gt;
&lt;p&gt;top-level-declaration  → statements­opt­&lt;/p&gt;
&lt;h3&gt;Code Blocks&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;code block&lt;/em&gt; is used by a variety of declarations and control structures to group statements together. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;{&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;statements&lt;/em&gt; inside a code block include declarations, expressions, and other kinds of statements and are executed in order of their appearance in source code. &lt;/p&gt;
&lt;p&gt;Grammar of a code block &lt;/p&gt;
&lt;p&gt;code-block  → &lt;code&gt;{­&lt;/code&gt;statements­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Import Declaration&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;import declaration&lt;/em&gt; lets you access symbols that are declared outside the current file. The basic form imports the entire module; it consists of the &lt;code&gt;import&lt;/code&gt; keyword followed by a module name: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;import &lt;em&gt;module&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Providing more detail limits which symbols are imported—you can specify a specific submodule or a specific declaration within a module or submodule. When this detailed form is used, only the imported symbol (and not the module that declares it) is made available in the current scope. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;import &lt;em&gt;import kind&lt;/em&gt; &lt;em&gt;module&lt;/em&gt;.&lt;em&gt;symbol name&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;import &lt;em&gt;module&lt;/em&gt;.&lt;em&gt;submodule&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of an import declaration &lt;/p&gt;
&lt;p&gt;import-declaration  → attributes­opt­&lt;code&gt;import­&lt;/code&gt;import-path­&lt;/p&gt;
&lt;p&gt;import-kind  →  &lt;code&gt;typealias­&lt;/code&gt; &lt;code&gt;struct­&lt;/code&gt; &lt;code&gt;class­&lt;/code&gt; &lt;code&gt;enum­&lt;/code&gt; &lt;code&gt;protocol­&lt;/code&gt; &lt;code&gt;var­&lt;/code&gt; &lt;code&gt;func­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;import-path  →  import-path-identifier­ import-path­&lt;/p&gt;
&lt;p&gt;import-path-identifier  →  identifier­ operator­&lt;/p&gt;
&lt;h3&gt;Constant Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;constant declaration&lt;/em&gt; introduces a constant named value into your program. Constant declarations are declared using the keyword &lt;code&gt;let&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;let &lt;em&gt;constant name&lt;/em&gt;: &lt;em&gt;type&lt;/em&gt; = &lt;em&gt;expression&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A constant declaration defines an immutable binding between the &lt;em&gt;constant name&lt;/em&gt; and the value of the initializer &lt;em&gt;expression&lt;/em&gt;; after the value of a constant is set, it cannot be changed. That said, if a constant is initialized with a class object, the object itself can change, but the binding between the constant name and the object it refers to can’t. &lt;/p&gt;
&lt;p&gt;When a constant is declared at global scope, it must be initialized with a value. When a constant declaration occurs in the context of a class or structure declaration, it is considered a &lt;em&gt;constant property&lt;/em&gt;. Constant declarations are not computed properties and therefore do not have getters or setters. &lt;/p&gt;
&lt;p&gt;If the &lt;em&gt;constant name&lt;/em&gt; of a constant declaration is a tuple pattern, the name of each item in the tuple is bound to the corresponding value in the initializer &lt;em&gt;expression&lt;/em&gt;. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let (firstNumber, secondNumber) = (10, 42)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this example, &lt;code&gt;firstNumber&lt;/code&gt; is a named constant for the value &lt;code&gt;10&lt;/code&gt;, and &lt;code&gt;secondNumber&lt;/code&gt; is a named constant for the value &lt;code&gt;42&lt;/code&gt;. Both constants can now be used independently: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;println("The first number is \\(firstNumber).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The first number is 10."&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The second number is \\(secondNumber).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The second number is 42."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The type annotation (&lt;code&gt;:&lt;/code&gt; &lt;em&gt;type&lt;/em&gt;) is optional in a constant declaration when the type of the &lt;em&gt;constant name&lt;/em&gt; can be inferred, as described in Type Inference. &lt;/p&gt;
&lt;p&gt;To declare a static constant property, mark the declaration with the &lt;code&gt;static&lt;/code&gt; declaration modifier. Static properties are discussed in Type Properties. &lt;/p&gt;
&lt;p&gt;For more information about constants and for guidance about when to use them, see Constants and Variables and Stored Properties. &lt;/p&gt;
&lt;p&gt;Grammar of a constant declaration &lt;/p&gt;
&lt;p&gt;constant-declaration  → attributes­opt­pattern-initializer-list­&lt;/p&gt;
&lt;p&gt;pattern-initializer-list  →  pattern-initializer­ pattern-initializer-list­&lt;/p&gt;
&lt;p&gt;pattern-initializer  → pattern­initializer­opt­&lt;/p&gt;
&lt;p&gt;initializer  → &lt;code&gt;=­&lt;/code&gt;expression­&lt;/p&gt;
&lt;h3&gt;Variable Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;variable declaration&lt;/em&gt; introduces a variable named value into your program and is declared using the keyword &lt;code&gt;var&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Variable declarations have several forms that declare different kinds of named, mutable values, including stored and computed variables and properties, stored variable and property observers, and static variable properties. The appropriate form to use depends on the scope at which the variable is declared and the kind of variable you intend to declare. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You can also declare properties in the context of a protocol declaration, as described in Protocol Property Declaration. &lt;/p&gt;
&lt;p&gt;You can override a property in a subclass by marking the subclass’s property declaration with the &lt;code&gt;override&lt;/code&gt; declaration modifier, as described in Overriding. &lt;/p&gt;
&lt;h4&gt;Stored Variables and Stored Variable Properties&lt;/h4&gt;
&lt;p&gt;The following form declares a stored variable or stored variable property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;var &lt;em&gt;variable name&lt;/em&gt;: &lt;em&gt;type&lt;/em&gt; = &lt;em&gt;expression&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You define this form of a variable declaration at global scope, the local scope of a function, or in the context of a class or structure declaration. When a variable declaration of this form is declared at global scope or the local scope of a function, it is referred to as a &lt;em&gt;stored variable&lt;/em&gt;. When it is declared in the context of a class or structure declaration, it is referred to as a &lt;em&gt;stored variable property&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;The initializer &lt;em&gt;expression&lt;/em&gt; can’t be present in a protocol declaration, but in all other contexts, the initializer &lt;em&gt;expression&lt;/em&gt; is optional. That said, if no initializer &lt;em&gt;expression&lt;/em&gt; is present, the variable declaration must include an explicit type annotation (&lt;code&gt;:&lt;/code&gt; &lt;em&gt;type&lt;/em&gt;). &lt;/p&gt;
&lt;p&gt;As with constant declarations, if the &lt;em&gt;variable name&lt;/em&gt; is a tuple pattern, the name of each item in the tuple is bound to the corresponding value in the initializer &lt;em&gt;expression&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;As their names suggest, the value of a stored variable or a stored variable property is stored in memory. &lt;/p&gt;
&lt;h4&gt;Computed Variables and Computed Properties&lt;/h4&gt;
&lt;p&gt;The following form declares a computed variable or computed property: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;var &lt;em&gt;variable name&lt;/em&gt;: &lt;em&gt;type&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;get {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;set(&lt;em&gt;setter name&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You define this form of a variable declaration at global scope, the local scope of a function, or in the context of a class, structure, enumeration, or extension declaration. When a variable declaration of this form is declared at global scope or the local scope of a function, it is referred to as a &lt;em&gt;computed variable&lt;/em&gt;. When it is declared in the context of a class, structure, or extension declaration, it is referred to as a &lt;em&gt;computed property&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;The getter is used to read the value, and the setter is used to write the value. The setter clause is optional, and when only a getter is needed, you can omit both clauses and simply return the requested value directly, as described in Read-Only Computed Properties. But if you provide a setter clause, you must also provide a getter clause. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;setter name&lt;/em&gt; and enclosing parentheses is optional. If you provide a setter name, it is used as the name of the parameter to the setter. If you do not provide a setter name, the default parameter name to the setter is &lt;code&gt;newValue&lt;/code&gt;, as described in Shorthand Setter Declaration. &lt;/p&gt;
&lt;p&gt;Unlike stored named values and stored variable properties, the value of a computed named value or a computed property is not stored in memory. &lt;/p&gt;
&lt;p&gt;For more information and to see examples of computed properties, see Computed Properties. &lt;/p&gt;
&lt;h4&gt;Stored Variable Observers and Property Observers&lt;/h4&gt;
&lt;p&gt;You can also declare a stored variable or property with &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; observers. A stored variable or property declared with observers has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;var &lt;em&gt;variable name&lt;/em&gt;: &lt;em&gt;type&lt;/em&gt; = &lt;em&gt;expression&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;willSet(&lt;em&gt;setter name&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;didSet(&lt;em&gt;setter name&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You define this form of a variable declaration at global scope, the local scope of a function, or in the context of a class or structure declaration. When a variable declaration of this form is declared at global scope or the local scope of a function, the observers are referred to as &lt;em&gt;stored variable observers&lt;/em&gt;. When it is declared in the context of a class or structure declaration, the observers are referred to as &lt;em&gt;property observers&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;You can add property observers to any stored property. You can also add property observers to any inherited property (whether stored or computed) by overriding the property within a subclass, as described in Overriding Property Observers. &lt;/p&gt;
&lt;p&gt;The initializer &lt;em&gt;expression&lt;/em&gt; is optional in the context of a class or structure declaration, but required elsewhere. The type annotation is required in all variable declarations that include observers, regardless of the context in which they are declared. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; observers provide a way to observe (and to respond appropriately) when the value of a variable or property is being set. The observers are not called when the variable or property is first initialized. Instead, they are called only when the value is set outside of an initialization context. &lt;/p&gt;
&lt;p&gt;A &lt;code&gt;willSet&lt;/code&gt; observer is called just before the value of the variable or property is set. The new value is passed to the &lt;code&gt;willSet&lt;/code&gt; observer as a constant, and therefore it can’t be changed in the implementation of the &lt;code&gt;willSet&lt;/code&gt; clause. The &lt;code&gt;didSet&lt;/code&gt; observer is called immediately after the new value is set. In contrast to the &lt;code&gt;willSet&lt;/code&gt; observer, the old value of the variable or property is passed to the &lt;code&gt;didSet&lt;/code&gt; observer in case you still need access to it. That said, if you assign a value to a variable or property within its own &lt;code&gt;didSet&lt;/code&gt; observer clause, that new value that you assign will replace the one that was just set and passed to the &lt;code&gt;willSet&lt;/code&gt; observer. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;setter name&lt;/em&gt; and enclosing parentheses in the &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; clauses are optional. If you provide setter names, they are used as the parameter names to the &lt;code&gt;willSet&lt;/code&gt; and &lt;code&gt;didSet&lt;/code&gt; observers. If you do not provide setter names, the default parameter name to the &lt;code&gt;willSet&lt;/code&gt; observer is &lt;code&gt;newValue&lt;/code&gt; and the default parameter name to the &lt;code&gt;didSet&lt;/code&gt; observer is &lt;code&gt;oldValue&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;didSet&lt;/code&gt; clause is optional when you provide a &lt;code&gt;willSet&lt;/code&gt; clause. Likewise, the &lt;code&gt;willSet&lt;/code&gt; clause is optional when you provide a &lt;code&gt;didSet&lt;/code&gt; clause. &lt;/p&gt;
&lt;p&gt;For more information and to see an example of how to use property observers, see Property Observers. &lt;/p&gt;
&lt;h4&gt;Class and Static Variable Properties&lt;/h4&gt;
&lt;p&gt;To declare a class computed property, mark the declaration with the &lt;code&gt;class&lt;/code&gt; declaration modifier. To declare a static variable property, mark the declaration with the &lt;code&gt;static&lt;/code&gt; declaration modifier. Class and static properties are discussed in Type Properties. &lt;/p&gt;
&lt;p&gt;Grammar of a variable declaration &lt;/p&gt;
&lt;p&gt;variable-declaration  → pattern-initializer-list­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-name­type-annotation­code-block­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-name­type-annotation­getter-setter-block­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-name­type-annotation­getter-setter-keyword-block­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-name­type-annotation­willSet-didSet-block­&lt;/p&gt;
&lt;p&gt;variable-declaration-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;var­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;variable-name  → identifier­&lt;/p&gt;
&lt;p&gt;getter-setter-block  → &lt;code&gt;{­&lt;/code&gt;setter-clause­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-setter-block  → &lt;code&gt;{­&lt;/code&gt;getter-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-clause  → attributes­opt­&lt;code&gt;get­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;setter-clause  → attributes­opt­&lt;code&gt;set­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;setter-name  → &lt;code&gt;(­&lt;/code&gt;identifier­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-setter-keyword-block  → &lt;code&gt;{­&lt;/code&gt;setter-keyword-clause­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-setter-keyword-block  → &lt;code&gt;{­&lt;/code&gt;getter-keyword-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-keyword-clause  → attributes­opt­&lt;code&gt;get­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;setter-keyword-clause  → attributes­opt­&lt;code&gt;set­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;willSet-didSet-block  → &lt;code&gt;{­&lt;/code&gt;didSet-clause­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;willSet-didSet-block  → &lt;code&gt;{­&lt;/code&gt;willSet-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;willSet-clause  → attributes­opt­&lt;code&gt;willSet­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;didSet-clause  → attributes­opt­&lt;code&gt;didSet­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;h3&gt;Type Alias Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;type alias declaration&lt;/em&gt; introduces a named alias of an existing type into your program. Type alias declarations are declared using the keyword &lt;code&gt;typealias&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;typealias &lt;em&gt;name&lt;/em&gt; = &lt;em&gt;existing type&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;After a type alias is declared, the aliased &lt;em&gt;name&lt;/em&gt; can be used instead of the &lt;em&gt;existing type&lt;/em&gt; everywhere in your program. The &lt;em&gt;existing type&lt;/em&gt; can be a named type or a compound type. Type aliases do not create new types; they simply allow a name to refer to an existing type. &lt;/p&gt;
&lt;p&gt;See also Protocol Associated Type Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a type alias declaration &lt;/p&gt;
&lt;p&gt;typealias-declaration  → typealias-assignment­&lt;/p&gt;
&lt;p&gt;typealias-head  → attributes­opt­typealias-name­&lt;/p&gt;
&lt;p&gt;typealias-name  → identifier­&lt;/p&gt;
&lt;p&gt;typealias-assignment  → &lt;code&gt;=­&lt;/code&gt;type­&lt;/p&gt;
&lt;h3&gt;Function Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;function declaration&lt;/em&gt; introduces a function or method into your program. A function declared in the context of class, structure, enumeration, or protocol is referred to as a &lt;em&gt;method&lt;/em&gt;. Function declarations are declared using the keyword &lt;code&gt;func&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;func &lt;em&gt;function name&lt;/em&gt;(&lt;em&gt;parameters&lt;/em&gt;) -&amp;gt; &lt;em&gt;return type&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If the function has a return type of &lt;code&gt;Void&lt;/code&gt;, the return type can be omitted as follows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;func &lt;em&gt;function name&lt;/em&gt;(&lt;em&gt;parameters&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The type of each parameter must be included—it can’t be inferred. Although the parameters to a function are constants by default, you can write &lt;code&gt;let&lt;/code&gt; in front of a parameter’s name to emphasize this behavior. Write &lt;code&gt;var&lt;/code&gt; in front of a parameter’s name to make it a variable, scoping any changes made to the variable just to the function body, or write &lt;code&gt;inout&lt;/code&gt; to make those changes also apply to the argument that was passed in the caller’s scope. For a discussion of in-out parameters, see In-Out Parameters. &lt;/p&gt;
&lt;p&gt;Functions can return multiple values using a tuple type as the return type of the function. &lt;/p&gt;
&lt;p&gt;A function definition can appear inside another function declaration. This kind of function is known as a &lt;em&gt;nested function&lt;/em&gt;. For a discussion of nested functions, see Nested Functions. &lt;/p&gt;
&lt;h4&gt;Parameter Names&lt;/h4&gt;
&lt;p&gt;Function parameters are a comma separated list where each parameter has one of several forms. The order of arguments in a function call must match the order of parameters in the function’s declaration. The simplest entry in a parameter list has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;parameter name&lt;/em&gt;: &lt;em&gt;parameter type&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For function parameters, the parameter name is used within the function body, but is not used when calling the function. For method parameters, the parameter name is used as within the function body, and is also used as a label for the argument when calling the method. The name of a method’s first parameter is used only within the function body, like the parameter of a function. For example: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func f(x: Int, y: String) -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return y \+ String(x)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;f(7, "hello") // x and y have no name&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class C {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func f(x: Int, y: String) -&amp;gt; String {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return y \+ String(x)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let c = C()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;c.f(7, y: "hello") // x has no name, y has a name&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can override the default behavior for how parameter names are used with one of the following forms: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;external parameter name&lt;/em&gt; &lt;em&gt;local parameter name&lt;/em&gt;: &lt;em&gt;parameter type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;h2&gt;&lt;em&gt;parameter name&lt;/em&gt;: &lt;em&gt;parameter type&lt;/em&gt;&lt;/h2&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;_ &lt;em&gt;local parameter name&lt;/em&gt;: &lt;em&gt;parameter type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A second name before the local parameter name gives the parameter an external name, which can be different than the local parameter name. The external parameter name must be used when the function is called. The corresponding argument must have the external name in function or method calls. &lt;/p&gt;
&lt;p&gt;A hash symbol (&lt;code&gt;#&lt;/code&gt;) before a parameter name indicates that the name should be used as both an external and a local parameter name. It has the same meaning as writing the local parameter name twice. The corresponding argument must have this name in function or method calls. &lt;/p&gt;
&lt;p&gt;An underscore (&lt;code&gt;_&lt;/code&gt;) before a local parameter name gives that parameter no name to be used in function calls. The corresponding argument must have no name in function or method calls. &lt;/p&gt;
&lt;h4&gt;Special Kinds of Parameters&lt;/h4&gt;
&lt;p&gt;Parameters can be ignored, take a variable number of values, and provide default values using the following forms: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;_ : &lt;em&gt;parameter type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;parameter name&lt;/em&gt;: &lt;em&gt;parameter type&lt;/em&gt;...&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;parameter name&lt;/em&gt;: &lt;em&gt;parameter type&lt;/em&gt; = &lt;em&gt;default argument value&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A parameter named with an underscore (&lt;code&gt;_&lt;/code&gt;) is explicitly ignored and can’t be accessed within the body of the function. &lt;/p&gt;
&lt;p&gt;A parameter with a base type name followed immediately by three dots (&lt;code&gt;...&lt;/code&gt;) is understood as a variadic parameter. A function can have at most one variadic parameter, which must be its last parameter. A variadic parameter is treated as an array that contains elements of the base type name. For instance, the variadic parameter &lt;code&gt;Int...&lt;/code&gt; is treated as &lt;code&gt;[Int]&lt;/code&gt;. For an example that uses a variadic parameter, see Variadic Parameters. &lt;/p&gt;
&lt;p&gt;A parameter with an equals sign (&lt;code&gt;=&lt;/code&gt;) and an expression after its type is understood to have a default value of the given expression. The given expression is evaluated when the function is called. If the parameter is omitted when calling the function, the default value is used instead. If the parameter is not omitted, it must have its name in the function call. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func f(x: Int = 42) -&amp;gt; Int { return x }&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;f() // Valid, uses default value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;f(x: 7) // Valid, name and value provided&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;f(7) // Invalid, value provided without its name&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Special Kinds of Methods&lt;/h4&gt;
&lt;p&gt;Methods on an enumeration or a structure that modify &lt;code&gt;self&lt;/code&gt; must be marked with the &lt;code&gt;mutating&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;p&gt;Methods that override a superclass method must be marked with the &lt;code&gt;override&lt;/code&gt; declaration modifier. It’s a compile-time error to override a method without the &lt;code&gt;override&lt;/code&gt; modifier or to use the &lt;code&gt;override&lt;/code&gt; modifier on a method that doesn’t override a superclass method. &lt;/p&gt;
&lt;p&gt;Methods associated with a type rather than an instance of a type must be marked with the &lt;code&gt;static&lt;/code&gt; declaration modifier for enumerations and structures or the &lt;code&gt;class&lt;/code&gt; declaration modifier for classes. &lt;/p&gt;
&lt;h4&gt;Curried Functions&lt;/h4&gt;
&lt;p&gt;You can rewrite a function that takes multiple parameters as an equivalent function that takes a single parameter and returns a function. The returned function takes the next parameter and returns another function. This continues until there are no remaining parameters, at which point the last function returns the return value of the original multiparameter function. The rewritten function is known as a &lt;em&gt;curried function&lt;/em&gt;. For example, you can rewrite the &lt;code&gt;addTwoInts&lt;/code&gt; function as the equivalent &lt;code&gt;addTwoIntsCurried&lt;/code&gt; function: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func addTwoInts(a: Int, b: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return a \+ b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func addTwoIntsCurried(a: Int) -&amp;gt; (Int -&amp;gt; Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func addTheOtherInt(b: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return a \+ b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return addTheOtherInt&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;addTwoInts&lt;/code&gt; function takes two integers and returns the result of adding them together. The &lt;code&gt;addTwoIntsCurried&lt;/code&gt; function takes a single integer, and returns another function that takes the second integer and adds it to the first. (The nested function captures the value of the first integer argument from the enclosing function.) &lt;/p&gt;
&lt;p&gt;In Swift, you can write a curried function more concisely using the following syntax: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;func &lt;em&gt;function name&lt;/em&gt;(&lt;em&gt;parameter&lt;/em&gt;)(&lt;em&gt;parameter&lt;/em&gt;) -&amp;gt; &lt;em&gt;return type&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;For example, the following two declarations are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func addTwoIntsCurried(a: Int)(b: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return a \+ b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func addTwoIntsCurried(a: Int) -&amp;gt; (Int -&amp;gt; Int) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func addTheOtherInt(b: Int) -&amp;gt; Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return a \+ b&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return addTheOtherInt&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In order to use the &lt;code&gt;addTwoIntsCurried&lt;/code&gt; function in the same way as the noncurried &lt;code&gt;addTwoInts&lt;/code&gt; function, you must call the &lt;code&gt;addTwoIntsCurried&lt;/code&gt; function with the first integer argument and then call its returned function with the second integer argument: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;addTwoInts(4, 5)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 9&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;addTwoIntsCurried(4)(5)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 9&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Although you must provide the arguments to a noncurried function all at once in a single call, you can use the curried form of a function to provide arguments in several function calls, one at a time (even in different places in your code). This is known as &lt;em&gt;partial function application&lt;/em&gt;. For example, you can apply the &lt;code&gt;addTwoIntsCurried&lt;/code&gt; function to an integer argument &lt;code&gt;1&lt;/code&gt; and assign the result to the constant &lt;code&gt;plusOne&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let plusOne = addTwoIntsCurried(1)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// plusOne is a function of type Int -&amp;gt; Int&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because &lt;code&gt;plusOne&lt;/code&gt; refers to the &lt;code&gt;addTwoIntsCurried&lt;/code&gt; function with its argument bound as the value &lt;code&gt;1&lt;/code&gt;, calling &lt;code&gt;plusOne&lt;/code&gt; with an integer argument simply adds &lt;code&gt;1&lt;/code&gt; to the argument. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;plusOne(10)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// returns a value of 11&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a function declaration &lt;/p&gt;
&lt;p&gt;function-declaration  → function-name­generic-parameter-clause­opt­function-body­&lt;/p&gt;
&lt;p&gt;function-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;func­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;function-name  →  identifier­ operator­&lt;/p&gt;
&lt;p&gt;function-signature  → function-result­opt­&lt;/p&gt;
&lt;p&gt;function-result  → &lt;code&gt;-&amp;gt;­&lt;/code&gt;attributes­opt­type­&lt;/p&gt;
&lt;p&gt;function-body  → code-block­&lt;/p&gt;
&lt;p&gt;parameter-clauses  → parameter-clauses­opt­&lt;/p&gt;
&lt;p&gt;parameter-clause  →  &lt;code&gt;(­``)­&lt;/code&gt; &lt;code&gt;(­&lt;/code&gt;parameter-list­&lt;code&gt;...­&lt;/code&gt;opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;parameter-list  →  parameter­ parameter-list­&lt;/p&gt;
&lt;p&gt;parameter  → &lt;code&gt;inout­&lt;/code&gt;opt­&lt;code&gt;let­&lt;/code&gt;opt­&lt;code&gt;#­&lt;/code&gt;opt­local-parameter-name­type-annotation­default-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;parameter  → &lt;code&gt;inout­&lt;/code&gt;opt­&lt;code&gt;var­``#­&lt;/code&gt;opt­local-parameter-name­type-annotation­default-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;parameter  → attributes­opt­type­&lt;/p&gt;
&lt;p&gt;external-parameter-name  →  identifier­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;local-parameter-name  →  identifier­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;default-argument-clause  → &lt;code&gt;=­&lt;/code&gt;expression­&lt;/p&gt;
&lt;h3&gt;Enumeration Declaration&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;enumeration declaration&lt;/em&gt; introduces a named enumeration type into your program. &lt;/p&gt;
&lt;p&gt;Enumeration declarations have two basic forms and are declared using the keyword &lt;code&gt;enum&lt;/code&gt;. The body of an enumeration declared using either form contains zero or more values—called &lt;em&gt;enumeration cases&lt;/em&gt;—and any number of declarations, including computed properties, instance methods, static methods, initializers, type aliases, and even other enumeration, structure, and class declarations. Enumeration declarations can’t contain deinitializer or protocol declarations. &lt;/p&gt;
&lt;p&gt;Enumeration types can adopt any number of protocols, but can’t inherit from classes, structures, or other enumerations. &lt;/p&gt;
&lt;p&gt;Unlike classes and structures, enumeration types do not have an implicitly provided default initializer; all initializers must be declared explicitly. Initializers can delegate to other initializers in the enumeration, but the initialization process is complete only after an initializer assigns one of the enumeration cases to &lt;code&gt;self&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Like structures but unlike classes, enumerations are value types; instances of an enumeration are copied when assigned to variables or constants, or when passed as arguments to a function call. For information about value types, see Structures and Enumerations Are Value Types. &lt;/p&gt;
&lt;p&gt;You can extend the behavior of an enumeration type with an extension declaration, as discussed in Extension Declaration. &lt;/p&gt;
&lt;h4&gt;Enumerations with Cases of Any Type&lt;/h4&gt;
&lt;p&gt;The following form declares an enumeration type that contains enumeration cases of any type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;enum &lt;em&gt;enumeration name&lt;/em&gt;: &lt;em&gt;adopted protocols&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;enumeration case 1&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;enumeration case 2&lt;/em&gt;(&lt;em&gt;associated value types&lt;/em&gt;)&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Enumerations declared in this form are sometimes called &lt;em&gt;discriminated unions&lt;/em&gt; in other programming languages. &lt;/p&gt;
&lt;p&gt;In this form, each case block consists of the keyword &lt;code&gt;case&lt;/code&gt; followed by one or more enumeration cases, separated by commas. The name of each case must be unique. Each case can also specify that it stores values of a given type. These types are specified in the &lt;em&gt;associated value types&lt;/em&gt; tuple, immediately following the name of the case. For more information and to see examples of cases with associated value types, see Associated Values. &lt;/p&gt;
&lt;h4&gt;Enumerations with Cases of a Raw-Value Type&lt;/h4&gt;
&lt;p&gt;The following form declares an enumeration type that contains enumeration cases of the same basic type: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;enum &lt;em&gt;enumeration name&lt;/em&gt;: &lt;em&gt;raw-value type&lt;/em&gt;, &lt;em&gt;adopted protocols&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;enumeration case 1&lt;/em&gt; = &lt;em&gt;raw value 1&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;case &lt;em&gt;enumeration case 2&lt;/em&gt; = &lt;em&gt;raw value 2&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In this form, each case block consists of the keyword &lt;code&gt;case&lt;/code&gt;, followed by one or more enumeration cases, separated by commas. Unlike the cases in the first form, each case has an underlying value, called a &lt;em&gt;raw value&lt;/em&gt;, of the same basic type. The type of these values is specified in the &lt;em&gt;raw-value type&lt;/em&gt; and must represent an integer, floating-point number, string, or single character. In particular, the &lt;em&gt;raw-value type&lt;/em&gt; must conform to the &lt;code&gt;Equatable&lt;/code&gt; protocol and one of the following literal-convertible protocols: &lt;code&gt;IntegerLiteralConvertible&lt;/code&gt; for integer literals, &lt;code&gt;FloatingPointLiteralConvertible&lt;/code&gt; for floating-point literals, &lt;code&gt;StringLiteralConvertible&lt;/code&gt; for string literals that contain any number of characters, and &lt;code&gt;ExtendedGraphemeClusterLiteralConvertible&lt;/code&gt; for string literals that contain only a single character. &lt;/p&gt;
&lt;p&gt;Each case must have a unique name and be assigned a unique raw value. If the raw-value type is specified as &lt;code&gt;Int&lt;/code&gt; and you don’t assign a value to the cases explicitly, they are implicitly assigned the values &lt;code&gt;0&lt;/code&gt;, &lt;code&gt;1&lt;/code&gt;, &lt;code&gt;2&lt;/code&gt;, and so on. Each unassigned case of type &lt;code&gt;Int&lt;/code&gt; is implicitly assigned a raw value that is automatically incremented from the raw value of the previous case. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;enum ExampleEnum: Int {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case A, B, C = 5, D&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the above example, the raw value of &lt;code&gt;ExampleEnum.A&lt;/code&gt; is &lt;code&gt;0&lt;/code&gt; and the value of &lt;code&gt;ExampleEnum.B&lt;/code&gt; is &lt;code&gt;1&lt;/code&gt;. And because the value of &lt;code&gt;ExampleEnum.C&lt;/code&gt; is explicitly set to &lt;code&gt;5&lt;/code&gt;, the value of &lt;code&gt;ExampleEnum.D&lt;/code&gt; is automatically incremented from &lt;code&gt;5&lt;/code&gt; and is therefore &lt;code&gt;6&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Enumerations that have cases of a raw-value type implicitly conform to the &lt;code&gt;RawRepresentable&lt;/code&gt; protocol, defined in the Swift standard library. As a result, they have a &lt;code&gt;rawValue&lt;/code&gt; property and a failable initializer with the signature &lt;code&gt;init?(rawValue: RawValue)&lt;/code&gt;. You can use the &lt;code&gt;rawValue&lt;/code&gt; property to access the raw value of an enumeration case, as in &lt;code&gt;ExampleEnum.B.rawValue&lt;/code&gt;. You can also use a raw value to find a corresponding case, if there is one, by calling the enumeration’s failable initializer, as in &lt;code&gt;ExampleEnum(rawValue: 5)&lt;/code&gt;, which returns an optional case. For more information and to see examples of cases with raw-value types, see Raw Values. &lt;/p&gt;
&lt;h4&gt;Accessing Enumeration Cases&lt;/h4&gt;
&lt;p&gt;To reference the case of an enumeration type, use dot (&lt;code&gt;.&lt;/code&gt;) syntax, as in &lt;code&gt;EnumerationType.EnumerationCase&lt;/code&gt;. When the enumeration type can be inferred from context, you can omit it (the dot is still required), as described in Enumeration Syntax and Implicit Member Expression. &lt;/p&gt;
&lt;p&gt;To check the values of enumeration cases, use a &lt;code&gt;switch&lt;/code&gt; statement, as shown in Matching Enumeration Values with a Switch Statement. The enumeration type is pattern-matched against the enumeration case patterns in the case blocks of the &lt;code&gt;switch&lt;/code&gt; statement, as described in Enumeration Case Pattern. &lt;/p&gt;
&lt;p&gt;Grammar of an enumeration declaration &lt;/p&gt;
&lt;p&gt;enum-declaration  → attributes­opt­union-style-enum­&lt;/p&gt;
&lt;p&gt;enum-declaration  → attributes­opt­raw-value-style-enum­&lt;/p&gt;
&lt;p&gt;union-style-enum  → &lt;code&gt;enum­&lt;/code&gt;generic-parameter-clause­opt­type-inheritance-clause­opt­&lt;code&gt;{­&lt;/code&gt;union-style-enum-members­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;union-style-enum-members  → union-style-enum-members­opt­&lt;/p&gt;
&lt;p&gt;union-style-enum-member  →  declaration­ union-style-enum-case-clause­&lt;/p&gt;
&lt;p&gt;union-style-enum-case-clause  → attributes­opt­&lt;code&gt;case­&lt;/code&gt;union-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;union-style-enum-case-list  →  union-style-enum-case­ union-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;union-style-enum-case  → tuple-type­opt­&lt;/p&gt;
&lt;p&gt;enum-name  → identifier­&lt;/p&gt;
&lt;p&gt;enum-case-name  → identifier­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum  → &lt;code&gt;enum­&lt;/code&gt;generic-parameter-clause­opt­type-inheritance-clause­&lt;code&gt;{­&lt;/code&gt;raw-value-style-enum-members­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-members  → raw-value-style-enum-members­opt­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-member  →  declaration­ raw-value-style-enum-case-clause­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-case-clause  → attributes­opt­&lt;code&gt;case­&lt;/code&gt;raw-value-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-case-list  →  raw-value-style-enum-case­ raw-value-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-case  → raw-value-assignment­opt­&lt;/p&gt;
&lt;p&gt;raw-value-assignment  → &lt;code&gt;=­&lt;/code&gt;raw-value-literal­&lt;/p&gt;
&lt;p&gt;raw-value-literal  →  numeric-literal­ string-literal­ boolean-literal­&lt;/p&gt;
&lt;h3&gt;Structure Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;structure declaration&lt;/em&gt; introduces a named structure type into your program. Structure declarations are declared using the keyword &lt;code&gt;struct&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;struct &lt;em&gt;structure name&lt;/em&gt;: &lt;em&gt;adopted protocols&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;declarations&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The body of a structure contains zero or more &lt;em&gt;declarations&lt;/em&gt;. These &lt;em&gt;declarations&lt;/em&gt; can include both stored and computed properties, static properties, instance methods, static methods, initializers, subscripts, type aliases, and even other structure, class, and enumeration declarations. Structure declarations can’t contain deinitializer or protocol declarations. For a discussion and several examples of structures that include various kinds of declarations, see Classes and Structures. &lt;/p&gt;
&lt;p&gt;Structure types can adopt any number of protocols, but can’t inherit from classes, enumerations, or other structures. &lt;/p&gt;
&lt;p&gt;There are three ways create an instance of a previously declared structure: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Call one of the initializers declared within the structure, as described in Initializers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If no initializers are declared, call the structure’s memberwise initializer, as described in Memberwise Initializers for Structure Types. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If no initializers are declared, and all properties of the structure declaration were given initial values, call the structure’s default initializer, as described in Default Initializers. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The process of initializing a structure’s declared properties is described in Initialization. &lt;/p&gt;
&lt;p&gt;Properties of a structure instance can be accessed using dot (&lt;code&gt;.&lt;/code&gt;) syntax, as described in Accessing Properties. &lt;/p&gt;
&lt;p&gt;Structures are value types; instances of a structure are copied when assigned to variables or constants, or when passed as arguments to a function call. For information about value types, see Structures and Enumerations Are Value Types. &lt;/p&gt;
&lt;p&gt;You can extend the behavior of a structure type with an extension declaration, as discussed in Extension Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a structure declaration &lt;/p&gt;
&lt;p&gt;struct-declaration  → attributes­opt­struct-name­generic-parameter-clause­opt­type-inheritance-clause­opt­struct-body­&lt;/p&gt;
&lt;p&gt;struct-name  → identifier­&lt;/p&gt;
&lt;p&gt;struct-body  → &lt;code&gt;{­&lt;/code&gt;declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Class Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;class declaration&lt;/em&gt; introduces a named class type into your program. Class declarations are declared using the keyword &lt;code&gt;class&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;class &lt;em&gt;class name&lt;/em&gt;: &lt;em&gt;superclass&lt;/em&gt;, &lt;em&gt;adopted protocols&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;declarations&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The body of a class contains zero or more &lt;em&gt;declarations&lt;/em&gt;. These &lt;em&gt;declarations&lt;/em&gt; can include both stored and computed properties, instance methods, class methods, initializers, a single deinitializer, subscripts, type aliases, and even other class, structure, and enumeration declarations. Class declarations can’t contain protocol declarations. For a discussion and several examples of classes that include various kinds of declarations, see Classes and Structures. &lt;/p&gt;
&lt;p&gt;A class type can inherit from only one parent class, its &lt;em&gt;superclass&lt;/em&gt;, but can adopt any number of protocols. The &lt;em&gt;superclass&lt;/em&gt; appears first after the &lt;em&gt;class name&lt;/em&gt; and colon, followed by any &lt;em&gt;adopted protocols&lt;/em&gt;. Generic classes can inherit from other generic and nongeneric classes, but a nongeneric class can inherit only from other nongeneric classes. When you write the name of a generic superclass class after the colon, you must include the full name of that generic class, including its generic parameter clause. &lt;/p&gt;
&lt;p&gt;As discussed in Initializer Declaration, classes can have designated and convenience initializers. The designated initializer of a class must initialize all of the class’s declared properties and it must do so before calling any of its superclass’s designated initializers. &lt;/p&gt;
&lt;p&gt;A class can override properties, methods, subscripts, and initializers of its superclass. Overridden properties, methods, subscripts, and designated initializers must be marked with the &lt;code&gt;override&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;p&gt;To require that subclasses implement a superclass’s initializer, mark the superclass’s initializer with the &lt;code&gt;required&lt;/code&gt; declaration modifier. The subclass’s implementation of that initializer must also be marked with the &lt;code&gt;required&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;p&gt;Although properties and methods declared in the &lt;em&gt;superclass&lt;/em&gt; are inherited by the current class, designated initializers declared in the &lt;em&gt;superclass&lt;/em&gt; are not. That said, if the current class overrides all of the superclass’s designated initializers, it inherits the superclass’s convenience initializers. Swift classes do not inherit from a universal base class. &lt;/p&gt;
&lt;p&gt;There are two ways create an instance of a previously declared class: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Call one of the initializers declared within the class, as described in Initializers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If no initializers are declared, and all properties of the class declaration were given initial values, call the class’s default initializer, as described in Default Initializers. &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Access properties of a class instance with dot (&lt;code&gt;.&lt;/code&gt;) syntax, as described in Accessing Properties. &lt;/p&gt;
&lt;p&gt;Classes are reference types; instances of a class are referred to, rather than copied, when assigned to variables or constants, or when passed as arguments to a function call. For information about reference types, see Structures and Enumerations Are Value Types. &lt;/p&gt;
&lt;p&gt;You can extend the behavior of a class type with an extension declaration, as discussed in Extension Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a class declaration &lt;/p&gt;
&lt;p&gt;class-declaration  → attributes­opt­class-name­generic-parameter-clause­opt­type-inheritance-clause­opt­class-body­&lt;/p&gt;
&lt;p&gt;class-name  → identifier­&lt;/p&gt;
&lt;p&gt;class-body  → &lt;code&gt;{­&lt;/code&gt;declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Protocol Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;protocol declaration&lt;/em&gt; introduces a named protocol type into your program. Protocol declarations are declared using the keyword &lt;code&gt;protocol&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;protocol &lt;em&gt;protocol name&lt;/em&gt;: &lt;em&gt;inherited protocols&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;protocol member declarations&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The body of a protocol contains zero or more &lt;em&gt;protocol member declarations&lt;/em&gt;, which describe the conformance requirements that any type adopting the protocol must fulfill. In particular, a protocol can declare that conforming types must implement certain properties, methods, initializers, and subscripts. Protocols can also declare special kinds of type aliases, called &lt;em&gt;associated types&lt;/em&gt;, that can specify relationships among the various declarations of the protocol. The &lt;em&gt;protocol member declarations&lt;/em&gt; are discussed in detail below. &lt;/p&gt;
&lt;p&gt;Protocol types can inherit from any number of other protocols. When a protocol type inherits from other protocols, the set of requirements from those other protocols are aggregated, and any type that inherits from the current protocol must conform to all those requirements. For an example of how to use protocol inheritance, see Protocol Inheritance. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;You can also aggregate the conformance requirements of multiple protocols using protocol composition types, as described in Protocol Composition Type and Protocol Composition. &lt;/p&gt;
&lt;p&gt;You can add protocol conformance to a previously declared type by adopting the protocol in an extension declaration of that type. In the extension, you must implement all of the adopted protocol’s requirements. If the type already implements all of the requirements, you can leave the body of the extension declaration empty. &lt;/p&gt;
&lt;p&gt;By default, types that conform to a protocol must implement all properties, methods, and subscripts declared in the protocol. That said, you can mark these protocol member declarations with the &lt;code&gt;optional&lt;/code&gt; declaration modifier to specify that their implementation by a conforming type is optional. The &lt;code&gt;optional&lt;/code&gt; modifier can be applied only to protocols that are marked with the &lt;code&gt;objc&lt;/code&gt; attribute. As a result, only class types can adopt and conform to a protocol that contains optional member requirements. For more information about how to use the &lt;code&gt;optional&lt;/code&gt; declaration modifier and for guidance about how to access optional protocol members—for example, when you’re not sure whether a conforming type implements them—see Optional Protocol Requirements. &lt;/p&gt;
&lt;p&gt;To restrict the adoption of a protocol to class types only, mark the protocol with the &lt;code&gt;class&lt;/code&gt; requirement by writing the &lt;code&gt;class&lt;/code&gt; keyword as the first item in the &lt;em&gt;inherited protocols&lt;/em&gt; list after the colon. For example, the following protocol can be adopted only by class types: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;protocol SomeProtocol: class {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/* Protocol members go here */&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Any protocol that inherits from a protocol that’s marked with the &lt;code&gt;class&lt;/code&gt; requirement can likewise be adopted only by class types. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If a protocol is marked with the &lt;code&gt;objc&lt;/code&gt; attribute, the &lt;code&gt;class&lt;/code&gt; requirement is implicitly applied to that protocol; there’s no need to mark the protocol with the &lt;code&gt;class&lt;/code&gt; requirement explicitly. &lt;/p&gt;
&lt;p&gt;Protocols are named types, and thus they can appear in all the same places in your code as other named types, as discussed in Protocols as Types. However, you can’t construct an instance of a protocol, because protocols do not actually provide the implementations for the requirements they specify. &lt;/p&gt;
&lt;p&gt;You can use protocols to declare which methods a delegate of a class or structure should implement, as described in Delegation. &lt;/p&gt;
&lt;p&gt;Grammar of a protocol declaration &lt;/p&gt;
&lt;p&gt;protocol-declaration  → attributes­opt­protocol-name­type-inheritance-clause­opt­protocol-body­&lt;/p&gt;
&lt;p&gt;protocol-name  → identifier­&lt;/p&gt;
&lt;p&gt;protocol-body  → &lt;code&gt;{­&lt;/code&gt;protocol-member-declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-property-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-method-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-initializer-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-subscript-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-associated-type-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declarations  → protocol-member-declarations­opt­&lt;/p&gt;
&lt;h4&gt;Protocol Property Declaration&lt;/h4&gt;
&lt;p&gt;Protocols declare that conforming types must implement a property by including a &lt;em&gt;protocol property declaration&lt;/em&gt; in the body of the protocol declaration. Protocol property declarations have a special form of a variable declaration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;var &lt;em&gt;property name&lt;/em&gt;: &lt;em&gt;type&lt;/em&gt; { get set }&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;As with other protocol member declarations, these property declarations declare only the getter and setter requirements for types that conform to the protocol. As a result, you don’t implement the getter or setter directly in the protocol in which it is declared. &lt;/p&gt;
&lt;p&gt;The getter and setter requirements can be satisfied by a conforming type in a variety of ways. If a property declaration includes both the &lt;code&gt;get&lt;/code&gt; and &lt;code&gt;set&lt;/code&gt; keywords, a conforming type can implement it with a stored variable property or a computed property that is both readable and writeable (that is, one that implements both a getter and a setter). However, that property declaration can’t be implemented as a constant property or a read-only computed property. If a property declaration includes only the &lt;code&gt;get&lt;/code&gt; keyword, it can be implemented as any kind of property. For examples of conforming types that implement the property requirements of a protocol, see Property Requirements. &lt;/p&gt;
&lt;p&gt;See also Variable Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a protocol property declaration &lt;/p&gt;
&lt;p&gt;protocol-property-declaration  → variable-name­type-annotation­getter-setter-keyword-block­&lt;/p&gt;
&lt;h4&gt;Protocol Method Declaration&lt;/h4&gt;
&lt;p&gt;Protocols declare that conforming types must implement a method by including a protocol method declaration in the body of the protocol declaration. Protocol method declarations have the same form as function declarations, with two exceptions: They don’t include a function body, and you can’t provide any default parameter values as part of the function declaration. For examples of conforming types that implement the method requirements of a protocol, see Method Requirements. &lt;/p&gt;
&lt;p&gt;To declare a class or static method requirement in a protocol declaration, mark the method declaration with the &lt;code&gt;class&lt;/code&gt; declaration modifier. Classes that implement this method also declare the method with the &lt;code&gt;class&lt;/code&gt; modifier. Structures that implement it must declare the method with the &lt;code&gt;static&lt;/code&gt; declaration modifier instead. If you’re implementing the method in an extension, use the &lt;code&gt;class&lt;/code&gt; modifier if you’re extending a class and the &lt;code&gt;static&lt;/code&gt; modifier if you’re extending a structure. &lt;/p&gt;
&lt;p&gt;See also Function Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a protocol method declaration &lt;/p&gt;
&lt;p&gt;protocol-method-declaration  → function-name­generic-parameter-clause­opt­function-signature­&lt;/p&gt;
&lt;h4&gt;Protocol Initializer Declaration&lt;/h4&gt;
&lt;p&gt;Protocols declare that conforming types must implement an initializer by including a protocol initializer declaration in the body of the protocol declaration. Protocol initializer declarations have the same form as initializer declarations, except they don’t include the initializer’s body. &lt;/p&gt;
&lt;p&gt;A conforming type can satisfy a nonfailable protocol initializer requirement by implementing a nonfailable initializer or an &lt;code&gt;init!&lt;/code&gt; failable initializer. A conforming type can satisfy a failable protocol initializer requirement by implementing any kind of initializer. &lt;/p&gt;
&lt;p&gt;When a class implements an initializer to satisfy a protocol’s initializer requirement, the initializer must be marked with the &lt;code&gt;required&lt;/code&gt; declaration modifier if the class is not already marked with the &lt;code&gt;final&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;p&gt;See also Initializer Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a protocol initializer declaration &lt;/p&gt;
&lt;p&gt;protocol-initializer-declaration  → generic-parameter-clause­opt­parameter-clause­&lt;/p&gt;
&lt;h4&gt;Protocol Subscript Declaration&lt;/h4&gt;
&lt;p&gt;Protocols declare that conforming types must implement a subscript by including a protocol subscript declaration in the body of the protocol declaration. Protocol subscript declarations have a special form of a subscript declaration: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;subscript (&lt;em&gt;parameters&lt;/em&gt;) -&amp;gt; &lt;em&gt;return type&lt;/em&gt; { get set }&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Subscript declarations only declare the minimum getter and setter implementation requirements for types that conform to the protocol. If the subscript declaration includes both the &lt;code&gt;get&lt;/code&gt; and &lt;code&gt;set&lt;/code&gt; keywords, a conforming type must implement both a getter and a setter clause. If the subscript declaration includes only the &lt;code&gt;get&lt;/code&gt; keyword, a conforming type must implement &lt;em&gt;at least&lt;/em&gt; a getter clause and optionally can implement a setter clause. &lt;/p&gt;
&lt;p&gt;See also Subscript Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a protocol subscript declaration &lt;/p&gt;
&lt;p&gt;protocol-subscript-declaration  → subscript-result­getter-setter-keyword-block­&lt;/p&gt;
&lt;h4&gt;Protocol Associated Type Declaration&lt;/h4&gt;
&lt;p&gt;Protocols declare associated types using the keyword &lt;code&gt;typealias&lt;/code&gt;. An associated type provides an alias for a type that is used as part of a protocol’s declaration. Associated types are similar to type parameters in generic parameter clauses, but they’re associated with &lt;code&gt;Self&lt;/code&gt; in the protocol in which they’re declared. In that context, &lt;code&gt;Self&lt;/code&gt; refers to the eventual type that conforms to the protocol. For more information and examples, see Associated Types. &lt;/p&gt;
&lt;p&gt;See also Type Alias Declaration. &lt;/p&gt;
&lt;p&gt;Grammar of a protocol associated type declaration &lt;/p&gt;
&lt;p&gt;protocol-associated-type-declaration  → type-inheritance-clause­opt­typealias-assignment­opt­&lt;/p&gt;
&lt;h3&gt;Initializer Declaration&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;initializer declaration&lt;/em&gt; introduces an initializer for a class, structure, or enumeration into your program. Initializer declarations are declared using the keyword &lt;code&gt;init&lt;/code&gt; and have two basic forms. &lt;/p&gt;
&lt;p&gt;Structure, enumeration, and class types can have any number of initializers, but the rules and associated behavior for class initializers are different. Unlike structures and enumerations, classes have two kinds of initializers: designated initializers and convenience initializers, as described in Initialization. &lt;/p&gt;
&lt;p&gt;The following form declares initializers for structures, enumerations, and designated initializers of classes: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;init(&lt;em&gt;parameters&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A designated initializer of a class initializes all of the class’s properties directly. It can’t call any other initializers of the same class, and if the class has a superclass, it must call one of the superclass’s designated initializers. If the class inherits any properties from its superclass, one of the superclass’s designated initializers must be called before any of these properties can be set or modified in the current class. &lt;/p&gt;
&lt;p&gt;Designated initializers can be declared in the context of a class declaration only and therefore can’t be added to a class using an extension declaration. &lt;/p&gt;
&lt;p&gt;Initializers in structures and enumerations can call other declared initializers to delegate part or all of the initialization process. &lt;/p&gt;
&lt;p&gt;To declare convenience initializers for a class, mark the initializer declaration with the &lt;code&gt;convenience&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;convenience init(&lt;em&gt;parameters&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Convenience initializers can delegate the initialization process to another convenience initializer or to one of the class’s designated initializers. That said, the initialization processes must end with a call to a designated initializer that ultimately initializes the class’s properties. Convenience initializers can’t call a superclass’s initializers. &lt;/p&gt;
&lt;p&gt;You can mark designated and convenience initializers with the &lt;code&gt;required&lt;/code&gt; declaration modifier to require that every subclass implement the initializer. A subclass’s implementation of that initializer must also be marked with the &lt;code&gt;required&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;p&gt;By default, initializers declared in a superclass are not inherited by subclasses. That said, if a subclass initializes all of its stored properties with default values and doesn’t define any initializers of its own, it inherits all of the superclass’s initializers. If the subclass overrides all of the superclass’s designated initializers, it inherits the superclass’s convenience initializers. &lt;/p&gt;
&lt;p&gt;As with methods, properties, and subscripts, you need to mark overridden designated initializers with the &lt;code&gt;override&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;p&gt;Note &lt;/p&gt;
&lt;p&gt;If you mark an initializer with the &lt;code&gt;required&lt;/code&gt; declaration modifier, you don’t also mark the initializer with the &lt;code&gt;override&lt;/code&gt; modifier when you override the required initializer in a subclass. &lt;/p&gt;
&lt;p&gt;To see examples of initializers in various type declarations, see Initialization. &lt;/p&gt;
&lt;h4&gt;Failable Initializers&lt;/h4&gt;
&lt;p&gt;A &lt;em&gt;failable initializer&lt;/em&gt; is a type of initializer that produces an optional instance or an implicitly unwrapped optional instance of the type the initializer is declared on. As a result, a failable initializer can return &lt;code&gt;nil&lt;/code&gt; to indicate that initialization failed. &lt;/p&gt;
&lt;p&gt;To declare a failable initializer that produces an optional instance, append a question mark to the &lt;code&gt;init&lt;/code&gt; keyword in the initializer declaration (&lt;code&gt;init?&lt;/code&gt;). To declare a failable initializer that produces an implicitly unwrapped optional instance, append an exclamation mark instead (&lt;code&gt;init!&lt;/code&gt;). The example below shows an &lt;code&gt;init?&lt;/code&gt; failable initializer that produces an optional instance of a structure. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct SomeStruct {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let string: String&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// produces an optional instance of 'SomeStruct'&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init?(input: String) {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if input.isEmpty {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// discard 'self' and return 'nil'&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return nil&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;string = input&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You call an &lt;code&gt;init?&lt;/code&gt; failable initializer in the same way that you call a nonfailable initializer, except that you must deal with the optionality of the result. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;if let actualInstance = SomeStruct(input: "Hello") {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// do something with the instance of 'SomeStruct'&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;} else {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// initialization of 'SomeStruct' failed and the initializer returned 'nil'&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A failable initializer of a structure or an enumeration can return &lt;code&gt;nil&lt;/code&gt; at any point in the implementation of the initializer’s body. A failable initializer of a class, however, can return &lt;code&gt;nil&lt;/code&gt; only after all stored properties of that class are initialized and &lt;code&gt;self.init&lt;/code&gt; or &lt;code&gt;super.init&lt;/code&gt; is called (that is, any initializer delegation is performed). &lt;/p&gt;
&lt;p&gt;A failable initializer can delegate to any kind of initializer. A nonfailable initializer can delegate to another nonfailable initializer or to an &lt;code&gt;init!&lt;/code&gt; failable initializer. &lt;/p&gt;
&lt;p&gt;Initialization failure propagates through initializer delegation. Specifically, if a failable initializer delegates to an initializer that fails and returns &lt;code&gt;nil&lt;/code&gt;, then the initializer that delegated also fails and implicitly returns &lt;code&gt;nil&lt;/code&gt;. If a nonfailable initializer delegates to an &lt;code&gt;init!&lt;/code&gt; failable initializer that fails and returns &lt;code&gt;nil&lt;/code&gt;, then a runtime error is raised (as if you used the &lt;code&gt;!&lt;/code&gt; operator to unwrap an optional that has a &lt;code&gt;nil&lt;/code&gt; value). &lt;/p&gt;
&lt;p&gt;A failable designated initializer can be overridden in a subclass by any kind of designated initializer. A nonfailable designated initializer can be overridden in a subclass by a nonfailable designated initializer only. &lt;/p&gt;
&lt;p&gt;For more information and to see examples of failable initializers, see Failable Initializers. &lt;/p&gt;
&lt;p&gt;Grammar of an initializer declaration &lt;/p&gt;
&lt;p&gt;initializer-declaration  → generic-parameter-clause­opt­initializer-body­&lt;/p&gt;
&lt;p&gt;initializer-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;init­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;initializer-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;init­``?­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;initializer-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;init­``!­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;initializer-body  → code-block­&lt;/p&gt;
&lt;h3&gt;Deinitializer Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;deinitializer declaration&lt;/em&gt; declares a deinitializer for a class type. Deinitializers take no parameters and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;deinit {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A deinitializer is called automatically when there are no longer any references to a class object, just before the class object is deallocated. A deinitializer can be declared only in the body of a class declaration—but not in an extension of a class—and each class can have at most one. &lt;/p&gt;
&lt;p&gt;A subclass inherits its superclass’s deinitializer, which is implicitly called just before the subclass object is deallocated. The subclass object is not deallocated until all deinitializers in its inheritance chain have finished executing. &lt;/p&gt;
&lt;p&gt;Deinitializers are not called directly. &lt;/p&gt;
&lt;p&gt;For an example of how to use a deinitializer in a class declaration, see Deinitialization. &lt;/p&gt;
&lt;p&gt;Grammar of a deinitializer declaration &lt;/p&gt;
&lt;p&gt;deinitializer-declaration  → attributes­opt­&lt;code&gt;deinit­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;h3&gt;Extension Declaration&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;extension declaration&lt;/em&gt; allows you to extend the behavior of existing class, structure, and enumeration types. Extension declarations are declared using the keyword &lt;code&gt;extension&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;extension &lt;em&gt;type name&lt;/em&gt;: &lt;em&gt;adopted protocols&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;declarations&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The body of an extension declaration contains zero or more &lt;em&gt;declarations&lt;/em&gt;. These &lt;em&gt;declarations&lt;/em&gt; can include computed properties, computed static properties, instance methods, static and class methods, initializers, subscript declarations, and even class, structure, and enumeration declarations. Extension declarations can’t contain deinitializer or protocol declarations, stored properties, property observers, or other extension declarations. For a discussion and several examples of extensions that include various kinds of declarations, see Extensions. &lt;/p&gt;
&lt;p&gt;Extension declarations can add protocol conformance to an existing class, structure, and enumeration type in the &lt;em&gt;adopted protocols&lt;/em&gt;. Extension declarations can’t add class inheritance to an existing class, and therefore you can specify only a list of protocols after the &lt;em&gt;type name&lt;/em&gt; and colon. &lt;/p&gt;
&lt;p&gt;Properties, methods, and initializers of an existing type can’t be overridden in an extension of that type. &lt;/p&gt;
&lt;p&gt;Extension declarations can contain initializer declarations. That said, if the type you’re extending is defined in another module, an initializer declaration must delegate to an initializer already defined in that module to ensure members of that type are properly initialized. &lt;/p&gt;
&lt;p&gt;Grammar of an extension declaration &lt;/p&gt;
&lt;p&gt;extension-declaration  → type-identifier­type-inheritance-clause­opt­extension-body­&lt;/p&gt;
&lt;p&gt;extension-body  → &lt;code&gt;{­&lt;/code&gt;declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Subscript Declaration&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;subscript&lt;/em&gt; declaration allows you to add subscripting support for objects of a particular type and are typically used to provide a convenient syntax for accessing the elements in a collection, list, or sequence. Subscript declarations are declared using the keyword &lt;code&gt;subscript&lt;/code&gt; and have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;subscript (&lt;em&gt;parameters&lt;/em&gt;) -&amp;gt; &lt;em&gt;return type&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;get {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;set(&lt;em&gt;setter name&lt;/em&gt;) {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;statements&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Subscript declarations can appear only in the context of a class, structure, enumeration, extension, or protocol declaration. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;parameters&lt;/em&gt; specify one or more indexes used to access elements of the corresponding type in a subscript expression (for example, the &lt;code&gt;i&lt;/code&gt; in the expression &lt;code&gt;object[i]&lt;/code&gt;). Although the indexes used to access the elements can be of any type, each parameter must include a type annotation to specify the type of each index. The &lt;em&gt;return type&lt;/em&gt; specifies the type of the element being accessed. &lt;/p&gt;
&lt;p&gt;As with computed properties, subscript declarations support reading and writing the value of the accessed elements. The getter is used to read the value, and the setter is used to write the value. The setter clause is optional, and when only a getter is needed, you can omit both clauses and simply return the requested value directly. That said, if you provide a setter clause, you must also provide a getter clause. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;setter name&lt;/em&gt; and enclosing parentheses are optional. If you provide a setter name, it is used as the name of the parameter to the setter. If you do not provide a setter name, the default parameter name to the setter is &lt;code&gt;value&lt;/code&gt;. The type of the &lt;em&gt;setter name&lt;/em&gt; must be the same as the &lt;em&gt;return type&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;You can overload a subscript declaration in the type in which it is declared, as long as the &lt;em&gt;parameters&lt;/em&gt; or the &lt;em&gt;return type&lt;/em&gt; differ from the one you’re overloading. You can also override a subscript declaration inherited from a superclass. When you do so, you must mark the overridden subscript declaration with the &lt;code&gt;override&lt;/code&gt; declaration modifier. &lt;/p&gt;
&lt;p&gt;You can also declare subscripts in the context of a protocol declaration, as described in Protocol Subscript Declaration. &lt;/p&gt;
&lt;p&gt;For more information about subscripting and to see examples of subscript declarations, see Subscripts. &lt;/p&gt;
&lt;p&gt;Grammar of a subscript declaration &lt;/p&gt;
&lt;p&gt;subscript-declaration  → subscript-result­code-block­&lt;/p&gt;
&lt;p&gt;subscript-declaration  → subscript-result­getter-setter-block­&lt;/p&gt;
&lt;p&gt;subscript-declaration  → subscript-result­getter-setter-keyword-block­&lt;/p&gt;
&lt;p&gt;subscript-head  → attributes­opt­parameter-clause­&lt;/p&gt;
&lt;p&gt;subscript-result  → &lt;code&gt;-&amp;gt;­&lt;/code&gt;attributes­opt­type­&lt;/p&gt;
&lt;h3&gt;Operator Declaration&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;operator declaration&lt;/em&gt; introduces a new infix, prefix, or postfix operator into your program and is declared using the keyword &lt;code&gt;operator&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;You can declare operators of three different fixities: infix, prefix, and postfix. The &lt;em&gt;fixity&lt;/em&gt; of an operator specifies the relative position of an operator to its operands. &lt;/p&gt;
&lt;p&gt;There are three basic forms of an operator declaration, one for each fixity. The fixity of the operator is specified by marking the operator declaration with the &lt;code&gt;infix&lt;/code&gt;, &lt;code&gt;prefix&lt;/code&gt;, or &lt;code&gt;postfix&lt;/code&gt; declaration modifier before the &lt;code&gt;operator&lt;/code&gt; keyword. In each form, the name of the operator can contain only the operator characters defined in Operators. &lt;/p&gt;
&lt;p&gt;The following form declares a new infix operator: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;infix operator &lt;em&gt;operator name&lt;/em&gt; {&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;precedence &lt;em&gt;precedence level&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;associativity &lt;em&gt;associativity&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;}&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;An &lt;em&gt;infix operator&lt;/em&gt; is a binary operator that is written between its two operands, such as the familiar addition operator (&lt;code&gt;+&lt;/code&gt;) in the expression &lt;code&gt;1 + 2&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Infix operators can optionally specify a precedence, associativity, or both. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;precedence&lt;/em&gt; of an operator specifies how tightly an operator binds to its operands in the absence of grouping parentheses. You specify the precedence of an operator by writing the context-sensitive keyword &lt;code&gt;precedence&lt;/code&gt; followed by the &lt;em&gt;precedence level&lt;/em&gt;. The &lt;em&gt;precedence level&lt;/em&gt; can be any whole number (decimal integer) from 0 to 255; unlike decimal integer literals, it can’t contain any underscore characters. Although the precedence level is a specific number, it is significant only relative to another operator. That is, when two operators compete with each other for their operands, such as in the expression &lt;code&gt;2 + 3 * 5&lt;/code&gt;, the operator with the higher precedence level binds more tightly to its operands. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;associativity&lt;/em&gt; of an operator specifies how a sequence of operators with the same precedence level are grouped together in the absence of grouping parentheses. You specify the associativity of an operator by writing the context-sensitive keyword &lt;code&gt;associativity&lt;/code&gt; followed by the &lt;em&gt;associativity&lt;/em&gt;, which is one of the context-sensitive keywords &lt;code&gt;left&lt;/code&gt;, &lt;code&gt;right&lt;/code&gt;, or &lt;code&gt;none&lt;/code&gt;. Operators that are left-associative group left-to-right. For example, the subtraction operator (&lt;code&gt;-&lt;/code&gt;) is left-associative, and therefore the expression &lt;code&gt;4 - 5 - 6&lt;/code&gt; is grouped as &lt;code&gt;(4 - 5) - 6&lt;/code&gt; and evaluates to &lt;code&gt;-7&lt;/code&gt;. Operators that are right-associative group right-to-left, and operators that are specified with an associativity of &lt;code&gt;none&lt;/code&gt; don’t associate at all. Nonassociative operators of the same precedence level can’t appear adjacent to each to other. For example, &lt;code&gt;1 &amp;lt; 2 &amp;lt; 3&lt;/code&gt; is not a valid expression. &lt;/p&gt;
&lt;p&gt;Infix operators that are declared without specifying a precedence or associativity are initialized with a precedence level of 100 and an associativity of &lt;code&gt;none&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;The following form declares a new prefix operator: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;prefix operator &lt;em&gt;operator name&lt;/em&gt; {}&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A &lt;em&gt;prefix operator&lt;/em&gt; is a unary operator that is written immediately before its operand, such as the prefix increment operator (&lt;code&gt;++&lt;/code&gt;) is in the expression &lt;code&gt;++i&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Prefix operators declarations don’t specify a precedence level. Prefix operators are nonassociative. &lt;/p&gt;
&lt;p&gt;The following form declares a new postfix operator: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;postfix operator &lt;em&gt;operator name&lt;/em&gt; {}&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A &lt;em&gt;postfix operator&lt;/em&gt; is a unary operator that is written immediately after its operand, such as the postfix increment operator (&lt;code&gt;++&lt;/code&gt;) is in the expression &lt;code&gt;i++&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;As with prefix operators, postfix operator declarations don’t specify a precedence level. Postfix operators are nonassociative. &lt;/p&gt;
&lt;p&gt;After declaring a new operator, you implement it by declaring a function that has the same name as the operator. If you’re implementing a prefix or postfix operator, you must also mark that function declaration with the corresponding &lt;code&gt;prefix&lt;/code&gt; or &lt;code&gt;postfix&lt;/code&gt; declaration modifier. If you’re implementing an infix operator, you don’t mark that function declaration with the &lt;code&gt;infix&lt;/code&gt; declaration modifier. To see an example of how to create and implement a new operator, see Custom Operators. &lt;/p&gt;
&lt;p&gt;Grammar of an operator declaration &lt;/p&gt;
&lt;p&gt;operator-declaration  →  prefix-operator-declaration­ postfix-operator-declaration­ infix-operator-declaration­&lt;/p&gt;
&lt;p&gt;prefix-operator-declaration  → &lt;code&gt;prefix­``operator­&lt;/code&gt;operator­&lt;code&gt;{­``}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;postfix-operator-declaration  → &lt;code&gt;postfix­``operator­&lt;/code&gt;operator­&lt;code&gt;{­``}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;infix-operator-declaration  → &lt;code&gt;infix­``operator­&lt;/code&gt;operator­&lt;code&gt;{­&lt;/code&gt;infix-operator-attributes­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;infix-operator-attributes  → associativity-clause­opt­&lt;/p&gt;
&lt;p&gt;precedence-clause  → &lt;code&gt;precedence­&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Attributes&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;Attributes&lt;/em&gt; provide more information about a declaration or type. There are two kinds of attributes in Swift, those that apply to declarations and those that apply to types. &lt;/p&gt;
&lt;p&gt;You specify an attribute by writing the &lt;code&gt;@&lt;/code&gt; symbol followed by the attribute’s name and any arguments that the attribute accepts: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;@&lt;em&gt;attribute name&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;@&lt;em&gt;attribute name&lt;/em&gt;(&lt;em&gt;attribute arguments&lt;/em&gt;)&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Some declaration attributes accept arguments that specify more information about the attribute and how it applies to a particular declaration. These &lt;em&gt;attribute arguments&lt;/em&gt; are enclosed in parentheses, and their format is defined by the attribute they belong to. &lt;/p&gt;
&lt;h3&gt;Declaration Attributes&lt;/h3&gt;
&lt;p&gt;You can apply a declaration attribute to declarations only. However, you can also apply the &lt;code&gt;noreturn&lt;/code&gt; attribute to a function or method &lt;em&gt;type&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;availability&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to any declaration to indicate the declaration’s lifecycle relative to certain platforms and operating system versions. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;availability&lt;/code&gt; attribute always appears with a list of two or more comma-separated attribute arguments. These arguments begin with one of the following platform names: &lt;code&gt;iOS&lt;/code&gt;, &lt;code&gt;iOSApplicationExtension&lt;/code&gt;, &lt;code&gt;OSX&lt;/code&gt;, or &lt;code&gt;OSXApplicationExtension&lt;/code&gt;. You can also use an asterisk (&lt;code&gt;*&lt;/code&gt;) to indicate the availability of the declaration on all of the platform names listed above. The remaining arguments can appear in any order and specify additional information about the declaration’s lifecycle, including important milestones. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;unavailable&lt;/code&gt; argument indicates that the declaration isn’t available on the specified platform. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;introduced&lt;/code&gt; argument indicates the first version of the specified platform in which the declaration was introduced. It has the following form: &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;introduced&lt;/code&gt; argument has the following form: &lt;/p&gt;
&lt;pre class="code literal-block"&gt;1.         introduced=_version number_
&lt;/pre&gt;


&lt;p&gt;The &lt;em&gt;version number&lt;/em&gt; consists of a positive integer or floating-point decimal number. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;deprecated&lt;/code&gt; argument indicates the first version of the specified platform in which the declaration was deprecated. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;deprecated=&lt;em&gt;version number&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;version number&lt;/em&gt; consists of a positive integer or floating-point decimal number. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;obsoleted&lt;/code&gt; argument indicates the first version of the specified platform in which the declaration was obsoleted. When a declaration is obsoleted, it’s removed from the specified platform and can no longer be used. The &lt;code&gt;obsoleted&lt;/code&gt; argument has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;obsoleted=&lt;em&gt;version number&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;version number&lt;/em&gt; consists of a positive integer or floating-point decimal number. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;message&lt;/code&gt; argument is used to provide a textual message that’s displayed by the compiler when emitting a warning or error about the use of a deprecated or obsoleted declaration. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;message=&lt;em&gt;message&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;message&lt;/em&gt; consists of a string literal. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;renamed&lt;/code&gt; argument is used to provide a textual message that indicates the new name for a declaration that’s been renamed. The new name is displayed by the compiler when emitting an error about the use of a renamed declaration. It has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;renamed=&lt;em&gt;new name&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;new name&lt;/em&gt; consists of a string literal. &lt;/p&gt;
&lt;p&gt;You can use the &lt;code&gt;renamed&lt;/code&gt; argument in conjunction with the &lt;code&gt;unavailable&lt;/code&gt; argument and a type alias declaration to indicate to clients of your code that a declaration has been renamed. For example, this is useful when the name of a declaration is changed between releases of a framework or library. &lt;/p&gt;
&lt;pre class="code literal-block"&gt;1. `// First release`
2. `protocol MyProtocol {`
3. ` // protocol definition`
4. `}`

1. `// Subsequent release renames MyProtocol`
2. `protocol MyRenamedProtocol {`
3. ` // protocol definition`
4. `}`
5. ` `
6. `@availability(*, unavailable, renamed="MyRenamedProtocol")`
7. `typealias MyProtocol = MyRenamedProtocol`
&lt;/pre&gt;


&lt;p&gt;You can apply multiple &lt;code&gt;availability&lt;/code&gt; attributes on a single declaration to specify the declaration’s availability on different platforms. The compiler uses an &lt;code&gt;availability&lt;/code&gt; attribute only when the attribute specifies a platform that matches the current target platform. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;noreturn&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to a function or method declaration to indicate that the corresponding type of that function or method, &lt;code&gt;T&lt;/code&gt;, is &lt;code&gt;@noreturn T&lt;/code&gt;. You can mark a function or method type with this attribute to indicate that the function or method doesn’t return to its caller. &lt;/p&gt;
&lt;p&gt;You can override a function or method that is not marked with the &lt;code&gt;noreturn&lt;/code&gt; attribute with a function or method that is. That said, you can’t override a function or method that is marked with the &lt;code&gt;noreturn&lt;/code&gt; attribute with a function or method that is not. Similar rules apply when you implement a protocol method in a conforming type. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;NSApplicationMain&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to a class to indicate that it is the application delegate. Using this attribute is equivalent to calling the &lt;code&gt;NSApplicationMain&lt;/code&gt; function and passing this class’s name as the name of the delegate class. &lt;/p&gt;
&lt;p&gt;If you do not use this attribute, supply a &lt;code&gt;main.swift&lt;/code&gt; file with a &lt;code&gt;main&lt;/code&gt; function that calls the &lt;code&gt;NSApplicationMain&lt;/code&gt; function. For example, if your app uses a custom subclass of &lt;code&gt;NSApplication&lt;/code&gt; as its principal class, call the &lt;code&gt;NSApplicationMain&lt;/code&gt; function instead of using this attribute. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;NSCopying&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to a stored variable property of a class. This attribute causes the property’s setter to be synthesized with a &lt;em&gt;copy&lt;/em&gt; of the property’s value—returned by the &lt;code&gt;copyWithZone&lt;/code&gt; method—instead of the value of the property itself. The type of the property must conform to the &lt;code&gt;NSCopying&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;NSCopying&lt;/code&gt; attribute behaves in a way similar to the Objective-C &lt;code&gt;copy&lt;/code&gt; property attribute. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;NSManaged&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to a stored variable property of a class that inherits from &lt;code&gt;NSManagedObject&lt;/code&gt; to indicate that the storage and implementation of the property are provided dynamically by Core Data at runtime based on the associated entity description. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;objc&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to any declaration that can be represented in Objective-C—for example, non-nested classes, protocols, properties and methods (including getters and setters) of classes and protocols, initializers, deinitializers, and subscripts. The &lt;code&gt;objc&lt;/code&gt; attribute tells the compiler that a declaration is available to use in Objective-C code. &lt;/p&gt;
&lt;p&gt;If you apply the &lt;code&gt;objc&lt;/code&gt; attribute to a class or protocol, it’s implicitly applied to the members of that class or protocol. The compiler also implicitly adds the &lt;code&gt;objc&lt;/code&gt; attribute to a class that inherits from another class marked with the &lt;code&gt;objc&lt;/code&gt; attribute. Protocols marked with the &lt;code&gt;objc&lt;/code&gt; attribute can’t inherit from protocols that aren’t. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;objc&lt;/code&gt; attribute optionally accepts a single attribute argument, which consists of an identifier. Use this attribute when you want to expose a different name to Objective-C for the entity the &lt;code&gt;objc&lt;/code&gt; attribute applies to. You can use this argument to name classes, protocols, methods, getters, setters, and initializers. The example below exposes the getter for the &lt;code&gt;enabled&lt;/code&gt; property of the &lt;code&gt;ExampleClass&lt;/code&gt; to Objective-C code as &lt;code&gt;isEnabled&lt;/code&gt; rather than just as the name of the property itself. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;@objc&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;class ExampleClass {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;var enabled: Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@objc(isEnabled) get {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Return the appropriate value&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;code&gt;UIApplicationMain&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to a class to indicate that it is the application delegate. Using this attribute is equivalent to calling the &lt;code&gt;UIApplicationMain&lt;/code&gt; function and passing this class’s name as the name of the delegate class. &lt;/p&gt;
&lt;p&gt;If you do not use this attribute, supply a &lt;code&gt;main.swift&lt;/code&gt; file with a &lt;code&gt;main&lt;/code&gt; function that calls the &lt;code&gt;UIApplicationMain&lt;/code&gt; function. For example, if your app uses a custom subclass of &lt;code&gt;UIApplication&lt;/code&gt; as its principal class, call the &lt;code&gt;UIApplicationMain&lt;/code&gt; function instead of using this attribute. &lt;/p&gt;
&lt;h4&gt;Declaration Attributes Used by Interface Builder&lt;/h4&gt;
&lt;p&gt;Interface Builder attributes are declaration attributes used by Interface Builder to synchronize with Xcode. Swift provides the following Interface Builder attributes: &lt;code&gt;IBAction&lt;/code&gt;, &lt;code&gt;IBDesignable&lt;/code&gt;, &lt;code&gt;IBInspectable&lt;/code&gt;, and &lt;code&gt;IBOutlet&lt;/code&gt;. These attributes are conceptually the same as their Objective-C counterparts. &lt;/p&gt;
&lt;p&gt;You apply the &lt;code&gt;IBOutlet&lt;/code&gt; and &lt;code&gt;IBInspectable&lt;/code&gt; attributes to property declarations of a class. You apply the &lt;code&gt;IBAction&lt;/code&gt; attribute to method declarations of a class and the &lt;code&gt;IBDesignable&lt;/code&gt; attribute to class declarations. &lt;/p&gt;
&lt;h3&gt;Type Attributes&lt;/h3&gt;
&lt;p&gt;You can apply type attributes to types only. However, you can also apply the &lt;code&gt;noreturn&lt;/code&gt; attribute to a function or method &lt;em&gt;declaration&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;autoclosure&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;This attribute is used to delay the evaluation of an expression by automatically wrapping that expression in a closure with no arguments. Apply this attribute to a function or method type that takes no arguments and that returns the type of the expression. For an example of how to use the &lt;code&gt;autoclosure&lt;/code&gt; attribute, see Function Type. &lt;/p&gt;
&lt;p&gt;&lt;code&gt;noreturn&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Apply this attribute to the type of a function or method to indicate that the function or method doesn’t return to its caller. You can also mark a function or method declaration with this attribute to indicate that the corresponding type of that function or method, &lt;code&gt;T&lt;/code&gt;, is &lt;code&gt;@noreturn T&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Grammar of an attribute &lt;/p&gt;
&lt;p&gt;attribute  → &lt;code&gt;@­&lt;/code&gt;attribute-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;attribute-name  → identifier­&lt;/p&gt;
&lt;p&gt;attribute-argument-clause  → &lt;code&gt;(­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;attributes  → attributes­opt­&lt;/p&gt;
&lt;p&gt;balanced-tokens  → balanced-tokens­opt­&lt;/p&gt;
&lt;p&gt;balanced-token  → &lt;code&gt;(­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;balanced-token  → &lt;code&gt;[­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;balanced-token  → &lt;code&gt;{­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;balanced-token  → Any identifier, keyword, literal, or operator&lt;/p&gt;
&lt;p&gt;balanced-token  → Any punctuation except &lt;code&gt;(­&lt;/code&gt;, &lt;code&gt;)­&lt;/code&gt;, &lt;code&gt;[­&lt;/code&gt;, &lt;code&gt;]­&lt;/code&gt;, &lt;code&gt;{­&lt;/code&gt;, or &lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Patterns&lt;/h2&gt;
&lt;p&gt;A &lt;em&gt;pattern&lt;/em&gt; represents the structure of a single value or a composite value. For example, the structure of a tuple &lt;code&gt;(1, 2)&lt;/code&gt; is a comma-separated list of two elements. Because patterns represent the structure of a value rather than any one particular value, you can match them with a variety of values. For instance, the pattern &lt;code&gt;(x, y)&lt;/code&gt; matches the tuple &lt;code&gt;(1, 2)&lt;/code&gt; and any other two-element tuple. In addition to matching a pattern with a value, you can extract part or all of a composite value and bind each part to a constant or variable name. &lt;/p&gt;
&lt;p&gt;In Swift, patterns occur in variable and constant declarations (on their left-hand side), in &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; statements, and in &lt;code&gt;switch&lt;/code&gt; statements (in their case labels). Although any pattern can occur in the case labels of a &lt;code&gt;switch&lt;/code&gt; statement, in the other contexts, only wildcard patterns, identifier patterns, and patterns containing those two patterns can occur. &lt;/p&gt;
&lt;p&gt;You can specify a type annotation for a wildcard pattern, an identifier pattern, and a tuple pattern to constrain the pattern to match only values of a certain type. &lt;/p&gt;
&lt;p&gt;Grammar of a pattern &lt;/p&gt;
&lt;p&gt;pattern  → type-annotation­opt­&lt;/p&gt;
&lt;p&gt;pattern  → type-annotation­opt­&lt;/p&gt;
&lt;p&gt;pattern  → value-binding-pattern­&lt;/p&gt;
&lt;p&gt;pattern  → type-annotation­opt­&lt;/p&gt;
&lt;p&gt;pattern  → enum-case-pattern­&lt;/p&gt;
&lt;p&gt;pattern  → type-casting-pattern­&lt;/p&gt;
&lt;p&gt;pattern  → expression-pattern­&lt;/p&gt;
&lt;h3&gt;Wildcard Pattern&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;wildcard pattern&lt;/em&gt; matches and ignores any value and consists of an underscore (&lt;code&gt;_&lt;/code&gt;). Use a wildcard pattern when you don’t care about the values being matched against. For example, the following code iterates through the closed range &lt;code&gt;1...3&lt;/code&gt;, ignoring the current value of the range on each iteration of the loop: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;for _ in 1...3 {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Do something three times.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a wildcard pattern &lt;/p&gt;
&lt;p&gt;wildcard-pattern  → &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Identifier Pattern&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;identifier pattern&lt;/em&gt; matches any value and binds the matched value to a variable or constant name. For example, in the following constant declaration, &lt;code&gt;someValue&lt;/code&gt; is an identifier pattern that matches the value &lt;code&gt;42&lt;/code&gt; of type &lt;code&gt;Int&lt;/code&gt;: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let someValue = 42&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;When the match succeeds, the value &lt;code&gt;42&lt;/code&gt; is bound (assigned) to the constant name &lt;code&gt;someValue&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;When the pattern on the left-hand side of a variable or constant declaration is an identifier pattern, the identifier pattern is implicitly a subpattern of a value-binding pattern. &lt;/p&gt;
&lt;p&gt;Grammar of an identifier pattern &lt;/p&gt;
&lt;p&gt;identifier-pattern  → identifier­&lt;/p&gt;
&lt;h3&gt;Value-Binding Pattern&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;value-binding pattern&lt;/em&gt; binds matched values to variable or constant names. Value-binding patterns that bind a matched value to the name of a constant begin with the keyword &lt;code&gt;let&lt;/code&gt;; those that bind to the name of variable begin with the keyword &lt;code&gt;var&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;Identifiers patterns within a value-binding pattern bind new named variables or constants to their matching values. For example, you can decompose the elements of a tuple and bind the value of each element to a corresponding identifier pattern. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let point = (3, 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// Bind x and y to the elements of point.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case let (x, y):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The point is at (\\(x), \\(y)).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The point is at (3, 2)."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;In the example above, &lt;code&gt;let&lt;/code&gt; distributes to each identifier pattern in the tuple pattern &lt;code&gt;(x, y)&lt;/code&gt;. Because of this behavior, the &lt;code&gt;switch&lt;/code&gt; cases &lt;code&gt;case let (x, y):&lt;/code&gt; and &lt;code&gt;case (let x, let y):&lt;/code&gt; match the same values. &lt;/p&gt;
&lt;p&gt;Grammar of a value-binding pattern &lt;/p&gt;
&lt;p&gt;value-binding-pattern  →  &lt;code&gt;var­&lt;/code&gt;pattern­ &lt;code&gt;let­&lt;/code&gt;pattern­&lt;/p&gt;
&lt;h3&gt;Tuple Pattern&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;tuple pattern&lt;/em&gt; is a comma-separated list of zero or more patterns, enclosed in parentheses. Tuple patterns match values of corresponding tuple types. &lt;/p&gt;
&lt;p&gt;You can constrain a tuple pattern to match certain kinds of tuple types by using type annotations. For example, the tuple pattern &lt;code&gt;(x, y): (Int, Int)&lt;/code&gt; in the constant declaration &lt;code&gt;let (x, y): (Int, Int) = (1, 2)&lt;/code&gt; matches only tuple types in which both elements are of type &lt;code&gt;Int&lt;/code&gt;. To constrain only some elements of a tuple pattern, provide type annotations directly to those individual elements. For example, the tuple pattern in &lt;code&gt;let (x: String, y)&lt;/code&gt; matches any two-element tuple type, as long as the first element is of type &lt;code&gt;String&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;When a tuple pattern is used as the pattern in a &lt;code&gt;for&lt;/code&gt;-&lt;code&gt;in&lt;/code&gt; statement or in a variable or constant declaration, it can contain only wildcard patterns, identifier patterns, or other tuple patterns that contain those. For example, the following code isn’t valid because the element &lt;code&gt;0&lt;/code&gt; in the tuple pattern &lt;code&gt;(x, 0)&lt;/code&gt; is an expression pattern: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let points = [(0, 0), (1, 0), (1, 1), (2, 0), (2, 1)]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// This code isn't valid.&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;for (x, 0) in points {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/* ... */&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The parentheses around a tuple pattern that contains a single element have no effect. The pattern matches values of that single element’s type. For example, the following are equivalent: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let a = 2 // a: Int = 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let (a) = 2 // a: Int = 2&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;let (a): Int = 2 // a: Int = 2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of a tuple pattern &lt;/p&gt;
&lt;p&gt;tuple-pattern  → &lt;code&gt;(­&lt;/code&gt;tuple-pattern-element-list­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;tuple-pattern-element-list  →  tuple-pattern-element­ tuple-pattern-element-list­&lt;/p&gt;
&lt;p&gt;tuple-pattern-element  → pattern­&lt;/p&gt;
&lt;h3&gt;Enumeration Case Pattern&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;enumeration case pattern&lt;/em&gt; matches a case of an existing enumeration type. Enumeration case patterns appear only in &lt;code&gt;switch&lt;/code&gt; statement case labels. &lt;/p&gt;
&lt;p&gt;If the enumeration case you’re trying to match has any associated values, the corresponding enumeration case pattern must specify a tuple pattern that contains one element for each associated value. For an example that uses a &lt;code&gt;switch&lt;/code&gt; statement to match enumeration cases containing associated values, see Associated Values. &lt;/p&gt;
&lt;p&gt;Grammar of an enumeration case pattern &lt;/p&gt;
&lt;p&gt;enum-case-pattern  → type-identifier­opt­&lt;code&gt;.­&lt;/code&gt;enum-case-name­tuple-pattern­opt­&lt;/p&gt;
&lt;h3&gt;Type-Casting Patterns&lt;/h3&gt;
&lt;p&gt;There are two type-casting patterns, the &lt;code&gt;is&lt;/code&gt; pattern and the &lt;code&gt;as&lt;/code&gt; pattern. Both type-casting patterns appear only in &lt;code&gt;switch&lt;/code&gt; statement case labels. The &lt;code&gt;is&lt;/code&gt; and &lt;code&gt;as&lt;/code&gt; patterns have the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;is &lt;em&gt;type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;pattern&lt;/em&gt; as &lt;em&gt;type&lt;/em&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;code&gt;is&lt;/code&gt; pattern matches a value if the type of that value at runtime is the same as the type specified in the right-hand side of the &lt;code&gt;is&lt;/code&gt; pattern—or a subclass of that type. The &lt;code&gt;is&lt;/code&gt; pattern behaves like the &lt;code&gt;is&lt;/code&gt; operator in that they both perform a type cast but discard the returned type. &lt;/p&gt;
&lt;p&gt;The &lt;code&gt;as&lt;/code&gt; pattern matches a value if the type of that value at runtime is the same as the type specified in the right-hand side of the &lt;code&gt;as&lt;/code&gt; pattern—or a subclass of that type. If the match succeeds, the type of the matched value is cast to the &lt;em&gt;pattern&lt;/em&gt; specified in the left-hand side of the &lt;code&gt;as&lt;/code&gt; pattern. &lt;/p&gt;
&lt;p&gt;For an example that uses a &lt;code&gt;switch&lt;/code&gt; statement to match values with &lt;code&gt;is&lt;/code&gt; and &lt;code&gt;as&lt;/code&gt; patterns, see Type Casting for Any and AnyObject. &lt;/p&gt;
&lt;p&gt;Grammar of a type casting pattern &lt;/p&gt;
&lt;p&gt;type-casting-pattern  →  is-pattern­ as-pattern­&lt;/p&gt;
&lt;p&gt;is-pattern  → &lt;code&gt;is­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;as-pattern  → type­&lt;/p&gt;
&lt;h3&gt;Expression Pattern&lt;/h3&gt;
&lt;p&gt;An &lt;em&gt;expression pattern&lt;/em&gt; represents the value of an expression. Expression patterns appear only in &lt;code&gt;switch&lt;/code&gt; statement case labels. &lt;/p&gt;
&lt;p&gt;The expression represented by the expression pattern is compared with the value of an input expression using the Swift standard library &lt;code&gt;~=&lt;/code&gt; operator. The matches succeeds if the &lt;code&gt;~=&lt;/code&gt; operator returns &lt;code&gt;true&lt;/code&gt;. By default, the &lt;code&gt;~=&lt;/code&gt; operator compares two values of the same type using the &lt;code&gt;==&lt;/code&gt; operator. It can also match an integer value with a range of integers in an &lt;code&gt;Range&lt;/code&gt; object, as the following example shows: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let point = (1, 2)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (0, 0):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(0, 0) is at the origin.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case (-2...2, -2...2):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(\\(point.0), \\(point.1)) is near the origin.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The point is at (\\(point.0), \\(point.1)).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "(1, 2) is near the origin."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;You can overload the &lt;code&gt;~=&lt;/code&gt; operator to provide custom expression matching behavior. For example, you can rewrite the above example to compare the &lt;code&gt;point&lt;/code&gt; expression with a string representations of points. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;// Overload the ~= operator to match a string with an integer&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;func ~=(pattern: String, value: Int) -&amp;gt; Bool {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return pattern == "\\(value)"&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;switch point {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;case ("0", "0"):&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("(0, 0) is at the origin.")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;default:&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;println("The point is at (\\(point.0), \\(point.1)).")&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;// prints "The point is at (1, 2)."&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Grammar of an expression pattern &lt;/p&gt;
&lt;p&gt;expression-pattern  → expression­&lt;/p&gt;
&lt;h2&gt;Generic Parameters and Arguments&lt;/h2&gt;
&lt;p&gt;This chapter describes parameters and arguments for generic types, functions, and initializers. When you declare a generic type, function, or initializer, you specify the type parameters that the generic type, function, or initializer can work with. These type parameters act as placeholders that are replaced by actual concrete type arguments when an instance of a generic type is created or a generic function or initializer is called. &lt;/p&gt;
&lt;p&gt;For an overview of generics in Swift, see Generics. &lt;/p&gt;
&lt;h3&gt;Generic Parameter Clause&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;generic parameter clause&lt;/em&gt; specifies the type parameters of a generic type or function, along with any associated constraints and requirements on those parameters. A generic parameter clause is enclosed in angle brackets (&amp;lt;&amp;gt;) and has one of the following forms: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&amp;lt;&lt;em&gt;generic parameter list&lt;/em&gt;&amp;gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&amp;lt;&lt;em&gt;generic parameter list&lt;/em&gt; where &lt;em&gt;requirements&lt;/em&gt;&amp;gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;generic parameter list&lt;/em&gt; is a comma-separated list of generic parameters, each of which has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;type parameter&lt;/em&gt;: &lt;em&gt;constraint&lt;/em&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A generic parameter consists of a &lt;em&gt;type parameter&lt;/em&gt; followed by an optional &lt;em&gt;constraint&lt;/em&gt;. A &lt;em&gt;type parameter&lt;/em&gt; is simply the name of a placeholder type (for instance, &lt;code&gt;T&lt;/code&gt;, &lt;code&gt;U&lt;/code&gt;, &lt;code&gt;V&lt;/code&gt;, &lt;code&gt;Key&lt;/code&gt;, &lt;code&gt;Value&lt;/code&gt;, and so on). You have access to the type parameters (and any of their associated types) in the rest of the type, function, or initializer declaration, including in the signature of the function or initializer. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;constraint&lt;/em&gt; specifies that a type parameter inherits from a specific class or conforms to a protocol or protocol composition. For instance, in the generic function below, the generic parameter &lt;code&gt;T: Comparable&lt;/code&gt; indicates that any type argument substituted for the type parameter &lt;code&gt;T&lt;/code&gt; must conform to the &lt;code&gt;Comparable&lt;/code&gt; protocol. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;func simpleMax&amp;lt;T: Comparable&amp;gt;(x: T, y: T) -&amp;gt; T {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;if x &amp;lt; y {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return y&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;return x&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Because &lt;code&gt;Int&lt;/code&gt; and &lt;code&gt;Double&lt;/code&gt;, for example, both conform to the &lt;code&gt;Comparable&lt;/code&gt; protocol, this function accepts arguments of either type. In contrast with generic types, you don’t specify a generic argument clause when you use a generic function or initializer. The type arguments are instead inferred from the type of the arguments passed to the function or initializer. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;simpleMax(17, 42) // T is inferred to be Int&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;simpleMax(3.14159, 2.71828) // T is inferred to be Double&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;Where Clauses&lt;/h4&gt;
&lt;p&gt;You can specify additional requirements on type parameters and their associated types by including a &lt;code&gt;where&lt;/code&gt; clause after the &lt;em&gt;generic parameter list&lt;/em&gt;. A &lt;code&gt;where&lt;/code&gt; clause consists of the keyword &lt;code&gt;where&lt;/code&gt;, followed by a comma-separated list of one or more &lt;em&gt;requirements&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;The &lt;em&gt;requirements&lt;/em&gt; in a &lt;code&gt;where&lt;/code&gt; clause specify that a type parameter inherits from a class or conforms to a protocol or protocol composition. Although the &lt;code&gt;where&lt;/code&gt; clause provides syntactic sugar for expressing simple constraints on type parameters (for instance, &lt;code&gt;T: Comparable&lt;/code&gt; is equivalent to &lt;code&gt;T where T: Comparable&lt;/code&gt; and so on), you can use it to provide more complex constraints on type parameters and their associated types. For instance, you can express the constraints that a generic type &lt;code&gt;T&lt;/code&gt; inherits from a class &lt;code&gt;C&lt;/code&gt; and conforms to a protocol &lt;code&gt;P&lt;/code&gt; as &lt;code&gt;&amp;lt;T where T: C, T: P&amp;gt;&lt;/code&gt;. &lt;/p&gt;
&lt;p&gt;As mentioned above, you can constrain the associated types of type parameters to conform to protocols. For example, the generic parameter clause &lt;code&gt;&amp;lt;T: Generator where T.Element: Equatable&amp;gt;&lt;/code&gt; specifies that &lt;code&gt;T&lt;/code&gt; conforms to the &lt;code&gt;Generator&lt;/code&gt; protocol and the associated type of &lt;code&gt;T&lt;/code&gt;, &lt;code&gt;T.Element&lt;/code&gt;, conforms to the &lt;code&gt;Equatable&lt;/code&gt; protocol (&lt;code&gt;T&lt;/code&gt; has the associated type &lt;code&gt;Element&lt;/code&gt; because &lt;code&gt;Generator&lt;/code&gt; declares &lt;code&gt;Element&lt;/code&gt; and &lt;code&gt;T&lt;/code&gt; conforms to &lt;code&gt;Generator&lt;/code&gt;). &lt;/p&gt;
&lt;p&gt;You can also specify the requirement that two types be identical, using the &lt;code&gt;==&lt;/code&gt; operator. For example, the generic parameter clause &lt;code&gt;&amp;lt;T: Generator, U: Generator where T.Element == U.Element&amp;gt;&lt;/code&gt; expresses the constraints that &lt;code&gt;T&lt;/code&gt; and &lt;code&gt;U&lt;/code&gt; conform to the &lt;code&gt;Generator&lt;/code&gt; protocol and that their associated types must be identical. &lt;/p&gt;
&lt;p&gt;Any type argument substituted for a type parameter must meet all the constraints and requirements placed on the type parameter. &lt;/p&gt;
&lt;p&gt;You can overload a generic function or initializer by providing different constraints, requirements, or both on the type parameters in the generic parameter clause. When you call an overloaded generic function or initializer, the compiler uses these constraints to resolve which overloaded function or initializer to invoke. &lt;/p&gt;
&lt;p&gt;Grammar of a generic parameter clause &lt;/p&gt;
&lt;p&gt;generic-parameter-clause  → &lt;code&gt;&amp;lt;­&lt;/code&gt;requirement-clause­opt­&lt;code&gt;&amp;gt;­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;generic-parameter-list  →  generic-parameter­ generic-parameter-list­&lt;/p&gt;
&lt;p&gt;generic-parameter  → type-name­&lt;/p&gt;
&lt;p&gt;generic-parameter  → type-name­&lt;code&gt;:­&lt;/code&gt;type-identifier­&lt;/p&gt;
&lt;p&gt;generic-parameter  → type-name­&lt;code&gt;:­&lt;/code&gt;protocol-composition-type­&lt;/p&gt;
&lt;p&gt;requirement-clause  → &lt;code&gt;where­&lt;/code&gt;requirement-list­&lt;/p&gt;
&lt;p&gt;requirement-list  →  requirement­ requirement-list­&lt;/p&gt;
&lt;p&gt;requirement  →  conformance-requirement­ same-type-requirement­&lt;/p&gt;
&lt;p&gt;conformance-requirement  → type-identifier­&lt;code&gt;:­&lt;/code&gt;type-identifier­&lt;/p&gt;
&lt;p&gt;conformance-requirement  → type-identifier­&lt;code&gt;:­&lt;/code&gt;protocol-composition-type­&lt;/p&gt;
&lt;p&gt;same-type-requirement  → type-identifier­&lt;code&gt;==­&lt;/code&gt;type-identifier­&lt;/p&gt;
&lt;h3&gt;Generic Argument Clause&lt;/h3&gt;
&lt;p&gt;A &lt;em&gt;generic argument clause&lt;/em&gt; specifies the type arguments of a generic type. A generic argument clause is enclosed in angle brackets (&amp;lt;&amp;gt;) and has the following form: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&amp;lt;&lt;em&gt;generic argument list&lt;/em&gt;&amp;gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The &lt;em&gt;generic argument list&lt;/em&gt; is a comma-separated list of type arguments. A &lt;em&gt;type argument&lt;/em&gt; is the name of an actual concrete type that replaces a corresponding type parameter in the generic parameter clause of a generic type. The result is a specialized version of that generic type. As an example, the Swift standard library defines a generic dictionary type as: &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;struct Dictionary&amp;lt;Key: Hashable, Value&amp;gt;: CollectionType, DictionaryLiteralConvertible {&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/* ... */&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;}&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The specialized version of the generic &lt;code&gt;Dictionary&lt;/code&gt; type, &lt;code&gt;Dictionary&amp;lt;String, Int&amp;gt;&lt;/code&gt; is formed by replacing the generic parameters &lt;code&gt;Key: Hashable&lt;/code&gt; and &lt;code&gt;Value&lt;/code&gt; with the concrete type arguments &lt;code&gt;String&lt;/code&gt; and &lt;code&gt;Int&lt;/code&gt;. Each type argument must satisfy all the constraints of the generic parameter it replaces, including any additional requirements specified in a &lt;code&gt;where&lt;/code&gt; clause. In the example above, the &lt;code&gt;Key&lt;/code&gt; type parameter is constrained to conform to the &lt;code&gt;Hashable&lt;/code&gt; protocol and therefore &lt;code&gt;String&lt;/code&gt; must also conform to the &lt;code&gt;Hashable&lt;/code&gt; protocol. &lt;/p&gt;
&lt;p&gt;You can also replace a type parameter with a type argument that is itself a specialized version of a generic type (provided it satisfies the appropriate constraints and requirements). For example, you can replace the type parameter &lt;code&gt;T&lt;/code&gt; in &lt;code&gt;Array&amp;lt;T&amp;gt;&lt;/code&gt; with a specialized version of an array, &lt;code&gt;Array&amp;lt;Int&amp;gt;&lt;/code&gt;, to form an array whose elements are themselves arrays of integers. &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;let arrayOfArrays: Array&amp;lt;Array&amp;lt;Int&amp;gt;&amp;gt; = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;As mentioned in Generic Parameter Clause, you don’t use a generic argument clause to specify the type arguments of a generic function or initializer. &lt;/p&gt;
&lt;p&gt;Grammar of a generic argument clause &lt;/p&gt;
&lt;p&gt;generic-argument-clause  → &lt;code&gt;&amp;lt;­&lt;/code&gt;generic-argument-list­&lt;code&gt;&amp;gt;­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;generic-argument-list  →  generic-argument­ generic-argument-list­&lt;/p&gt;
&lt;p&gt;generic-argument  → type­&lt;/p&gt;
&lt;h2&gt;Summary of the Grammar&lt;/h2&gt;
&lt;h3&gt;Statements&lt;/h3&gt;
&lt;p&gt;Grammar of a statement &lt;/p&gt;
&lt;p&gt;statement  → expression­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → declaration­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → loop-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → branch-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → labeled-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statement  → control-transfer-statement­&lt;code&gt;;­&lt;/code&gt;opt­&lt;/p&gt;
&lt;p&gt;statements  → statement­statements­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a loop statement &lt;/p&gt;
&lt;p&gt;loop-statement  → for-statement­&lt;/p&gt;
&lt;p&gt;loop-statement  → for-in-statement­&lt;/p&gt;
&lt;p&gt;loop-statement  → while-statement­&lt;/p&gt;
&lt;p&gt;loop-statement  → do-while-statement­&lt;/p&gt;
&lt;p&gt;Grammar of a for statement &lt;/p&gt;
&lt;p&gt;for-statement  → &lt;code&gt;for­&lt;/code&gt;for-init­opt­&lt;code&gt;;­&lt;/code&gt;expression­opt­&lt;code&gt;;­&lt;/code&gt;expression­opt­code-block­&lt;/p&gt;
&lt;p&gt;for-statement  → &lt;code&gt;for­``(­&lt;/code&gt;for-init­opt­&lt;code&gt;;­&lt;/code&gt;expression­opt­&lt;code&gt;;­&lt;/code&gt;expression­opt­&lt;code&gt;)­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;for-init  →  variable-declaration­ expression-list­&lt;/p&gt;
&lt;p&gt;Grammar of a for-in statement &lt;/p&gt;
&lt;p&gt;for-in-statement  → &lt;code&gt;for­&lt;/code&gt;pattern­&lt;code&gt;in­&lt;/code&gt;expression­code-block­&lt;/p&gt;
&lt;p&gt;Grammar of a while statement &lt;/p&gt;
&lt;p&gt;while-statement  → &lt;code&gt;while­&lt;/code&gt;while-condition­code-block­&lt;/p&gt;
&lt;p&gt;while-condition  →  expression­ declaration­&lt;/p&gt;
&lt;p&gt;Grammar of a do-while statement &lt;/p&gt;
&lt;p&gt;do-while-statement  → &lt;code&gt;do­&lt;/code&gt;code-block­&lt;code&gt;while­&lt;/code&gt;while-condition­&lt;/p&gt;
&lt;p&gt;Grammar of a branch statement &lt;/p&gt;
&lt;p&gt;branch-statement  → if-statement­&lt;/p&gt;
&lt;p&gt;branch-statement  → switch-statement­&lt;/p&gt;
&lt;p&gt;Grammar of an if statement &lt;/p&gt;
&lt;p&gt;if-statement  → &lt;code&gt;if­&lt;/code&gt;if-condition­code-block­else-clause­opt­&lt;/p&gt;
&lt;p&gt;if-condition  →  expression­ value-binding-pattern­&lt;/p&gt;
&lt;p&gt;else-clause  →  &lt;code&gt;else­&lt;/code&gt;code-block­ &lt;code&gt;else­&lt;/code&gt;if-statement­&lt;/p&gt;
&lt;p&gt;Grammar of a switch statement &lt;/p&gt;
&lt;p&gt;switch-statement  → &lt;code&gt;switch­&lt;/code&gt;expression­&lt;code&gt;{­&lt;/code&gt;switch-cases­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;switch-cases  → switch-case­switch-cases­opt­&lt;/p&gt;
&lt;p&gt;switch-case  →  case-label­statements­ default-label­statements­&lt;/p&gt;
&lt;p&gt;switch-case  →  case-label­&lt;code&gt;;­&lt;/code&gt; default-label­&lt;code&gt;;­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;case-label  → &lt;code&gt;case­&lt;/code&gt;case-item-list­&lt;code&gt;:­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;case-item-list  →  pattern­guard-clause­opt­ pattern­guard-clause­opt­&lt;code&gt;,­&lt;/code&gt;case-item-list­&lt;/p&gt;
&lt;p&gt;default-label  → &lt;code&gt;default­``:­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;guard-clause  → &lt;code&gt;where­&lt;/code&gt;guard-expression­&lt;/p&gt;
&lt;p&gt;guard-expression  → expression­&lt;/p&gt;
&lt;p&gt;Grammar of a labeled statement &lt;/p&gt;
&lt;p&gt;labeled-statement  →  statement-label­loop-statement­ statement-label­switch-statement­&lt;/p&gt;
&lt;p&gt;statement-label  → label-name­&lt;code&gt;:­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;label-name  → identifier­&lt;/p&gt;
&lt;p&gt;Grammar of a control transfer statement &lt;/p&gt;
&lt;p&gt;control-transfer-statement  → break-statement­&lt;/p&gt;
&lt;p&gt;control-transfer-statement  → continue-statement­&lt;/p&gt;
&lt;p&gt;control-transfer-statement  → fallthrough-statement­&lt;/p&gt;
&lt;p&gt;control-transfer-statement  → return-statement­&lt;/p&gt;
&lt;p&gt;Grammar of a break statement &lt;/p&gt;
&lt;p&gt;break-statement  → &lt;code&gt;break­&lt;/code&gt;label-name­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a continue statement &lt;/p&gt;
&lt;p&gt;continue-statement  → &lt;code&gt;continue­&lt;/code&gt;label-name­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a fallthrough statement &lt;/p&gt;
&lt;p&gt;fallthrough-statement  → &lt;code&gt;fallthrough­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a return statement &lt;/p&gt;
&lt;p&gt;return-statement  → &lt;code&gt;return­&lt;/code&gt;expression­opt­&lt;/p&gt;
&lt;h3&gt;Generic Parameters and Arguments&lt;/h3&gt;
&lt;p&gt;Grammar of a generic parameter clause &lt;/p&gt;
&lt;p&gt;generic-parameter-clause  → &lt;code&gt;&amp;lt;­&lt;/code&gt;generic-parameter-list­requirement-clause­opt­&lt;code&gt;&amp;gt;­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;generic-parameter-list  →  generic-parameter­ generic-parameter­&lt;code&gt;,­&lt;/code&gt;generic-parameter-list­&lt;/p&gt;
&lt;p&gt;generic-parameter  → type-name­&lt;/p&gt;
&lt;p&gt;generic-parameter  → type-name­&lt;code&gt;:­&lt;/code&gt;type-identifier­&lt;/p&gt;
&lt;p&gt;generic-parameter  → type-name­&lt;code&gt;:­&lt;/code&gt;protocol-composition-type­&lt;/p&gt;
&lt;p&gt;requirement-clause  → &lt;code&gt;where­&lt;/code&gt;requirement-list­&lt;/p&gt;
&lt;p&gt;requirement-list  →  requirement­ requirement­&lt;code&gt;,­&lt;/code&gt;requirement-list­&lt;/p&gt;
&lt;p&gt;requirement  →  conformance-requirement­ same-type-requirement­&lt;/p&gt;
&lt;p&gt;conformance-requirement  → type-identifier­&lt;code&gt;:­&lt;/code&gt;type-identifier­&lt;/p&gt;
&lt;p&gt;conformance-requirement  → type-identifier­&lt;code&gt;:­&lt;/code&gt;protocol-composition-type­&lt;/p&gt;
&lt;p&gt;same-type-requirement  → type-identifier­&lt;code&gt;==­&lt;/code&gt;type-identifier­&lt;/p&gt;
&lt;p&gt;Grammar of a generic argument clause &lt;/p&gt;
&lt;p&gt;generic-argument-clause  → &lt;code&gt;&amp;lt;­&lt;/code&gt;generic-argument-list­&lt;code&gt;&amp;gt;­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;generic-argument-list  →  generic-argument­ generic-argument­&lt;code&gt;,­&lt;/code&gt;generic-argument-list­&lt;/p&gt;
&lt;p&gt;generic-argument  → type­&lt;/p&gt;
&lt;h3&gt;Declarations&lt;/h3&gt;
&lt;p&gt;Grammar of a declaration &lt;/p&gt;
&lt;p&gt;declaration  → import-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → constant-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → variable-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → typealias-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → function-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → enum-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → struct-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → class-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → protocol-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → initializer-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → deinitializer-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → extension-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → subscript-declaration­&lt;/p&gt;
&lt;p&gt;declaration  → operator-declaration­&lt;/p&gt;
&lt;p&gt;declarations  → declaration­declarations­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a top-level declaration &lt;/p&gt;
&lt;p&gt;top-level-declaration  → statements­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a code block &lt;/p&gt;
&lt;p&gt;code-block  → &lt;code&gt;{­&lt;/code&gt;statements­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of an import declaration &lt;/p&gt;
&lt;p&gt;import-declaration  → attributes­opt­&lt;code&gt;import­&lt;/code&gt;import-kind­opt­import-path­&lt;/p&gt;
&lt;p&gt;import-kind  →  &lt;code&gt;typealias­&lt;/code&gt; &lt;code&gt;struct­&lt;/code&gt; &lt;code&gt;class­&lt;/code&gt; &lt;code&gt;enum­&lt;/code&gt; &lt;code&gt;protocol­&lt;/code&gt; &lt;code&gt;var­&lt;/code&gt; &lt;code&gt;func­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;import-path  →  import-path-identifier­ import-path-identifier­&lt;code&gt;.­&lt;/code&gt;import-path­&lt;/p&gt;
&lt;p&gt;import-path-identifier  →  identifier­ operator­&lt;/p&gt;
&lt;p&gt;Grammar of a constant declaration &lt;/p&gt;
&lt;p&gt;constant-declaration  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;let­&lt;/code&gt;pattern-initializer-list­&lt;/p&gt;
&lt;p&gt;pattern-initializer-list  →  pattern-initializer­ pattern-initializer­&lt;code&gt;,­&lt;/code&gt;pattern-initializer-list­&lt;/p&gt;
&lt;p&gt;pattern-initializer  → pattern­initializer­opt­&lt;/p&gt;
&lt;p&gt;initializer  → &lt;code&gt;=­&lt;/code&gt;expression­&lt;/p&gt;
&lt;p&gt;Grammar of a variable declaration &lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-declaration-head­pattern-initializer-list­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-declaration-head­variable-name­type-annotation­code-block­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-declaration-head­variable-name­type-annotation­getter-setter-block­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-declaration-head­variable-name­type-annotation­getter-setter-keyword-block­&lt;/p&gt;
&lt;p&gt;variable-declaration  → variable-declaration-head­variable-name­type-annotation­initializer­opt­willSet-didSet-block­&lt;/p&gt;
&lt;p&gt;variable-declaration-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;var­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;variable-name  → identifier­&lt;/p&gt;
&lt;p&gt;getter-setter-block  → &lt;code&gt;{­&lt;/code&gt;getter-clause­setter-clause­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-setter-block  → &lt;code&gt;{­&lt;/code&gt;setter-clause­getter-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-clause  → attributes­opt­&lt;code&gt;get­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;setter-clause  → attributes­opt­&lt;code&gt;set­&lt;/code&gt;setter-name­opt­code-block­&lt;/p&gt;
&lt;p&gt;setter-name  → &lt;code&gt;(­&lt;/code&gt;identifier­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-setter-keyword-block  → &lt;code&gt;{­&lt;/code&gt;getter-keyword-clause­setter-keyword-clause­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-setter-keyword-block  → &lt;code&gt;{­&lt;/code&gt;setter-keyword-clause­getter-keyword-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;getter-keyword-clause  → attributes­opt­&lt;code&gt;get­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;setter-keyword-clause  → attributes­opt­&lt;code&gt;set­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;willSet-didSet-block  → &lt;code&gt;{­&lt;/code&gt;willSet-clause­didSet-clause­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;willSet-didSet-block  → &lt;code&gt;{­&lt;/code&gt;didSet-clause­willSet-clause­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;willSet-clause  → attributes­opt­&lt;code&gt;willSet­&lt;/code&gt;setter-name­opt­code-block­&lt;/p&gt;
&lt;p&gt;didSet-clause  → attributes­opt­&lt;code&gt;didSet­&lt;/code&gt;setter-name­opt­code-block­&lt;/p&gt;
&lt;p&gt;Grammar of a type alias declaration &lt;/p&gt;
&lt;p&gt;typealias-declaration  → typealias-head­typealias-assignment­&lt;/p&gt;
&lt;p&gt;typealias-head  → attributes­opt­access-level-modifier­opt­&lt;code&gt;typealias­&lt;/code&gt;typealias-name­&lt;/p&gt;
&lt;p&gt;typealias-name  → identifier­&lt;/p&gt;
&lt;p&gt;typealias-assignment  → &lt;code&gt;=­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;Grammar of a function declaration &lt;/p&gt;
&lt;p&gt;function-declaration  → function-head­function-name­generic-parameter-clause­opt­function-signature­function-body­&lt;/p&gt;
&lt;p&gt;function-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;func­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;function-name  →  identifier­ operator­&lt;/p&gt;
&lt;p&gt;function-signature  → parameter-clauses­function-result­opt­&lt;/p&gt;
&lt;p&gt;function-result  → &lt;code&gt;-&amp;gt;­&lt;/code&gt;attributes­opt­type­&lt;/p&gt;
&lt;p&gt;function-body  → code-block­&lt;/p&gt;
&lt;p&gt;parameter-clauses  → parameter-clause­parameter-clauses­opt­&lt;/p&gt;
&lt;p&gt;parameter-clause  →  &lt;code&gt;(­``)­&lt;/code&gt; &lt;code&gt;(­&lt;/code&gt;parameter-list­&lt;code&gt;...­&lt;/code&gt;opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;parameter-list  →  parameter­ parameter­&lt;code&gt;,­&lt;/code&gt;parameter-list­&lt;/p&gt;
&lt;p&gt;parameter  → &lt;code&gt;inout­&lt;/code&gt;opt­&lt;code&gt;let­&lt;/code&gt;opt­&lt;code&gt;#­&lt;/code&gt;opt­external-parameter-name­opt­local-parameter-name­type-annotation­default-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;parameter  → &lt;code&gt;inout­&lt;/code&gt;opt­&lt;code&gt;var­``#­&lt;/code&gt;opt­external-parameter-name­opt­local-parameter-name­type-annotation­default-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;parameter  → attributes­opt­type­&lt;/p&gt;
&lt;p&gt;external-parameter-name  →  identifier­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;local-parameter-name  →  identifier­ &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;default-argument-clause  → &lt;code&gt;=­&lt;/code&gt;expression­&lt;/p&gt;
&lt;p&gt;Grammar of an enumeration declaration &lt;/p&gt;
&lt;p&gt;enum-declaration  → attributes­opt­access-level-modifier­opt­union-style-enum­&lt;/p&gt;
&lt;p&gt;enum-declaration  → attributes­opt­access-level-modifier­opt­raw-value-style-enum­&lt;/p&gt;
&lt;p&gt;union-style-enum  → &lt;code&gt;enum­&lt;/code&gt;enum-name­generic-parameter-clause­opt­type-inheritance-clause­opt­&lt;code&gt;{­&lt;/code&gt;union-style-enum-members­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;union-style-enum-members  → union-style-enum-member­union-style-enum-members­opt­&lt;/p&gt;
&lt;p&gt;union-style-enum-member  →  declaration­ union-style-enum-case-clause­&lt;/p&gt;
&lt;p&gt;union-style-enum-case-clause  → attributes­opt­&lt;code&gt;case­&lt;/code&gt;union-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;union-style-enum-case-list  →  union-style-enum-case­ union-style-enum-case­&lt;code&gt;,­&lt;/code&gt;union-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;union-style-enum-case  → enum-case-name­tuple-type­opt­&lt;/p&gt;
&lt;p&gt;enum-name  → identifier­&lt;/p&gt;
&lt;p&gt;enum-case-name  → identifier­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum  → &lt;code&gt;enum­&lt;/code&gt;enum-name­generic-parameter-clause­opt­type-inheritance-clause­&lt;code&gt;{­&lt;/code&gt;raw-value-style-enum-members­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-members  → raw-value-style-enum-member­raw-value-style-enum-members­opt­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-member  →  declaration­ raw-value-style-enum-case-clause­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-case-clause  → attributes­opt­&lt;code&gt;case­&lt;/code&gt;raw-value-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-case-list  →  raw-value-style-enum-case­ raw-value-style-enum-case­&lt;code&gt;,­&lt;/code&gt;raw-value-style-enum-case-list­&lt;/p&gt;
&lt;p&gt;raw-value-style-enum-case  → enum-case-name­raw-value-assignment­opt­&lt;/p&gt;
&lt;p&gt;raw-value-assignment  → &lt;code&gt;=­&lt;/code&gt;raw-value-literal­&lt;/p&gt;
&lt;p&gt;raw-value-literal  →  numeric-literal­ string-literal­ boolean-literal­&lt;/p&gt;
&lt;p&gt;Grammar of a structure declaration &lt;/p&gt;
&lt;p&gt;struct-declaration  → attributes­opt­access-level-modifier­opt­&lt;code&gt;struct­&lt;/code&gt;struct-name­generic-parameter-clause­opt­type-inheritance-clause­opt­struct-body­&lt;/p&gt;
&lt;p&gt;struct-name  → identifier­&lt;/p&gt;
&lt;p&gt;struct-body  → &lt;code&gt;{­&lt;/code&gt;declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a class declaration &lt;/p&gt;
&lt;p&gt;class-declaration  → attributes­opt­access-level-modifier­opt­&lt;code&gt;class­&lt;/code&gt;class-name­generic-parameter-clause­opt­type-inheritance-clause­opt­class-body­&lt;/p&gt;
&lt;p&gt;class-name  → identifier­&lt;/p&gt;
&lt;p&gt;class-body  → &lt;code&gt;{­&lt;/code&gt;declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a protocol declaration &lt;/p&gt;
&lt;p&gt;protocol-declaration  → attributes­opt­access-level-modifier­opt­&lt;code&gt;protocol­&lt;/code&gt;protocol-name­type-inheritance-clause­opt­protocol-body­&lt;/p&gt;
&lt;p&gt;protocol-name  → identifier­&lt;/p&gt;
&lt;p&gt;protocol-body  → &lt;code&gt;{­&lt;/code&gt;protocol-member-declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-property-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-method-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-initializer-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-subscript-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declaration  → protocol-associated-type-declaration­&lt;/p&gt;
&lt;p&gt;protocol-member-declarations  → protocol-member-declaration­protocol-member-declarations­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a protocol property declaration &lt;/p&gt;
&lt;p&gt;protocol-property-declaration  → variable-declaration-head­variable-name­type-annotation­getter-setter-keyword-block­&lt;/p&gt;
&lt;p&gt;Grammar of a protocol method declaration &lt;/p&gt;
&lt;p&gt;protocol-method-declaration  → function-head­function-name­generic-parameter-clause­opt­function-signature­&lt;/p&gt;
&lt;p&gt;Grammar of a protocol initializer declaration &lt;/p&gt;
&lt;p&gt;protocol-initializer-declaration  → initializer-head­generic-parameter-clause­opt­parameter-clause­&lt;/p&gt;
&lt;p&gt;Grammar of a protocol subscript declaration &lt;/p&gt;
&lt;p&gt;protocol-subscript-declaration  → subscript-head­subscript-result­getter-setter-keyword-block­&lt;/p&gt;
&lt;p&gt;Grammar of a protocol associated type declaration &lt;/p&gt;
&lt;p&gt;protocol-associated-type-declaration  → typealias-head­type-inheritance-clause­opt­typealias-assignment­opt­&lt;/p&gt;
&lt;p&gt;Grammar of an initializer declaration &lt;/p&gt;
&lt;p&gt;initializer-declaration  → initializer-head­generic-parameter-clause­opt­parameter-clause­initializer-body­&lt;/p&gt;
&lt;p&gt;initializer-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;init­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;initializer-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;init­``?­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;initializer-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;init­``!­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;initializer-body  → code-block­&lt;/p&gt;
&lt;p&gt;Grammar of a deinitializer declaration &lt;/p&gt;
&lt;p&gt;deinitializer-declaration  → attributes­opt­&lt;code&gt;deinit­&lt;/code&gt;code-block­&lt;/p&gt;
&lt;p&gt;Grammar of an extension declaration &lt;/p&gt;
&lt;p&gt;extension-declaration  → access-level-modifier­opt­&lt;code&gt;extension­&lt;/code&gt;type-identifier­type-inheritance-clause­opt­extension-body­&lt;/p&gt;
&lt;p&gt;extension-body  → &lt;code&gt;{­&lt;/code&gt;declarations­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a subscript declaration &lt;/p&gt;
&lt;p&gt;subscript-declaration  → subscript-head­subscript-result­code-block­&lt;/p&gt;
&lt;p&gt;subscript-declaration  → subscript-head­subscript-result­getter-setter-block­&lt;/p&gt;
&lt;p&gt;subscript-declaration  → subscript-head­subscript-result­getter-setter-keyword-block­&lt;/p&gt;
&lt;p&gt;subscript-head  → attributes­opt­declaration-modifiers­opt­&lt;code&gt;subscript­&lt;/code&gt;parameter-clause­&lt;/p&gt;
&lt;p&gt;subscript-result  → &lt;code&gt;-&amp;gt;­&lt;/code&gt;attributes­opt­type­&lt;/p&gt;
&lt;p&gt;Grammar of an operator declaration &lt;/p&gt;
&lt;p&gt;operator-declaration  →  prefix-operator-declaration­ postfix-operator-declaration­ infix-operator-declaration­&lt;/p&gt;
&lt;p&gt;prefix-operator-declaration  → &lt;code&gt;prefix­``operator­&lt;/code&gt;operator­&lt;code&gt;{­``}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;postfix-operator-declaration  → &lt;code&gt;postfix­``operator­&lt;/code&gt;operator­&lt;code&gt;{­``}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;infix-operator-declaration  → &lt;code&gt;infix­``operator­&lt;/code&gt;operator­&lt;code&gt;{­&lt;/code&gt;infix-operator-attributes­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;infix-operator-attributes  → precedence-clause­opt­associativity-clause­opt­&lt;/p&gt;
&lt;p&gt;precedence-clause  → &lt;code&gt;precedence­&lt;/code&gt;precedence-level­&lt;/p&gt;
&lt;p&gt;precedence-level  → A decimal integer between 0 and 255, inclusive&lt;/p&gt;
&lt;p&gt;associativity-clause  → &lt;code&gt;associativity­&lt;/code&gt;associativity­&lt;/p&gt;
&lt;p&gt;associativity  →  &lt;code&gt;left­&lt;/code&gt; &lt;code&gt;right­&lt;/code&gt; &lt;code&gt;none­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a declaration modifier &lt;/p&gt;
&lt;p&gt;declaration-modifier  →  &lt;code&gt;class­&lt;/code&gt; &lt;code&gt;convenience­&lt;/code&gt; &lt;code&gt;dynamic­&lt;/code&gt; &lt;code&gt;final­&lt;/code&gt; &lt;code&gt;infix­&lt;/code&gt; &lt;code&gt;lazy­&lt;/code&gt; &lt;code&gt;mutating­&lt;/code&gt; &lt;code&gt;nonmutating­&lt;/code&gt; &lt;code&gt;optional­&lt;/code&gt; &lt;code&gt;override­&lt;/code&gt; &lt;code&gt;postfix­&lt;/code&gt; &lt;code&gt;prefix­&lt;/code&gt; &lt;code&gt;required­&lt;/code&gt; &lt;code&gt;static­&lt;/code&gt; &lt;code&gt;unowned­&lt;/code&gt; &lt;code&gt;unowned­``(­``safe­``)­&lt;/code&gt; &lt;code&gt;unowned­``(­``unsafe­``)­&lt;/code&gt; &lt;code&gt;weak­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;declaration-modifier  → access-level-modifier­&lt;/p&gt;
&lt;p&gt;declaration-modifiers  → declaration-modifier­declaration-modifiers­opt­&lt;/p&gt;
&lt;p&gt;access-level-modifier  →  &lt;code&gt;internal­&lt;/code&gt; &lt;code&gt;internal­``(­``set­``)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;access-level-modifier  →  &lt;code&gt;private­&lt;/code&gt; &lt;code&gt;private­``(­``set­``)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;access-level-modifier  →  &lt;code&gt;public­&lt;/code&gt; &lt;code&gt;public­``(­``set­``)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;access-level-modifiers  → access-level-modifier­access-level-modifiers­opt­&lt;/p&gt;
&lt;h3&gt;Patterns&lt;/h3&gt;
&lt;p&gt;Grammar of a pattern &lt;/p&gt;
&lt;p&gt;pattern  → wildcard-pattern­type-annotation­opt­&lt;/p&gt;
&lt;p&gt;pattern  → identifier-pattern­type-annotation­opt­&lt;/p&gt;
&lt;p&gt;pattern  → value-binding-pattern­&lt;/p&gt;
&lt;p&gt;pattern  → tuple-pattern­type-annotation­opt­&lt;/p&gt;
&lt;p&gt;pattern  → enum-case-pattern­&lt;/p&gt;
&lt;p&gt;pattern  → type-casting-pattern­&lt;/p&gt;
&lt;p&gt;pattern  → expression-pattern­&lt;/p&gt;
&lt;p&gt;Grammar of a wildcard pattern &lt;/p&gt;
&lt;p&gt;wildcard-pattern  → &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of an identifier pattern &lt;/p&gt;
&lt;p&gt;identifier-pattern  → identifier­&lt;/p&gt;
&lt;p&gt;Grammar of a value-binding pattern &lt;/p&gt;
&lt;p&gt;value-binding-pattern  →  &lt;code&gt;var­&lt;/code&gt;pattern­ &lt;code&gt;let­&lt;/code&gt;pattern­&lt;/p&gt;
&lt;p&gt;Grammar of a tuple pattern &lt;/p&gt;
&lt;p&gt;tuple-pattern  → &lt;code&gt;(­&lt;/code&gt;tuple-pattern-element-list­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;tuple-pattern-element-list  →  tuple-pattern-element­ tuple-pattern-element­&lt;code&gt;,­&lt;/code&gt;tuple-pattern-element-list­&lt;/p&gt;
&lt;p&gt;tuple-pattern-element  → pattern­&lt;/p&gt;
&lt;p&gt;Grammar of an enumeration case pattern &lt;/p&gt;
&lt;p&gt;enum-case-pattern  → type-identifier­opt­&lt;code&gt;.­&lt;/code&gt;enum-case-name­tuple-pattern­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a type casting pattern &lt;/p&gt;
&lt;p&gt;type-casting-pattern  →  is-pattern­ as-pattern­&lt;/p&gt;
&lt;p&gt;is-pattern  → &lt;code&gt;is­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;as-pattern  → pattern­&lt;code&gt;as­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;Grammar of an expression pattern &lt;/p&gt;
&lt;p&gt;expression-pattern  → expression­&lt;/p&gt;
&lt;h3&gt;Attributes&lt;/h3&gt;
&lt;p&gt;Grammar of an attribute &lt;/p&gt;
&lt;p&gt;attribute  → &lt;code&gt;@­&lt;/code&gt;attribute-name­attribute-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;attribute-name  → identifier­&lt;/p&gt;
&lt;p&gt;attribute-argument-clause  → &lt;code&gt;(­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;attributes  → attribute­attributes­opt­&lt;/p&gt;
&lt;p&gt;balanced-tokens  → balanced-token­balanced-tokens­opt­&lt;/p&gt;
&lt;p&gt;balanced-token  → &lt;code&gt;(­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;balanced-token  → &lt;code&gt;[­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;balanced-token  → &lt;code&gt;{­&lt;/code&gt;balanced-tokens­opt­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;balanced-token  → Any identifier, keyword, literal, or operator&lt;/p&gt;
&lt;p&gt;balanced-token  → Any punctuation except &lt;code&gt;(­&lt;/code&gt;, &lt;code&gt;)­&lt;/code&gt;, &lt;code&gt;[­&lt;/code&gt;, &lt;code&gt;]­&lt;/code&gt;, &lt;code&gt;{­&lt;/code&gt;, or &lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;h3&gt;Expressions&lt;/h3&gt;
&lt;p&gt;Grammar of an expression &lt;/p&gt;
&lt;p&gt;expression  → prefix-expression­binary-expressions­opt­&lt;/p&gt;
&lt;p&gt;expression-list  →  expression­ expression­&lt;code&gt;,­&lt;/code&gt;expression-list­&lt;/p&gt;
&lt;p&gt;Grammar of a prefix expression &lt;/p&gt;
&lt;p&gt;prefix-expression  → prefix-operator­opt­postfix-expression­&lt;/p&gt;
&lt;p&gt;prefix-expression  → in-out-expression­&lt;/p&gt;
&lt;p&gt;in-out-expression  → &lt;code&gt;&amp;amp;­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;p&gt;Grammar of a binary expression &lt;/p&gt;
&lt;p&gt;binary-expression  → binary-operator­prefix-expression­&lt;/p&gt;
&lt;p&gt;binary-expression  → assignment-operator­prefix-expression­&lt;/p&gt;
&lt;p&gt;binary-expression  → conditional-operator­prefix-expression­&lt;/p&gt;
&lt;p&gt;binary-expression  → type-casting-operator­&lt;/p&gt;
&lt;p&gt;binary-expressions  → binary-expression­binary-expressions­opt­&lt;/p&gt;
&lt;p&gt;Grammar of an assignment operator &lt;/p&gt;
&lt;p&gt;assignment-operator  → &lt;code&gt;=­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a conditional operator &lt;/p&gt;
&lt;p&gt;conditional-operator  → &lt;code&gt;?­&lt;/code&gt;expression­&lt;code&gt;:­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a type-casting operator &lt;/p&gt;
&lt;p&gt;type-casting-operator  → &lt;code&gt;is­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;type-casting-operator  → &lt;code&gt;as­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;type-casting-operator  → &lt;code&gt;as­``?­&lt;/code&gt;type­&lt;/p&gt;
&lt;p&gt;Grammar of a primary expression &lt;/p&gt;
&lt;p&gt;primary-expression  → identifier­generic-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;primary-expression  → literal-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → self-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → superclass-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → closure-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → parenthesized-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → implicit-member-expression­&lt;/p&gt;
&lt;p&gt;primary-expression  → wildcard-expression­&lt;/p&gt;
&lt;p&gt;Grammar of a literal expression &lt;/p&gt;
&lt;p&gt;literal-expression  → literal­&lt;/p&gt;
&lt;p&gt;literal-expression  →  array-literal­ dictionary-literal­&lt;/p&gt;
&lt;p&gt;literal-expression  →  &lt;code&gt;__FILE__­&lt;/code&gt; &lt;code&gt;__LINE__­&lt;/code&gt; &lt;code&gt;__COLUMN__­&lt;/code&gt; &lt;code&gt;__FUNCTION__­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;array-literal  → &lt;code&gt;[­&lt;/code&gt;array-literal-items­opt­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;array-literal-items  →  array-literal-item­&lt;code&gt;,­&lt;/code&gt;opt­ array-literal-item­&lt;code&gt;,­&lt;/code&gt;array-literal-items­&lt;/p&gt;
&lt;p&gt;array-literal-item  → expression­&lt;/p&gt;
&lt;p&gt;dictionary-literal  →  &lt;code&gt;[­&lt;/code&gt;dictionary-literal-items­&lt;code&gt;]­&lt;/code&gt; &lt;code&gt;[­``:­``]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;dictionary-literal-items  →  dictionary-literal-item­&lt;code&gt;,­&lt;/code&gt;opt­ dictionary-literal-item­&lt;code&gt;,­&lt;/code&gt;dictionary-literal-items­&lt;/p&gt;
&lt;p&gt;dictionary-literal-item  → expression­&lt;code&gt;:­&lt;/code&gt;expression­&lt;/p&gt;
&lt;p&gt;Grammar of a self expression &lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­``.­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­``[­&lt;/code&gt;expression­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;self-expression  → &lt;code&gt;self­``.­``init­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a superclass expression &lt;/p&gt;
&lt;p&gt;superclass-expression  →  superclass-method-expression­ superclass-subscript-expression­ superclass-initializer-expression­&lt;/p&gt;
&lt;p&gt;superclass-method-expression  → &lt;code&gt;super­``.­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;p&gt;superclass-subscript-expression  → &lt;code&gt;super­``[­&lt;/code&gt;expression­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;superclass-initializer-expression  → &lt;code&gt;super­``.­``init­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a closure expression &lt;/p&gt;
&lt;p&gt;closure-expression  → &lt;code&gt;{­&lt;/code&gt;closure-signature­opt­statements­&lt;code&gt;}­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → parameter-clause­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → identifier-list­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → capture-list­parameter-clause­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → capture-list­identifier-list­function-result­opt­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;closure-signature  → capture-list­&lt;code&gt;in­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;capture-list  → &lt;code&gt;[­&lt;/code&gt;capture-specifier­expression­&lt;code&gt;]­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;capture-specifier  →  &lt;code&gt;weak­&lt;/code&gt; &lt;code&gt;unowned­&lt;/code&gt; &lt;code&gt;unowned(safe)­&lt;/code&gt; &lt;code&gt;unowned(unsafe)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a implicit member expression &lt;/p&gt;
&lt;p&gt;implicit-member-expression  → &lt;code&gt;.­&lt;/code&gt;identifier­&lt;/p&gt;
&lt;p&gt;Grammar of a parenthesized expression &lt;/p&gt;
&lt;p&gt;parenthesized-expression  → &lt;code&gt;(­&lt;/code&gt;expression-element-list­opt­&lt;code&gt;)­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;expression-element-list  →  expression-element­ expression-element­&lt;code&gt;,­&lt;/code&gt;expression-element-list­&lt;/p&gt;
&lt;p&gt;expression-element  →  expression­ identifier­&lt;code&gt;:­&lt;/code&gt;expression­&lt;/p&gt;
&lt;p&gt;Grammar of a wildcard expression &lt;/p&gt;
&lt;p&gt;wildcard-expression  → &lt;code&gt;_­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a postfix expression &lt;/p&gt;
&lt;p&gt;postfix-expression  → primary-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → postfix-expression­postfix-operator­&lt;/p&gt;
&lt;p&gt;postfix-expression  → function-call-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → initializer-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → explicit-member-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → postfix-self-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → dynamic-type-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → subscript-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → forced-value-expression­&lt;/p&gt;
&lt;p&gt;postfix-expression  → optional-chaining-expression­&lt;/p&gt;
&lt;p&gt;Grammar of a function call expression &lt;/p&gt;
&lt;p&gt;function-call-expression  → postfix-expression­parenthesized-expression­&lt;/p&gt;
&lt;p&gt;function-call-expression  → postfix-expression­parenthesized-expression­opt­trailing-closure­&lt;/p&gt;
&lt;p&gt;trailing-closure  → closure-expression­&lt;/p&gt;
&lt;p&gt;Grammar of an initializer expression &lt;/p&gt;
&lt;p&gt;initializer-expression  → postfix-expression­&lt;code&gt;.­``init­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of an explicit member expression &lt;/p&gt;
&lt;p&gt;explicit-member-expression  → postfix-expression­&lt;code&gt;.­&lt;/code&gt;decimal-digits­&lt;/p&gt;
&lt;p&gt;explicit-member-expression  → postfix-expression­&lt;code&gt;.­&lt;/code&gt;identifier­generic-argument-clause­opt­&lt;/p&gt;
&lt;p&gt;Grammar of a self expression &lt;/p&gt;
&lt;p&gt;postfix-self-expression  → postfix-expression­&lt;code&gt;.­``self­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a dynamic type expression &lt;/p&gt;
&lt;p&gt;dynamic-type-expression  → postfix-expression­&lt;code&gt;.­``dynamicType­&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Grammar of a subscript expression &lt;/p&gt;
&lt;p&gt;subscript-expression  → postfix-expression­&lt;code&gt;[­&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;Document Revision History&lt;/h2&gt;
&lt;p&gt;This table describes the changes to &lt;em&gt;The Swift Programming Language&lt;/em&gt;. &lt;/p&gt;
&lt;p&gt;Date &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;p&gt;Notes   &lt;/p&gt;
&lt;p&gt;---|---  &lt;/p&gt;
&lt;p&gt;2014-10-16 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Added a full guide to Failable Initializers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a description of Failable Initializer Requirements for protocols. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Constants and variables of type &lt;code&gt;Any&lt;/code&gt; can now contain function instances. Updated the example for Any to show how to check for and cast to a function type within a &lt;code&gt;switch&lt;/code&gt; statement.   &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;2014-09-15 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Enumerations with raw values now have a &lt;code&gt;rawValue&lt;/code&gt; property rather than a &lt;code&gt;toRaw()&lt;/code&gt; method and a failable initializer with a &lt;code&gt;rawValue&lt;/code&gt; parameter rather than a &lt;code&gt;fromRaw()&lt;/code&gt; method. For more information, see Raw Values and Enumerations with Cases of a Raw-Value Type.   &lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;2014-09-09 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Added a new reference section about Failable Initializers, which can trigger initialization failure. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Custom operators can now contain the &lt;code&gt;?&lt;/code&gt; character. Updated the Operators reference to describe the revised rules. Removed a duplicate description of the valid set of operator characters from Custom Operators.   &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;2014-08-18 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Initializer Requirements in protocols. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Class-Only Protocols. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Assertions can now use string interpolation. Removed a note to the contrary. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the Concatenating Strings and Characters section to reflect the fact that &lt;code&gt;String&lt;/code&gt; and &lt;code&gt;Character&lt;/code&gt; values can no longer be combined with the addition operator (&lt;code&gt;+&lt;/code&gt;) or addition assignment operator (&lt;code&gt;+=&lt;/code&gt;). These operators are now used only with &lt;code&gt;String&lt;/code&gt; values. Use the &lt;code&gt;String&lt;/code&gt; type’s &lt;code&gt;append&lt;/code&gt; method to append a single &lt;code&gt;Character&lt;/code&gt; value onto the end of a string. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added information about the &lt;code&gt;availability&lt;/code&gt; attribute to the Declaration Attributes section.   &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;2014-08-04 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Optionals no longer implicitly evaluate to &lt;code&gt;true&lt;/code&gt; when they have a value and &lt;code&gt;false&lt;/code&gt; when they do not, to avoid confusion when working with optional &lt;code&gt;Bool&lt;/code&gt; values. Instead, make an explicit check against &lt;code&gt;nil&lt;/code&gt; with the &lt;code&gt;==&lt;/code&gt; or &lt;code&gt;!=&lt;/code&gt; operators to find out if an optional contains a value. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Swift now has a Nil Coalescing Operator (&lt;code&gt;a ?? b&lt;/code&gt;), which unwraps an optional’s value if it exists, or returns a default value if the optional is &lt;code&gt;nil&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated and expanded the Comparing Strings section to reflect and demonstrate that string and character comparison and prefix / suffix comparison are now based on Unicode canonical equivalence of extended grapheme clusters. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;You can now try to set a property’s value, assign to a subscript, or call a mutating method or operator through Optional Chaining. The information about Accessing Properties Through Optional Chaining has been updated accordingly, and the examples of checking for method call success in Calling Methods Through Optional Chaining have been expanded to show how to check for property setting success. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Accessing Subscripts of Optional Type through optional chaining. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the Accessing and Modifying an Array section to note that you can no longer append a single item to an array with the &lt;code&gt;+=&lt;/code&gt; operator. Instead, use the &lt;code&gt;append&lt;/code&gt; method, or append a single-item array with the &lt;code&gt;+=&lt;/code&gt; operator. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a note that the start value &lt;code&gt;a&lt;/code&gt; for the Range Operators &lt;code&gt;a...b&lt;/code&gt; and &lt;code&gt;a..&amp;lt;b&lt;/code&gt; must not be greater than the end value &lt;code&gt;b&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Rewrote the Inheritance chapter to remove its introductory coverage of initializer overrides. This chapter now focuses more on the addition of new functionality in a subclass, and the modification of existing functionality with overrides. The chapter’s example of Overriding Property Getters and Setters has been rewritten to show how to override a &lt;code&gt;description&lt;/code&gt; property. (The examples of modifying an inherited property’s default value in a subclass initializer have been moved to the Initialization chapter.) &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the Initializer Inheritance and Overriding section to note that overrides of a designated initializer must now be marked with the &lt;code&gt;override&lt;/code&gt; modifier. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the Required Initializers section to note that the &lt;code&gt;required&lt;/code&gt; modifier is now written before every subclass implementation of a required initializer, and that the requirements for required initializers can now be satisfied by automatically inherited initializers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Infix Operator Functions no longer require the &lt;code&gt;@infix&lt;/code&gt; attribute. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;@prefix&lt;/code&gt; and &lt;code&gt;@postfix&lt;/code&gt; attributes for Prefix and Postfix Operators have been replaced by &lt;code&gt;prefix&lt;/code&gt; and &lt;code&gt;postfix&lt;/code&gt; declaration modifiers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a note about the order in which Prefix and Postfix Operators are applied when both a prefix and a postfix operator are applied to the same operand. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Operator functions for Compound Assignment Operators no longer use the &lt;code&gt;@assignment&lt;/code&gt; attribute when defining the function. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The order in which modifiers are specified when defining Custom Operators has changed. You now write &lt;code&gt;prefix operator&lt;/code&gt; rather than &lt;code&gt;operator prefix&lt;/code&gt;, for example. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added information about the &lt;code&gt;dynamic&lt;/code&gt; declaration modifier in Declaration Modifiers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added information about how type inference works with Literals. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added more information about Curried Functions.   &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;2014-07-21 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Added a new chapter about Access Control. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the Strings and Characters chapter to reflect the fact that Swift’s &lt;code&gt;Character&lt;/code&gt; type now represents a single Unicode extended grapheme cluster. Includes a new section on Extended Grapheme Clusters and more information about Unicode Scalars and Comparing Strings. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the String Literals section to note that Unicode scalars inside string literals are now written as &lt;code&gt;\u{n}&lt;/code&gt;, where &lt;code&gt;n&lt;/code&gt; is between one and eight hexadecimal digits. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;NSString&lt;/code&gt; &lt;code&gt;length&lt;/code&gt; property is now mapped onto Swift’s native &lt;code&gt;String&lt;/code&gt; type as &lt;code&gt;utf16Count&lt;/code&gt;, not &lt;code&gt;utf16count&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Swift’s native &lt;code&gt;String&lt;/code&gt; type no longer has an &lt;code&gt;uppercaseString&lt;/code&gt; or &lt;code&gt;lowercaseString&lt;/code&gt; property. The corresponding section in Strings and Characters has been removed, and various code examples have been updated. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Initializer Parameters Without External Names. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Required Initializers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Optional Tuple Return Types. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the Type Annotations section to note that multiple related variables can be defined on a single line with one type annotation. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The &lt;code&gt;@optional&lt;/code&gt;, &lt;code&gt;@lazy&lt;/code&gt;, &lt;code&gt;@final&lt;/code&gt;, and &lt;code&gt;@required&lt;/code&gt; attributes are now the &lt;code&gt;optional&lt;/code&gt;, &lt;code&gt;lazy&lt;/code&gt;, &lt;code&gt;final&lt;/code&gt;, and &lt;code&gt;required&lt;/code&gt; Declaration Modifiers. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the entire book to refer to &lt;code&gt;..&amp;lt;&lt;/code&gt; as the Half-Open Range Operator (rather than the “half-closed range operator”). &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the Accessing and Modifying a Dictionary section to note that &lt;code&gt;Dictionary&lt;/code&gt; now has a Boolean &lt;code&gt;isEmpty&lt;/code&gt; property. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Clarified the full list of characters that can be used when defining Custom Operators. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;nil&lt;/code&gt; and the Booleans &lt;code&gt;true&lt;/code&gt; and &lt;code&gt;false&lt;/code&gt; are now Literals.   &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;2014-07-07 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Swift’s &lt;code&gt;Array&lt;/code&gt; type now has full value semantics. Updated the information about Mutability of Collections and Arrays to reflect the new approach. Also clarified the Assignment and Copy Behavior for Strings, Arrays, and Dictionaries. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Array Type Shorthand Syntax is now written as &lt;code&gt;[SomeType]&lt;/code&gt; rather than &lt;code&gt;SomeType[]&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Dictionary Type Shorthand Syntax, which is written as &lt;code&gt;[KeyType: ValueType]&lt;/code&gt;. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added a new section about Hash Values for Dictionary Key Types. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Examples of Closure Expressions now use the global &lt;code&gt;sorted&lt;/code&gt; function rather than the global &lt;code&gt;sort&lt;/code&gt; function, to reflect the new array value semantics. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated the information about Memberwise Initializers for Structure Types to clarify that the memberwise structure initializer is made available even if a structure’s stored properties do not have default values. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updated to &lt;code&gt;..&amp;lt;&lt;/code&gt; rather than &lt;code&gt;..&lt;/code&gt; for the Half-Open Range Operator. &lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Added an example of Extending a Generic Type.   &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;2014-06-02 &lt;/p&gt;
&lt;p&gt;| &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;New document that describes Swift, Apple’s new programming language for building iOS and OS X apps. &lt;/li&gt;
&lt;/ol&gt;&lt;/div&gt;</description><category>Swift</category><guid>http://muxuezi.github.io/posts/the-swift-programming-language-en.html</guid><pubDate>Sun, 24 Aug 2014 05:44:10 GMT</pubDate></item></channel></rss>